# 2026-09-06

## 人修罗支线（彩蛋后期支线，提前施工）文本编辑工作

用户提供了完整大纲（文天→阿卡→混沌王/虎妙），本次完成全部 data 文本编辑，Flash 元件由用户自行添加。

### 新增/修改文件
- `data/items/消耗品_药剂.xml`：新增「全属性抗性药剂」（图标借净化药剂）。效果：10秒内七抗各+10（resistanceBuff）+ 七抗 percent×1.2（普通 buff 词条对 `魔法抗性.电/热/冷/波/蚀/毒/冲` 叶子路径）+ 即时回复600HP。percent 走叶子路径是新增词条路径，未实机回归，status=runtime-test-pending，balanceMode=exception。
- `data/task/bonus_tasks.json`：新增 1000081（文天发→找阿卡，奖励 全属性抗性药剂×20+复活币×3，get_requirements=[1000011,77]，chain=彩蛋#81）、1000082（阿卡发→打"挑战混沌王#简单"→回阿卡，奖励含大型拳套×1，chain=彩蛋#82）。
- `data/task/text/bonus_text.json`：上述两任务的完整对话（含虎妙告别假真空衰变/三蝶段、阿卡人外/不净猫段）。
- `data/task/challenge_tasks.json`：新增 130006「挑战混沌王」（get_npc=混沌王，finish_npc=阿卡，finish_requirements=["挑战混沌王#简单"]，chain=挑战#6，get_requirements=[1000081]）。
- `data/task/text/challenge_text.json`：130006 文本（混沌王开场台词 + 阿卡"干得不错"结算）。
- `data/stages/副本任务/挑战混沌王.xml`（新）+ `__list__.xml` 注册。背景 ciyuanta_魔神图专用.swf（锡蒙利Lv16同款），玩家 (100,600)，混沌王 兵种348@ (600,600) 间距约500。
- `data/dialogues/npc_dialogue_人修罗支线.xml`（新，UTF-8 with BOM）+ list.xml 注册：阿卡（1条占位，用户后续手写扩展）、虎妙（6条，按用户大纲）、混沌王（4条，按"真女神转生3 人修罗/Demi-fiend"设定：旋涡界、非人类、力量法则；heeho君/杰克霜精已是 SMT 系彩蛋）。

### 关键实现决策
- 定时事件用波次计时实现：波次0(30s 混沌王)→波次1(30s 空)→波次2(120s 空)→波次3(无限)。WaveFinished 事件在30/60/180秒触发虎妙入场(兵种330, IsHostile=false, Event Enemy 动作)/撤退/再入场对话。UnitDeath(混沌王, InstanceName 绑定) → 结束对话 + StageProgress Finish 立即结算，保证提前击杀直接收尾。
- XML 无法移除场上单位（关卡回调无删除函数），60秒"撤退"只是剧情对话+虎妙停手，观感需实机验证。
- 空 EnemyGroup 必须写 `<EnemyGroup></EnemyGroup>`（StageInfo.parseWaveInfo 直接访问 .Enemy，缺节点会崩）。
- finish_requirements 统一用 `挑战混沌王#简单`；无"足球"物品，拳套用现有「大型拳套」。

### 后续变更（同日 23:17）
- 用户撤回了 challenge_tasks.json / challenge_text.json 的 130006 修改（challenge_tasks 不允许改），无需恢复。
- 副本任务改挂在 mercenary_tasks.json：新增 20071「挑战混沌王」（get_npc=混沌王，finish_npc=阿卡，finish_requirements=["挑战混沌王#地狱"]，奖励 经验值#3000000 = 挑战战斗天才 500万的60%，参考武装JK的20053写法）。
- mercenary_text.json 补 $GEM_PK_HCW_GET（混沌王挑战前3句）/ $GEM_PK_HCW_FINISH（阿卡"干得不错"）。
- 注意：bonus_tasks.json 的 1000082 仍引用「挑战混沌王#简单」，与 mercenary 20071 的地狱难度并存，均指向同一关卡。
