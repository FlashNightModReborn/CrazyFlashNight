# 理发店目录图标网格与共享外观服务动效（2026-09-08）

**文档角色**：建角 / 理发店 / 整形三面板 UI 取长补短列车的施工与验证记录。覆盖：服务面板共享扫描光与微交互基线、整形反馈补强、理发店目录图标网格 / 性别页签 / 当前发型卡 / 变更摘要 / 全身与胸像预览切换、hairdresser snapshot 可选 `portrait` 投影。协议 canonical 仍以 [AS2 → Web 迁移护栏](../agentsDoc/as2-web-panel-migration.md) 为准；共享组件结构以[医务室整形 doc](医务室整形-Web面板与外观共享-2026-09-08.md) §2 为基线，理发店侧现状描述由本文件接管。

**最后核对代码基线**：release source `1d03e77cb2666a4686e41ab4a647e180d2e5d0bc`（tag `runtime-build-v2/20260909-hairdresser-grid-v1`），部署与合并 head `c595bc2cd261342d37cd461cf5ff08ee62b9c76a`。本列车已完成双 signer / 双故障域共识、40/40 production policy、原子 promotion 与 post-promotion Audit，当前为 `promoted / FIELD_REVALIDATION_PENDING`；发布身份与过程见 §7。

## 1. 范围与决策

- **P0 服务面板共享动效**：换装扫描光从建角 `ccPreviewSwap` / 角色构筑 `cbDollSwap` 同签名提取进 `appearance-service.css`（`.appearance-service-swap-flash` + `appearanceServicePreviewSwap`，服务面板 canonical 版本，reduced-motion 收束）；同文件新增按钮 / 输入 / 性别卡的微交互过渡基线。建角与角色构筑两处既有拷贝刻意不动（不同文档上下文且均已人类验收），本文件起服务面板以共享块为唯一真源。
- **整形反馈补强**：接入 BootstrapAudio 语义音效（select/activate/success/unknown/rejected/illegal/back）；业务错误（如 K 点不足）改走共享 `appearance-service-error` + `role="alert"` 红行并联动面板 `data-state="error"`，中性提示仍走 hint 行；身高缩放 canvas 补 `transform` 过渡对齐建角手感；性别 / 身高变更触发扫光。
- **P1 理发店目录**：manifest PNG 图标网格（紧凑 36×36 默认 / 完整卡切换，独立 localStorage key `cf7.hairdresser.catalog-density`）；名称剥前缀（「发型-男式-黑暴走头」→「黑暴走头 · 男式发型」）；列数感知键盘导航（实测列数）+ 防跳动滚动；`aria-posinset/aria-setsize`；紧凑格悬浮全名（生产 overlay 走 PanelTooltip，无模块环境退化 title）；目录顶部「当前发型」槽卡；底栏变更摘要「当前：X → 试戴：Y」。
- **P2 性别页签**：全部 / 男式 / 女式，性别仅从 catalog name 前缀确定性解析，光头（中性）随每个页签显示；纯展示分组——不排序、不去重、不改 wire 顺序与重复行；选中试戴不因切页签丢失。计数：全部 77 / 男式 52 / 女式 26。
- **P2 全身预览切换**：脸部特写（默认，原对话 rig 路径逐字保留）/ 全身（共享 `CharacterAppearancePreview`，battle rig、七站姿合并取景、maxScale 12、zoom 1.0 / margin 12、animate:false）。理发店按试戴场景**不应用头盔遮发**（调用点覆盖共享组件的 `hairHidden`，注释标明场景化偏离，整形 / 角色构筑语义不变）。`portrait` 缺失（旧 asLoader）时切换控件完全隐藏、静默胸像。

## 2. 协议变化（可选 `portrait`，偏斜安全）

hairdresser snapshot 成功回包新增可选顶层字段：

```
portrait: { equipment: { "<槽名>": "<物品名>", ... }, hair: "<发型identifier|空串>", face: "<脸型|空串>" }
```

- 槽名 ∈ 11 槽（与整形 SLOTS 同集，两侧刻意平行实现不交叉引用）；只出现非空槽；actor 不可用时 equipment 为空对象、不新增失败分支，snapshot 既有成功语义零变化。
- C# `HairdresserTask` 校验：portrait 缺失放行（兼容旧 asLoader），存在则严格校验形状（对象结构、槽名白名单、hair/face 0..160、物品名 1..160），不合格 malformed_response。commit 路径不变。
- 偏斜矩阵：新 Core + 旧 asLoader → 无 portrait 正常放行；旧 Core + 新 asLoader → DeepClone 透传不校验，Web 照常消费。当前已部署 asLoader.swf 未含 portrait 时 Web 隐藏全身切换，即现阶段真机默认路径。
- 发型免费保持契约：`pricing_unsupported` 整目录拒用语义不动。

## 3. 契约更新（tools/run-hairdresser-harness.js）

- 删除「禁 gender filter/guess」正则禁令，替换为正向契约：名称前缀解析 helper、`['全部','男式','女式']` 页签、全部页签完整 77 原序；77 项 id 0..76 顺序、id 20/32 重复「平头」、禁 `sort/reverse/splice()` 空调用与去重、`pricing` 框架禁令全部原样保留。
- 胸像默认路径断言逐字保留；新增全身路径正向断言（portrait 门控、不遮发覆盖、双渲染器 animate:false、切换不重置试戴）。
- 浏览器旅程 48 → 58 项：图标渲染、密度记忆、页签计数与光头每签可见、切页签不丢试戴、列数感知键盘、当前卡、摘要出现/消失、无 portrait 切换控件不存在、有 portrait 全身渲染；对账故障矩阵原旅程全部保留。

## 4. 自动验证

| 门 | 结果 |
|---|---|
| AS2 TestLoader（真实 Flash CS6 编译执行，`scripts/run-hairdresser-tests.ps1`） | 39/39；blocks=1，32K retry=0，compiler 0/0，fresh trace/output |
| Host xUnit 全量（`launcher/tests/run_tests.ps1`，exact SDK 10.0.300） | 4849 通过 / 3 跳过 / 0 失败；HairdresserTaskTests 54/54（含 3 个新 portrait 用例，13 种畸形变异） |
| 理发店 harness（`run-hairdresser-harness.js`，Edge + playwright） | 静态通过；58/58 × 3 视口（1024×576 / 1366×768 / 1920×1080） |
| 整形 harness（`run-plastic-surgery-harness.js`） | 静态 18；72/72 × 3 视口 |
| Panel runtime / lazy closure / css bundle / panel contracts / doc governance | 41/41；passed；ok:true；errors:[]；[doc-governance] ok |

几何实测：1024×576 视口紧凑格 15 列 × 6 行，77 项一屏全显无滚动；全身预览人物主体占 canvas 高约 80%（offline 几何，不代替真机画面验收）。

## 5. 边界与未验收

- asLoader.swf 已经两次 CS6 发布（本列车初版与合并上游后重发，§6），真机可回传 `portrait`；合并后上游浮空修复（`cc56d2cd73`）的 `.as` 改动未进入 SWF，属下一列车。
- 真机待复验（机器门不代签）：紧凑格 hover tooltip 生产落点（PanelTooltip 路径 harness 只能覆盖 title 兜底）、portrait 发布后全身预览真机画面、性别页签与密度切换手感、整形音效与红行观感。
- 遗留观察：理发店 `requestClose(silent)` 的 truthy 判断使 `back` 音效实际不可达（疑似既有 bug，本列车未改理发店该路径）；整形侧已按正确语义实现。
- 工程注意：编辑工具会剥离 UTF-8 BOM；`.as` / `.ps1` 改动后必须复查头三字节（本列车 3 个 BOM 文件已逐一修复并验证）。

## 6. asLoader 发布记录

2026-09-08 22:03 经 `scripts/compile_test.ps1 -Target publish`（隐式 publish-only + `-VerifySwf scripts/asLoader.swf`）发布完成，一次成功未重试。发布前 Flash.exe 未在运行，由 `CompileTriggerTask` 计划任务正常拉起完成；预编译 BOM 门 240 个 `.as` 全部通过。

| | 时间（本地） | 字节数 | SHA-256 |
|---|---|---|---|
| 发布前 | 2026-09-08 17:45:10 | 1,284,839 | `D7312E648E3B4A1E0C99F913B07EDB7A14EBB628EF22EEAF60FA7C6416F0CEBE` |
| 发布后 | 2026-09-08 22:03:27 | 1,285,049 | `AFFDCFBF68CF96D755FE28500663D1A72CD8A8EA771BC6E978AA09E20B8FE273` |

- compiler 证据：本轮 Compiler Errors `0 个错误, 0 个警告`；编译耗时 135s；`compile_output.txt` 22:03:29 刷新。日志与副本：`tmp/hairdresser-portrait-asloader-publish.log`、`tmp/hairdresser-portrait-asloader-compiler-errors.txt`、`tmp/hairdresser-portrait-asloader-compile-output.txt`。publish-only 不运行游戏，trace 未刷新不作为行为验证（行为证据由 §4 的 39/39 TestLoader 承担）。
- SWF 内容验证（zlib 解压 CWS 正文 5,140,634 字节文本扫描，`tmp/hairdresser-portrait-asloader-swf-symbols.log`）：`portrait` ×13、`hairdresserSnapshot` ×1、`hairdresser_response` ×1、`HairdresserPanelService` ×4、SLOTS 槽名 `头部装备` ×23 / `手雷` ×49 均在产物中；测试类 `HairdresserPanelServiceTest` 无残留。
- 类归属审计（`node tools/audit-as2-class-embedding.js --policy single-ownership`，`tmp/hairdresser-portrait-class-embedding.log`）：main `0`、asLoader `634`、重复 `0`，通过 single-ownership，与整形列车基线一致（本列车只改既有类、无新增类）。

**合并上游后重发**（merge `3502232a6e` 引入基础斩杀等 6 个 `.as` 后）：2026-09-08 22:48:15 发布，1,285,649 字节（+600），SHA-256 `71406672B550B968D5FECF9C2FACD210789DE5882355C7A75A0359E3A87E8DF9`；Compiler `0 错 0 警`，BOM 门 240 个 `.as` 全过。SWF 双侧符号探针前后对比：我方 `portrait` ×13 / `hairdresserSnapshot` ×1 等在产物，上游 `_sustainNormalAttack` / `_startNormalAttack` / `_isGrounded` 发布前逐一确认 ABSENT、本次全部 FOUND（`tmp/merged-asloader-swf-symbols.log`）；合并后 TestLoader 理发店 39/39、compiler 0/0（`tmp/merged-hairdresser-testloader.log`）；类归属审计 main `0` / asLoader `634` / 重复 `0`（`tmp/merged-class-embedding.log`）。本版本即进入发布闭包的 asLoader。

## 7. 正式发布记录

- release source `1d03e77cb2666a4686e41ab4a647e180d2e5d0bc`；tag `runtime-build-v2/20260909-hairdresser-grid-v1`；release tree `9f4589240cb5e9b32e68897c84907fe4d0fe6932`；request `506501A386CF7DE1D9190C067E7A35E07201042ACC9D902B3C34049D82317A16`；build identity `E9A5CB8B197E9E11613A73AFEE0C5948FA2775CD7605478491034F28C01BBB07`；33-file closure `45928E8531265638F61CD7E8260B6DA4F9F6205FC795E8164CA18359B850FA5F`；Core DLL SHA-256 `8F55BCB6C9541A21B2BA622251F821ADCC6482F36A0C7AE2B23AF8B80C9420FA`。
- 双 signer / 双 faultDomain：本地 X509 `builder-local-a / physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）+ GitHub OIDC/Sigstore `github-hosted-windows`（builder `0DB3937109370E092BCC708092B7B76F192A94DEDCB5131E7C41ADA4CB0C541E`，[cloud run 34253257230](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34253257230)，attestation-only 证明已在 promotion 对本地 CAS 完整重放）。
- production policy 40/40；policy hash `4524763835AE36CFC09E8F0273BDFE52CEB6368532321B0EB467520250818752`；receipt SHA-256 `62A6ED834DF16B545E0A02A9B87C4D1FEE6ECDC1B3D166308F1E387B4ADF92B5`；manifest SHA-256 `C8992F8C9B8130AB3595F1687A5C03B79480C9200E1ACE183AFEA7BDE9511199`；consensus SHA-256 `A85524504721CF6A6C2D12551E91B85DB71E53B88E14AD0D1E0644D8D0F1383A`；promotion 时间 `2026-09-08T17:03:50.8323522Z`；原子替换与本机 `--verify-only` 通过，previous 保留于 `tmp/runtime-promotions/20260908T170316426Z-64d3746faf814d53b466933eaafe2cf2/previous`。
- 过程性节点：request `1BAE5720…` 因 WB130 CSS 债务越限停止（4 处减债 + ceiling 收紧 2089→2088 + 材料字典补派生，提交 `98774ee7ae`）；request `5690F5CE…` 因 source 未上远端与 `sourceRef` 未指向本列车 tag 停止（补推送 + config 提交 `1d03e77cb2`）；两者均由最终 request 原子 supersede，未 dispatch、未 promotion。三轮 build identity 不变，本地 candidate 幂等复用。
- 推送与 Audit：源码推送 [Audit run 34252003361](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34252003361) 报 `source-ahead`；部署提交 `03471c52fc6c91511df046def69739cc5bf3d738` 与上游浮空修复合并为 `c595bc2cd261342d37cd461cf5ff08ee62b9c76a` 快进推送，[post-promotion Audit run 34255411571](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34255411571) 报 `state=promoted / deploymentChanged=true / forcedDeploymentVerification=false`（base `cc56d2cd73…` → head `c595bc2cd2…`）。
- 当前状态：`promoted / FIELD_REVALIDATION_PENDING`。§5 所列真机复验项与基础斩杀 AI 行为仍待测试员正式入口确认；上游浮空修复 `.as` 未进 asLoader，材料字典待下次派生，均属下一列车。
