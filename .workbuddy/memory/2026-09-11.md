# 2026-09-11 工作日志

## 抗性药剂图标烘焙完成（承接 09-10 的阻塞项）

**阻塞已解除**：用户在 CS6 给 8 个元件补了"为 ActionScript 导出"+ 同名 linkage，
并重新 publish 了 `flashswf/arts/素材库-物品技能图标.swf`（00:08，4792385 字节）。

- XFL 复核：`LIBRARY/消耗道具相关/0.回复增益类相关/图标-*抗性药剂.xml` 9 个全部有
  `linkageExportForAS="true"` + `linkageIdentifier="图标-XXX"`。
- SWF 符号表复核（`ffdec-cli -export symbolClass`，只花 1.5 秒）：453 个符号，
  9 个 `图标-XXX` 全部在（char id 2/4/6/8/10/12/14/16/18）。
- `asset_source_map.xml` 重跑登记：`unique assets` 5637→5645（+8），`CONFLICTS` 保持 121。
  备份在 `tmp/asset_source_map.before2.xml`。
- `bake-icons-offline.py --scope all --name <9个>`：125 秒，`processed=9 unchanged=9
  created=0 unresolved=0`。9 个 webp 早已在磁盘上（mtime 00:12:57–00:13:01，像素与当前 SWF
  渲染结果完全一致）→ 这次等于二次确认。manifest `1582 → 1591`，新增恰好 9 条。

**端到端一致性核对（9/9 全绿）**：物品 XML 的 `<icon>` = manifest key = 存在的 webp 文件 = 格格巫商店条目。
manifest key **不带** `图标-` 前缀，SVG 文件名和 linkage 才带。

**耗时结论（可复用）**：
- `scan_linkage.py` 冷启 ~3.5 分钟、文件缓存热 24 秒。
- `bake --name <9个>` 稳定 ~2 分 05 秒（`swf2xml` 超时退化不影响静态图标）。
- `-export symbolClass` + `symbols.csv` 是 1.5 秒级的符号表自查手段，比跑整条 bake 划算得多。
- Pillow 走系统 Python 3.10（`C:/Users/Akatosh/AppData/Local/Programs/Python/Python310/python.exe`）。

**产物**：`tmp/icon-preview/抗性药剂-线上资产对照.png`（128px / 32px 两档，含净化药剂参照），
生成脚本 `tmp/contact_sheet_resist_icons.py`。

**遗留观察**：中央标签圆的黑色环在 32px 下仍偏重（对比净化药剂更明显），
但用户已按当前 SVG 导入并发布，改的话要重新导入 + republish，未动。

## 抗性药剂 hp 恢复改为缓回

`data/items/消耗品_药剂.xml`：从「全属性抗性药剂」到「能量抗性药剂」共 9 条，
`<effect type="heal" hp="600" mp="0" target="self" .../>` →
`<effect type="regen" hp="600" duration="300" interval="30" mode="total" id="<物品名>_缓释" scaleWithAlchemy="true" />`
（300 帧 = 10 tick × 60 HP/tick，总量仍是 600，先炼金后分摊）。

- 位置不变：仍在 `resistanceBuff` 之后、`playEffect` 之前，`buffDomain domain="enhancer"` 依然在最前，
  缓释 Buff 会通过 `registerDomainBuffId` 登记进 enhancer 域 → 换抗性药剂会互相顶掉。
- `id` 按九龙先例（《九龙_狻猊》）给每瓶独立槽位，避免和食品的默认 `药剂_缓释_HP` 槽位串味。
- **未动**（用户没要求）：9 条 `<description>` 里的「恢复600点生命」措辞没改成「10 秒内…」，
  总量仍对得上，但语义上现在容易被读成瞬回，后续要改文案时一起处理。
- 这些条目 status 仍是 `runtime-test-pending`，未实机回归。

## 对话立绘超采样落地（外部 3x，内部不动）

原脚本新增 `--supersample`（默认 3）：FFDec 按 `zoom × supersample` 渲染，再 LANCZOS 降回原尺寸。
`export_external_frames` 加了 `frames-all` 回退子目录（替代原来"删掉已导出帧重导"，
后者会触发批量删除保护）。internal 分支保持 `args.zoom`（1x）——内置矢量肖像在 zoom>1 会抛
`InternalError: Odd number of new curves!`，实测只有 1x 能过。

**结果**（已应用到 `launcher/web/assets/dialogue-portraits/`）：
- external 68 → 71 条目（新增 `人修罗` / `虎妙` / `阿卡`，此前从未导出过）
- 114 张 external 全部重导出：尺寸全部不变，锐度平均 +2.6%，54/114 提升 >5%，最高 `歌留多` +22.9%
- internal 23 条目逐字节未变（**关键**：`--semantic-baseline-dir` 必须指向旧产物目录，
  否则 23 个空肖像会被重导成 851 宽）
- 应用用的是 `tmp/apply-external-portraits-once.py`（一次性，已删）

**过程坑**：烘焙时因输出到全新空目录导致 baseline 缺失、又反复重跑 FFDec 数十分钟。
补跑应改用一次性脚本直接读 `tmp/ss3-out-014006` 的已导出 PNG 重建 external 条目，不重跑渲染。

**中间产物**（可清）：`tmp/ss3-out-*`、`tmp/ss3-tmp-*`、`tmp/backup-portraits-before-ss/`（应用前全量备份）。
