# 装备调制 → Web 工作台：绞杀式迁移工程落地规划

**文档角色**：装备强化、强化度转换、装备进阶与插件改装从旧 AS2「物品与技能相关界面」迁入 Web 双栏工作台的专题 ADR、行为冻结表与施工恢复入口。物品系统共用壳、库存 lease 和迁移总序仍以 [物品系统 → Web Panel：双栏工作台 架构设计](物品系统-双栏工作台-架构设计-2026-06-15.md) 为准；跨栈协议与验证护栏以 [AS2 UI → Web Panel 迁移](../agentsDoc/as2-web-panel-migration.md) 为准。

**最后核对代码基线**：source `f01f4b121a4ceebd7dae051f14bb511c5ae3f1cb`（tag `runtime-build-v2/20260730-workbench-no-as2-fallback-v1`、request `3BEBE136773D2C09022F01E5B3C176A788FE3D84E453F6500C6C560F03184C7B`）；早期装备调制检查点与 §11.3–§11.4 的数字只作迁移历史。现役准入口径以本文顶部、[物品系统总 ADR](物品系统-双栏工作台-架构设计-2026-06-15.md) §0.0.39、[AS2 UI → Web Panel 迁移](../agentsDoc/as2-web-panel-migration.md)及 testing guide 的 fresh 输出为准。

**当前状态**：装备调制已进入 **Web-only fail-closed / `standard_entry_verified`**。七类 operation 只走 `EquipmentTuningService` 的 snapshot/preview/token/commit；AS2 `executeLegacy`、旧 renderer adapter、Host `legacy_equipment_tuning/migration_paused` 专项与 tooltip 合并 `html` 兼容字段均已删除。`EQUIP_UI` 固定进入 Character Build，storage/build/tuning 在同一 workbench 实例内组合；发送、准入、lazy load 或挂载失败只显示可见错误，不回退旧 AS2 UI，主 XFL 对旧装备 Hub、改装、物品栏等聚合资产不可达。GUI 人工验收、双故障域共识、promotion 与同身份标准入口 attempt `8baf52bbcceb452da32da641e58d2922` 已完成；该标准入口只读 smoke 停在首个权威 tuning snapshot，未发送 preview/commit，也不代证七类写操作、普通 close 或持久化专项验收。

> **2026-07-30 supersession**：本文后续关于 R1–R4、旧 renderer 观察期、`migration_paused`、环境故障 fallback、`EQUIP_UI` 尚未改道或“人工验收后再删资产”的段落均是历史施工记录，不是现役兼容合同；凡与本段冲突，以本段和总 ADR §0.0.39 为准。

---

## 0. Compact 后从这里恢复

### 0.1 一句话结论

装备调制绞杀已完成到待人工验收候选：旧 UI 的直接写、显示入口、Host 专项和 main 资产可达性都已移除；现役唯一前端是 Web workbench，唯一业务 writer 是 `EquipmentTuningService`。

### 0.2 已冻结决策

| ID | 决策 |
|---|---|
| D1 | 采用绞杀式迁移；先抽 AS2 `EquipmentTuningService` 权威，再接 Web，再重定向旧入口，最后才谈删除资产。 |
| D2 | Web 仍使用 `panel=workbench`，增加 `view=storage\|tuning`；不新建另一套顶层背包面板。 |
| D3 | 业务总线使用独立 `domain=equipment_tuning`；不得把强化、进阶、插件规则塞进 `inventory` domain。 |
| D4 | v1 只允许 `_root.物品栏.背包` 内的武器/防具作为主装备；不接受已穿戴装备、仓库或战备箱来源。 |
| D5 | 强化、转换、进阶、插件安装/拆卸都必须走 AS2 预览计划 + opaque token + 单次原子提交；Web/C# 不重算规则。 |
| D6 | `EquipmentTuningService` 是唯一 writer；旧 AS2 renderer adapter 与 `executeLegacy` 已删除，禁止恢复第二条同步提交路径。 |
| D7 | `profile=battlebox` 的 storage/build/tuning 固定 Web-only，不保留 feature flag、`tuningAvailable` capability 或环境故障 UI fallback。 |
| D8 | 存档 schema 不变；主 XFL 已移除旧 Hub 的 Include/递归放置/ImportAssets/linkage 可达性，standalone legacy XFL 只可封存。 |
| D9 | `EQUIP_UI` 固定进入 Character Build；装备槽、属性、已穿戴调制和四槽药剂均由现役 workbench/领域服务承载。 |

### 0.3 生产施工冻结决策

| ID | 待定点 | 本文建议 | 不冻结的风险 |
|---|---|---|---|
| D10 | 等级相同的强化度转换 | 明确为成功 no-op：不改值、不涨 revision、不标脏、不记成就 | legacy 当前会显示成功但实际无变化，难以写稳定测试 |
| D11 | 原地装备属性变更如何推进背包 revision | 给 `ArrayInventory` 增加明确的单槽/多槽 touch transaction，一次提交只推进一次 revision，并在提交完成后发事件 | 仅靠 lease confirm projection 会让缓存与 `containerVersion` 失真 |
| D12 | 材料扣除/返还的可观察原子性 | 给 `DictCollection` 增加计划式批事务或等价的无事件写入 + 统一 publish；至少要证明同步监听器看不到半提交 | 当前 `ItemUtil`/集合事件可能在装备写入前后同步触发 |
| D13 | token 的会话归属 | 绑定 AS2 tuning 子会话 generation，并由 Host 盖章 `panelInstanceId/viewSessionId`；关闭、切离 tuning、rebind、断线或成功提交都使 token 失效 | 只绑定浏览器局部状态无法覆盖同一 workbench 内切 View |
| D14 | 无人值守直达边界 | 只允许 `agent_control.openEquipmentTuning` 在 `cf7_agent_*` 克隆槽、同一 attempt/save runtime ack 和 fresh handoff 后，发送正式 AS2 `openInventoryWorkbench(profile=battlebox,view=tuning)`；`/console` 仅用于复用原读盘帧和诊断 | 绕过旧按钮探索，不绕过 AS2 opener、`panel_request`、Host 白名单、业务协议或真实入口专项 |
| D15 | 配件高频操作确认策略 | 默认安全模式全部停在权威预览；可选快速模式仍先取 AS2 preview/token，只在单件安装、无连带的单件替换/卸下且材料 delta 精确匹配时自动 token-only commit；模式仅存 Web 本地 | 若把“快速”实现为直写或宽松自动提交，会绕过依赖级联和材料异常的玩家确认 |
| D16 | 筛选视觉与共享组件边界 | 继续复用 `ItemFilter.FilterNavigator` 的树、路径和交互，只通过 tuning host variables/scoped CSS 呈现 DLS 晶体索引语言 | 复制 navigator 会产生第二套筛选语义和后续维护漂移 |
| D17 | 已穿戴物品如何进入后续调制 | D4 的 backpack-only 继续约束当前 v1；5b-2B 只通过装备容器 adapter 扩展同一 `EquipmentTuningService`，不复制 writer | 若装备页自建调制路径，会重新产生两套原子性、材料与 dirty 语义 |
| D18 | 活体刷新频率 | 角色构筑页采用互斥 `edit/stats`：装备穿脱/已穿戴调制只标 `liveRefreshDirty`，切入数值态或关闭前最多刷新一次；药剂装配零人物刷新，无 loadout revision 变化也零刷新 | 逐 hover/逐装备调用 `_root.刷新人物装扮` 会反复清理射击状态并重建单位 |
| D19 | 个人信息与纸娃娃归属 | 装备槽、四槽药剂、旧 Hub「个人信息」详情和 Web 纸娃娃联合进入逻辑 `CharacterBuildSession`；编辑态保持 45/55 双栏，个人信息使用全宽 SecondaryPage；常驻战斗 HUD 与「情报」仍属独立路线 | 按旧 Hub tab 一比一迁移或把完整属性塞进 55% 右栏，会重复快照、挤压 1024×576 并模糊领域边界 |
| D20 | 长期实时数值预览 | 只把静态数据契约与纯角色构筑 kernel 做 TS/AS2 同源；Web 结果只读，AS2 继续持有装备、存档和提交 authority | 把“共享计算”误写成“Web/V8 接管游戏状态”会破坏现有安全与回退边界 |

> D17–D20 已由 [角色构筑 → Web 双栏工作台：工程落地规划](角色构筑-Web双栏工作台-工程落地规划-2026-07-26.md) 落地：四槽药剂、`CharacterBuildSession` 关闭屏障与 worn source adapter 均为现役；`convert` 仍只允许 inventory source。

### 0.4 硬停线

- 调制入口只允许现役 workbench；五类 GUI 人工验收未闭合前状态保持 candidate，不宣称部署或正式验收。受控 agent 直达仍只开放在专用克隆槽。
- Skill S4C/S5 尚在共用 `物品与技能相关界面.xfl`、asLoader manifest、Launcher/Web 工作台附近施工时，不并行编辑同一文件；先做文件所有权交接或把切片串行化。
- 旧 UI 的直接 writer、adapter、Host 专项和 main 可达资产已经归零；任何回流都必须由结构门与生产 grep 拒绝。
- 未取得新鲜 TestLoader trace、asLoader 输出或独立 XFL 发布证据时，不声称 AS2 已编译通过。
- commit 超时、断连或畸形回包进入 `needs_reconcile`；不得自动重放写命令。
- 已穿戴单件调制通过 loadout source adapter 进入同一服务；跨容器 `convert` 仍拒绝，不能从单件支持外推。

### 0.5 本轮完成边界与正常路径观察

本轮已完成 AS2 单一 writer、Host/Web 协议纵切、Character Build/收纳箱入口、旧 AS2 UI 清除、main 资产闭包收口和受控直达。battlebox 工作台不通过环境变量或 capability 构造两种运行环境；宿舍 warehouse profile 不提供调制。下一步只做五类 GUI 人工验收与真实存档写后重启回读；不恢复旧 writer/renderer/Host source，也不新增调试专属启用开关。只读 runner 的 snapshot 证据不能替代真实业务写证据。

---

## 1. 可行性与排序裁决

### 1.1 为什么适合作为下一个目标

| 维度 | 评价 | 证据与含义 |
|---|---|---|
| 玩家热点 | 高 | 强化、转换、进阶、插件会在装备生命周期内反复发生，不是一次性配置。背包可打开时，玩家就有检查和调整新掉落装备的理由。 |
| 与已迁基建的邻接度 | 高 | 输入装备当前就来自背包；Web 工作台已有 owned inventory、分页、tooltip、slot lease、write gate 与动态右 View。 |
| 与其他旧 UI 的正交性 | 中高 | v1 限定背包装备后，不涉及穿戴角色的动画、活体属性刷新、换装或战斗状态；主要交集是背包、材料、成就和存档。 |
| 绞杀收益 | 很高 | 旧装备 Hub 中最有状态、最常操作的一组责任可先失去直接写权；其余装备槽/属性仍可留在旧 UI，适合逐块掏空。 |
| 规则复用成熟度 | 中 | `EquipmentUtil`、`TierSystem`、`TagManager` 已有大量领域判断；但旧 UI 仍持有进阶顺序、费用与直接写流程，尚未形成事务服务。 |
| 事务成熟度 | 低 | 原地变更不推进容器 revision，材料扣除与 dirty/achievement 偶然耦合，转换缺显式 dirty；这是主要成本，不是 Web 绘制。 |
| 资产/发布冲突 | 中高 | 调制 renderer 位于独立 `物品与技能相关界面.xfl`，当前 Skill fallback 也触及这一旧 Hub；必须串行施工。 |

总体判断：**适合作为下一目标，但必须把“业务权威抽取”视为主体工程，把 Web 页面视为后半段。** 若只按界面工作量估算，会严重低估原子性与持久化风险。

### 1.2 与“先迁装备槽/属性”的比较

| 项目 | 先迁装备调制 | 先迁装备槽/属性 |
|---|---|---|
| 输入是否已在 Web | 是，背包已完成 | 部分；需要新建穿戴槽和角色投影 |
| 角色活体副作用 | v1 基本无 | 高：equip/unequip、外观、属性、被动与运行态刷新 |
| 可独立切走的责任 | 高 | 中，容易牵出玩家信息/HUD/换装 |
| 旧 UI 掏空效果 | 先移走高频编辑责任 | 先移走展示与穿脱责任 |
| 对下一切片的帮助 | 调制完可直接进入穿戴流程，减少旧 UI 往返 | 穿戴后仍需回旧调制页配置装备 |
| 失败回退 | 可回旧 renderer，但仍走新服务 | 回退常涉及活体状态重新对账 |

“玩家通常先调制、再穿戴”不是所有玩家路径的严格事实，因此不能当成数据依赖；但它是很强的体验顺序和工程顺序。背包卡片上的“调制”动作可以自然进入右栏工作台，完成后再进入未来的“穿戴/比较”。这会让装备槽迁移建立在已经稳定的装备编辑权威之上。

### 1.3 成本—收益结论

收益主要来自三层：减少背包与旧 Hub 往返；复用背包高出场率扩大 Web 工作台价值；让旧装备 UI 首次发生可计量的事实退役。代价集中在 AS2 事务语义，而不是布局。

| 切片 | 相对成本 | 主要产出 |
|---|---|---|
| T0 探索/ADR | 中，已完成 | 行为冻结、缺口地图、目标架构、停线 |
| T1 AS2 权威服务/测试 | 高 | 六个 operation 只有一个 writer，修正 dirty/revision/原子性 |
| T2 协议 + C# Task | 中高 | preview/token/commit/reconcile 闭环和写状态机 |
| T3 Web tuning View | 中 | 复用背包与双栏壳，新增调制组合层与 harness |
| T4 legacy 委托/入口改道 | 中高 | 旧 UI 失去业务权威；独立 XFL 精确发布 |
| T5 GUI 人工验收 | 中 | 七类 operation、两条正式入口、失败可见 fail-closed 与写后重启回读 |
| T6 main 资产闭包 | 已完成到候选 | 旧 Hub/改装聚合资产对 main 不可达，standalone 仅封存 |

和装备槽切片相比，本方案前期事务成本更高，但迁移杠杆也更高。它值得做，因为这些事务缺口无论哪天迁移都必须解决，而越早集中成服务，旧 UI 与后续 Web 都越少继续制造分叉状态。

---

## 2. 现役旧 UI 拓扑与绞杀边界

### 2.1 迁移前的旧 Hub 拓扑（历史）

独立资产入口是：

- XFL：`flashswf/UI/物品与技能相关界面/物品与技能相关界面.xfl`
- 发布产物：`flashswf/UI/物品与技能相关界面.swf`
- 主符号：`LIBRARY/新版物品栏界面.xml`
- 嵌套调制符号：`LIBRARY/新版装备强化界面.xml`，实例名 `装备强化界面`

`新版物品栏界面` 曾同时承载物品栏、个人信息、技能、材料、情报等帧；`新版装备强化界面` 内再分强化装备、强化度转换、插件改装。这解释了迁移为何必须先拆领域责任，而不是一次替换显示帧。

2026-07-30 现役入口已收口：Launcher `EQUIP_UI` 走 Character Build preflight，storage/build/tuning 由 Web workbench 承载；`openEquipUI`、旧调制子入口和服务 adapter 均已删除，旧 Hub 聚合资产对 main 不可达。

### 2.2 本切片包含

- 强化装备：选择目标强化度、费用预览、提交。
- 强化度转换：两个背包装备之间交换强化度。
- 装备进阶：二阶/三阶/四阶及特殊材料候选、预览、提交。
- 插件改装：候选可用性、安装、拆卸单个、级联依赖返还、全部拆卸。
- 背包卡片到调制 View 的上下文动作与热切换。
- 旧 renderer 委托新权威服务、Web 反馈入口、legacy redirect 暂停与观察指标。

### 2.3 本切片不包含

- 穿戴槽、equip/unequip、角色属性总览、角色外观或活体刷新。
- 从仓库、战备箱或已穿戴槽直接调制。
- 装备数值公式重做、平衡调整或材料命名迁移。
- 存档结构迁移、坏档自动修复、历史装备全量归一化。
- 删除旧 Hub、XFL 帧、linkage 或 `EQUIP_UI`。
- 将装备调制业务下沉到通用 Shell、Grid、OwnedInventoryView 或 InteractionBroker。

这些是 **当前 5b-2A 切片的实施边界**，不是永久产品禁区。已穿戴来源、纸娃娃、穿脱和个人信息详情归下一条 5b-2B 角色构筑纵切；后者只能复用本服务，不能回头修改 D4 来假装当前协议已经支持装备容器。

---

## 3. 当前行为冻结

以下是 T0 从生产源码核出的现状，不代表所有行为都应原样保留；有意改变处必须在 T1 测试中显式记录。

### 3.1 来源与选择

- 迁移前由 `InventoryIcon` 限制调制来源；该类已于 2026-07-30 随全屏 renderer 删除。现役 [EquipmentTuningService.as](../scripts/类定义/org/flashNight/arki/item/EquipmentTuningService.as) 的 `resolveWebInventorySlot` 只接受 `_root.物品栏.背包` 中的武器/防具进入调制主槽或转换目标。
- 主装备变化时，旧 UI 会按当前 tab 重建预览；转换候选按同一 `item.getData().use` 高亮。
- 旧调制 UI 只监听物品移除；强化度、tier、mods 等原地变化不触发移除事件。
- 因此 v1 保持 backpack-only，不能借 Web 已能查看仓库就扩大写入来源。

### 3.2 强化装备

| 项 | 现状 |
|---|---|
| 强化上限 | 主线进度 `>129 → 13`；否则 `>74 → 9`，其余 `7`。铁匠功能开启且等级 `>=10` 时，非 13 上限分支再 `+1`。 |
| 默认目标 | 当前强化度 `+1`；超过上限则截到上限。 |
| 调整按钮 | 旧资产提供 `+1`、`-1`、直接 `13`、直接 `2` 等操作，最终仍受预览 clamp。 |
| 材料 | `强化石`。 |
| 每级成本 | 对每个目标级 `i` 累加 `floor(multiplier * (i - 1)^3 + 1)`。 |
| 折扣 | 铁匠被动形成 `multiplier=max(1-level*0.05, 0)`；展示节省值使用 `ceil((1-multiplier)*(i-1)^3)`。 |
| 当前提交 | `singleSubmit("强化石", cost)` 后直接写 `item.value.level=target`，再记录 `装备强化次数` 并刷新图标。 |

目标服务必须从现有权威状态读取上限与折扣依赖，token 至少绑定主线进度、铁匠开关/等级、装备 lease/状态和强化石精确数量。C#/Web 不复制上述公式。

### 3.3 强化度转换

| 项 | 现状 |
|---|---|
| 候选 | 与主装备 `use` 相同；不能是同容器同槽。 |
| 成本 | 无。 |
| 结果 | 直接交换两个 `value.level`。 |
| 相同等级 | 旧 UI 会警告“无效果”，但仍可走成功反馈。 |
| 副作用缺口 | 不显式 dirty、不推进两个槽的 mutation revision、不写 item timestamp、无专属生命周期事件。 |

目标实现需要两个独立 slot lease，并把交换视为一个不可拆分的多槽事务。D10 建议把相同等级冻结为成功 no-op。交互上左栏只选择主装备并保持玩家原筛选、面包屑和排序；同 `use` 目标通过 inventory coordinator 的独立只读 projection 取得新鲜 lease，在右栏候选格中选择，projection 不覆盖左栏可见 request/window。

### 3.4 装备进阶

`TierSystem.isTierMaterialAvailable()` 只回答装备静态配置是否支持某材料/默认低级防具资格，不负责当前 tier 到下一 tier 的顺序。现有进阶顺序实际被旧 UI renderer 硬编码：

- 无 tier：锁定三阶/四阶，允许配置支持的二阶或特殊冰/火等材料。
- 当前二阶：只开放三阶。
- 当前三阶：只开放四阶。
- 其他状态或四阶：全部锁定。

提交路径却只再次调用较弱的 `isTierMaterialAvailable()`，随后扣材料并直接写 `item.value.tier`。这意味着若 Web 直接复用该函数，会绕过旧 UI 的进阶顺序。**T1 必须把合法 transition 收回 `EquipmentTuningService`/领域层，Web 只消费候选与拒绝原因。** 不应在 Web 复制硬编码材料名。

### 3.5 插件改装

插件规则的现有领域权威较完整：`EquipmentUtil` 聚合配置，`TagManager` 判断槽位、重复、tag 依赖/冲突、blocked tag、武器类型、子弹与安装条件。现有可用性状态包括：

| 状态 | 含义 |
|---:|---|
| `1` | 可安装 |
| `0` | 未知/不可识别 |
| `-1` | 插槽已满 |
| `-2` | 重复插件 |
| `-4` | 技能/特殊武器冲突 |
| `-8` | 同 tag 冲突 |
| `-16` | 缺少前置依赖 |
| `-32` | 拆卸会破坏依赖 |
| `-64` | blocked tag |
| `-128` | 子弹类型不兼容 |
| `-256` | 安装条件不满足 |
| `-512` | 子弹要求不满足 |

旧提交路径：

- 安装：校验候选、扣一个插件材料、`mods.push()`、记录 `配件安装次数`。
- 拆单个：先计算一层直接依赖者；存在依赖者时无视目标 `detachPolicy`，移除并返还“目标 + 直接依赖者”；无直接依赖且目标 `detachPolicy=cascade` 时清空并返还全部 mods；否则只拆目标。旧路径不会以 `-32` 拒绝。
- 全拆：逐个返还材料后 `mods=[]`。

静态数据扫描（2026-07-16）得到 104 个插件、10 个 provider、24 个 consumer、6 个 `detachPolicy=cascade`、18 个 install condition；忽略 use 约束时有 101 条可能 provider→consumer 边，当前数据没有 provider→consumer→consumer 的二级链。直接依赖计算因此覆盖当前数据，但新服务仍应使用 fixed-point 或提交后全量有效性检查，避免数据扩展后漏掉级联链。

只读 snapshot 遇到非数组 `mods` 必须 fail closed 并给出结构化错误；不得借读取路径偷偷把存档改为 `[]`。现役 `BaseItem.createFromObject` 已会把大多数加载态坏形状归一为数组，这不构成新协议可以静默写档的理由。

---

## 4. Writer 与副作用地图

### 4.1 已确认的直接 writer

| 操作 | 当前装备写 | 材料写 | 成就 | 显式 dirty | 容器 revision |
|---|---|---|---|---|---|
| 强化 | `value.level=target` | `ItemUtil.singleSubmit` | `装备强化次数` | 无；依赖成就记录间接标脏 | 无 |
| 转换 | 交换两个 `value.level` | 无 | 无 | 无 | 无 |
| 进阶 | `value.tier=tierName` | `ItemUtil.singleSubmit` | 进阶相关记录 | 无；依赖成就记录间接标脏 | 无 |
| 插件安装 | `mods.push(name)` | `ItemUtil.singleSubmit` | `配件安装次数` | 无；依赖成就记录间接标脏 | 无 |
| 插件拆卸/全拆 | splice 或覆写 `mods=[]` | `ItemUtil.acquire` 返还 | 无 | `acquire` 会标脏 | 无 |

关键事实：`ItemUtil.acquire` 末尾显式 dirty，`ItemUtil.submit` 不 dirty；`AchievementMetrics.record` 会 dirty。因此强化、进阶、安装是否持久化，当前偶然依赖成就子系统。转换没有这条间接链。新服务必须显式拥有存档脏标记，不再把 achievement 当 persistence primitive。

### 4.2 lease 与 revision 的现状

`ArrayInventory` 已有 `mutationRevision`、事务写与事务替换；add/remove/sort 等结构变化会推进 revision。直接修改 `BaseItem.value.level/tier/mods` 不会推进 revision。

`InventoryPanelService` 的 lease confirm projection 已包含强化度、tier、mod signature 等字段，所以旧 lease 在原地调制后大概率会校验失败；但容器 `containerVersion` 仍不变化，事件订阅与缓存更新语义不完整。不能把“旧 lease 恰好失败”当成事务实现。

T1 应提供明确的 in-place touch transaction：

- 强化、进阶、插件提交触碰一个槽；转换原子触碰两个槽。
- 先校验全部 lease、规则、材料与回收容量，再进行任何写。
- 一个业务 commit 只产生一个逻辑 revision 批次。
- 所有装备值与材料写完成后再发布生命周期事件。
- 成功后返回受影响槽与单调事务水位；Web 统一调用 `InventoryCoordinator.completeExternalWrite(true)`，按 coordinator 当前实际窗口/筛选全量重取并轮换 lease，不把“受影响物理页”误当当前可见窗口，也不同时应用两套 snapshot。

### 4.3 材料集合缺口

材料栏使用 `DictCollection`，当前没有与 `ArrayInventory.mutationRevision` 对等的 revision/批事务。`add/addValue/setItems` 会直接改变集合并派发生命周期事件。拆卸又是“先返还材料、再改装备”的现状顺序；一旦中途失败，没有统一 rollback 和延迟事件发布。

D12 必须在 T1 做选择和测试。最低正确性要求：计划阶段精确计算所有材料 delta 与返还结果；提交前全量 revalidate；失败零写；成功时监听器不能把中间态当最终态。若现有同步事件阻碍该不变量，就新增材料批事务，而不是在 `EquipmentTuningService` 里堆脆弱的反向补偿。

---

## 5. 目标架构

### 5.1 分层

```text
Web panel=workbench
  ├─ 左：既有 Backpack owned view / InventoryCoordinator
  └─ 右：view=tuning / EquipmentTuningRuntime + View
             │ domain=equipment_tuning
Launcher EquipmentTuningTask
             │ gameCommand / equipment_tuning_response
AS2 EquipmentTuningService
  ├─ InventoryPanelService：slot lease / affected snapshot
  ├─ EquipmentUtil / TierSystem / TagManager：领域规则
  ├─ ArrayInventory / DictCollection：计划式原子写
  ├─ AchievementMetrics：成功后 exactly-once 记账
  └─ `_root.存档系统.dirtyMark`：显式 persistence ownership
```

边界原则：Shell 只管理 View 生命周期；InventoryCoordinator 只管理背包窗口、lease 与共享写门；EquipmentTuningRuntime 管理调制请求状态；所有“能否强化/进阶/安装、扣多少、拆哪些依赖”的答案都来自 AS2 服务。

### 5.2 为什么复用工作台而不新开面板

现有 `DualPaneShell` 已支持注册、挂载和替换右侧 View；`inventory-workbench.js` 已让一个 `InventoryCoordinator` 独占背包与右栏库存窗口。目标只需把右栏从 storage View 换为 tuning View，并保留左侧背包筛选、分页和选择上下文。

目标入口形状：

```text
panel=workbench
profile=battlebox|warehouse
view=storage|tuning
```

T0 基线里的 AS2 `InventoryPanelService.handlePanelRequest` 和 C# `OpenInventoryWorkbench` 只白名单 `profile=warehouse|battlebox`，当时尚不接受 `view`。本轮 T2 已让 AS2、C#、Web 三端对 `view=storage|tuning` 使用同一显式白名单，并以结构化拒绝处理未知值，不能透传任意字符串。

### 5.3 背包与调制的交互接缝

- 正常 storage View 保持现有跨栏 InteractionBroker 与拖拽语义。
- 进入 tuning View 时，取消在途拖拽并停用“背包↔右库存”的 transfer intent；背包卡片点击改为选择/热切换主装备。
- 转换 tab 可进入第二候选选择态，但仍由调制组合层解释，不污染通用 owned inventory 组件。
- 背包卡片“调制”按钮由 `inventory-workbench.js` 组合层追加；共享卡片壳只暴露通用 action 插槽/事件，不认识装备规则。
- 切回 storage 时恢复原右栏、页码与已选 profile；不得重复创建 ResizeObserver、pointer handler 或拖拽镜像。

现有 `InventoryCoordinator.beginExternalWrite(owner)` / `completeExternalWrite(needsRefresh)` 已被其他领域提交复用。调制 commit 采用同一接缝：开始时 `beginExternalWrite('equipment_tuning.commit')`，期间禁止 sort/discard/move；确定成功或 reconcile 完成后 `completeExternalWrite(true)`，刷新背包并轮换 lease。

### 5.4 与 5b-2B 角色构筑纵切的交汇边界

后续装备页是**会话与刷新策略上的特例宿主**，不是新的调制服务。目标形状为：

```text
CharacterBuildSession（跨层逻辑会话；AS2 CharacterBuildService 持 revision/dirty authority）
  ├─ edit：纸娃娃 + 11 装备槽 + 4 药剂槽 + 筛选候选 + 已装备物品调制
  └─ stats：全宽 SecondaryPage 的冻结个人信息详情；禁止换装/调制
          │
          ├─ domain=loadout           装备/药剂 equip/unequip 管理命令
          ├─ domain=equipment_tuning  继续复用本服务
          ├─ character projection     只读属性快照/未来纯计算核
          └─ DressupDollRenderer      Web 视觉预览
```

当前背包调制仍按 commit 后立即轮换 inventory lease；5b-2B 增加的是装备容器 adapter，以及由 AS2 session state 权威持有的 `loadoutRevision/liveRevision/liveRefreshDirty`。Web/Host 只镜像这些值以驱动页面，不得成为断线后会丢失的唯一 dirty owner。首轮已穿戴来源只开放单件原地操作：强化、进阶、插件安装/替换/拆卸；强化度转换需要同时覆盖背包与装备容器双 lease、锁顺序、失败回滚和两个 projection，单独通过原子性 Gate 前继续拒绝。调制规则、材料计划、opaque token、achievement 与 persistence ownership 均不得搬入 `CharacterBuildSession`。

短期 `edit` 中每次确认装备/调制提交只推进权威 loadout revision 并置 `liveRefreshDirty`，不调用活体刷新；Web 纸娃娃首版只按明确 click/键盘选择本地重绘，hover 只显示 tooltip。`edit → stats` 必须等待所有写与 reconcile 收口，仅在 `liveRefreshDirty=true` 时刷新一次活体，并从 `PlayerInfoProvider` 现有 getters 抽取结构化快照；`stats` 使用全宽 SecondaryPage 并禁止装备操作。正常 close 走同一 finalize barrier：live dirty 控制人物刷新，全局存档 dirty 控制本地持久化；两阶段成功后才释放 pause lease。Host 断线则由 AS2 lifecycle fallback 读取同一 AS2 session 状态完成 finalize，不能依赖已经消失的 Web session。切页不能隐式提交未确认预览，无装备变更切页必须零人物刷新。

长期若角色构筑输入 schema 稳定，可把装备派生属性抽为严格 TS 子集纯 kernel，并生成 Web JS 与可读 AS2，使 edit 态完整数值预览不再触及活体。该优化只改变 projection，不改变本服务的提交 authority；启动条件与版本握手见 [TS 到 AS2 编译基建可行性备忘](TS到AS2编译基建可行性备忘-2026-07-18.md)。

---

## 6. 协议草案与事务不变量

### 6.1 命令面

协议字段名在 T2 实现前做一次最终冻结；操作集合不再扩张：

| cmd | 读写 | 用途 |
|---|---|---|
| `snapshot` | 只读 | 按背包 source lease 投影装备、cap、材料、tier/plugin 候选和拒绝原因 |
| `preview` | 只读 | 对某 operation 生成 before/after、材料 delta、依赖拆卸集和 opaque `tuningToken` |
| `commit` | 写 | 只接收 `expectedTuningToken`，执行服务端缓存计划 |
| `tooltip` | 只读 | 仅补调制候选特有 tooltip；装备主体尽量复用 inventory lease-bound tooltip；请求可携带 `source`（可选，lease 规范化同 snapshot），命中后响应附带候选装上当前装备的试算 `statsBefore`/`statsAfter`（规则不允许安装的候选不附带；材料缺失不阻挡试算） |

`operation` v1 白名单：`enhance`、`convert`、`install_tier`、`install_mod`、`detach_mod`、`detach_all_mods`。

Web→Host 严格 envelope 与领域 payload 分层；Host 只把白名单字段重建为 AS2 参数：

```json
{
  "type": "panel",
  "panel": "workbench",
  "domain": "equipment_tuning",
  "cmd": "preview",
  "callId": "tune.<session>.<seq>",
  "panelInstanceId": "host-stamped-instance",
  "payload": {
    "v": 1,
    "viewSessionId": "tuning-subsession",
    "operation": "enhance",
    "source": {
      "containerId": "背包",
      "slot": 7,
      "expectedLease": "opaque-inventory-lease"
    },
    "targetLevel": 9
  }
}
```

转换额外带 `target` slot ref；插件/tier 只带服务端 snapshot 已返回的候选 key。Web 传来的 target level、候选名和 slot 都是不可信意图，AS2 必须重新校验。

preview 响应的 `before/after.{source,target}.equipment` 自 2026-08-15 起附带**可选** `stats` 属性行数组，供 Web 渲染操作前后属性对比（旧 runtime 无此字段时 Web 整段回落，不视为协议违约）：

```json
"stats": [ { "key": "power", "label": "子弹威力", "value": 108 } ]
```

- 行由 AS2 `EquipmentStatProjector.project` 对 before/after 对称试算产出（`EquipmentCalculator.calculateInPlace` 应用 tier/强化/插件后的**最终生效值**，整数属性已按游戏内规则取整）；数值型字段取 `TooltipConstants.PROPERTY_DICT` 收录项并按 `PROPERTY_PRIORITIES` 排序，`magicdefence` 拍平为 `magicdefence.<元素>` 子行，label 为剥离 HTML 标签后的中文显示名。
- Host 白名单条件放行 `stats` 并逐行 sanitize（key 唯一、行 ≤64、value 为有限数值且保留原始 Integer/Float token 类型）；snapshot 投影不携带 `stats`。
- Web 端以 key 联结 before/after 生成 diff 行，极性表区分增益/劣化/中性（weight/interval/diffusion/reloadPenalty 为越低越好，`level` 等级限制恒中性）；preview 区与候选 tooltip 图片栏（`iconFootHTML` 槽位）共用同一 diff 行模型。
- commit 响应的 `before` 回显 preview 计划（含 stats），`after` 由提交后权威状态重建、不含 stats；commit 校验在比对 preview 绑定与 commit after 前剥离 stats 键。

### 6.2 token 绑定内容

opaque token 对应的 AS2 内部 plan 至少绑定：

- AS2 tuning owner/generation、Web `panelInstanceId + viewSessionId`（或 legacy generation）、operation、一次性消费状态和短生命周期；
- 主装备 container/slot、对象引用 lease、调制 confirm projection；
- 转换目标的独立 lease/projection；
- 精确材料 before count 与 delta；
- 强化 cap 的主线/铁匠依赖；
- tier transition、mod candidate、依赖闭包和相关配置签名；
- 预期 after projection、成就 delta、受影响槽集合。

commit 不接收价格、公式结果、after item、依赖拆卸列表或材料 delta。成功、确定失败、过期都会消费 token；超时/断连由 Host 进入 reconcile，不自动猜测 token 是否可重用。

### 6.3 原子提交不变量

1. `snapshot`/`preview` 零写：不改装备、材料、revision、dirty、achievement 或时间戳。
2. commit 开始时重新验证 token 会话、全部 slot lease、参与字段、规则依赖和材料精确数量。
3. 任一预检失败：零装备写、零材料写、零事件、零 revision、零成就、零 dirty。
4. 成功：装备字段、材料 delta、item timestamp、容器 touch、领域事件、成就与 dirty 形成一个逻辑 commit。
5. 转换同时触碰两槽；插件级联返还与 mods 新数组同时生效，不存在半提交。
6. 成就只在成功后记录一次；dirty 由服务显式触发，不依赖成就或 `acquire` 的副作用。
7. 回包包含受影响槽、更新后的 tuning snapshot、单调 transaction/reconcile 水位和可诊断但不含内部 token 数据的 transaction id；库存刷新由当前 coordinator 窗口统一重取。
8. 确定 no-op 遵循 D10：成功但无任何持久化副作用。

### 6.4 Host 写状态机

新增 `EquipmentTuningTask`，沿用现有领域 Task 的 pending request 与 write gate 形状，但不把 panel 名当 domain：

```text
idle → write_pending → idle
                 └→ needs_reconcile
needs_reconcile → 水位不早于未知写的 structurally-valid snapshot/new preview → idle
```

timeout、send failure、断连、callId/cmd 不匹配或畸形成功回包进入 `needs_reconcile`。Host 记录 `reconcileAfterCallId/writeEpoch`，只有 AS2 回包水位不早于未知写时才解除；已知的权威业务拒绝可直接回 idle。reconcile 只允许读取并要求玩家重新预览，不重放原 commit。

Flash 的 `equipment_tuning_response` 由 `TaskRegistry` 注册到 `EquipmentTuningTask.HandleFlashResponse`；`WebOverlayForm` 只负责 Web 请求按 domain 分流、active workbench/Host instance 门和 Task 的 PostToWeb 注入。所有错误响应仍由 Host 盖回可信的 `domain/cmd/callId/panelInstanceId` 关联字段。

---

## 7. 旧 UI 绞杀与事实退役状态机

| 阶段 | 旧 AS2 renderer | Web | 业务 writer | 可回退性 | 放行门 |
|---|---|---|---|---|---|
| R0 历史 | 主入口，直接写 | 无 tuning | 多处分散 | 高，但语义分叉 | 本文 T0 |
| R1–R4 历史 | 先委托、双前端、切流与观察的过渡阶段 | 逐步接管 | `EquipmentTuningService` | 当时曾设计兼容回退 | 仅作历史审计，不得恢复 |
| R5 当前候选 | adapter/入口/main 可达资产均已删除 | Web-only | `EquipmentTuningService` | **无 AS2 UI 回退** | 静态门 + fresh Flash/Host/Web 自动门 + 五类 GUI 人工验收 |

当前入口来源只保留正式 workbench/Character Build 与受控 `agent_control`；业务日志记录 operation、success/rejected/reconcile 即可。旧 `legacy_tuning_*`、`migration_paused` 和 `launcher_unavailable_fallback` 名称不再属于协议、指标或测试夹具。

---

## 8. 施工切片

### T0 — 前期工程探索（本轮已完成）

- 冻结旧 UI 拓扑、六个调制操作与来源边界。
- 追踪直接 writer、dirty、revision、achievement、材料事件与 lease。
- 核对 Web 工作台动态 View、InventoryCoordinator 外部写接缝、Host/domain 路由。
- 形成 Conditional Go、硬停线、协议草案和退役状态机。
- 只改文档，不产生编译/真机通过声明。

### T1 — AS2 权威服务与行为测试

- 新增 `EquipmentTuningService` 与 TestLoader 专项测试；新增 `.as` 必须复制现有 BOM 文件再改名。
- 冻结 D10–D13；把 tier progression 从 renderer 移入领域服务。
- 为 ArrayInventory in-place touch 与材料批事务建立最小 primitive 和重入测试。
- 实现 snapshot/preview/commit，覆盖六个 operation、token 失效、原子 rollback、dirty/achievement exactly-once。
- 把旧 renderer 的六个调制执行函数改为服务 adapter；全仓 writer scan 对持久化 writer 分类，证明调制函数集合直接写归零。出售自动拆件、坏档归一、历史迁移、临时投影和测试 fixture 必须进入显式 allowlist 或另行事务化，不能用“全仓字符串为零”伪造门禁。
- 暂不改变玩家入口归属。

### T2 — AS2 bridge、C# Task 与协议闭环

- 新增 `equipment_tuning` gameCommand handler/response channel。
- 新增 `EquipmentTuningTask`、TaskRegistry/Program 注入、`WebOverlayForm` 路由与 xUnit。
- AS2/C#/Web 同步增加 `view=storage|tuning` 白名单。
- 覆盖 callId 隔离、错误盖回、write_pending、needs_reconcile、close/reopen 与迟到回包。

### T3 — Web workbench tuning View

- 新增 tuning runtime/view/harness，并登记 lazy assets。
- 左栏复用既有背包；右栏实现强化、转换、进阶、插件四个状态，不复制业务公式。
- 实现背包卡片上下文动作、主装备热切换、转换第二候选选择。
- 使用 `beginExternalWrite/completeExternalWrite(true)` 仲裁 inventory 写。
- 验证 storage↔tuning 往返不丢页码/筛选，不泄漏 observer、pointer 或 drag 状态。

### T4 — 旧入口与 adapter 退役（已完成）

- 旧 renderer adapter、`executeLegacy`、Host legacy source 专项与旧 tooltip 合并字段已删除。
- `EQUIP_UI` 固定 Character Build，工作台内部进入 tuning；Launcher/Host 不可用时可见 fail-closed。
- 主 XFL 已移除旧聚合 UI 资产可达性；standalone XFL 只封存，不作为 fallback。发布仍只选实际目标，不用 `main` 代替子 SWF。

### T4A — 受控无人值守直达

- `agent_control.status` 暴露与 arena 解耦的 `readyForRuntimeAutomation/runtimeReadyBlockedBy`；通用 ready 只看 Launcher Ready、reveal、socket、安全 snapshot 与同一 attempt/save 的 runtime load ack。
- `openEquipmentTuning` 必须同时校验 `expectedSlot/expectedAttemptId`，且 slot 匹配 `cf7_agent_*`；正式 `crazyflasher7_saves*` 永久拒绝。
- 动作只发送 AS2 `openInventoryWorkbench(profile=battlebox,view=tuning,source=agent_control)`，继续走 `panel_request → LauncherCommandRouter → PanelHost`；不得直接 `PanelHost.OpenPanel`、`Panels.open` 或用 `/console` 调业务 commit。
- runner 在 start 前记录 `/logs` 水位，等待本轮新鲜 `[BootstrapAS] event=handoff` 后才单次调用 `agentEnterResolvedSave()`，再等 runtime ready、workbench active instance 和首个 tuning snapshot；任一阶段超时都停止并留证。
- 默认专用槽 `cf7_agent_equipment_tuning`，由明确 seed/shadow 克隆。当前 runner 只做到 runtime ready、正式 opener、active workbench 与首个权威 snapshot，并在此停止；它不会发送 preview/commit。若后续增加显式写验收模式，必须在专用克隆槽另做写前业务基线、commit、强制存盘、完全重启与权威回读，并区分启动规范化与调制写入，不能用启动前整档 hash 直接归因。

### T5 — 当前 GUI 人工 Gate

- 覆盖七个 operation、材料不足、cap/tier 边界、插件依赖级联、双槽陈旧、commit 未知结果与重启回读。
- 从 Character Build 已穿戴装备和 battlebox 背包装备两条正式入口进入；确认关闭/重开、storage/build/tuning 往返与 exact instance。
- 模拟 Launcher/socket/lazy/mount 失败时只出现可见错误，且不出现旧 Flash 调制、物品栏或装备 Hub。
- 受控只读直达只证明正式 opener、Host 实例绑定与权威 snapshot 读链；事务写和重启回读仍由 TestLoader 与专用克隆槽写验收分别取证。

### T6 — main 资产闭包（已完成到候选）

- 装备槽/属性、材料、技能教师等责任已有独立新归属，旧 Hub 对 main 不可达。
- XFL 治理三件套、linkage scanner、source/SWF reachability gate 与 FFDec 定点审计均为放行门。
- standalone legacy XFL 的物理归档/删除可另做，不得影响 main closure，也不得成为恢复聚合 SWF 的理由。

---

## 9. 验证矩阵

### 9.1 T0 静态门

- `node tools/validate-equipment-mod-ui.js`
- `node tools/validate-doc-governance.js`
- `git diff --check`

这些只能证明静态数据/文档与 diff 基本健康，不能证明 AS2 编译或运行态行为。

### 9.2 T1 AS2 门

- TestLoader：每个 operation 的 preview 零写、成功、确定拒绝、陈旧 lease、token 重放、材料竞争、rollback、dirty/revision/achievement 计数。
- asLoader：服务与 bridge 实际注入；需要新鲜 trace/Output Panel 证据。
- writer scan：六个调制执行函数不再直接写 `value.level/tier/mods` 或自行扣/返材料；全仓扫描输出分类 allowlist，至少区分出售自动拆件、构造/坏档归一、历史迁移、临时运行态投影与测试 fixture。
- 运行命令以 [Flash CS6 自动化编译指南](../scripts/FlashCS6自动化编译.md) 为准；不能把 marker 单独当成功。

### 9.3 T2/T3 Host 与 Web 门

- Launcher build 与新增 `EquipmentTuningTaskTests`。
- tuning harness：snapshot/preview/commit/reconcile、迟到/错 callId、关闭/重开、token rotation。
- workbench regression：storage/battlebox/warehouse 原路径、背包 sort/discard/move 写门不退化。
- agent control：arena status 不可读时 runtime-ready 仍可成立；旧 attempt/slot、live slot、未 ready 均零发送；正确动作只发一次正式 AS2 opener，并等待 active instance + 首个 tuning snapshot。
- UI：1024×576、letterbox、缩放、鼠标/键盘、长文案、材料不足、依赖拆卸确认。
- 性能：候选较多时不全量重绘背包；tab 切换不重复请求无关 inventory window。

### 9.4 T4 独立 Flash 目标

若修改 `scripts/类定义/*PanelService.as`、WebView bridge include 或 UI 交互 include，它们属于 asLoader，使用 `-Target publish`；测试属于 `-Target test`。

若修改 `flashswf/UI/物品与技能相关界面/LIBRARY/*.xml`，实际发布目标是：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/compile_test.ps1 `
  -Target 'flashswf/UI/物品与技能相关界面/物品与技能相关界面.xfl' `
  -PublishOnly `
  -VerifySwf 'flashswf/UI/物品与技能相关界面.swf'
```

发布前后按顶层约束运行 `scripts/tools/xfl/` 的 audit / rename_a_class / fix_includes 与 `tools/linkage_scanner/scan_linkage.py`；linkageId 冲突只在 CS6 人工处理。关键帧脚本还需 FFDec 导出检查，不能用 `main` 发布或 SWF mtime 代替内容证据。

### 9.5 必测故障案例

| 类别 | 案例 | 预期 |
|---|---|---|
| 强化 | preview 后材料变化/主线 cap 变化/装备被移动 | commit 拒绝，零部分写，刷新 snapshot |
| 转换 | source 或 target 任一 lease 陈旧；同槽；不同 use；相同等级 | 前三者拒绝；相同等级按 D10 no-op |
| 进阶 | Web 伪造跳四阶/特殊材料；当前 tier 变化 | AS2 transition 拒绝，Web 规则不可绕过 |
| 插件 | 槽满、重复、tag 冲突、缺依赖、子弹不兼容 | 返回稳定错误码/文案 key，零扣料 |
| 拆卸 | direct dependent、cascade、多级未来数据、返还过程失败 | 预览列明闭包；提交全成或全败 |
| 总线 | timeout、断连、迟到 success、畸形回包 | `needs_reconcile`，不自动重放 |
| 无人值守入口 | live slot、attempt 不符、handoff 不新鲜、面板无首包 | Host 零业务写或超时停机留证；不得退化为 `/console` 直调业务 |
| 生命周期 | commit 时 sort/discard/move、切 view、关闭重开 | Web 写门阻止并发；旧 token 失效；无 handler 泄漏 |
| 持久化 | 每类成功后重启；每类拒绝后重启 | 成功状态与材料一致；拒绝不污染存档 |

---

## 10. 文件触点预案

此表用于恢复施工，不表示 T0 已修改这些文件。

| 层 | 可能触点 | 目的 |
|---|---|---|
| AS2 服务 | `scripts/类定义/org/flashNight/arki/item/EquipmentTuningService.as` | 单一权威、plan/token/commit、候选富注释分段 |
| AS2 测试 | 同目录专项 Test + `scripts/TestLoader.as` | 行为与原子性门 |
| 库存集合 | `ArrayInventory.as`、`DictCollection.as` | in-place touch / 材料批事务 |
| 领域规则 | `EquipmentUtil.as`、`equipment/TierSystem.as`、`equipment/TagManager.as` | 收口 transition 与依赖闭包，不重写计算器 |
| bridge | `scripts/逻辑系统分区/*_WebView.as`、asLoader manifest | gameCommand 与 response |
| legacy adapter | **已删除（2026-07-30）** | 六类旧 renderer writer 与样品栏脚本不再进入 asLoader；调制只走 EquipmentTuningService |
| Launcher | `src/Tasks/EquipmentTuningTask.cs`、TaskRegistry/Program、`WebOverlayForm.cs`、`LauncherCommandRouter.cs` | 独立 domain、view whitelist、写状态机 |
| Web | `inventory-workbench.js`、`inventory-runtime.js` 接缝、新 tuning runtime/view/harness、lazy registry | 组合右 View，不污染通用 primitive |
| XFL | `flashswf/UI/物品与技能相关界面/*` | 只在入口改道/旧 renderer 适配需要时触及 |
| 文档 | 本文、父工作台 ADR、canonical 迁移/测试文档（仅协议或门槛真的变化时） | 同轮同步事实 |

本轮在既有装备调制迁移工作树上增量施工；只调整共享工作台 primitive 的原生 `title` 输出为 `aria-label`，未改变 View/Slot 组合契约，也未触及 XFL/FLA 资产。

---

## 11. 实施与证据账本

### 11.1 已落地代码事实

- `EquipmentTuningService` 是七类 wire 调制的单一权威；`executeLegacy` 与旧 renderer adapter 已删除。Web 通过 `replace_mod` 原子组合“拆旧件及直接依赖 + 装新件”，分类 writer scan 未发现旁路直接写装备值或自行扣料。
- `ArrayInventory` 的原地装备批 touch 与 `DictCollection` 的计划式批 delta 已具备 preflight / apply / 单次 publish；失败不推进 revision，成功事务只发布一次可观察变更。
- `EquipmentTuningTask`、TaskRegistry、Program 与 `WebOverlayForm` 已接通 `equipment_tuning_response`；Host 重建严格 envelope，持有 panel/view session、write epoch、未知写水位与 exact reconcile barrier。
- `inventory-workbench.js` 已支持 `view=storage|tuning`；tuning view 以四栏组合覆盖七类 wire operation、候选 tooltip、前后投影、token-only commit、inventory external-write 仲裁、刷新失败重试与切离 session 失效。
- 调制候选 tooltip 只保留 `introHTML/descHTML/itemType/itemUse/text` 分段到 Web；合并 `html` 兼容字段已从 AS2、Host exact schema 与 Web fallback 同步删除。调制与共享 owned grid 不输出浏览器原生 `title`，精确信息由可见文案、`aria-label` 或共享富注释承担。
- `EQUIP_UI` 固定打开 Character Build；battlebox 背包装备动作和已穿戴装备都进入同一 Web 调制 View，不存在开关/capability/AS2 renderer 分支。
- `agent_control.openEquipmentTuning` 只接受当前 `cf7_agent_*` slot/attempt，固定发送正式 AS2 opener；runner 永久拒绝 live target 与 `--fresh`。

### 11.2 自动门

- 2026-07-17 的 Flash/测试数字是历史基线；2026-07-30 删除 adapter 后必须重新取得 fresh asLoader publish、Compiler Errors/Warnings 与专项 TestLoader 输出，不能继承本节旧产物。
- 专项仍须覆盖七类 wire 成功/拒绝、`replace_mod` 单事务返旧扣新、动态/永久强化上限、材料持有量投影、token/session、revision、插件元数据投影，以及缺 `sourceKind` 零写且不存在 adapter 旁路。
- `powershell -ExecutionPolicy Bypass -File launcher/build.ps1` 完整通过，runtime Core DLL 与 manifest 已按同次 build 刷新。
- `launcher/tests/run_tests.ps1` 全量 **945/945** 通过；装备 Task、AgentControl、Router、PanelHost 与生命周期 detach/reconcile 另有定向断言。
- `node tools/test-equipment-tuning-runtime.js` **25/25**、`node tools/run-equipment-tuning-harness.js` **56/56**、`node tools/run-kshop-harness.js` **76/76**、`node tools/equipment-tuning/run-checks.js` **6 suites** 全绿；三视口覆盖四个顶层操作、自动预览、顶部强化石核心的精确持有/消耗/剩余及仅保留 CTA 的紧凑 footer、`+13` 永久封顶且无 `+14` 幻象、在途旧核算不阻塞继续调整且只采纳最后目标、转换候选独立 projection 不改左栏 request/window、同强化度候选剔除、右栏直接选择目标及紧凑强化度角标、已安装件选择替换/原子双键 preview/专用单拆、DLS 插件树/面包屑、安全/快速 token-only 提交边界与级联回退、候选富注释分段、零浏览器原生 `title`、物品格同构数量角标、已安装插件/当前升阶材料图标、全工作台 DLS 基调与强化石动效降级、工作台顶栏帮助和紧凑密度。
- `node tools/validate-equipment-mod-ui.js` 通过：`mods=104 low=13 medium=31 high=49 special=11 scopes=armor:19,firearm:44,blade:14,fist:4,universal:14,underbarrel:9 tags=35 roles=6 pluginMaterials=100`，并覆盖四个特殊材料的跨文件唯一映射。

### 11.3 2026-07-17 反馈迭代收口

- 排版保持“强化度 / 交换 / 进阶 / 配件”四个顶层栏目，删除强化度内部的二级切换行；领域新增 `replace_mod` 组合写但不增加顶层栏目，token-only commit 不变，紧凑右栏最小宽度仍可稳定容纳四栏。
- 强化度借用技能研习的目标等级交互：步进、数值、滑杆、可点刻度和“升至当前上限”共用一个整数目标；停止调整后 debounce 只预览最终值，退役常驻“核算强化”，CTA 显示“强化至 +N · X 强化石”。snapshot 分离剧情/铁匠动态 `availableMaxLevel` 与永久 `hardMaxLevel=13`，封顶时直接显示终态而不再构造 `+14`；能量核心从材料行数组读取真实强化石持有量，并把 preview 消耗和强化后剩余合并在同一视觉单元，持有量放大为主信息，footer 不再重复装备 delta、材料行和核算说明，只保留提交 CTA。玩家文案不暴露“权威核算/一次性令牌”等实现术语。整个调制工作台复用 DLS 青/深蓝/黑铁语言，强化栏目继续使用现有强化石动态图；紧凑模式及 `prefers-reduced-motion` 使用静态首帧，避免重复动效。
- 进入互换态时，组合层通过 `InventoryCoordinator.readProjection()` 以 source 的精确 `majorType+use` 请求背包权威候选及新鲜 lease，并在右栏选择目标；projection 不写入协调器的可见 `_requests/_windows`，所以左栏 `filterKey/filterSpec`、面包屑和排序保持不变。Web 过滤同槽、不同 `use` 及同强化度装备，空态明确提示没有强化度不同的同类装备；AS2 继续保留类型校验和同强化度 no-op 防线，应对 projection 后状态变化。
- 插件定义子文件根层现显式声明 `modGrade/catalogScope`；`ui_presentation.xml` 只负责受控标签/色号/符号，材料物品 XML 不复制该分类。右栏以 Flash 候选投影构建“档级 / 用途 / 定位 / 状态”共享树和面包屑。
- 完整/紧凑密度控制器已注册 tuning View；紧凑模式只收紧配件/进阶候选目录，不隐藏最终提交动作；强化专属 delta 和材料变动已上移合并到 DLS 核心，不在 footer 二次呈现。候选严格对齐物品格 `48px` 格、`40px` 图标与 `4px` 间距，并复用同一持有数量缩写，精确值进入 `aria-label`，完整条目说明走共享富注释；1024×576 下用 25 项夹具锁定无需滚屏。当前升阶与已安装插件均使用真实物品图标呈现；点击已安装插件只选择待替换项，新旧 opaque key 在同一个 `replace_mod` preview 中送出，AS2 一次 commit 完成返旧、扣新和最终 mods 写入，另保留“仅卸下所选/卸下全部”。`?` 已提升到工作台顶栏并复用 modal，说明强化度/交换/进阶/配件、直接替换和“选择只预览、确认后提交”。

- 插件树继续使用共享 `FilterNavigator`，仅由 tuning host 的 DLS 色变量、晶体索引底纹和 scoped active state 改变视觉；未复制树构建、路径或事件语义。每个已安装插件新增专用单拆按钮。顶栏“配件｜安全/快速”默认安全并写入 `cf7.equipmentTuning.modConfirmationMode`；快速模式的预览完成后严格检查 `removedMods` 和材料 delta，只有单件安装、无连带的单件替换/卸下自动调用既有 `commit()`，其余情况提示并停在预览，`detach_all_mods` 和其他三类调制永不自动提交。wire payload、AS2 token、inventory external-write gate 与未知结果对账均未变化。

### 11.4 真机只读直达门

2026-07-16 从 `crazyflasher7_saves2` 只读播种 `cf7_agent_equipment_tuning`，原 seed SHA-256 在运行前后保持 `5666062ff3cc6e71f5b0524612746539e3007a2124e21fd363028af22bb63d13`，mtime 也保持 `2026-07-04T16:28:13.2434067+08:00`。本轮观察到 fresh handoff、同一 attempt 的 runtime loaded ack、正式 `openEquipmentTuning`，最终命中：

- `attemptId=eabdd3ffaa8c47f2a1a1e9078c3083ea`
- `panelInstanceId=panel.8dee348801de736.1`
- `viewSessionId=tuning.mrnmbzhf.2jk563`
- `snapshot callId=tune.mrnmbzhevjd1wz.1.1`
- `writeEpoch=0`

报告状态为 `snapshot_gate_reached`，明确 `uiBusinessClicks=false`、`businessWritesAttempted=false`。这证明无需搜索旧 AS2 按钮即可走完整生产 opener 与权威读链；它不证明真实业务 commit 或写后重启回读。后两者只可在专用克隆槽另取证，不能把 TestLoader 或 snapshot 结果冒充真机写证据。

---

## 12. 最终裁决

装备调制纵切已完成到 Web-only 待人工验收候选。不可逆成果包括：七类 writer 收口到 `EquipmentTuningService`，旧 renderer adapter/Host 专项/合并 tooltip fallback 删除，`EQUIP_UI` 与已穿戴来源进入 Character Build，以及旧聚合资产对 main 不可达。

当前未闭合的是七类真实业务写、普通 panel close 与真实存档写后重启回读在本次 `f01f4b121a4c…` 正式身份下的专项旅程；GUI 人工验收与正式发布状态机已闭环，严格总体状态为 `standard_entry_verified`。不得把上一冻结 source 的写入证据与本次 Tuning-only snapshot smoke 拼成同一身份验收。
