# 对白 v2 审阅修复：人力验收单

修复前基线：`7497f5cf429c81a787b06d9485b0cf5c9c422377`。本轮范围是回调超时单位、旧 status 查询的裁决范围，以及追加失败时的正文/暂停/事件移交；协议细节见[wire v2 契约](对白权威迁移-wire-v2协议与分期立项-2026-09-16.md)。

## 执行版本

- 从本工作区根目录 `CRAZYFLASHER7MercenaryEmpire.exe` 启动，使用平时测试的存档。
- C# 使用当前正式运行库；AS2 使用本轮重编的 `scripts/asLoader.swf`（2026-09-16 22:20，1,358,951 字节；SHA-256 `a396bdbf2929d779e0fbde2876ffe40f2f6fb8ac8c8c25808c4955c4ee44147f`）。
- 完整退出正在运行的旧游戏后再开始，使新 AS2 载入。本轮状态为 `HUMAN_ACCEPTANCE_PASSED`：维护者于 2026-09-17 反馈“可行，提交推送”。

## 三组验收

预计 5–10 分钟，顺序做一次即可。

| 组 | 操作 | 通过表现 |
|---|---|---|
| 1. 启动等待 | 冷启动启动器，在选档页停留 10–15 秒，再选测试存档进入游戏。 | 正常进入基地；没有“启动器握手失败 / callback timeout”，不永久停在加载页。 |
| 2. 地图与领取 | 用地图在两个可达基地地点间往返；在平时测试的关卡打开战利品面板，领取一件物品，再关闭面板。 | 地图点击能完成到达，面板正常打开和领取；没有无故超时/重复打开，关闭后角色可继续操作。 |
| 3. 对白与剧情 | 打开一次普通 NPC 对话，换句后按 Esc 关闭；再进入一个已解锁且带剧情对白的关卡，读完对白。 | 普通换句和关闭自然；剧情正文未提前消失，关卡后续只在对白结束后发生一次，随后正常恢复操作。 |

直接反馈哪组异常、出现了什么现象即可；如有异常，附大致时间和关卡/NPC 名，便于从日志定位。

## 机器验证与边界

自动回归覆盖旧查询与后发追加交叉、同时超时不丢操作、旧 rowCount 不覆盖新行集、超限后的新 rid 回退、4097 句的跨窗口正文/配图保留、暂停责任移交及事件只消费一次。人力验收聚焦真实游戏入口、交互与剧情表现。

| 验证 | 结果 |
|---|---|
| 回调期限与发送失败 | 9/9；同一回归可拦住原基线的 2 秒握手失败 |
| CS6 对白 | 408/408，fresh run `a74b89ab834e44039545ea13e2407797` |
| CS6 地图 | 60/60，fresh run `7f997f385c6d435c85b656147710b340` |
| CS6 战利品/结算 | 795/795，另含箱子仲裁 13 个场景 / 53 个断言；fresh run `54e8374ed00747489368dd51cb90f602` |
| asLoader 发布编译 | 新鲜 Compiler 0 错误 / 0 警告，SWF 已刷新；查询范围及回退方法/窗口标记已进入产物，函数体均低于 60000 字节门槛 |
| 其它检查 | 修改的 AS2 保留 UTF-8 BOM，文档治理和差异检查通过；正式 Core 与 HEAD 字节一致 |

本机证据保存在 `tmp/dialogue-review-fixes-20260916/`。2026-09-17 的维护者反馈记录为本轮修复的人力接受，并授权源码与配套 asLoader 一并提交推送；不外推为完整产品 `standard_entry_verified`。
