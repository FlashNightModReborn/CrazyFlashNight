# 任务界面-webview迁移-施工-2026-05-30

## 状态：限定旅程正式入口人验通过（2026-09-10）；残余复验尾巴转入 Epic #19（2026-09-22 回填）

> 本节原状态为“协议接入，收尾验证中”（缺 Flash fresh trace 与游戏内端到端复核），2026-09-22 按 #40 点查结论回填；文末“待验证”五项的逐条去向见该节批注。

截至回填日的证据边界：

- **机器门与限定旅程（2026-09-10）**：callId 回归专项（[修复记录](任务面板-我的任务详情callId回归修复-2026-09-10.md)）取得 fresh focused TestLoader 15/15、Launcher 全树 4869 passed / 0 fail、克隆槽真机 E2E（列表→详情→前往交付→跳场景+自动存档）与 publish 证据（Compiler 0/0、64KB 函数门、FFDec 定点）；测试员于正式入口 HUMAN_ACCEPTANCE_PASSED，含原生「任务」按钮路径，详情/前往交付/事件日志正常。该验收按专项声明只覆盖限定旅程，不外推完整产品 `standard_entry_verified`。
- **头像与交付修复（2026-09-11）**：`85b168e35` 修复任务单次返回与交付选择、补齐头像、防止结算后走门黑屏。
- **面板后续扩展**：事件日志任务树（WS6）、第一防线调度板聚合视图等超出本文原始范围，状态以各自专项为准，不由本文代签。

按 `agentsDoc/as2-web-panel-migration.md` §1 迁移分级，本文原始范围（snapshot/detail/面板生命周期/头像）已有构建、xUnit、fresh trace、游戏内端到端与限定人验，达到“生产可用”的限定旅程证据；但“待验证”节残余子项未闭环，整包不宣称“迁移完成”。残余项由 Epic #19「迁移线复验尾巴」条目按既有边界承接跟踪，#40 按本次点查结论以 not planned 归档退出。

## 已完成

### 第一阶段：harness.html 视觉原型（5/29 完成）
- 左侧可滚动任务图标列表（类型 + 任务名 + NPC头像占位）
- 右侧任务详情区（标题 → 描述 → 需求面板 → 奖励）
- 32×32 物品图标，CSS 动画（扫光、角标伸缩、NPC揭示等）

### 第二阶段：C#/Flash/JS 对接（5/30 完成）

**新建 4 个文件：**

| 文件 | 说明 |
|------|------|
| `launcher/src/Tasks/TaskTask.cs` | C# 桥接：cmd→action 映射，双层 callId 管理，10s timeout |
| `scripts/类定义/org/flashNight/arki/task/TaskPanelService.as` | AS2 服务：从 `_root.tasks_to_do` + `TaskUtil.tasks` 读取数据 |
| `scripts/逻辑系统分区/任务系统_WebView.as` | AS2 安装入口（3行） |
| `launcher/web/modules/tasks/task-panel.js` | WebView 面板 JS：Panels.register，Bridge 通信 |
| `launcher/web/css/task_panel.css` | 任务面板生产 CSS（5/31 从 JS 内联样式拆出） |
| `launcher/web/modules/tasks/assets/*.png` | 任务面板生产美术资源（5/31 从 dev 原型目录提升） |

**修改 8 个已有文件：**

| 文件 | 改动 |
|------|------|
| `launcher/src/Guardian/NotchOverlay.cs` | OtherButtons 添加"新任务界面" (NEW_TASK_UI) |
| `launcher/src/Guardian/Hud/NotchWidget.cs` | 同上 |
| `launcher/src/Guardian/LauncherCommandRouter.cs` | 添加 case NEW_TASK_UI → OpenPanel("tasks") |
| `launcher/src/Guardian/WebOverlayForm.cs` | field/setter/HandlePanelMessage 路由/ClearPending/ResolvePanelCloseGameCommand |
| `launcher/src/Program.cs` | 创建 TaskTask，RegisterAll，SetTaskTask |
| `launcher/src/Bus/TaskRegistry.cs` | 参数 + RegisterAsync "task_response" |
| `launcher/web/modules/panels-lazy-registry.js` | 注册 lazy panel 'tasks' |
| `scripts/asLoader/LIBRARY/asLoader.xml` | #include 任务系统_WebView.as |

## 数据协议

### snapshot（打开面板时拉取全部任务概要）
- JS → C#: `{type:"panel", panel:"tasks", cmd:"snapshot", callId:"..."}`
- C# → Flash: `{task:"cmd", action:"taskSnapshot", callId:N}`
- Flash → C#: `{task:"task_response", callId:N, success:true, tasks:[{taskId, title, type, npcName, satisfied}]}`
- C# → JS: `{type:"panel_resp", panel:"tasks", cmd:"snapshot", callId:"...", success:true, tasks:[...]}`

`satisfied` 必须复用游戏内交付权威判定 `_root.taskCompleteCheck(index)`：关卡要求完成后，还需同时满足提交/持有物品与特殊需求。禁止只依据 `requirements.stages.length == 0` 显示完成图标，否则交物型任务会出现假完成。

### detail（加载单个任务详情）
- JS → C#: `{type:"panel", panel:"tasks", cmd:"detail", callId:"...", index:N}`
- Flash → C#: `{task:"task_response", callId:N, success:true, taskData:{taskId, type, title, description, stageReq, itemReqs, npcName, rewards}}`
- C# → JS: `{type:"panel_resp", panel:"tasks", cmd:"detail", callId:"...", success:true, taskData:{...}}`

## 已知问题与修复

### BOM 缺失（已修复）
- **症状**：Flash AS2 编译器跳过无 BOM 的 .as 文件，gameCommands 未注册，数据请求无响应
- **影响文件**：`TaskPanelService.as`、`任务系统_WebView.as`
- **修复**：为两个文件添加 UTF-8 BOM (`\xef\xbb\xbf`)
- **参考**：PetPanelService.as、战宠系统_WebView.as 均以 UTF-8 BOM 开头

### 面板黑屏（已修复）
- **症状**：面板打开后全黑，无任何内容渲染
- **根因**：createDOM 根 div `_el` 未设 `width/height: 100%`，子元素 `.task-panel-container` 的 `height: 100%` 计算为 0
- **修复**：添加 `_el.style.width = '100%'; _el.style.height = '100%';` + `return _el;`

### 数据加载失败 → 左侧正常，右侧无任务数据（5/30 第二次测试）
- **根因**：`handleDetail` 的 AS2 响应中 `task` key 重复：
  ```as2
  sendResponse({ task: "task_response", ..., task: task });
  //             ^^^ 信封字段 = "task_response"    ^^^ 数据对象 — AS2 对象字面量中后者覆盖前者
  ```
  LiteJSON 序列化后 `task` = 数据对象（非 `"task_response"`），C# 的 `msg.Remove("task")` 错误地删除了任务数据，JS 收到的响应无 `task` 字段 → "无任务数据"。
- **修复**（5/30 23:XX）：
  - AS2：`task: task` → `taskData: taskData`（避免与信封 `task: "task_response"` 冲突）
  - JS：`renderTaskDetail(data.task)` → `renderTaskDetail(data.taskData)`
- **对比**：`handleSnapshot` 正常，因数据字段是 `tasks`（复数），不与 `task` 信封冲突。PetPanelService 所有响应数据字段名（`snapshot`、`adoptable` 等）均不与信封 `task` 冲突。

### NPC 头像加载（5/30 23:XX）
- **需求**：从 `flashswf/portraits/profiles/{npcName}.png` 加载 NPC 头像
- **实现**：
  - C#：新增 `TryRegisterGameAssetsVirtualHost`，映射 `cfn-assets.local` → `{projectRoot}/flashswf/`
  - JS：`avatarUrl(npcName)` → `https://cfn-assets.local/portraits/profiles/{npcName}.png`
  - 无对应文件时 `onerror` 回退到 `无头像.png`
  - 左侧任务图标 + 右侧 NPC 详情区域均使用 `<img>` 代替文字占位

### 面板黑屏（已修复）

## 待验证（2026-09-22 逐条去向批注）
1. ~~Flash fresh trace：确认编译并注册~~ **已过（2026-09-10）**：专项 fresh focused TestLoader 15/15 与 fresh trace、Compiler 0/0、FFDec 定点证实进 SWF；同批克隆槽 E2E 经真实 wire 实际调用 taskSnapshot/taskDetail/navigate，强于注册静态检查。
2. 游戏内端到端：**限定旅程已过（2026-09-10）**——打开、snapshot 列表、detail 详情、前往交付后关闭跳场景 + 自动存档，经克隆槽 E2E 与正式入口人验（含原生「任务」按钮路径）。**残余**：`stages=[]` 但物品未满足任务的 `satisfied=false` → 补齐物品 → `true` 翻转用例未见记录（E2E 只构造了 satisfied=true 形态）；普通关闭（不跳场景）后的输入焦点与 HUD 恢复无逐条断言。
3. NPC 头像：**已过（2026-09-10 人验详情含 NPC 区域；2026-09-11 `85b168e35` 补齐头像）**。**残余**：头像文件缺失时回退 `无头像.png` 的分支未见验收记录。
4. ~~LiteJSON 中文字段~~ **事实覆盖**：生产使用与真机截图中中文标题/描述/奖励均正常渲染。
5. ~~`_root.tasks_to_do` 结构假设~~ **事实覆盖**：09-10 E2E 注入真实任务结构与长期生产使用均正常。

残余项（第 2 条的 satisfied 翻转与普通关闭焦点/HUD、第 3 条的缺失回退）转入 Epic #19「迁移线复验尾巴」按既有边界跟踪；#40 按 2026-09-22 点查结论以 not planned 归档退出。
