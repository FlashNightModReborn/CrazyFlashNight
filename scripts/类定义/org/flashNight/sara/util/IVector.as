import org.flashNight.sara.util.*;

/**
 * IVector 接口
 * 定义了向量运算的基本方法，适用于二维和三维向量。
 * 实现此接口的类应提供向量的初始化、复制、算术运算、几何运算、归一化、插值等功能。
 */
interface org.flashNight.sara.util.IVector {

    // 返回当前向量的字符串表示
    function toString():String;
}
