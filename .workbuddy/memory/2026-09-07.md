# 2026-09-07

## 立绘 / 头像资产接入路径（调研，无代码改动）
- **头像（profile）**：`flashswf/portraits/profiles/<名字>.png`，400×400，按需按名字直读；AS2 `刷新NPC头像`（任务栏 UI 用 `taskData.finish_npc`），Web `launcher/web/modules/tasks/task-panel.js` 走 `cfn-assets.local/portraits/profiles/`，缺省回退 `无头像.png`。**无需任何登记表**。
- **对话立绘**：`UI交互_lsy_对话框UI.as`。char 字段 `角色#表情` → 名字命中 `flashswf/portraits/list.xml`（`loadPortraitDict`）走外部 SWF `flashswf/portraits/<名字>.swf`，`gotoAndStop(表情)`（帧标签=表情名，找不到回退"普通"）；否则走内置 `对话框界面.swf` 的 `对话框肖像` 元件（帧标签=角色名，内含 肖像.肖像.man.头.头.基本款/装扮 嵌套 rig）。两边都无帧 → gotoAndStop 无效，残留上一立绘。
- 外部立绘规格：舞台 1024×576（ffdec 验证 Andy Law.swf displayRect 20480×11520 twip），帧标签顺序 `普通/微笑/大笑/严肃/坏笑/愤怒/悲伤/...`，首帧 DoAction 调 `_root.初始化人物立绘(this)`；Web 取景窗口 x30,y30,880×375。
- 新增外部立绘：复制现有 portrait FLA→改名→换美术→发布 SWF→在 `flashswf/portraits/list.xml` 加 `<portrait>名字</portrait>`→重跑 `tools/bake-dialogue-portraits.py`（需 `tools/ffdec/ffdec-cli.exe`）刷新 `launcher/web/assets/dialogue-portraits/manifest.json`。
- 名字大小写敏感，仅 Boy/King/Pig 有 `立绘大小写字典` 兜底；主角 `$PC_CHAR/主角模板/玩家` 无静态立绘（Web 走 dressup rig）。现有 91 entry 中不含 阿卡/虎妙/混沌王/人修罗，三个新 NPC 都需新建。

## StageEvent 新增 MinAliveEnemies 通用条件 + 真九命猫妖撤退计时器（asLoader 层改动，待编译）
- **StageEvent.as**：新增可选字段 `minAliveEnemies`（XML `<MinAliveEnemies>n</MinAliveEnemies>`，缺失/<=0 不限制）；`满足存活敌人条件()` + `统计场上存活敌人数()`（用 `_root.帧计时器.获取敌人缓存(玩家,1)` 逐个剔除 element/已删除/hp<=0；取不到玩家时放行 -1）。**StageEventHandler.as**：`checkParameter` 通过后、execute 前插入 `if(!event.满足存活敌人条件()) continue;`（WaveFinished 只发布一次，等价不触发）。
- **九命猫妖.as 真九命猫妖初始化**：新增可选撤退计时——退场毫秒来源 `自机.退场时间`（关卡 XML Parameters / 修改器写入，优先）> 装备 initParam `参数对象.退场时间`；`_root.帧计时器.添加单次任务(fn, 退场毫秒)` 到时执行 `剩余复活次数=0; respawn=false; 状态改变("血腥死")`。防重入用 `自机.退场计时器已启动`；回调里 `已删除===true` 时静默取消。TaskManager interval 是**毫秒**（内部 framesPerMs=30/1000 折帧，32000ms=960帧），与 WaveSpawner 帧计时同源（30FPS）。
- **挑战混沌王.xml**：三个 WaveFinished 均加 `<MinAliveEnemies>1</MinAliveEnemies>`；30 秒虎妙 Parameters 带 `退场时间:32000`（入场后32秒≈62秒时刻真实撤退，撤退对话在 60 秒 WaveFinished(1) 先行）；180 秒重入场的虎妙不带退场时间（常驻）。
- 生命周期参数链：`单位函数_fs_装备生命周期配置.as` `initFunc(反射对象, init.initParam||{})`；关卡 Parameters 克隆到单位属性，所以单位级参数读 `自机.退场时间`。

## 单位声音库机制 + 真九命猫妖独立声库参数
- **呐喊声音库 = 单位实例字段**：`男/女_攻击呐喊_库`、`男/女_中招呐喊_库`、`男/女_击倒呐喊_库`（wav 文件名数组）。主角模板默认库写在 `单位函数_fs_aka_玩家模板迁移.as` 玩家初始化 ~2199-2204（女攻击=01/02/03_kin_a+19_kin_nage，女中招=women_hit0-6，女击倒=women_die1-5）；敌人侧 `_root.敌人函数.配置音效(类型,target)`（僵尸/人类）写同名字段，目前无调用方。消费方：`攻击呐喊/中招呐喊/击倒呐喊` 按 `性别` 选库随机播（击倒 300ms 间隔）；**死亡音效也走 击倒呐喊()**（玩家模板死亡流程 1591-1594），三库即完整换声。跑步音效 soundfootstep_female 仅控制目标播放，与 NPC 无关。
- **真九命猫妖初始化新增参数化独立声库**：`自机.声库攻击/声库中招/声库击倒`（关卡 Parameters/修改器写入，优先）> initParam 同名键；值 = 竖线分隔 wav 文件名（`Jk_att_H_1.wav|Jk_att_H_2.wav`）。**关卡 Parameters 的逗号/冒号是保留分隔符（ObjectUtil.cloneParameters 按 , 再 : 切，para.length===2），文件列表必须用 `|`**。按 自机.性别 前缀写 女_/男_ 库，只覆盖给出的项。
- 现成独立女声素材（sounds/export/人物/，共 213 文件）：JK 全套（Jk_att_H/L_1-3、Jk_hit_L_1-5、Jk_die_1-2、Jk_fly、Jk_getup、Jk_jump）、闪6女特工*4、女角色飞踢.mp3、女声_Buster Wolf / Power Wave、登上明星女声.wav。
- **空声库=静音语义落地**：玩家模板（主角函数）与敌人模板（敌人函数）的 攻击呐喊/中招呐喊/击倒呐喊 六个函数全部加了 `arr == null || arr.length == 0 → return` 空库守卫（否则空数组会让 getRandomArrayElement 返回 undefined 传给 playSound）。
- **声库改为生命周期默认绑定（用户明确要求，非关卡传参）**：`真九命猫妖初始化` 内无条件按 自机.性别 前缀把 攻击/中招/击倒 三库置为**虎妙专属默认配置**（当前空数组=静音），后续换配音直接改 九命猫妖.as 里的数组（注释含 JK 示例）。其后的 `声库覆盖` 参数块保留为可选单实例覆盖（自机.声库攻击/声库中招/声库击倒，竖线分隔；Parameters 的逗号/冒号是保留分隔符 cloneParameters 按 , 再 : 切）。挑战混沌王.xml 已删掉两个虎妙的 声库攻击:/声库中招:/声库击倒: 传参，头注释同步改写。
- **注意**：默认绑定对一切走 真九命猫妖初始化 的单位生效；已按用户要求补 `_name != _root.控制目标` 守卫（真·九命猫妖项链只给虎妙、玩家拿不到，仅防御）。
- **九命猫妖周期新增动作模组轮换**（用户要求）：非控制目标单位每 300 帧（周期任务 interval=0 每帧执行，约 10 秒）从 `兽形拳/兽王崩拳/破极拳/截拳/迅驰腿/强化/拳击` 真随机取一个写 `自机.空手动作类型`（= 装备 actiontype，运行时由 `空手攻击路由.获取普攻连招首帧标签` 拼 "XX1连招"，下次平A 生效）。七个值均已在 `data/items/asset_source_map.xml` 有对应 `空手攻击容器-XX1连招` 资产。

## 人形怪（主角模板单位）AI 定制：需求 + 调研（两份文档，待用户确认方向后开发）
- 产出：`docs/人形怪AI定制-需求记录-2026-09-07.md`（R1~R7 需求原文不缩减）、`docs/人形怪AI定制-调研结论与开发方向-2026-09-07.md`（含文件行号证据 + 拟定方向 A/B/C）。`node tools/validate-doc-governance.js` 通过。
- **核心结论**：① AI 选型唯一入口 `UnitAIInitializer.as:5-21`，优先级 `unitAIType > 使用人形怪AI > 佣兵数据 > 允许拾取 > Enemy`，关卡 `<Parameters>` 即可注入 → 新增一套 AI 只改 `BaseUnitAI.as` + 新建 behavior（**禁止对人形写 unitAIType="Mecenary"**）；② AI tick 固定 **每 4 帧**（`UnitUpdateWheel WHEEL_SIZE=4`，不可按单位配置）；③ `zrange` 被 `UnitAIData.as:111` **硬编码为 10** 并回写 `y轴攻击范围`；④ Z 轴抖动成因 = 4 帧决策 × 跑Y(≤5) ≈ 20px ≥ 2×zrange，**根治点在 `Mover.move2D`（`Mover.as:134-146`）加步长截断**，切走路（走Y≤2.5）只能缓解；⑤ AI 唯一主动技能源是 `已学技能表`（`玩家模板迁移.as:374-386` 结构），**随机生成 + 按 名字+等级+技术 全局缓存**，`units.json` 无技能字段，只能在 `初始化可用技能()`(:2230) 后覆写；⑥ 技能互切根因 = `ActionExecutor.canInterruptBody:152-156` 技能间 `<=` 放行 + `commitFrames`(8~16帧) 远短于真实动画；CD 已是技能级（`SkillCandidateStrategy.as:46`）；平A 基线 0.5 低于技能基线，需加保底；⑦ 战技只存 `主动战技[攻击模式]`、仅玩家按键触发、AI 不碰，需 push 进 `已学技能表` + ActionExecutor 分支 + 补 CD 门；⑧ 出场打拳不是初始化强制，是 `HeroCombatModule.chase:152-158` 首 tick 触发，现成冻结门是 `_warlordActionAiHeld`（`UpdateEventComponent.as:103-105`）。

## 空手专用 AI（HeroUnarmed）开发文档已产出，待评审
- 文档：`docs/人形怪AI定制-空手专用AI开发文档-2026-09-07.md`。决策：①**默认 AI 是他人模块，绝不动**（ActionArbiter/ActionExecutor/UtilityEvaluator/MovementResolver/Mover/HeroCombat*）；②**自研技能脑**（只调现成 `AIEnvironment.routeSkill` 与 `_root.战技路由.战技标签跳转_旧`）；③ Z 轴精确对齐**只在靠近时接管**（X<250 且 |Z差|<倍率×每帧Z速度 且方向为靠近），用我们自己的每帧任务 + `Mover.move2D` 末步截断；④ 残血=可上下跳脱离 + 迂回上限 2~3 秒且优先上下远离、**禁止逛街**、立刻回战；喝药独立（<40% 必喝、<70% 概率喝）；⑤ 技能注入=生命周期覆写 `已学技能表` + `初始化可用技能()` 加 1 行"已注册则跳过随机"兼容；⑥ 平A 保底 25%。
- 关键事实：`unitAIType` 必须在 attachMovie 阶段就绪（`UnitAIInitializer` 早于 `DressupInitializer`）→ **不能**在装备生命周期里设；`真猫妖头套`（`防具_40+级.xml:2701`，头部，挂 `九命猫妖初始化`）与 `真九命猫妖项链`（`防具_颈部装备.xml:2126`，挂 `真九命猫妖初始化`）是**两件不同装备**；战技现成 API：`装载主动战技({skillname,cd,hp,mp,level}, 攻击模式)`（`玩家模板迁移.as:1809`）、`战技标签跳转_旧(unit, 名)`；飞身踢已有空手实现（`单位函数_雾人_aka_fs_主动战技.as:43`），咒针条目待确认；`AIEnvironment.useHealPack(unitName)` 为喝药 API。
- 待用户确认 3 项：生命周期 attr 挂头套还是项链；低血 commitFrames 语义（本档按"更坚持 24~36 帧"）；兵种330 虎妙实际是否穿真猫妖头套（否则兜底在 behavior 激活时自写技能表）。② AI tick 固定 **每 4 帧**（`UnitUpdateWheel WHEEL_SIZE=4`，不可按单位配置）；③ `zrange` 被 `UnitAIData.as:111` **硬编码为 10** 并回写 `y轴攻击范围`；④ Z 轴抖动成因 = 4 帧决策 × 跑Y(≤5) ≈ 20px ≥ 2×zrange，**根治点在 `Mover.move2D`（`Mover.as:134-146`）加步长截断**，切走路（走Y≤2.5）只能缓解；⑤ AI 唯一主动技能源是 `已学技能表`（`玩家模板迁移.as:374-386` 结构），**随机生成 + 按 名字+等级+技术 全局缓存**，`units.json` 无技能字段，只能在 `初始化可用技能()`(:2230) 后覆写；⑥ 技能互切根因 = `ActionExecutor.canInterruptBody:152-156` 技能间 `<=` 放行 + `commitFrames`(8~16帧) 远短于真实动画；CD 已是技能级（`SkillCandidateStrategy.as:46`）；平A 基线 0.5 低于技能基线，需加保底；⑦ 战技只存 `主动战技[攻击模式]`、仅玩家按键触发、AI 不碰，需 push 进 `已学技能表` + ActionExecutor 分支 + 补 CD 门；⑧ 出场打拳不是初始化强制，是 `HeroCombatModule.chase:152-158` 首 tick 触发，现成冻结门是 `_warlordActionAiHeld`（`UpdateEventComponent.as:103-105`）。
- **假设核实结论**（代替实机验证的静态确认）：① `获取敌人缓存(t,i)` 签名 = (观察单位, 缓存更新间隔帧数)，i=1 每帧刷新 ✓；② `状态改变("血腥死")` 真实存在（战技路由 hp<=0 即进血腥死、敌人模板迁移同款）✓；③ `respawn=false` 被 DeathEventComponent/RespawnEventComponent/ZeroHPDetector 消费，真实阻止复活 ✓；④ **`已删除`/`剩余复活次数` 在代码库中不存在**（用户口述字段，照写但仅作自定义属性）——存活判定改用代码库惯例 `_name == undefined`（removeMovieClip 后 _name 变 undefined），element 判地图元件与 `无限过图.as:174` 同口径；⑤ 撤退回调额外加 `复活上限 = 0`：若单位同时挂九命猫妖周期，防止其绕过 respawn 用扭转乾坤复活。

## 空手AI开发文档修订：三个待定项已核实并落档
- **attr 挂 `真猫妖头套`（已确认）**：`units.json` 兵种330「虎妙」`data.head="真猫妖头套#13"`、`neck="九命猫妖项链#13"` → 生命周期必定执行。已核实挂载唯一性：`九命猫妖初始化/九命猫妖周期` **仅**真猫妖头套（`防具_40+级.xml:2745-2760`）、`真九命猫妖初始化` **仅**九命猫妖项链（`防具_颈部装备.xml`）→ 声库代码从 `真九命猫妖初始化` 迁到头套 init **零回归**。新增独立 attr `attr_虎妙头套` → `虎妙头套初始化/周期`（新文件 `scripts/逻辑/装备函数/虎妙头套.as`），声库+技能库+AI迁移+AI参数全放这里；动作模组轮换留在 `九命猫妖周期` 不动。
- **生命周期内可强制迁移 AI（已确认可行，且优于兵种注入）**：`自机.unitAI.destroy(); 自机.unitAI = new HeroUnarmedAI(自机);`。依据：① `UpdateEventComponent.as:104` 每 4 帧 `target.unitAI.update()` 换对象照常驱动；② `StaticDeinitializer.as:30` 调 `unitAI.destroy()`，我们实现同名方法即可；③ `单位函数_fs_佣兵ai.as:9` 旧佣兵 AI 见 `unitAI != null` 即 `stop()` 自退，替换不会复活它；④ 全仓**无** `unitAI.type` / `instanceof BaseUnitAI` 用法，外部只读 `unitAI.data.arbiter` 且 `DeathEventComponent.as:27`、`ImpactStateHandler.as:39-45` 全 null 守卫（仅诊断埋点）。→ **UnitAI 目录零改动**，BaseUnitAI 不再需要加 case。兵种注入不可行原因：`units.json` 无 `unitAIType` 字段，且 `UnitAIInitializer`（`ComponentInitializer.as:21`）早于 Dressup，拿不到"是否戴头套"。关卡 `<Parameters> unitAIType` 虽可行（`WaveSpawner.as:328-330` 在 spawnEnemy 前 cloneParameters），但依赖加 case，仅作备选。
- **低血 commit 语义（已确认，与原档相反）**：低血 HP<50% → `commitFrames=30` 且**允许**被更高优先级技能打断（灵活保命）；高血 → 以动画播完为准，不主动打断。
- 文档已更新（决策 D8/D9/D10、改动边界收缩为仅 1 处兼容行、注入方式、时序图、P0/P1 阶段、V4/V13 验收、风险区改为"已解决+未决"）。`node tools/validate-doc-governance.js` 通过。
- 未决：佣兵系统生成的虎妙是否也走 `DressupInitializer`（否则不迁移 AI，需在佣兵生成处另找注入点）；`退场时间` 退场计时器是否一并从项链迁到头套。

## 2026-09-07 晚：P0 重构——撤销独立虎妙头套初始化，合并进九命猫妖初始化

- **维护者拍板：不要加新的初始化**。真猫妖头套本就挂 `attr_九命猫妖`（九命猫妖初始化/周期），虎妙共用配置（声库默认绑定 / AI 行为参数 / 空手技能库覆写 / HeroUnarmed AI 强制迁移 / Z 轴精确对齐每帧任务）**全部合并进 `九命猫妖.as` 的 `九命猫妖初始化`**，不再单设 `虎妙头套初始化`。
- **已回滚/清理**：删除 `scripts/逻辑/装备函数/虎妙头套.as`；`frame37.as` 撤掉 include；`防具_40+级.xml` 移除 `attr_虎妙头套` 块并把 `出场保护帧=90`、`平A保底概率=0.25` 回填进 `attr_九命猫妖` 的 initParam；README 索引更新。
- **函数内守卫**：合并段开头 `if (自机._name == _root.控制目标) return;`（玩家穿戴头套不接管）；`真九命猫妖初始化`（项链）不变，退场计时器仍项链专属。
- **分工边界（最终版）**：`九命猫妖初始化`（头套挂载）= 九命复活 + 所有虎妙共用配置；`真九命猫妖初始化`（项链挂载）= 复活上限/概率 + 退场计时器。
- **P0 完成前遗留确认**：`单位函数_fs_aka_玩家模板迁移.as` 的 `初始化可用技能()` 兼容分支（已学技能表非空则跳过随机）已确认落盘（:300-304），注释改为"如九命猫妖初始化"。
- **注意**：合并段引用的 `HeroUnarmedSkillTable / HeroUnarmedAI / HeroUnarmedMoveHelper` 属 P1 待建类，类文件并入编译前该段会导致编译失败（AS2 直接类引用是编译期解析）——P1 需尽快落地这三个类。开发文档 D8 已追加变更记录。

## 2026-09-07 深夜：P1~P5 全部编码完成（待编译+实机验收）

- **新增 6 个 AS2 类**（`scripts/类定义/org/flashNight/arki/unit/UnitAI/behavior/`）：`HeroUnarmedAI`（顶层，update/destroy/isHeroUnarmed）、`HeroUnarmedBehavior`（根机：Selector/Combat/Evade，Selector 每tick重搜防停留）、`HeroUnarmedCombatModule`（Chasing/Engaging 子机）、`HeroUnarmedSkillBrain`（自研技能脑：分层裁决0-3同层随机/平A保底25%/低血30帧可打断高血打完一套/霸体不重复/单图一次标记在 自机.单位已用永久技能/喝药独立轨 0.4必喝0.7插值）、`HeroUnarmedSkillTable`（独立标定20条+战技2条，空中组{震地,地震,踩人}）、`HeroUnarmedMoveHelper`（alignTick 末步截断 + shouldTakeOverZ 三条件）。
- **修复 P0 遗留 bug**：Z对齐每帧任务原来用 `添加或更新任务`——TaskManager.addOrUpdateTask(:505) 新建任务 repeatCount=1 **单次执行即死**（AchievementService 注释早已警告"单次任务陷阱"）。已改 `添加生命周期任务`（无限循环+unload自动清理）。
- **咒针已核实无独立条目**：按 贯空天盖战技 模式——写 `自机.手部发射子弹属性`（普通咒针：威力10×内力/速度35/毒2000/霰弹3）→ 战技路由 `"手部发射"`。
- **关键框架事实（本次核实）**：FSM_StateMachine 状态机级回调 super(null, enter, null) 重入重置；FSM_Status 参数序 (action, enter, exit)；transitions.push(from,to,fn,isGate)；destroy() 置 onAction 为 noop；`添加生命周期任务`=repeatCount true+unload回调。
- **战技/技能释放契约**：技能→`AIEnvironment.routeSkill`（内部 状态改变("技能")），战技→`_root.战技路由.战技标签跳转_旧`（状态改变("战技")）；释放方需自写 `sk.上次使用时间=getTimer()` 与 `self.技能等级=min(ceil(等级/10),10)`（默认管线 mp/消耗不由路由扣，技能帧自行处理）。
- 追击期跑完整技能脑（远程/位移技能追击中可放）；Evade 进入 50% 概率小跳/闪现脱离（先设 上行/下行 远离方向）。
- 全部待 Flash CS6 编译 + 实机验收 V1~V13（验收清单在开发文档 §9）。
