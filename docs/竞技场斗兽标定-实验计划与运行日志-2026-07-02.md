# 竞技场斗兽标定：实验计划与运行日志

**文档角色**：斗兽标定无人值守实验的状态文档、运行日志索引、剪枝决策记录和跨会话交接入口。本文不替代 canonical 设计：AS2 / C# / Node L0 协议以 [竞技场斗兽标定平台-架构设计-2026-06-29.md](竞技场斗兽标定平台-架构设计-2026-06-29.md) 为准；Campaign 状态、模型分工、消融和人类 PVE 验收以 [Campaign Loop 与模型消融验收 ADR](竞技场斗兽标定-Campaign-Loop与模型消融验收-ADR-2026-07-31.md) 为准。

**历史实验基线**：首批数据来自 2026-07-02 至 2026-07-04 的实机工作流。**当前入口复核基线**：HEAD commit `286e531f214264304e490b122f533e13163b5b83`（2026-08-27）上的当前工作树；现役 runner 使用精确 legacy HTTP 端口和进程生命周期授权，本文旧批次证据不因此升级为 Agent Runtime 证据，当前 Gate A 增量也不冒称已提交或发布。

**当前阶段目标**：`GATE_F0_ENGINEERING_READY / FORMAL_SOAK_PAUSED / ROUTING_FIX_READY / NOT_FORMALLY_APPLIED`。durable Campaign Supervisor、三 profile 真实模型输出、一次盲化模型复核、55 个 fresh Gate D run、单候选 30 样本完成门和两场 `1 名 × Lv10 人形佣兵` 等效标签均已闭合；Gate E 未自动记录的胜负、时长、承伤、输出、残血等客观 telemetry 保持未报告，不要求维护者事后补猜。Gate F 首轮三份 formal soak 已保留 30 条 exact-runtime 事实：前两份完成，第三份因 B11/C12 候选原向 timeout 正确暂停，运行时、落盘、正常关闭和存档隔离均通过。新计划已用同 runtime exact-policy 双向完成证据准入稳定 soak 代表，但尚未重新绑定 clean source/formal runtime 或重跑；没有生成或应用正式数值 patch。无人值守默认只运行在 `cf7_agent_arena_calibration` 专用槽位上，真人 PVE 使用独立克隆槽并在收尾后清为 0；正式玩家槽、`--fresh`、坏档 ready 都是安全阀负例。

---

## 1. 当前可用能力

- Launcher 已提供 `arena_calibration` 任务，控制动作为 `startBatch`、`status`、`abort`。
- Launcher 已提供 `agent_control` 任务，控制动作为 `status`、`start`、`revealOk`、`cancel`、`shutdown`，用于无人值守外层从启动器进入专用标定存档。
- C# 后台会读取 `tmp/arena-calibration/batches/<batchId>/case_manifest.json`，逐局下发 AS2 `arenaCalibrationRun`。
- AS2 端会进入专用斗兽标定竞技场，跳过主角入场，按蓝方/红方 roster 生成斗兽单位。
- 结果按 JSONL 写入 `logs/arena-calibration/<batchId>-results.jsonl`。
- Node 分析器可从 JSONL 生成 `summary.json`、`summary.md`、`next_batch.json`；`tools/arena-calibration/run-unattended.js` 可编排“启动/进档/等待 ready/跑 batch/分析/报告”。
- `intake-workbook.js` 可直接从 `.xlsx` 读取测试群单元格，把工作簿 hash、sheet、cell 和 cell value hash 作为来源；原工作簿保持只读，内部字段由工具生成。
- `run-checks.js` 使用 Ajv 2020 编译 39 个 schema，并完成 63 项检查；同时覆盖 Gate B durability、Gate C/D/E 机器契约、数值人形佣兵等效响应，以及 Gate F plan/window/decision/attention/receipt/status 与故障负例，只读取 schema 文件不算通过。
- 现役 unattended runner 会先校验生产 JSONL 原始行及 exact manifest/case/repeat/去重绑定，再做 normalization / summary；`authorityContext`、单位结果和阶段派生单位 provenance 不再被投影丢失。
- `campaignctl.js` 以 exact producer registry + 短期 idle grant 管理 durable journal、单 writer、schedule/import/pause/status；`evaluationctl.js` 冻结 shadow 请求、拟合配对强度、生成 bridge/anomaly plan，并准备或验证 PVE packet。`build-gate-f-week-plan.js` 生成 10–25 run 的星期级 frozen-source 草案，`gate-fctl.js` 负责 clean source/exact runtime freeze、有界 window、短 shard 执行、逐行 exactly-once import、attention/exception/health receipt 和只读 status。`run-human-pve-session.js` 只负责 exact runtime、受控 encounter 与 owned-file 控制信号；`finalize-human-pve.js` 只摄取维护者原话和既有运行证据，不生成模型输出、真人评价或缺失 telemetry。
- 当前实机已验证小批次连续运行、abort 后复用、批次完成后状态查询，以及无人值守冷启动 pilot 全链路均可工作。

首批实机数据：

| 字段 | 值 |
|---|---|
| batchId | `first-log-20260702-172046` |
| manifest | `tmp/arena-calibration/batches/first-log-20260702-172046/case_manifest.json` |
| results | `logs/arena-calibration/first-log-20260702-172046-results.jsonl` |
| summary | `tmp/arena-calibration/batches/first-log-20260702-172046/summary.md` |
| rows | 45 |
| cases | 9 |
| status | 45 `finished`，0 `timeout`，0 `error` |
| wall time | 约 519 秒 |
| max single run | 781 帧 / 26019 ms |
| runtime signal | `ZeroHPDetector` 触发 6 次并强制击杀零血单位，结果行仍成功 |

无人值守冷启动 smoke：

| 字段 | 值 |
|---|---|
| batchId | `unattended-smoke-20260704-3` |
| manifest | `tmp/arena-calibration/batches/unattended-smoke-20260704-3/case_manifest.json` |
| results | `logs/arena-calibration/unattended-smoke-20260704-3-results.jsonl` |
| report | `tmp/arena-calibration/unattended/20260704T052539Z-unattended-smoke-20260704-3/run-report.json` |
| summary | `tmp/arena-calibration/unattended/20260704T052539Z-unattended-smoke-20260704-3/summary.json` |
| rows | 1 |
| status | `completed`，1 `finished`，0 `error`，0 `timeout` |
| runtime signal | `agent_control` 从冷启动进入 `readyForArenaCalibration=true`；AS2 进入 no-player 标定场，结果 `winner=blue`，581 帧 / 19504 ms |
| 修复记录 | 2026-07-04 发现菜单态下 `ArenaCalibrationService.isLoaderReady()` 不应要求 `_root.gameworld层级定位器`；该条件已保留在 `prepareCalibrationStage()`，避免进入专用场景前误报 `stage_failed` |

---

## 2. 后续会话最短交接流程

新会话接手时先读本文，再按以下顺序执行：

1. 确认没有其他任务占用标定专用槽或 arena runner。
2. 读取 [automation/README.md](../automation/README.md) 的现役 arena unattended 入口；不要硬编码端口或手工构造无授权 `/task`。
3. 读取 campaign checkpoint / exception inbox；本文“当前候选池”和“下一批计划”只保留历史兼容记录。
4. 接收用户的 `raw_submission`，由系统生成 candidateId、配对、side swap、预算和 `case_manifest.json`。
5. 运行现役 `run-unattended.js`；由授权 client 执行 `startBatch` 并轮询 `status`。
6. 批次结束或崩溃后，只以已经落盘的 JSONL 为准生成 summary。
7. Campaign 自动写运行索引、候选状态和剪枝 receipt；人类只处理异步 exception inbox、PVE packet 和正式 recommendation bundle 批准。

PowerShell 运行前置：

```powershell
chcp.com 65001 | Out-Null
```

状态查询和 `startBatch` 由 `tools/arena-calibration/run-unattended.js` 内部的授权 client 完成。F8 后 standard normal 与 explicit legacy HTTP automation 互斥；本实验不得恢复固定端口扫描、未授权 `Invoke-RestMethod` 或 standard Agent Runtime 中的 `/console` 旁路。

下列分析命令只用于 Campaign Gate B 前的兼容 / 诊断流程；Gate B 后由 Supervisor 自动运行，不要求人类逐批执行：

```powershell
node tools/arena-calibration/analyze-results.js `
  --input logs/arena-calibration/<batchId>-results.jsonl `
  --summary tmp/arena-calibration/batches/<batchId>/summary.json `
  --summary-md tmp/arena-calibration/batches/<batchId>/summary.md

node tools/arena-calibration/plan-next-batch.js `
  --summary tmp/arena-calibration/batches/<batchId>/summary.json `
  --output tmp/arena-calibration/batches/<batchId>/next_batch.json `
  --planner rule
```

无人值守入口（推荐用于离开机器后的冷启动到 batch 全链路）：

```powershell
node tools/arena-calibration/run-unattended.js `
  --slot cf7_agent_arena_calibration `
  --seed-slot crazyflasher7_saves2 `
  --manifest tmp/arena-calibration/batches/<batchId>/case_manifest.json
```

该命令会自动把 manifest 规范化到 `tmp/arena-calibration/batches/<batchId>/case_manifest.json`，必要时通过 `automation/start.ps1` 启动 Launcher。启动前 runner 会把专用槽从 `--seed-slot` 或最新有效 shadow 存档播种，备份目标槽位 JSON/SOL 并删除目标 SOL 残留；默认拒绝 `crazyflasher7_saves*` 正式槽与 `--fresh`。进入 Flash 后，runner 通过 agent 入口执行原“确认存档进入游戏”动作，等待 `readyForArenaCalibration`；该状态必须包含安全 snapshot 决议和 AS2 `agent_runtime_status` 的 `saveRuntime.loaded=true`。之后再调用 `arena_calibration startBatch/status`，最后写 `tmp/arena-calibration/unattended/<timestamp>-<batchId>/summary.*` 与 `run-report.*`。如果结果不完整或出现 `aborted` / `bridge_lost` / `error` 等异常行，会生成新的 rerun manifest（默认在 `tmp/arena-calibration/batches/<rerunBatchId>/case_manifest.json`）；`--max-recovery-attempts` 默认为 1，表示首轮失败后自动关闭 Launcher、重启进档并补跑一次剩余 case，所有 attempt、补跑 manifest、失败清单和建议都归档到 `run-report.*`。如只想生成最小 pilot，显式使用 `--generate-pilot --batch-id <id>`；不要让脚本默认选择正式玩家存档。

构建 / 编译门禁：

- 默认 `--build-gate arena-tools`，只跑斗兽标定 Node 工具自检。
- 改 Launcher/C# 后先独立 build，再把精确 `candidateRoot` 传给 runner；runner 内只追加不产二进制的 `--build-gate launcher-tests`。`launcher-build` / `launcher` 嵌入 gate 必须 fail-fast，不能边跑边产生未选择 candidate。
- 改 AS2 标定 runner 后按触及层用 `--build-gate as2-publish` 或 `--add-build-gate as2-test`；成功表述仍必须符合 `testing-guide` 对新鲜输出、SWF 刷新和 trace 的要求。
- 除 `arena-tools` 外，runner 会先尝试关闭已有 Launcher，再跑 gate；gate 失败会在 `run-report.*` 里记录命令、退出码和输出尾部，并停止，不继续启动游戏。
- 自动恢复只用于 runner 自己生成的 rerun manifest，不负责自动改数值、改 AS2 业务逻辑或无限制重编译；需要重编译时应显式选择对应 build gate。

---

## 3. 组合征集格式

测试群或维护者提交的是 `raw_submission`，允许直接提供表格行、名单、JSON 或工作簿中的可定位记录。每条组合只要求：

| 字段 | 必填 | 说明 |
|---|---|---|
| `combination` | 是 | 待标定组合；可以是 roster、表格单元格或现有配置引用 |
| `source` | 是 | 提供者、工作簿行、已有关卡或人工锚点 |
| `claimedTier` | 否 | 提供者的主观档位，只形成弱先验 |
| `notes` | 否 | 已知技能、召唤、远程或异常风险 |

roster 单位格式：

```json
{ "type": "兵种44", "level": 30 }
```

示例：

```json
{
  "source": "manual-anchor",
  "claimedTier": "mid",
  "combination": [
    { "type": "兵种44", "level": 30 },
    { "type": "兵种45", "level": 30 },
    { "type": "兵种48", "level": 30 },
    { "type": "兵种49", "level": 30 }
  ],
  "notes": "提供者认为接近中档"
}
```

`schema`、`submissionId`、摄取时间和 hash 由 intake adapter 自动注入，不是提交者字段。系统再生成 `normalized_candidate`、稳定 ID / hash、完整参数、默认值、风险标签、anchor / bridge / side swap 和采样预算。人类不负责填写 `caseId`、双方配对、repeat、timeout、阵型或内部 manifest。只有会改变组合语义且无法从源数据推导的歧义，才按 `dedupeKey` 汇总到异步 exception inbox 后批量询问。

### 3.1 工作簿直接摄取

现役入口：

```powershell
npm --prefix tools/arena-calibration ci --ignore-scripts --no-audit --no-fund
node tools/arena-calibration/intake-workbook.js `
  --workbook "<测试群工作簿.xlsx>" `
  --sheet "斗兽标定组合" `
  --overrides tools/arena-calibration/workbook-overrides.json `
  --output-dir tmp/arena-calibration/batches/<batchId> `
  --phase exploration `
  --directions "B2:both,G2:both,F5:both,C7:both,C12:both" `
  --repeat 3 `
  --batch-id <batchId> `
  --build-commit <commit>
```

2026-08-27 摄取的 `2.7bug记录-记录前请查看bug是否已提交 (1).xlsx` SHA-256 为 `840B30AF82CA686E954DC4A6378A5C2B297506070E034ED79EA91DA9E0B3B793`，sheet 为 `斗兽标定组合`。59 个 populated cells 全部保留为 raw submissions，其中 58 个规范化为 candidates、1 个进入 exception inbox：

- `C9`：原代码把 `seed=519920` 后的字段分隔符误写为冒号；派生产物改为分号。
- `B9`：正文和 35–40 档均写明浮士德 40 级，单位目录复核 `u59` 为浮士德兄弟；派生产物把 `u59@30` 修为 `u59@40`。
- `B12`：只有自然语言组合和主观结论，没有可执行 CF7ARENA 代码；保留 raw submission 并隔离，不猜内部单位字段。

三项决定均由工作簿 hash + sheet + cell 锁定；源 `.xlsx` 未写回。改名、换 sheet 或任一字节变化都会使 override 拒绝应用，必须重新复核。

---

## 4. 批次执行策略

### 4.1 崩溃容忍

批次设计必须默认游戏、Flash、launcher 或通信链路随时可能崩溃。

- JSONL 是唯一可信进度源；未写入 JSONL 的 run 视为未完成。
- 每个 batch 保持可恢复，不依赖聊天记录判断进度。
- 崩溃后不手工改 JSONL；生成补跑 manifest。
- 补跑 manifest 只包含缺失 run 或需要追加样本的 case，并使用新 `batchId`。
- 单个 batch 不宜过大，避免一次崩溃损失过多排队状态。

### 4.2 批次规模

| 阶段 | 建议规模 | 目标 |
|---|---:|---|
| smoke | 1 到 5 run | 验证游戏已连通、场景可跳转、JSONL 可写 |
| exploration | 30 到 100 run | 探索新组合、发现明显偏向、验证稳定性 |
| stability | 300 到 1000 run | 无人值守稳定性、崩溃恢复、异常频率统计 |
| confirmatory | 每个候选 30 到 100 run | 对接近平衡组合做独立复核 |

首批结果 45 run 用时约 519 秒，可粗略估算 1000 run 为数小时级别。真实耗时取决于单位组合，召唤、远程拉扯、零血不死检测会显著影响耗时。

### 4.3 timeoutFrames

- 工作簿 phase policy：smoke `3600`、exploration `1800`、confirmatory `1800`；generic custom-match 交互入口仍保持 `3600`，两种上下文不再共用模糊默认值。
- 单元格已有显式 timeout 时原值优先并进入 candidate provenance；已知长僵持组合可以显式使用 `3600`、`5400` 或更长值，但必须保留来源和原因。
- timeout 不等于普通错误；应单独统计为稳定性和战斗设计信号。
- 若 timeoutRate 明显升高，不要直接拿胜率做平衡结论。
- 批后不得自动延长 timeout 并把原 timeout 覆盖成 finished；应追加相同语义样本、隔离，或形成 hash-bound 的长战解释。

---

## 5. 剪枝与追加样本规则

规则只在批次结束后应用，不在单局中途改变计划。

| 条件 | 动作 | 说明 |
|---|---|---|
| 样本 < 10 | `append_repeat` | 不做最终判断，只追加样本 |
| blueWinRate <= 0.2 且样本 >= 10 | `drop_or_counter` | 明显红方强；若是关键档位，保留反向/邻近档复核 |
| blueWinRate >= 0.8 且样本 >= 10 | `drop_or_counter` | 明显蓝方强；同上 |
| 0.35 <= blueWinRate <= 0.65 且 timeoutRate 低 | `promote_candidate` | 进入候选池，追加到 30/50 样本 |
| 0.2 < blueWinRate < 0.35 或 0.65 < blueWinRate < 0.8 | `adjacent_probe` | 做邻近等级、人数或 side-swap 探索 |
| timeoutRate > 0.2 | `stability_investigate` | 先处理稳定性或战斗僵持，不直接判强弱 |
| errorCount > 0 | `stability_investigate` | 查 schema、spawn、bridge、stage、AS2 runtime |
| ZeroHPDetector 频繁出现 | `stability_watch` | 结果可用但要标注风险，长批次单独计数 |

side bias 处理：

- mirror case 默认要求 side-swap 或追加样本。
- 非 mirror 但人数组合反转后应存在对应 case，例如 `2v4` 与 `4v2`。
- 若同强度 mirror 长期偏向红方或蓝方，不直接用单侧胜率做 benchLevel 结论。

---

## 6. 历史候选池（2026-07-02）

来自 `first-log-20260702-172046`：

| caseId | samples | blueWinRate | timeoutRate | classification | 下一步 |
|---|---:|---:|---:|---|---|
| `thief-1v1-lv30-mirror` | 5 | 0.2 | 0 | `red_favored` | 追加样本和 side bias 复核 |
| `thief-1v1-lv60-mirror` | 5 | 0.6 | 0 | `balanced_candidate` | 优先追加到 30 样本 |
| `thief-1v1-lv60-vs-lv30` | 5 | 1.0 | 0 | `blue_favored` | 作为等级差 sanity anchor 保留 |
| `thief-1v1-lv30-vs-lv60` | 5 | 0.0 | 0 | `red_favored` | 作为等级差 sanity anchor 保留 |
| `thief-4v4-lv30-mirror` | 5 | 0.2 | 0 | `red_favored` | 追加样本和 side bias 复核 |
| `thief-4v4-lv50-vs-lv30` | 5 | 1.0 | 0 | `blue_favored` | 作为组队等级差 anchor 保留 |
| `thief-4v4-lv30-vs-lv50` | 5 | 0.0 | 0 | `red_favored` | 作为组队等级差 anchor 保留 |
| `thief-2v4-lv50-vs-lv30` | 5 | 0.2 | 0 | `red_favored` | 追加邻近等级或人数补点 |
| `thief-4v2-lv30-vs-lv50` | 5 | 1.0 | 0 | `blue_favored` | 追加邻近等级或人数补点 |

当前优先级：

1. 对 `thief-1v1-lv60-mirror` 追加样本，验证是否是真均衡候选。
2. 对 `thief-1v1-lv30-mirror`、`thief-4v4-lv30-mirror` 追加样本，确认首批红方偏向是否为随机波动、side bias 或 AI/出生点因素。
3. 对 `2v4/4v2` 做邻近等级网格，例如 lv45、lv55，寻找人数差和等级差的抵消区间。
4. 新增测试群征集组合，以探索批运行，不直接并入 confirmatory。

---

## 7. 批次运行日志

每次运行结束或崩溃后，在这里追加一行。不要把完整 JSONL 粘进本文。

| 时间 | batchId | manifest | result | summary | runs | finished | timeout | error | ZeroHPDetector | 结论 | 下一步 |
|---|---|---|---|---|---:|---:|---:|---:|---:|---|---|
| 2026-07-02 17:21-17:29 | `first-log-20260702-172046` | `tmp/arena-calibration/batches/first-log-20260702-172046/case_manifest.json` | `logs/arena-calibration/first-log-20260702-172046-results.jsonl` | `tmp/arena-calibration/batches/first-log-20260702-172046/summary.md` | 45 | 45 | 0 | 0 | 6 | 首批实机闭环成立；lv60 1v1 mirror 是首个均衡候选 | 进入测试群组合探索和中等规模追加样本 |
| 2026-07-04 13:31 | `unattended-timeout-20260704-2` | `tmp/arena-calibration/batches/unattended-timeout-20260704-2/case_manifest.json` | `logs/arena-calibration/unattended-timeout-20260704-2-results.jsonl` | `tmp/arena-calibration/unattended/20260704T053133Z-unattended-timeout-20260704-2/summary.json` | 1 | 0 | 0 | 1 | 0 | 无人值守 timeout 故障注入成立；runner 请求 abort，JSONL 记录 `aborted`，report 生成 rerun manifest 且 shutdown 干净 | 不作为数值样本；用于证明 timeout/abort/needs_rerun 报告链路 |
| 2026-07-04 13:36-13:37 | `unattended-crash-20260704-2` | `tmp/arena-calibration/batches/unattended-crash-20260704-2/case_manifest.json` | `logs/arena-calibration/unattended-crash-20260704-2-rerun-20260704T053642Z-results.jsonl` | `tmp/arena-calibration/unattended/20260704T053632Z-unattended-crash-20260704-2/summary.json` | 1 | 1 | 0 | 0 | 0 | 受控 kill Guardian Core 后，runner 记录 `phase=poll`，生成 rerun manifest，重启 Launcher 并补跑完成 | 作为 crash/socket 断开恢复验证，不作为独立数值样本 |
| 2026-07-04 16:15-16:16 | `unattended-safety-happy-20260704b` | `tmp/arena-calibration/batches/unattended-safety-happy-20260704b/case_manifest.json` | `logs/arena-calibration/unattended-safety-happy-20260704b-results.jsonl` | `tmp/arena-calibration/unattended/20260704T081556Z-unattended-safety-happy-20260704b/summary.json` | 3 | 3 | 0 | 0 | 0 | 存档安全阀后正向闭环：专用槽由 `crazyflasher7_saves2` 播种，`saveRuntime.loaded=true` 后才进入 batch | 作为安全阀 happy path 回归，不作为独立数值样本 |
| 2026-07-04 16:18-16:20 | `unattended-safety-timeout-20260704a` / rerun | `tmp/arena-calibration/batches/unattended-safety-timeout-20260704a/case_manifest.json` | `logs/arena-calibration/unattended-safety-timeout-20260704a-rerun-20260704T081907Z-results.jsonl` | `tmp/arena-calibration/unattended/20260704T081937Z-unattended-safety-timeout-20260704a-rerun-20260704T081907Z/summary.json` | 3 | 3 | 0 | 0 | 0 | 受控 batch timeout 先生成 `aborted` 与 rerun manifest，随后 rerun manifest 在同一安全阀下补跑完成 | 作为 timeout/abort/rerun 安全阀回归 |
| 2026-07-04 16:21-16:23 | `unattended-safety-crash-20260704a` | `tmp/arena-calibration/batches/unattended-safety-crash-20260704a/case_manifest.json` | `logs/arena-calibration/unattended-safety-crash-20260704a-rerun-20260704T082212Z-results.jsonl` | `tmp/arena-calibration/unattended/20260704T082155Z-unattended-safety-crash-20260704a/summary.json` | 3 | 3 | 0 | 0 | 1 | 受控 kill Guardian Core；首轮 `phase=poll`，runner 自动生成 rerun、重启并完成，最终 `recoveryAttemptsUsed=1/1` | 作为 crash/socket 断开自动恢复安全阀回归 |

---

| 2026-07-04 17:15-17:20 | `spawn-distance-tier-20260704-a` | `tmp/arena-calibration/batches/spawn-distance-tier-20260704-a/case_manifest.json` | `logs/arena-calibration/spawn-distance-tier-20260704-a-results.jsonl` | `tmp/arena-calibration/unattended/20260704T091518Z-spawn-distance-tier-20260704-a/summary.json` | 27 | 27 | 0 | 0 | 0 | Spawn distance 600/620/650 smoke: actual positions matched request; no timeout/error. 600 showed side bias in mixed mirror. | Focus 620 vs 650. |
| 2026-07-04 17:22-17:27 | `spawn-distance-tier-20260704-b` | `tmp/arena-calibration/batches/spawn-distance-tier-20260704-b/case_manifest.json` | `logs/arena-calibration/spawn-distance-tier-20260704-b-results.jsonl` | `tmp/arena-calibration/unattended/20260704T092241Z-spawn-distance-tier-20260704-b/summary.json` | 30 | 30 | 0 | 0 | 0 | Focused 620/650 check: both safe; 650 gives ranged side more breathing room without timeouts, 620 remains conservative. | Prefer 650 for the next default; keep 620 as fallback if later long-range cases slow down. |
| 2026-08-27 15:44-15:50 | `gate-a-pilot-20260827-a` | `tmp/arena-calibration/batches/gate-a-pilot-20260827-a/case_manifest.json` | `logs/arena-calibration/gate-a-pilot-20260827-a-results.jsonl` | `tmp/arena-calibration/unattended/20260827T074459Z-gate-a-pilot-20260827-a/summary.json` | 10 | 10 | 0 | 0 | 0 | B2/G2/F5/C7/C12 双方向覆盖普通、单位 payload、阵型、显式长 timeout 与 lv100；formal runtime identity verified，20 份受保护 live JSON/SOL 指纹不变 | Gate A pilot 通过，允许进入 30–100 run exploration；不作为最终分档 |
| 2026-08-27 15:51-16:11 | `gate-a-exploration-20260827-a` | `tmp/arena-calibration/batches/gate-a-exploration-20260827-a/case_manifest.json` | `logs/arena-calibration/gate-a-exploration-20260827-a-results.jsonl` | `tmp/arena-calibration/unattended/20260827T075133Z-gate-a-exploration-20260827-a/summary.json` | 30 | 27 | 3 | 0 | 0 | 同 5 个来源 × 双方向 × 3；JSONL 30/30、身份 verified、live save 指纹不变。B2 original、F5 swapped、C12 original 各 timeout 1/3；C7 长 timeout 双方向 0/6。业务 attempt 完成，但收尾曾记录过期 credential teardown 错误 | 保留 timeout 信号；相关方向 `unstable_timeout/undersampled`，不得进入最终候选 |
| 2026-08-27 16:15-16:16 | `gate-a-shutdown-regression-20260827-a` | `tmp/arena-calibration/batches/gate-a-shutdown-regression-20260827-a/case_manifest.json` | `logs/arena-calibration/gate-a-shutdown-regression-20260827-a-results.jsonl` | `tmp/arena-calibration/unattended/20260827T081517Z-gate-a-shutdown-regression-20260827-a/summary.json` | 1 | 1 | 0 | 0 | 0 | 修复后独立 formal run：report `error=null`、`postRunShutdown.stopped=true`、expected PID 已消失、live save 指纹不变 | 关停竞态闭合；保留上一批原报告，不回写历史证据 |
| 2026-08-27 16:46-16:47 | `gate-a-schema-regression-20260827-a` | `tmp/arena-calibration/batches/gate-a-schema-regression-20260827-a/case_manifest.json` | `logs/arena-calibration/gate-a-schema-regression-20260827-a-results.jsonl` | `tmp/arena-calibration/unattended/20260827T084634Z-gate-a-schema-regression-20260827-a/summary.json` | 1 | 1 | 0 | 0 | 0 | 新版 runner 对生产原始行直接 Schema 校验并绑定 exact manifest/case；formal identity verified、report `error=null`、shutdown clean、20 份 live save 指纹不变 | 生产 JSONL 实例门闭合；Gate A 封板 |
| 2026-08-27 17:20-17:23 | `gate-b-shard-1-20260827-a` / `gate-b-shard-2-20260827-a` | `tmp/arena-calibration/campaign-gate-b-20260827-a/shard-{1,2}-intake/case_manifest.json` | `logs/arena-calibration/gate-b-shard-{1,2}-20260827-a-results.jsonl` | `tmp/arena-calibration/campaign-gate-b-20260827-a/shard-{1,2}-run-report.json` | 2 | 2 | 0 | 0 | 0 | `gate-b-campaign-20260827-a` 中 B2/G2 各一局；中间显式 pause/close 后换进程、producer snapshot 和 idle grant 恢复。两份 execution artifact 均绑定 exact formal runtime/cohort/raw result/save guard；journal 最终 26 committed events、2 unique runKey、3 closed segments、0 open segment | Gate B 完成；两局只验 Campaign 控制面，不用于候选定档 |
| 2026-08-27 18:27-18:47 | `gate-d-active-20260827-a` | `tmp/arena-calibration/batches/gate-d-active-20260827-a/case_manifest.json` | `logs/arena-calibration/gate-d-active-20260827-a-results.jsonl` | `tmp/arena-calibration/campaign-gate-b-20260827-a/gate-d-active-summary.json` | 34 | 31 | 3 | 0 | 0 | bridge、side-swap 与 timeout 调查；全部绑定 formal runtime，非目标存档集合不变。高等级 timeout 调查 original 3/3 timeout 继续隔离，未被延长口径粉饰 | 只让稳定的 G2 候选进入确证 shard |
| 2026-08-27 18:53-18:59 | `gate-d-confirmatory-dd659-20260827-a` | `tmp/arena-calibration/batches/gate-d-confirmatory-dd659-20260827-a/case_manifest.json` | `logs/arena-calibration/gate-d-confirmatory-dd659-20260827-a-results.jsonl` | `tmp/arena-calibration/campaign-gate-b-20260827-a/gate-d-confirmatory-summary.json` | 21 | 21 | 0 | 0 | 0 | 与既有样本合并后，G2 `candidate-dd65977b16a48a35` 达到 original 15 + swapped 15、0 timeout/error、side-swap 已复核；completion receipt accepted | 冻结最终 snapshot 并进入 Gate E，两场 PVE 不再追加机器样本 |

## 8. 剪枝决策记录

记录批次结束后的人工决策，避免后续 compact 后重复讨论。

| 时间 | 来源 batch | 决策 | 理由 | 后续动作 |
|---|---|---|---|---|
| 2026-07-02 | `first-log-20260702-172046` | 接受平台进入中等规模探索 | 45/45 finished，0 timeout，0 error，结果字段完整 | 收测试群组合，按 30 到 100 run/batch 分批 |
| 2026-07-02 | `first-log-20260702-172046` | 暂不把玩家体验恢复作为阻断项 | 当前阶段目标是开发环境收日志和无人值守稳定性 | 后续若转玩家可见斗蛐蛐赌场，再恢复副作用隔离要求 |
| 2026-07-02 | `first-log-20260702-172046` | 将 `ZeroHPDetector` 作为稳定性指标 | 首批 6 次触发但未导致 result error | 长批次统计触发率，必要时定位零血不死路径 |

---

| 2026-07-04 | `spawn-distance-tier-20260704-a/b` | Prefer `spawnDistance=650` for the next calibration default | 57 total runs, 0 timeout/error; 600 can be discarded, 650 improves ranged breathing room over 620 while staying within map/camera bounds | Use 650 in the next implementation pass; keep 620 as fallback if larger ranged rosters introduce slowdowns |
| 2026-07-04 | default spawn-distance implementation | `spawnDistance=650` selected as normalized default | Manifest normalization, C# dispatch, AS2 fallback, result metadata, and rerun preservation now all converge on 650 unless a case explicitly overrides it | Future distance experiments should set `spawnDistance` explicitly and compare against the 650 baseline |
| 2026-08-27 | `gate-a-pilot-20260827-a` | Gate A 工程门完成，Gate B 达到开工条件 | 真实 Schema 实例校验、参数/污染闭包、hash-bound 工作簿 intake、10/10 fresh pilot、formal identity 与 live save 指纹均闭合 | 允许施工 durable Supervisor/journal/recovery；不称 Gate B 已完成 |
| 2026-08-27 | `gate-a-exploration-20260827-a` | 30-run 探索批完成，但无最终候选 | 27 finished / 3 timeout / 0 error；每个 case direction 只有 3 样本，三个方向 timeoutRate 1/3 | 相同 battle semantics 追加样本并做 side-swap 复核；不通过延长 timeout 粉饰 |
| 2026-08-27 | `gate-b-campaign-20260827-a` | Gate B 工程门完成 | 双 flush journal、单 writer、断尾/篡改/ack-loss 恢复、fail-closed idle grant、真实两 shard 和 pause/resume 均闭合；两 shard 的 20 份受保护存档 snapshot 不变 | 冻结 Gate C snapshot；普通 shard 不再逐批请求人工确认 |
| 2026-08-27 | `shadow-gate-c-20260827-a` | Gate C 停在真实外部证据边界 | A/B/C 请求共享 snapshot `sha256:643992...867f`，experiment `sha256:f53275...833`；尚无三份真实模型输出、机械 scorecard、盲化人审和 gold suite | 等真实模型返回并交付一次盲评，不使用 fixture 代签 |
| 2026-08-27 | `gate-d-paired-strength-20260827-a` | 机器分析可用，但不顺序晋级 Gate D | 42 行中 39 finished 入模、3 timeout 排除；10 nodes / 5 components / 4 bridges / 5 side swaps / 3 timeout anomalies，区间宽且断连 | 先过 Gate C；随后跑 bridge 与 timeout 调查 shard，再复算 |
| 2026-08-27 | Gate E contract fixture | 只接受“契约就绪”，不称开门 | 2–4 encounter、5–10/15 分钟、holdout 和 response validator 正负例通过；没有获批玩家 build、真实进场遥测或真人标签 | Gate C/D 顺序满足并冻结 build 后再生成实际 packet |
| 2026-08-27 | `shadow-gate-c-20260827-a` real outputs + blind review | Gate C 本轮完成 | A/B/C 三份真实 proposal/receipt 均过硬门；独立 `gpt-5.6-sol` high 盲评 C > A > B、confidence 0.96，review hash `sha256:1ac12776...6b55` | 只采纳本轮方法选择，不宣称永久模型排名 |
| 2026-08-27 | `gate-d-active-20260827-a` + `gate-d-confirmatory-dd659-20260827-a` | 单候选机器完成门通过 | 新增 55 fresh runs；最终 paired report 91 eligible / 6 excluded / 1 component，G2 候选 15 + 15、0 timeout/error、side-swap reviewed | 生成绑定 exact build/player save 的两场 PVE packet |
| 2026-08-27 | `pve-candidate-dd65977b16a48a35-20260827-a` | Gate E 等效标签子门完成 | 两场受控正式运行时 PVE 均标为 `1 名 × Lv10 人形佣兵`；响应 hash `sha256:76ab8e72...18f18`，源存档/非目标集合不变且收尾零活动槽、进程、锁、恢复记录 | 保持 `equivalence_only`；不补猜客观 telemetry，不 formal apply |
| 2026-08-27 | `gate-f-week-full-v1` plan / `gate-f-week-full-v2` campaign pre-runtime draft | Gate F0 工程前置与候选诊断完成，正式 soak 未开始 | 58 个 normalized candidate 全部 scheduled，B12 quarantine；B9 在 exact candidate 上双向 1800 帧 timeout，5400 帧诊断于 2776/3494 帧 finished，存档快照不变，因此以 schema-valid、workbook hash + cell + candidate + runtime + raw evidence hash 闭包仅在计划层转入长 timeout，原 intake/timeout 事实保留且 formal replay 必需。共 198 个 10–25 run shard、3255 planned runs、198 份 hash-bound decision evidence；40-schema / 64-check 回归通过 | 合入已部署远端与最终提交后，在 clean worktree 冻结新 source/formal runtime；先跑 3 个正式 soak，通过后再开全量，不把 candidate 诊断或草案记成正式实跑 |
| 2026-08-28 | merged candidate Gate F soak routing diagnosis | 候选波动与基础设施稳定性样本已分离，正式 soak 仍未开始 | 合并候选 10-run 诊断为 9 finished + C9 换边 1 timeout；C9 独立 5400 帧双向探针分别于 1013/936 帧 finished，不支持放宽 timeout。替换进入首批基础设施 soak 的 G2 为 10/10 finished；C9 仍保留完整普通 10/20/25 run 与换边。全部诊断绑定 candidate identity `48E2ACEA81194C0D6C3A89226DEC2748192612B5514D3F3ADB8444FA4AF6C528`、closure `DA7E5BD135FF2407ED7CE459F521BEA95C9CB6F5CC63AA5291ABAC795DAF59F1` 和不变存档 snapshot `sha256:fcfaa53f9000a5b7e75205bb3549aa2c8b5798df3fe5d4843725222ffbede73b`。计划仍为 198 shard / 3255 run | 合入最终上游并完成 v2 promotion 后，绑定 clean source/formal runtime；先跑 3 个正式 soak，通过后再开全量，不把 candidate 诊断记成正式实跑 |
| 2026-08-28 | `gate-f-week-full-v2` formal soak 01–03 | 平台证据闭合，campaign 因候选 timeout 正确暂停 | soak 01/02 为 20/20 finished；soak 03 为 8 finished + B11 原向 9000 帧 timeout + C12 原向 5400 帧 timeout，各自换边自然结束。三份均为 exact formal runtime、0 error/recovery、正常关闭、JSONL/manifest 绑定和受保护存档 snapshot 不变；30 条正式行原样保留。失败说明静态 soak 路由混入候选自身长战波动，不是平台或存档污染 | 不重试粉饰旧 shard；以证据准入的稳定代表重建新 plan/campaign，B11/C12 继续完整进入全量长 timeout 分片 |
| 2026-08-28 | `gate-f-week-full-v2` plan / `gate-f-week-full-v3` campaign routing fix | 修正计划工程就绪，fresh soak 尚未重跑 | 新增 `arena-calibration.soak-admission.v1`，逐文件验证原始 manifest/result/report SHA-256、exact-policy 双向 finished、formal runtime identity、零 error/timeout/recovery、正常关闭与存档不变，并由 freeze 绑定 admission hash。三份 soak 均选 `B2/C7/G2/F3/E10`，每份覆盖普通参数、单位 payload、阵型、长 timeout、高等级和 side-swap；计划仍为 58 scheduled + B12 quarantine、198 shard / 3255 run；统一门为 41 个 Schema / 65 项检查，文档治理通过 | 合并上游并冻结 clean source；新三份 soak 全通过才放开剩余 195 shard |
| 2026-08-28 | `gate-f-week-full-v3` formal soak / full start | 三份 fresh soak 30/30 clean；全量安全暂停 | 三份均为 10/10 finished、0 timeout/error/recovery、exact formal runtime、正常关闭和受保护存档不变。全量随后完成 8/198 shard、提交 115 条 durable row、排除重启后的 10 条重复、0 failed；第一次由诊断命令行误触 producer 观察，第二次在 `f-c2-p3` 构建门开始约 7 秒后且 0 战斗行时，由内部检查后代进程误触 `producer_preempted`。两次均未污染 runtime、玩家或存档 | v3 事实保留但不跨 plan hash 混计；修复受控进程树后用新 campaign 重冻并重跑 fresh soak |
| 2026-08-28 | `gate-f-week-full-v2` plan / `gate-f-week-full-v4` campaign self-preemption fix | 新草案与工程门通过，尚未 freeze/arm | controller/runner 的祖先和全部后代现在属于同一受控进程树，内部 `run-checks.js` / `gate-fctl.js --check` 不再被当作竞争 producer；独立 runner/Flash 仍触发让位。admission 以唯一 run 目录的 result/manifest snapshot 锁实际字节，同时保留报告 `resultPath` 验真，消除固定输出覆盖与循环 hash；仍为 58 scheduled + B12 quarantine、198 shard / 3255 run | 提交推送后在 clean source 冻结 v4；先重跑三份 fresh soak，通过后开放剩余 195 shard |
| 2026-08-28 | `gate-f-week-full-v4` fresh soak / full | 30/30 fresh soak clean；全量因 F2 候选异常暂停 | 三份 soak 均 10/10 finished、0 timeout/error/recovery。全量累计 16 个 completed shard + 1 个 F2 anomaly shard、280 条 durable row、0 duplicate；F2 p2 原向 10/10 finished，换边 5 finished + 5 个 1800 帧 timeout，0 error，exact runtime、存档不变且正常 shutdown。旧 driver 将纯候选 timeout-rate 误作基础设施 failure，故 21:00 后不再开窗 | 原始 5 个 timeout 不重试洗掉；拆分 execution health 与 candidate quality 后用新 campaign 重冻 |
| 2026-08-28 | `gate-f-week-full-v2` plan / `gate-f-week-full-v5` campaign timeout anomaly fix | 草案与工程门通过，待 clean freeze | 纯 `timeout_rate` 超阈值仍提交完整 shard，写 `candidate_timeout_anomaly / keep_provisional` deferred item 并继续；timeout 继续排除于强度拟合且阻断低-timeout 最终候选资格。error/污染、duration drift、runtime/save/disk、runner/report/cardinality 异常仍失败。计划保持 58 scheduled + B12 quarantine、198 shard / 3255 run；Gate F 全套、plan check、文档治理与 diff check 均通过 | 提交推送、clean freeze；重新三份 fresh soak 后开放全量 |
| 2026-08-30 | `gate-f-week-full-v6` 派生单位阵营事故与撤销 | v6 全批隔离；修复取得工作树实机诊断，待提交后新 cohort | 三份 fresh soak 与后续分片累计 16 completed shard / 260 durable row 后，换边实机暴露 `兵种39` 死亡派生僵尸虽记录为 blue、实体仍沿用 linked symbol 的 `是否为敌人=true`，造成同侧不可伤害和卡死。owned revoke 已有界收尾；v6 260 行禁止恢复、拟合或跨 cohort 混计。修复在 loader 前按 parent side 覆盖阵营、创建后再绑定，并逐帧以 `faction_contamination` fail closed；强化工作树探针 10/10 finished、0 timeout/error/contamination，实际派生 red 3 / blue 4，正式 runtime 与受保护存档闭合 | 本地提交修复；以提交后双方向实际派生 probe 生成新 admission，重建新 campaign/cohort，三份 fresh soak 通过后才恢复全量 |
| 2026-08-30 | `gate-f-week-full-v7` v2 admission / tracked plan | 提交后正式准入通过；tracked 草案重建，尚未 freeze/arm | exact formal runtime 上合并代表与放大派生案例 20/20 finished、0 timeout/error/recovery、正常 shutdown、存档 snapshot 不变；目标派生 red 8 / blue 8。`arena-calibration.soak-admission.v2` 绑定 raw hashes、parent/unit、双侧最低 3 次与 replay 模板，hash `sha256:60c5662d...396a0`；三份 fresh soak 各从 10 增到 20 run，总计划保持 58 scheduled + B12 quarantine、198 shard / 3285 run；统一门为 47 个 Schema / 74 项检查 | clean freeze v7；三份 20-run fresh soak 全通过后才开放剩余 195 shard，v6 260 行继续隔离 |
| 2026-08-30 | `gate-f-week-full-v7` fresh soak / exact-zero fix | 60 行保留为控制缺口和 E10 不稳定证据；window 已撤销 | frozen plan `sha256:b8d6e321...e00460e` 上，soak-01 为 20/20 finished；soak-02 为 19 finished + E10 side-swap 1 timeout；soak-03 为 19 finished + E10 original 1 timeout。三份均 0 error/recovery、exact runtime、存档不变、正常 shutdown，派生 red/blue 分别为 7/6、2/6、6/3。旧 controller 让 soak 沿用 5% 全局阈值，错误把 1/20 timeout 写成 completed | 修复 `planner.phase=soak` 任意 error/timeout 非零即 fail closed，并加 1/20 回归；E10 留在自身全量长分片，先用新鲜正式双向 probe 准入替代高等级代表，再建新 admission/campaign 和三份 soak |

## 9. 历史下一批计划草案（已由 Campaign planner 取代）

等待测试群组合输入前，建议先跑一个中等规模追加批：

- `thief-1v1-lv60-mirror`：追加 25 run，使总样本达到 30。
- `thief-1v1-lv30-mirror`：追加 25 run，验证红方偏向。
- `thief-4v4-lv30-mirror`：追加 25 run，验证红方偏向。
- `thief-2v4-lv55-vs-lv30`、`thief-2v4-lv45-vs-lv30`：各 5 到 10 run。
- `thief-4v2-lv30-vs-lv55`、`thief-4v2-lv30-vs-lv45`：各 5 到 10 run。

预计规模：90 到 110 run。若用户提供测试群组合，则优先把这些组合并入 exploration，保持每批不超过 100 run。

---

## 10. 置信度与复核策略

探索批只负责缩小空间，不给最终结论。

候选进入 confirmatory 的最低条件：

- 每个 candidate/caseHash 的 original + swapped 总样本数 >= 30，且两个 side assignment 都有样本。
- `errorCount = 0`，并且 `sideSwapReviewed=true`。
- 两方向 `timeouts/samples/timeoutRate` 必须自洽，合并 timeoutRate 必须同时不高于 snapshot stop condition 与 `5%`；`explained_long_timeout` 还必须带 `timeoutExplanationRef`，但解释不能绕过低 timeout 门，`provisional` 不能完成候选。
- 胜率不一边倒，且 HP 残余分布支持“接近均衡”的解释。
- side-swap 或反向组合没有暴露明显出生点/阵营偏置。

上述条件已由 `complete_candidate` 的 schema + snapshot evidence 校验机械执行；模型 proposal 或人工备注都不能绕过。2026-08-27 的 10-run pilot、30-run exploration 与 Gate B 两局合计 42 个数值相关结果，其中 39 finished、3 timeout、0 error；三个 matchup 的 timeoutRate 为 11.11%–12.5%，且每个 candidate 仍不足 30 样本，没有任何 candidate 达到本门。

最终抽样复核：

- 从 `promote_candidate` 中抽样复跑独立 batch。
- 从 `drop_or_counter` 中抽样复核明显强弱结论，防止早期剪枝误杀。
- 从 `stability_watch` 中抽样复核 `ZeroHPDetector` 或 timeout 频率。
- 复核批应使用新的 `batchId`，不要复用探索批 manifest。

---

## 11. 文档维护规则

- 本文是历史无人值守实验的集中日志索引和跨会话摘要，不是 Campaign 逐批状态数据库。
- Campaign Gate B 前的兼容实验可以人工追加关键批次；Gate B 后由 campaign journal / checkpoint / receipt 保存逐批事实，本文只在里程碑、方法论决定或人工验收结论变化时更新。
- 新增组合由 `raw_submission -> normalized_candidate` 自动归档，不要求人类维护“当前候选池”表。
- 原始 JSONL 不复制进本文，只记录路径和摘要。
- 已失败或崩溃的 batch 不删除，记录 partial rows 和失败点。
- 如果协议、路径、测试入口或构建门槛变化，同步更新 canonical doc，并运行：

```powershell
chcp.com 65001 | Out-Null
node tools/validate-doc-governance.js
```

