_root.装备生命周期函数.挂载披风初始化 = function(反射对象, 参数对象) 
{
    参数对象 = 参数对象 || {};
    参数对象.type = "身体_引用";  // 设定物件类型
    参数对象.object = 参数对象.cloak || "将军披风";  // 设定物件种类

    _root.装备生命周期函数.挂载背景物件初始化(反射对象, 参数对象);
};

_root.装备生命周期函数.挂载披风周期 = function(反射对象, 参数对象) 
{
    // 同步版本号，防止因其他装备变更（换武器等）导致披风被版本检查误删
    // 真正的装备变更由挂载背景物件周期中的装备名称对比兜底
    if (反射对象.是否为主角 && 反射对象.版本号 !== 反射对象.自机.version) {
        反射对象.版本号 = 反射对象.自机.version;
    }
    EquipmentTick.cleanup(反射对象);
    _root.装备生命周期函数.挂载背景物件周期(反射对象, 参数对象);
};

_root.装备生命周期函数.挂载后发初始化 = function(反射对象, 参数对象)
{
    参数对象 = 参数对象 || {};
    参数对象.type = "发型_引用";  // 设定物件类型
    参数对象.object = 参数对象.behindhair || "后发-男式-阴阳两隔";  // 设定物件种类

    _root.装备生命周期函数.挂载背景物件初始化(反射对象, 参数对象);
};

_root.装备生命周期函数.挂载后发周期 = function(反射对象, 参数对象)
{
    // 同步版本号，防止因其他装备变更导致后发被版本检查误删
    if (反射对象.是否为主角 && 反射对象.版本号 !== 反射对象.自机.version) {
        反射对象.版本号 = 反射对象.自机.version;
    }
    EquipmentTick.cleanup(反射对象);
    _root.装备生命周期函数.挂载背景物件周期(反射对象, 参数对象);
};

_root.装备生命周期函数.挂载背景物件初始化 = function(反射对象, 参数对象)
{
    var 自机 = 反射对象.自机;
    var 搭载层 = 自机.底层背景;
    var 物件类型 = 参数对象.type ? 参数对象.type : "身体_引用";
    var 物件种类 = 参数对象.object ? 参数对象.object : "将军披风";
    var 物件标签 = 反射对象.标签名 + 物件种类;
    var 物件;

    if(自机[物件标签])
    {
        物件 = 自机[物件标签];
        // _root.服务器.发布服务器消息("读取缓存 " + _root.常用工具函数.对象转JSON(物件));
    }
    else
    {
        物件 = 搭载层.attachMovie(物件种类, 物件标签, 搭载层.getNextHighestDepth());
        自机[物件标签] = 物件;
        // _root.服务器.发布服务器消息("创建物件 " + _root.常用工具函数.对象转JSON(物件));
    }

    反射对象.搭载层 = 搭载层;
    反射对象.物件 = 物件;
    反射对象.物件类型 = 物件类型;
    反射对象.物件种类 = 物件种类;
    反射对象.物件标签 = 物件标签;

    _root.常用工具函数.设置卸载回调(自机, function() {
        物件.removeMovieClip();
    });    // 使用设置卸载回调函数
    var 卸载对象 = {动作:function(额外参数){
                        额外参数.物件.removeMovieClip();
                   },
                   额外参数:{物件:物件}
    };

    反射对象.生命周期函数列表.push(卸载对象);
};

_root.装备生命周期函数.挂载背景物件周期 = function(反射对象, 参数对象)
{
    EquipmentTick.cleanup(反射对象);
    var 物件 = 反射对象.物件;
    var 自机 = 反射对象.自机;
    var 引用 = 自机[反射对象.物件类型];

    if(自机.hp <= 0 || ((反射对象.是否为主角 ? _root.物品栏.装备栏.getNameString(反射对象.装备类型) : 反射对象.自机[反射对象.装备类型]) !== 反射对象.装备名称) || !引用)
    {
        _root.装备生命周期函数.移除周期函数(反射对象);
        自机[反射对象.物件标签] = null;
        物件.removeMovieClip();
        return;
    }

    // 身体引用尚未挂载到显示列表时跳过本帧，等待就绪
    if (!引用._parent) return;

    // 投影身体引用的局部坐标系到搭载层（参照剑圣装备方案）
    // 同时计算位置、朝向和镜像符号
    var 原点 = {x: 0, y: 0};
    var x轴 = {x: 100, y: 0};
    var y轴 = {x: 0, y: 100};

    引用.localToGlobal(原点);
    引用.localToGlobal(x轴);
    引用.localToGlobal(y轴);

    反射对象.搭载层.globalToLocal(原点);
    反射对象.搭载层.globalToLocal(x轴);
    反射对象.搭载层.globalToLocal(y轴);

    // 位置：身体引用原点在搭载层中的映射
    物件._x = 原点.x;
    物件._y = 原点.y;

    // 朝向与镜像检测
    var vxX = x轴.x - 原点.x;
    var vxY = x轴.y - 原点.y;
    var vyX = y轴.x - 原点.x;
    var vyY = y轴.y - 原点.y;

    var angle = Math.atan2(vxY, vxX) * 180 / Math.PI;
    var det = vxX * vyY - vxY * vyX; // <0 表示发生镜像

    if (det < 0) {
        // 镜像时用负 xscale 表达，并把由镜像带来的 180° 偏差抵消
        angle -= 180;
        物件._xscale = -100;
    } else {
        物件._xscale = 100;
    }
    物件._rotation = angle;
};
