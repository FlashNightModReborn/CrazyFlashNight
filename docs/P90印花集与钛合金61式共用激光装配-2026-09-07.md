# P90印花集与钛合金61式共用激光装配

## 范围与物品身份

维护者已确认施工：旧钛合金P90涂装另建 `P90印花集`，新机甲稿接替原 `钛合金P90`，共用激光发射器用于新钛合金P90和钛合金QJZ171。普通QJZ171不加装。

- `P90印花集` 是新增的手枪槽冲锋枪，继承旧图标与 `枪-手枪-钛合金P90` 美术，采用下述33级合成一层数值。
- `钛合金P90` 保留名称身份、45级基础参数及现有获取关系，装扮改指 `枪-手枪-Codex-钛合金P90`，图标改指 `Codex-钛合金P90`。现有两家K店的6000K条目、诛神短枪配方材料和佣兵预装仍引用该身份；不做存档改名迁移。
- `钛合金QJZ171` 保留55级、2345威力、17%暴击、20kg、270ms、60发及原有10帧后坐动画，只新增固定发射器与激光视觉生命周期。

## 印花集合成一层

### 数值

维护者已接受的基础配置为33级、5kg、50发、120ms、130威力、impact原值50。继续使用完整物理穿刺、冲锋枪钢芯穿甲弹（price=400）、split=1和P90弹匣视觉。`reloadPenalty=-17` 沿用P90系现有值，没有新增暴击、击溃、斩杀或能源能力。

公式输入：`dualWield=2, pierce=2, damageType=1, shotgun=1, magPrice=400, weightLayers=1, category=2, formula=1`。

- 平均DPS：6985.519139296343。
- 加权DPS：6988.241559014966。
- 有符号残差：-0.0389571496%。

以上为工作簿formula 1的基础预算投影，不是实际角色双持DPS或通关时间；不额外计入强化、配件、角色技能和真实帧调度。旧170威力/90ms/9kg原样降至33级一层时残差约+61.975%，因此本轮同时调整了重量、射速与威力。

### 获取与价格

现役配方位于 `data/crafting/武器合成.json`，ID为 `craft.weapon.057`：

| 消耗 | 数量 |
|---|---:|
| P90 | 1 |
| 上色涂料 | 5 |
| 复合式军用塑料 | 1 |
| 枪械内构强化套件 | 1 |
| 镭射瞄准具 | 1 |
| 金币加工费 | 55000 |
| K点加工费 | 0 |

`枪械!C26` 的合成成本贡献一层；不复制原钛合金P90的K店条目到印花集，不从图标名称继承套装或K点身份。33级已超过工作簿慢/高射速穿刺枪的20/30级门槛，不扣超前层。

价格域与DPS层分开：穿刺类 `category=2` 来自 `装备价格!D13`；手枪定价系数1.5来自D11。按价格层0计算装备标价：`33×3900×2/1.5=171600`，没有给合成装备添加“仅购买高价”层。

合成成本按 `合成表成本!N3:P4`、`J13` 及材料档位折算：普通P90为 `0.25×13×3900/1.5=8450`，5份绿色材料为5000、2份橙色材料为12000、1份蓝色材料为50000；连同55000加工费，当前折算成本130450，对应33级/一层/穿刺种族2的目标132000，偏差约-1.17%。实际购买所有材料的花费不等于工作簿的装备折算成本。

审计记录 `weapon:P90印花集:data` 使用 `acquisition.crafting`，compact profile由 `balance-sync --write` 生成。证据以当前物品、配方、弹药和纯视觉生命周期为准，不修改工作簿版本映射。

## 素材与运行时接口

P90交接ZIP SHA256：`8a4e18f4e584459da9607a90e876d3ff15ea3af9682a4c85cc178ac8d3d429e5`。入库内容以Codex作者库XFL为唯一原生编辑源；保留原生分组、50帧弹匣、弹列与外壳遮罩。SVG与设计复盘作为溯源资料，后续原生编辑不会反向同步SVG。

- `Codex/P90-Ti61/`：P90分件、两种总装、游戏装扮和两帧图标。
- `Codex/Ti61-共用/`：实体发射器、不可见出口标记、红束样式。
- `Codex/QJZ171-Ti61/00_总装_含激光发射器`：静态展示组合；原 `00_总装` 和 `动画` 保留原来的15个分件及轨迹。
- P90和171的游戏外壳均提供 `激光发射器.出光位置`。原有 `枪口位置` 继续只负责枪口火光。

`枪械激光瞄准.as` 使用独立lifecycle反射对象，通过placement刷新与常规装备周期挂载 `激光瞄准-Codex-钛合金61-红束`。新建光束仍命名为 `激光模组`，与P90的既有显隐字段兼容；实体改名为 `激光发射器`，不会随光束消失。

只有战斗持枪姿态才动态创建光束。出口与前方一点经过真实MovieClip变换后，在枪外壳中求出位置和方向；反向、旋转和双持不使用角色坐标直接相加。参考束长250，默认显示长度750个枪外壳局部单位，保持参考束体的原生厚度。光束不参与伤害、命中裁决或套装能源机制。

换装、角色版本变化、placement替换和卸载只清理当前反射对象拥有的光束与订阅，不触碰其他枪械的同名元件。图标和纸娃娃离线烘焙不执行装备生命周期，因此只包含实体发射器。

## 首轮施工验证

施工证据位于 `tmp/weapon-animation/p90-ti61-implementation-20260907/`。本轮已完成数据落盘、CS6素材与asLoader发布、实际AS2测试和三件物品的Web图片应用。

- **原生编辑源**：最终XFL共83个元件、8个AS导出；audit八项CLEAN，rename/fix dry-run均无待处理项。全局linkage扫描已由素材工作台共享core重新执行，来源表新增P90图标、P90装扮和共用红束三项。普通及钛合金171的10帧后坐profile检查均为current，原总装和运动轨迹保留。
- **实际AS2行为**：最终run `79b6f8607db44a7589031f25d7dc04f4` 加载当前物品XML与实际Codex素材SWF，手枪、手枪2、长枪三组fixture合计 **89/89**。覆盖50帧弹匣、双持、显隐、出口变换、镜像、固定发射器与10帧后坐互不干扰，以及重复初始化、换装、旧回调与卸载清理；新鲜trace完整，Compiler 0 error / 0 warning。测试结束后共享runner及TestLoader施工文件已恢复。
- **asLoader发布**：CS6已输出包含六个激光生命周期函数的新SWF，Compiler 0 error / 0 warning。编译脚本在超时边界返回1，原生完成标记约晚6ms到达；以新SWF、输出日志、任务完成状态和IDE目标关闭共同复核为 `published_verified`，不将该脚本退出码记为0。最大函数55798字节，低于60000门槛，施工事务和本轮不确定标记均已清理。
- **数值与配方**：balance-check **40/40**、balance-sync零文件变化且ledger无漂移；印花集审计为 `confirmed / displayEligible`，钛合金171既有获取价格未闭合状态仍保留。合成目录283条、281种产物；runtime测试36/36，inspector的男女562条路由检查通过。材料派生字典与文档、装备函数索引检查通过。
- **派生资产**：三个最终任务均无导出警告且已应用：钛合金P90 `e3928520284a47a39a2fde21786f3304`、钛合金171 `a09fe6730ad8480f80ed2654e171fca5`、印花集 `cd3a8a34eafc4b0db0c488ef3ae1cded`。新P90男女双持与171长枪姿态已在Web素材工作台查看；印花集沿用旧素材的静态表现，包括原有青色光束。新共用红束由装备生命周期挂载，离线图片只烘焙实体发射器。

| 首轮产物 | SHA256 |
|---|---|
| `flashswf/arts/new/Codex专用素材.swf` | `d29ec8302306e437e8911ba0071bf9a485bff9c42ca1226059f120ec6dc61ed3` |
| `scripts/asLoader.swf` | `4fd2c88cceaefd8ccf89f1f0d3b884be3760f35533a14601fce81f0112ab3739` |

上述证据覆盖实际AS2测试夹具与Web展示，不替代正式游戏入口的玩家操作复验。重启后的窄范围复验为：合成印花集、左右手及双持钛合金P90、切换战斗姿态、171连续射击和卸下武器。套装供能、护盾和负重增益继续按61式ADR的后续玩法阶段施工。

## 实机画风反馈后的修订

维护者提供的实机截图显示，共用红束比既有武器细，缺少长度方向渐隐；P90中低亮度机身偏灰，第一帧图标展示范围过大。此次只调整原生美术和图标取景。

- **共用红束**：保持250参考长度、安装出口与运行时750长度，将亮红束体由2.8加宽为8，暖白芯为2.2，近晕/远晕为14/20个枪外壳局部单位；每层沿长度淡至透明。参考现役XM556的纵向渐隐，原武器素材保持原样。该共享元件同时作用于钛合金P90和钛合金QJZ171。
- **P90黑色底材**：15个分件/灯壳/弹匣外壳文件共238处中低亮度中性色压暗；例如 `#606061 → #2E2E2F`、`#515152 → #272727`。白甲、红衬及发光颜色保留，渐变与轮廓仍分层。完整替换表在P90源稿的 `assembly.json` 中。
- **第一帧特写**：采用后托白甲、红衬与弹匣接口，`a=d≈0.42, tx=26.05, ty=5.25`，覆盖约四分之一枪长。按24像素实际大小及放大图检查；第二帧继续完整展示。裁切发生在XFL第一帧，Flash和Web从同一源派生。

实际导出还发现嵌套弹匣路径把遮罩外的一段外壳计入第一帧边界，使烘焙后的特写被再次缩小。第一帧改用两个静态展示元件，保留既有分件引用和满弹首帧的49个弹列实例，去掉该视图不需要的弹量时间轴及内层窗口遮罩；10倍导出的内容边界由580×240恢复为240×240。运行时50帧弹匣、第二帧完整展示和装备生命周期维持原实现。

修订证据位于 `tmp/weapon-animation/p90-ti61-visual-polish-20260907/`。正式入口对新画风的复验以重启后的实机结果为准，不能用离线比较页代替。

修订版已由CS6原生保存并发布：Codex素材SWF为44553字节，SHA256 `25d225a55a6b01f061d025dabb0904026e50bcd2a44358da0a223bd92f8c74b8`。最终实际素材测试run `3dff04188c2a4fa88c9a1e1d570a961a` 为89/89、Compiler 0/0、32K retry 0。XFL三件套为85元件、8个导出、无断链或重名；50帧运行时弹匣、171的10帧后坐、游戏数值XML、配方及asLoader字节均保留原值。

最终P90任务 `21e6a1e4117047d283970539655459f4` 与171任务 `c1958e812a394270bd6738d407544e5a` 均无导出警告且已应用。第一帧实际烘焙内容边界240×240，规范化输出128×128，并按24像素查看；整枪及红束导出对比见同一证据目录的 `final-review.png`。

## P90系列性取景回调

维护者进一步指出，后托红白甲板特写不足以辨认P90。与普通P90、战术版和印花集并排核对后，第一帧改为同时展示顶置长弹匣、连续弹列与完整握持孔，左侧白甲红衬保留钛合金版本的配色特征。

当前取景矩阵为 `a=d=0.3599853515625, tx=9, ty=4.2`，覆盖约三成枪长。沿用上一节解决嵌套遮罩边界的静态满弹展示路径；本轮只修改第一帧取景，第二帧、运行时枪体、黑色调色、共用红束、50帧弹匣和游戏数据均保持原值。证据位于 `tmp/weapon-animation/p90-family-icon-20260907/`。

CS6原生保存及发布通过，Compiler 0/0；当前Codex素材SWF为44556字节，SHA256 `a8da8933ea879da41e61ab84c844ee1bc4fe449e49c5febfd41bbada35448344`。图标任务 `0df35a64bc87478ca88922653bdeb567` 无导出警告且已应用，第一帧实际内容边界240×240，规范化输出128×128，并与三款P90按24像素并排查看。第二帧源构图没有变化，按现役导出流程一并重新烘焙。上一节89/89行为测试保留为上一素材版本的证据，本轮以取景实际导出、源变更范围和显示核对为验证重点。

## P90与钛合金171共用纯红方案

维护者确认当前暖白芯仍使激光偏橙，并指定参考AK74战术版；本轮同时覆盖钛合金P90和钛合金QJZ171。原AK74路径为 `枪-长枪-AK74战术版 → 无AS链接/Symbol 3722 → 无AS链接/Symbol 3721`，最后一项使用单层 `#FF0000`、透明度0.6至0的线性渐隐，原束体宽11.5、长737.95。

共用红束复制其视觉画法，将参考长度归一到250、中心线归零，宽度保持11.5；现有生命周期仍按配置延长至750。去掉暖白芯及多层橙红外晕，保留原导出ID与历史元件路径；不复制AK74的load事件显隐脚本。P90的系列图标、黑色调色、171枪体及后坐动画均维持原值。施工证据位于 `tmp/weapon-animation/ti61-pure-red-laser-20260907/`。

当前纯红版Codex素材SWF为44447字节，SHA256 `ffe249c22b8b1605674ee81bbb0cb29725315e7c137d47e50e681a3152f83111`，已由CS6原生保存并发布，Compiler 0/0。实际光束按4倍导出为1000×46，所有可见像素的绿色和蓝色通道均为0；中心线Alpha采样为153、114、76、38、0，长度方向逐渐淡出。

运行时测试覆盖手枪、手枪2和长枪三组fixture，本轮run `6273e2e2b30f4fe8b2a078808a45f71b` 实际89/89、Compiler 0/0、32K retry 0。初次runner快照只采到Start，因缺少Complete返回1；随后在Flash Player原始日志中核对到本轮唯一、顺序完整的Start/Complete及89项结果，并核实当前素材/TestLoader摘要、计划任务Ready且结果0。该次退出码保留为1，以 `native_behavior_verified` 记录补采核验，随后仅清理本轮内容匹配的不确定标记；未修改共享runner或测试断言。

本轮只更换动态光束，静态图标及纸娃娃图片保持原值。正式游戏入口的新色调以重启后的实机复验为准。
