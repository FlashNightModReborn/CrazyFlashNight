# 统一合成与输入归属：R1 裁决输入（C0 → 是否进 C1）

**文档角色**：R1 节点裁决输入包，供用户提交 GPT-6 Pro；不是已采纳结论。<br>
**冻结状态**：HEAD `424fe2eed3`（docs-only）+ 未提交工作区 diff（`tmp/r1-frozen-worktree.diff`，
SHA-256 前 16 位 `12e6bd987030ba60`，23 改 + 3 新文件，1837+/213- 行）。<br>
**候选实际身份**：`tmp/flash-compositor/game/development-pair.json`（2026-09-22T11:07Z）——
Core.exe `3988B5F2` / Core.dll `8205E6B5` / FlashCompositorNative.dll `5D167CA9` /
FlashInputBroker.exe `8DD9DB81` / FlashInputBridge.dll `D5F2886F` / asLoader.swf `19B726C9`
（各为 SHA-256 前 8 字节 = 16 个十六进制字符）。实验 GPU：Intel UHD 630 / GTX 1650。

> G0 勘误（裁决后）：初版材料把两轮 suite 的 covered 帧数混写（77–93），已更正为当前套件
> 四组原始值（71/69/97/68）；"0 像素品红进入源帧"收窄为"采样源 proof 像素未命中品红阈值"；
> 负控2 降级为"判定器逻辑存在、独立构造未实跑"；GPU 名称以报告 Adapter 字段为准。

## 1. 要决定的一句话

S/P/F 两顶层根拓扑（F 嵌入 S、按 S 捕获裁 F 区、P 为独立顶层根完整覆盖源区）在本机实验证明成立后，
**是否进入 C1**（真实 surgery Web 面板纵切 + 两个 Native 代表样本），还是收缩为 A0＋B
（保留现役 Web 后端，直接批量迁移 Native HUD），或先补特定实验再裁决？

## 2. 本轮施工内容（A0＋返修，均已在冻结 diff 内）

| 项 | 内容 | 验证 |
|---|---|---|
| A0.1 多 HUD 诊断 | 全局唯一 HUD 槽 → HWND 注册表 + 五级归属，失效句柄惰性剔除，不冒认 | focused 49/49 |
| A0.2 焦点资格 | 看门狗资格纯状态机：仅"仍有效的用户返回/会话前台"授予；外部前台、失活、抑制边界均撤销；无资格零恢复 | focused 36/36 + 反例矩阵 |
| A0.3 桥协议 v2 | epoch/geometry/gesture/sequence 信封；共享 minEpoch 撤销下界；有限队列 256；Post 分级；版本协商 fail-safe | focused 28/28 |
| 返修 R1 | kernel32 无 Interlocked 导出（真实运行必炸）→ 真实映射薄层 + 对齐 32 位读写 + 真实映射 smoke | 见 §4 负控史 |
| 返修 R2 | 原生 WM_KILLFOCUS/WM_ENTERMENULOOP 抬本地代次下界 + Host owner Deactivate→CancelPointer 双保险 | 原生自测 15/15 |
| 返修 R3 | 审查反例"激活→抑制→NULL"原输出 Restore，修复后输出 Observe | 测试内固化 |
| 返修 R4 | epoch 16 位 wire vs 32 位下界 → 全链 16 位模运算，65535→0 回绕有测试 | 同上 |

关键函数原文（冻结工作区状态）：

```cpp
// InputBridge.cpp —— 过期门（SetFocus/CallWindowProcW 之前）
bool StaleEpoch16(UINT epoch,UINT floor) {
    return static_cast<SHORT>(static_cast<USHORT>((epoch&0xFFFFu)-(floor&0xFFFFu)))<0;
}
// WindowProc 包处理入口：
if(!shared || shared->stop) { if(shared) InterlockedIncrement(&shared->stoppedRejected); return 0; }
if(StaleEpoch16(epoch,static_cast<UINT>(shared->minEpoch)) || StaleEpoch16(epoch,localFloor)) {
    InterlockedIncrement(&shared->staleRejected); return 0;
}
if(!StaleEpoch16(epoch,(lastEpoch+1)&0xFFFFu)) lastEpoch=epoch;  // 高水位在门之后更新
// WM_KILLFOCUS / WM_ENTERMENULOOP：
enabled=false; buttons=0; captured=false;
localFloor=(lastEpoch+1)&0xFFFFu;  // 失焦即撤销已见代次，不等 Host floor
```

```csharp
// ForegroundVacuumWatchdogPolicy.OnForegroundTick（资格在执行前以最新状态复核）
// Session 样本授予 / External 样本撤销 / NULL 中性；OnSessionDeactivated 与
// OnSuppressedTick（锁屏/最小化/面板）均撤销——历史激活不是无限期许可。
// 真空须连续 2 tick（≈800ms）确认；Restore/Observe 共用 2s 输出节流。
```

## 3. C0 决定性实验（probe，真实 GPU 双卡）

拓扑：`--topology embeddedF` —— fixture 控件或真实 Flash 主窗口经 SetParent 嵌入 S；
`ProbeStart(source=S)` + `ProbeSetCrop` 裁 F 区；P 独立顶层根（owner=S、非 S child、
GA_ROOT=P 自身）完整覆盖 F 区（overlap fraction=1.0）。

| 实验 | 结果 | 原始证据 |
|---|---|---|
| 对象隔离 | capture==S、crop==F 区、P 非 S 后代、P 自持根，四组（fixture/flash × intel/nvidia）全成立 | `tmp/flash-compositor/suite-20260922-185603-920/` |
| 覆盖出帧（决定性） | covered 相位 captured/presented 持续递增（四组分别 71/71、69/69、97/97、68/68），proof 每轮落地 | 同上各 embeddedF 报告 |
| 标记隔离 | P 品红标记在所采样源 proof 像素中未命中品红阈值（`streaming-under-cover`）；注意：这是采样判定，不是整帧逐像素计数 | 同上 |
| 遮挡活性 | occluded 相位 captured 68–76 持续；offscreen 1–3、minimized 0–1、hidden 0（隐藏/移屏后停帧是该路线的合法形态，不构成失败）；restore-minimized/restore-hidden 恢复 73–94 | 同上 |
| **负控 1** | 误捕获 P（`--capture-output`）：标记泄漏检出，exit 2，success=false | embeddedF-capture-output 报告 |
| **负控 2** | covered 零出帧构造 `captured=0∧presented>0`：判定器逻辑已冻结在 CorePassed，**独立构造运行未执行**（原报告"成立"为越界声明，降级为逻辑存在，列入补证项） | 无独立原始输出 |
| 回归 | embedded/toproot 既有判定逐字节不变；CLI 负例 8 个 | 套件 22/22（含 8 个 CLI 负例与 2 个误配负控，非 22 个拓扑证明） |

原生接收端反例（真实窗口 + 真实 SetWindowLongPtrW 子类化 + 真实 SetFocus 产生真 WM_KILLFOCUS +
真实共享段）：「Down(epoch=5) 已受理 → 失焦 → 同代次 Down 再到」零 SetFocus、零原窗口过程交付；
代次推进后新 Down 恢复准入。`InputBridgeSelfTest.exe` 15/15（构建目标独立，默认产物不变）。

## 4. 测试与人验

- Host 全量 `run_tests.ps1`：5698 总 / 5694 通过 / 4 跳过 / 0 失败（串行 policy marker 确认）。
- 原生 pinned 工具链构建通过（check-runtime-build-env 校验 OK）。
- 人验（维护者实机，同一候选）：①按住移动键切出、外部释放、返回——不卡键，首击仅激活；
  ②surgery 中文输入/选候选/取消返回——正常（未付费）；③最小化恢复/移动/缩放/战斗换档——
  仅拖动边框缩放有瞬间不跟随（几何交接过渡期 surface 先藏后锚，记为 C1 评估项）。
- 人验与日志对齐：全程 `[FgWatchdog]` 零行（无资格零夺焦）；每次失活配对
  `world_pointer_cancel reason=owner_deactivate`；broker 收尾 `stale=1 unowned=12 dupDowns=0`；
  `world_compositor_failed` 零次；`EntryPointNotFound` 零次。

## 5. 明确未测 / 未知

- 真实 Alt-Tab/任务栏交互体感、真实键盘长按时序（人验只覆盖部分）。
- 多显示器、Per-Monitor DPI、负坐标（本机单屏）。
- broker↔projector 端到端装载在生产装配下的表现（C0 是 probe，不是真 Guardian 装配）。
- WebView2 面板的 CompositionController 接入、IME、capture 丢失恢复（C1 对象）。
- F8 本地消费链路（worldDrag 仍跳过 HookChainResult）；geometry/gesture 字段原生侧仅携带不强制。
- 缩放瞬间不跟随（已知过渡，C1 评估）。
- P 遮 S 标题栏形态、多 SWF 遍历。

## 6. 候选处理（2–3 个）

1. **进入 C1**：surgery 真实面板 + 一个现役 Native 交互目标 + 一块 PlayerInfo 分块缓存，
   按计划 §6 出口 R2 再裁决 B。
2. **收缩 A0＋B**：保留现役 WebView2 后端不接入统一输入，直接批量迁移 Native HUD；
   Web 统一无限期后置。
3. **先补实验再裁决**：指定缺口（如真实 Guardian 装配下的 broker 端到端、或 P 遮标题栏形态）
   补做后再裁。

## 7. 提交提示词（按计划 §9 建议口径）

> 请基于本阶段冻结代码和实际实验裁决是否进入下一阶段。逐项反驳成功声明，区分发送/接收/业务与源帧/呈现；指出负控是否有效、哪些缺口会改变路线。只给当前节点必要的修改，不重开全部架构。若材料不足，明确缺失证据，禁止补造实测。给出继续、有限修补或收缩路线及其条件。
