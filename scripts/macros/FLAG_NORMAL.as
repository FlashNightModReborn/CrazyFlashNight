/**
 * FLAG_NORMAL - 普通子弹类型标志位
 * 
 * 位掩码值：1 << 6 = 64 (二进制: 01000000)
 * 位置：第6位
 * 
 * 功能用途：
 * • 标识普通类型子弹，如常规射击、标准弹药、基础攻击等
 * • 分类条件：满足(!isPierce && !isExplosive)且符合以下条件之一的子弹：
 *   - 近战类型子弹 (isMelee)
 *   - 透明类型子弹 (isTransparency)
 *   - 明确标记为普通的子弹 (子弹种类.indexOf("普通") != -1)
 * • 标准行为：普通子弹使用标准的碰撞检测、生命周期管理和特效处理
 * • 性能优化：普通类型作为默认分类，减少特殊逻辑的处理开销
 * • 兼容基础：为大部分常规子弹提供统一的处理标准
 * 
 * 使用示例：
 * • 检测：(bullet.flags & FLAG_NORMAL) != 0
 * • 设置：bullet.flags |= FLAG_NORMAL
 * • 清除：bullet.flags &= ~FLAG_NORMAL
 * 
 * 应用场景：
 * • 子弹类型的标准分类和识别
 * • 普通子弹的统一处理逻辑
 * • 特殊类型的排除检测
 * • 默认行为的标准化实现
 */
var FLAG_NORMAL:Number       = 1 << 6;  // 普通标志位 - 位值: 64 (第6位)