# 物品系统 → Web Panel：双栏工作台 架构设计

**文档角色**：物品系统从 AS2 迁移到 Launcher Web Panel 的**纲领设计 / ADR**。这是背包 / 仓库 / 商店 / 装备属性 / 强化 / 合成 / 改装等物品相关界面的地基文档，确立统一抽象与迁移序列。技能栏仅复用本文的双栏展示 primitive，领域主线已拆到 [技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md](技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md)；装备调制的行为冻结、事务缺口与旧 UI 绞杀步骤见 [装备调制-Web工作台绞杀式迁移-工程落地规划-2026-07-16.md](装备调制-Web工作台绞杀式迁移-工程落地规划-2026-07-16.md)。

> **2026-08-27 药剂布局增量 supersession**：本文后续 `DrugInventory(4)`、数字槽 `0..3`、`11+4`、四药剂同排和 `快捷物品栏N` root 镜像等文字记录的是既有列车历史，不再作为当前容量/布局真源。现役 authority 为 `DrugInventory(8)`、物理槽 `0..7`、上下同列共享 4 条 lane 冷却；Character Build 使用严格 `drugLayout.v=2` 和两排四列，HUD root 镜像已退役为容器直接投影。完整迁移、兼容和验收边界见[双药剂组与八槽共享冷却 ADR](双药剂组-八槽共享冷却-ADR-2026-08-27.md)。

**最后核对代码基线**：正式 source `730c6be781ddd22bfd7a59a2e7773acce892f105`（角色构筑原始审阅基线为 `3669d48466dde211108c99ad60896c7df4e47964`）已由 tag `runtime-build-v2/20260806-workbench-authority-a1-a6-v2`、request `D45D0DEF50E159B8A875DCA922D856BEC69D64A6B779B6B7B7082D68AC6B92CF` 完成 v2 promotion，配对正式 runtime 达到 `standard_entry_verified`。该 release 包含 A1–A6 权威数据、身份三分离、注释/布局、直接装备与乐观只读缓存闭环，也继续包含此前 legacy renderer/fallback 退役；本次正式入口只覆盖 Launcher 与 allow-listed Help WebOverlay WGC、可信退出、测试存档不变和无新增残留进程，不冒充 Character/Equipment 业务 opener、A1–A6 写操作、物理拖拽或玩家目视签收。历史 GUI、isolated candidate 和 current-tree Gate 仍按各自 identity 承担业务证据，不能跨身份拼接。

**前置必读**：本文只管**物品系统专属的抽象与序列**；跨 AS2 / C# 总线 / Web panel / Flash CS6 的稳定性护栏、协议闭环表规则、C#/AS2/Web 接入清单、验证门槛与收尾格式，一律以 [agentsDoc/as2-web-panel-migration.md](../agentsDoc/as2-web-panel-migration.md) 为准，本文不复制。

---

## 0. 状态

- 阶段：**Step 0–4 已完成并经用户验收；Step 5a 完整仓库曾推进到 Gate A3 候选，Step 5b-1 NPC 商店与 KShop 直接交付结算已完成自动门；2026-07-30 cut 已把仓库/战备箱、装备/角色构筑、NPC 商店、合成和技能教师冻结为 Web-only fail-closed；2026-08-06 双栏工作台 A1–A6 已完成 current-tree Gate、独立审阅、双故障域共识、promotion 与标准入口复验，当前正式状态为 `standard_entry_verified`**。商城库存态固定为背包—战备箱；完整仓库能力由宿舍专用 `workbench profile=warehouse` 承载，Native HUD 默认 `profile=battlebox`。Host 不再读取 `CF7_WEB_INVENTORY_WORKBENCH`，不再派发旧 `warehouse/openEquipUI`；入口失败只提示，不恢复旧 Hub、旧商店、旧改装或旧教师 UI。AS2 业务权威与写后对账不变。历史 GUI、isolated candidate 与自动门按各自 identity 保留，不能与当前 Help-only formal smoke 拼接扩张；当前 tag `runtime-build-v2/20260806-workbench-authority-a1-a6-v2` / request `D45D0DEF50E159B8A875DCA922D856BEC69D64A6B779B6B7B7082D68AC6B92CF` 已完成双故障域共识、promotion DLL 与无 candidate id 的标准入口复验。
- 组件化进度（2026-07-13）：语义卡片/格子、完整/紧凑密度、分类模型/渐进树导航、权威筛选/权威整理控件、分页与 owned 库存视图壳均已从各组合层抽出；商城、NPC 商店、独立仓库/战备箱共用这些基建。纯 Web 展示排序已退役，树筛选承担浏览收窄，未筛选窗口保持 `physicalSlot` 顺序。`InventoryCoordinator` 继续独占窗口请求、lease/write gate、`setFilterSpec` 与 `sortAndMerge`，领域写入未下沉到视觉组件。
- 玩家文案收敛（2026-07-11）：商城/库存共享壳已移除玩家无决策价值的协议英语、实现说明、双栏 marker、重复范围、槽号与空槽文字；正常同步态仅显示可悬浮的状态点，只有同步中、失败与恢复动作保留短文案。顶栏由 64px 收至 48px，业务标题、价格、数量、分页、操作与不可逆后果继续常驻。
- 已决：双栏升维为「通用双栏工作台」；交互=点击为主+拖拽加速；首战=KShop **加重版**（购物车作为可拖拽容器）；右栏亦可换栏。技能可复用这一展示叙事，但拥有独立 `skills` domain、Task 和 AS2 权威服务。
- 细节已定（画布 1024×576 / 拖拽镜像 Web 自绘 / 乐观更新功能完成后补 / 槽位 左∈{目录,购物车}·右∈{背包,购物车} 由视图单例不变量产三配置）：见 §11、§2。
- 长期战略改为按交互形态分流：**物品/商店/装备等全屏管理界面外迁 Launcher Web Panel；常驻只读显示外迁 C# NativeHud；AS2 保留游戏权威与尚不能安全剥离的输入/冷却/活体副作用逻辑**。本迁移只负责第一类，不能再用“所有 AS2 UI 都进 Web”概括全局。
- 跨主线交汇（2026-07-30 收敛）：与「玩家信息界面 → C# NativeHud」迁移（[玩家信息界面-NativeHud迁移-架构设计-2026-06-21.md](玩家信息界面-NativeHud迁移-架构设计-2026-06-21.md) §4.2）仍在装备/卸载交互处交汇，但已按领域拆线：药剂装配的容器、使用与冷却 authority 留在物品/药剂域，管理入口并入角色构筑的 loadout session；技能 equip 归独立 [Skill 主线](技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md) S1/S3。旧全屏物品/教师 renderer 与 fallback 已退役；战斗快捷技能/药剂 HUD 的轻量图标、输入、冷却和现役动画投影继续保留，它们不是全屏 UI fallback，也不能再把旧聚合 SWF 接回 main。
- 装备纵切排序（2026-07-27 路线收敛）：Step 5b-2A 已完成调制权威收口与 Web 反馈纵切，Step 5b-2B 已把装备槽、四槽药剂装配、旧 Hub「个人信息」详情与现役 Web 纸娃娃联合收口为**角色构筑工作台**，并完成隔离候选 E2E。首版以“换装编辑态 / 全宽数值检查态”互斥和批次刷新活体止损；长期再以同源 TS 纯计算核让 Web 本地实时投影。详见 §0.0.36、§8.1、[角色构筑专题规划](角色构筑-Web双栏工作台-工程落地规划-2026-07-26.md)与[调制专题规划](装备调制-Web工作台绞杀式迁移-工程落地规划-2026-07-16.md)。
- 资源箱容量整理（2026-07-21）：ordinary `target_full / inventory_full` 不再 suspend 回 AS2 中转，而是在同一 `panel=loot` / exact `panelInstanceId` 内打开背包—战备箱整理子页；只复用 owned inventory 的 `snapshot/autoTransfer/discard`，瞬态 loot container 继续留在独立 loot domain。详见 §0.0.37、§9.2 与 [S1/S2 ADR](地图资源箱-S1S2真实战利品容器与Web双栏-ADR-2026-07-18.md)。
- 评审收敛：命令分层、双端并发校验、KShop 生命周期、claim 重投影、资源箱边界、排序语义和独立 `InventoryTask` 闭环已落地；Step 5a 曾完成窗口分页、displaySort、权威 sortAndMerge 与整容器失效，其中 displaySort 已在 §0.0.28 退役。Step 5b 已拆成 NPC 商店、装备调制与角色构筑三条纵切；NPC 商店已完成，装备调制、角色构筑与 no-AS2-fallback 退役字节已进入 `f01f4b121a4c…` 正式 runtime，其中 `c4faf14460…` 历史 release 的角色构筑 ↔ 玩家自身技能达到 `standard_entry_verified`。这些旧身份的写入、普通关闭、GUI 与只读 Tuning snapshot 证据不能彼此拼接；2026-07-30 的旧 writer/renderer/fallback 退役以 §0.0.39 的结构门和新人工验收为准，并已由当前独立 promotion 身份发布；各历史身份证据仍不得拼接。

### 0.0 评审改判决议（2026-06-20，外部双评审 + 代码核查）

本文经两份外部 AI 评审（Gemini 3 Deep Think、GPT-5.5 Pro）独立审阅，二者**收敛到同两处地基问题**；随后经代码核查 workflow 把评审的代码级断言逐条对真机源码复验，确认**外部 AI 看不到实现、其中数条断言被代码证伪或夸大**。最终改判如下；下方各节已按此重写，任何与本块冲突的旧表述以本块为准。

**决议 A — 首战 KShop 不承载"泛化 Broker 已验证"，只冻结 Shell/ViewHost。** KShop 全程未流经通用跨栏转移引擎（目录→购物车=模态②意图暂存、checkout=模态②整体提交、claim/discard=模态①b 单向领域写），无一条是 owned↔owned 的 move/merge/swap。故：①§8 把首战正名为「KShop 双栏壳与可靠性试车」，删除"验证完整 Broker"暗示；②在迁移序列插入明确架构门——**KShop（冻结 Shell，Broker/并发契约标 `provisional`）→ 背包↔仓库最小纵切（真正的 Broker 验收门 Gate A2）→ 冻结 Broker → 后续界面**；③§2 第一性原则重写为四层（Shell 只认 View 生命周期 / InteractionBroker 只处理跨 View Intent / DomainCoordinator 负责命令与并发），避免 Broker 退化成同时认识 `shopSaveCart/inventoryMove/equip/claim/sell/craft` 的中央 switch（N² 耦合换地方没消失）。这些抽象在全仓零既有同名符号，拆分/改名零迁移成本。

**决议 D — 废除"完整存储态 canonical descriptor 作 v1 普遍并发防线"，改 AS2 snapshot-local slot-lease。** full canonical 把"防陈旧槽位误操作"扩大成"任意深层字段完全相等"，语义过度、且在 AVM1 上要递归遍历+键排序+对象分配+字符串膨胀（1200 格仓库放大），是性能自杀。改用 AS2 铸造的 slot-lease（O(1) 按对象引用判定）+ 按命令定义的语义前置条件（见 §4.1 新表）。canonical encoder 降级为**诊断工具/测试 oracle/少数确需 exact-storage-equality 命令专用**，不再是 inventory-domain 全局基础设施。**代码核查给 lease 落地补了关键边界**：`add/getItem/纯 rebuildOrder` 全程零克隆保对象身份，且 [InventoryPanelServiceTest.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelServiceTest.as) 已锁定 whole-slot move/swap 的 BaseItem 引用保持，故 lease 可按 BaseItem 引用锁；但公开排序入口 `ItemSortUtil.sortInventory` 必带 `mergeStackables → setItems → createFromObject`，会**全量重铸所有物品对象身份**（不止被合并项），任何经 `setItems`/排序的路径后 lease 必须**整容器失效**，且 `Inventory.merge`（原地 addValue 保身份）与 `ItemSortUtil.mergeStackables`（铸新对象）语义相反，失效规则须按路径分。

**代码核查对评审的纠偏（勿把以下评审措辞回灌进文档）：**

| 评审断言 | 代码裁决 | 文档处置 |
|---|---|---|
| Gemini：`singleAcquire` 返回 false 前可能部分写入 → `acquire_failed` 需 `needs_reconcile` 强制对账 | **证伪**。`acquire` 首步 `require` 纯只读全量预检，容量不足在任何 mutate 前 `return false`（首个写 `_root.金钱+=` 在其后） | **不采纳**。§9.1 现状（`inventory_full/destination_full/acquire_failed` 直接回 idle）正确；补脚注说明原子无副作用，`needs_reconcile` 仅留给 timeout/断连/畸形回包 |
| 评审：照 MapTask 范式"盖回 domain/cmd" | **不准确**。MapTask 盖的是 `panel/cmd`，回包无 `domain` 字段 | §0.1/§9.2 注明：MapTask 盖 `panel/cmd`，`domain/cmd` 是 InventoryTask 新增形状（沿用 PendingRequest 范式、`panel` 位置换 `domain`） |
| GPT-5.5/Gemini（暗示）：既有面板缺并发 token 是缺陷遗漏 | **夸大语境**。merc/pet/arena/kshop 确零 AS2 铸造 token，但用业务身份值比对（`mercId` 字符串相等）已够其单命令场景 | token 是"连续跨库转移"这一新交互的 **net-new 需求**，非修补既有面板缺陷 |
| 评审（隐含）：PanelScale 写死 16:9 铺满 | **不精确**。PanelScale 是通用 contain（`scale=min(w/dW,h/dH)` 保形不裁切），异比画布留 letterbox | §11 注明 contain 语义，勿误导后续非 16:9 画布 View |

**经对抗复核坐实、必须守住的代码契约**（判错会让 ADR 决策跟着错）：legacy `shopCheckout` 已在 2026-07-12 与新结算统一到 `balance >= total`、`finalizeCheckout` 直接交付和“不新增待领取记录”；这是有意的兼容入口行为变更，旧存档既有 `_root.商城已购买物品` 仍由 `shopClaim` 清理。依赖旧“付款后再领取”交互的外部调用方必须显式迁移，不能继续引用更早的严格 `_root.虚拟币 > total` 历史语义。`Inventory.swap` 四步无回滚/同步派发事件/`return true` 恒真（见 §4.1 原子性段，须自建事务重入守卫）；裸 `snapshot` 默认落 `MapTask`、`close` 已 switch 最优先、全文不读 `domain`（见 §9.2 路由顺序）；`ItemSortUtil` 全文不标脏 + `validateSortMethod` 把未知 method 静默回退 `byType`（见 §4.1 `methodName` 规则与 §6 排序语义，须 handler 自校验 + 成功后显式 `dirtyMark`）。

### 0.0.1 2026-07-10 ADR 刷新与 Step 0/1 施工裁决

**本轮只批准并完成迁移序列 Step 0/1**：

> 本节是 Step 0/1 的历史施工边界；当前状态已由后续 §0.0.2 推进到 Step 2，不能再据本节的“未做双栏 Shell”判断现状。

- Step 0：`ShopTask` 对 `saveCart/checkout/claim` 建单写 owner，状态为 `idle/write_pending/needs_reconcile`；第二写分别回 `busy/reconcile_required`，断线/timeout/发送失败/畸形写回包保留对账态，只有进入该状态后新发起并成功的 `bulkQuery` 能解除。活动与有界近期 callId 不再次下发。
- Step 0 Web：连续购物车编辑为“单个在途 + 一个最新期望值”；checkout/claim 等待 save pipeline，claim 全局单飞；未知写结果只做 `bulkQuery`，saveCart 对账后若权威影子不同只发送一条**当前最新值**同步，checkout/claim 永不自动重放；正常 close 等在途写与对账收口后关闭。
- Step 1：单一 `KShopRequestMux` 生成 `wb.<sessionNonce>.<openGeneration>.<seq>`，pending 记录 channel/cmd/generation；错通道/错 cmd 不消费 pending，close/open 后迟到回包不落新 session，Web client timeout 也进入同一对账语义。
- **明确未做**：不新增/修改任何 AS2 `shop*` handler，不改变既有请求/`shop_response`/`panel_resp` 字段形状；不新增 `InventoryTask`、`inventory_response`、slot lease、背包 snapshot、discard 或双栏 Shell。claim 成功后的 inventory snapshot 全链锁仍归 Step 3。

**当前 Launcher 集成事实（旧 ADR 必须吸收）**：

- 所有 Web Panel 由 `PanelHostController` 全 anchor 打开并持有通用 `webPanelPause` lease；KShop 另持 `shopPanelOpen/shopPanelClose` 的 `shop` lease，两个 lease 都释放才真正解除暂停。断线重连同时处理 `_pauseNeedsRestore` 与通用 `webPanelUnpause`，工作台不得自建第三套暂停状态。
- 当时 NativeHud 常驻“装备”仍发 AS2 `openEquipUI`，刘海“战备箱”已切到独立 `panel=workbench`，环境变量尚可回退旧 `warehouse`；这是 Step 0/1 历史状态，已被 §0.0.39 的 Web-only 退役裁决取代，当前代码不得恢复这些命令或开关。
- 生产物品格/装备格必须走 `Icons.html()`，富 tooltip 走 `PanelTooltip.dynamicIconHtml()`，`Icons.resolve()` 仅作首帧 fallback；GridRenderer 必须负责动态图/分层图 wrapper 生命周期，不能把图标退化为静态 URL。
- KShop 已新增 `modules/kshop/dev/harness.html` 与 `node tools/run-kshop-harness.js`；该入口已在 Step 2 按 §0.0.2 扩展为 Gate A1 确定性门，不能用“游戏里点一下”冻结公共 primitive。
- **Step 0/1 当时的自动 gate**：Launcher 全量 xUnit **733/733**（其中 `ShopTaskTests` 20 例）、KShop Edge headless harness 11 项通过、生产 `launcher/build.ps1` 完成并通过当时 117 项 Web runtime 资产检查、图标入口/分层周期审计与 `validate-doc-governance.js` 均通过。本轮没有修改 AS2/Flash 资产，故没有伪造不适用的 CS6 编译结论。Step 2 扩展后的当前数字与人工止损项统一以 §0.0.2 为准。

**本轮顺手冻结的后续 ADR 选择（只定协议，不施工）**：

1. snapshot v1 只保留 range-capable 形状 `{v,requests:[{containerId,offset,limit}]}`；背包首版也用 `offset:0,limit:50`，不再同时维护 `{containerIds}` 第二真相。
2. slot lease 为 AS2 铸造的不透明 `sessionNonce+leaseId`，服务端记录真实 `{containerId,physicalSlot,occupantRef,...}`；`containerEpoch` 独立存在，**不嵌入每个 lease 令无关槽随任意写全失效**。move/merge/swap 只失效并重铸受影响槽；排序/`setItems`/整容器重铸才清空该容器全部 lease 并递增 epoch。
3. claim 后续选用 **purchased-list token + 位置索引**，不造持久化 receipt ID：`shopBulkQuery` 返回 AS2 铸造的 `{purchasedToken}`，`shopClaim` 必须携 `{purchasedIdx,expectedPurchasedToken}`；checkout/claim 改变列表即换 token，重连换 session 后旧 token 必拒 `stale_state`。这是 Gate A2 前独立 shop 协议迁移，**不回填本轮 Step 0/1**。
4. 装备与药剂的管理命令后续归 `domain=loadout` 的 `CharacterBuildTask`，通过工作台共享 write arbiter 与 InventoryCoordinator 串行化；AS2 `CharacterBuildService` 分别调用装备/药剂规则，原子完成背包源与目标槽的权威提交，不把 DrugInventory 伪装成装备 inventory owned 容器。角色构筑 Panel 持有统一 pause lease 时，昂贵的装备活体重建允许只标记 `liveRefreshDirty`，延迟到切入数值检查态或关闭前一次性 flush；药剂使用/冷却继续留在既有药剂服务，快捷技能继续归独立 skills domain。

### 0.0.2 2026-07-10 Step 2 施工结果与 Gate A1 止损裁决

**Step 2 已在当前工作树完成到 Gate A1 候选态**：

- 新增 [workbench.js](../launcher/web/modules/workbench.js)，独立提供 `DualPaneShell`、`WorkbenchViewHost`、`ViewChrome`、`GridRenderer`、`ContainerViewAdapter`、`GridContainerView`、`InteractionBroker` 与 `PointerDragController`。Shell 负责双槽生命周期、按 `instanceKey + singletonByBinding` 的原子冲突回落、栏位焦点和模态焦点恢复；GridRenderer 不持传输 pending。
- KShop 仍由既有 `panel=kshop` 入口打开，但生产 DOM 已替换为 1024×576 双栏工作台：左=`shop:catalog`，右=`shop:cart` 复合 View；购物车明确标 `item-model=intent`，待领取明确标 `owned-pending`。Step 3 背包 View 不存在前，KShop 不伪造 `(购物车,背包)` 配置；共享 Shell 的默认回落/单例算法用 mock View 独立验证。
- 目录→购物车支持“选择 source 后点击 sink”、双击直送与 pointer 自绘拖拽；统一经 Offer×Accept 产出中性 `{operationId:"shop.addCartIntent"}`，再由 KShop 领域层改购物车。没有 HTML5 native drag，也没有把目录意图包装成 inventory transfer。无 move/up 的 pointerdown 以可配置超时强制释放 capture/幽灵。
- checkout 已变为壳内模态②：确认前不下发；ESC 只退模态、保持面板和购物车；确认后继续复用 Step 0 的 save pipeline/单写 gate/对账，legacy `shop*` 请求与回包形状完全不变。
- `Workbench.contractStatus()` 将 Shell/ViewHost/Grid/pointer 标成 `gate-a1-candidate`，将 `InteractionBroker` 明确标成 `provisional`，将 owned transfer 标成 `not-present`。KShop 只能证明 catalog intent→cart，**不能据此冻结 Step 4 的 owned↔owned Broker/lease/并发 API**。

**自动 Gate A1 证据**：`node tools/run-kshop-harness.js` 为 **20/20**，另含公共层禁词静态审计（`ShopTask/InventoryTask/callId/saveCart/checkout/claim` 不得出现在 `workbench.js`，GridRenderer 不得持 transport state）；覆盖双挂载、item-model 分离、PanelScale、焦点、单例冲突回落、模态确认/ESC、点击 sink、pointer 拖拽、超时清理和 1200 项离屏 GridRenderer 基线。Edge 视觉场景已核对 1024×576、1366×768、1600×900 与 1280×960（4:3 contain 信箱），均无 body overflow/page error/failed request。Launcher xUnit **733/733**；生产 `launcher/build.ps1` 完成并通过 **118 项 Web runtime 资产检查**，确认 `workbench.js` 进入发布闭包。

**Gate A1 止损点**：自动证据已齐，但公共 primitive 只有在真机确认实际图标、全量 13 类目录、InputShield/CDP 拖拽、模态/ESC/遮罩与 K 点写入/重启读档后才从 candidate 记为人工验收通过。本轮到此停止；不创建 `InventoryTask`，不改 AS2，不实现背包/丢弃/claim→inventory 链，不进入 Step 4。

### 0.0.3 2026-07-10 Step 3/4 施工结果与 Gate A2 止损裁决

- 用户已确认 Gate A1 验收通过；拖拽越过阈值后 tooltip 立即隐藏，拖拽期间悬停移动不能复活，Gate A1 回归保留。
- AS2 新增 `InventoryPanelService`，安装 `inventorySnapshot/inventoryDiscard/inventoryMove/inventoryMerge/inventorySwap`；snapshot 唯一形状为 `{v,requests:[{containerId,offset,limit}]}`，空槽和占用槽都铸 snapshot-local lease。`ArrayInventory.transactionWrite/publishTransactionChange` 将无事件提交与提交后事件分开；双端预检、第二端失败回滚、dirtyMark、事件重入守卫与成功 snapshot 在一个权威事务路径内。
- Host 新增 `InventoryTask/inventory_response`：`domain=inventory` 在通用 close 后、legacy cmd 前早分流；payload v1 递归重建白名单字段，拒绝 partial `count`、未知 cmd/version、非法 callId/lease，回包恢复原 `domain/cmd/callId`。断线清 pending，活动/近期 callId 不重放。
- Web 新增 `inventory-runtime.js` 的 `InventoryCoordinator`，继续复用单一 `KShopRequestMux`；KShop 顶栏可在采购态与 `(背包,仓库当前页)` 间切换。owned View 共用同一 `ContainerViewAdapter`，Offer×Accept 只产 `{operationId:"inventory.transfer"}`，Coordinator 仅按目标槽状态选 move/merge/swap，不按具体 View 对写配对分支。v1 只 whole-slot、round-trip 确认，不含排序/搜索/乐观更新/复杂分页。
- KShop purchased-list 并发迁移已完成：`shopBulkQuery/checkout/claim` 维护 AS2 `purchasedToken`，claim 必须携 `expectedPurchasedToken`；claim Web 锁从请求延长到 inventory snapshot 完成，成功但刷新失败只允许重试 snapshot，不重发 claim。
- **Gate A2 八项证据**：AS2 `InventoryPanelServiceTest` 19/19 覆盖 source/target 陈旧、空目标被占、merge count 漂移、领域拒绝、同步事件重入、第二端提交失败无部分写、正反向 move/merge/swap、discard 确认投影；Web harness 26/26 覆盖同一 operation 左右反转与 owned pair 零分支静态审计。`Workbench.contractStatus()` 当前为 Shell/View/Grid/pointer=`gate-a1-accepted`，InteractionBroker/owned transfer=`gate-a2-candidate`。
- **发布证据**：TestLoader fresh trace；asLoader publish `0 个错误, 0 个警告` 且 SWF 从 926242 刷新为 929950 bytes；AS2 class ownership 与 AVM1 64KB 门通过；Launcher xUnit 748/748；`launcher/build.ps1` 完成并通过 119 项 Web runtime 资产检查；1366×768 inventory 视觉无 body overflow/page error/failed request。

**Gate A2 止损点**：自动架构门已达到，Step 5/6 不在本轮授权范围。仍需人工在真实存档覆盖实际动态图标、InputShield 点击/拖拽、背包↔仓库 move/merge/swap/discard/claim、断线后重开对账与重启回读；完成前只称 `Gate A2 candidate`，不称生产迁移完成。

### 0.0.4 2026-07-10 Gate A2 验收与 Step 5a / Gate A3 止损裁决

用户已明确“整体验收通过”，故 Gate A2 正式记为人工验收完成，`InteractionBroker/ownedTransfer=gate-a2-accepted`，并放行 Step 5。原 Step 5 同时打包“1200 槽仓库”“NPC 金钱买卖”“装备活体副作用”三类独立高风险，不适合作为一个不可分割施工单元；本轮新增 **Step 5a / Gate A3**，先冻结完整仓库的窗口、整容器失效和性能契约，再让金币/装备风险进入 5b。

- **浏览器行为边界**：新增 Overlay 启动级 `game-ui-behavior.css/js`，游戏 UI 默认抑制 `selectstart/dragstart/contextmenu`、文本选取和原生图片/链接拖影；`input/textarea/contenteditable/[data-browser-native]` 显式放行。KShop harness 同时验证事件与计算样式，避免蓝色文本选择层重新混入游戏视觉。
- **完整仓库窗口**：仓库容量仍由 AS2 snapshot 返回（当前 1200），Web 只保留当前 50 槽 request/window；翻页重拉背包 + 当前仓库页以重铸 snapshot-local lease，DOM 始终只渲染 50 个仓库槽。当前页 `displaySort=physicalSlot/name/quantity` 只重排 DOM，模型仍携权威 `physicalSlot`，不发写命令。
- **权威整理**：新增 `domain=inventory cmd=sortAndMerge` → `inventorySortAndMerge`。C# 与 AS2 双重严格白名单八种策略；AS2 在隔离 `ArrayInventory` 上调用既有 `ItemSortUtil` 生成计划，经“每物品名数值总量 / 非数值实例数”守恒校验后才用 `transactionReplaceAll` 一次提交。未知/插件历史物品若会被旧反序列化路径过滤，整次返回 `sort_failed`，原仓库不变。
- **整容器失效**：成功整理后只清仓库 lease、`containerEpoch+1`、`dirtyMark`，再在事务 guard 内派发生命周期事件并返回原当前页；背包 lease 不被无关整仓操作误伤。commit failure、未知策略、lossy plan 均不标脏、不产生部分写。
- **证据**：`InventoryPanelServiceTest` 39/39 fresh trace；50/400/1200 × 0/50/100% 占用的 50 槽窗口各连续 100 次，实测约 140–339ms、单回包约 3.5–16.6K 字符；1200/1200 满仓整理合并 282ms。Launcher xUnit 750/750；Edge harness 31/31，页面错误/失败请求为 0；1024×576 与 1600×900 视觉无 body overflow，仓库工具条按两行稳定排布。

**Gate A3 止损点**：自动架构门已达到，当前标 `gate-a3-candidate`。真实存档仍须人工覆盖背包内/仓库当前页内/跨容器的 move/merge/swap，24 页首/中/末页、真实装备与材料排序、整理后重启回读、InputShield 下分页/选择器/确认模态，以及含插件/历史未知物品时“拒绝且不丢物”。在 Gate A3 人工验收前不进入 NPC 金钱买卖、装备槽/属性或 Step 6。

### 0.0.5 2026-07-10 Gate A3 真机反馈：同容器换位闭环

真机深度使用发现背包↔仓库成立，但背包→背包、仓库→仓库被 Web `probeAccept` 与 AS2 `executeTransfer` 双重拒绝。该限制不属于 Step 5b，而是 Gate A3 owned-container 基本能力缺口；现已改为只拒绝**同一 physicalSlot**，同容器不同槽继续按目标态选择 `move/merge/swap`。AS2 最终裁决保留双 lease、两槽事务、第二槽失败回滚、dirtyMark 和生命周期事件；同容器同 50 槽窗口的成功回包去重为一个 snapshot，避免同 containerId 重复投影。自拖回原槽在 Web 零写入，AS2 另以 `same_slot` 防止自 merge 数据损坏。

回归刷新为：Edge harness **35/35**（含背包 pointer move、背包 stack merge、仓库 swap、自槽零写入和跨容器不回归）；Launcher xUnit **751/751**（同容器 source/target 白名单透传）；`InventoryPanelServiceTest` **46/46** fresh trace（含同容器三操作、snapshot 去重、自槽保护与提交失败回滚）。Gate A3 仍保持 candidate，等待真实存档复核后再放行 5b。

### 0.0.6 2026-07-11 提交审阅修复：商城就绪门与真实材料合并

提交审阅发现两处自动门假阴性并已收口。其一，KShop 模块会跨 open generation 保留旧目录/购物车，而写协调器在新 `bulkQuery` 返回前已开放编辑与结账；现在每次打开先清空旧投影并将 `shopReady=false`，只有形状完整的权威 `bulkQuery` 成功后才开放加购、结账和领取，失败或关闭继续锁写。Edge harness 新增延迟 `bulkQuery` 的重开用例，自动门更新为 **36/36**。其二，既有 `ItemSortUtil` 把材料误写成不存在于正式数据的 `type="材料"`，测试夹具也复刻了该错误；正式 `data/items` 使用 `type="收集品" + use="材料"`。排序工具现按数值型 `消耗品/收集品` 判定可堆叠，测试夹具同步采用正式 schema，避免“整理成功但真实材料不合并”。

### 0.0.7 2026-07-11 背包/仓库实例注释与抓取抑制

owned 槽位新增 `domain=inventory cmd=tooltip` 只读闭环。请求必须携当前 `{containerId,slot,expectedLease}`，AS2 重新校验 occupant 引用后直接把真实 `BaseItem` 交给 `TooltipComposer`，因此装备强化、进阶与配件信息不会退化为 name-keyed 基础说明；旧 lease 返回 `stale_state`。Web 端仅在 occupied 槽静止悬浮时显示注释，点击选取 source 或开始 pointer 拖拽即隐藏并保持抑制，释放后须重新进入槽位才显示，避免注释框与抓取镜像持续碰撞。Edge harness 更新为 **38/38**。

### 0.0.8 2026-07-11 owned 物品格中文信息密度收敛

物品格不再向玩家显示内部机器分类 `stack/equipment`；`itemKind` 仍保留在协议模型中供 move/merge/swap 决策。非装备数量与装备强化度迁到图标右下角：`<10000` 显示精确整数，之后按中文 `万/亿` 向下截断为紧凑值，完整数量留在 `title/aria-label` 与富注释；数字始终单行。装备达到动态 `EquipmentUtil.getMaxLevel()`（当前 13）时只给图标框加金色满级框，不占用卡片交互边框。装备名称下固定渲染 `阶 + 插件 1/2/3` 四格状态条，分别表达不存在、存在未使用、已使用；AS2 presentation projection 从真实实例派生 `maxEnhancementLevel/isMaxEnhancement/tierSlotAvailable/tierSlotUsed/modSlotCapacity/modSlotUsed`，Web 不复制升阶或插件槽规则。1024×576 visual runner 已确认大数量不换行、背包四格完整；仓库窄卡片使用 8px 槽单元。Edge harness 更新为 **39/39**。

### 0.0.9 2026-07-11 升阶/插件语义拆分与档级角色编码

0.0.8 的 `阶 + 1/2/3` 四个同形文字格已被本轮方案取代：升阶不是插件，现改为装备图标左上角独立菱形（空心=可升阶、实心=已升阶）；名称下方只保留三个插件槽，空槽/不存在仍以轮廓状态表达，已安装槽用“颜色=材料档级、形状=主要角色”的正交编码。四档固定沿用现有图标边框标准色：低级 `#006600`、中等 `#996600`、高等 `#0099FF`、特殊 `#FFFF00`；特殊档现有紫/蓝错框视为美术流程问题，不做运行时取色或兼容。

新增 `data/items/equipment_mods/ui_presentation.xml`：集中维护角色的受控符号 token 以及 35 个现役 `tag` 的默认角色。插件子文件仍是语义归属，必要时可用 `<uiRole>` 显式覆盖；加载器按 `低级/中等/高等/特殊材料_` 文件前缀派生档级，并在注册前解析 `uiGrade/uiGradeColor/uiRole/uiSymbol`。库存 presentation projection 新增 `modSlots[{name,grade,gradeLabel,gradeColor,role,roleLabel,symbol}]`，Web 仅消费白名单 token，不解析 XML、不猜插件效果。`node tools/validate-equipment-mod-ui.js` 已接入 Launcher build。

### 0.0.10 2026-07-11 插件槽镶嵌式辉光收敛

0.0.9 首版的整槽档级填色在暗色工作台中接近可点击按钮/技能快捷栏，并与装备图标争夺视觉焦点。本轮不改 XML、AS2 projection 或角色协议，只收敛 Web 材质：插件槽体统一为中性深灰金属凹槽，带暗银边、内高光和底部阴影；档级标准色仅进入符号核心的浅亮染色、中心微光和 2–3px 短距离 `drop-shadow`。低/中/高/特殊仍分别由 `#006600/#996600/#0099FF/#FFFF00` 驱动，空槽和不存在槽不发光。升阶菱形同步改为中性凹槽＋紫色内部光点，继续与三个插件槽保持语义和位置分离。Edge harness 维持 **39/39**，并新增计算样式断言：四档已装槽背景必须统一为 `rgb(11,15,19)`，档级色只能留在 `--mod-grade-color` 与内部符号；1024×576 visual runner 无 overflow。

### 0.0.11 2026-07-11 插件符号线框语义与玩家可见名修正

角色符号 token 扩展为 `形状-solid|outline` 的受控组合：火力使用 `triangle-solid`（▲），新增“精准与操控”角色使用 `triangle-outline`（△）；稳定与防护、续航、结构与功能分别使用线框方形、圆形、菱形，特殊机制保留实心星。`柄侧板/柄尾/侧导轨/瞄具/枪机/枪口/握柄包覆` 从原“稳定与防护”调整为“精准与操控”，使三角实心/线框差异有稳定的功能含义，而非纯装饰。加载器、构建审计与 Web 都只接受白名单 token；Web 对首版无填充后缀的五个旧 token 保留渲染兼容，但 XML 单源只产出新 token。当前闭包为 102 插件 / 35 tag / 6 role。

同时修正 owned item projection：协议继续保留内部 `name` 作为存档/图标查找键，`displayName` 改为优先读取物品数据 `displayname`，缺失或空字符串时才回退 `name`。因此“吉他喷火器”这类内部名会在格子与 lease-bound 注释中正确显示为“烈火吉他”，动态图标仍使用独立 `icon` 字段。自动门新增 name/displayname 分离夹具及实心/线框计算样式断言；验证为数据闭包 102 插件 / 35 tag / 6 role、Edge harness **39/39**、`InventoryPanelServiceTest` **50/50** fresh trace、Launcher xUnit **753/753**、整包 build 与文档治理通过，1024×576 inventory 截图无 body overflow。真机仍需用实际装备确认小尺寸线框辨识度。

### 0.0.12 2026-07-11 24 页仓库快速定位

前后页箭头继续承担相邻浏览；中间页码由静态 `1 / 24` 改为可展开的 4×6 页码矩阵，当前页金色高亮，每个页码的 title/aria-label 同时声明对应物理槽位范围。工具条常驻显示 `1–50 / 1200`，支持 `PageUp/PageDown` 相邻翻页及 `Ctrl+Home/End` 首末页；编辑控件、商城态和模态打开时不截获快捷键。点击外部或 ESC 关闭矩阵，页码按钮支持方向键六列导航。`InventoryCoordinator.close()` 不重置 request offset，因此同一 Web 模块会话关闭再打开自然回到最后页，不引入 localStorage。

直接跳页仍只调用既有 `setWindow("仓库", offset, 50)`，每次只请求背包 50 槽与目标仓库 50 槽，未改变 snapshot/lease/C#/AS2 协议。Edge harness 新增 `kshop-ui8x`，覆盖第 14 页直跳、PageDown、第 24 页边界、重开记忆和 Ctrl+Home，当前 **40/40**；`--visual=inventory-pages --viewport=1024x576` 确认矩阵不被 ViewHost 裁切、工具条未溢出、body 无 overflow。

### 0.0.13 2026-07-11 库存分页与整理控件组件化

完成组件化阶段 1–2，新建 [inventory-ui.js](../launcher/web/modules/inventory-ui.js)。纯函数 `derivePageState(snapshot,request,defaults)` 统一 offset/limit/capacity 到 page/pageCount/物理槽位范围的派生，`pageFromShortcut` 统一 `PageUp/PageDown/Ctrl+Home/End` 目标；`InventoryWindowPager` 接管前后页、4×6 页码矩阵、方向键/ESC、外部点击关闭、全局快捷键 attach/detach 与 disabled/boundary 状态。组件只通过 `getSnapshot/getRequest/onRequest/onBeforeChange/onResult/shortcutEnabled` 注入点工作，不读取 KShop request mux、callId、购物车或具体 AS2 action。

`InventorySortControls` 现只组合权威树筛选、八策略权威整理选择器与提交按钮。原当前页 `physicalSlot/name/quantity` 展示排序已在 §0.0.28 退役，避免与树筛选形成第二套浏览决策；未筛选窗口直接沿用 snapshot 的 `physicalSlot` 顺序。权威整理控件只发出 `{methodName,label}` 意图，由 KShop 打开确认模态，最终仍调用 `InventoryCoordinator.sortAndMerge`。丢弃、claim、商城 checkout 等领域策略未进入组件。`kshop.js` 删除仓库页码计算、菜单渲染、快捷键监听和直接 select/button 引用，只保留容器配置、权限 predicate、确认文案与结果提示。

加载闭包已同步 `panels-lazy-registry.js`、`launcher/build.ps1` 与 dev harness；KShop 架构审计会拒绝旧分页函数回流组合层，并要求 `derivePageState/InventoryWindowPager/InventorySortControls` 三个边界同时存在。Edge harness 保留独立页码模型断言；24 页矩阵、权威树筛选、展示排序退役、sortAndMerge、会话位置记忆与 owned transfer 用例全部通过。

生产验证：`launcher/build.ps1` 通过，Web runtime 发布闭包为 **122 项**且包含 `inventory-ui.js`；`launcher/tests/run_tests.ps1` 为 **753/753**。构建只出现既有 pet roster、miniaudio 与 WindowsBase warning，没有本组件化引入的新错误。

### 0.0.14 2026-07-11 玩家文案与排版空间收敛

本轮以玩家决策链而非开发诊断链重排 KShop 视觉信息。共享壳不再显示 `PROCUREMENT WORKBENCH`、`CATALOG SOURCE`、`INTENT BUFFER` 等英文层级，也不显示 AS2、snapshot、lease、physicalSlot 等实现解释；商城只保留“商品/购物车/待领取”，库存只保留“背包/仓库”。正常 `ready` 状态压成状态点并通过 `title/aria-label` 保留“已同步”，同步中、失败、库存重试等会影响操作的状态仍显示玩家短文案。内部错误码进入 `console.warn`，不再直接进入 toast。

布局同步把共享顶栏从 64px 压到 48px，隐藏 22px 双栏 marker 行和各 View kicker，中央 rail 只保留方向箭头。仓库分页常驻文案由“第 N 页 + 槽位范围”合并为 `N / 24`，物理范围只保留在页码按钮的 title/aria-label；页码矩阵移除“页码/物理槽位”标题。物品格不再渲染可见槽号或 `EMPTY`，空槽使用弱横线，背包丢弃按钮仅在 hover/focus 时出现。结账、整理、丢弃和关闭对账模态全部改为玩家后果说明，同时继续保留确认门与领域写入边界。

Edge harness 增加两项玩家文案守门：商城态拒绝技术英语/正常状态长文案回流，库存态拒绝槽号、空槽文字、重复范围和协议术语回流；结账、整理、丢弃既有测试同步核对确认框只出现玩家文案，当前为 **43/43**。`workbench|inventory|inventory-pages|modal` 四种 1024×576 visual 均无 body overflow，页码矩阵单独顺序重跑无绘制残缺。

### 0.0.15 2026-07-11 刘海战备箱独立双栏工作台

数据核查确认 `_root.物品栏.仓库`（1200）与 `_root.物品栏.战备箱`（400）一直是独立存档容器；此前 Web 侧出现 24 页，是 KShop 库存态明确请求真实仓库 1200/50 的结果，不是数据串仓。旧刘海 `WAREHOUSE` 只是在共享 Flash `_root.仓库界面` 上临时绑定战备箱，因此视觉上像“仓库伪装”。现新增 [inventory-workbench.js](../launcher/web/modules/inventory-workbench.js)，刘海直接打开 `panel=workbench` 的背包—战备箱双栏，不进入 `shopPanelOpen/bulkQuery/saveCart/checkout/claim` 生命周期。

AS2 `InventoryPanelService` 已把战备箱纳入 inventory domain，并把旧 `计算战备箱总页数` 的剧情规则收口为 `getAccessibleCapacity`：物理容量继续为 400 保持存档兼容，可访问容量按进度只为 0/40/120/200/240，快照新增 `accessibleCapacity/pageSizeHint/locked`；越界读取与写入返回 `slot_locked`。Web 页码组件以可访问容量派生 0..6 页，右栏 40 槽采用 5×8 紧凑图标柜，一屏看全；背包仍为 50 槽带名称的操作区。两面板共同复用 `InventoryUI.renderOwnedSlot`、`InventoryWindowPager` 与 `InventoryCoordinator`，没有复制 lease 或事务实现。关闭只做 Web cleanup/unpause，不触发旧 Flash 仓库重排。

后续复核把图标加载从工作台私有 `onOpen` 上移为 `Panels.init/open` 的共享 required-assets 门：Overlay 启动即预热 manifest，任何生产 Panel 都在加载尝试完成后才进入 `create/onOpen`；失败仍由 `Icons` 空 map + 各视图占位 fail-open，不永久锁死 UI。战备箱同时补齐 `[data-panel=workbench] #panel-content { inset:0 }`，消除通用 `4% 6%` 卡片内缩；固定 1024×576 画布继续按当前 Web Panel 标准等比铺满全 anchor。dev harness 已移除会掩盖生产缺口的全局 `#panel-content{inset:0}`。

确定性证据：Edge harness **49/49**（含首个 Panel 冷启动 required-assets 门、全屏 anchor、无商城请求、槽位与富注释使用同一真实图标、最多 6 页、400 保留区不可见、未解锁态、背包↔战备箱双向移动）；Launcher xUnit **756/756**；Flash `InventoryPanelServiceTest` **59/59** fresh trace；`publish`/`test` 均重写目标 SWF；build 通过并检查 **123 项** Web runtime 资产。`--visual=battlebox-real-icons --viewport=1366x768` 已用生产 manifest 同帧确认格子与注释图标，`contentRect=1366×768`、`fullAnchor=true`，并在 1024×576 / 1920×1080 复核无 body overflow/page error/failed request。尚需真游戏用现有存档复核入口点击、剧情不同阶段页数、移动后存盘重启回读；自动门不替代这项人工验收。

### 0.0.16 2026-07-11 三容器共享整理、分类与权威整理

`InventorySortControls` 已扩展为可组合的三段工具：显示排序、分类筛选、权威整理。KShop 库存态的背包与仓库、独立工作台的背包与战备箱都挂载同一组件；宿主只负责玩家文案、确认框和容器权限。打开面板时分类复位为“全部”以避免隐藏物品造成误判；仓库页码位置仍按既有会话策略记忆。

分类不是 Web 当前页过滤。`filterKey=all|weapon|armor|consumable|material|other` 经过 `InventoryTask` 严格枚举后，由 `InventoryPanelService` 扫描容器权威可访问范围、按匹配项分页，并返回 `viewCapacity/filterKey`、真实 `physicalSlot` 与 slot lease。分页组件以 `viewCapacity` 派生页数；筛选状态下发生转移、丢弃或整理后，Coordinator 丢弃可能不符合当前筛选的局部写快照并重新请求所有当前窗口，避免筛选状态悄然退回“全部”。

权威整理范围按容器明确裁决：背包整理完整 50 槽，仓库整理完整 1200 槽，战备箱只整理 `getAccessibleCapacity` 给出的已解锁前缀。`ArrayInventory.transactionReplacePrefix` 在一次事务中紧凑写入前缀并逐槽保留后缀对象引用；AS2 回归用物理槽 80/399 的哨兵验证锁定保留区不被移动或重铸。所有成功路径继续 bump `containerEpoch`、重发必要生命周期事件并 `dirtyMark`。

当前自动门：Edge harness **54/54**，Launcher xUnit **760/760**，Flash `InventoryPanelServiceTest` **68/68** fresh trace；1024×576 与 1366×768 的库存/战备箱截图均 `fullAnchor=true`、工具条无 overflow、无 page error/failed request。真游戏仍需用实际存档复核真实物品分类、24 页仓库整理、不同剧情阶段战备箱前缀与重启回读。

### 0.0.17 2026-07-11 宿舍真实仓库与商城战备箱入口归属

容器语义现正式分流：KShop 的库存模式从背包—真实仓库改为背包—战备箱；Native HUD `WAREHOUSE` 同样固定 `profile=battlebox`。真实仓库只由宿舍 `宿舍房间.xml` 的仓库按钮调用 AS2 `openInventoryWorkbench({profile:"warehouse",source:"dormitory"})`，经 `panel_request panel=workbench initData.profile` 交给 Launcher。C# 只接受 `warehouse|battlebox` 并重建固定 runtime initData，拒绝 XFL 夹带任意 `containerId`；2026-07-30 起 Launcher 不可用、发送失败或准入失败只显示可见错误并 fail-closed，不再回退原 Flash 仓库界面。

`inventory-workbench.js` 不再写死右栏，而是在每次 open 前以 profile 重建双栏 DOM 并调用 `InventoryCoordinator.configureRequests`：仓库使用 50 槽窗口/24 页，战备箱使用 40 槽窗口/剧情 0..6 页。两者继续共用 snapshot、lease、tooltip、权威树筛选、sortAndMerge 与 move/merge/swap，不复制领域写逻辑，也不进入商城 `bulkQuery/saveCart/checkout/claim` 生命周期。

自动证据刷新为：Edge harness **55/55**（新增商城战备箱与宿舍仓库 profile 24 页/双向移动）、Launcher xUnit **762/762**（默认/显式/非法 profile 路由）、Flash `InventoryPanelServiceTest` **71/71** fresh trace；asLoader 与 `基地场景合集.swf` 均通过刷新门，后者另经 FFDec 导出确认宿舍按钮 bytecode 含 `openInventoryWorkbench({profile:"warehouse",source:"dormitory"})`。这组数字是 2026-07-11 历史里程碑；当前真游戏人工门改为确认宿舍 Web 成功链、断线可见 fail-closed、商城/刘海战备箱及存档重启回读，不再测试 Flash fallback。

### 0.0.18 2026-07-11 Step 5b-1 NPC 物品商店纵切

Step 5b 不再把金币商店与装备活体副作用绑成一个施工包；首个纵切只迁 NPC 物品商店。生产主入口是主 XFL `sprite/Symbol 1770` 的场景 NPC 对话按钮，使用 NPC 初始化时保存的 `NPC商店检索名` 调用 `openNpcShop({shopId,source:"world_npc_dialogue"})`；平板联系人是次级入口。共享 `_root.物品UI函数.刷新商店图标` 按 `_root.shops[shopId] === NPC物品栏` 的对象身份反查，并以 `source="npc_shop_refresh"` 进入同一 Web opener；2026-07-30 起 Launcher 不可用、发送失败或准入失败都只显示可见错误并 fail-closed，不再执行原版商店 UI。数据现已拆到 `data/shops/npcs/*.json`；物品数据、情报门槛、口才倍率、金钱与容器仍由 AS2 权威解释，Web 不复制价格公式，也不决定买入落点。

右栏顶层采用两个并列 owned View：`bag` 映射 `_root.物品栏.背包`，`collection` 作为收集品复合 View，内部切换 `material`→`_root.收集品栏.材料` 与 `intelligence`→`_root.收集品栏.情报`。三视图只在 Web Shell 组合：背包 snapshot/lease 唯一来自 `domain=inventory`，NPC snapshot 只返回材料/情报，不把材料/情报伪装成背包筛选，也不再让两个域各投影一份背包。左栏固定 NPC 目录；购买后 AS2 按 `ItemUtil.singleAcquire` 的实际规则路由到背包、材料、情报或既有药剂快捷栏，并回 `destinationView` 供提示。出售边界保持原版语义：背包与材料可售，情报只读；背包使用容器版本绑定的稳定 slot lease，材料使用同商店、同 key/count 下跨纯读稳定的 collection lease。

样品栏继续最多选择 5 个物品名，但批售改为两阶段：`batchPreview` 在 AS2 扫描背包、跳过强化/进阶/带插件等非普通实例并铸造 opaque `batchToken`；`batchSell` 先逐项复核对象引用、槽位和数量，token 单次消费，未知写结果只允许 snapshot 对账、不自动重放。Host `NpcShopTask` 独占该 domain 的 `idle/write_pending/needs_reconcile` 写门。

自动证据：Web NPC shop harness **15/15**，Launcher 全量 xUnit **777/777**，Flash `NpcShopPanelServiceTest` **17/17 fresh trace**；`asLoader.swf` 最终刷新为 **939213 bytes**，主 `CRAZYFLASHER7MercenaryEmpire.swf` 已刷新为 **485665 bytes**。FFDec 导出确认主入口 bytecode 含 `source:"world_npc_dialogue"`，asLoader 含 `resolveShopIdByCatalog` 与 `source:"legacy_shop_refresh"`；独立 `平板电脑界面.swf` 的次级入口仍保留 `source:"tablet_contacts"`。真机首轮先发现 NPC 本地 `LiteJSON` 出口把打开请求发送成字面量 `undefined`；打开信封现沿用地图面板的受控字符串构造，并由测试精确断言完整 `panel_request`，复杂响应复用现役商城公共 JSON 出口。第二轮空面板的根因是后置玩家信息 UI 初始化执行 `_root.UI系统 = {}`，抹除了早期注册的 `NPC商店WebView`；该初始化现改为 `_root.UI系统 = _root.UI系统 || {}`，TestLoader 另按真实启动顺序断言早期服务引用不变。克隆槽无人值守进档还暴露出旧 `agentEnterResolvedSave()` 提前 `loadAll()` 后跳向不存在标签、停留主菜单的问题；2026-07-11/12 当时改为在 reveal 后只跳主时间轴 `读盘` 帧，统一复用原进档流程。

2026-07-12 正式进档后的世界 NPC 主入口已完成只读真机门：`冷兵器商人` 以 `source="world_npc_dialogue"` 打开，snapshot 回包约 71 KB 且 `success=true`，Web 实际渲染商品 **53** 项、背包 **42** 项、材料 **125** 项、情报 **25** 项与金币 **2,271,561**；背包/材料/商品 tooltip 均有成功回包，材料显示出售动作，情报 View 不显示出售动作。未进游戏前出现的“背包 0 项”属于玩家物品栏尚未完成场景初始化的前置状态，不再作为数据缺失判断。

同日以正式槽精确副本 `cf7_agent_npcshop_smoke` 完成经济写真机门，正式槽本身未写：Pig 背包买入 `机枪通用弹药` 1 个扣 **1,000**，再卖出 1 个回 **250**；`迷之盔甲君` 买入 `碳纤维布料` 1 个落 `destinationView=material` 并从 **410→411**，卖回 1 个获得 **12,500** 并恢复 **410**；买入未持有的 `翅虎堂徽章` 落 `destinationView=intelligence`，情报 **25→26** 且仍无出售按钮；样品栏以 `烈火吉他` 生成 opaque token，预览 **1 件/$14,500** 后单次批售，背包 **42→41**。最终金币 **2,245,811**；强制存盘后关闭 Launcher，按 2026-07-12 当时的进档链重新由 `agent_control` 选择同槽、在 reveal 后进入 `读盘` 帧，resolver 从 `sol` snapshot 回读，金币、背包 41 项、机枪弹药 **2,979**、情报 26 项、徽章存在、烈火吉他移除、材料 125 项/碳纤维布料 410 均与提交后状态一致。

### 0.0.19 2026-07-12 NPC 商店首轮可用性与原子结算重构

初版内联数量框、逐件买卖和样品栏被收敛为两层交互。主页面只负责浏览与选择：点击目录商品加入待购，点击背包或材料加入待售，情报保持只读；选中态与 tooltip 焦点分离。二级结算页集中展示两侧明细，以自定义 `−/+` 步进器调整数量、逐行移除，并实时显示 AS2 回传的购买总额、出售总额、预计余额和阻塞原因。NPC 商店范围内不再使用原生 number input；目录、owned grid 与结算列表的滚动条统一为面板皮肤，背包卡片移除底部内联操作区后不再横向溢出。

协议新增 `tradePreview` / `tradeCommit`。Web 只提交稳定 `catalogIndex`、数量和 lease-bound sale source；AS2 逐行重算物品、情报门槛、口才价格、出售收益、实际落点、余额与容量，预览只回公开明细和 opaque token，不序列化 inventory/ref。commit 单次消费 token，再次复核余额、引用、数量与价格后整单提交：所选售款可抵扣购买，售出腾出的格子可供同一笔购买使用；获取失败会回滚已暂扣的出售项，不留下半笔交易。Host 把 preview 作为读、commit 作为写，严格去重目录/来源身份并延续 `needs_reconcile` 门。该节原始实现时点曾保留旧 buy/sell/batch 兼容面；A3 current supersession 已把 ordinary `sell` 从 contract、Web allowlist、Host resolver 与 AS2 registration 完整退役，现役精确出售只走 `tradePreview→tradeCommit`。legacy `buy/batchPreview/batchSell` 仍不可从生产 UI 路由到达，新 Web 不调用，也不得据此恢复旧商店 renderer。

`data/shops/shops.json` 已拆为 `data/shops/npcs/*.json` 共 **35** 个 `npc-shop.v2` 文件、**833** 条商品。运行时仍归一化为 `_root.shops[shopId]`，不破坏 NPC 对目录对象的旧引用或 `catalogIndex`；展示分组进入 `_root.shopLayouts`。冷兵器商人、Andy Law、Boy、Pig、King、迷之盔甲君首批使用开发者人工分组，其余商店回退类型分类；`tools/validate-npc-shops.js` 与 build Step 1h3 强制文件身份、索引、字段白名单和人工分组完整覆盖。

自动证据：数据校验 **35 NPC / 833 商品**，Web harness **17/17**，Launcher xUnit **783/783**，Flash `NpcShopPanelServiceTest` **26/26 fresh trace**，库存回归 **78/78**；发布态 `asLoader.swf` 以 0 编译错误和刷新门更新为 **941719 bytes**，Launcher 全构建通过。

真机门使用正式槽精确副本 `cf7_agent_npcshop_trade_v2`，正式槽不写。Pig 人工分组渲染为霰弹武器/发射器/机枪/弹药，snapshot 为约 **69 KB** 并恢复背包 **42** 格。权威预览购买 `机枪通用弹药` 1 个：`buyTotal=1000`、`projectedBalance=2,270,561`、`canCommit=true`；opaque token 提交后旧 token 重放返回 `stale_state`，再次预览反推当前余额为 **2,270,561**。强制落盘后克隆 JSON 为金币 **2,270,561**、背包 **42** 格、机枪通用弹药 **2,980**（基线 2,979）；完全关闭并重启 Launcher 后，resolver 从 **SOL** 加载同槽，再次预览仍返回预计余额 **2,269,561**，证明提交状态回读成立。测试结束后克隆 JSON 与 1 个 SOL 均已删除；正式槽仍为金币 **2,271,561**、背包 **42** 格，mtime 保持 09:44:46。首轮自动进档还证明 reveal watchdog 可能早于物品数据完成；2026-07-12 当时的克隆验收门要求先等新鲜 `[BootstrapAS] event=handoff` 再调用 `agentEnterResolvedSave()`，否则会把未初始化物品字典误写成空背包。

> **历史入口边界**：上述 2026-07-11/12 经济写与冷启动回读事实早于 2026-07-21 attempt-bound Agent 门，只保留为当时的业务与持久化证据，不能证明当前入口。当前受控顺序必须是：调用 `start` 前记录 `/logs` 水位 → `start` → 在该水位后同时取得 fresh handoff 与真实 `[LaunchFlow] bootstrap_reveal_ready: Flash reveal cleared`（watchdog 不算）→ 仅调用一次 `_root.agentEnterResolvedSave()` → helper 同包发送 `s:1|ga:<attemptId>` → Host 从非 legacy 包实收并确认 `gameEnteredAttemptId == expectedAttemptId` → runtime ready。watchdog、`revealPerformed` 或 handoff 任一单独都不构成当前证据。

### 0.0.20 2026-07-12 NPC 商店批量交易 v2

第二轮以三个高频阻力为边界：重复装备清仓、背包满时继续采购、不可堆叠装备复数购买。结算页的装备待售行新增“同名全售”，但 Web 不上传物品名或匹配清单，只提交当前背包 seed lease、`scope=same_name` 与固定 `policy=plain_only`。AS2 扫描完整背包，把同名普通实例展开为内部出售项，并公开回显匹配格数、合格格数、保护格数；强化等级大于 1、有进阶或带插件的实例继续保护。commit 再次验证对象引用、数量、价格与普通装备条件，避免预览后原地改装绕过保护。旧样品栏 5 名限制不再约束新 Web 批售；legacy batch API 只保留为不可从生产 UI 路由到达的协议兼容面，不是 Flash fallback。

目录装备默认允许单笔 50 件，其他物品默认 100；`npc-shop.v2` 对象条目可用 `purchaseLimit=1..100` 收紧。AS2 不能把装备数量写成单个 `{name,value:N}`，因为 `BaseItem.create` 会把 `value` 解释成强化等级；因此交易计划把 N 件装备展开为 N 个 `{name,value:1}`，使 `ItemUtil.require/acquire` 的空格预检和实际实例数一致。preview 每行返回 `purchaseLimit/maxAffordable/maxByCapacity/maxPurchasable`，整单返回 `requiredSlots/availableSlots/missingSlots`，Web 提供 `−/+5/最大` 但不复制价格或容量算法。

容量阻塞时，结算页进入嵌入式背包—战备箱整理子路由。该路由不新增 NPC 商店库存写命令，而是复用 `domain=inventory` 的 `InventoryCoordinator`、剧情可访问容量、slot lease 与 `autoTransfer(mergeThenEmpty)`；点击任一已占用槽即权威转移到另一容器。返回时重新请求 NPC shop snapshot，采购意图按稳定 `catalogIndex` 保留，待售意图只在同槽同物或同名批售仍可找到安全 seed 时重绑，其他项明确移除，再重新 `tradePreview`。首轮不做“直接购买到战备箱”，避免改变物品获得落点语义。

自动证据：NPC shop Edge harness **25/25**，Launcher 全量 xUnit **788/788**，Flash `NpcShopPanelServiceTest` **31/31 fresh trace**，库存领域回归仍为 **78/78**；数据校验保持 **35 NPC / 833 商品**。专项覆盖同名批售保护、token 提交、复数装备独立实例、金钱/容量最大值和整理返回重算。发布态 asLoader 为 **0 错误 / 0 警告**，SWF 刷新为 **942,968 bytes**；Launcher 七阶段全构建与 125 项 Web runtime 资产门通过。

### 0.0.21 2026-07-12 NPC 商店帮助与渐进引导

NPC 商店标题栏新增常驻“？”入口，打开不清空待购/待售意图的二级帮助页。帮助页覆盖选择与交易的区别、结算数量调整、同名全售保护规则、背包—战备箱整理四条关键流程；从选购页或结算页进入都保留原页面状态，返回时恢复进入前页面，帮助路由自身不发送 AS2/Host 请求。结算页同时按普通订单、同名批售和容量阻塞更新就地说明，避免玩家只看按钮猜测规则。

首次进入结算、首次启用同名全售、首次遇到背包空间不足时各显示一条非阻塞提示；提示状态仅以 Web `localStorage` 的 `cf7.npcshop.guide.v1.*` 键记录，不进入存档、不参与权威交易状态，也不改变断线对账。标题栏入口始终可重新查看完整说明；交易写入或库存整理写入期间入口禁用，Esc/面板关闭键先退帮助页。Edge harness 扩展为 **34/34**，覆盖四张帮助卡、两层入口、意图保留、零权威请求及三类首次提示。

### 0.0.22 2026-07-12 NPC 商店自动分类树与人工分组回撤

首轮为六个大型商店编写的人工 sections 以及其余商店的单分组占位全部撤回；35 个生产 `npc-shop.v2` 文件现为 **0 个 manual shop**，833 个稳定 `catalogIndex` 未重排。人工分组能力继续保留为完整覆盖自动树的高级机制，配置入口、覆盖语义和校验命令集中到 `data/shops/README.md`，只建议用于科技武器、剧情陈列等无法从物品属性推导的经营语义。

默认目录改为互斥的逐层分类树：一级读取 `type=武器/防具/消耗品/收集品`，二级读取 `use`，武器三级按刀的 `actiontype` 或手枪/长枪的 `weapontype`。AS2 `snapshot.catalog[]` 新增 `actionType/weaponType` 展示投影，均来自现有物品数据；不新增 XML 字段，不读取源文件名，也不参与价格、购买落点或交易复核。Web 只渲染本商店存在的节点，单子节点自动下钻，固定语义顺序优先、未知值进入“其他”。这同时消除了 `type=消耗品,use=材料` 在旧扁平“消耗品/材料”标签中重复出现的问题。

防具套装不进入运行时推断。现有 651 件防具具备稳定装备位，但没有可靠 set ID，且存在少于五件、同槽多变体等情况；新增 `tools/audit-equipment-set-candidates.js` 仅按同商店名称词干与跨装备位生成 `runtimeAuthoritative:false` 候选。当前数据得到 19 个待复核候选，任何结果都不会写回商店或创建购买意图，后续须以人工审核的可变件数/同槽选项清单另行施工。

自动证据：数据 **35 NPC / 833 商品 / manual-shops=0 / 大类 fallback=0 / 武器子类“其他”=4**；NPC shop Edge harness **40/40**，覆盖三级路径、单节点下钻、消耗品材料与收集品材料互斥、人工完整覆盖回退及既有交易/帮助/整理闭环。Flash `NpcShopPanelServiceTest` **32/32 fresh trace**，asLoader 发布为 **0 错误 / 0 警告**，`scripts/asLoader.swf` 刷新为 **943,014 bytes**；Launcher xUnit **788/788**，七阶段构建与 125 项 Web runtime 资产门通过。

### 0.0.23 2026-07-12 NPC 商店审阅收口

交易计划新增展开后槽位身份去重：`same_name/plain_only` 批售与逐格待售若覆盖同一真实背包槽，preview 直接返回 `duplicate_line`，不再重复累计售价或可用空格。多件出售装备返还的同名插件、以及同笔购买的材料/情报，会先按名称聚合数量再交给 `ItemUtil.acquire`，避免底层 collection map 覆盖同名条目。购买上界在“价格与所需槽位随数量单调不减”的现有契约下改为二分搜索，单行最多从 100 次权威容量重算收敛到约 7 次；默认上限 100 与装备上限 50 的结果由 TestLoader 固定回归。

Host 对成功写回包增加权威 state 结构校验：缺 `v/shopId/balance/catalog/layout/views`，或 `tradeCommit` 缺 `operation/trade`，统一改写为 `malformed_response + requiresReconcile` 并保持 `needs_reconcile`；更早发出的 snapshot 即使随后成功，也不能解除仍在途的写门。Web 收到该标志后只请求 snapshot 对账，不重放 commit。地图与 NPC 商店入口同时迁入共享 `PanelRequestEnvelope`，统一转义 JSON 字符串。legacy `buy` 的装备单次 1 件约束保留并明确与新协议 `purchaseLimit` 分离。

自动证据刷新为：数据校验 **35 NPC / 833 商品**，NPC shop Edge harness **41/41**，Launcher xUnit **791/791**；Flash `NpcShopPanelServiceTest` **37/37 fresh trace**，新增覆盖信封特殊字符、批售/逐格重叠拒绝、同名插件无损返还与购买上界二分等价性。Launcher 七阶段全构建与 125 项 Web runtime 资产门通过；asLoader 发布为 **0 错误 / 0 警告**，`scripts/asLoader.swf` 刷新为 **947,527 bytes**，FFDec `ExportAssets` 确认 `PanelRequestEnvelope` 已嵌入。

### 0.0.24 2026-07-12 KShop 二级结算与直接交付

复核确认“购物车 → 扣 K 点 → 商城已购买物品 → 逐项 claim”不是外部发货或防作弊要求，而是旧商城把付款与背包容量隔离的历史设计。新 KShop 改用 NPC 商店已经验证的上位交易模型：主页面维护购物车意图；二级页调用只读 `checkoutPreview`，由 AS2 重解目录、等级、价格、K 点与 `ItemUtil.require` 容量，返回逐行购买上界、整单阻塞原因和单次 `checkoutToken`；`checkoutCommit` 消费 token 后再次复核，并以 `ItemUtil.acquire` 先完成整单交付，再扣 K 点、清购物车与强制存盘。容量或余额不足零写，等额 K 点可正常结算。commit timeout、断连或畸形成功回包仍只允许 `bulkQuery + inventory snapshot` 对账，不重放。

`_root.商城已购买物品` 不删除、不迁空：旧存档已有记录继续由当前 Web 的窄兼容 View 以 `purchasedToken + purchasedIdx` 单飞 claim，自然清空；旧全屏 Flash/Host UI 入口均不可路由，`shopCheckout/shopClaim` 只承担旧存档与协议兼容，不能作为恢复旧界面的理由。新 Web 只把该 View 标作“历史待领取”，新购买永不追加。这样既不丢旧存档资产，也不让历史队列继续成为常规购买步骤。

Web 结构同步拆出 `kshop-views.js`，承载 Catalog/Cart/历史待领取 View 与二级结算 DOM；`kshop.js` 保留领域协调、tooltip、库存态和协议调用，`kshop-runtime.js` 继续独占 save/commit/claim 写流水线。自动证据：Edge harness **61/61**，覆盖直接交付不增长历史队列、满包零扣款、preview/commit 顺序、ESC 返回与畸形 commit 对账；Launcher xUnit **798/798**；七阶段 Launcher 构建与 **125 项** Web runtime 资产门通过。asLoader 发布得到 `0 个错误, 0 个警告`，`scripts/asLoader.swf` 从 947,527 刷新为 **949,074 bytes**，FFDec 导出确认 `shopCheckoutPreview/shopCheckoutCommit/kcheckout` 已进入帧脚本；TestLoader fresh trace 通过 `InventoryPanelServiceTest` **78/78**、`NpcShopPanelServiceTest` **37/37** 与新增 `KShopCheckoutServiceTest` **10/10**，后者锁定直接交付、材料分流、等额余额、满包零写、目录 stale、token 防重放及畸形预览失效。KShop 新 AS2 交易在真游戏仍须按 testing guide 覆盖实际落点、断线对账和重启回读。

### 0.0.25 2026-07-13 物品格五层统一与权威树筛选

物品格公共基建按“统一卡片/格子 → 统一布局密度 → 统一分类模型与导航 → 权威库存筛选协议 → 统一 owned 库存视图壳”完成收敛。`ItemCard` 统一语义骨架并把状态标记收进独立 overlay 层，紧凑态由 `GridDensityController` 对注册的 `ItemGrid/GridRenderer` 统一驱动，不再让商城、NPC 商店、仓库各自循环改 class。紧凑瓦片以图标为主要点击面，选择语义由不遮字的边框/角标承担；外圈只保留键盘焦点、选中边界和相邻目标分隔所需的最小空间。

新增共享 `ItemFilter` 模块，稳定大类 ID 为 `weapon/armor/consumable/material/collection/other`，统一目录路径、固定顺序、单子节点下钻和键盘导航。NPC 商品目录与 KShop 目录都改由同一模型生成；owned 库存通过 `InventoryFilterControl` 消费 AS2 权威 facets。`InventorySortControls` 保留组合 facade，内部只含 `InventoryFilterControl` 与 `AuthoritySortControl`；`DisplaySortControl` 已在 §0.0.28 删除。`OwnedInventoryViewShell` 统一工具栏、快照、空态、分页与 ItemGrid 组合，领域租约、移动、丢弃、购买和整理仍由各 Coordinator/宿主持有。

协议保持向后兼容：旧 `filterKey` 继续可用，新请求可带 `{major,use?,subtype?}` 的 `filterSpec`。Host 与 AS2 双层校验二者映射一致（`collection → other`，其余同名），不接受矛盾筛选。AS2 从完整可访问容器计算 `filterFacets/filterItemCount`；缓存以 `containerEpoch + accessibleCapacity + inventoryRef + mutationRevision` 为键，命中校验为 O(1)，不再逐槽核对 50/400/1200 槽可访问前缀。inventory-domain 写入仍主动清缓存，外部拾取/购买等通过 `ArrayInventory` 成功写入口推进单调版本。分类按权威物品原始 `type/use/actiontype/weapontype` 匹配；Web 不再根据当前页伪造全容器计数。旧 AS2 不返回 facets 时自动退回扁平筛选 select；Host 只允许稳定字段、枚举与长度，并原样镜像通过验证的结构，不接受未知键或非法父子组合。

组合差异以语义槽位契约收敛：卡片声明 `item-card-icon/body/auxiliary/selection-marker`，密度规则只依赖这些公共角色，不再逐个识别 NPC 背包、商城背包或独立工作台皮肤。紧凑态统一隐藏 body 与未选中辅助文字；选中态只恢复 icon-only 角标。架构审计锁定三种组合必须经过 `OwnedInventoryViewShell` 和语义槽位，浏览器 conformance 断言真实 computed style，避免每个宿主重复一整套布局测试。

自动证据：共享分类模型 **11/11**、KShop Edge harness **70/70**、NPC shop harness **49/49**、Launcher xUnit **803/803**；TestLoader fresh trace 中 `InventoryPanelServiceTest` **80/80**，编译器 **0 个错误 / 0 个警告**；asLoader publish 刷新 `scripts/asLoader.swf` 为 **949,896 bytes**；Launcher 七阶段构建与 **125 项** Web runtime 资产门通过。真实存档仍按 testing guide 复核树筛选、紧凑密度、跨页计数、断线回退和重启回读。

### 0.0.26 2026-07-13 组合 conformance、商店双树与二级页覆盖

公共筛选导航删除 `npcshop-category-row` 皮肤泄漏，只输出 `item-filter-row` 语义角色，NPC 目录样式由父容器作用域提供。修复单子节点自动下钻的遍历错误：过去遇到“武器下只有长枪、长枪下有多个枪械 subtype”的生产目录时，渲染器跳过冗余 use 行后直接终止，导致孙级全部消失；现在跳过该行但继续渲染 subtype。Pig 等单 use 商店由专项 DOM 断言覆盖。

NPC 商店背包不再只显示待售提示：主页面常驻打开 inventory-domain Coordinator，以 `InventoryFilterControl` 消费完整背包的权威 `filterFacets/filterItemCount`，树选择通过 `setFilterSpec('背包', ...)` 请求权威窗口并保留 physicalSlot/lease；材料与情报仍沿 NPC 商店领域投影。整理页复用同一已打开 Coordinator，不再反复关闭并丢失主页面筛选状态。

帮助、结算等二级页增加 `workbench-secondary-page` 角色，统一从 `--workbench-header-height:48px` 起覆盖到工作台底部；打开帮助时底层 `.workbench-body` 同时设为不可见，消除旧 64px 定位遗留的顶部漏层。新增 `tools/visual/item-grid-matrix.html` 与 `tools/run-item-grid-visual-matrix.js`，以 **7 个正交视觉样本 / 8 项 computed-style conformance** 覆盖 KShop/NPC 目录、owned 格的完整/紧凑与普通/选中/锁定/禁用/只读/空槽，以及二级页完整覆盖。

本轮自动证据：生产商店数据 **35 NPC / 833 商品** 校验通过，共享分类模型 **12/12**，NPC shop harness **53/53**，KShop Edge harness **70/70**，视觉矩阵 **8/8**，无 page error / failed request；C#/AS2 协议未变，沿用上一节 Launcher **803/803** 与 Flash inventory **80/80** 基线。

### 0.0.27 2026-07-13 自适应层级导航与单滚动所有权

窄栏出现的白色横纵滚动条不是单纯皮肤缺失，而是“每级分类行横向滚动 + 外层完整树纵向滚动”的双滚动所有权冲突；完整树的祖先层又持续参与文档流，使目录/背包网格高度随路径深度变化。按业界 adaptive disclosure 模式收敛：宽目录使用 `presentation=drilldown`，始终只渲染当前一层，并提供精确返回父级的按钮；窄背包使用固定 24px 自绘 trigger，层级导航在绝对定位 popover 中展开，不改变 chrome 或网格高度。popover 内只保留一个纵向滚动所有者，当前行允许换行但禁止嵌套横向滚动；权威 `filterSpec`、计数和路径模型不变。

`FilterNavigator` 继续负责路径与单子节点下钻，但新增 drilldown renderer；返回按钮走 exact path，避免返回单子节点父级时被自动下钻立即弹回。`InventoryFilterControl` 新增 popover presentation、焦点离开/Escape 收起、选中叶节点收起与路径摘要 trigger；legacy 无 facets 时仍回退原筛选入口。NPC/KShop 目录统一使用单层下钻，NPC 背包强制使用 popover，其他宽库存工具栏默认继续内联 drilldown。

视觉矩阵扩为 **8 个样本 / 10 项 conformance**，新增宽区单层下钻与窄区自绘 popover 样本，断言 native select 隐藏、触发器高度固定、popup 不造成 reflow、无横向溢出。自动证据：NPC shop harness **54/54**、KShop **70/70**、分类模型 **12/12**、视觉矩阵 **10/10**，无 page error / failed request。

### 0.0.28 2026-07-13 facet 缓存闭环与展示排序退役

提交审阅发现权威 facet 缓存只以 `containerEpoch + accessibleCapacity` 判断命中，而普通 move/merge/discard/NPC 出售只失效 slot lease，不增加整容器 epoch；拾取、购买等外部 `ArrayInventory.add/remove` 更不会进入 inventory-domain 失效函数。结果是首次 snapshot 后分类计数可能长期停留在旧状态。现在 inventory-domain 单槽/整容器失效同步删除对应 `_facetCache`，缓存命中前再逐槽比较可访问前缀的对象引用；这样保留分类元数据缓存收益，同时覆盖外部容器写。TestLoader 新增“先建两端缓存，再跨容器移动”的回归，锁定来源 facet 消失、目标 facet 新增。

同轮补齐结构化筛选双层一致性：`filterSpec.major=collection` 只允许搭配 legacy `filterKey=other`，其余 major 必须与 filterKey 同名；Host 与 AS2 均拒绝矛盾组合，避免 Web 显示武器路径但 AS2 因 `filterKey=all` 返回全部槽位。Launcher xUnit 新增三组负例与 collection 正例。

交互层按用户反馈收敛：owned 库存删除纯 Web `DisplaySortControl/displaySortSlots`，树筛选成为唯一“看什么”的浏览入口；无筛选时严格保持 snapshot 的 `physicalSlot` 顺序。会真实改写存档的八策略整理仍以独立 select + 明确确认按钮保留。背包无分页工具栏压为单行“树筛选 + 整理方式 + 整理”，仓库/战备箱因分页保留两行自适应布局。KShop 架构门新增 `authorityTreeReplacesDisplaySort`，同时拒绝展示排序符号回流。

自动证据：KShop Edge harness **70/70**、NPC shop harness **54/54**、视觉矩阵 **10/10**、分类模型 **12/12**、Launcher xUnit **807/807**；TestLoader fresh Output Panel 为 `InventoryPanelServiceTest` **83/83**、`NpcShopPanelServiceTest` **38/38**、`KShopCheckoutServiceTest` **10/10**，编译器 **0 个错误 / 0 个警告**。asLoader publish 刷新 `scripts/asLoader.swf` 为 **952,766 bytes**，Launcher 七阶段构建与 **125 项** Web runtime 资产门通过。

### 0.0.29 2026-07-13 筛选交互统一与 KShop 稳定轨道

NPC 商店背包撤下“查看”后不再保留专属 trigger/popover：背包与商品目录统一使用共享 `FilterNavigator` 的行内单层 drilldown，每次只替换当前层，并以精确父路径返回。背包操作提示移到标题同行并单行省略，筛选工具栏不再承担说明文字或“分类”标签，整行宽度都交给 facets。权威 `filterSpec/filterFacets`、完整容器计数和 legacy 无 facets 回退均不变；这次只收敛展示与交互选择，不改协议或 AS2 权威。

KShop 删除 `catalogPath(...,{weaponSubtype:false})` 特例，武器目录恢复完整 `majorType → use → actionType/weaponType` 路径，因此选择“刀”会继续列出刀剑、长刀等实际存在的子型。分类轨道固定为 48px 设计画布高度，超出时由轨道自身纵向滚动；分类按钮取消 `transition:all`，切层不再改变下方网格顶边，也不再用过渡动效抢占注意力。

自动证据：分类模型 **12/12**、KShop Edge harness **71/71**（新增“刀→子型→叶节点”及筛选轨道/网格顶边稳定断言）、NPC shop harness **55/55**（目录/背包同为行内单层下钻，标题提示与筛选整行宽断言）、视觉矩阵 **10/10**，均无 page error / failed request。C#/AS2 协议与实现未改，继续沿用 §0.0.28 的 Launcher xUnit、fresh Output Panel 与 asLoader 发布基线。

### 0.0.30 2026-07-13 类别 / 专柜双维目录

KShop 数据核对确认 13 份 `data/kshop/*.json` 的 entry `type` 是策划人工专柜名而非物品大类，共覆盖 227 件商品；此前 Web 只按物品 `majorType` 建树，导致这层内容语义丢失。目录现在以“类别 / 专柜”两个一级入口承载替代维度：类别继续下钻 `majorType → use → actionType/weaponType`，专柜下钻 JSON `type`。没有把 13 个长专柜名与五个自动大类平铺在根行，避免重新制造选择压力；固定筛选轨道与内部纵向滚动规则保持不变。

NPC 商店已有 `layout.sections` 投影，但当前生产数据 `manual-shops=0`，因此此前只在 fixture 使用；组合逻辑改为有 sections 时同样显示“类别 / 专柜”双入口，人工分组不再完整覆盖自动树，未配置 sections 的生产商店仍只显示自动类别。共享 `ItemFilter.branchTree` 只组合展示树，不改变任何价格、购买落点或权威提交规则。

同轮补齐生产 KShop `shopBulkQuery.catalog[]` 的 `actionType/weaponType` 投影；策划专柜继续来自 entry `type`，自动子型来自物品 `actiontype/weapontype`，两者在 wire 上独立。TestLoader fresh Output Panel：`InventoryPanelServiceTest` **83/83**、`NpcShopPanelServiceTest` **38/38**、`KShopCheckoutServiceTest` **11/11**，编译器完成；asLoader 刷新为 **952,791 bytes**。Web 自动证据：分类模型 **16/16**、KShop **71/71**、NPC shop **56/56**、视觉矩阵 **10/10**，均无 page error / failed request。

### 0.0.31 2026-07-13 facet 缓存单调版本号

逐槽引用核对虽然补上了外部写入后的正确性，但每次 snapshot 缓存命中仍要扫描完整可访问前缀，1200 槽仓库还要保留同规模 `itemRefs` 数组。现在 `ArrayInventory` 增加容器级 `mutationRevision`：`add/remove/addValue/setItems/transactionWrite/transactionReplaceAll/transactionReplacePrefix` 每次成功写入后单调推进，失败写入保持不变；带生命周期事件的入口在同步事件可见前先推进版本，避免监听器内 snapshot 短暂命中旧缓存。facet 缓存同时记录真实 `inventoryRef`，防止整体替换容器后误命中。命中判断收敛为 `inventoryRef + mutationRevision + accessibleCapacity + containerEpoch` 的 O(1) 比较，inventory-domain 主动失效仍保留为快速清理。

TestLoader 新增全部写入口、同步事件可见性与失败写入的版本推进回归，并沿用“外部 remove 后重新 snapshot”验证 facet 自动刷新。fresh Output Panel 为 `InventoryPanelServiceTest` **84/84**、`NpcShopPanelServiceTest` **38/38**、`KShopCheckoutServiceTest` **11/11**，编译器 **0 个错误 / 0 个警告**；publish-only 刷新 `scripts/asLoader.swf` 为 **953,476 bytes**。性能矩阵继续覆盖 50/400/1200 容量与 0/50/100% 占用；本轮主要收益是缓存命中复杂度和引用数组常驻空间，不把 50 格投影与 JSON 序列化耗时误算成版本号收益。

### 0.0.32 2026-07-13 稳定 OCC lease 与 NPC 背包单一权威

金币商店结算曾出现确定性 `stale_state`：Web 同时请求 inventory 背包与 NPC snapshot，而 NPC `buildState` 又在内部请求第二份背包；旧 `executeSnapshot` 每次纯读都会 `beginSession()` 清空全部槽位 lease，所以两个成功读会互相作废，混合买卖预览拿到的背包 token 已过期，Web 对账又关闭结算页，外观即“点击结算立刻跳回”。修复把 inventory slot lease 绑定到 `ArrayInventory` 实例、`mutationRevision`、槽位对象引用与确认指纹；相同版本的重复读复用 token，真实写入、容器替换或数量/名称/强化/进阶/插件/更新时间变化才拒绝旧 token。snapshot 额外回 `containerVersion` 供诊断。

NPC `buildState` 删除嵌套 bag snapshot，Host 成功载荷只要求 material/intelligence，Web 始终从现有 `InventoryCoordinator` 读取背包；整理返回结算时以 inventory bag + NPC material 合并重绑意图。collection lease 同步收敛为按 shop/view/key/count 稳定复用并清理消失键，避免复制“读使读失效”的反模式。一次性 `tradeToken/checkoutToken/batchToken` 仍属交易计划，重同步或提交尝试后继续消费，未被资源 lease 稳定化。自动证据：NPC harness **58/58**、Launcher xUnit **808/808**、fresh Output Panel `InventoryPanelServiceTest` **88/88**、`NpcShopPanelServiceTest` **40/40**、`KShopCheckoutServiceTest` **11/11**，编译器 **0 个错误 / 0 个警告**；publish-only 刷新 `scripts/asLoader.swf` 为 **958,774 bytes**。

### 0.0.33 2026-07-13 合成目录状态与共享筛选视觉契约

`FilterNavigator` 新增 `visualStyle=catalog`，KShop、NPC 商店与合成目录统一消费 `.item-filter-catalog .item-filter-option` 的暗色按钮几何、交互态和计数样式；宿主只通过 `--item-filter-*` / `--wb-accent` 覆盖色彩。旧 `kshop-cat-btn` 专属类已从渲染和 CSS 删除，避免合成目录落回浏览器默认白色按钮。合成工作台生产 `#panel-content` 与商城/独立工作台一致使用 `inset:0` 全 anchor，内部 1024×576 scale-shell 等比铺满；双栏调整为同向约 60:40，左侧为高频目录浏览，右侧保持单配方权威详情。

合成 snapshot 为每条配方增加严格配对的 `canCraftOne/availability`。Flash 每条配方只运行一次 `craftCount=1` 计划，复用 preview 的材料、等级、双货币和保守容量语义，但明确跳过批量最大值二分；Web 据最近 snapshot 显示卡片可合成/阻塞标记、总计和“只看可合成”本地开关，切换开关不得额外请求 snapshot。该状态是浏览引导而非写授权，提交仍必须取得新的 preview/token；材料或余额写入后的 snapshot 会刷新目录状态。自动回归固定验证“配方数次单份计划、零最大份数探测”、材料/余额变化刷新、提交后卡片状态刷新、本地筛选无额外网络读，以及共享视觉契约和实际栏宽比。

本轮自动证据：合成 Edge harness **27/27**（含生产 DOM 的全 anchor 与 scale-shell 覆盖率）、共享分类模型 **16/16**、KShop **72/72**、NPC shop **58/58**、视觉矩阵 **10/10**、Launcher xUnit **819/819**；TestLoader fresh Output Panel 为 `CraftingPanelServiceTest` **25/25**、`InventoryPanelServiceTest` **88/88**、`NpcShopPanelServiceTest` **40/40**、`KShopCheckoutServiceTest` **11/11**。已刷新 `scripts/asLoader.swf` 为 **964,296 bytes**，Launcher 七阶段构建与 **127 项** Web runtime 资产门通过。

### 0.0.34 2026-07-15 Skill 主线独立拆分裁决

代码级复核确认，技能查看、教师学习和 12 槽装备只与物品工作台共享“格子/详情/双栏/拖放”视觉形态，不共享 ItemCollection、physicalSlot、slot lease 或 inventory move/merge/swap 权威。`快捷技能栏N` 的真实状态是 `_root.主角技能表`、12 个 root 槽字段、技能点和被动副作用；强塞进 inventory domain 会制造不存在的 owned 容器，并让通用 Coordinator 认识教师会话和被动技能。

因此原 Step 6 的 `SkillView` 规划被 [Skill 独立主线](技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md) 取代：建立 `panel=skills`、`domain=skills`、`SkillTask`、`SkillPanelService`、`SkillLoadoutService`，以 `view=manage|trainer` 覆盖查看、学习/升级和装备。`manage` 主入口归刘海屏独立 `SKILLS`，教师仍由 NPC 上下文进入；旧物品栏技能页不迁成 Web 转发器。本文的 `DualPaneShell` / PanelScale / 卡片/模态 primitive 可复用，但 `InventoryCoordinator`、inventory lease 和本节 equip payload 不进入技能协议。原 §4.1.1 / §4.3 / §4.4 的技能内容保留为历史事实和副作用清单，施工契约以 Skill 主线为准。

拆线同时解除 NativeHud 的排期误耦合：快捷技能输入先在 AS2 从 HUD MovieClip 描述符改读 `SkillLoadoutService`，再由 Skill S3 接管 equip/unequip；不再等待物品装备栏 Step 5b-2 或物品 Step 6。药剂装配的容器、使用与冷却 authority 仍属物品/药剂域，但其管理入口并入 5b-2B 角色构筑，不再另开独立面板收口。

### 0.0.35 2026-07-15 Skill 展示 primitive 复用闭环

Skill 四项体验优化验证了“共享展示 primitive、隔离领域 authority”的拆线。`GridDensityController` 现允许非 ItemGrid 宿主提供自有 `compactClass`，Skill 复用同一偏好/切换机制但只挂 `skills-density-compact`；`PointerDragController` 增加数据源无关的 rejected target 视觉态，Skill 用自有 Offer/Accept 把拖放汇入原子 `equip` 确认；Skill 复用 `FilterNavigator/matchesPath` 的按钮、计数、键盘和本地匹配 primitive，但不再使用物品 `branchTree` 的中间目录语义，未引入物品大类、AS2 facets、inventory lease 或写命令。共享层仍不认识 `skillKey/trainerSession/equip`。

Skill 的具体布局冻结为：manage 上方使用全宽技能库，下方使用 1—12 连续单排技能带；完整态为带名称/等级/类型的自适应多列卡，紧凑态借用物品格的方形图标瓦片语言，但保持 Skill 自有 DOM/class/状态角标，不复用物品 taxonomy、facets、lease 或 authority。常驻详情栏已移除，技能卡与已占用快捷槽统一复用 `PanelTooltip.convertAS2Html` 安全注释链。manage 使用“形态 / 配置 / 流派”，trainer 使用“形态 / 学习 / 流派”；三组 direct facet 永久并列，每个选项首击即过滤、跨组可组合且可一键清除，manage 将形态/配置并排并让流派独占第二行；流派只投影真实 `Type` 中存在的武术、剑术、枪术、内功、神功、科技、超能力、投技、龙吼。名称搜索默认收起并按需与 facet 本地合取。Skill 的 player-facing chrome 进一步限定为 manage 零 metric、trainer 仅等级/技能点，稳定同步/revision/常驻刷新/协议术语不进入常态界面；异常态才提供友好提示和不含 trainer session/learnToken 的诊断复制。Skill 视觉隐藏通用 L/R slot marker，但保留 Shell slot/ARIA/焦点语义；情境 `?` 模态分别说明 manage 与 trainer 操作，零传输、关闭恢复焦点。教师页与教师来源 manage 的双向切换属于 Host/Skill 生命周期：session 只存 Host，NativeHud manage 无返回权限，不下沉 `DualPaneShell`。公共回归为 ItemFilter `22/22`、视觉矩阵 `10/10`、KShop `73/73`；Skill 自身三视口 `83/83`，证明共享钩子没有改变既有物品格完整/紧凑、拖拽和筛选契约。

### 0.0.36 2026-07-19（2026-07-26 收敛）角色构筑工作台与批次刷新裁决

装备槽迁移、四槽药剂装配与旧 `新版物品栏界面` 的「个人信息」详情不再拆成互不知情的 Web 页面，而按同一逻辑 `CharacterBuildSession` 联合规划：编辑态左侧常驻现役 Web 纸娃娃与装备/药剂槽，右侧在筛选后的可装备候选和已装备物品调制之间切换；详细属性使用覆盖整个 workbench body 的全宽 SecondaryPage，与编辑态互斥。完整背包不在角色构筑首屏复刻；背包/装备/药剂容器仍是 AS2 authority，Web 只消费候选与只读投影。旧 Hub 的「情报」保持独立 intelligence 领域；本裁决所称「个人信息」也不等于战斗中常驻的 `flashswf/UI/玩家信息界面`，后者继续走 C# NativeHud 路线。

短期明确采用两个互斥状态。`mode=edit` 允许连续换装、卸装、药剂装配和调制，Web 纸娃娃只按明确 click/键盘选择即时重绘；hover 首版只显示 tooltip，完整人物数值不跟随每次候选变化。只有显式确认的装备穿脱/已穿戴调制提交推进 AS2 持有的 `loadoutRevision` 并置 `liveRefreshDirty`；药剂装配只推进 `drugRevision` 与全局存档 dirty，零活体刷新。切入全宽 `mode=stats` 是结算屏障：先等待写入/对账收口，仅当 `loadoutRevision != liveRevision` 时执行一次活体刷新，再从 `PlayerInfoProvider` 现有 getters 抽取结构化冻结快照；数值态禁止换装。关闭 Panel 走两阶段屏障：`liveRefreshDirty` 先完成人物刷新，全局存档 dirty 再完成 `flushNow`，两者成功后才 `webPanelUnpause`；已在数值态结算过或没有装备变更时必须零人物刷新。切页不隐式确认 selection 预览；两个 dirty latch 都不能只存在于 Web/Host，断线/异常关闭必须由 AS2 生命周期 fallback 继续完成 finalize。

装备页因此可以成为玩法与刷新策略上的特例宿主，但不能形成第二套业务权威。`CharacterBuildSession` 只是跨层逻辑会话，AS2 `CharacterBuildService` 负责穿脱/药剂装配与 dirty authority，`EquipmentTuningService` 继续独占调制写，角色属性读取/投影进入独立 provider/kernel，Web `DressupDollRenderer` 只负责视觉。已穿戴物品直调在后续切片通过适配装备容器进入同一调制服务；首轮只放行单件原地操作，跨背包/装备容器的强化度转换继续后置。长期共享计算的边界与启动门槛见 [TS 到 AS2 编译基建可行性备忘](TS到AS2编译基建可行性备忘-2026-07-18.md)，该路线是未来优化项，不阻塞短期批次刷新版本。

2026-07-27 历史施工状态：B1/B2A 后续已推进到完整 B2B 四原子写、B3 已穿戴调制和 B4 入口/关闭协议。fresh CS6 focused 六套件为 **529/529**；最终 Launcher/Web 数字、候选身份与证据哈希只在专题 §9 单源维护。两条 visual-retire High 已由 PanelHost exact 原语收口，不再依赖可能丢失的 `PanelClosed`，并覆盖 tracked reservation/lease、replacement 与乱序事件。最终 isolated candidate 又完成真实装备/药剂往返、已穿戴强化、finalize、安全退出和同一 SOL 重启读回，严格状态为 `e2e_verified / NOT_DEPLOYED`。当时工作树仍有显式旧 Hub fallback；该历史分支现已由 §0.0.39 删除，不能作为当前兼容合同。

### 0.0.37 2026-07-21 资源箱同面板整理与 inventory capability 裁决

资源箱因背包容量阻塞时，不再采用“suspend → 返回 AS2 → 刘海战备箱 → 回到原箱”的中转。Web 在**同一个 tracked `panel=loot`、同一个 `panelInstanceId`** 内切到嵌入式背包—战备箱整理子页；期间不发 `lootClose`、不打开第二个 `workbench` panel，原 loot coordinator、全局 pause 和 AS2 `LOOT_ACTIVE` 保持。整理写入与未知结果对账全部收口后，必须取得 fresh loot ACTIVE snapshot 才能返回战利品页；刷新失败就留在整理页，禁止重放写入。

这不是把资源箱纳入通用 owned transfer。Host 只在当前 active loot instance 上接受七键 `{type,panel,domain,cmd,callId,panelInstanceId,payload}`，且 `panel=loot`、`domain=inventory`、`panelInstanceId` 必须 exact；专用白名单只有 `snapshot/autoTransfer/discard`。`autoTransfer` 只允许**背包↔战备箱**且策略固定 `mergeThenEmpty`，`discard` 只允许背包整槽，瞬态 loot container 永远不是 inventory source/target。`InventoryTask` 为该请求保留 panel/instance binding，成功、本地校验错误、timeout 和发送失败都回显 binding，Web mux 再做 exact 校验；AS2 不能凭消息内容伪造 Host 持有的 active panel capability。

### 0.0.38 2026-07-22 商店审阅收尾与长注释交互契约

KShop 的动态目录上限必须来自**写后状态**：成功 `checkoutCommit` / `shopClaim` 回包都携刷新后的 `catalog`，Host 与 Web 把该字段列入确定性成功结构，缺失时进入只对账、不重放的 `reconcile_required`。Web 采用新目录后重新清理当前购物车；首次 `bulkQuery` 自动移除失效行或夹紧超限数量时，还必须把清理结果经 `saveCart` 写回权威恢复影子。动态上限降为 0 时使用 `sold_out` 与“已达持有上限”文案，不再产生“当前可购买上限 0”的机械提示。NPC 商店的容量分类同时按函数自身契约防御重复情报行：先按名称聚合总量，再和实时剩余容量比较，确保超限归为 `destination_full`；即使正常 Web wire 已按 `catalogIndex` 去重，伪造或未来内部调用也不会退化成 `inventory_full`。

长 tooltip 采用非模态 hovercard 的通行交互：触发物与浮层组成复合 hover 区，离开触发物后保留 140ms 过桥时间；进入浮层可继续阅读并用滚轮滚动，离开整个复合区域才隐藏。键盘焦点仍留在触发物，`PageUp/PageDown`（以及 `Alt+↑/↓`）滚动说明，`Esc` 明确关闭；浮层保持 `role=tooltip`，因此当前不放按钮、链接等需要焦点的控件，未来出现交互内容时必须升级为 popover/dialog。确定性关闭、面板切换和重渲染仍立即 `hide()`，只有真实 hover leave 使用 `hideHover()`。

本轮新增/复跑自动证据：跨层契约 validator 通过（3 domains / 19 commands / 3 numeric fields / 3 vectors / 7 source checks），契约变异 **20/20**，KShop Edge harness **88/88**，NPC shop **84/84**，NPC secondary pages **6/6**，Crafting **76/76**（三视口），Launcher xUnit **1262/1262**；workbench audit **0 errors / 0 warnings**、atlas **48/48**、item-grid **14/14**。本次未改 AS2，故未重复编译；此前同一 AS2 字节的 fresh trace 仍为 `KShopCheckoutServiceTest 18/18`、`NpcShopPanelServiceTest 44/44`，失败均为 0，Compiler Errors **0/0**。source `77d7630b6a17770b59f3675a756b59f95e204e1f` 经 request `D6D9BA75DDE90615C2953636F7E60117FD7025118C6EAC8B26F3098F4A9B6B87` 的本地 X509 + GitHub OIDC 双故障域共识完成 promotion；无参正式入口核对 build identity `089BD4B726CD4167B94D0AA228426EFAA68ADCB07594B4A30139A58FD7C47864` / closure `BA4DE8E6615363166F9FF8856F4A2851958682DA133A3BC2C947146D35F710CF` / Core `EB2AEC6A402E3E43592F950AD32A863D62B8B1BAC0CBC2DBC5F0738B1A8089E8` 一致。真人 smoke 又验证大数量可继续调整、超出当前可直接结算时安全阻断、`最大` 可恢复、返回加购第二件仍保留且未提交交易，故严格状态为 `standard_entry_verified`。

### 0.0.39 2026-07-30 全屏 legacy renderer/fallback 退役候选

- **入口单轨**：仓库/战备箱、装备/角色构筑、NPC 商店、合成与技能教师统一 Web-only fail-closed。Host 删除 `CF7_WEB_INVENTORY_WORKBENCH`、旧 `warehouse` 与 `openEquipUI` 分支；AS2 场景/NPC opener 仍走正式 `panel_request`，但发送、准入、lazy load 或挂载失败只显示可见错误，不 attach、跳帧或重建旧 Flash UI。AS2 容器、金钱、配方、装备、技能与存档 authority 不迁移。
- **工作台闭包收敛**：standalone storage 初始闭包从 35 个脚本降到 16 个，tuning/build 由单一 2.9KB feature loader 按 view 加载封闭依赖，当前源码载荷净减约 388.7 KiB；任一依赖缺失或 mount 拒绝直接关闭或停留并提示，不用 eager preload 伪装可靠性。`DualPaneShell` 只接受六个具名 layout profile，宏观 grid 由 `core.css` 单点拥有。合成内的背包整理改为 crafting-owned local child，不再调用 `Panels.open("workbench")`，并删除 `returnTo`、无实例与 Host owner 特判。
- **main 资产闭包**：主 XFL 移除旧物品改装、物品栏、商店、仓库、教师与资源箱的 Include/时间轴放置，发布主 SWF 不再 ImportAssets `物品与技能相关界面.swf` / `物品改装界面.swf`，使旧大 UI 对 main 不可达。常驻快捷技能/药剂 HUD 所需的轻量图标由现役 HUD 资源单独承担；不得为了 `attachMovie` 复用而恢复旧聚合 SWF。
- **结构门**：单测 `node tools/test-audit-main-legacy-ui-reachability.js`；CS6 发布前跑 `node tools/audit-main-legacy-ui-reachability.js --source-only`，主文件精确 publish/verify 后跑 `node tools/audit-main-legacy-ui-reachability.js --require-swf`。前者只读 main Include/递归放置，后者经 FFDec 审 ImportAssets/linkage/PlaceObject；另叠加 XFL audit/rename/fix、linkage scanner、fresh Compiler Errors/Output Panel 与实际入口/失败链手测。
- **发布止点**：本节是源码/Flash/隔离候选裁决，不是部署事实。维护者先人工验收五类入口的成功路径、关闭/重开、写后回读与可见 fail-closed；确认可用后才允许冻结 immutable Git-tree request、构建本地 X509 + GitHub OIDC/Sigstore 双 signer/双 faultDomain 云端共识，并通过正式 DLL promotion 与标准入口复核。

### 0.1 施工定位约定（耐重构）

本文不把精确行号、帧号或临时目录位置当作施工入口。定位统一使用 **仓库根目录搜索锚点 + 当前路径提示**：

```powershell
rg -n -F '<唯一符号、协议字面量或语义标记>' . -g '!docs/**'
```

- 锚点优先级：协议名 / `gameCommand` / action / task > 类、函数或字段全名 > 唯一业务字面量 / 事件名 > 人工语义标记。
- 路径只用于缩小搜索范围；代码重构或搬迁后，以仓库根目录搜索结果为准。锚点随协议或符号改名时，同轮更新本文。
- 行号只可作为一次性审阅快照，不得写成 canonical 施工定位。
- 文档本身会引用锚点字面量，故默认排除 `docs/**`；施工锚点应在源代码/资源施工范围内唯一。若业务上必须多命中，本文须写明预期命中数/范围，并逐处核对上下文。
- 对 asLoader、帧脚本等缺少稳定符号的位置，或预计在重构中反复改名/搬迁的关键跨栈入口，施工时把对应表项晋升为少量、在施工源范围内唯一且不随实现命名变化的耐久语义标记，例如 `// @workbench-anchor inventory-bootstrap`。标记随代码块移动但不随函数改名；每轮施工用 `rg -n -F` 核验预期命中数。XFL/XML 优先用稳定 symbol/linkage/属性组合定位；只有确认 CS6/XFL 往返会保留注释时，才使用 `<!-- @workbench-anchor inventory-bootstrap -->`。不要依赖帧序号或泛滥添加标记。

| 要核对的事实 | 稳定搜索锚点 | 当前路径提示 |
|---|---|---|
| 权威库存构造 | `_root.物品栏 = {` | [SaveManager.as](../scripts/类定义/org/flashNight/neur/Server/SaveManager.as) |
| 背包来源调制门控 | `private static function resolveWebInventorySlot` / `checked.containerId != "背包"` | [EquipmentTuningService.as](../scripts/类定义/org/flashNight/arki/item/EquipmentTuningService.as) |
| 现役丢弃路径 | `_root.gameCommands["inventoryDiscard"]` | [InventoryPanelService.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelService.as) |
| BaseItem 完整存储投影 | `public function toObject():Object` | [BaseItem.as](../scripts/类定义/org/flashNight/arki/item/BaseItem.as) |
| 集合可变引用暴露 | `public function getItem(key:String):Object{` / `public function getItems():Object{` | [ItemCollection.as](../scripts/类定义/org/flashNight/arki/item/itemCollection/ItemCollection.as) |
| acquire 预检后写入 | `public static function acquire(itemArray:Array):Boolean` | [ItemUtil.as](../scripts/类定义/org/flashNight/arki/item/ItemUtil.as) |
| 既有移动便利方法（非事务边界） | `public function move(target:Inventory` / `public function merge(target:Inventory` / `public function swap(target:Inventory` | [Inventory.as](../scripts/类定义/org/flashNight/arki/item/itemCollection/Inventory.as) |
| 权威整理入口 | `public static function sortInventory(` | [ItemSortUtil.as](../scripts/类定义/org/flashNight/arki/item/ItemSortUtil.as) |
| 整理策略当前回退点 | `private static function validateSortMethod` | [ItemSortUtil.as](../scripts/类定义/org/flashNight/arki/item/ItemSortUtil.as) |
| 资源箱库存构造（当前） | `public static function materialize(target:Object):Object` | [LootMaterializationPlanner.as](../scripts/类定义/org/flashNight/arki/item/LootMaterializationPlanner.as) |
| 资源箱 Web-only 权威（当前） | `class org.flashNight.arki.item.LootContainerService` | [LootContainerService.as](../scripts/类定义/org/flashNight/arki/item/LootContainerService.as)；旧 `掉落物转换为物品栏` / `创建资源箱图标` 已退役且生产零引用 |
| 资源箱整理 Host capability | `IsValidLootInventoryEnvelope` / `IsAllowedLootOrganizerInventoryPayload` | [WebOverlayForm.cs](../launcher/src/Guardian/WebOverlayForm.cs) |
| 资源箱同面板整理子页 | `root.LootOrganizer = api` / `data-loot-organizer` | [loot-organizer.js](../launcher/web/modules/loot/loot-organizer.js) |
| 战备箱可访问容量 | `public static function getAccessibleCapacity` | [InventoryPanelService.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelService.as) |
| KShop 当前结算与旧存档兼容命令 | `_root.gameCommands["shopBulkQuery"]` / `_root.gameCommands["shopSaveCart"]` / `_root.gameCommands["shopCheckoutPreview"]` / `_root.gameCommands["shopCheckoutCommit"]` / `_root.gameCommands["shopCheckout"]` / `_root.gameCommands["shopClaim"]` | [商城系统_WebView.as](../scripts/逻辑系统分区/商城系统_WebView.as) |
| KShop Web 领取入口 | `function onClaim(e)` | [kshop.js](../launcher/web/modules/kshop.js) |
| Web `panel_resp` 广播 | `if (handlers[data.type])` | [bridge.js](../launcher/web/modules/bridge.js) |
| KShop request mux / 写协调（Step 0/1 已落） | `function KShopRequestMux` / `function KShopWriteCoordinator` | [kshop-runtime.js](../launcher/web/modules/kshop-runtime.js) |
| Workbench Gate A1 primitive（Step 2 已落） | `function DualPaneShell` / `function GridRenderer` / `function PointerDragController` / `contractStatus` | [workbench.js](../launcher/web/modules/workbench.js) |
| Inventory 领域协调 / 通用 UI 组件 / 独立战备箱组合 | `function InventoryCoordinator` / `function InventoryWindowPager` / `function InventoryFilterControl` / `function OwnedInventoryViewShell` / `var InventoryWorkbench` | [inventory-runtime.js](../launcher/web/modules/inventory-runtime.js) / [inventory-ui.js](../launcher/web/modules/inventory-ui.js) / [inventory-workbench.js](../launcher/web/modules/inventory-workbench.js) |
| 统一分类模型与渐进导航 | `function FilterNavigator` / `function buildModel` / `function fromFacets` | [item-filter.js](../launcher/web/modules/item-filter.js) |
| 统一语义卡片/格子与密度 | `function ItemGrid` / `function GridDensityController` / `item-card-overlays` | [workbench.js](../launcher/web/modules/workbench.js) / [panels.css](../launcher/web/css/panels.css) |
| Host 面板路由 | `private void HandlePanelMessage` | [WebOverlayForm.cs](../launcher/src/Guardian/WebOverlayForm.cs) |
| Web→Flash 保留键守卫 | `private static readonly HashSet<string> Reserved` | [PanelBridge.cs](../launcher/src/Tasks/PanelBridge.cs) |
| ShopTask 回包形状 | `public sealed class ShopTask` + `msg.Remove("task")` | [ShopTask.cs](../launcher/src/Tasks/ShopTask.cs) |
| MapTask pending/回包范式 | `public sealed class MapTask` + `private sealed class PendingRequest` | [MapTask.cs](../launcher/src/Tasks/MapTask.cs) |
| InputShield 收起冷却 | `收起冷却：hitRect 缩小后短暂禁止 CDP mouseMoved` | [InputShieldForm.cs](../launcher/src/Guardian/InputShieldForm.cs) |
| 技能图标运行态出图载体（双 linkage，主 FLA 为迁移蓝本，仍须确认运行态命中，见 §4.4） | `attachMovie("技能图标"` / `linkageExportForAS="true" linkageIdentifier="技能图标"` | 运行态 attach=[引擎_lsy_技能系统.as](../scripts/引擎/引擎_lsy_技能系统.as)；linkage 候选=[arts/things 旧结构](../flashswf/arts/things/LIBRARY/技能图标.xml) + [主 FLA 新结构](../CRAZYFLASHER7MercenaryEmpire/LIBRARY/sprite/技能图标.xml)；资产索引=[asset_source_map.xml](../data/items/asset_source_map.xml) |

---

## 1. 背景与现状

### 1.1 现有 AS2 物品系统（迁移源）

编排器：[scripts/展现/UI交互/UI交互_lsy_物品栏UI.as](../scripts/展现/UI交互/UI交互_lsy_物品栏UI.as)。数据模型：`scripts/类定义/org/flashNight/arki/item/`。

运行态权威结构（**AS2 持有，等于存档真相**）：

权威构造见 [SaveManager.as](../scripts/类定义/org/flashNight/neur/Server/SaveManager.as)，定位锚点 `_root.物品栏 = {`（容量数字以此为准，勿凭记忆）。下方为**示意伪代码**——构造首参实为存档数据对象（真实形如 `new ArrayInventory(mydata.inventory.背包, 50)`，首参=存档数据、第二参=容量），此处用容器名仅作示意：

```
_root.物品栏 = {
  背包:   ArrayInventory(背包, 50),       // 主库，数字索引 0..49
  装备栏: EquipmentInventory(装备栏),     // 11 槽，键=装备类型字符串
  药剂栏: DrugInventory(药剂栏, 4),       // 快捷药剂，全局 HUD 层
  仓库:   ArrayInventory(仓库, 1200),     // 容量 1200
  战备箱: ArrayInventory(战备箱, 400)     // 容量固定 400；仅"可访问页数"随主线/基建变化（非容量动态）
}
_root.收集品栏 = {
  材料: DictCollection(材料),             // 字符串键→数量，可堆叠
  情报: InformationCollection(情报)       // 上限受 informationMaxValueDict 约束
}
```

子系统与背景 MovieClip（迁移前的形态）：背包+装备栏同在 `物品栏界面` 的「物品栏」帧；材料/情报为该界面的「材料」「情报」帧；商店=`购买物品界面`；仓库=`仓库界面`；资源箱=动态加载的 `资源箱界面`；强化=`物品栏界面.装备强化界面`（空/默认/强化装备/强化度转换/插件改装 多帧状态机）。

### 1.2 「只对背包栏放行」约束（散点，迁移要收口）

迁移前，源-集合判断散落在完整物品栏 renderer、强化/样品栏脚本与图标 hitTest 中，本质都是「只有背包能作为某类操作的来源」。2026-07-30 已完成收口：

- 完整物品栏/样品栏 renderer 与 `InventoryIcon`、`EquipmentIcon` 已删除，不再作为生产规则或失败回退。
- [EquipmentTuningService.as](../scripts/类定义/org/flashNight/arki/item/EquipmentTuningService.as) 的 `resolveWebInventorySlot` 统一裁决调制来源必须是 `背包`。
- [CharacterBuildService.as](../scripts/类定义/org/flashNight/arki/item/CharacterBuildService.as) 统一裁决 Web 角色构筑中的装备/卸装/药剂装卸、目标槽与 `inventory_full`。轻量 `DrugIcon` 仍服务常驻快捷药剂 HUD 的点击卸药并直接同步药剂栏/root 镜像；这是战斗 HUD 的窄输入责任，不是全屏 UI renderer 或故障 fallback。
- [InventoryPanelService.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelService.as) 统一持有库存窗口、lease、转移、整理与丢弃规则。

这些判断已集中到 AS2 领域服务的策略层（一处持有、最终裁决）。Snapshot 可附带"当前 capabilities"供 Web 展示与预检（灰显/提示），但**Web/Broker 的能力声明只是 UX 预检，不是权限 SOT**——真实放行与否由 AS2 在执行命令时重新裁决（见 §5、护栏 §7）。这样 Web 声明过期/错误时不会导致非法跨容器操作。

### 1.3 已建立的 Web Panel 迁移范式（迁移底座）

merc / pet / arena / kshop 已迁移，全部是 **snapshot + command** 模型：开面板 AS2 下发只读 snapshot；用户动作发离散命令；AS2 改权威态，按既有业务契约 `dirtyMark` 或显式强制存盘，再回包。**没有任何已迁移面板做过两个活动库之间的连续跨库转移**——这正是物品系统的核心交互，也是本设计要新建的部分。

复用件：`PanelScale`（[panel-scale.js](../launcher/web/modules/panel-scale.js)；**通用 contain 等比缩放** `scale = min(w/designW, h/designH)`、两轴取小保形不裁切，**非写死 16:9**——画布 1024×576 与 16:9 anchor 同比时无损铺满，异比画布会留 letterbox，后续非 16:9 的 View 须知此语义）；双层 callId 桥（`PanelBridge.BuildFlashCommand` + 保留键守卫）；`Panels.register`；PanelHost open/close 生命周期。

---

## 2. 核心抽象：双栏工作台

把「物品面板」升维为**通用双栏工作台**：双栏只是两个 view-slot，背包/技能/仓库/商店都是可插拔 **View**；物品界面、技能界面只是不同的 View 配置。

```
DualPaneShell（壳：双 view-slot + PanelScale 缩放 + 换栏 + 冲突原子回滚 + 焦点）
   ├─ slotL ─┐
   ├─ slotR ─┴─ 各挂一个 WorkbenchView（任一槽皆可换；壳只认 View 生命周期接口）
   │
   ├─ InteractionBroker（仅交互撮合：Offer × Accept → 中性 OperationIntent；不知 AS2 命令名/timeout/dirtyMark）
   │
   └─ DomainCoordinator（领域命令 + 并发；OperationIntent → 命令/单飞/对账，AS2 最终裁决）
         ├─ InventoryCoordinator   背包/仓库/战备箱；持 backpack write gate
         ├─ ShopCoordinator        saveCart/checkout/claim 单飞 + needs_reconcile
         └─ CharacterBuildSession  loadout/tuning 会话 + edit/stats 结算屏障；不持领域 writer

WorkbenchView（可插拔；契约=mount/unmount + render/subscribe + exportOffer(hit) + probeAccept(offer,hit)）
   ├─ CatalogView      商店目录（非容器：购买意图的来源）
   ├─ GridContainerView  ← 网格容器 primitive（见 §6；拆 GridRenderer / ContainerViewAdapter / ViewChrome）
   │     背包 / 仓库 / 战备箱 / 资源箱 / 购物车 / 已购买 全是它的配置（按 binding 区分实例）
   ├─ EquipView        角色构筑编辑态：纸娃娃 + 装备/药剂槽 + 候选；详细属性为全宽互斥二级态
   ├─ SkillView        技能栏（仅复用 View/Shell 视觉契约；领域实现归独立 skills 主线）
   ├─ EnhanceView      强化 / 强化度转换 / 插件改装
   └─ CraftView        合成
```

**第一性原则（四层职责，2026-06-20 按评审改判）**：**Shell 只认识 WorkbenchView 生命周期契约**（mount/unmount/render/subscribe + slot 元数据；mount/unmount 不绕 Broker）；**跨 View 交互只认识 Offer/Accept**——发起方 `exportOffer` 产出**不含 AS2 命令名**的交互报价，落点 `probeAccept` 返回是否接受 + UX hint，InteractionBroker 据此生成中性 `OperationIntent`；**领域命令与并发只存在于各 DomainCoordinator**——`InventoryCoordinator` 结合当前目标态决定发 `move`/`merge`/`swap`、`ShopCoordinator` 处理购物车意图，AS2 再做最终裁决。原"壳与 View 互不知道对方、只通过 Broker 能力契约对话"的窄表述已被本四层取代：否则 Broker 会同时认识 `shopSaveCart/inventoryMove/equip/claim/sell/craft`，N² 耦合只是从 View 之间搬进一个中央 switch、并未消失。

最小接口（命名空间隔离领域命令）：

```js
// View 产出的交互报价——不含 AS2 命令名
{ subjectKind: "ownedItem"|"catalogEntry"|"purchaseReceipt",
  sourceRef: { containerId, slot, lease },
  offeredOperations: ["inventory.transfer"] }
// 目标 View 的探测结果
{ accepted: true, operationId: "inventory.transfer",
  targetRef: { containerId, slot, lease }, hint: "merge" }   // hint 仅 UX，非最终命令
// Broker 生成的中性领域意图
{ operationId: "inventory.transfer", sourceRef, targetRef }  // 由 InventoryCoordinator 落 move/merge/swap
```

Catalog→Cart 走 `operationId: "shop.addCartIntent"`（ShopCoordinator 处理），只借用同一套手势撮合，**不被包装成 inventory transfer**。代码核查确认：`DualPaneShell / WorkbenchView / InteractionBroker / DomainCoordinator / GridContainerView / TransferBroker` 在全仓零既有同名符号，是纯空地，本次拆分/改名零迁移成本。

**视图单例不变量**（壳职责，**按 binding key 而非组件类型判定**）：单例性按 `instanceKey`（如 `inventory:背包`、`shop:cart`）+ `instancePolicy:"singletonByBinding"` 判定，**不是**按构造函数 / View class / 笼统"同一个 View"——`BackpackView` 与 `WarehouseView` 都基于同一个 `GridContainerView` 类却显然可并存（绑定不同容器），只有同一 binding（如双购物车）才排他。同一 `instanceKey` 默认不允许同时挂两个槽；用户在 A 槽选了一个已在 B 槽显示的 instanceKey 时，壳原子解冲突：先把目标移入 A；B 优先接收 A 原先的 View（仅当 B 允许且不会再次冲突），否则回落到 B 的结构默认 View；若回落仍冲突或不可用，则拒绝本次切换并保持原状态。该规则取代任何"避免双购物车"之类的视图专属状态机，并确保 displaced 槽在后续多视图组合中仍有确定去向。

---

## 3. 两种转移模态（关键区分）

| 模态 | 例子 | 特征 | 发出的命令 |
|---|---|---|---|
| **①a 同质 owned 转移** | 背包↔仓库/战备箱 | 即时、逐件、落点即提交 | `inventoryMove`/`inventoryMerge`/`inventorySwap`（§4.1，仅稳定同质 owned 容器；资源箱瞬态、暂排除，见 §4.3/§6） |
| **①b 显式领域动作** | 装备/卸装、领取(claim)、丢弃(discard)、出售 | 即时，但**含专属业务逻辑**（装备=等级检查+外观刷新；claim=移除待领记录+入包+强制存盘；discard=确认后从指定 owned 槽删除；出售=定价+金钱） | 各自显式命令 `equip`/`unequip`/`claim`/`inventoryDiscard`/`sell`，**不是**通用转移 |
| **② 暂存转移** | 商城购买（目录→购物车→结账→已购买→claim）、将来合成/批量出售 | 累积进缓冲 → 单次 commit → 常带代价（K点/金钱/配方） | view 专属 `checkout`/`craft` 批量提交 |

双栏语言**中心是模态①**；模态② 的 UI（购物车/结算）**保持视图本地**，直到第二个视图（合成/批量出售）真的需要同款暂存，再机械式抽取到壳级 staging 托盘。命名好抽取点，但不提前付泛化的钱。

---

## 4. 权威与命令模型

- **AS2 永远是权威态**，Web 只是投影。沿用 snapshot + command；新增 inventory-domain 写入成功后必须 `dirtyMark`，既有领域动作保留其明确的持久化契约（如 KShop claim 的强制存盘）。写操作必须由 AS2 权威路径重新校验价格/权限/槽位/状态（见迁移护栏 §7）；Web 端禁用按钮只是 UX，不是安全校验。

### 4.1 命令分层（修正 itemTransfer 过宽）

不存在一个吃下一切的 `itemTransfer`。命令按模态分层：

- **模态①a 同质 owned 转移**——仅用于 owned↔owned 的稳定同质容器（背包/仓库/战备箱）：`inventoryMove` / `inventoryMerge` / `inventorySwap`。（资源箱是瞬态容器，暂不入此列，见 §6。）
- **模态①b 显式领域命令**——`equip` / `unequip` / `claim` / `inventoryDiscard` / `sell` / `enhance` 等各自携带专属业务（等级检查、外观刷新、移除待领记录、确认后删除、强制存盘、定价、强化），**不得**塞进通用转移。`inventoryDiscard` 是首战“腾格子”的明确写命令，不复刻 Flash UI hitTest 路径。
- **模态①c 整容器写**——`inventorySortAndMerge`（真实重排槽位 + 合并堆叠 + dirtyMark）：既非逐件转移、非领域动作、非批量提交，而是整容器重组写，亦走本节信封；它与只读的权威树筛选严格区分，纯 Web `displaySort` 已退役（§6）。既有算法见 [ItemSortUtil.as](../scripts/类定义/org/flashNight/arki/item/ItemSortUtil.as)，定位锚点 `public static function sortInventory(`；**该工具本身不设置 `dirtyMark`，新领域 handler 必须在成功后显式设置**。
- **模态② 批量提交**——`checkout` / `craft`。

**命令信封（显式版本化）**。信封要求**仅适用于新增 inventory-domain 及后续模态① owned 命令**；既有 KShop `shop*` 命令是模态② view 专属命令，首战**不强制套信封、不改其形状**（见末尾"信封适用范围"）。Web 外层桥接信封与 inventory 领域负载必须分层，避免 `cmd`/`op` 双重真相；版本兼容通过 `payload.v` 明确迁移，不靠未知字段静默透传。

```js
{
  type: "panel",
  panel: "kshop",             // 发起面板，仅用于 Web 生命周期/诊断
  domain: "inventory",        // 领域路由
  cmd: "discard",             // InventoryTask 白名单键
  callId: "...",
  payload: {
    v: 1,                     // 领域负载版本
    source: { containerId, slot, expectedLease } // slot lease（不透明 token）；containerEpoch 可选携带
  }
}
```

InventoryTask 下发给 AS2 时去掉 Web 路由层并展开已验证 payload，例如：

```js
{ task: "cmd", action: "inventoryDiscard", callId: 17, v: 1, source: {...} }
```

关键约束：

- **可信 action 只由 C# 白名单映射产生**：`snapshot→inventorySnapshot`、`tooltip→inventoryTooltip`、`discard→inventoryDiscard`、`move→inventoryMove`、`merge→inventoryMerge`、`swap→inventorySwap`、`sortAndMerge→inventorySortAndMerge`。`InventoryTask` 校验 `domain/cmd/payload.v`，按下表为每个 v1 cmd **递归新建仅含白名单字段的 normalized payload**，再调用 `PanelBridge.BuildFlashCommand`；不得把原始 Web 消息或原始 payload 直接透传。这样 `op/action/task/cmd/domain/panel/callId` 及未知字段均不会下发。未知 cmd 回 `unsupported_cmd`，未知版本回 `unsupported_version`，字段非法回 `invalid_payload`。保留键守卫见 [PanelBridge.cs](../launcher/src/Tasks/PanelBridge.cs)，定位锚点 `private static readonly HashSet<string> Reserved`。
- **`containerId` 是稳定的领域容器标识**（如 `背包`/`仓库`/`战备箱`），**不是可换栏的展示槽（slotL/slotR）、View 实例或可拼接的 AS2 属性路径**。Web 换栏不影响命令寻址；AS2 领域注册表显式白名单解析 containerId，未知值回 `unsupported_container`。首战只向 inventory domain 暴露 `背包`。
- **并发防线 = AS2 snapshot-local slot lease（按命令前置条件），不是"全字段相等"**（2026-06-20 按评审改判，废除原"全量 canonical descriptor 现算"方案）。要防的是 **stale intent / 地址前置条件**——用户操作快照里的槽位 A，命令抵达时 A 已被换成别的东西、或目标槽状态已变——而**不是**"整个物品存储态逐字段完全相等"。后者语义过度（耐久、`lastUpdate` 等不影响寻址的字段变化会被误判 `stale`），且在 AVM1 上要递归遍历 + 键排序 + 对象分配 + 字符串膨胀（1200 格仓库放大成数十~数百 ms 定格、易触发 Flash 15s 脚本超时）。改用 AS2 铸造的有状态 lease；跨容器 move/merge/swap 的 source 与 target **都**携 lease，由 AS2 在**同一临界区**内一并比对、通过才执行，任一不符回 `stale_state`、Web 重拉相关 snapshot。
  - **lease 形态（2026-07-10 刷新裁决）**：AS2 维护有限生命周期记录 `sessionNonce → leaseId → {containerId,physicalSlot,occupantRef,kind,count/mergeKey,confirmProjection}`；token 对 Web 只暴露不透明的 `sessionNonce+leaseId` 并原样回显，**不再把 containerEpoch/containerGeneration 嵌入每个 slot token**，避免无关槽随任意写全失效。AS2 可按 containerId 保存平行数组（`leaseIds[]/refs[]/counts[]/keys[]/confirm[]`）实现 O(1) 查验，不建深对象哈希。
  - **O(1) 校验**：对象物品 `currentItem === lease.occupantRef`；primitive/dict stack `currentValue == lease.expectedValue`；空槽 `currentItem == null`。直接发现：槽被另一物品替换 / 物品被移走 / 目标空槽被占 / source 被删 / target stack 被换。同一物品对象内部字段变化时，再按下表具体命令检查相关字段，不做全字段比较。
  - **lease 失效边界（代码核查 2026-06-20 补强，落地必须守住）**：lease 按 **BaseItem 对象引用**锁——`add/getItem/纯 rebuildOrder` 全程零克隆、保身份，AVM1 `===` 按引用相等；[InventoryPanelServiceTest.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelServiceTest.as) 的 whole-slot move/swap 用例是当前生产契约证据。但公开排序入口 [ItemSortUtil.as](../scripts/类定义/org/flashNight/arki/item/ItemSortUtil.as) 的 `sortInventory` 必带 `mergeStackables → rebuildInventory → ArrayInventory.setItems → super.setItems → ItemCollection.setItems → BaseItem.createFromObject`（见 [ArrayInventory.as](../scripts/类定义/org/flashNight/arki/item/itemCollection/ArrayInventory.as) 与 [ItemCollection.as](../scripts/类定义/org/flashNight/arki/item/itemCollection/ItemCollection.as)），会**全量重铸所有物品对象身份（不止被合并项）**→ 任何经 `setItems`/排序的路径后，受影响容器 lease **整体失效**（失效钩子挂 `sortInventory/mergeStackables`；挂纯 `rebuildOrder` 只够应对 key 失配、不够身份失配）。注意两条 merge 语义相反：`Inventory.merge` 走 `addValue` 原地改值、目标身份**保持**；`ItemSortUtil.mergeStackables` 铸新对象、身份**替换**。又：`createFromObject` 重铸 BaseItem 外壳却**复用同一 `value` 子对象引用**，故 lease 必须锁 BaseItem 外壳、**不能锁 `value` 子对象**（否则 setItems 后假阳性命中）。
  - **按命令定义 stale 前置条件**（取代"所有写命令都比完整 descriptor"）：

    | 命令 | 前置条件 |
    |---|---|
    | `discard` | source occupant lease + 用户确认投影一致 |
    | `move` | source occupant lease + target occupancy lease |
    | `swap` | source、target occupant lease |
    | `merge` | source/target lease + mergeKey + 两端 count |
    | `equip` | source lease；等级/use/槽位规则现场重判 |
    | `sortAndMerge` | **通常无需 stale token**；对当前权威容器执行（客户端只上送 `methodName`、不上送待应用布局）。最多可选携 `containerEpoch` 做结构失效，**勿**因"玩家刚捡到一个物品后点整理"误判 `stale_state` |
    | `claim` | purchased-list token 或稳定 claimId（**不是** inventory fingerprint，见 §9.1/§9.2） |
    | `snapshot` | 无前置条件 |

  - **`confirmProjection` 显式版本化**：只含"若变化必须让用户重新确认"的字段——`{ itemKind, displayName, quantity, enhancementLevel, rarity }`，**不含** `lastUpdate` 等用户没看到的内部字段（确认框没展示的内部变更不应自动使确认失效）。
  - **presentation projection 与确认投影分离**：occupied slot 的 `item` 额外携 `maxEnhancementLevel/isMaxEnhancement/tierSlotAvailable/tierSlotUsed/modSlotCapacity/modSlotUsed/modSlots[]`。这些字段只驱动物品格角标、动态满级框、独立升阶菱形，以及插件槽状态/档级色/角色符号，不扩大 `confirmProjection`，也不参与 transfer stale 判定；`itemKind=stack/equipment` 是领域机器字段，只供操作选择，不直接显示给玩家。现役生产 XML 的 `modslot` 只出现 `0/1/3`，因此物品格保持“1 个进阶 + 3 个插件”的固定比较轨，并把插件位置区分为已装、空闲与不存在；这是一项紧凑 presentation 合同，不是 Web 对调制合法性的裁决。Tuning 决策面另以 `snapshot.equipment.modSlotCapacity + snapshot.equipment.mods` 全量投影权威容量，不得把 presentation 的 `modSlots[]` 当成同一字段；若未来生产 schema 真正扩到 4+，必须同轮升级物品格比较轨、AS2 projection 与视觉门，而不能只让数字溢出。
  - **拆开被一个 `expectedFingerprint` 混在一起的四种职责**：`snapshotSeq`（仅供 Web 忽略倒序回包）/ `slotLease`（验证 source/target 仍是快照中占用者）/ `containerEpoch`（可选，结构性变更与整容器缓存失效）/ 未来 `instanceId`（跨 snapshot、跨 session、可持久化的物品身份）。协议字段 `expectedFingerprint` 统一改名 **`expectedLease`/`slotLease`**。v1 不引入 `instanceId`；旧"完整存储态相同视为可互换"的身份语义被 lease 的"按运行态对象引用区分"取代（更严格，但只多一次 stale、不会误操作，且无需现在决定持久化 instanceId）。
  - **canonical encoder 降级**：从 inventory-domain 全局基础设施降为 **诊断工具 / 测试 oracle / 少数确需 exact-storage-equality 的命令专用实现**；其存储健康检查（`undefined`/函数/循环引用/NaN/Infinity）应放在加载、存档验证或诊断工具，**不应顺带成为每个 UI 命令的前置条件**（否则仓库里有一件遗留脏数据就能禁掉整个 `sortAndMerge`）。若某命令坚持无状态 token，次优方案是 operation-specific digest：`hash(versionTag, kind, identityFields, operationRelevantFields)`，固定字段序 + 长度前缀 + 流式计算 + 只传定长摘要，不构造完整 canonical 字符串、不遍历无关字段；因 Web 不算 token，这里不存在跨语言 canonical JSON 必要性。`BaseItem.toObject()`（锚点 `public function toObject():Object`）仍是物品级存储投影的来源，但只在需要 exact-equality 的诊断/测试路径用，不进每命令热路径。
  - **token 铸造边界**：所有 lease/digest token 均由 AS2 snapshot 生成，Web 只缓存并原样回显。
- **AS2 最终裁决**：版本、容器/槽位、token 以及所有业务合法性（容量、use 匹配、背包-only、金钱/K点、等级）都在执行时由 AS2 领域服务重判；C# 结构校验与 Web capabilities 仅是前置拒绝/UX 预检。

**v1 inventory cmd 负载形状**：

| Web cmd | `payload` 必需字段 | 并发覆盖 | 成功结果 |
|---|---|---|---|
| `snapshot` | `{v, requests:[{containerId,offset,limit}]}`；背包首版固定 `offset=0,limit=50` | 无；只读 | 请求窗口的 snapshot + AS2 铸造 lease/snapshotSeq/containerEpoch |
| `discard` | `{v, source}` | source 槽 | 刷新后的背包 snapshot |
| `move` / `merge` | `{v, source, target}`；v1 仅整槽移动/整组堆叠合并，拒绝 `count` | source + target 槽 | 受影响容器 snapshot |
| `swap` | `{v, source, target}`；整槽互换，拒绝 `count` | source + target 槽 | 受影响容器 snapshot |
| `sortAndMerge` | `{v, container, methodName}` | 完整容器 | 刷新后的容器 snapshot |

`methodName` 在 v1 必填，并由 AS2 领域 handler 对显式策略白名单做严格校验；未知值回 `unsupported_sort_method` 且不得写入。既有 `ItemSortUtil` 的 `private static function validateSortMethod` 会把未知值静默回退到默认策略（实测：未知/空 method 静默回退 `byType`，其"合法集"是 `STRATEGY_CHAINS` 八个键 `byType/byUse/byPrice/byLevel/byID/byName/byValue/byTime`、并非协议白名单），只能作为当前行为定位点，**不能直接充当协议验收器**；handler 校验通过后才可调用既有排序工具。每个 inventory-domain 写命令都必须在同一权威处理路径中完成“比对 lease → 重判业务 → mutate → dirtyMark → 生成回包”，不得把校验和写入拆到可被其他事件插入的两段。

**snapshot 窗口化（形状已冻结并在 Step 3–5a 落地）**：v1 唯一请求形状为 `{v,requests:[{containerId,offset,limit}]}`、回包 `{containerId,capacity,snapshotSeq,containerEpoch,offset,slots}`；move/merge/swap 回受影响可见窗口，`sortAndMerge` 使仓库全部 lease 失效但只返回原当前页，翻页按需重拉背包 + 当前仓库页，**不把 1200 格容器当成一个不可分割 snapshot**。背包同样使用 `offset=0,limit=50`，未经历 `{containerIds}` 过渡协议。

**原子性不能靠“AS2 单线程”想当然**：现有 [Inventory.as](../scripts/类定义/org/flashNight/arki/item/itemCollection/Inventory.as) 的 `move/merge/swap` 是多步便利方法，过程中 `add/remove/addValue` 会同步发布生命周期事件，`swap` 也未逐步检查失败并回滚；事件回调是可重入点，因此这些方法本身不是 inventory-domain 的事务边界。新领域 handler 必须先完整预检两端，在事务重入守卫下完成无部分失败的状态提交，再统一发布事件、`dirtyMark` 与 snapshot；若不能保证提交成功，必须保持两端原状。禁止把“直接调用现有 move/merge/swap”当作原子性证明。部分堆叠拆分会引入数量、时间戳、回滚与确认语义，留给后续显式协议版本，v1 拒绝 `count`。

白名单按迁移阶段启用：首战只开放 `snapshot` 与 `discard`，其余 cmd 在对应 AS2 handler、闭环表和测试全部落地前均回 `unsupported_cmd`。C# 只负责结构/版本/action 白名单；containerId、槽位、token 与业务合法性仍由 AS2 最终裁决。

**信封适用范围**（解决"统一信封"与"首战保留 KShop 字段形状"的冲突）：信封 + 双端 token 是**新命令族（inventory-domain、后续模态①）的契约**；KShop 的 `shopBulkQuery/saveCart/checkout/claim` 首战保持既有请求/回包字段形状、不回填信封，但其写命令新增 §9 所述 ShopTask 单飞与 `busy/reconcile_required` 前置拒绝行为。若日后要给 shop* 套信封，单列为协议迁移项。

#### 4.1.1 equip / unequip（模态①b）：槽寻址、payload 与活体副作用（2026-06-21 补，源 玩家信息界面 HUD 迁移交汇）

equip/unequip 现 doc 此前只有 §3 模态①b 一行 + 「各自携带专属业务」一句，属真空缺口；首战（KShop）/ Step3-4 都不碰 equip，故此处先补物品/药剂契约占位，等真正排到对应纵切再提进白名单。**装/卸是 owned 主库（背包）↔ 槽位化目标（装备栏 / 药剂栏）的领域写，不是同质转移**，故 payload 在 source lease 之外**必须显式携带 target 容器 + 槽号**（「装到几号槽」），不靠落点 hitTest 反推。快捷技能不从背包出发，已于 §0.0.34 拆到独立 Skill 协议：

```js
// equip：背包某槽 → 目标槽位
{ task:"cmd", action:"equip", callId, v:1,
  source:{ containerId:"背包", slot, expectedLease },
  target:{ containerId, slot } }   // containerId∈{装备栏,药剂栏}
// unequip：目标槽位 → 背包（背包落点由 AS2 取 getFirstVacancy，不由 Web 指定）
{ task:"cmd", action:"unequip", callId, v:1,
  source:{ containerId, slot, expectedLease } }
```

| target containerId | slot 语义 | AS2 落点（一次性快照锚点） |
|---|---|---|
| `装备栏` | 装备类型**字符串键**（EquipmentInventory，键=use/装备类型，**非数字**） | `ItemUtil.moveItemToEquipment`：`index!=equipmentIcon.index` 拒、等级门 `data.level>_root.等级` 拒 |
| `药剂栏` | 数字索引 0..3（DrugInventory(4)） | `ItemUtil.moveItemToDrug`：冷却门 `!isCoolDown() → return false` |
| `快捷技能栏` | 数字槽号 1..12，但无 owned source/ItemCollection | **不使用本 equip payload**；见 [Skill 主线](技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md) `skillEquip/skillUnequip`，技能协议字段为 `skillKey + slot + expectedRevision` |

**槽寻址与并发约束**：
- `target.containerId` 由 AS2 领域注册表**显式白名单**解析，未知值回 `unsupported_container`；首战不开放 equip。
- **slot 必须显式**：v1 `equip` 拒绝「未带 target.slot 让 AS2 猜空位」；装备栏 slot 是**类型字符串键**不是数字（与背包/仓库的数字 physicalSlot 不同栏寻址，渲染绑定须分别保留）。`unequip` 落背包由 [CharacterBuildService.as](../scripts/类定义/org/flashNight/arki/item/CharacterBuildService.as) 现场选择空位，不由 Web 指定；满则回 `inventory_full` 明确提示，不静默失败。
- **并发**：物品 `equip` 携 source lease；target 槽是否被占由 AS2 现场判（占用即按目标栏规则 swap 或拒绝，**不复用 inventoryMove 的 target occupancy lease**——装备栏/药剂栏是领域槽非同质 owned）。快捷技能改用 `skillRevision`，无 source lease。

**药剂 unequip 的两条非显式路径（无叉按钮，迁移须各自等价复刻，不可套通用 unequip）**：
1. **Web 管理入口由 CharacterBuildService 裁决**：[CharacterBuildService.as](../scripts/类定义/org/flashNight/arki/item/CharacterBuildService.as) 的 `readDrugCooldown` 通过 `ManualCooldownService.isReady(drugKey)` 读取逻辑冷却 authority，再决定装入/卸下是否允许并同步药剂栏与 root 镜像；不再读取 `药剂进度条.冷却`。常驻快捷 HUD 另保留 `DrugIcon.Press()` 的点击卸药路径，它直接 `collection.move(...)` 并清 root 镜像；这是 HUD 输入，不得扩张成全屏物品栏 fallback。
2. **耗尽自动卸**：药剂用尽时 `药剂控制器` 自动清栏（`addValue(index,-1)` 后 `if(!icon.item)` → `_root[控制参数]=""` + 「耗尽！」）。这是**战斗内事件驱动清栏**（§3 模态之外），不是 Web 命令；Web 侧药剂 snapshot 用药/耗尽后须被动重投影，**Web 不得对耗尽清栏发 unequip**（会与自动清栏竞态）。

**equip/unequip 活体副作用清单（AS2 必做、Web 不可代偿）**——装/卸不是纯数据搬移，落地后触发改在场战斗单位的派生态，全部留 AS2 重做：

| 目标栏 | 装/卸必做的 AS2 活体副作用 | 锚点（一次性快照，须按 §4.4 激活份校正） |
|---|---|---|
| **快捷技能栏** | 写 `主角技能表[i][2]/[4]`（装 true/卸 false）→ `_root.更新主角被动技能()` → 回写 `_root.gameworld[_root.控制目标].被动技能 = _root.主角被动技能` → `_root.排列技能图标()` | 装=技能图标.xml（新结构）；卸=快捷技能界面.xml `取消装备技能`（**仅被动技能改时才重算**） |
| **装备栏** | `_root.刷新人物装扮(_root.控制目标)`（外观）+ 音效 + `dirtyMark` | CharacterBuildService（equip/unequip） |
| **药剂栏** | `物品栏.药剂栏` move/addValue 改权威 + 同步 `快捷物品栏N` 镜像 + `dirtyMark` | CharacterBuildService（equip/unequip）/ DrugInputService（使用扣减）/ 轻量 HUD 投影 |

- **技能装/卸的「活体」核心 = 被动技能重算并回写战斗单位**（`更新主角被动技能()` + hero 投影），直接影响在场角色战斗数值，**绝不能由 Web 算或跳过**。旧代码“卸仅被动时重算、装无条件重算”是分散实现事实，不是新协议必须复制的控制流；独立 Skill 服务采用成功写后统一收敛，只要结果与动态 CD/MP 副作用正确且无漏算。
- **dirtyMark 责任**：装备/药剂路径沿用各自 AS2 authority；Skill 主线已由 `SkillLoadoutService` 在成功的 learn/equip/unequip/reorder/setPassive 原子提交后统一 `dirtyMark=true`，no-op 与失败/回滚不标脏。该缺口已关闭，Web 仍不得代写存档标记。
- **重投影范围**：物品 equip 影响 source（背包）+ target 栏 + 派生态（装扮）；技能 equip 没有背包 source，成功后返回完整 Skill snapshot + AS2 被动/快捷描述符投影。两者都不以 Web 局部 patch 作为权威。

### 4.2 渐进

- **v1 用 round-trip 确认**（快车道低毫秒级，够用）；乐观更新 + snapshot 对账作为第二步增量，不在首版背回滚复杂度。
- **重投影**：AS2 mutate 后，受影响窗口（携当前 `snapshotSeq/containerEpoch` + 新铸 slot lease）重新下发，Web 重渲染；move/merge/swap 只失效并重铸 source/target 等真实受影响槽，排序/`setItems`/整容器重铸才使该容器全部 lease 失效（见 §4.1 失效边界）。
- **写命令不自动重试**：已下发写命令遇 timeout/断连/未知结果时，先拉权威 snapshot 对账；只有确认状态后，才允许用户重新发起新意图。禁止网络层静默重放 move/swap/discard/checkout 等非幂等写。`saveCart` 这类完整期望态覆写可在对账后按**当前最新期望值**构造一条新同步，但同样不得重放旧请求。

### 4.3 容器 ID ↔ AS2 运行态映射（开工核对表）

降低逐次核对负担；权威以 [SaveManager.as](../scripts/类定义/org/flashNight/neur/Server/SaveManager.as) 的 `_root.物品栏 = {` 锚点为准：

| 逻辑容器 ID | AS2 运行态 | 模型 | 备注 |
|---|---|---|---|
| `背包` | `_root.物品栏.背包` | owned, ArrayInventory(50) | 主库 |
| `仓库` | `_root.物品栏.仓库` | owned, ArrayInventory(1200) | |
| `战备箱` | `_root.物品栏.战备箱` | owned, ArrayInventory(400) | 可访问页数动态，容量固定 |
| `装备栏` | `_root.物品栏.装备栏` | owned, EquipmentInventory（键=装备类型） | 显式 equip/unequip |
| `药剂栏` | `_root.物品栏.药剂栏` | owned, DrugInventory(4) | |
| `快捷技能栏N` | `_root.玩家信息界面.快捷技能界面.快捷技能栏N`（N=1..12）+ 镜像 `_root.快捷技能栏N`；数据源数组 `_root.主角技能表` | **slotted, 非 ItemCollection**（无集合对象；MovieClip 将降为可选 renderer） | **不入 inventory-domain 容器注册表**，归独立 [Skill 主线](技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md)；装/卸以 `skillKey + slot + expectedRevision` 寻址。⚠ 出图载体存双 symbol 分叉，见 §4.4 |
| `快捷物品栏N` | `_root["快捷物品栏"+N]`（N=0..3）= `药剂栏` 第 N 槽已装药名镜像 | **镜像视图, 非独立容器**（权威=`药剂栏` DrugInventory(4)） | 不是新容器：是 `药剂栏` 的 HUD 投影；equip=`ItemUtil.moveItemToDrug` 写药剂栏并回填镜像，unequip 见 §4.1。⚠`快捷物品栏4`（第 5 格镜像）无数据接入、仅 SaveManager 留位（NativeHud doc §3.3） |
| `材料` | `_root.收集品栏.材料` | owned, DictCollection（堆叠） | |
| `情报` | `_root.收集品栏.情报` | owned, InformationCollection | |
| `购物车` | `_root.商城购物车` | **intent**（恢复影子，见下） | `saveCart` 写入；checkout 结算的是 Web 传入 `params.cart` 并清空此影子（§9.0） |
| `已购买` | `_root.商城已购买物品` | owned-待领 | claim 出口 |
| `合成暂存` | （尚无，CraftView 引入时定） | intent/暂存 | 待设计 |
| `资源箱`（独立 loot domain） | `LootMaterializationPlanner.materialize(target)` 按已准入箱体的严格 `row×col` 瞬态构造 `ArrayInventory`；`LootContainerService` 是唯一地图网格箱 UI 权威，Flash renderer 已退出该路径 | 原库存对象仍不入 owned 领域注册表；`LootContainerService` 铸造 session-scoped `{chestSessionId,lootContainerId,containerEpoch}` 与 slot/close lease | **仍排除于通用 inventory/Broker transfer**；由 loot domain 专用 claim/close 寻址。容量整理只操作背包↔战备箱，不把 loot container 注入 owned 协议。`1×1` 有效，当前 Web 能力边界为 `col<=8 && row*col<=64`；畸形或超界网格 fail-closed，不得恢复旧 renderer |

注：`购物车`/`已购买` 是 KShop 专属领域态，非 `_root.物品栏.*`；K点=`_root.虚拟币`。`购物车` 是**恢复影子**——会话内意图主居 Web，`saveCart` 持久化以便重开恢复；checkout **不读**此影子而结算 Web 上送的 `params.cart`（二者可能不一致，§9.0）。

> **注（快捷栏寻址语义，2026-07-15 更新）**：`快捷技能栏N` 与 `快捷物品栏N` 都不是 `_root.物品栏.*` 下的独立 owned 集合，但不能因此合并成同一 equip 协议。技能背后是 `_root.主角技能表` + 12 个 root 槽字段，使用独立 `skillEquip/skillUnequip`；药剂背后是 `DrugInventory(4)`，仍使用 `containerId=药剂栏 + targetSlot`。两者真实写入均由 AS2 完成，HUD 镜像由 AS2 投影、Web 不得直写。SaveManager 持久化口径保持：`快捷技能栏1..12`=`装备储存数据[16..27]`，药剂槽 0-3 随 `药剂栏.toObject()` 入物品存档。

### 4.4 技能/药剂 HUD 图标与全屏 UI 退役边界（2026-07-30 更新）

`技能图标` / `物品图标` 的 `attachMovie` 仍服务战斗快捷槽、药剂数量、冷却和现役动画投影；这些轻量 HUD linkage 不是旧物品栏、商店、仓库、改装或教师全屏 renderer。技能写继续只由 `SkillLoadoutService` / `SkillPanelService` 裁决；药剂除 `DrugInputService` 与 Character Build 领域服务外，仅允许保留 `DrugIcon.Press()` 这一现役 HUD 点击卸药责任。图标体不得恢复旧 1–6 槽、字符串布尔、全屏页面生命周期或分叉存档写。

- **main 不可达门**：旧 `物品与技能相关界面.swf` / `物品改装界面.swf` 不得作为 RSL/ImportAssets 接回 main；旧全屏 symbol 不得出现在 main Include、递归放置、linkage 或 PlaceObject 闭包。
- **HUD 例外是窄复用，不是 fallback**：现役玩家信息/HUD XFL 可以单独导出绘制快捷槽所需的轻量图标，并保留 `DrugIcon.Press()` 的快捷药剂点击卸药；它不拥有仓库、角色构筑、商店、配方或教师的全屏页面生命周期。
- **剩余 duplicate linkage 债务**：若主 FLA、`arts/things` 或 HUD 仍有同名轻量图标副本，后续可通过反编译/唯一 trace 逐个撤导出或改名；该清理继续走 XFL 三件套与 linkage scanner，但不再阻塞 §0.0.39 的全屏 fallback 退役，也不得成为恢复旧聚合 SWF 的理由。

---

## 5. View 契约

每个 View 向壳/Broker 声明四件事：

1. **数据需求**：订阅哪些 snapshot 键（如背包订阅 `inventory.背包`）。
2. **作为 source 的能力（UX 预检）**：能"吐出"什么。例：`BackpackView` 声明自己可作 equip/sell/enhance/move 的来源——用于灰显/提示。**注意：这是展示层投影，不是权限 SOT**；"背包-only"等真实约束由 AS2 领域服务持有并最终裁决（§1.2），Web 能力来自 snapshot 下发的 capabilities。
3. **作为 sink 的能力（UX 预检）**：能接收什么 + 落点提示。例：`EquipmentSlotView` 预检 `use` 匹配；`CartView` 只收目录来的购买意图。同样仅预检。
4. **渲染与局部交互**：自身网格/列表/页脚，以及不经 Broker 的本地动作（翻页、树筛选、tooltip）。

上述四件声明对应 §2 四层接口：第 2/3 项的 source/sink 能力即 View 的 `exportOffer(hit)`/`probeAccept(offer,hit)` 产出（不含 AS2 命令名）。**InteractionBroker 职责仅交互撮合**：拖拽/点击发起时用 Offer × Accept 做**本地预检**（不合则不发、给即时反馈），通过则生成中性 `OperationIntent`（如 `inventory.transfer`/`shop.addCartIntent`），**交由对应 DomainCoordinator 决定落 `move`/`merge`/`swap` 或 shop 命令**——Broker 自身不认识 AS2 命令名（否则退化成同时认识 `shopSaveCart/inventoryMove/equip/claim/sell/craft` 的中央 switch）。模态分层（①a/①b/②）由 Coordinator 据 OperationIntent + 当前目标态裁定。**最终合法性永远由 AS2 重判**；Broker/Coordinator 都不是权限或业务规则 SOT。Web capabilities 过期时，AS2 拒绝并回错误码，Web 重拉 snapshot。

---

## 6. GridContainerView primitive（复用基石）

一个网格容器组件，参数化三件：

- **item-model**：`owned`（真实拥有实例）/ `intent`（购买意图 `{catalogIdx, qty}`）/ `owned-待领`。
- **itemActions（逐格）**：单格出口/编辑动作。如 owned 格 = `move`/`equip`/`claim`/`discard`；intent 格（购物车）= `changeQty`/`remove`（走 `saveCart`，**非** checkout）。
- **viewCommitActions（整 View 批量）**：模态② 的整体提交。如购物车 = `checkout`、合成 = `craft`。**勿与逐格动作混为一谈**：逐格改的是缓冲内容，commit 才是带代价的整体结算。
- **sink-accept**：作为落点接受什么。

通用能力：网格渲染 + 拖拽/点击 + 筛选 + 批量动作 + 分页。

**避免 God-component（评审，2026-06-20）**：`GridContainerView` 同时担渲染 / item-model / itemActions / sink-accept / 批量 / 分页 / filter-sort / 拖拽点击，职责过载。落地至少拆三层——`GridRenderer`（DOM/虚拟化/physicalSlot/命中测试）、`ContainerViewAdapter`（item 投影 + `exportOffer`/`probeAccept` + 领域动作）、`ViewChrome`（filter/分页/页脚/commit bar）。`checkout` 等 `viewCommitActions` **不是 Grid primitive 的通用职责**，而是 `CartView` 的组合部件（commit bar 挂 ViewChrome、逻辑归 ShopCoordinator）。配套 code-review 硬门禁：`DualPaneShell` 内不得出现 `saveCart/checkout/claim`；`InteractionBroker` 内不得出现 `ShopTask/InventoryTask/callId`；`GridRenderer` 不得持 pending 表。

**浏览与整理只保留两个正交语义**（现有 `sortInventory` = `rebuildOrder` 重排槽位 + `mergeStackables` 合并堆叠，是权威写；见 [ItemSortUtil.as](../scripts/类定义/org/flashNight/arki/item/ItemSortUtil.as)，定位锚点 `public static function sortInventory(`）：

- **`filterSpec` 树筛选**：选择“看什么”，由 AS2 在完整可访问容器上权威过滤并分页，**不改真实槽位**；未筛选窗口保持 `physicalSlot` 顺序。纯 Web `displaySort` 已退役，不再制造第三种排序语义。
- **`inventorySortAndMerge`**：AS2 权威写命令（真实重排槽位 + 合并堆叠 + dirtyMark），走 §4.1 信封。Web 端“整理”按钮只触发这条闭环。**代码核查（2026-06-20）**：`ItemSortUtil` 全文不设 `dirtyMark`，新 handler 必须在成功后负责标脏并由测试断言；且 handler 须先对 `methodName` 做显式白名单校验——`validateSortMethod` 会把未知/空 method 静默回退 `byType`（其合法集是 `STRATEGY_CHAINS` 八键、非协议验收器，见 §4.1）。

每个渲染项必须始终携带 snapshot 下发的权威地址 `{containerId, physicalSlot}` 及对应 **slot lease**；筛选只改变可见窗口，不得把 DOM 序号或过滤后的可见序号写回为 physicalSlot。点击、拖拽、丢弃和落点命令均从绑定模型读取权威地址，绝不从当前网格位置反推。lease 按对象引用绑定（§4.1），整理/`setItems` 后整容器 lease 失效、须重绑。

### 纠正线（必须守住，否则留债）

**只在视图/交互层统一，数据/所有权 + 出口命令按实例保持区分。**

仓库格子里是**已拥有的真实物品实例**；购物车格子里是**购买意图**（尚未拥有）。两者长得像、交互像，但语义与出口命令不同。"购物车是仓库的变体"——**是组件复用意义上的变体，不是数据所有权意义上的变体**。绝不可把购物车内容物建模成 owned 槽位，否则把"购买意图"语义漏进所有权模型。

### 容器分类法

| 容器实例 | item-model | itemActions（逐格） | viewCommit | sink-accept |
|---|---|---|---|---|
| 背包 | owned + 主库特权 | equip/sell/enhance/move/discard | — | move / claim / 购买结果 |
| 仓库 / 战备箱 | owned（容量/分页规则） | move | — | 背包来的 owned |
| **购物车** | **intent** | changeQty/remove（→`saveCart`） | **checkout** | 目录来的"加购意图" |
| **已购买** | owned-待领 / 回执 | claim→背包（模态①b）/ 只读 | — | — |

战备箱分页只从 [InventoryPanelService.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelService.as) 的 `getAccessibleCapacity` 与快照 `pageSizeHint` 派生；旧 Flash `计算战备箱总页数` 适配器已随全屏 renderer 退役，不得在 Web/Host 复制剧情公式。

**资源箱保持独立寻址**：地图箱正常交互先由六箱 preset 做领域准入，再由 `LootMaterializationPlanner` 一次物化瞬态库存；`LootContainerService` 在独立 loot domain 铸造 session-scoped triple、失效错误与 close cleanup。已退役的 `_root.地图元件.掉落物转换为物品栏` / `_root.物品UI函数.创建资源箱图标` 不再是生产构造或 UI 锚点，也不得作为 Web 故障回退。瞬态 loot 原对象没有 owned inventory 的稳定 containerId，不进入 §4.1 通用领域注册表；视觉可复用 Grid primitive，organizer 也只通过 exact loot panel capability 操作**背包与战备箱**，不得把 loot container 伪装成 inventory source/target。

---

## 7. 交互模型：点击为主 + 拖拽加速

- **点击为主**：点选源格 → 点目标栏/格（或双击直送对侧、Ctrl 快移）。可靠、round-trip 语义干净、与既有命令模型同构。
- **拖拽加速**：pointer 式手写拖拽作为锦上添花，失败可降级回点击。

### CDP 可行性（已确认）

输入经 InputShield → CDP `Input.dispatchMouseEvent` 注入。**结论**：

- HTML5 原生拖放（`draggable`/`dragstart`/`drop`）在合成事件下**不触发**——全仓零使用，禁用路线。
- **pointer 式手写拖拽完全可用**，生产级证据：lockbox 的 `setPointerCapture`（[lockbox-panel.js](../launcher/web/modules/minigames/lockbox/lockbox-panel.js)）、jukebox 的滑块/进度条鼠标拖动模式（[jukebox.js](../launcher/web/modules/jukebox.js)）、map 的 `pointermove` 命中（[map-hotspot-hitcapture.js](../launcher/web/modules/map/map-hotspot-hitcapture.js)）。定位均直接搜索上述 API/事件名。
- 拖拽实现走 `pointerdown → setPointerCapture → pointermove(更新拖拽镜像) → pointerup → releasePointerCapture`；无 touch 事件（CDP 只模拟鼠标）。
- 注意 InputShield 在 hitRect 收缩后有 **800ms move 抑制窗口**（源码称「收起冷却」：hitRect 缩小后直接短暂禁止 CDP `mouseMoved`，防 mouseenter 重新展开振荡；见 [InputShieldForm.cs](../launcher/src/Guardian/InputShieldForm.cs)，定位锚点 `收起冷却：hitRect 缩小后短暂禁止 CDP mouseMoved`）。代码核查（2026-06-20）：**800 是硬编码字面量（无命名常量、无 ± 容差）；是全量抑制（直接 return）非限流；仅抑制 `mouseMoved`——`mousePressed/Released/wheel/leave` 不受此门控**。故开合 UI 时勿在拖拽中触发。
- **拖拽超时兜底（评审雷区）**：因冷却期抑制 move，若玩家开面板瞬间极快拖拽，前端 JS 会收到 `pointerdown`（捕获静止坐标）但**永远收不到 `pointermove`**。须给 `setPointerCapture` 加防抖/超时兜底：按下后一定时间内无 move 也无 up/cancel，强制销毁自绘镜像并 `releasePointerCapture`，避免满屏残留幽灵元素阻挡点击。

拖拽镜像**已定走 Web 自绘**（不续用 AS2 `_root.鼠标.物品图标容器`），与"AS2 UI 全面外迁"长期战略一致。

---

## 8. 迁移序列（地基优先、逐步加压；2026-06-20 按评审改判插入两道架构门）

**关键改判**：原序列把"KShop 首战"与"验证 Broker"绑在一起，但 KShop 全程未流经通用跨栏转移引擎（§0.0 决议 A）。新序列拆出 Step 0/1 的纯可靠性/基础设施改造、把 KShop 正名为「双栏壳与可靠性试车」、并把**真正的 Broker 验收推迟到 Step 4 背包↔仓库最小纵切（Gate A2）**。

| 步 | 目标 | 验证什么 | 风险 / 门 |
|---|---|---|---|
| **0（已施工）** | 在现有 KShop 上**独立**修复 ShopTask + Web 写协调 | saveCart 合并 / checkout 等待 save / claim 单飞 / `busy` vs `reconcile_required` / 断连保留 `needs_reconcile`（§9.1/§9.2） | 自动 gate 已落；仍需真机写状态/重启回读 |
| **1（已施工）** | 提取 request mux | `wb.<sessionNonce>.<openGeneration>.<seq>` 单一 mux；错通道/错 cmd/迟到回包隔离、callId 不撞号（§9.2） | 当前只消费 legacy shop channel；inventory channel 待 Step 3 接入 |
| **2（已施工 / Gate A1 已验收）** | KShop 迁入 Shell/View/Grid（§9） | 壳 + 双挂载 + PanelScale + GridContainerView + 模态②(checkout) + 目录→购物车 sink | 用户已验收；Shell/ViewHost/Grid/pointer=`gate-a1-accepted` |
| **3（已施工）** | Inventory snapshot + discard（独立 InventoryTask / `inventory_response`） | 首个真实背包写闭环 + claim 链式重投影 + backpack write gate | 自动闭环已落；真机存档回读仍待人工 |
| **4（已施工 / Gate A2 已验收）** | **背包 ↔ 仓库最小纵切** | 模态① + InteractionBroker + move/merge/swap + 左右反转 + 双端 slot lease 对账 | 用户已验收，Broker/owned transfer=`gate-a2-accepted` |
| **5a（已施工 / Gate A3 候选）** | 完整仓库：50 槽窗口分页 + 权威树筛选 + 权威 sortAndMerge | 1200 槽不全量入 DOM、physicalSlot 稳定、整容器 epoch/lease 失效、事务守恒与性能 | 自动门已通过；真机分页/排序/重启回读前不放行 5b |
| **5b-1（已完成）** | NPC 物品商店（待购/待售选择、二级结算、买入按真实类型落点） | 金钱副作用、背包/材料 lease、情报只读、背包/收集品复合 View 与原子 trade token | 数据、自动门、克隆槽原子提交与 SOL 重启回读均已通过 |
| **5b-2A（Web-only，退役 cut 已人验）** | 装备调制：强化/转换/进阶/插件改装；七类 operation 只走 Web + EquipmentTuningService | 背包/已穿戴来源、opaque plan/token、单/双槽原子 touch、材料/成就/dirty 收口；旧 renderer adapter 与 Host legacy source 专项已删除 | Engineering Complete；当前 cut 的五类 GUI 人工验收、cloud quorum 与 promotion 已通过，失败只可见 fail-closed，不恢复旧入口；总体标准入口 smoke 只覆盖 Tuning opener/snapshot，不代证七类写操作 |
| **5b-2B（B0–B4 + 双向 Skills；上一 source 已 `standard_entry_verified`，退役 cut 已人验）** | 角色构筑工作台：纸娃娃 + 11 装备槽 + 4 药剂槽 + 筛选候选 + 已穿戴物品调制 + 全宽个人信息 | `CharacterBuildSession`、loadout authority、edit/stats 互斥态、批次活体刷新、已装备来源适配、共享写仲裁和自身 Skills 双向一次性交接 | 上一冻结 source 已完成原子写、finalize、安全退出、重启读回、双向 GUI、promotion 与正式入口；2026-07-30 已删除旧 Hub/writer/fallback 并收口 main 资产闭包，当前新 cut 的 fresh 自动门、GUI 人工验收、独立 cloud quorum、promotion 与 Tuning-only 标准入口 smoke 已完成，但不得外推为 Character 专项标准入口 |
| **6（旧 Hub 责任已退役）** | 其余物品领域只做 Web 能力与共享壳的后续演进；合成纵切按 §0.0.33 当前状态继续收口 | 旧物品/装备 Hub 已无生产责任且不得恢复；后续仅评估模态②抽取到壳级托盘的时机 | 不得回接旧 UI |
| **Skill 独立主线** | `panel=skills`：查看 / 教师学习 / 12 槽装备 | 复用视觉 primitive，不进入 inventory domain；详见 [独立规划](技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md) | 独立 Gate S0–S6，不受 5b-2/Step 6 排期阻塞 |

### 8.1 Step 5b-2B 角色构筑工作台冻结形态

> 2026-07-27 的跨 AS2 / Host / Web 取证、药剂四槽纳入裁决、关闭 flush barrier、已穿戴调制 adapter、交互状态机、Kimi k3 异构审计与 B0–B4 施工门，统一见 [角色构筑 → Web 双栏工作台：工程落地规划](角色构筑-Web双栏工作台-工程落地规划-2026-07-26.md)。本节继续承担迁移总序与产品冻结摘要；较早 B4 隔离候选只冻结写入/读回历史，本轮 `c4faf14460` 的完整 `view=build` / `domain=loadout`、默认 `EQUIP_UI`、自身 Skills 双向导航与普通关闭负链已完成 promotion 和无参正式入口，严格状态为 `standard_entry_verified`，见[正式关闭证据](evidence/character-build-skills-bidirectional-navigation-closure-2026-07-28.md)。

**产品形态**：1024×576 的编辑态继续使用双栏，不扩成三个常驻信息面。左右固定 `55:45`，右栏不得窄于 `360px`；55% 是仍可维持三列完整候选和八列紧凑图标的左倾上限，不继续压到 `56:44`。左侧为单 Canvas 纸娃娃、6 护具槽、5 武装槽和底部 4 药剂槽；内层由纸娃娃弹性占满余量，槽区按约 `204px` 内容宽收缩并贴右，不能把槽区右侧留白当作固定列宽，1024 下 Canvas 至少保持 `300px` 且四药剂同排。已占用的 11+4 槽以真实动态图标/分层图为主要识别面，保留槽名、数量/强化/升阶/插件角标与完整 ARIA；颈部零 dressup projection 使用静态物品图标，缺图显式 fallback，不退回纯文字装备砖。右侧按所选上下文切换“可装备候选 / 已装备物品调制”。候选由槽位、职业、等级与领域规则筛选，不显示完整背包。角色装备、嵌入式调制与个人信息使用主角 DLS 诊断风格，普通物品筛选仍保持 inventory 中性。详细属性冻结为覆盖整个 workbench body 的全宽 SecondaryPage，避免把 40+ 个展示字段/派生写入挤进右栏并留下不可操作的左栏。

**首屏交互冻结**：左侧“外观与配置 / 当前构筑”的 PaneChrome 标题行同时放当前浏览提示与放大预览入口；固定分组与全部可见槽已表达 11+4 结构，不再重复显示 `15 / 15` 或“护具/武装/药剂”的分组计数，routine 浏览提示也不占 pane 底部空间。右侧候选动作与“背包候选 / 候选对比”同处标题行，删除通用“详情”和 pin，装备上下文最多三项“装备 / 调制 / 卸下”，药剂提交显示“装入”。独立动作行与候选底部重复 rail 均不恢复。一个持久化完整/紧凑控件、一个 `GridDensityController` 和一个 `cf7.itemgrid.mode.workbench` 在 storage、构筑候选与 embedded tuning 标题间迁移；构筑态只改变候选网格，调制态只改变材料/配件/转换候选等浏览型卡片，固定 11+4 槽、operation tabs 与主提交语义不压缩，切换零业务流量并保持选择、焦点和预览。调制保留“调制说明”作为 operation/candidate 的解释层，但不提供 pin/pressed/persisted subject；说明打开时随 operation 与当前 focus 更新。

**个人信息冻结**：首屏延续旧重量—速度线，以 AS2 已投影的比例、阈值、状态和速度绘制负重带；生存/战斗 KPI 后接武器威力 `log10(value+1)` 相对量级与八抗性线性组内相对量级。完整 exact 9 组 47 行继续以 3×3 展示，抗性摘要 4×2、明细 2×4；八图标只作装饰，字段文字与原始权威值始终保留。不得复制速度/战斗公式、预测候选或把相对条称为百分比。

**放大预览冻结**：SecondaryPage 仍迁移同一个 exact stage/Canvas，不复制 renderer 或 current/preview state；`workbench-inspection-viewport.js` 只为放大态提供瞬时 wheel/`+/-` 缩放、主键拖拽/方向键平移和“全貌”复位，transform 只作用 Canvas。关闭时相机复位，嵌入态不得吞 pointer/wheel/键盘输入。Esc 每次只消费最上层，顺序为纸娃娃放大页 → 调制检视 modal → 调制说明 → 未提交候选 → tuning/stats/storage → 面板关闭，并恢复对应 opener。

**短期刷新状态机**：

1. `edit` 中单击/键盘首次确认只更新 Web 纸娃娃与物品自身差值，hover 首版只显示 tooltip；未确认预览不写 AS2，也不触发活体刷新。
2. 确认装备 equip/unequip 或已穿戴 tuning 后由各自 AS2 领域服务原子提交，推进 `loadoutRevision` 或等价装备变更版本并置 `liveRefreshDirty`；药剂 equip/unequip 只推进 `drugRevision` 与全局存档 dirty。Panel 已持统一 pause lease，装备活体可暂时落后于容器 authority，药剂变化不触发该链。
3. `edit → stats` 先排空在途写与 reconcile；仅 `liveRefreshDirty=true` 时执行一次 `_root.刷新人物装扮`，成功后令 `liveRevision=loadoutRevision`，再从 `PlayerInfoProvider` 现有 getters 抽取结构化冻结快照。`stats` 中禁止换装/调制，返回 `edit` 不刷新。
4. close/disconnect 复用同一 finalize barrier：`liveRefreshDirty` 只控制人物刷新，全局存档 dirty 单独控制 `flushNow`；两阶段都成功或 clean no-op 后才释放 `webPanelUnpause`。失败必须保持暂停并进入可恢复的 reconcile/重载路径，不能让容器已提交而活体仍陈旧时静默继续游戏。无装备变更重复切页、从已结算 stats 退出均不得刷新人物。后续可把 live dirty reason 拆为外观、属性和武器运行态，但首版只需要正确的一次性全刷新门。

**领域边界**：Web/Host 的逻辑 `CharacterBuildSession` 只持会话代次、选中槽、预览与对账状态；AS2 `CharacterBuildService` 权威持有 `loadoutRevision/liveRevision/liveRefreshDirty`，以保证 Host 断线后仍能结算。装备和药剂的 equip/unequip 归 `domain=loadout`，调制仍归 `domain=equipment_tuning`，属性快照/预测归只读 projection，纸娃娃只消费装备与外观快照。Web 算出的禁用态或属性值都不是提交授权。已穿戴装备的调制通过装备容器 adapter 复用 `EquipmentTuningService`；首轮放行强化、进阶、插件安装/拆卸等单件原地操作，强化度转换等跨容器双装备操作在双 lease/回滚语义单独验证前继续拒绝。

**切片结果**：B0 的 SaveManager、装备事务、live flush、exact-instance close 与键盘 focus spike，B1 的只读纸娃娃、11+4 槽与全宽个人信息快照，B2A 的候选/standalone tuning 筛选，B2B 的装备与药剂原子装卸，B3 的已装备单件调制，以及 B4 的入口/退出代码、自动门和真实候选闭环均已施工。`EQUIP_UI` 已切到 Web build；较早 B4 候选的真机批次刷新、写后重启回读、SAFEEXIT 与真实 WebView2 合成证据曾停在 `e2e_verified / NOT_DEPLOYED`，相关实现字节后来进入正式 release，`c4faf…` 双向 GUI 与 `9118…` 只读 Tuning smoke 仍各自只作历史闭包，不能拼成同一身份验收。2026-07-30 新 cut 已退役旧 Hub 责任，已按 §0.0.39 完成 fresh 静态、Flash 与 GUI 人工验收；新的云端共识/promotion、部署与标准入口只读 snapshot 复验已完成，严格状态为 `standard_entry_verified`；Character 写入/读回仍沿用其独立历史证据，不由本次 smoke 代证。长期 TS 同源属性核是输入契约稳定后的独立项目，不得作为本轮前置。

**Gate A2 薄片范围**（不必立刻做完整仓库 UI，只做足以证伪抽象的薄片）：背包 10~50 槽 / 仓库只开当前一页 / 只 whole-slot / 支持 move·merge·swap·左右反转 / 不做排序·搜索·乐观更新·复杂分页 / 失败只刷新当前可见窗口。**必须覆盖**：① source 与 target 都陈旧；② 空目标变 occupied；③ merge 目标数量变化；④ AS2 领域规则拒绝；⑤ 事务事件重入；⑥ commit 失败无部分写；⑦ 左右反转仍调用同一 operation；⑧ Shell/Broker 中**不存在** `BackpackView`×`WarehouseView` 的配对分支（否则就是伪 Broker）。

**Gate A3 完整仓库范围**：只把 Step 5 的容器规模风险推进到可止损节点——50 槽窗口覆盖完整 1200 容量、前后页按需 snapshot、树筛选保留真实 physicalSlot、`sortAndMerge` 严格策略白名单、整仓 epoch/lease 失效、unknown-item mass 守恒、commit/reentry/dirtyMark 与当前页回包一致；不夹带 NPC 金钱、sell/buy、装备槽或 stats。

**为何这个顺序**：避免一次 PR 同时改 UI 架构 + Shop 并发 + Task 路由 + inventory 协议，导致"能跑了，但不知道是哪层设计正确"。每步可独立验收、可独立回退。

**弱机性能门**（贯穿，详见 §9.2 验证门槛）：canonical 热路径已废止；用 `getTimer()` 测 50 槽 lease snapshot 的耗时/回包字符量，覆盖 50/400/1200 槽 × 0%/50%/100% 占用 × 连续 100 次，并单测 1200/1200 整仓 sortAndMerge。TestLoader 数字只是自动基线，最终仍要在 release/min-spec 真机复核。

---

## 9. 首战：KShop 加重版

本节按机制描述 KShop 加重版，施工边界以 §8 为准：Step 0–4 已完成并验收，Step 5a 完整仓库已到 Gate A3 candidate，Step 5b-1 NPC 商店已完成原子结算；装备调制 5b-2A 已到反馈预览，角色构筑 5b-2B 与玩家自身 Skills 双向入口在 `c4faf14460…` 历史 release 已达到 `standard_entry_verified`，合成与技能其余能力的后续状态分别见 §0.0.33 / §0.0.34。当前实现与分身份证据统一见 §0 的施工记录。

### 9.0 KShop 真实数据生命周期（当前直接交付 + legacy 确权）

KShop 当前权威生命周期是“目录 + 购物车恢复影子 + 权威结算 plan + 实际物品容器直接交付”；`商城已购买物品` 只保留 legacy 存档兼容。固定画布仍为 1024×576，AS2 权威生命周期见 [商城系统_WebView.as](../scripts/逻辑系统分区/商城系统_WebView.as)：

| 阶段 | gameCommand | 改什么 | 注意 |
|---|---|---|---|
| 加载 | `shopBulkQuery` | 只读：目录 + `_root.商城购物车` + `_root.商城已购买物品` + K点(`_root.虚拟币`) + **玩家等级 `_root.等级` + 逆向被动等级 `_root.主角被动技能.逆向`** | 购物车有**服务端恢复影子**（供重开恢复），会话内意图主居 Web |
| 存购物车 | `shopSaveCart` | 写：用 `params.cart` 覆写恢复影子 `_root.商城购物车` | |
| 权威预览 | `shopCheckoutPreview` | 读：校验 cart、目录、等级、价格、K 点；`ItemUtil.require` 预检整单容量；铸单次 `checkoutToken` | 不信任 Web 价格/名称；逐行返回金钱/容量上界；每次新预览替换旧 plan |
| 原子提交 | `shopCheckoutCommit` | 写：消费 token → 重解并复核 → `ItemUtil.acquire` 整单直接交付 → 扣 K 点 → 清空影子 → `强制存盘` | 容量/余额不足零写；`balance == total` 合法；成功后链式 inventory snapshot，未知结果不重放 |
| 兼容结账 | `shopCheckout` | 写：校验 `balance >= total`，经 `finalizeCheckout` 直接交付、扣 K 点、清购物车并存盘 | 不新增 `商城已购买物品`；仅作不可从生产 UI 路由到达的协议兼容，旧 Flash/旧 Host 入口不得恢复 |
| 历史领取 | `shopClaim` | 写：按 `params.purchasedIdx` 从 `商城已购买物品`→背包 + 强制存盘 | 只处理旧存档队列；`purchasedIdx + purchasedToken` 单飞，新购买不再进入 |

上表以 [商城系统_WebView.as](../scripts/逻辑系统分区/商城系统_WebView.as) 中对应 `_root.gameCommands["shopBulkQuery"]` / `shopSaveCart` / `shopCheckoutPreview` / `shopCheckoutCommit` / `shopCheckout` / `shopClaim` 为定位锚点。后文保留的“首战不改生命周期”“checkout 后新增待领取”等叙述是 Step 0–3 的历史施工决策，当前实现以本表与 §0.0.24 为准。

`acquire_failed` 可按“明确未写入”处理的当前依据是 [ItemUtil.as](../scripts/类定义/org/flashNight/arki/item/ItemUtil.as) 的 `public static function acquire(itemArray:Array):Boolean`：先以 `require` 完整预检，失败直接返回，之后才进入 mutate。该顺序是 claim 错误语义的回归契约，重构时必须由测试守住。**2026-06-20 对抗复核坐实**：`require` 内 `getVacancies` 容量门不足即 `return null`，`acquire` 见 `list==null` 在任何 `_root` mutate（首个写 `_root.金钱+=`）之前 `return false`，`singleAcquire` 只是 `acquire([{name,value}])` 薄包装——故 `acquire_failed`/`inventory_full`/`destination_full` 路径**原子无副作用**。外部评审"`singleAcquire` 可能部分写入、`acquire_failed` 需 `needs_reconcile` 强制对账"的质疑被代码**证伪**：KShop claim 收到这两个码后回 idle/释放锁是安全的，`needs_reconcile` 只留给 timeout/断连/畸形回包等真正结果不确定的情形（§9.1）。（残余风险仅 AS2 无 try/catch：若提交段运行时异常会停在中途且无回滚，但那属"运行时异常"而非"逻辑部分写入"，且容量预检已消除最常见溢出型中断。）

当前 `shopClaim` 在调用 `singleAcquire` 前先做统一 `getFirstVacancy()==-1` 物理空格悲观检查，因此即使物品理论上可合并到现有堆叠，满背包也会先回 `inventory_full`（拒绝时未调 `singleAcquire`、无副作用，属安全的过度拒绝）。首战按现状保留；若要放宽（满包仍允许领取可合并的可堆叠物品），须把前置检查从 `getFirstVacancy` 改为基于 `singleRequire` 的可行性预检，并作为**独立游戏行为变更评审**，不能在 UI 迁移中顺手改掉。

**checkout 边角（迁移须连带审视）**：`total` 仅累加通过 idx/qty 校验的条目，空购物车 `total=0` 时严格 `虚拟币>0` 对任何持正 K 点玩家为真，会走成功分支——扣 0 K 点、push 0 条已购、清空购物车、`record 商城结账次数+1` 并强制存盘。无 K 点经济偏差，但"空结账"会让成就计数 +1 + 触发一次存盘，前端改判定时勿误触发。

由此修正先前文档错误：① 购物车并非"纯 Web 本地"也非"纯服务端持久"——它是 **会话内 Web 主态 + saveCart 恢复影子**，且当前 preview/commit 结算的是 Web 上送意图而非把恢复影子当唯一真相；② **当前 checkout commit 与兼容 `shopCheckout` 都通过 `finalizeCheckout` 直接改变实际物品容器，新购买不再等待 claim**；③ "批量领取"只属于旧存档既有的**历史待领取 View**，购物车（intent）的逐格动作是增删/改量，不是领取；④ 历史 claim 回包不含背包，处理旧队列后仍需独立 inventory snapshot（§9.1/§9.2）。

**历史说明**：首战曾明确不改“请求 cart → 已购买 → claim”生命周期；该止损边界已由 §0.0.24 的独立协议迁移取代。购物车仍不是 AS2 持久态单一真相：preview 读取 Web 当前 cart，`saveCart` 只保存恢复影子；commit 只认 AS2 缓存的 opaque plan。

### 9.1 加重版叠加（保留既有 shop* 请求/回包形状）

> 2026-07-12 更新：本节关于 Shell、购物车恢复影子、inventory gate 与 legacy claim 的约束继续有效；其中常规 `checkout` 已替换为 `checkoutPreview/checkoutCommit`。写协调现在串行化 `saveCart/checkoutCommit/legacy claim`，直接交付 commit 的 owner 与 claim 一样延长到 inventory snapshot 完成；新购买不再改变 purchased list/token。

- **槽位**：完整目标为左∈{目录, 购物车} / 右∈{背包, 购物车}，视图单例不变量产三配置（§11）；默认进面板态 = (目录, 购物车)。Step 2 当前生产配置只挂该默认态，背包与其余两配置待 Step 3；共享 Shell 的冲突/回落算法已由 mock View 验证，不用占位背包污染生产 UI。
- **视图**：CatalogView=现有目录逻辑包成 View；CartView=GridContainerView(model=intent, itemActions=changeQty/remove→`saveCart`, viewCommit=`checkout`)；**已购买是独立容器**（model=owned-待领, itemActions=claim），v1 UI 可沿用现有 sidebar 把 购物车/已购买 两区并置于同一槽（CartView 复合），也可拆独立可换视图——布局自由，但**数据与 egress 必须分开**。
- **目录→购物车** = sink 接受"加购意图"（可拖可点，**理想的低风险拖拽沙盒**：错了零成本、不碰真实库存）→ 走 `shopSaveCart` 写恢复影子。
- **KShop 写协调（不可继续沿用各按钮各自 pending）**：`saveCart` 会覆写恢复影子，`checkout` 会扣 K 点、追加已购买并清空影子，`claim` 会删除已购买并入背包；三者必须共享一个 ShopTask/Web 写协调器。Web 对高频 `saveCart` 只保留“单个在途 + 一个最新期望 cart”的合并槽，不逐次排队；`checkout` 发出前必须等待 save pipeline 空闲并阻止新 save，避免旧 saveCart 在 checkout 清空影子后迟到回写。`checkout/claim` 均为非幂等写，只允许单飞且不自动重发。正常关闭也必须进入协调器：停止新编辑，等待当前 shop 写终止或完成对账，再只 flush 一次最新 cart 后发送 close；不得像当前 `requestClose()` 那样在已有写 pending 时并发补发 saveCart。强制关闭可跳过等待，但重开必须先对账。Host 在任一 shop 写 pending 时拒绝第二个 shop 写为 `busy`。
- **checkout 不确定结果对账（Step 0 已落）**：Web 写协调器已取代单一 `_checkingOut` 布尔。已下发 checkout 遇 timeout/途中断连/未知或畸形回包时，结账与领取入口保持禁用，先成功 `shopBulkQuery`，以返回的 K 点、购物车影子与已购买列表重投影后才允许新意图，绝不重发原 checkout。`insufficient_kpoints` 是明确未写入失败，可直接结束；`busy` 是发送前拒绝，不得释放另一 owner 或触发对账。
- **claim 单飞 + 背包重投影（已落地）**：共享 ShopTask/Web claim single-flight 继续串行化 claim/checkout/saveCart；claim owner 现延长到链式 `domain=inventory cmd=snapshot` 完成。AS2 `shopBulkQuery` 铸造 snapshot-local `purchasedToken`，claim 必须回显 `expectedPurchasedToken`；checkout/claim 改表即换 token，重新 bulkQuery 也换 token，旧 index+token 回 `stale_state`。该迁移只在现有 shop wire 上新增 token 字段，没有把 inventory payload 信封回填给 shop*。
- **claim 结果与刷新失败分开处理**：claim 成功后即视为不可重试的已提交写，再链式补发 inventory snapshot。若 snapshot 失败，提示“领取已成功但背包刷新失败”，只允许重试 snapshot，不得重发 claim；若**已下发 AS2** 的 claim timeout/途中断连或收到未知/畸形结果，保持领取入口锁定，先用 `shopBulkQuery` + inventory snapshot 对账，完成后才允许下一次 claim。发送前即因未连接被拒绝则没有写入歧义，可直接失败。`inventory_full`/`destination_full`/`acquire_failed` 是当前代码的明确未写入失败，可不发 snapshot 并释放锁；`item_not_found` 也未写入，但表明已购买列表陈旧，须先成功 `shopBulkQuery` 再启用领取。面板初次打开/重开时，领取入口也必须等 `shopBulkQuery + inventory snapshot` 均成功后才启用，避免关闭面板丢失 Web 内部 refresh 状态。
- **背包满时领取的功能边界**：claim 需背包空位，满时**拦截+明确提示**，不静默失败（沿用 AS2 现有 `getFirstVacancy()==-1` 判定）。(购物车/已购买, 背包) 同屏即为此场景。v1 腾格子工具箱仅限背包自身 egress：至少提供经明确二次确认的 `inventoryDiscard`；卖出/挪仓库需对应目标/视图存在，留到 NPC 商店、仓库步骤。现役丢弃只走 [InventoryPanelService.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelService.as) 的 `inventoryDiscard` 领域命令；旧 Flash 图标 hitTest 路径已删除，不得恢复。
- **v1 背包写串行化**：工作台层维护一个 backpack write gate；claim 从发出到链式 inventory snapshot/对账终止、discard 从发出到其刷新 snapshot 终止，均独占该 gate。其他背包写直接显示 busy、**不排队**；与当前 owner 无关的背包 snapshot 请求延后/合并到 gate 释放后，只有 owner 的刷新/对账 snapshot 可结束本轮写。关闭/强制关闭不能视为取消已下发写；若关闭时 gate 有 owner，清理 Web pending 后只忽略迟到回包，不重发、不推断结果，重开时所有背包写入口继续禁用，直到新的 inventory snapshot（claim 另加 bulkQuery）完成重投影。这样既避免陈旧意图延迟执行，也避免跨 ShopTask/InventoryTask 的回包倒序使 Web 投影回退。该 gate 只是首战投影顺序约束，不能替代 AS2 slot lease 与最终裁决；出现第二个可同时写背包的 Web 面板时，应提升为共享 inventory-domain 协调器或引入 AS2 铸造的 snapshot epoch，禁止复制互不知情的 panel-local gate。
- 拖拽镜像 Web 自绘（§7）。

Web 侧先以 shop 写协调器串行化 `saveCart/checkout/claim`；其中 claim 子状态机固定为：`idle → claim_pending → inventory_refresh_pending → idle`；`inventory_full/destination_full/acquire_failed` 直接回 `idle`；`item_not_found` 进入 `shop_refresh_required`，成功 bulkQuery 后回 `idle`；claim timeout/途中断连/未知结果进入 `reconcile_required`，仅 `shopBulkQuery + inventory snapshot` 均成功后回 `idle`；claim 已成功但 snapshot 失败进入 `refresh_required`，仅重试 inventory snapshot，禁止重发 claim。Host 只维护请求级 shop 写 gate 与 `needs_reconcile`，不负责 inventory 刷新状态。

定位诚实声明：KShop 加重版是**壳 + GridContainerView + 模态② + claim 重投影 + discard 单向领域写**的试车；模态①双向 Broker（owned↔owned）的完整证明在 §8 Step 4（背包↔仓库 Gate A2）。

### 9.2 协议闭环表（七列，遵循护栏 §2）

**闭环表**遵循护栏 §2（迁移闭环表）；**C#/AS2/Web 接入清单**遵循护栏 [§3/§4/§5](../agentsDoc/as2-web-panel-migration.md)（护栏 §2 是闭环表、非接入清单，勿混）。商城现有业务请求只带 `type/cmd/callId/...`，**不带 `panel=kshop`**；仅 close 请求带 `panel=kshop`。Host 以全局 cmd 分支把下列业务命令交给 `ShopTask`，其再机械派生 `action="shop"+Cap(cmd)`，回包 task=`shop_response`：

| Web cmd | C# action | AS2 handler | AS2 resp task | C# panel_resp | JS handler | 写状态 |
|---|---|---|---|---|---|---|
| `bulkQuery` | `shopBulkQuery` | `shopBulkQuery` | `shop_response` | `panel_resp` + `callId`（ShopTask 仅剥 `task`、置 `type/callId`，**不盖 panel/cmd**） | `KShopRequestMux` pending（channel=`shop`, cmd=`bulkQuery`） | 读 |
| `saveCart` | `shopSaveCart` | `shopSaveCart` | `shop_response` | 同上（仅 callId） | 同上 | 写(购物车) |
| `checkoutPreview` | `shopCheckoutPreview` | `buildCheckoutPreview` | `shop_response` | 同上（仅 callId） | 二级结算 callback | 读(权威计划 + 单次 token) |
| `checkoutCommit` | `shopCheckoutCommit` | token 复核 + `ItemUtil.acquire` | `shop_response` | 同上（仅 callId） | `KShopWriteCoordinator` + inventory 刷新 | 写(K点+实际物品容器+cart+存盘) |
| `checkout`（compat） | `shopCheckout` | `balance >= total` + `finalizeCheckout` | `shop_response` | 同上（仅 callId） | 生产 UI 不路由 | 写(K点+实际物品容器+cart+存盘；不新增待领取) |
| `claim`（legacy） | `shopClaim` | `shopClaim` | `shop_response` | 同上（仅 callId） | 历史待领取 callback | 写(已购买→背包+存盘) |

Web callback 必须区分 `busy` 与 `reconcile_required`：`busy` 表示已有 shop 写在途，不得据此重发，也不得让被拒的重复请求释放当前 owner 锁；`reconcile_required` 表示 Host 持有不确定 shop 写结果，必须先 `shopBulkQuery`。`checkoutCommit` 的 `insufficient_kpoints/inventory_full/stale_state` 是 token 已消费但明确零写入的终态，必须重新 preview，不能复用 token；未知结果才进入对账。legacy claim 仍区分 `item_not_found/inventory_full/destination_full/acquire_failed/stale_state`。

**共享背包单独立 domain/Task，勿绑进 shop_response**（背包是后续所有面板共享的领域；若复用 `shop_response` 会把共享容器永久绑死在商城）。新增：

| Web cmd | C# action | AS2 handler | AS2 resp task | C# panel_resp | JS handler | 写状态 |
|---|---|---|---|---|---|---|
| `snapshot`(domain=inventory) | `inventorySnapshot` | `InventoryPanelService.execute("snapshot")` | **`inventory_response`** | `panel_resp domain=inventory cmd=snapshot` | `InventoryCoordinator` range callback | 读 |
| `tooltip`(domain=inventory) | `inventoryTooltip` | `InventoryPanelService.execute("tooltip")` | **`inventory_response`** | `panel_resp domain=inventory cmd=tooltip` | owned slot hover callback / `PanelTooltip` | 读（lease-bound 实例注释） |
| `discard`(domain=inventory) | `inventoryDiscard` | `InventoryPanelService.execute("discard")` | **`inventory_response`** | `panel_resp domain=inventory cmd=discard` | `InventoryCoordinator.discard` + 背包刷新 | 写(背包删除+dirtyMark) |
| `autoTransfer`(domain=inventory) | `inventoryAutoTransfer` | `InventoryPanelService.execute("autoTransfer")` | **`inventory_response`** | `panel_resp domain=inventory cmd=autoTransfer` | `InventoryCoordinator.autoTransfer` | 写(owned 容器 merge-first→首空位+dirtyMark) |
| `move`(domain=inventory) | `inventoryMove` | `InventoryPanelService.execute("move")` | **`inventory_response`** | `panel_resp domain=inventory cmd=move` | `InventoryCoordinator.transfer` | 写(whole-slot move+dirtyMark) |
| `merge`(domain=inventory) | `inventoryMerge` | `InventoryPanelService.execute("merge")` | **`inventory_response`** | `panel_resp domain=inventory cmd=merge` | `InventoryCoordinator.transfer` | 写(whole-stack merge+dirtyMark) |
| `swap`(domain=inventory) | `inventorySwap` | `InventoryPanelService.execute("swap")` | **`inventory_response`** | `panel_resp domain=inventory cmd=swap` | `InventoryCoordinator.transfer` | 写(whole-slot swap+dirtyMark) |
| `sortAndMerge`(domain=inventory) | `inventorySortAndMerge` | `InventoryPanelService.execute("sortAndMerge")` | **`inventory_response`** | `panel_resp domain=inventory cmd=sortAndMerge` | `InventoryCoordinator.sortAndMerge` | 写(仓库整容器重排/合并+dirtyMark) |

**loot organizer 的 inventory 子协议**：普通工作台仍按 profile 和通用命令白名单运行；只有 active `panel=loot` 的内嵌整理子页追加 exact capability 门。Host 要求顶层严格七键、exact 当前 `panelInstanceId`，且仅允许：①两个 all-filter 窗口的 `snapshot`（背包 + 战备箱各一）；②背包↔战备箱、固定 `mergeThenEmpty`、携同一双窗口集的 `autoTransfer`；③source 必须来自背包的 `discard`。任何顶层附加字段、跨向仓库/瞬态 loot 的 transfer、战备箱 discard 或 instance 漂移都在进入 `InventoryTask` 前 fail-closed。AS2 仍只执行通用 `InventoryPanelService` 权威校验，不接收 panel capability，也不能借 AS2→Host 回包注入 Web 请求。

**saveCart/checkout 闭环**：购物车编辑只更新 Web 期望态并触发“单个在途 + 最新值合并槽”的 save pipeline；checkout 必须等该 pipeline 空闲后才发送，并在终止前冻结新的 save。saveCart 结果不确定时先 bulkQuery；若返回影子与当前 Web 最新期望 cart 不同，对账完成后发送一次**新的最新值同步**，不得重放旧请求。checkout success 用回包中的 `newBalance/purchased` 清空 Web cart；`insufficient_kpoints` 是明确未写入终态；已下发 checkout 的 timeout/途中断连/未知结果只允许 `shopBulkQuery` 对账，禁止重发。正常 close 等待该闭环空闲后才做最后一次 saveCart；force_close 不补发。测试必须覆盖 saveCart 在途时点击 checkout/关闭、连续编辑只落最终影子、saveCart timeout 后对账并仅同步最新值、旧 saveCart 不得在 checkout 后回填影子、checkout 快速双击/绕过 UI 第二次发送、timeout 后仅 bulkQuery 对账、force_close 后重开对账。

**claim→背包刷新的链式闭环**（方案 A）：`claim`(shop_response, success) → Web 回调内发 `snapshot`(domain=inventory) → `inventory_response` → 背包视图刷新。两条独立通道串接，AS2 claim 回包不变；Host shop 写 gate 防在途重入，Web 全局锁覆盖整条链。claim timeout/途中断连/未知结果后 Host 将 shop 写 gate 置为 `needs_reconcile`，后续 shop 写一律拒绝，直到成功完成 `shopBulkQuery` 对账；Web 仍须再完成 inventory snapshot 才解锁 UI。测试必须覆盖快速点击两条已购买记录、绕过 UI 直接发第二个 claim、三条显式失败均不修改背包/已购买、claim 成功但 snapshot 失败、claim timeout 后对账。

该 gate 已由 [ShopTask.cs](../launcher/src/Tasks/ShopTask.cs) 落地：pending 记录 WebCmd，写状态为 `idle/write_pending/needs_reconcile`，其中 `write_pending` 记录 `saveCart/checkout/claim` owner。`write_pending` 拒绝后续 shop 写时回 `busy`；`needs_reconcile` 拒绝 shop 写时回 `reconcile_required`，但允许新的 bulkQuery 用于对账。saveCart 收到 success、checkout 收到 success 或 `insufficient_kpoints`、claim 收到 success 或明确未写入的 `item_not_found/inventory_full/destination_full/acquire_failed/stale_state` 才回 idle；未知/畸形回包、已下发 shop 写的 timeout/途中断连都先转 `needs_reconcile`。重开时 KShop 现在并行等待 shop bulkQuery 与 inventory snapshot，领取入口在两者完成前保持禁用。

**discard 闭环**：Web 在用户明确确认（展示物品名与数量；确认投影 = `{itemKind,displayName,quantity,enhancementLevel,rarity}` 版本化子集，§4.1）后发送 `domain=inventory cmd=discard`，payload 携背包 source slot lease；C# 白名单映射 `inventoryDiscard`；AS2 v1 仅允许 `containerId=背包`，重新校验 source slot lease（`currentItem === lease.occupantRef`）、槽位存在性与领域丢弃策略后删除**整个槽/整组堆叠**、设置 `dirtyMark`，并在同一 `inventory_response` 回包中带刷新后的背包 snapshot。v1 不接受 partial count；以后若要部分丢弃，单列协议版本。删除失败不得局部更新 Web；`stale_state` 后只能刷新并要求用户重新确认。发送前拒绝可直接失败；已下发 discard 的 timeout/途中断连/未知回包一律视为结果不确定，只允许拉 inventory snapshot 对账，**禁止自动重发破坏性写**。测试覆盖取消确认、陈旧 source、空槽/非法槽、非背包容器、策略拒绝、夹带 count、成功删除并标脏、成功回包刷新、timeout 后仅对账。

**sortAndMerge 闭环**：Web 显式确认后发送 `{v:1,container:{containerId:"仓库",offset,limit},methodName}`；C# 只重建 container 三字段与八种 method 白名单；AS2 只允许仓库，在隔离副本中计划排序/合并并做 mass 守恒，随后单次 `transactionReplaceAll`。成功使仓库 lease 全失效、`containerEpoch+1`、标脏并返回原当前页；未知策略、lossy plan、commit failure 或 timeout 都不自动重发，失败后只 snapshot 对账。

**Web 回包复用边界（Step 1/3 已落）**：[bridge.js](../launcher/web/modules/bridge.js) 仍广播同一 `type`，KShop 只由 [kshop-runtime.js](../launcher/web/modules/kshop-runtime.js) 的单一 `KShopRequestMux` 消费 shop + inventory 两个 channel。callId 固定 `wb.<sessionNonce>.<openGeneration>.<seq>`；inventory 回包必须匹配 `domain=inventory` 与原 cmd，legacy shop 回包必须无 inventory domain。只有形状通过才消费 pending；close/force_close 清 pending，迟到回包不得落新 session；Grid/View 不建第二张 pending 表。

**本地拒绝也必须可路由**：InventoryTask 自身产生的 `unsupported_cmd/unsupported_version/invalid_payload/timeout` 以及**发送前** `disconnected` 均须回 `panel_resp + domain=inventory + 原 cmd + callId`；请求若带受支持的 Web panel binding，还必须在成功、本地错误、timeout 与发送失败中原样回显 `panel/panelInstanceId`。Host 对未知 domain 的拒绝须回显原 `domain/cmd/callId`，不得让 mux 只能等超时。已下发请求后的途中断连不是普通本地拒绝，按上文强制关闭 + 重开对账处理。工作台 callId 必须匹配 `^[A-Za-z0-9._-]{1,96}$`。Step 0 已令 ShopTask 以同一锁维护 pending/timer/写 owner/活动与 256 项近期 callId：活动重复静默忽略，完成/拒绝/timeout/断连后的重放不再次下发，`ClearPending()` 不清近期历史；本地错误均用 `JObject` 构造。InventoryTask 落地时机械复用这些约束。即使有重放缓存，新 callId 重发旧 `purchasedIdx` 仍无法被 Host 识别，完整防线是已冻结的 `expectedPurchasedToken`。

**路由顺序（必修，否则落错 Task）**：代码核查（2026-06-20）坐实当前 [WebOverlayForm.cs](../launcher/src/Guardian/WebOverlayForm.cs) 的 `private void HandlePanelMessage` 是 `switch(cmd)`、`case "close"` 为第一个 case（已最优先），裸 `snapshot`（panel 缺失/未知）经 `panel==` 白名单逐一不匹配后落末尾 `else → _mapTask.HandleWebRequest`，且**全文不读 `domain`**（domain 早分流是 net-new）。⚠**caveat**：落 MapTask catch-all 的不止 `snapshot`——该 case 组里所有非 drop-list（`enter/jump_frame/return_frame/open_stage_select` 在未知 panel 时被显式丢弃）的未知-panel cmd 都会落 MapTask，故 domain 早分流不能只拦 `snapshot`、要拦整个 `domain=inventory` 白名单（detail/refresh 等同理）；另 `_mapTask` 有 `!= null` 守卫，插 InventoryTask 在前须保持守卫一致。改造顺序必须是：①保留通用 `cmd=="close"` 最优先；②若消息携 domain，则 `domain=="inventory"` 只把白名单 cmd 交 `InventoryTask`，其他 domain 回 `unsupported_domain`，**不得**回落既有全局 cmd；③仅无 domain 的 legacy 请求进入既有 panel/cmd 路由。`InventoryTask` 不处理 close，KShop close 仍由 Host 通用关闭路径解析为 `shopPanelClose`。路由测试至少覆盖：

- `{cmd:"snapshot", domain:"inventory", panel:"kshop"}` 命中 InventoryTask，不落 MapTask。
- `{cmd:"close", domain:"inventory", panel:"kshop"}` 仍走通用 close，不被 InventoryTask 吞掉。
- `{cmd:"unknown", domain:"inventory"}` 回 `unsupported_cmd`，不派生或下发任意 action。
- `{cmd:"claim", domain:"unknown"}` 回 `unsupported_domain`，不得回落 ShopTask。
- Web 夹带 `action`/`task`/`op` 不能覆盖 C# 白名单映射；InventoryTask 只下发经验证并解包的 payload。
- InventoryTask 本地拒绝/timeout 与 Host `unsupported_domain` 回包均保留可供 mux 校验的原 `domain/cmd/callId`；非法 callId 不创建 pending。
- mux 不注册重复 callId；Host 收到活动/近期已见 callId 时不再次下发 Flash，且活动 ID 的重复请求不能用同 ID 错误回包干扰 owner。

`InventoryTask` 与 `inventory_response` 已按护栏 §3 接入：Program 创建注入、TaskRegistry 注册、WebOverlayForm 在 close 后做 domain 早分流、断连清传输 pending；`InventoryTaskTests` 覆盖 cmd→可信 action、递归负载白名单、拒绝 count、回包重写、断连/timeout/callId 防重放、路由优先级，以及 loot organizer exact envelope / active instance / owned 容器方向 / binding 回显。SDK glob 风格未手改 csproj。discard/transfer 结果不确定时 Web 只拉 snapshot 对账，不自动重发写。Launcher source-tree 当前全量 **1182/1182**。这仍只是单元回归，不等于新整理路径已在 candidate 或真机通过。

> 备注：`shop_response` 与 `inventory_response` 是两条独立回包通道；前者承载商城领域，后者由所有 owned 容器 View 共享，不绑死在 KShop。
>
> **模板选型（重要，决定 InventoryTask 怎么写）**：当前 `ShopTask` 是现有桥中不在 `panel_resp` 盖 `panel/cmd` 的最小桥；见 [ShopTask.cs](../launcher/src/Tasks/ShopTask.cs)，定位锚点 `public sealed class ShopTask` 与 `msg.Remove("task")`。即使为 shop 写 gate 扩展其内部 pending，shop 回包仍保持只按 callId 匹配的既有形状。`InventoryTask` 的回包要带 `domain=inventory cmd=<原 cmd>`，故应照 [MapTask.cs](../launcher/src/Tasks/MapTask.cs) 的 `private sealed class PendingRequest` 范式记录 WebCmd——**但注意 MapTask 现状盖回的是 `panel=map`/`cmd`、回包并无 `domain` 字段**（核查纠偏：勿误以为 MapTask 已有 domain 可直接复制）；InventoryTask 是把 PendingRequest 的 `panel` 位置换成 `domain`、盖回 `domain/cmd` 的**新形状**，且 action 映射必须改成 inventory 专用显式白名单，**勿机械复制 ShopTask 的字符串拼接**。

### 验证门槛（按改动面叠加，详见护栏 §8）

C# Task → `build.ps1` + `tests/run_tests.ps1`；KShop Web → `node tools/run-kshop-harness.js`；其他 Web → 对应 browser harness/Node QA；AS2 → `compile_test.ps1` fresh trace / fresh Output Panel 副本或 IDE 复核；写 K点/背包 → 游戏内端到端手测 + 重启回读存档；全屏 fallback/main 资产退役 → `node tools/test-audit-main-legacy-ui-reachability.js` + 发布前 `--source-only` + main publish 后 `--require-swf` + XFL 三件套/linkage scanner；文档/协议入口变化 → `node tools/validate-doc-governance.js`。

物品工作台专项门槛：**Gate A1/A2 已由用户验收**；各历史自动数字只证明对应冻结 source，当前 tree 以 fresh 输出为准。专项覆盖商城背包—战备箱、独立 `battlebox` 与宿舍 `warehouse` profile、合成子路由、稳定 OCC lease、NPC 背包单一 inventory 权威、统一 ItemCard/ItemGrid/GridDensityController/OwnedInventoryViewShell、类别/专柜双维目录、目录/背包统一行内单层下钻、KShop 武器三级路径、二级页、分层 `filterSpec`、facets/count、`mutationRevision`、权威 sortAndMerge、防丢回滚与性能矩阵。视觉矩阵截图用 `node tools/run-item-grid-visual-matrix.js --shot=tmp/item-grid-visual-matrix.png`；本轮 Web-only 退役已由人工覆盖五类入口成功、正常关闭/重开、关键写后回读，以及发送/准入/挂载失败只给提示且没有 Flash UI 出现。当前 tag/request/quorum/promotion 与标准入口只读 snapshot smoke 已闭环，严格状态为 `standard_entry_verified`；该 smoke 不等于五类入口与写后回读均在同一正式身份下专项重跑。

`move/merge/swap/sortAndMerge` 已满足 cmd→action/负载白名单与闭环表：同容器不同 physicalSlot 与跨容器走同一三操作，Web/AS2 双层拒绝 `same_slot`，转移拒绝 `count`；未知排序策略/lossy plan 不写入；事务重入与失败无部分写；成功后事件、dirtyMark、epoch/lease 与受影响窗口一致。后续 5b 的 sell/buy/equip 必须另建显式领域命令和金钱/活体副作用测试，不得复用本组同质转移命令。

---

## 10. 债务防线小结

1. 容器只在视图层统一，所有权/命令按实例区分（§6 纠正线）。
2. 背包-only 约束的 SOT 在 **AS2 领域服务**；视图能力声明只是 snapshot 下发的 UX 预检，不是权限 SOT（§1.2、§5）。四层职责：Shell 只认 View 生命周期 / InteractionBroker 仅产中性 `OperationIntent`、不认识 AS2 命令名 / DomainCoordinator 落命令与并发 / AS2 最终裁决业务规则（§2）。
3. 模态② UI 视图本地，第二消费者出现才抽壳级托盘，命名抽取点但不提前泛化（§3）。
4. 命令分层（§4.1）：①a 同质 owned 用 `inventoryMove/merge/swap`；①b 装备/卸装/claim/discard/sell 用显式领域命令（带专属业务）；②批量用 checkout/craft；不存在吃下一切的 `itemTransfer`。逐格 `itemActions` 与整 View `viewCommitActions` 分开（§6）。
5. 命令寻址用**稳定 containerId**（非展示槽）。并发防线 = **AS2 snapshot-local slot lease（O(1) 按 BaseItem 对象引用判定 + 按命令前置条件）**，按写范围覆盖 source/target；排序/`setItems` 后整容器 lease 失效。**已废除"全量 canonical descriptor 作普遍防线"**（语义过度 + AVM1 性能自杀）；canonical encoder 降为诊断/测试 oracle/少数 exact-equality 命令专用。`expectedFingerprint` 改名 `expectedLease`，并把 `snapshotSeq`/`slotLease`/`containerEpoch`/未来 `instanceId` 四职责拆开。Web `cmd` 只经 C# 白名单映射可信 action，信封带版本 `v`，且**仅适用新命令族，不回填 KShop shop\***（§4.1）。
6. 浏览只走权威树筛选，纯 Web `displaySort` 已退役；真实重排只走 `inventorySortAndMerge`，成功后显式 `dirtyMark`（§6）。
7. AS2 权威不动；Web 禁用钮≠安全校验；写操作 AS2 重校验（§4、护栏 §7）。
8. 共享背包走独立 `InventoryTask`/`inventory_response`；Host 先保留通用 close，再路由白名单 `domain=inventory`。Web 工作台以单一 request mux 隔离 legacy shop 与 inventory 回包，勿只凭可撞号 callId 跨通道分发（§9.2）。
9. KShop 已以共享 shop 写 gate 串行化 `saveCart/checkout/claim`，不确定结果先 bulkQuery 对账；Step 3 再把 claim 锁延长到 inventory snapshot，并以共享 backpack write gate 串行化 claim/discard。AS2 slot lease 仍是 inventory-domain 权威防线。**claim 的 `purchasedIdx` 漂移**：后续固定用 AS2 `purchasedToken + purchasedIdx`，位置索引 splice/checkout 重排或重连换 session 后旧 token 必须 `stale_state`（§0.0.1、§9.1/§9.2）。
10. 资源箱 S1/S2 的瞬态容器现以独立 `LootContainerService` / `loot` domain 接入：它不复用通用 Broker 的 owned↔owned transfer，而以 session-scoped `{chestSessionId,lootContainerId,containerEpoch}`、AS2 slot lease 和专用 close v1 裁决寻址。`remainingCount<=0` 优先归一为 `CONSUMED`（包括 `abandon=true`）；非空 `close(false)` 为非终态 `LOOT_SUSPENDED`，非空 `close(true)` 为 `ABANDONED`。suspend 仅同场景 exact target anchor 可重开同一 triple/inventory，且必须重铸 `closeLease/panelInstanceId`；跨场景/重启不持久化。容量阻塞的日常整理不再主动 suspend，而是在同一 loot instance 内以 Host capability 复用 owned inventory 的 `snapshot/autoTransfer/discard`；只允许背包↔战备箱，瞬态 loot container 不进入该协议。`ForceDetach` 仍是 transport fault recovery，不是正常整理或 suspend 路径。完整协议与退出门以 [S1/S2 ADR](地图资源箱-S1S2真实战利品容器与Web双栏-ADR-2026-07-18.md) 为准。
11. Gate A1/A2 与上一 source 的正式闭包继续只作历史证据；2026-07-30 已把仓库、装备、NPC 商店、合成和技能教师旧 writer/renderer/fallback 退役，并由 main source+SWF 结构门阻止资产回流。当前 cut 的 fresh 自动门与 GUI 人工验收已通过，immutable request、双 signer / 双 faultDomain 共识、production receipt、promotion 与标准入口只读 snapshot 复核也已闭环，故总体状态为 `standard_entry_verified`；仍不得把该窄 smoke 称为所有物品业务专项验收。公共 primitive 仍不认识 Task/callId/具体容器对；Overlay 原生浏览器行为统一由 `GameUiBehavior` 边界收口。Gemini 的 `acquire_failed → needs_reconcile` 因 `acquire` 原子无副作用被证伪、不采纳（§0.0、§9.0）。

---

## 11. 已定细节（2026-06-15）

- **双栏槽位 UX**：**左∈{目录, 购物车} / 右∈{背包, 购物车}**，由**视图单例不变量**（§2）自动产出且仅产出三种配置，双购物车被排除：
  - (目录, 背包) 逛+看空间 ｜ (目录, 购物车) 逛+攒单（**面板初始进入态**）｜ **(购物车, 背包) 背包满时腾格子领取**。
  - **「默认」消歧**（两义勿混）：右槽的**结构默认 View = 背包**（displaced 时的回落目标）；**面板初始进入态 = (目录, 购物车)**（进面板首屏，右槽初值=购物车）。
  - 当右=购物车、在左点"购物车"，单例规则把购物车移入左槽；左槽原先的目录不允许挂到右槽，因此右槽 displaced 后回落其**结构默认 View（=背包）**，正好落到 (购物车, 背包) 腾格子态。比购物车专属状态机更省，对后续背包+仓库同屏腾挪沿用同一冲突解析规则。
  - 取舍：切右栏不打断浏览；背包+购物车同屏是**背包满才能领取**的刚需（非锦上添花）。早期"左=目录⇄购物车 / 右=背包"的窄方案已被本方案取代。
- **设计画布**：**1024×576**，对标既有面板/Flash（Stage 尺寸本作动态，落到实处即沿用 PanelScale 标准画布）。好处：AS2 网格几何可原样搬（图标 28px、背包 10 列=280px）。约束：每栏可用约 480–500px，施工保持布局紧凑。
- **乐观更新**：v1 全程 round-trip 确认；**功能完成后再补**乐观更新 + snapshot 对账。
- **拖拽镜像**：**Web 自绘**（不续用 AS2 `物品图标容器`），与"AS2 UI 全面外迁"长期战略一致。

---

## 12. 关联文档

- 迁移稳定性护栏（协议闭环、接入清单、验证门槛、收尾格式）：[agentsDoc/as2-web-panel-migration.md](../agentsDoc/as2-web-panel-migration.md)
- 技能查看 / 教师学习 / 12 槽装备独立领域：[技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md](技能系统-Web面板独立迁移-工程落地规划-2026-07-15.md)
- 装备穿脱 / 四槽药剂 / 已穿戴调制 / 个人信息联合迁移：[角色构筑-Web双栏工作台-工程落地规划-2026-07-26.md](角色构筑-Web双栏工作台-工程落地规划-2026-07-26.md)
- 战斗常驻 HUD 与旧 Hub「个人信息」的职责消歧：[玩家信息界面-NativeHud迁移-架构设计-2026-06-21.md](玩家信息界面-NativeHud迁移-架构设计-2026-06-21.md)
- 角色构筑属性长期同源 TS/AS2 研究边界：[TS到AS2编译基建可行性备忘-2026-07-18.md](TS到AS2编译基建可行性备忘-2026-07-18.md)
- 沉浸全屏化方向：[docs/web-panel-沉浸全屏化-审计与方向-2026-06-11.md](web-panel-沉浸全屏化-审计与方向-2026-06-11.md)
- 既有复杂面板参考：[docs/佣兵界面-webview迁移-施工-2026-05-27.md](佣兵界面-webview迁移-施工-2026-05-27.md)、[docs/战宠界面-webview迁移-施工-2026-05-25.md](战宠界面-webview迁移-施工-2026-05-25.md)
