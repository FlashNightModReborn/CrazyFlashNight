# 统一输入：真实游戏帮助面板试点

最后核对源码基线：commit `d46d2219e147f663a5142c9c787cd027fa43d6aa`。当前已完成双故障域共识与正式 promotion，帮助路径默认启用；部署与审计见文末。

## 范围与收口

维护者批准“收口改动、合并上游、进入真实游戏候选”，并优先使用本机免费 SWE2。上游从 `5c02a10360` 快进到上述基线；合并前备份 253 份本地改动，只有 Program.cs 与 launcher/README.md 重叠，三方合并完成，其余备份文件字节未变。上游已有正式 gym runtime/SWF 一同进入工作区；本次候选构建没有重新 promotion。没有新增提交或推送。

暂停扩大独立基地根初始化、NPC/门/演员重装、全旧 UI 模拟器、时间轮/VFX 改造。此前 fixture-only 的未编译源码保留，不装入正常游戏；少量已存在的隔离开关保留其测试用途。AS2 移动、攻击、NPC 与调度业务继续使用正常游戏后台。

首个真实接入为只读 **帮助** 面板。正常 Program/Guardian/游戏加载链不变；只有该面板在明确选择本候选后，改用 `CompositionSceneHost` + `CoreWebView2CompositionController`。实际帮助脚本、Markdown、CSS、Panel 生命周期继续复用，物理鼠标经 `SendMouseInput`，键盘焦点归新 controller。旧 Web 的 Flash 焦点恢复在新面板持有期间受抑制；关闭精确绑定页面实例，退休 controller 后再准备下一实例。

这仍是局部合成端点：世界、Backdrop、HUD 和其他面板尚未共用一个 HWND；AS2 世界输入仍走原路径。没有把 C1 的 Raw Input/handoff 资格直接授予普通游戏，也没有伪造 AVM1 取消确认。不能称全游戏统一输入或焦点根治完成。

## 入口与人工验收

当前发布准备已获维护者明确授权：帮助合成作为正式默认。开发仍运行 [本地开发启动.cmd](../../本地开发启动.cmd)，无需 `-CompositionHelp`；正式入口沿原根 EXE。候选专用开关和两个纯转发根入口均已退役；C1 历史证据重放保留 `tools/run-c1-input-acceptance.ps1`，不冒充正常游戏。构建、共识与部署进度见发布记录。

候选和正式游戏从同一路径读取 SWF，**不隔离存档**。本轮不自动进入或写入玩家档。人工测试宜使用已有测试档；需要克隆时使用现有 `clone-save-guard.js` 合同，不覆盖真实槽、不删除恢复锁。

一次集中试玩即可，优先顺序：

1. 在真实基地通过刘海工具中的“帮助”打开，切换三个分页、滚轮、Tab、关闭按钮/ESC，连续开关几次。
2. 帮助打开时拖窗、缩放、最大化/还原、切到其他应用再回来。此只读试点采用保守边界：离开游戏会关闭帮助；外部返回首击仅激活。确认没有卡在暂停、空白或恢复中。
3. 关闭帮助后立即移动、攻击、点 NPC，再打开任务/装备/修改器等原路径面板，最后进实际关卡短战斗。这里验证的是合入后的正常游戏回归，不能自动证明世界输入已迁移。

若异常，给时间和操作即可；Host 日志区分 `[CompositionHelp]`、`[HelpSurface]`、`[PanelGeometry]`。不要求人工复跑所有隔离矩阵。真实物理点击/键盘、窗口感觉、正常战斗与长期卡键尚待此轮人工验收。

## 自动证据

- 合并后的 Core 编译通过；最终候选 normal producer 的 native/Core/manifest 构建通过。
- 完整 Host suite：5834 项中 5827 通过、4 跳过、3 失败。失败均为既有源码顺序测试仍查找旧直调用方法名；保留顺序合同并改用 surface 分流方法名后，相关 156 项全部通过。不冒称又跑了一次全量绿灯。
- `node tools/qa-composition-help.js`：真实 Edge 加载生产帮助页面，25/25 通过，含两种尺寸、实际 Markdown、滚轮、Tab、关闭/重开实例及出站 RPC 拒绝。只模拟 Host transport，属于页面层，不是游戏物理输入证据。
- SWE2 Medium 完成三份边界调查、两个实现工单、窄集成审阅和页面 harness。smart 拒绝的两次 shell 调用不计完成；改用内置文件工具续接，未更改全局权限。主控修正实例绑定、负滚轮值、异步预热、防止同步 COM 回调内退休、RPC 的内部发送旁路等问题。最后补齐快速连点下按键配对与鼠标消息单次分发，重新经 producer 封装最终候选；没有为这两项重复整套隔离矩阵。
- 审阅曾称 WebMessage Source 只有 origin；本机固定 SDK XML 明确是发送文档 URI，因此保留精确 PageUrl 校验，未照单降低来源约束。

候选目录：`tmp/runtime-candidates/v2/live-help-20260924b`。
Build identity：`660DE84753CB1EAE367968F384790168B7167E2B07971D128C780C6A9D3A0A77`。
Payload closure：`FBD64348A757C241B37116A3FDAAFD04422FF230AE44DC37F13AE34EFC033863`。
Core SHA256：`0A1AB9CA1388FE0F0886581DEB7CE6E9308BB42DA033E67E74E8F43FA59B6B65`。
本机记录集中在 `tmp/unified-live-entry/`；producer 验证正式 runtime 相对合并后基线未变。

最终入口实跑于 17:40：Guardian PID `23476` 的实际路径为上述候选，启动脚本校验 identity/closure/Core hash 一致；17:40:05.907 的 `[HelpSurface] prepare ready gen=0` 与随后的 `[CompositionHelp] ready` 证明正常后台内的新 controller 完成预热。未自动进入玩家档或调用游戏业务。证据到 `candidate_executed`；真实帮助打开/物理输入、玩法验收、全架构迁移及正式 promotion/standard_entry_verified 均不在已完成结论内。


## 人验回传与帮助黑屏修复（2026-09-24）

维护者回传“帮助打开之后黑屏，其他测试均正常”。原候选日志中 help 的 ready、open、几何提交和 ESC 关闭均正常；这些日志不能证明原生窗口已显示网页。

用同一 `CompositionHelpSurface.cs`、`CompositionSceneHost.cs` 与候选 native DLL 做最小真实 WebView2 组件短测：打开后 DOM 已含完整帮助正文、container=block，prepare/resume/controller 的 HWND 三者相同，但 controller `IsVisible=false`。因此否定 SWE2 的 Owner 导致句柄重建猜测；实际缺口是隐藏窗口预热后只 Show 了 WinForms，没有显式开启浏览器呈现。

局部修复：预热显式隐藏 controller；开面板、可见性变化与几何恢复时按 active/宿主可见/owner 非最小化同步 `IsVisible`。不改 Owner、AS2 输入、世界合成或其他面板。

修后实际窗口截图已有帮助正文，controller 显示、隐藏、恢复、退休和新实例重开短测通过；相关 Host 156 项通过。证据在 `tmp/help-black-screen/` 的 before/after facts、实际窗口 PNG、probe 日志和 focused-tests.log。该 probe 使用真实组件及浏览器，人工仍需在正常游戏中补验帮助打开和关闭重开；其他此前回传正常的范围不要求重做整套人验。本轮不重跑全套隔离矩阵，不写玩家档，不 promotion。

修复候选：`tmp/runtime-candidates/v2/live-help-visible-20260924`；统一开发入口加 `-CompositionHelp` 选用该试点。候选完整身份由该目录 `runtime-build-metadata.v2.json` 及 `tmp/runtime-dev/active.v1.json` 给出。


## 当前交接：人验完成、入口归一（2026-09-24）

维护者在黑屏修复候选后回传“未发现问题”，本轮帮助试点人验收口。实际日志 18:24:30、18:26:00、18:26:12 三次帮助打开均为 controller visible=True；先前“其他测试均正常”仅沿本轮约定旅程记录，不升级为完整世界输入迁移或 #119 原故障已修复。

原临时 CMD 只做环境变量转发，没有独立架构职责，已按既有自动化规范退役。`本地开发启动.cmd -CompositionHelp` 复用原 verifier、candidate identity 与 start；普通无参数路径不变。脚本回归 76 项通过，包含继承变量清除、显式参数子进程传递、失败恢复及临时入口不得回流。本次仅改入口编排和文档，运行 Core/SWF 字节未变，不为这次入口归一重编已验收候选。焦点诊断启动/日志收集有单独的现场采样用途，本轮不删除它们。

交接起点：真实游戏帮助试点已验收；C1 实验基座有自己的历史证据；独立 RuntimeWorld 源码未编译，不装入正常游戏。该阶段的代码和文档现已归档到源码提交 d46d2219e147f663a5142c9c787cd027fa43d6aa 并推送；后继可按 Git 接班。正式发布结果见末节。

下一批只接一个真实的已迁移只读面板，复用当前生产接管路径，选择最小依赖消费者后再实施；保留普通 AS2 业务，不重新装配基地/时间轮/VFX。世界持续及战斗输入、全层统一合成根、设备/IME/性能资格、正式发布是后续工作。原生 UI 随既定 UI 迁移退休，不建设全覆盖模拟器。看板已读回确认：[#120 帮助试点人验收口](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/120) 为完成/CLOSED 且归档；[#121 下一个真实只读面板](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/121) 为 Ready/OPEN 且未归档，两者原生父项均为 #28。#103 已补进展、#87 已补本次上游集成记录；#103、#119 保持 OPEN。只创建这两张有明确交付边界的卡，不为长期未施工阶段铺空卡。

一句话接班：**先读本报告“当前交接”与施工计划 §21，接续本次发布的源码与部署提交；在正常游戏里为已验收面板端点接入一个最小只读消费者，沿现有开发入口和一次集中人验推进，停止根游戏重装与全旧 UI 模拟，勿把局部试点当全局焦点根治。**


## 2026-09-24 正式发布授权与准备

维护者明确要求“获取云端共识，构建部署推送，以及其他需要收尾的工作”，并选择“正式默认启用帮助合成”。该授权包含本轮范围化提交/推送、保护 tag、云端独立构建、同身份共识与唯一 promotion writer；不扩大到新 UI 消费者、全游戏输入迁移或真实玩家存档试写。

帮助在正式/开发入口均默认使用已验收端点；`-CompositionHelp` 和环境选择已移除。C1/B1 仍需专用隔离调用，未编译 RuntimeWorld 不进入普通游戏。发布前新鲜 CS6 正常驱动 11/11、整形 50/50，asLoader 与修改器 SWF 均已刷新且 Compiler 0/0；完整 Host 5830 通过、4 跳过。其他工作区的堕落城文档/列车成本审查材料不入本轮提交。当前阶段只到准备完成，正式部署结果随后按实际记录补齐。


## 正式共识与部署（2026-09-24）

维护者已授权的正式默认帮助路径经唯一 promotion writer 部署。源码 `d46d2219e147f663a5142c9c787cd027fa43d6aa`，tag `runtime-build-v2/20260924-unified-input-v1`；request `417A24A494788178150B2B17DEC8F509E2F2F125B1265960E8C6F0D0A3FA3B38`。

注册本机 builder-local-c（physical-host-c）与 [GitHub Hosted 独立构建](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/35992145481) 的 identity 均为 `599D336D54638AA5D825E1EB5C79749B2CCB3946CD705171CF5DA2F9257A5D1E`，payload closure 均为 `94ECEBC3FDB7723E24B495947F694AD044A651C5A693D4E6DFE9DEF723B1A097`。验真、policy 与 promotion 的具体结果见本机 `tmp/unified-release/`；持久签名证据在 `config/build/runtime-release-consensus.json`。没有把单机候选复制到正式目录，也没有改变 tag 或降级发布门。

正式/开发入口默认启用帮助合成，不需专项 CMD 或候选开关。C1 历史重放只留工具入口；未开放 B1、世界完整输入、全层统一合成及全部现场焦点问题未据此完成。其余 17 份无关工作区文档继续保留。本轮不新发稳定整包、不递增 2.718 版本号。

正式入口仅按实际完成的启动/身份核验记录；候选人验不自动升级为全部业务的 standard_entry_verified。#120 保持限定里程碑完成，#121 后继 Ready，#103/#119 保持开放。

发布收尾还发现 52 个被 Git 忽略的实验 SWF/宏副本计入磁盘 policy 枚举；未修改冻结政策或削弱检查，而是确认其非 reparse、非 tracked 源码后按原 SHA-256 完整保存到 `tmp/unified-release/quarantine/`。映射在 `quarantine-manifest.json`，原始宏源码和 C1 冻结人验包未改动。政策签收 41/41；其中工作台回归 67/67 的内置审计耗时 341506ms，单独重复审计的成本留后续优化，本次未裁剪。

正式入口核验：`automation/start.ps1` 无 candidate selector 启动 Guardian PID 32056，实际路径为根 `runtime/CRAZYFLASHER7MercenaryEmpire.Core.exe`，mode=formal_runtime；Core SHA-256 `2B718A269D6C37DE50C441B592194327BAC7A985D30B957B9056A82A074EC3C1`，identity/closure 与签名共识一致。该核验只覆盖正式启动和身份，没有代做玩家档业务或扩写为全游戏人验。

远端收尾：部署提交 `2a5c32eb4a0c995a0c205567aa4b6b2ea438d632` 已推送；[CI Audit 35996103622](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/35996103622) 成功，输出 `state=promoted / deploymentChanged=true`。这是发布链审计，不追加全游戏业务验收结论。
