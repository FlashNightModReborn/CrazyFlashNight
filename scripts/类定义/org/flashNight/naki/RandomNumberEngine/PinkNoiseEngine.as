import org.flashNight.naki.RandomNumberEngine.BaseRandomNumberEngine;

/*
 * PinkNoiseEngine 粉红噪音生成器
 *
 * 设计理念：
 * -------------------------------
 * 该类基于线性同余生成器（LCG）生成的均匀随机数，通过多层叠加（分层法）产生粉红噪音。
 * 粉红噪音具有 1/f 的功率谱特性，即频域能量与频率成反比分布。
 *
 * 分层机制与能量补偿：
 * -------------------------------
 * 1. 分层结构：使用 5 层（numStages=5）的随机数，每一层更新周期为 2^i（i=0~4）。
 *    - 较低层（低频部分）更新频率低，承载长时程相关性；较高层（高频部分）更新频率高。
 * 2. 权重设计：采用公式 weights[i] = 1/(numStages - i + c) 给每层分配权重，
 *    其中平滑因子 c 经实验与数学推导（频域能量补偿分析、拉普拉斯变换等方法）确定为 8.54，
 *    使得各层噪声能量在频域内得到均衡补偿，最终合成的总噪音满足 1/f 特性。
 *
 * 性能优化：
 * -------------------------------
 * 1. 内联展开：将 for 循环及数组访问全部展开为静态成员变量（s0, s1, s2, s3, s4），
 *    减少数组索引和动态查找带来的额外开销。
 * 2. 固化参数：numStages 固定为 5，平滑因子 c 固定为 8.54，所有与分层相关的参数（更新周期、权重）
 *    均在编译时固化，确保运行时只进行必要的数值更新。
 * 3. 硬编码 LCG 公式：底层熵源直接展开为 (seed = (1192433993 * seed + 1013904223) % 4294967296)
 *    与归一化常量 2.3283064365386963e-10（= 2^-32），省掉一切函数调用开销。
 *
 * 继承关系说明：
 * -------------------------------
 * 直接继承自 BaseRandomNumberEngine 而非 LinearCongruentialEngine。
 * 原因：LCG 在热路径上把 randomFluctuation/randomOffset/... 等助手方法的内部
 * nextFloat() 调用全部内联成单步 LCG 公式（绕过虚派发以提速）。若 PinkNoise
 * 继承 LCG，这些助手会绕过 PinkNoise.nextFloat() 直接吐出均匀分布，破坏 1/f
 * 频谱语义。继承 BaseRNE 后，未覆盖的助手仍走 this.nextFloat() 虚派发，
 * 自动落回本类的 5 层粉噪实现。
 *
 * 由于本引擎为伤害波动等极热路径特化设计，不打算泛用，因此底层 LCG 算术
 * 通过常量硬编码而非引用复用——可读性让位于性能。
 *
 * 单例模式：
 * -------------------------------
 * 该类采用单例模式，确保系统中只有一个粉红噪音生成器实例，从而避免静态变量索引开销。
 *
 * 数学与工程调参：
 * -------------------------------
 * - 理论上，通过能量补偿，理想粉红噪音 PSD 对数谱斜率应为 -1。
 * - 对应权重的选择和更新周期的设计保证各层在不同倍频程内能量均衡分布。
 * - 通过实验和离散傅里叶变换验证，固定参数 c=8.54 在 5 层结构下使得生成噪音的 PSD 斜率接近 -1，
 *   实现了理想粉红噪音的特性。
 *
 * 注意：
 * -------------------------------
 * 1. 由于 AS2 中静态变量开销较高，本实现尽量避免静态成员，将状态保存在成员变量中以提高性能。
 * 2. 所有的数值常量（如权重系数及归一化因子）均为预先计算好，确保每次调用 nextFloat() 时只进行最少量的计算。
 */

class org.flashNight.naki.RandomNumberEngine.PinkNoiseEngine extends BaseRandomNumberEngine {
    // 单例实例（仅用于获取实例，不参与频繁访问计算）
    public static var instance:PinkNoiseEngine;

    // 种子（与 BaseRandomNumberEngine.seed 同一份实例变量，此处重声明仅为
    // 抑制 AS2 编译器对父类 private 字段直接访问的警告，与 SeededLCG 同样的写法）
    private var seed:Number;

    // 分层状态：各层随机数值，采用成员变量而非数组以降低索引开销
    private var s0:Number, s1:Number, s2:Number, s3:Number, s4:Number;
    // 更新计数器，用于控制各层的更新频率
    private var counter:Number;

    // 私有构造函数，禁止外部直接实例化，保证单例模式
    private function PinkNoiseEngine() {
        super();
        reset();
    }

    /*
     * reset() 方法
     * -------------------------------
     * 用于初始化各层状态，直接硬编码展开 LCG 公式以减少函数调用开销，
     * 同时更新种子（seed，继承自 BaseRandomNumberEngine）并归一化到 [0,1) 区间。
     */
    private function reset():Void {
        s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10;
        s1 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10;
        s2 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10;
        s3 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10;
        s4 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10;
        counter = 0;
    }

    /*
     * getInstance() 方法（单例获取）
     * -------------------------------
     * 保证系统中只有一个 PinkNoiseEngine 实例，
     * 避免静态变量频繁访问带来的性能损耗。
     */
    public static function getInstance():PinkNoiseEngine {
        if (instance == undefined) {
            instance = new PinkNoiseEngine();
            // 重定义 getInstance 以直接返回实例，进一步减少调用开销
            getInstance = function():PinkNoiseEngine {
                return instance;
            };
        }
        return instance;
    }

    /*
     * nextFloat() 方法
     * -------------------------------
     * 生成一个粉红噪音样本，使用内联展开所有 super.nextFloat() 调用，
     * 并根据 counter 控制各层的更新频率（利用按位与操作优化模运算）。
     *
     * 更新策略说明：
     * - modVal 为 counter 与 15 的按位与结果，相当于 counter % 16，
     *   根据不同的模值条件，决定更新多少层：
     *   - 当 modVal == 0 时：更新所有 5 层（s0, s1, s2, s3, s4）
     *   - 当 (modVal & 7) == 0 时：更新前 4 层（s0, s1, s2, s3）
     *   - 当 (modVal & 3) == 0 时：更新前 3 层（s0, s1, s2）
     *   - 当 (modVal & 1) == 0 时：更新前 2 层（s0, s1）
     *   - 否则只更新 s0
     *
     * 这样设计确保各层以不同的频率更新，模仿不同频段噪声的特性，
     * 同时结合预设的权重（内联固化的权重常量），实现频域能量的均衡分布。
     *
     * 加权求和：
     * - 返回值为加权求和结果，权重已预乘归一化因子（1/0.43995），直接输出 [0,1) 区间。
     * - 归一化后的加权系数：
     *   s0 * 0.16788, s1 * 0.18127, s2 * 0.19693, s3 * 0.21566, s4 * 0.23826
     *   （原始系数 0.07386/0.07975/0.08664/0.09488/0.10482 除以总和 0.43995）
     *   使得最终合成的噪音功率谱接近理想的 1/f 分布。
     */
    public function nextFloat():Number {
        // 利用按位与运算优化 mod 运算，counter++ 与 15 进行与运算
        var modVal:Number = ++counter & 15;

        if (modVal == 0) {
            // 当 modVal == 0 时，更新所有 5 层
            // 内联展开嵌套调用，依次更新 s0 到 s4
            s4 = (s3 = (s2 = (s1 = (
                // 最内层先更新 s0，并立即更新 seed，用于后续 s1 的赋值过程
                (s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10;
        } else if ((modVal & 7) == 0) {
            // 当 modVal 的低 3 位为 0 时（等价于 counter % 8 == 0），更新前 4 层
            s3 = (s2 = (s1 = ((s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10;
        } else if ((modVal & 3) == 0) {
            // 当 modVal 的低 2 位为 0（等价于 counter % 4 == 0），更新前 3 层
            s2 = (s1 = ((s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10;
        } else if ((modVal & 1) == 0) {
            // 当 modVal 为偶数（但不满足以上条件），更新前 2 层
            s1 = ((s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10;
        } else {
            // 其它情况下，只更新最低层 s0
            s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10;
        }

        // 加权求和，内联展开加权操作，归一化输出
        return s0 * 0.16788 + s1 * 0.18127 + s2 * 0.19693 + s3 * 0.21566 + s4 * 0.23826;
    }

    /*
     * randomFluctuation() 覆盖 BaseRandomNumberEngine 的同名方法
     * -------------------------------
     * DamageCalculator 是全游戏最热路径，每次命中都通过
     * PinkNoiseEngine.instance.randomFluctuation(15) 计算伤害波动。
     * 这里把 nextFloat() 的完整 5 层粉噪合成逻辑内联进来，
     * 省掉一次 this.nextFloat() 的原型链查找 + 函数调用开销。
     *
     * 语义一致性：counter/s0-s4/seed 均为同一份实例状态，每次调用
     * 仍精确执行 1 次分层更新 + 加权求和，1/f 频谱特性不变。
     *
     * 代数化简：1 + (r * 2F - F) / 100  =>  1 + (r * 2F - F) * 0.01
     * 无回放一致性需求，*0.01 与 /100 的浮点微差可忽略。
     *
     * 维护提示：若 nextFloat() 的 5 层更新逻辑发生改动，
     * 本方法内的对应展开代码需要同步更新。
     */
    public function randomFluctuation(fluctuationRange:Number):Number {
        var modVal:Number = ++counter & 15;

        if (modVal == 0) {
            s4 = (s3 = (s2 = (s1 = (
                (s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10;
        } else if ((modVal & 7) == 0) {
            s3 = (s2 = (s1 = ((s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10;
        } else if ((modVal & 3) == 0) {
            s2 = (s1 = ((s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296)) * 2.3283064365386963e-10;
        } else if ((modVal & 1) == 0) {
            s1 = ((s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10, (seed = (1192433993 * seed + 1013904223) % 4294967296))) * 2.3283064365386963e-10;
        } else {
            s0 = (seed = (1192433993 * seed + 1013904223) % 4294967296) * 2.3283064365386963e-10;
        }

        var r:Number = s0 * 0.16788 + s1 * 0.18127 + s2 * 0.19693 + s3 * 0.21566 + s4 * 0.23826;
        return 1 + (r * fluctuationRange * 2 - fluctuationRange) * 0.01;
    }
}
