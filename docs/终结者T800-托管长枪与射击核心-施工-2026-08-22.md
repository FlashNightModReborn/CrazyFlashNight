# 终结者 T800 托管长枪与射击核心施工记录

**状态**：`HUMAN_ACCEPTANCE_PASSED / SOURCE_HANDOFF_READY / NOT_DEPLOYED`。首轮实机确认射击成立并发现 66 号获取入口缺口；第二轮捕获 Host 新命令漏放行造成永久“处理中”；随后又确认普通敌人模板缺失四个换装生命周期入口，导致 M134 与 XM214 的装备生命周期完全未装载。r5 补齐桥接后，玩家已确认 M134 枪管旋转、XM214 特效与变速均正常；验收后的源码变化仅删除无状态副作用的诊断脚手架，并已重新完成 focused test 与 `asLoader.swf` 发布验证。
**数值边界**：中阶/终阶等级与费用均是首轮实测占位，不是最终平衡结论。

## 产品契约

T800 保留原 AI 与原近战。玩家可在非战斗地图从背包交付一把长枪；交付后武器离开背包，战宠则使用完整快照的运行时副本。玩家战宠在没有托管长枪时回退默认 L85A1；普通关卡 T800 只有在外部明确配置长枪时才射击，未配置武器则为空手近战。

现役玩家入口是统一战队 Web 面板；旧 `战宠相关界面.swf` 不再是标准入口。本轮仍把其删除按钮作为兼容面补上同一 fail-closed 返还约束并独立发布，防止旧独立 SWF/遗留调用吞装备，但不把它列入现役业务旅程或要求玩家以旧界面完成主验收。

托管长枪交互复用角色构筑的候选筛选语义，放在培养页右栏、进阶方案上方：默认“兼容”只列可交付项，显式切到“背包”才列出全部长枪及锁定原因；玩家先单选候选，再以唯一主按钮确认交付。Web 只筛选 Host/AS2 的权威候选投影，不自行放宽枪种、等级、lease 或战斗锁定规则。

机械名册卡片保持原有信息密度，不再塞入武器行；选中单位后，右侧详情以佣兵装备格的图标语言显示冻结物品或默认 L85A1 的真实图标、强化角标、名称与“托管/预设”。培养页当前武器及全部候选格复用项目 `PanelTooltip` 的 canonical 富物品注释，候选区复用完整/紧凑密度并独立滚动；Web 不维护枪名到图标或装备属性的第二份映射，也不使用浏览器原生 `title`。

| 阶位 | 占位解锁/费用 | 枪种 | 有效需求等级 |
|---|---:|---|---|
| 初始 | 无升阶 | 冲锋枪、突击步枪 | `<= min(15, 战宠等级)` |
| 武器扩展 | Lv.30 / 100000 金币 | 任意 `use=长枪`，含发射器 | `<= min(30, 战宠等级)` |
| 全武装 | Lv.50 / 300000 金币 | 任意 `use=长枪` | `<= 战宠等级` |

战宠不使用玩家枪时回退 L85A1，因此“宠物无供枪”不等于剥夺旧版战力；普通 T800 不获得这项战宠兜底。枪种解锁不赋予新 AI：发射器、狙击/战斗步枪仍用 T800 现有的 500 攻击范围与 100 保持距离，其实战效率由人工验收决定，不在本轮自动调 AI。

## 所有权与弹药不变量

```text
背包 BaseItem --lease 复证/移除--> petInfo[5].托管长枪.item（冻结权威）
                                           |
                                           +--深克隆--> 出战单位运行态
                                                              |
                                                              +--射击/换弹/被动可写
取回 <-------------------------- 只重建冻结权威，不读运行态
```

- 冻结字段包括 `name/value/lastUpdate`，因而保留交付时弹耗、强化、插件、tier 与其它 `BaseItem.value` 字段。
- AI 换弹不读、不扣主角弹药；`ShootInitCore` 给非主角正哨兵弹匣库存，`ReloadManager` 的现有 AI 分支直接把运行副本 `shot` 复位。
- 宠物出战期间可获得武器自带的返弹、形态或被动收益，但取回时全部丢弃；不能经战宠获得免费子弹或免费插件变更。
- 手动取回与删除宠物都先预检背包空位。快照损坏、背包未就绪或满包时禁止删除，不会静默吞装备。
- 统一战队 Web 面板是权威入口；已编译的旧 Flash 删除兼容面也执行同一预检和强制返还，避免遗留调用绕过所有权约束。

## 射击核心与生命周期

T800 改用与人形怪/二足机器人同源的 `hasDressup + enableShoot + ShootInitCore.initLongGun`：

- 根时间轴改为 `长枪站立/行走/跑/跳/攻击`，并为同一组运动/近战帧补齐 `空手站立/行走/跑/跳/攻击` 别名；普通无枪 T800 不会误入射击帧。
- 攻击 `man` 使用标准 `初始化并开始射击`、`换弹匣()` 帧点和 `枪.枪.装扮.枪口位置` 路径。
- 旧 holder 已切到 `装备引用配置.配置装扮`，生命周期可以通过 `unit.长枪_引用` 和 placement-ready 事件复用成熟组件。
- 普通敌人模板没有人形模板提供的 `装载主动战技/装载副武器控制槽/装载生命周期函数/完成生命周期函数装载`。`prepareUnit` 现只按缺失项从成熟的 `_root.主角函数` 挂接这四个入口，且不覆盖素材已有实现；否则 `DressupInitializer` 会在基础装备与射击就绪后提前中断，形成“白板枪能开火，但 M134/XM214 lifecycle 全未注册”的半初始化状态。
- T800 更换攻击姿态时会整体替换 `man`。`DressupReferenceManager` 在新 holder 已位于当前 `unit.man` 时，会清除同一基础引用、同一实例但仍挂在旧 `man` 分支的陈旧注册项，让当前可见武器接管规范引用；同一活动 `man` 内的真实并存 holder 仍保留 `#N` 冲突隔离。该规则面向全部标准装扮视觉引用，不按 M134 武器名特判。
- 换枪对已出战宠物整只重建；销毁前先 `DressupInitializer.teardownLifeCycles`，再 `removeMovieClip`，保留 `onUnload/StaticDeinitializer` 后续清理。
- 选择标准 `hasDressup` 也意味着继承二足机器人/非主角装备投影：友军 HP×3、MP÷10，默认字符串武器随单位等级获得标准强化投影，装备重量/速度与抗性由通用组件重建。该变化没有另写 T800 例外，必须在人工验收中与旧 T800、二足机器人并排确认。

生命周期静态盘点覆盖 37 把长枪、39 个 lifecycle 节点、40 个 binding 和 59 个唯一 routine，59/59 有定义。分级为 A 21、B 12、C 3、D 1；已将 C/D 共性问题收口在共用守卫：

- XM214 非主角不写主角子弹 HUD。
- G1111 非主角不推送全局镜头、不绘制玩家锁定框。
- 吉他喷火器对非主角纯长枪宿主不别名刀槽、不装载兵器战技、不读取玩家变形键。
- PIG 版 M134 仅当单位显式声明 `装备能力.自动抡枪 !== false` 时自动跳技能标签；T800 明确禁用，保留普通 M134 主体。
- M134 枪管意图继续兼容旧 `长枪射击` 预事件，同时监听真正提交子弹后的 `processShot`；只接受主长枪，排除 `长枪副武器`。成功出弹负责产生旋转意图，生命周期更新还必须落到当前可见枪体，两者缺一不可。生命周期桥补齐后，r5 实机已确认当前可见 M134 枪管恢复旋转。

A/B/C/D 是静态兼容分级，不代表已经验证所有时间轴、枪口、AI 命中效率或平衡。

## 实机反馈与根因修复

- 首轮已确认 T800 能使用托管长枪开火，证明基础 `ShootInitCore` 主路径成立；但 M134 枪管没有可见旋转。把枪管意图锚到成功 `processShot` 并修复活动 `man` 引用接管后，实机仍不转；随后 XM214 又同时表现为特效缺失、疑似不升速，表明问题位于多把枪共有的 lifecycle 装载层，而不是单一 M134 事件或枪管路径。
- 逐步追踪 `DressupInitializer` 调用序列后确认：T800 走普通敌人模板，缺少人形模板的四个换装入口；初始化会在物品数据、属性和基础射击已经可用之后，第一次调用缺失函数时停止。因此 lifecycle init/cycle 根本没有注册，能够同时解释 M134 动画、XM214 特效与射速爬升缺失。修复采用 T800 服务边界的按缺失项桥接，不修改全局敌人模板，并保留复合枪械、自动抡枪等能力关闭状态。
- r5 人工验收确认 M134 枪管旋转、XM214 特效与变速均恢复正常。根因闭合后已删除 `[M134Visual]` 诊断状态、逐帧引用扫描和包装函数；最终 `M134.as` 相对既有实现只保留旧事件兼容、成功 `processShot` 监听及副武器门禁。生命周期桥、活动 `man` 引用接管和 init-only/cycle-only 通用兼容属于功能性结构修复，继续保留。
- 第二轮 Web 交付永久停在“处理中”的根因是 `PetTask` 和前端都已实现命令，但 `WebOverlayForm` 总路由允许表漏掉 `equip_weapon/withdraw_weapon`，消息在进入领域处理器前被静默丢弃。Host 已补齐两个 exact case 和路由回归；Web 另加 12 秒有界超时，超时必须解除 busy、提示用户并重新拉取权威 snapshot，迟到旧回包不得复活旧请求。
- 66 号 T800 定义和 `RosterType=mechanical` 原本都存在，但没有被任何 `PetStore/Category/List` 引用。Web 名册只显示存档中已经拥有的单位，`RosterType` 本身不会凭空把未拥有单位加入战队，因此首轮“机械名册看不到 66”在未领养存档上是预期表象，真正缺口是没有获取入口。
- 66 号现已加入“???/彩蛋”领养分类。Host 会在机械类型请求仍从 `categoryIndex=0` 打开时回退到首个实际非空原始索引并回传 `selectedCategoryIndex`；不修改玩家存档、不自动赠送。未拥有时从机械页的“+领养”获取，拥有后才出现在机械名册。

## 自动化证据与人工验收

本轮自动门与关键人工感知验收已经完成：

- Focused TestLoader：诊断减法后的 fresh runId `33f82604edaf45789d287ffb03709fec`，`ManagedLongGunServiceTest` **43/43**、`DressupReferenceManagerTest` **70/70**、Compiler **0/0**、32K retry **0**；`TestLoader.swf` 为 **458,759 bytes** / SHA-256 `D8A54184D50514BF4AEA4E05F8C7397B6D1DEF2DEC6FCA324EF08EF49CE19BF0`，最大函数体 **19,072B**。新增断言固定四个换装生命周期入口按缺失项挂接且不覆盖自定义实现，并固定候选 exact lease 与当前冻结权威都复用 canonical 富注释；既有整链断言继续覆盖成功出弹后当前可见 M134 枪管前进、副武器隔离、旧事件兼容，以及活动 `man` 接管/同分支碰撞隔离。
- Launcher：全量 **4021 passed + 3 explicit opt-in skipped / 4024 total**、0 fail；Pet catalog/PetTask 定向 **22/22**，逐一证明两个写命令与 `weapon_tooltip` 同时存在于 Host 总允许表并路由到 `_petTask.HandleWebRequest`，候选注释保留 exact lease-bound source。只有既存 WindowsBase 版本解析 warning。
- Team Edge harness：1024×576、1366×768、1920×1080 的 T800 专项各 **7/7**、合计 **21/21**，覆盖名册卡片不再塞武器行、右侧详情权威图标/名称、项目富注释、候选完整/紧凑密度与独立滚动、兼容/背包筛选、锁定原因、超时解锁回拉、交付与取回；每档总结果 **175/179**。四个失败仍为本施工前已有的 `team-kb1/team-i1/team-j6a/team-j6c` 字体/卡片基线，不属于 T800 路径。
- Web/静态门：Workbench strict audit **0 error / 0 warning**，CSS bundle **25 imports / 969,131 bytes / 24,173 lines**、SHA-256 `5DD69049EC4989A565090E013E8A702730AF8AE6FB083FA82EADAD4F5EFB5FDD`，visual atlas **66/66**；pet roster **0 error / 8 既存 warning**；五个改动 XML 均可解析，`git diff --check`、Web JS、预编译 **223** 个 AS2 BOM、装备函数 **65/65** 覆盖与 doc governance 均通过。
- Flash 发布：诊断减法后的最新 `scripts/asLoader.swf` **1,117,489 bytes** / SHA-256 `C5242145836BE046CBCE5079DE92C2ABB4E4A5E510F4030CD90203D1F51B0B7B`、**10,115 functions**、最大 **46,285B < 60,000B**；精确 `publish` 重跑在 **103s** 内 exit 0，SWF 刷新门、fresh compile output 与 Compiler **0/0** 全通过，publish-only 的 flashlog 未刷新属预期。`flashswf/arts/things1.swf` 仍为 **2,728,163 bytes** / `DDAFD75F9070FD59A8053104EA7E6B79A64C757296795244367B8A363A506EB8`；legacy `flashswf/UI/战宠相关界面.swf` 仍为 **2,880,315 bytes** / `22DB038D452E5F489BD88AB2311FA00D877BFB43593893B94DDF75B86E5D5FAB`，本轮未再次修改其 XFL。
- FFDec 21.1.1 复核：things1 产物包含 `ManagedLongGunService.prepareUnit`、`初始化并开始射击()`、`换弹匣()` 与 `配置装扮(...,"长枪_引用")`；legacy 战宠 SWF 包含预检/取回托管长枪的删除守卫；最新 asLoader 共导出 **615** 份脚本，除托管服务、`use=长枪` 重建复证、非主角弹匣哨兵及 XM214/G1111/吉他/PIG 通用守卫外，定点命中 `DressupReferenceManager.pruneInactiveManBranchEntries` 的定义与调用。
- XFL Layer 0：things1 除历史 `Symbol 2117/linkageId=加载mc库1` 命名气味外，无重名、linkage 撞车、失效 include、断引用或 orphan；legacy 战宠 XFL 保留 39 条历史 bitmap/library 断引用，但无重名、linkage 撞车、include 错配或 orphan。两目标 rename/fix dry-run 均无实际修复，linkage scanner 重扫前后字节一致。本轮未修改主 XFL，不以 `main` 代替独立素材库发布。
- Launcher 隔离候选：`tmp/runtime-candidates/v2/t800-gun-r5` 已完成构建、33 文件 integrity-only 验证并由精确 `ReuseOnly` 入口启动为 Guardian PID **17112**，状态为 `candidate_executed / NOT_DEPLOYED`；进程路径精确来自 r5。Core SHA-256 `420305DBE54F11A80BABE48BD111FEA960248D3E3430FB90F1B4829EA056DE40`，build identity `0A4478EF7C9D18BCF560B6AB8F2EBA66C255930FCDC361F5FECC36469509A522`，payload closure `B18AB5EC8A2161621D55ED35C860C96248B9081772CC1DD4B911444815C2169C`。玩家从可见界面完成真实业务旅程并确认 M134 与 XM214 均正常；该结论只覆盖本次明确观察到的托管、射击、枪管/特效和变速路径，不外推为全部枪械、真实存档重启或全游戏 E2E。验收后的唯一 AS2 改动是删除不参与状态转移的诊断字段、日志与包装层，功能性监听及视觉更新调用保持不变，且由上述 fresh focused test 与 fresh `asLoader.swf` 复验，因此不再要求玩家重复同一感知动作。正式 Core 仍为 `E0A7B31C70DA564ECCA43B8B58DDA2F6443553112DD95B6EE05182FB94F78474`，正式 35 文件 closure 仍为 `510DE1B21A3DF43F886F6E601832A7913B3399EE308C67B9C73319871BCC76CE`，构建器确认均未修改。

以下项目中，M134 枪管与 XM214 特效/变速已经完成人工验收；其余仍是后续扩大回归与数值实测清单，不阻断本轮诊断减法后的提交：

- 若存档尚未拥有 66：机械页 → “+领养” → “???/彩蛋”应出现“终结者-x”；领养成功后才应进入机械名册。若存档已拥有 66，则应直接在机械名册看到它。
- 机械名册卡片不应再显示武器行或改变原排版；选中不同 T800 时，右侧详情应同步显示当前武器真实图标、强化角标、名称与“预设/托管”，交付/取回后无需重开面板即可切换。
- 培养页托管长枪必须位于右栏进阶方案上方；默认“兼容”只见可交付项，“背包”能看到全部长枪与锁定原因，候选多时可在本区独立滚动，完整/紧凑按钮都生效。悬停或键盘聚焦右侧详情、培养页当前武器和任一候选格，都应显示项目统一的完整装备属性注释，而不是浏览器原生白框。交付/取回都应正常返回，不能永久停在“处理中”。
- 已验收：给 T800 装 M134 与 XM214 持续开火，当前可见 M134 枪管正常旋转，XM214 特效与射速爬升正常。后续扩大回归仍应覆盖停火回首帧、跨站立/移动/攻击姿态、M134 暴力版、副武器隔离，以及 PIG 版在 T800 上不得自动跳“抡枪”。
- 战宠无托管枪默认 L85A1；普通 T800 无配置枪为空手近战；初阶 UZI/突击步枪正例与战斗步枪/超级武器负例。
- 交付前后背包物品、弹耗、插件和强化一致；存档—重启—取回往返。
- 出战换枪重建，休息/重新出战，过图，死亡，满包取回，以及统一战队 Web 面板的满包删除。旧 Flash 只属已编译兼容面，不作为现役标准入口主验收项。
- 射至空弹并让 AI 换弹后取回：武器必须仍是交付时 `shot`，主角弹匣数不变。
- 代表枪 AR57/RPG/GM6、G11/双面雷神/铁枪、XM214/吉他/G1111/PIG；观察 HUD、镜头、状态软锁与 lifecycle task 重建后不增长。
- 中阶发射器与终阶高等级长枪的 AI 射程/命中效率、观感与实际强度；据此再调整占位等级和费用。
- 与旧 T800/二足机器人并排核对 HP、MP、移动速度、韧性、机械抗性和默认 L85A1 强化投影；若通用 `hasDressup` 数值语义不适合 T800，再决定是否另立明确能力开关。

本轮关键人工感知验收已经通过，可以进入中文 commit 标题、推送与后续发布流程；Launcher 正式 runtime promotion 仍须另走 immutable request、双 signer/双故障域与 strict verifier，不由本记录代签。
