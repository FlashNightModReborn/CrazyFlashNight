# R1 存盘 API 迁移收尾（2026-09-05）

**文档角色**：交接提交 `4ae00a176265b7d00ea38364d545d29cbe601efa` 后步骤 10–14 的施工与验证记录。规范仍以[存盘风暴 ADR](存盘风暴止血-Reward相邻durable-cut桥接与存盘次数回归门-ADR-2026-09-03.md)和[调用点工具说明](../tools/save-api-migration/README.md)为准。

**最后核对代码基线**：commit `4ae00a176265b7d00ea38364d545d29cbe601efa` 加当前 R1 工作树。源码迁移与本机自动验证已完成；11 个目标均实际经 CS6 发布，复核生产入口后只保留 5 个 SWF 更新，另外 6 个恢复接手时原字节。Flash 在最后一项完成后按 `-QuitFlashAfterPublish` 正常退出，字体弹窗阻塞已解除。

## 改动与边界

- Reward `flushSave(cutName)` 内核改为 `flushDurableNow("reward." + cutName)`；八 cut、attempt/result probes、strict true 判定及失败恢复语义保留。
- SceneChanged 无条件调用 `flushDurableNow("scene.changed_safety_net")`，仍在 `deactivateAll()` 前执行。
- XFL C1–C6 改为注册 reason 的 `flushDurableNow`。接手时 C1–C5 被误改为 `flushBeforeTransition`，且 reason 不在 allowlist，实际会返回 false 而不存盘；已纠正。主/live 四组副本保持源码 parity。
- E1/E9/E10 保留原 dirty guard；E6 接通 canonical markDirty；其余 E 组无条件 request。E3/E11/E12 保留 opt-in reason probe 与 `suspect_legacy` 状态。C6 保留两处购物车 partial 保存和关窗 partial→full 顺序。
- 修复 PAT 静态检查把 executeOpen 与 executeOpenMany 混为一个区域的缺陷；两区域现在各自精确检查首次写前的一次标脏，合计 23 个 direct-authority caller。
- 调用点门分别核对旧入口扫描数与 strict/debounce 分类数，增加 CDATA 实际语句、reason、dirty guard、返回消费和编译可达符号的检查。

R1 基础施工不扩张 R3b schema/clock、Host/runtime DLL、发布身份与共识；随后现场反馈增量见文末。旧 API shim 保留，完整发布周期尚不能由工作树源码完成代签。没有执行 commit、push、tag、workflow dispatch 或 runtime promotion。

## 自动验证

本机新鲜证据位于 `tmp/r1-closeout-20260905/`，不纳入版本库；可复现命令由工具 README 与 testing-guide 管理。

| 门 | 结果与证明范围 |
|---|---|
| 调用点清单 | PASS；38 物理点 / 32 逻辑点，scripts/XFL 旧入口均为 0 |
| Node 合同 | 17/17；包括错误 API/reason 负例及实际 SceneChanged 回调顺序 |
| Map loot | 676/676 = Loot 259 + Planner 12 + Stage 405；原 675 项保留，补 N=50 边界；反馈前基线 |
| Character build | 824/824，其中 SaveManager 352/352；新增 N=1/2/20/50 wrapper→pack/doSaveAll/SOL 的精确 N+1 与场景 clean/pending 物理门 |
| PAT | 117/117，direct-authority 23；Compiler 0/0、32K retry=0 |
| KShop | 75/75；Compiler 0/0、32K retry=0 |
| NPC | 66/66；Compiler 0/0、32K retry=0 |
| XFL 校准与资产扫描 | 10 目标三件套已跑；8 个 audit CLEAN，主 XFL/战宠报告既有命名及引用项，rename/fix_includes dry-run 均无冲突/失效 include；20 份改动 XML 的非 CDATA 字节内容（统一换行后）与 HEAD 一致，linkage scanner 已重扫到临时证据目录 |
| CS6 / FFDec | 11/11 实际发布，均 fresh Compiler 0/0；10 份 XFL 导出合同通过。按下表仅保留 5 个更新；其余 6 份导出证明的是临时试编产物，不能冒充恢复后旧 SWF 的迁移证明 |
| 最终产物 | 本轮 11 份文件 hash 由 manifest 核对；反馈前 asLoader 10,986 函数 / 最大 55,798 B，低于 60,000 B 门；single-ownership：main 0 个 org 类，asLoader 626 个，交集 0 |

Reward 的 save-image restart/fault-cut 由 map-loot 原有测试继续覆盖；新增物理门从真实 Reward wrapper 委托到 SaveManager 与独立测试槽 SOL。该组合不冒充正式玩家进程的杀进程旅程。

## 本轮 XFL 生产归属与产物处置

代码归属必须由入口、派发、时间轴实例和共享库闭包共同判断。独立 XFL 能编译、asset_source_map 中有 linkage、遗留 gameCommands 仍定义，均不足以证明正式入口会使用它。`pairRole=live/mainCopy` 是历史源码位置标签，不是本轮产品 live 结论。

| 产物 / 调用点 | 核对依据 | 本轮处置 |
|---|---|---|
| asLoader / A2、B1 | RewardInboxService wrapper 与 SceneChanged hook | 保留发布；现役服务 |
| main / E1 | 淡出动画进入主时间轴闭包；FFDec 仅命中 E1 | 保留发布；主库其他副本仅源码 parity |
| 平板 / E7 | `LauncherCommandRouter.cs` 的 TABLET → `toggleTablet`；主时间轴导入平板 | 保留发布；现役 Flash |
| 基地特殊 UI / E6 | `flashswf/levels/基地场景合集/LIBRARY/地图/医务室.xml` 仍以 `从库中加载外部UI("整容整形界面")` 打开整形，主时间轴加载该共享素材库 | 保留发布；医务室整形与 Web Hairdresser 理发是不同业务 |
| 奖励物品 / C3、C4 | main 仍实例化 `sprite/奖励物品界面` 并从独立 SWF 导入；旧道具资产也保留调用 | 保留发布；兼容导入仍在，不因 Web loot 存在就删除或宣称业务已 dead |
| 商城 / C6 | SHOP → Web `kshop`；AS2 `openShop` 仅提示使用 Launcher | 源码迁移保留，旧 SWF 不刷新 |
| 战宠 / E8 | PETS → Web `team` 的 partner 页；main 宠物层无实例，旧 `togglePets` 定义没有现役 Host 派发 | 源码迁移保留，旧 SWF 不刷新 |
| 物品与技能 / E9–E12 | 仓库、装备、NPC 商店和技能均走 Web；main 的禁回流门明确禁止该共享库导入 | 源码迁移保留，旧 SWF 不刷新 |
| 玩家信息 / E13 | E13 的旧长方块按钮未进入该独立 SWF 编译闭包；主 HUD 仍有 Flash 部分，NativeHud B0 不能外推整体替代 | 仅保留未引用按钮的源码修改，不刷新、不退役玩家 HUD |
| 系统设置 / C5 | GAMESETTINGS 与 AS2 openSettings → Web `settings`；迁移规范明确旧 Flash 设置不可达 | 双份源码 parity 保留，旧 SWF 不刷新 |
| 获得任务奖励 / C1、C2 | TASK_UI → Web `tasks`；main 的“获得任务奖励”层已清空 | 双份源码 parity 保留，旧 SWF 不刷新 |

Host 入口见 [LauncherCommandRouter.cs](../launcher/src/Guardian/LauncherCommandRouter.cs)；AS2 命令见 [UI 管理](../scripts/展现/UI交互/UI交互_lsy_UI管理.as)；显示层边界见 [迁移护栏](../agentsDoc/as2-web-panel-migration.md)。具体当前 hash、体积和 `r1Disposition` 只由 [manifest](../tools/save-api-migration/callsites.v1.json)维护。已排除产物的试编结果位于 `tmp/r1-closeout-20260905/excluded-published/`，恢复前已核对备份与 HEAD 完全相等；没有覆盖接手前的 SWF 改动。

## 正式入口复验与后续

本轮未在正式玩家进程执行游戏旅程，业务复验仍为 `FIELD_REVALIDATION_PENDING`。后续从现役入口检查 Web 收件箱领取/连续开箱、场景切换及保存后重启恢复，并覆盖本次实际更换的平板关闭与医务室整形。奖励物品共享库按兼容范围复核；已由 Web 接管的任务奖励、设置、商城等旧按钮不再列为必须寻找的人工验收旅程。E3/E11/E12 的 opt-in reason trace 仅用于后续退役调查，未命中不能单独证明 dead，不因此阻塞已授权的机器工作。

编译未引用的库副本、以及脚本由独立 SWF 提供的 runtime shared import 副本，只能对本地副本声明源码 parity；FFDec 门按时间轴/linkage 可达图精确核对实际生成的调用，`sourceOnlyCallsites` 不代表已通过游戏内 live/dead 验收。完整 R1 发布周期之后再评估 shim deprecated；R3b 继续作为独立阶段，不能与本补丁或同开关混合。

## 持久材料与本机临时文件

`.workbuddy-ai/` 已按本机会话目录忽略，不清除其原始记忆。根目录的 `pat-full.log`、`run-map-loot-last.log`、`run-pat-last.log` 与机器绝对路径包装器 `scripts/_pat-wrapper.ps1` 移入 `tmp/r1-closeout-20260905/incoming-workbuddy/`；正式 runner 仍在 `scripts/`。Flash 发布附带的 `flashswf/UI/*.html` 包装页忽略，嵌套的开发 harness 不受此规则影响。

SaveManager/R3b 与 K店 batch/openMany 两份原始裁决完整落入 `docs/裁决存档/`，各带原始 SHA-256；正文未裁剪。R3b 七项仲裁语义、六阶段 reader-first、reader 不回退和 R1/R3b 分开列车的约束不再依赖 WorkBuddy 或 tmp。交接入口与 ADR 已更新持久引用、loot 待复验队列、NPC 已迁 wrapper 的事实，以及 SaveAutoRepair 启动直写须纳入 R3b 的范围。旧工作量估算移到临时历史材料，不能继续据其“9 个 SWF/19 个 XML/必须人工数日”推导当前范围。

可复用检查器放在 `tools/save-api-migration/`；源码留在所属 `scripts/`、XFL 库目录；本轮保留的运行时 SWF 留在各自正式路径。完整试编目标、编译日志、FFDec、原始备份和临时排队脚本仍归 `tmp/r1-closeout-20260905/`。这些证据记录实际发生过什么，不能将全量历史队列重新当成未来必须发布的清单。

本轮原始异常另保留：PAT 旧区域扫描失败；编译图初版未排除 runtime shared import 导致 C3.main 误报（已修正并对当前主 SWF 重新核验通过）；NPC runner 包装名称写错，随后使用正式 `run-item-panel-tests.ps1 -Suite Npc` 并通过。没有放宽存盘次数或把失败记录改写成首次成功。

legacy shim 仍须保留：本轮有意不刷新旧 UI 产物，不能把“源码旧入口归零”说成所有历史 SWF 旧入口归零。完整正式发布周期之后再决定 shim 生命周期，不做本轮隐式删除。

## 验收反馈增量：礼包奖励合并与领取超时

2026-09-05 用户多次复现大量开包后的“需要核对”，手动重查可正常完成。冻结日志 `tmp/loot-reconcile-20260905/launcher-observed.log` SHA-256 为 `E7FAF6D62CB71295A4EE1295A439F8A2D3967ACE59E84747654EF3ED2ADB1E19`。6 次 `claimBatch` 转发到后续 `loot_response` 的间隔分别为 8.215 / 14.224 / 16.585 / 3.190 / 18.408 / 3.175 秒，对应 24 / 43 / 50 / 10 / 50 / 9 个去重 child 资产通知。响应正文被日志脱敏，不能把间隔冒充抓到精确 callId 的 timeout 事件；但 08:56:16.352 的手动 query 已在原 50 项响应之前转发，结合 Host 10 秒超时、写 pending 时拒绝 query 的代码，支持“领取已继续执行，Host 等待先超时”的诊断。该会话 205 次 `flush=true` 与 205 次 shadow 确认 true，未观察到保存失败标记。

43 项批次仅有 9 种可堆叠物品；18.408 秒的 50 项批次仅 11 种，其中普通 hp 药剂占 33 项。另一个 50 项批次只有 6 种；24 项批次有同名装备，装备必须逐件保留，不能按名称全部缩到 6 项。这些是日志分组估算，不能声称新版本实机耗时已测得。

修复分两部分：

- `ItemUseService` 保持 K 次 RNG 原始 draw 顺序，全部结果冻结后，在本次操作内合并同名、数值型消耗品/收集品。合并归属为首次出现的 package/位置；装备不合并，安全整数相加会越界时保留独立条目。单开同样合并包内重复项。容量 64 检查与单开 capability 上界均按合并后条目计算。
- K 个 ordinal/唯一 batchId、K 次 staged append、一次扣 K、一个 operation receipt、一次开包 fence 保留。`packages[].entryCount` 表示本包在归并后实际持有的新增条目数；0 可表示未命中，也可表示全部并入前包。这个口径替代早期裁决“0 仅来自未命中”的假设，不改字段集合。旧 receipt 仍按原值 exact replay；不重写已有 ledger、已冻结 root 或旧存档。
- `RewardInboxService.advanceActiveRoot` 在 750ms 预算后从 durable child 边界让出，至少处理一项；健康让出是 `pending/in_progress + 空 error`，不附资产投影。Web 保持忙碌并查询相同 `rootOperationId` 续办，绝不重发领取。3 次连续无进展、真实 `commit_pending`、断连、畸形/倒退回包或 detached 会停止自动续查并保留人工核对入口。原 N+1 保存次数不变；单个不可中断保存本身若超过 Host 超时，仍需重查。

增量自动门已通过：CS6 fresh map-loot **684/684 = Loot 267 + Planner 12 + Stage 405**，runId `431a51ad62d94b0bbe0c57dfedb06d5a`，Compiler 0/0、32K retry 0。新增的 8 项断言覆盖压缩数量/装备、安全整数、容量、N+1 与分段 save-image 重启；旧 RNG、失败回滚、receipt replay/conflict 测试继续通过。Web state **80/80**，生产 Edge harness **104/104**（含可见“领取中”与自动完成关闭），lazy cancel **6/6**，player copy **322 字面量 / 0 forbidden**。原 N=50 保存门按同 root 的健康 query 完成后计数，51 次保存不变。增量只需刷新 `scripts/asLoader.swf`；Host/Core 与其他 XFL 产物不受本次反馈影响。原 R1 自动验证表是本增量前的已完成记录；最新 asLoader 已由 CS6 publish：**1,242,629 bytes**，SHA-256 `8026A67116070A1FFCF05893606F585780D1BBF831A737AC2D36D062500CA37B`，fresh Compiler **0 个错误 / 0 个警告**，10,988 函数 / 最大 55,798 B；FFDec 确认 `compactRolledPackages` 与 750ms 分段分支进入实际产物。JSFL 已发送 `-QuitFlashAfterPublish` 退出；因本轮 TestLoader 播放器残留，确认没有打开的创作文件后已清理本轮新启动的 CS6 进程。2026-09-05 用户在本轮复验后反馈“均有效”，奖励合并与自动续查的现场功能复验通过。该反馈只覆盖本增量旅程，不外推其他 R1 验收点、候选身份或正式入口 standard_entry_verified；没有取得新版本耗时量化。

## 验收反馈增量：手动存盘完成提示卡住

2026-09-05 用户报告点击右上 × 后持续显示“存盘中”。冻结日志 `tmp/safeexit-packet-20260905/launcher-observed.log` SHA-256 `1C56A756E87E02246B625E353E70AAA1687218686BC93D71971CEC243980EA35`：10:29:27.225、10:29:36.985 均实收 `sv:1|sv:1|q:77|sv:2|q:77`，随后都有本地 `flush=true` 与 shadow confirm true；不是这两次保存未写入。日志启动路径属于隔离候选，不据截图或用户反馈外推正式入口状态。

根因是 NativeHud 的终值快照与 SafeExit fallback 的 delta 字典都把同包 sv:1 覆盖成 sv:2，而 Arm 门正确拒绝未见开始的完成信号。修复让主路和 fallback 均顺序消费原始 s/sv，终态相同的重试也不能去重；一包只提交一次显示变化，其他 widget 保留快照去重。未放宽 Arm、失败及一次性退出能力，没有改变保存调用、AS2 或 SWF。本轮新增 14 项真实 packet 回归在修复前 7 失败 / 7 通过；修复后相关 Launcher 分区 174/174 通过，完整 Launcher runner 4,600 通过 / 3 跳过 / 0 失败（总计 4,603），exact SDK 与串行策略检查通过；文档治理通过。隔离候选已构建并通过 33-file integrity-only 校验，状态为 `candidate_built / NOT_DEPLOYED`；完整生产 policy、候选游戏旅程与正式 promotion 未执行。候选目录 `tmp/runtime-candidates/v2/c-c3cb3b79fb19-08846e81b3-20260905t025129343z-b805eceb`，build identity `C3CB3B79FB19AB87DD6AFE4EEACC192D9CAD561C7F2B85FA27569549F19D5242`，payload closure `5F0C06EA3BCC2233DADE6AE3DFF8F81B2CE7A75F13A03B7D55007D20B6C70244`，Core SHA-256 `45E0317FF618671BE04F335D12361BB010A37B763DDEE989CA8F5A0B6CC839CA`。本地开发选择索引已更新，重新运行根 `本地开发启动.cmd` 会校验并选择该候选。正式 35-file 闭包构建前后 fingerprint 均为 `A520CE300F38A293C46E06823CC56B3F059B800CB8F04375B2128527944EE01B`，asLoader SHA-256 仍为 `8026A67116070A1FFCF05893606F585780D1BBF831A737AC2D36D062500CA37B`。最小现场复验：重开后连续两次点击 ×，均能进入“存盘成功”并取消返回；本次未代签此游戏旅程。

本增量当时尚未施工存盘图标。性能核对发现 NativeHud 每次视觉更新会重绘所有可见 widget 并提交整个 bounds union，新增右侧区域还可能扩大合成面；复用窗口不能等价为零成本。后续用户已选择复用 ×，施工结果见下一增量。

候选首轮构建在当前 PowerShell 7.6.5 的 Rust 阶段失败，原始记录保留在 `tmp/safeexit-packet-20260905/`。独立复现显示 `check-runtime-build-env.ps1` 调用 SetEnvironmentVariable(null) 后，7.6.5 仍留下空的 CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_LINKER，rustc 命令带空 linker 参数并报无法提取文件名；Windows PowerShell 5.1 对照运行正确删除该键。本轮改用现有本地开发入口同款 Windows PowerShell 5.1 重新构建，不修改构建 recipe 或工具链锁。临时 Rust 诊断目录位于本机 LOCALAPPDATA/CF7/safeexit-rust-diagnostic-20260905；自动审批拒绝其递归清理，未提供具体原因，目录保留且未放入运行时或仓库。

## 验收反馈增量：多轮领取进度被 Host 截断与 × 保存反馈

用户在前述隔离候选再次复现“第一次可以自愈、第二次持续需要核对”。这份新证据收窄了此前“均有效”的结论，不能继续声称所有多轮自动续查都已现场通过。冻结日志 `tmp/loot-stall-save-feedback-20260905/launcher-observed.log` SHA-256 `1415B1D84BC33660BCA82CBDC58A0CE0D513E47A19B0A96F2CF70DC9991C2292`：11:12:27 的批次在首次自动 query 后停止，11:12:50 手动 query 才完成；11:13:16 的材料批次首次领取处理 3 项，自动 query 推进至 5，11:13:37、11:13:50 两次手动 query 分别推进至 8、10。这些回包耗时约 0.8–1.4 秒，尚未触及原 Host 10 秒超时。每段都有 flush=true 与 shadow confirm true。只读冻结的 shadow 显示 root 共 14 项、cursor/appliedCount=10、剩余 4，下一子项为 PREPARED 的弹簧；rootStatus=pending、error/stopReason 为空。未编辑玩家存档。

根因在 `LootTask`：首次 pending 设置 reconcile_required 写栅栏是正确的；后续 query 尚未 terminal，也理应保留栅栏。但原分支同时把经校验的健康 pending 进度替换成通用 reconcile_required，Web 收不到继续推进的信号。短批次在下一轮直接 terminal 可以自愈，较长批次每次按“重新核对”只推进一段；UI 为避免混合资产投影继续保留旧快照，看起来完全没动。这与早先未合并导致的长批次超时是不同缺陷。

修复只改 Host：同一 active binding、同一未知 root、revision 不低于 watermark、已通过 sanitizer 的 pending query 原样转发进度/error；写栅栏不清除，不更新待决资产投影，不授予关闭。真实 commit_pending 仍触发 Web 停止自动续办，foreign/stale/mixed/detached 仍拒绝。request/reply 日志新增 callId 关联，以及 rootStatus/applied/error/forwarded，便于以后分清“已推进但展示被阻断”与真实存盘失败。不重发 claimBatch，不改 AS2 事务、保存次数、超时或 Web 恢复预算。

用户已选择“保留 ×，在按钮内部短暂显示保存状态，平时静止”。现由 `RightContextWidget` 内的 `SaveFeedbackState` 实现：存盘中静止琥珀底线；成功绿色边框/小勾依次亮起、保持、淡出，1.2 秒后回到原按钮；未确认静止琥珀警示。× 和点击行为始终保留。悬停复用已有提示行展示最近完整保存成功时间及本次就绪阶段收到的 sv:2 次数；s:0 清空，不宣称进程之前或云端保存计数。sv:2 只说明完整本地 flush 成功，不能代签异步 shadow 完成。模型不调用保存、不授予退出能力；同帧 sv:1→sv:2 只展示最终事实，不倒放“存盘中”。

没有新窗口、素材、字体或扩大的 HUD 合成/命中区。单次成功最多四次阶段重绘（含清除），连续成功合并视觉、逐次计数；空闲/存盘中/未确认均不申请动画 tick。NativeHud 仍会在这些重绘点绘制整个现有 union，因此不能宣称零合成成本。Web 全屏遮挡时不加旁路浮层，成功按单调时钟自然过期、恢复后不补播，最后成功信息仍可悬停查看。`SaveFeedbackStateTests` 用 10,000 次时钟推进验证四次重绘/停止，用 100 次保存验证视觉合并与成功计数分离；四档缩放校验所有新增像素留在按钮内。真实 NativeHud/BeginInvoke 测试验证相同终态不去重、非空 bounds/union 不变、SAFEEXIT 路由不变及自动保存不能获得退出许可。

自动验证：新增 6 项 Host 回归在修复前 3 失败/3 通过，修复后 `LootTaskTests` 146/146；保存反馈/右上 HUD/SafeExit/NativeHud 分区 133/133；完整 Launcher runner **4,616 通过 / 3 既有跳过 / 0 失败（4,619 总计）**，exact SDK 与串行策略检查通过；Web loot state **80/80**。预览使用实际 C# RightContextWidget、仓库字体目录，检查 1024×576 与 1600×900 两档，产物与原始日志留在上述 tmp 目录；它是离屏渲染验证，未代签游戏旅程。新图标无需刷新任何 SWF，本增量 asLoader 保持 `8026A67116070A1FFCF05893606F585780D1BBF831A737AC2D36D062500CA37B`。

本增量隔离候选已构建，33-file integrity-only 通过，状态 **candidate_built / NOT_DEPLOYED**；未执行 production policy、候选游戏旅程或 promotion。目录 `tmp/runtime-candidates/v2/c-ed88cf84351f-08846e81b3-20260905t034248236z-53a11739`，build identity `ED88CF84351FDED824CA2A74E57536B40CA75BBB6F1879EF32EA65AACF949606`，payload closure `5AF5672DBD7F21FE5245EFEC89206C5B077E5C4F61FDDBD5073F68A3AB1E4E44`，Core SHA-256 `09C6F0D7F58F852597325F293E18CE35EDED382694BCA7EE9502AE619055A626`。本地开发索引与当前源码匹配，`automation/dev.ps1 -Status` 为 ready；根 `本地开发启动.cmd` 重开会选择并重新校验该候选。构建使用 Windows PowerShell 5.1，未改 recipe/toolchain。正式 35 文件与构建前逐一比对未变，Core 仍为 `3948B44863DC42757EAEC42117470821F8EF1CEEDBB57DAD65B1058268B1C8DE`；asLoader 未变。结构化证据位于本增量 tmp 目录的 result.json，文档治理通过。

本增量待人类复验：重开本地开发入口后，连续多包领取应在正常分段期间保持忙碌并自动收尾；观察 × 的成功提示随后恢复静止，悬停能查时间/次数，连续两次手动保存均应进入完成并能取消返回。这两项是新候选的现场确认，不能用上述单测/离屏绘制替代。原 R1 的其他旅程边界仍按此前章节保留。

2026-09-05 用户反馈“可行”，并明确授权合并上游、取得云端共识、构建部署推送。后续正式发布将以合并后的源码、重新构建的 Flash 业务产物及 v2 双故障域共识为准；本节候选身份保留为合并前验证记录。

## 合并上游后的正式部署

本轮源码以 `962dbbb701` 提交，再将上游 `bbdb1fac1de053f195c3d041f534edbc7a30cb7a` 合入 `5891ce2631`；`RightContextWidget` 保留双方改动。冲突只涉及 asLoader 产物和调用点 manifest：按合并后的实际 AS2 源码重新编译 asLoader，保留 R1 的结构化归属记录并校正上游新增行号。保存修复字典由现有生成器刷新排序/元数据，物品与发型集合未变；最终冻结 source `5a9d06210244bedebac41d19579ca1921c362182`。

| 合并后验证 | 结果与边界 |
|---|---|
| CS6 map-loot | **684/684**，runId `3281edd87ba04c3ea8fac8f441cf2d65`；fresh Compiler 0/0，32K retry 0；TestLoader 测试内容已恢复 |
| asLoader 发布 | **1,267,316 bytes**，SHA-256 `AF8B9566BA6EAFD49DBD8820B49AED7568EAE7C84472766AD136EFAAD0FC2EC5`；fresh Compiler 0/0；11,207 函数，最大 55,798 B |
| R1 manifest | 38 个物理 / 32 个逻辑调用点；5 个保留 SWF 的当前 hash 全通过，其他 4 个 SWF 无上游冲突，沿用本轮已验证产物 |
| Launcher 完整 runner | **4,713 通过 / 3 既有跳过 / 0 失败（4,716 总计）**；exact SDK 和串行策略通过 |
| Web / 派生资产 | loot state **80/80**；材料目录、19 项 release 生成物与冻结源一致；文档治理通过 |
| 正式发布 | production policy **40/40**；本地 X509 与 GitHub OIDC/Sigstore 双故障域闭包一致；原子 promotion 与正式根 bootstrap `--verify-only` 通过 |

首次合并后完整 Launcher 测试有 1 项在 Wings `AuthorizeRead` 设置阶段报 `human_only_surface_publish_failed`，原日志保留；CS6 结束后该分区 21/21 通过，随后完整 runner 达到上表结果。未修改或放宽测试；并发 GUI 活动与失败时间重合，但未证明因果。全部发布期日志归 `tmp/release-r1-save-feedback-20260905/`，queue/CAS 位于本机 `C:/qsf95`；长期签名证据由正式 consensus 内嵌保存，不把临时日志或候选入库。

不可变 tag 为 `runtime-build-v2/20260905-r1-reward-save-feedback-v1`；正式 request、产物身份、部署提交与远端审计统一见 [发布真源](runtime-build-reproducibility.md)。本轮自动门覆盖合并后的代码和实际 SWF，未在正式入口重跑业务旅程，状态保持 **promoted / FIELD_REVALIDATION_PENDING**。现场复验仍按前文现役入口范围进行，重点为连续多包领取自动收尾、连续两次手动存盘，以及 × 保存成功提示恢复静止；不要求重新寻找已迁到 Web 的旧 Flash 按钮。
