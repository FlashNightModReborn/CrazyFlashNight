/**
 * FLAG_CHAIN - 联弹子弹类型标志位
 * 
 * 位掩码值：1 << 1 = 2 (二进制: 00000010)
 * 位置：第1位
 * 
 * 功能用途：
 * • 标识联弹类型子弹，如散弹枪、多重射击、连发武器等
 * • 发射逻辑：联弹类型一次发射多发子弹，但发射次数固定为1
 * • 散射控制：联弹子弹无随机散射偏移，使用精确射击角度
 * • 碰撞检测：选择专用的多边形或覆盖范围AABB碰撞器
 * • 生命周期：使用特殊的帧处理器，包含坐标点检测和特效处理
 * • 霰弹值处理：基础素材名提取通过分隔符"-"前的部分获得
 * 
 * 使用示例：
 * • 检测：(bullet.flags & FLAG_CHAIN) != 0
 * • 设置：bullet.flags |= FLAG_CHAIN
 * • 清除：bullet.flags &= ~FLAG_CHAIN
 * 
 * 应用场景：
 * • 子弹工厂的发射计数和霰弹值计算
 * • 碰撞器工厂的智能选择系统
 * • 动态帧处理器的性能优化选择
 * • 基础素材名的提取和缓存处理
 */
var FLAG_CHAIN:Number        = 1 << 1;  // 联弹标志位 - 位值: 2 (第1位)