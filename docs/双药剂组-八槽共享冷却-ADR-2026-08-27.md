# 双药剂组与八槽共享冷却 ADR（2026-08-27）

> 当前状态：`HUMAN_ACCEPTANCE_PASSED / promoted`；正式 identity/lifecycle 窄纵切 `standard_entry_verified`
> 2026-08-30 affinity 增量：`IMPLEMENTED / promoted / FIELD_REVALIDATION_PENDING`
> 适用范围：AS2 药剂输入与存档、玩家信息 HUD XFL、Character Build、Settings、Launcher Host/Web。
> 本文冻结产品语义和跨层契约；自动化通过不代表真实游戏观感、手感或发布验收。

## 1. 决策

- 药剂背包固定为 `2 组 × 4 通道 = 8 个物理槽`，不把可同时使用的冷却通道从 4 扩为 8。
- `6` 为“药剂组切换”默认键，`7 / 8 / 9 / 0` 继续对应 4 个冷却通道。
- 上下两组的同列槽共享同一条 `drug:0..3` 冷却；切换使用独立 `drug:switch` 冷却。
- 首个候选的切换冷却为 `3000 ms`，由独立根参数提供，不与 `_root.吃药冷却时间` 共用存储。真人手感验收可以只调整该参数，不改变协议。
- 战斗 HUD 顺序固定为 `6 | 7 | 8 | 9 | 0`。Character Build 显示两排四列药剂槽，不创建第九个“切换槽”。
- 活动药剂组是进程内会话状态：死亡、场景转换不重置；成功读档、新建角色和删档清理重置为第 I 组；不写入存档。

## 2. AS2 权威模型

`DrugInputService` 必须显式分离三个维度，不再用一个含混的 `SLOT_COUNT` 同时表示输入、冷却和容器容量：

```actionscript
BANK_COUNT = 2
LANE_COUNT = 4
PHYSICAL_SLOT_COUNT = 8
```

物理槽与通道映射：

```text
physicalSlot = bank * 4 + lane
bank         = floor(physicalSlot / 4)
lane         = physicalSlot % 4
cooldownKey  = "drug:" + lane
```

切换状态机：

1. 每帧先读取“药剂组切换键”，再处理 4 个药剂通道。
2. 切换只在物理键的上升沿尝试一次。即使当时 `drug:switch` 仍在冷却，该次按下也必须被消耗；持键等到冷却结束不得自动切换。
3. 切换成功时翻转活动组，启动 `drug:switch`，并把 4 个药剂通道全部置为“必须先松键”。
4. 切换成功的同一帧不使用药剂；切换键因冷却被拒绝时，不额外封锁正常药剂输入。
5. 切换不调用任何 `drug:0..3` 冷却的 reset/start，因此上下位冷却永远连续。
6. HUD MovieClip 只是投影。MovieClip 缺失、重绑或可见帧切换失败都不得成为输入或冷却权威。

`ManualCooldownService` 新增独立 switch key 与按 key 重置能力。按 key 重置只用于成功读档、新建角色或删档清理的会话边界，不在暂停、死亡、场景转换或切换药剂组时调用。

## 3. 存档与旧档迁移

不提升 `mydata.version = "3.0"`。双组布局最初使用 v2；2026-08-30 起，耗尽槽位 affinity 进入同一 feature 的 v3：

```text
mydata.ext.drugLoadout = {
  version: 3,
  slots: [
    { itemKey: string, lastDepletedSequence: integer }, // 精确 8 行
    ...
  ],
  nextDepletedSequence: positive integer
}
```

读档规则：

- 无标记的历史存档只保留药剂键 `0..3`。构造 8 槽容器前必须丢弃所有越界键，尤其是旧 `ArrayInventory` 曾读入但因容量过滤而隐藏的 `4..7`，防止扩容后“幽灵物品”复活。
- 完成历史幽灵键清理后，把无标记/v2 存档归一为 v3；当前已占用药剂槽绑定其物品名，空槽初始无绑定。迁移状态必须进入权威全量存盘。
- 已标记 v3 的存档保留精确 8 个 affinity 槽；`packGameState` 不得再盖回 v2。版本大于 3 必须原样保留并 fail closed，不能由旧运行时降级或清空。
- 新角色直接建立 8 槽空容器和 v3 空 affinity。
- 旧槽 `0..3` 继续是第 I 组；不做物品平移、复制或索引换位。

`packGameState` / SOL / Launcher snapshot / JSON shadow / `loadFromMydata` / 新建角色必须共用同一布局规则，不允许只修本地 SOL 分支。

### 3.1 获取、耗尽与手动换槽的确定性规则

- 药剂变为 0 并从物理槽删除时记录 `{itemKey,lastDepletedSequence}`；序列越新，恢复优先级越高。序列只表达同名药剂的最近耗尽位置，不是时间戳，也不改变四条共享冷却通道。
- 获得药剂时依次选择：同名且为空的 affinity 槽（最近耗尽优先、槽号作 tie-break）→ 已占用同名药剂最低物理槽 → 背包同名栈 → 背包最低空槽。八槽都参与规划，不依赖当前活动组。
- 同名药剂同时存在于第 I/II 组时固定合并最低物理槽；从未绑定的新药剂继续进入背包，避免奖励或商店购买擅自改写玩家配装。
- 手动 equip/unequip、Character Build mutation、`DrugIcon` 旧入口、任务/商店/拾取的 `ItemUtil` 路径和关卡 Loot v2 必须调用同一 `DrugSlotAffinityService` 归一/规划/提交规则。跨容器提交先冻结物理 before 与 affinity before；目的写、affinity 写、来源删除任一步不确定都保留 journal 或精确回滚，不能复制药剂。
- affinity 是存档事实，活动药剂组仍是进程会话事实；读档只恢复槽位记忆并把活动组重置为第 I 组。

## 4. 键位迁移

Settings 注册新逻辑 ID `药剂组切换键`，默认键码 `54`（数字 `6`）。键表从 35 行升为 36 行，AS2、C# Host、Web runtime 与 harness 必须使用同一 ID 顺序。

历史玩家可能已把其他动作改到 `6`。迁移必须：

1. 保留每个历史逻辑 ID 的有效自定义键码，不抢走旧绑定。
2. 只对缺失的“药剂组切换键”选键：若 `54` 空闲则用 `54`；否则按冻结的人体工学候选 `T(84) → Y(89) → V(86) → X(88) → Z(90)` 选择首个空闲键，再按默认表键码顺序和 KeyManager 合法键码升序兜底。所有阶段都排除 `Esc` 和 `F1..F12`。
3. 结果必须是可在 Settings 中直接应用与保存的无冲突表。
4. 快照应给 Web 一条有界的迁移提示，说明保留了旧 `6` 绑定，并显示新动作被分配到的实际键。成功存盘后清除 pending 提示。
5. “恢复默认”始终把新动作设为 `6`；正常改键仍由现有冲突校验裁决。

Settings 顶层 domain envelope 继续使用 v1，以避免把本次键表增量扩散到刚完成的复活/返回基地等无关命令；携带键表的 snapshot/apply 必须精确增加 `keySchemaVersion:2`。Host/Web/AS2 对该子协议 fail closed，不通过 `keys.length` 暗猜版本。

## 5. Character Build 严格投影

现有顶层 Character Build v1 request/response envelope 不改名；药剂布局新增严格 `drugLayout.v=2` 子协议，不通过数组长度暗猜布局版本。

```text
drugLayout = {
  v: 2,
  bankCount: 2,
  laneCount: 4,
  physicalSlotCount: 8,
  activeBank: 0 | 1,
  switchKeyLabel: string,
  switchCooldown: {
    ready, totalSteps, currentStep,
    progressPercent, animationFrame, remainingMs
  }
}
```

`drugs` 固定 8 行，每行在旧药剂投影字段上显式增加：

```text
slot: 0..7
bank: floor(slot / 4)
lane: slot % 4
active: bank == drugLayout.activeBank
```

- `keyLabel` 从 lane 的 `7/8/9/0` 绑定获取，上下同列一致。
- 同列两行投影同一 `drug:lane` 冷却，并在该冷却激活时共享 equip/unequip mutation lock。
- C# 必须验证 `bank/lane/active`、数组长度和 cooldown 同列一致性；Web 不重算权威映射。
- Web 使用两个明确的组容器呈现两排四列，标记第 I / II 组与当前组；候选、tooltip、拖放、键盘漫游、equip/unequip 必须覆盖全部 8 个物理槽。

## 6. HUD / XFL

`玩家信息界面.xfl` 的五个背景格与整体宽度保持不变：

- 第一列复用 `控制器4 + 进度条4`，绑定“药剂组切换键”和 `drug:switch`。
- 原 `位置示意0..位置示意3`、`进度条0..进度条3`、`控制器0..控制器3` 整体向右平移一列，实例名与逻辑 lane 不变。
- 删除休眠 `位置示意4`，在同一图标层直接放置专用 `药剂组切换图标` MovieClip。它不设 linkage，不使用 `attachMovie`。
- 首候选的同色 `I / II` 在小分辨率真人验收中辨识不足，已废弃。修订候选以大轮廓 `○ / ×` 为第一视觉锚点、以同位置的高对比 `1 / 2` 为语义兜底；圆叉使用同一中性色，不照搬手柄品牌配色，也不让颜色单独承载状态。每帧 `stop()`；冷却条在图标上方，按键文字在冷却条上方。
- 四个可见 `DrugIcon` 在切换后通过现有 `CollectionIcon.reset(collection, physicalSlot)` 重绑到目标组，不销毁容器、不重建冷却、不修改任何物品。

PlayerInfo 必须使用独立 XFL 目标发布；AS2 service/UI include 变更另外发布 `asLoader`。本功能不需要发布 main XFL。

## 7. 验证和人类门

机器门至少覆盖：

- AS2：4 通道/8 物理槽映射、同列冷却、切换上升沿、持键不重触发、同帧抑制、死亡/场景/读档边界、旧档幽灵键、新档幂等性、键 `6` 冲突迁移、全 8 槽物品取得/提交/商店/合成涟漪。
- affinity 增量：v2→v3、v3 pack/unpack 不降级、future v4 原样 fail closed、最近耗尽原槽恢复、0/4 双组同名确定性、满背包仍合并药剂栏、从未绑定的新药剂留背包，以及领取/回包丢失/重启后的 journal 对账。
- Host/Web：Character Build 严格 v2 布局、8 行药剂、全 mutation 范围、同列冷却；Settings 36 键顺序、冲突迁移提示、默认恢复；`1024×576 / 1366×768 / 1920×1080` 下的两排布局、无横向溢出、键盘可达与 busy/error 状态。
- XFL：三件套、linkage scanner、唯一 Include/itemID、五列坐标/层级/参数结构门、精确 PlayerInfo publish、fresh compiler/output、SWF identity 刷新和 FFDec 两帧/实例/矩阵验证。
- 集成：Character Build、Settings、Workbench 结构门与 Launcher 全量回归；编译绿灯不代签真实游戏旅程。

2026-08-30 当前增量已由 fresh Character/SaveManager `211/211 + 231/231`、PlayerAsset `113/113`、Loot/Stage `546/546`、Launcher `4449 pass + 3 explicit opt-in skip / 4452 total`、Loot/KShop/Tuning Web 门、fresh asLoader publish 与 Panel contract `68/68` 闭合。异构复核前的早期隔离候选 identity `4B3DFEC54046494CFCC627212586D72F447D7E6E67FCDA9276569D3032A4FEB9`、closure `9CBEE0CDEA19B3B28BDDE9DB0B53489E816207A56175E06241DE93E683B855F2` 已被后续 B1/B2 修复 supersede，只保留为历史 `NOT_DEPLOYED` 证据；维护者已授权当前修复树进入正式供应链，但血瓶三来源回原槽与领取后重启读回仍为 `FIELD_REVALIDATION_PENDING`。完整路径、SWF identity 和证据边界见[关卡结果与基地结算 ADR](关卡结果与基地结算-CSharp-Web-ADR-2026-08-27.md) §0A。

affinity 增量已随 release source `5789d597fbb7af32753fe4a35887b1f2a3a34e10`、tag `runtime-build-v2/20260830-tester-feedback-stability-v2`、request `90E4BFB9875C73EE6672E89F404763C68E2BF33184AD2ED68421D5C975ECEED6` 与 deployment `3e23bda255dae09e20e309a12c5b21d86b28f347` 正式 promotion；identity `9B146D22C82925853757DCA8D63CDDBECD31CD19C43EC1771F6B26D8F5824CEE`、closure `C8B22C4532AD259E4C514BED30C87DC016E2FE17F9C001158F4CF18F900E1C94`、39/39 production policy、双 signer / 双 faultDomain 与 audit run `33289302965` 均通过。该部署不改变下述 `FIELD_REVALIDATION_PENDING` 边界。

首轮现场回归的集中窗口：

1. 战斗 HUD 的 `6 | 7 | 8 | 9 | 0`、`○ / × + 1 / 2` 辨识度、图标/数量/冷却遮挡与整体拥挤度。
2. 切换、切换冷却、同列共享冷却、不重置、持键不自动触发、同帧不吃药。
3. Character Build 两排的阅读和配装感受，含 1024 尺寸。
4. 一个历史存档的 `0..3` 原位保留、`4..7` 为空，以及存档/重启读回。
5. 默认 `6` 和“旧动作已占用 6”两条键位迁移路径。
6. 槽 4 的血瓶用尽后，经任务奖励、商店/拾取和关卡奖励各重新获得一次，均回槽 4；手动改槽后应以新槽为准。领取后重启再次核对槽位、数量与两个组的共享冷却。

原双药剂组列车仍保留下方已完成的人类验收历史。2026-08-30 affinity 增量则由维护者单独明确授权发布，上述现场旅程不再阻断 source tag、云端共识或 runtime promotion，但在测试员回报前不得声称该增量 `e2e_verified`、`HUMAN_ACCEPTANCE_PASSED` 或业务 `standard_entry_verified`。

首个隔离候选（native identity `6C9CF4699C217CC65083038D3AA69B0D6640C4E8DF5A50D367D0628E366D379D`、closure `1D0C3A1272CD084ECC9532B0565E017E07A233C1511B4372C091FF081701252F`）已由维护者确认功能有效、切换体验良好；但 `I / II` 在小分辨率容易看混，因此该轮只记 `HUMAN_FUNCTIONAL_PASS`。随后 PlayerInfo 改为同色大轮廓 `○ / ×` 并保留黄色 `1 / 2` 兜底，维护者再次确认“有效”并明确授权进入云端共识、构建与部署，因此产品门记为 `HUMAN_ACCEPTANCE_PASSED`。最终 PlayerInfo 资产不属于 native 33-file payload，不能仅由相同 native identity 代签；正式发布另以最终 release source/tree、XFL/SWF 哈希与自动门绑定修订后的资产。

截至 2026-08-27，本工作树自动证据已闭合：真实 CS6 focused suites 覆盖 player manual `486+57+55+14`、Character Build `674`、Equipment Tuning `78+170`，均为 Compiler `0/0`、32K retry `0`；Host focused `266/266`、Launcher `4308 pass + 3 explicit opt-in skip / 4311 total`；Web Character Build `222/222×3`、生产 workbench `1110/1110×3 + 12/12 + 18/18`、dressup `224/224`、Settings `18/18 + visual 116/116`，strict workbench audit `0 error / 0 warning`、ratchet `67/67`、item-grid `20/20`、visual atlas `66/66`。最终验收版 PlayerInfo SWF 为 `105,013 bytes`、SHA-256 `63FAD7E1DFCB28E2928FC6B7B7514D355ADDDA0286EFB936C0037FB948C2D764`、Compiler `0/0`；FFDec 已确认 sprite `92` 为两帧 `○ + 1 / × + 2`，第一帧四段曲线圆、第二帧对角叉、文字 `1 / 2` 与同色描边均进入产物。asLoader SWF SHA-256 为 `0CEBD6FAE41DE4CDF7DAB26926E0536968609F714432BE7E8403A52B43DF1415`。这些数字证明冻结 source 与最终验收资产的自动闭包。

正式发布绑定 implementation `9a85f95e748a986faca8f9a846ab360e2c7900d2`、release source `b2bc05775c621616fe64be55354aebe21c63a2af`、tag `runtime-build-v2/20260827-dual-drug-banks-v1`、tree `826f37292cb591782e3d3cc41145b9158a426032` 与 request `9FE68E5BEB945B969A2D4CDAA1D20B2D2838A4208DB8773EF7344918B0383658`。本地 X509 `physical-host-a` 与 GitHub Hosted OIDC/Sigstore run `33090620311` 对 identity `6C9CF4699C217CC65083038D3AA69B0D6640C4E8DF5A50D367D0628E366D379D`、closure `1D0C3A1272CD084ECC9532B0565E017E07A233C1511B4372C091FF081701252F` 达成双 signer / 双 faultDomain 共识；39/39 production receipt SHA-256 为 `361CAAEDD6322796631112882DB069C5B1C2F6599AF20685F9A20BB719BD6677`。deployment `6902b2b6ed067c4882e9a67267d055ce0db90b34` 与 audit run `33092179946` 已通过，明确输出 `state=promoted`、`deploymentChanged=true`。

部署后前两次 strict formal smoke 在 Default input desktop 持续 `GetForegroundWindow()==0` 时，surface 门按设计 fail closed 为 `trusted_runner_credential_timeout`，均不计成功。Default input desktop 上确认 `GetForegroundWindow()!=0` 后，复用相同 equipment 槽与同一 runner/harness 配置的无 candidate selector 第三次 run `2478a9f25873043318f2402f80105b52` 取得两次 verified status、同一 lifecycle、fresh handoff/reveal、可信 shutdown receipt、Guardian/Flash code 0、存档哈希不变且未触发强制清理；报告 SHA-256 为 `0F3349124141B5570B9FD51061AD736A71999CF1824DBF76B6AEBF50AFE41BF9`。这只把正式 identity/lifecycle 窄纵切推进到 `standard_entry_verified`；`businessJourneyExecuted=false`，没有从正式入口重跑八槽、切换、旧档/键位迁移或重启读回。

## 8. 发布边界

人类验收通过后才进入；本列车已完整执行：

```text
source commit/tag
  -> immutable Git-tree request
  -> registered local X509 builder
  -> GitHub Hosted OIDC/Sigstore builder
  -> same identity + payload closure
  -> production policy + strict v2 consensus
  -> atomic promotion
  -> formal entry identity/lifecycle smoke
  -> push and post-promotion audit
```

发布结论严格使用 `compiled → candidate_built → candidate_executed → e2e_verified → promoted → standard_entry_verified`。正式入口只检查 identity/lifecycle 不能冒充药剂业务旅程；若未在正式入口重跑八槽、切换和读回，必须明确保留该边界。
