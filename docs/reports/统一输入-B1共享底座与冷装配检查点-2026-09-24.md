# B1 施工检查点：共享合成、输入结算与真实资源冷装配

2026-09-24；基线 commit `5c02a103603ca60ccadd62348fe83753838fb0ca` 加当前未提交工作树。**B1 真实基地完整闭环尚未完成；本记录不是人验放行。** 原 R13 固定候选与入口保持不变。

## 已落地与实跑

- 将 C1 场景树实现抽到 `launcher/native/world-compositor/CompositionScene.cpp/.h`，默认 `FlashCompositorNative.dll` 提供独立 Scene ABI 1；旧 Compositor ABI 3 不变。私有 C1Scene.cpp 仅为兼容 shim，同一实现无复制分叉。
- `CompositionSceneHost` 在 Core 源码中管理线程、句柄、visual 引用和释放；C1 已是实际消费者。候选 producer 的 canonical source-copy 清单补齐 cpp/h，三个新增共享输入均在 artifact-source 域中。没有签名、发布或改正式 runtime。
- 不带私有 `C1Scene.dll` 的真实 C1 smoke 四项通过，证明已调用默认 native 模块中的共享实现。构建记录在 `tmp/b1-unified-base/native-build.log`、`endpoint-build-final.log`。
- END 物理完成后立即阻止纯 hover 抢走正在等待兑现的输入意图；实际发出 END 才开始期限，匹配 gesture/round/op 的终局回包才结算。无 opener 的空白点击会解锁，旧/重复/非 END 回包不误清新等待。成功结算与 HandoffCompleted 在同一 observer 回调完成。所有等待仅为逻辑等待，不清物理账本。
- 准备及 Web grant 回执期限绑定具体 round；超时 Fail，不能当成功或自动重放。合法 Held／WaitNeutral／外部前台不加失败期限。只为当前 WebController 接 ProcessFailed；Host 增加非只读业务的授予检查，C1 原本已有的三层付费拒绝保留。
- 新 AS2 端点由真实 CS6 编译，fresh Compiler `0/0`；新增纯状态反例测试。`endpoint-matrix` **37 次断言、35 个不同名称通过**，含真实 END 回包延迟后滑入 Native hover、空白点击恢复 hover、6 秒外部停留/持键、丢 prepared 后有界失败及旧键盘尾包。
- Launcher 全量 **5783 通过、4 跳过**，实际串行 runner 已确认。没有将这些机器结果扩写为 B1 普通游戏通过。

## 真实基地最早验证

在独立无 imports bootstrap 中先替换 SharedObject.getLocal 为显式拒绝函数，验证函数 identity 后才允许后续加载；探针没有调用原生 getter。真实 Flash Player 20 实跑证明替换可用、拒绝调用一次；新鲜编译记录和 PID 对应结果保留于 `cold-capability-run1`。

随后实际 RSL 导入 `基地场景合集.swf` 与 `things0.swf`，递归核到另外两份依赖：`素材库-地图元件.swf`、`新版人物文字信息.swf`。四份发布 SWF 顶层 DoInitAction 为零，未检出 SharedObject 字符串；这只是扫描事实，不代替全部脚本审计。没有手工修改任何发布 SWF。

`real-attachment-run2` 已核 TCP 对端为本轮独立 Flash PID：真实医务室与真实 `主角-男` attach 成功，跨 SWF 后拒绝函数 identity 仍完整。探针没有安装 `_root.初始化玩家模板`，其存在性检查为 false；`actorReady=false` 和 `inputGranted=false` 是该阶段的显式关闭状态。**资源装上不是演员可玩，更不是普通基地完成。**

此夹具暂为本机依赖实验，RSL URL 含本机绝对路径；不是可分发候选。后续需要确定性装配真实根服务、受控帧驱动、移动键态与 NPC 只读交互，并接入正常 Host 候选分支。

## SWE2 使用与采纳边界

本机 `devin models list` 列 `swe-2-max` 为 Free。已有独立 AS2 盘点、Host 接入、输入审阅、AS2 闭包补核及反驳报告。共享合成代码与 END 结算补丁由 SWE2 离线生成，主控核对后落地；修正了 native 导出声明/兼容编译边界，以及原补丁把超时写成 Revoke/重新尝试的错误，改为精确 round 的 Fail。

没有采纳“运行完整共享安装器后覆盖危险入口”的建议；它不能满足启动前隔离。没有采纳永久锁住 completed gesture 的 hover 修法，也不把已有 C1 写保护描述为已发生付费越权。原报告、反驳和完整导出留在 `tmp/b1-unified-base/devin/`，不把模型意见当运行证据。

Devin 的 accept-edits 非交互模式多次拒绝其自行调用的目录/编码检查 exec，相关任务虽退出0却无产物，不计成功。后续用户选择优先使用 smart 模式；2026-09-24 实际 swe-2-max 会话成功执行 PowerShell Get-Content，导出与 SMART_EXEC_SMOKE_COMPLETE 已核。仅为本批命令设置 --permission-mode smart，由 CLI 评估动作；未改变全局权限、未启用 dangerous。后续实施仍按文件归属拆单，由主控检查差异并统一编译实跑。

## 下一步续接

1. 保留上述源码和证据，不重开已完成调查。已采用本批 smart 参数继续 SWE2 并行施工。
2. 根服务按真实定义装配，尤其 `_root.初始化玩家模板`、真实行走、场景转换与 NPC 只读数据链。排除整批 boot 后再覆盖，不允许用空服务让 actorReady 为真。
3. 移动键集与唯一指针手势同 owner 共存；跨 owner 仍等全部物理键真实松开。门往返是明确白名单，旧 NPC/场景回调按实例代次拒绝；先核既有持续移动语义再实现。
4. 真对话阅读＋整形草稿；任务接取/奖励、商店/理发提交、真实存档保持拒绝。未注册动态 SWF 不加载。
5. 最后才形成 B1 固定候选、正常 Host 启动分支和一次人验。当前不新增 B1 人验入口，也不切换 R13 selector。

本批无 commit/push/发布。所有当前工作仍在原工作树中，其他施工修改保留。

## smart 续建：共享 Host 与持续键准入

用户选择优先使用 smart 后，本批 SWE2 实际完成持续输入协调器及共享 Host 补丁生成。smart 普通 PowerShell 读取成功，但一次递归枚举仍被拒绝；该工单不计完成，改用已知源码输入、离线生成 codemod 后由主控审查应用。未修改 Devin 全局配置。

C1 四个 Host partial 已抽到 `launcher/src/Guardian/UnifiedHost/`，原 executable 仅保留薄适配。Core 与 fixture 的隐式 using 设置不同，首次编译失败后补齐显式 using；修正后构建成功。模块名、源尺寸、命中映射由封闭 scene profile 绑定，目前只开放 C1，B1 调用显式拒绝。Core 新候选分支在既有 runtime verifier 之后，且必须是已验证隔离候选；成功进入后直接运行共享宿主，不构造旧 Guardian／Overlay／InputShield。诊断注入默认关闭，测试夹具明确开启。

共享 Host 冻结包 `tmp/b1-unified-base/shared-host-candidate` 实跑矩阵37次断言通过；该包仍为 UNQUALIFIED，不替换 R13。它证明共享代码的 C1 实际消费；后续 profile/启动分支变更由新的 Core producer 候选实跑覆盖，不能借用这个包冒称已执行。

持续输入增加封闭 scope→键集及逻辑准入集合，默认 C1 策略为空；同 owner 可持有已准入移动键并完成单个点击，跨 owner 仍须真实全松开。主控独立反例实测原补丁3项失败：过期 Down 在真实 Up 后仍可准入、未核对 baseline 可开始移动、未准入外键持有时仍可准入。已补当前 prefix、真实状态、baseline 与准入集合检查；已有移动遇额外未准入键或 baseline 丢失走取消，物理 Down 保留。没有采纳原报告“只凭单调序号接纳批次旧边沿”的建议。

修正 SWE2 两个测试中“被拒绝的 Down 未松开就再次 Down”的不真实序列，并将“未知 baseline 中启动新移动”的错误成功期待改为拒绝；额外保留完整 END 后合法新移动与迟到 opener 共存的正向测试。定向48项通过；随后完整 Launcher **5806通过、4跳过**，串行 testhost 已核。日志：`continuous-boundary-before.log`、`continuous-focused2.log`、`shared-host-full-tests.log`。实际 WASD→真实 Flash 行走尚未接通，协调器结果不升级为这一层通过。

## 后续已验证与当前收口

正常 producer 隔离 Core `CD44991A6E86330B24E198A9173208C1109322925E78490015DFC7BD0D0EB398`（payload `D81C8682AF2DBF988F529FB172E0F115724806893F54E51C18E310ACDA4B3775`）经原 verifier 后进入共享宿主，`core-entry-matrix`37项通过，正式 runtime 未变。该产物早于后续输入复核修正，不能代签最新源码。

第二轮 SWE2 静态审阅提出快照残留准入、重复 Up 与带持续键 scope 替换问题；独立测试实际4失败后修正，定向58通过。快照矛盾走取消，不伪造Up；只保留当前已完成手势的同键重复Up，不新增手势；旧持续键scope替换明确拒绝，待独立策略资格，不静默降级。`review-matrix`38项含真实重复Up通过。

Flash源窗口和承载父窗口明确 EnableWindow(false)，初次父窗口状态不符保留为失败，补显式调用后真实短测通过。最新 `passive-source-matrix4`40项通过，含源窗口两级输入关闭、真实AS2帧继续增长、重复Up以及原窗口生命周期矩阵。`passive-source-matrix3`是在启动验收阶段前台回到 ChatGPT，未取得确切前台而失败，不能记作通过。当前 C# 还预接了默认空的持续键转发与尚未公开的 B1 profile；真实Actor输入闭环未被上述测试代签。

冷启动实验发现直接改写 Key.isDown/Key.addListener/Mouse.addListener 不生效；先 ASSetPropFlags(...,0,4) 解只读后替换并重新锁7，真实VM验证调用进入替代函数且不能普通赋值覆盖。XMLSocket 先建立唯一诊断通道，再禁止后续连接。新 RuntimeBootstrap fresh CS6 0/0，先等源HWND禁用和显式 LOAD 才导入资源；`runtime-bootstrap-real-attachment3`实际绑定子进程PID，真实医务室/演员资源挂载、存档读取拒绝成立。两次漏复制递归RSL依赖的超时保留，补齐4份实际依赖后通过；这仍不是演员初始化成功。

SWE2原基地调查后遭smart拒绝，续接及另起离线请求最终均返回 retryable/unavailable，无实现输出。主控后续用本机实现支线补齐 runtime/services 初版和确定性生成器；生成检查通过，未编译/运行，不能称actor ready。用户随即提出成本边界疑问，当前停止扩大独立基地重装，先复核低侵入输入兼容路线，详见计划的成本边界节。没有切换R13人验入口，没有提交、推送或发布。
