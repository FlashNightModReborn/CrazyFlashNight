# 军阀基础闭环施工交接（2026-09-05）

本页是当前施工状态的唯一入口；产品范围与原决策见 [军阀 ADR](军阀战棋关卡化-N阵营编制与大地图演进-ADR-2026-08-31.md)。旧 r8–r14 记录是历史，不得继续作为启动或验收指令。

## 当前停在哪里

- Slice 6.1 基础闭环收口，尚未进入 Slice 7 上线。r14 的参战控制权拒收已修；r15 真人复验又在首次战斗后出现整个客户区黑屏，故 **r15 人类验收失败**，不能凭机器门沿用“可玩”结论。
- r16 修正 Host 浏览器显隐配对并补一次性只读诊断。2026-09-05 维护者确认完整游玩有效；Demo 2 基础闭环达到 **HUMAN_ACCEPTANCE_PASSED / promoted**，正式运行库与已验收候选逐字节一致。源码、部署和验收摘要已经推送；没有开放 Slice 7 玩家入口，不继续扩展玩法，也不代签发布后正式入口业务复验。
- 源码先从 `80b40e0173` 快进到 `4ae00a176265b7d00ea38364d545d29cbe601efa`，合入上游 37 个提交；军阀实现随后保存为 `29157bff9a`，最终 release source 为 `41a8bdc9d55bc153d9f9678aa646c34eb7691883`。当前应从下节发布锚点续接，不再从旧候选失败记录重新施工。
- 当前机器有主板故障，高负载可能死机。禁止 Computer Use、自动真实游玩、浏览器／GPU 压测；编译和纯逻辑测试串行、限制并行度。真实游玩由人类完成，硬件维修前不以长时间压力测试作为收口前置。

## 已保存的发布锚点

- source tag：`runtime-build-v2/20260905-warlord-basic-loop-v1`；release tree：`a9a0d3f899e459ec13f5e60370d0c3b2ea930744`；request：`89508212BBC4C24CC7E70D5871C9858FC86A83791E2CA18A33353F843554660A`。
- 注册本地 `builder-local-a / physical-host-a` 与 [GitHub 云构建 33939427506](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/33939427506) 独立构建、签名，精确取得下述 r16 identity / closure；production policy `40/40`、完整运行库、签名共识、GitHub proof replay 与正式根 bootstrap `--verify-only` 均通过。没有手工复制候选覆盖正式运行库。
- 原子 promotion 时间：`2026-09-05T02:40:48.6497994Z`；部署提交：`2b2913d3bf453f1b6783a68509f72e8058320bfa`，已快进推送。远端部署审计 [33939954269](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/33939954269) 首次 attempt 成功，明确输出 `state=promoted / deploymentChanged=true`，精确绑定 base `41a8bdc9d5` → head `2b2913d3bf`；完整 [发布列车记录](runtime-build-reproducibility.md) 已同步。
- 旧运行库可恢复备份：`tmp/runtime-promotions/20260905T024022536Z-6ff22c46a8784dd0a5627f7cd80fe305/previous`；本轮源码、原始游玩日志、request、生产 receipt、签名结果与阶段日志保存在 `tmp/warlord-release-20260905/`，本机签名 CAS/队列在 `C:\qw95`。它们是本地续接资料；长期发布证明已内嵌 tracked consensus，不依赖 Actions 临时 artifact 永久保留。
- 当前只保存基础闭环，不宣称所有 Slice 1–6 功能验收完成，不开放正式选关，不推进玩法扩展。后续从“再做全”清单继续；真实感知仍由维护者负责，不重复已通过的长局。

## r16：战后整窗黑屏

现场日志已经保存于 `tmp/warlord-s61-black-return-20260905/launcher.log`。请求 `battle.2.72.1.mtnoluu4` 于 09:05:50 得到 AS2 admission；09:05:51 完成 frame 209 世界初始化、双方演员安放和玩家操控；09:05:57 发回 terminal，Host 打开新的 resume 面板，随后收到 `warlord_battle_resume_applied disposition=accepted`。浏览器 CSS viewport 为 `1067×600 / dpr=1.5`，controller 为 `1600×900 / visible=True`。09:06:12 人类关闭窗口，进程正常退出。**已排除本次停在 start 准入、等待战斗 terminal 或 Web 未收到结果；仍不能由这段日志证明 DOM 完成呈现或 GPU/驱动健康。**

源码缺口：`DoFullIdleSuspend` 原来只 `SW_HIDE`，controller 一直保持 `IsVisible=true`；后续几何同步又提前写 `true`。r16 让 controller 在父窗隐藏前明确设为 false，重开/最小化恢复时跨 HWND/样式/几何变化保持隐藏，父子窗恢复后再设 true。几何同步只负责几何。沿用现有 panel owner、过图、战果和显式返回语义，不增加业务重试、恢复状态机或 WebView 重建。[WebView2 官方显隐约定](https://learn.microsoft.com/en-us/dotnet/api/microsoft.web.webview2.core.corewebview2controller.isvisible)支持配对；修复后真人完整游玩成功，但不据此声称唯一根因已经穷尽或硬件故障消失。

每个已接受结果的面板 generation 最多一次 `warlord_presentation_sample`：150 ms 后读取 DOM 是否存在、祖先样式/文档可见性、结算按钮几何/命中和 canvas 数量，等待上限 1.5 s；采集前后核对同一实例。不截屏、不读像素/玩家文字、不循环、不作为结算/验收门。下次若仍黑屏，可直接区分“DOM 丢失/隐藏”与“DOM 可见但原生呈现不正常”，不能再将结果 ACK 当可见成功。

机器门：Host 定向 `128/128`（含显隐顺序及诊断边界）；Web 单进程 `204/204`（含生产 wrapper 的无图形元数据读取）；runtime verifier `131 files / 2,180,127 bytes`。新增 Node VM 测试所需类型声明已补齐；中途类型检查失败不算通过。AS2 与 asLoader 均未改动/重编，继续使用下述 r15 指纹。这些证据不证明真实绘制、不证明 GPU 负荷或硬件故障已解决。

r16 candidate：`tmp/runtime-candidates/v2/warlord-s61-visible-r16`，`integrity-only / 33 files` 通过；Core SHA-256 `27AF835FA33C71E0E9CE0FE82A96D54D0D4270226DD0CC053814DC5A98CE1B55`，build identity `7902F8DFCE50E3F09C245E0F849E9AC3E3D7B07791D66C543C60E38472A59CAE`，closure `09E8754A6E8DEE9ABB4A389AA7F578AC92E19650268912EC42B44FE9E63601A3`。候选制备当时没有修改正式运行库；后续发布锚点见上节，不能把当时的 NOT_DEPLOYED 沿用为当前状态。Host TRX 在 `tmp/warlord-s61-black-return-20260905/test-results/black-return.trx`，编辑前文件在同目录 `pre-edit/`。

## r15：参战控制权修复（历史证据）

1. 最近失败请求 `battle.6.439.10.mtm8h4bb`：袁望攻击我方精锐，主角在另一个据点。Host 按“参战阵营属于玩家”写入 `playerControlledSide=red`；AS2 的 v2 合同却要求 redRoster 恰有一个 `player_avatar`，于是场景创建前便拒收。日志里的四次发送均无 admission，不能再记为已证实的 GameWorld 创建失败。
2. Host 控制权改为从**已通过身份校验且实际参战的投影**推导。主角在攻方为 `blue`、守方为 `red`，未参战为 `none`。无主角的双方 AI 旁观沿用已有 v2 合同，不新增协议版本、恢复状态机或场景 owner；战斗局部 blue/red 仍表示攻守方，不代表战略阵营染色。
3. 保留 `StageManager → 正常淡出跳帧/frame 209 → StageInfo` 的共用场景路径，不改变正常无限过图、保存和装备权威。保留 r14 的结算层释放隐藏沙盘、停止渲染与固定底部按钮。
4. 引导提示的无限阴影脉冲改为静态高亮，帮助层移除背景模糊，减少无操作时的重绘。源码检查不等于实测 GPU 百分比下降。

已完成：Host 军阀定向 `102/102`（含新增 8 种攻守／主角在场组合及结算回执）；合并边界 Host `464/464`（Loot、ItemUse、Shop、Archive、StageOutcome、右侧 HUD、SafeExit、reveal watchdog、exact replace）；Web 单进程纯逻辑 `202/202`，生成 runtime `128 + 4 vendor`，verifier `131 files / 2,180,127 bytes`。

fresh Flash：Action `97/97` + Host 实际发包 → LiteJSON → AS2 validator `16/16`（runId `8afdbff30e974982a4d43d63c8e57fef`）；SubStage `78/78`（runId `3a164c5390774ff4ac05a5542efb5c4d`）。均为 Compiler `0/0`、32K retry `0`。测试模板首次直接嵌 JSON 对象字面量被 AS2 编译器拒绝，已改为经过真实 LiteJSON 解析的字符串；首次失败不算成功证据。

asLoader 精确发布：`1,266,843 bytes`，SHA-256 `1E6CC63B5B1D173BA25178073F10E4C196ED47F62BFF129F5E5A074775D67E14`，Compiler `0/0`，`11,205` 函数、最大 `55,767B < 60,000B`；未编 main。上游 Save API 调用点仅同步六处行号和 asLoader 指纹，`37` 物理点／`32` 逻辑点一一对应及 SWF hash 校验通过，不修改保存语义。文档治理与编译入口静态回归通过。

历史候选目录：`tmp/runtime-candidates/v2/warlord-s61-stoploss-r15`，`integrity-only / 33 files` 已通过，但上述人类黑屏已否决它的闭环验收。

- Core SHA-256：`F35FE03E9F5086731771F07B625BBFE7071B1F81FC3610383ABEDFD44EAE3C51`
- build identity：`FA6559E0991237C89751F123A987C6CD0B008C9BBA651DC2581EF9C47722CFBD`
- payload closure：`E73425FB107041B2E463E751F933A88AAB974ED1263963652F7A139550560674`
- 上游同步带入的正式 Core 为 `3948B44863DC42757EAEC42117470821F8EF1CEEDBB57DAD65B1058268B1C8DE`；军阀 candidate 构建没有改写它。

## r16 人类验收与日志复核

2026-09-05 09:28:40–09:58:18，实际进程路径绑定上述 `warlord-s61-visible-r16`。维护者明确反馈“有效，完整的进行了一次游玩”。本轮日志与先前 09:03:35 开始的 r15 失败会话分开计数：

- Demo 2 共 38 次战斗：玩家进攻 7 次、防守 2 次、无玩家参战的部队战斗 29 次；38 次 admission、38 次 terminal、38 次 resume applied，重发与恢复拒绝均为 0。
- 38 份一次性呈现诊断均为可见、返回按钮可命中、`canvasCount=0`；这些是 DOM/生命周期证据，不是 GPU 利用率测量。
- 最终 `victory`，奖励 `claimed / remainingRewards=0`，返回基地，随后用户正常关闭；Flash exit code `0`，uptime `1776.2s`。
- 非阻塞项：旧 `crazyflasher7_saves1` 影子 JSON 启动解析失败，本轮 `dev` 槽保存正常；终局面板关闭后约 75ms 到达的重复 close 被正确拒绝；完整战略快照日志约 3.4 MB，日志降噪留待后续，不在发布冻结期追加补丁。

这关闭 **r16 Demo 2 基础闭环**的人类验收，不外推为 Demo 1、所有 Slice 1–6 功能、敌指挥官对精锐且主角缺席这一精确角落、硬件长稳或发布后正式入口业务复验。原始日志与发布前文件备份保存在忽略目录 `tmp/warlord-release-20260905/`；不将私人存档或完整战斗快照提交到 Git。

## 后续人类验收边界（不阻塞本轮保存产出）

正式根启动器／无 CandidateRoot 的 `automation/start.ps1` 现在消费上述已提升运行库；`本地开发启动.cmd` 仍是隔离开发入口。军阀 Demo 继续只从**测试菜单**进入，不要直接启动旧 r14/r15。本次发布不要求新增游玩；后续功能补全若需验收，再合并说明最小场景。机器负责日志与构建校验，人类不需要抄日志或填 receipt。

首次战后黑屏及完整 Demo 2 游玩已由上节人类反馈关闭，不要求为源码冻结和构建重复游玩。以下场景纳入下一轮功能补全清单，不把本次未覆盖项补签为通过：

- Demo 1：普通部队进攻与被攻击都能进入 AI 战斗，结束后返回沙盘并可继续行动。
- Demo 2：主角进攻／被攻击的操控、真实装备与结算头像正确；主角留在别处时，让敌指挥官与我方精锐交战，仍能结束并回到原战局。不必先灭两家或复刻一整局长流程，关键条件是主角不在本次参战名单中。
- 结算按钮始终可见可用；返回基地后能重新选择另一个 Demo，不被旧“恢复演习”拦住。敌方整批运镜结束后回到原玩家视野。
- 正常短时游玩即可；出现明显发热、负荷异常或卡顿就停止，不为收集证据坚持到死机。GPU 长时稳定性与硬件问题分开记，不据源码／单测宣布已解决硬件风险。

## 接下来严格按三步推进

| 阶段 | 做什么 | 离开条件 |
|---|---|---|
| 先跑通（当前） | 两种 Demo 的上述战斗、结算、继续行动、退出重选闭环；阻断缺陷只做可定位的小修复 | 同一精确候选经人类确认基本可玩；不再扩展玩法 |
| 再做全 | 核对原 ADR Slice 1–6 功能清单，把未完成项逐项收口；恢复／重开仅保留明确产品需求 | 功能缺项清零，机器合同门与集中人类验收完成 |
| 最后做好玩 | 基地易守、中心争夺、近中远距离、兵种就业、阵型与经济深度 | 人类玩法反馈驱动；另行授权 Slice 7 上线 |

不要借本轮重写全部 AS2／C# 业务权威，不再用超时加长、重试叠加或深度交换掩盖缺少 admission 的错误。若复发，先区分“Host 未发出／AS2 拒收／场景未就绪／战斗无 terminal／Web 未应用”所在边界，再修改该边界。

## 机器续接信息

- 原工作树 275 文件逐一备份并核对 SHA-256：`tmp/warlord-s61-stoploss-20260905/pre-integration/manifest.json`、`files/`、`tracked.patch`。
- 六个交叠路径保护 stash：`4e845f909a46bbfbe87fd34e8ded3d35e87d0579`，文本已恢复，旧 asLoader 保存在备份；本轮不删除历史 stash，不另建 worktree。
- Host TRX：`tmp/warlord-s61-stoploss-20260905/test-results/warlord-stoploss.trx`。
- 合并边界 TRX：同目录 `integration-boundaries.trx`；Flash 新鲜 trace／Compiler／Output 副本在 `flash-action/`、`flash-substage/`、`flash-publish/`。
- 本轮只启动并回收一个 Flash CS6 编译实例（PID `7540`）；publish 后 IDE 未自行完全退出，确认诊断落盘后通过本轮 `FlashCS6Task` 收掉。交付时无该实例／测试播放器残留，无真实游戏运行。
- Host wire：`tmp/warlord-s61-stoploss-20260905/host-wire/*.json`，由 `ControlWireMatrix_ActualParticipantsOwnControl` 的 transport 回调导出。
- 复现该跨栈门：设置 `CF7_WARLORD_WIRE_FIXTURE_DIR` 为本轮空目录，执行上述 Host 测试；随后 `scripts/run-warlord-action-encounter-tests.ps1 -HostWireFixtureDirectory <同目录>`。Flash 只解析和验证请求，不运行真实战斗。不要用手写另一套“合法样本”代替 Host 产出的请求。
