# Build and Release Folders
bin-debug/
bin-release/
[Oo]bj/
# [Bb]in/

# Other files and folders
.settings/

# Native build intermediates
*.obj
*.exp
*.lib
*.pdb

# Repair dict atomic-write intermediate (tools/cf7-save-repair-dict-build 产物的临时文件,
# 写完后会 rename 成 save_repair_dict.json; 误 commit 会污染包并破坏 JSON 校验工具).
launcher/data/save_repair_dict.json.tmp

# Executables
# *.swf
*.air
*.ipa
*.apk
恢复_*.fla
~$*.xlsx
~$*.docx
~*.tmp

# Project files, i.e. `.project`, `.actionScriptProperties` and `.flexProperties`
# should NOT be excluded as they contain compiler settings and other important
# information for Eclipse / Flash Builder.
launcher_ports.json
.vs/
/新建文件
恢复*/
*.swd
flashswf/arts/new/天网大家庭_死亡.swf

# Benchmark probe/test SWFs (generated, not source)
scripts/优化随笔/性能基准评估/探针.swf
scripts/优化随笔/性能基准评估/测试.swf

# Test loader (SWF is generated)
scripts/TestLoader.swf

# Flash compile automation (generated/machine-specific)
scripts/flashlog.txt
scripts/compile_env.sh
scripts/publish_done.marker
scripts/publish_error.marker
scripts/compile_trigger
scripts/screenshot.png

# Claude Code local settings (machine-specific)
.claude/settings.local.json
.claude/scheduled_tasks.lock
scripts/TestLoader.as
scripts/testloader_options.txt
scripts/compile_output.txt
scripts/compiler_errors.txt

# db
memory.db
# vector_index
tools/vector_index

# illustration
flashswf/portraits/illustration/

# agent
data/task/agent_tasks.json
data/task/text/agent_text.json
scripts/TestLoader.as
data/rag/sync_state.json

# Node modules (npm install restores)
node_modules/

# Launcher build output & NuGet packages (dotnet build/restore regenerates)
launcher/bin/
launcher/packages/
launcher/tests/bin/

# Rust cargo build intermediates (launcher/native/sol_parser)
# Cargo.lock IS tracked: sol_parser ships as an application cdylib, not a
# library crate, so pinning transitive deps is required for reproducible /
# offline builds.
launcher/native/sol_parser/target/

# WebView2 runtime cache (machine-specific)
launcher/webview2_userdata/
launcher/webview2_overlay_userdata/
weboverlay_debug.log

# Launcher runtime logs
logs/

# Legacy user prefs path (runtime now migrates to %LOCALAPPDATA%)
launcher_user_prefs.json

# Save shadow backups (player-specific, not source)
saves/*.json
saves/*.json.tmp
# 玩家本地修复备份与 audit log（C1b/C2-α 自动写入，含真实存档数据，不入库）
saves/.repair-backups/
# launcher 版本 gate marker（机器本地状态）
saves/.launcher-version-marker.json
saves/.launcher-version-marker.json.tmp

# cf7-save-repair TS 工具产物（dist 由 tsc 生成；coverage 由 vitest 生成）
tools/cf7-save-repair/dist/
tools/cf7-save-repair/coverage/
# 同源 dict-build 工具
tools/cf7-save-repair-dict-build/dist/
tools/cf7-save-repair-dict-build/coverage/

# AI sim (generated results + Python bytecode cache)
tools/mercenary-ai-sim/results/
tools/mercenary-ai-sim/__pycache__/
tools/combat-movement-sim/results/
tools/combat-movement-sim/__pycache__/

# 调试探针/临时 DOM 快照等 ad-hoc 产物, 禁止入库
/tmp/
/tmp-*
/tmp-*.html
/tmp-*.json
/tmp-*.txt
/tmp_ffdec_*/

# Native cursor image-generation review sheets (generated, not source)
launcher/web/assets/cursor/native/review/
