/**
 * WHEEL_SIZE_MACRO - 时间轮大小常量
 * 
 * 数值：128 (2^7)
 * 含义：时间轮的总槽位数量
 * 
 * 功能用途：
 * • 定义时间轮（时间轮调度器）的槽位容量
 * • 时间精度控制：128个槽位提供精细的时间分片管理
 * • 哈希分布：使用2的幂次便于高效的模运算和位运算优化
 * • 内存分配：预分配128个槽位的时间轮数组或链表结构
 * • 调度效率：合理的槽位数量平衡内存使用和调度精度
 * • 位运算优化：128 = 2^7，支持使用位掩码进行快速索引计算
 * 
 * 时间轮原理：
 * • 将时间分为128个离散的时间片段
 * • 每个槽位存储该时间点需要执行的任务
 * • 通过轮循遍历实现高效的延时任务调度
 * • 适用于游戏中的定时器、延时效果、状态管理等
 * 
 * 使用示例：
 * • 槽位索引：index = (currentTime + delay) % WHEEL_SIZE_MACRO
 * • 位运算优化：index = (currentTime + delay) & WHEEL_MASK_MACRO
 * • 数组初始化：timeWheel = new Array(WHEEL_SIZE_MACRO)
 * 
 * 应用场景：
 * • 游戏定时器系统的时间轮调度
 * • 延时效果和状态变化的管理
 * • 高频任务调度的性能优化
 * • 时间片轮转的精确控制
 */
var WHEEL_SIZE_MACRO:Number = 128;  // 时间轮槽位总数 - 值: 128 (2^7)