# 2026-09-12 工作日志

## 天启大封印 · 起手两特效（眼睛白光流光 / 手间太极流转冲出）

### 交付物

- `tmp/天启大封印-特效/gen_open_effects.py` —— 生成脚本（新）
- `tmp/天启大封印-特效/眼睛流光/眼白光_00..05.svg` —— 6 帧循环，画布 **200×76**
- `tmp/天启大封印-特效/手间流转/手间_00..05.svg` —— 6 帧一次性，画布 **240×300**
- `tmp/天启大封印-特效/preview_open.html` —— 预览页（可逐帧点、可切缩放）
- `tmp/天启大封印-特效/起手特效接入说明.md` —— CS6 接入手册
- `tmp/天启大封印-特效/_render_check.py` —— 极简 SVG→PNG 渲染器（仅支持本项目的
  polygon/ellipse + 字面 `#RRGGBB`），用于目视检查；用系统 Python 3.10（有 Pillow）

### 尺寸判定（**重要，别再重算**）

- `gameworld` 在 `cameraZoomToggle = false`（玩家存档现状）时缩放 **100%**，
  元件贴上去 **1:1**。开启动态缩放时 gameworld 整体缩放，元件跟随 —— 视觉与判定同步，不出错
- 用户方案：**画 600 矩形对着调**，成立。法阵圆阵调 600、光柱柱体调 660
- 光柱柱体外层实测半宽 **1111**（直径 2222），即 660/2222 = **29.7%**
- 圆阵外缘实测直径 **940**（`rx=470`）+ 描边 8 → 视觉 948；缩到 600 是 **63.8%**

### 两特效的关键设计决策（踩过坑，勿回退）

1. **眼睛画月牙不画圆点**：参考图是水平纺锤白光斑；圆点在 1x 下变"两个白圆"，不像漫画眼白发光
2. **流光的起点必须在月牙轮廓之外**（≥ 轮廓 1.04 倍）。早版让光丝穿过主体 → 整体糊成"刮痕"，已废
3. **太极图用「整圆打底 + 一条 S 形鱼覆盖」**，不是画两条鱼轮廓。
   两条鱼的边界在多边形表示里必然留缝或被奇偶填充漏出背景三角，试了两版都失败。
   标准构造：`大圆左半 + 下小圆左半 + 上小圆左半`（小圆 r=R/2，圆心 (0,∓R/2)）
4. **圆环一律用扇形段拼**（`_ring_segments`），不能用"外圈正走+内圈倒走"——
   Flash 与 Pillow 都用奇偶填充，那样会被填实
5. **不要用弧带做光晕**（1x 下露齿轮缝）。改用**整圆叠层**，由外到内变亮
6. **帧 2→3 尺寸要衔接**：帧2 太极+光晕外径 ≈ 帧3 余环外径，否则"啪"地缩一下

### 容器手部实测轨迹（从 `战技容器-天启大封印.xml` 矩阵提取）

动作层帧结构 `0+4 → 7+2 → 9+17 → 26+2`（共 28 帧）。

| 帧 | 右手 | 左手 | 间距 |
|---|---|---|---|
| 0 | (66.1, -162.4) | (93.0, -140.2) | **35** |
| 4 | (73.8, -163.7) | (118.8, -155.9) | 46 |
| 7 | (78.7, -179.2) | (142.0, -176.6) | 63 |
| 9 | (79.8, -195.6) | (154.9, -204.6) | 75 |
| 14 | (82.6, -211.5) | (154.9, -226.7) | 72 |
| 20+ | (88.4, -221.5) | (162.2, -242.7) | 74 |

头在 `(-0.1, -201.85)`。双手间距只有 35~75 → 手间特效舞台极小，
太极外径取 114（略大于手距以包住手）。

**落点**：手间元件画布 240×300 的 `(120,236)` 对准元件原点后，
元件放到容器坐标 **`(80, -151)`**（帧0 双手之间）。

### 坑

- 该 XFL 的 `<DOMLayer name="X">` 图层名**和内容不对应**（图层名是标签，
  内容是拼装序列）。要拿真实坐标得按 `libraryItemName` 搜实例 + 读它的 Matrix
- SVG 画布尺寸 ≠ 图形尺寸。光柱画布 7600×5680 但图形只占中间一条
  （柱体直径 2222）。**CS6 按画布建元件**，所以元件会比图形大很多
- 统计 SVG 宽度时**只取 `x`/`cx`/`points` 的偶数位**。把 `fill="#FFFFFF"` 里的数字
  当坐标会算出暴走的假值
- Pillow 的 `ImageDraw.polygon` 填充规则与 Flash 一致（奇偶），可用来预先验证
  形状会不会被填实 —— 这是本次发现太极问题的关键手段

### 未决

- 两特效**位置是静态的**，不跟手走。要精确跟手需在容器里逐帧摆位（纯 CS6 操作）
- 眼睛特效**不含眨眼逻辑**，属角色动画层，不在本特效范围

---

## 天启大封印 · bug 1/2 修复（元件不播放 / 位置没锁在法阵）

用户反馈两条：①元件卡住不播放；②位置没像法阵一样严格锁定，无敌人时落在玩家脚底。
关键前提（用户澄清）：**光柱美术直接画在 `天启大封印` 元件里，不嵌套**；元件只有一句帧脚本
「第 30 帧 `gotoAndPlay(16);`」；元件是整条时间轴（爆发 1~15 一次性 → 16~30 循环）。

### bug 1 根因：`封印()` 里的 `宿主.stop()`

`attachMovie` 后立刻 `stop()` → 停在第 1 帧 → 爆发段不播、永远进不了 `gotoAndPlay(16)` 的循环。
**已删除该 stop()**，改为让元件自由播放（SealDomain 只读宿主坐标、挂 onUnload，与播放头无关）。

### bug 1 二次保险：`SealDomain.确保播放(宿主)`

删除 `stop()` 只解决了「代码主动停」这一路。若**元件自身帧脚本里循环 stop()**，
同样的卡死会复现。新增 `确保播放()`：给宿主挂一个**空的 `onEnterFrame`** ——
AVM1 里空 onEnterFrame 就等于「每帧被访问一次」，足以打破 stop 冻结。

- 只在 `宿主.onEnterFrame == undefined` 时挂 → 不与元件自身处理器冲突
- 由帧任务包装函数 `帧任务本体` 每帧调用（在 `每帧(状态)` 之后）
- 只挂空实现，不写业务逻辑

### bug 2 根因：落点算错（三处不一致）

①`落成子弹` 用 `自机._x/_y`（施术者脚下）；②`每帧` 只在 `目标名` 查到活敌时才吸附；
③`落成` 先 `撤法阵` 再建宿主，事后读不到法阵坐标。

**修复**：新增 `取落成位置(自机)`（与 `建上下文` 的兜底公式逐字一致：
`施术者._x + 法阵兜底距离 × 朝向, 施术者._y`）；`落成()` 在**撤法阵之前**把
`上下文.法阵._x/_y` 存进 `落点`，再传给 `_落成核心(容器, 落点)` / `落成子弹(容器, 落点)`。

**`SealDomain.每帧` 的吸附逻辑（279-285 行）确认正确、不需改**：有敌人吸附到敌人
（与法阵重合），没敌人停在落点（= 法阵兜底位），两种情况都对。

### 改动文件

| 文件 | 改动 |
|---|---|
| `scripts/逻辑/单位函数/单位函数_雾人_aka_fs_主动战技.as` | 2095 行。删 `宿主.stop()`；`落成` 加落点锁定；新增 `取落成位置`；`落成子弹`/`_落成核心` 加落点参数 |
| `scripts/类定义/org/flashNight/arki/skill/SealDomain.as` | 523 行。帧任务改为 `帧任务本体` 包装；新增 `public static function 确保播放(宿主)` |

两文件 BOM `ef bb bf` ✓、块注释配平 ✓、`SealDomain` 花括号 76/76 ✓。

### 遗留

- `天光元件名 = "天启大封印天光"` 现在**可能冗余**：美术已在宿主元件里，建议设为 `""`
  （代码已有该分支，`封印()` 里会跳过 attachMovie）。**等用户确认再改**
- 用户明确说过「起手特效是我没描述清楚需求，后面再调整」→ 特效需求后续会改
- 待用户 CS6 编译 + 真机验证两个 bug

---

## 天启大封印 · 定位语义变更（宿主不再吸附 + 逐单位独立计时）

用户需求：释放后宿主不再吸附敌人（吸附是法阵的活），法阵最终位置 = 封印释放位置；
释放时封印一次范围内的敌人；宿主随后静止。可选：每秒判定一次，把后续进入法阵的
敌人也封印，但**强度从各敌人被封印那一刻起各自累计**，而非从技能开始统一累计。

用户明确回答：`天启大封印` 元件里**没有 onEnterFrame**，只有素材 + 第 30 帧一句
`gotoAndPlay(16)`。

### SealDomain.as（568 行）改动

1. **删掉 `每帧` 里的宿主吸附整段**（原 306-313 行）→ 宿主落成后静止。
   中心 = `封()` 当帧的 `宿主._x/_y`，之后不变（`封()` 的 JSDoc 已写明调用方
   必须保证宿主已落在法阵消失前的坐标上）
2. **强度累加/额度判定下移到 `记录` 级**：`记录` 新增 `强度: 0` / `已存续帧: 0`；
   `每帧` 里对每条记录 `记录.强度 += 每秒强度 / 率`，判定改 `记录.强度 >= 记录.额度`
3. **`终止()` 到点判定同步改**：`记录.强度 >= 记录.额度`（否则到期那一刻
   刚抓进来没多久的敌人会被误放逐）
4. **删掉 `状态.强度` 累加器**与 `状态.目标名`（吸附移除后二者皆无用）
5. **`补收间隔帧` 15 → 30**（= 1 秒 @30fps），即用户要的「每秒判定一次」
6. **新增 `挂卸载回调(目标, 回调)`**：`封()` 里原本裸写 `宿主.onUnload = …`。
   EventCoordinator 的 autoCleanup 会把 `onUnload` 换成 proxy 并 `watch` 住，
   watch 处理器一律 `return oldVal` → **之后所有直接赋值都被静默丢弃**。
   现在优先走 `EventCoordinator.addUnloadCallback`（顶部 `import org.flashNight.aven.Coordinator.*`），
   拿不到才退化为裸链式包装

### 单位函数_雾人_aka_fs_主动战技.as（2099 行）改动

1. `法阵交给宿主` 里的 `宿主.onUnload = …` 改用 `SealDomain.挂卸载回调`（同上原因）；
   注释改为「法阵从此钉在宿主落成位置不动」
2. `落成子弹` 的 initObject 去掉 `目标名`（吸附已不存在）
3. `_挂宿主收尾` 的 JSDoc 补充说明：**该 onEnterFrame 同时兼任播放头保活**
   （任何 onEnterFrame 存在即打破 AVM1 的 stop 冻结），会覆盖 `SealDomain.确保播放`
   挂的空 handler —— 属预期行为，不是 bug
4. 三处过时注释修正（"每帧吸附到目标" → "落成后静止"）

### 关键认知（跨会话有效）

- **`记录` 才是强度的正确粒度**。`状态.强度` 是全局单一累加器，晚进来的敌人会
  "白嫖"前面攒的进度，与「从被封印开始累计」的需求直接冲突
- **`_挂宿主收尾` 覆盖 `确保播放` 无害**：自毁 handler 本身确保每帧被访问，
  播放头照常推进。两处都写 `onEnterFrame` 但顺序上后者胜出，功能等价

### 第二轮修正：天光逻辑彻底删除 + 调度器修复方案重做

**① 天光彻底移除**（用户明确："根本没有天启大封印天光，从来没加过这个元件"）

删掉 `天光元件名 = "天启大封印天光"` 常量 + `封印()` 里的 `attachMovie` 分支
（共 4 处）。全仓 `grep 天光` 已归零。原 `封印()` 里 `接口` 局部变量已随之去掉。

**② 调度器修复方案推倒重做 —— 第一版有真 bug**

⚠ **第一版用「节点反向持有 Task 引用」做归属校验是错的**：
`SingleLevelTimeWheel` 的 `delayTimerByNode`(378) / `rescheduleTimerByNode`(411) 会在
**节点仍然存活**时调用 `node.reset(node.taskID)`（原地换槽位复用），
`reset()` 一旦清掉那个引用，**正在跑的合法任务就会被误判成残留而跳过执行 → 任务停摆**。
即修 bug 引入了更严重的 bug。**教训：`reset(非 null)` 是「原地复用」语义，不能清附加状态。**

**② 改用「占用代次 epoch」方案（最终版）**

| 文件 | 改动 |
|---|---|
| `naki/DataStructures/TaskIDNode.as` | 新增 `public var epoch:Number = 0`；`reset()` 里**仅当 newTaskID == null 时**清零（池回收才作废，原地换槽位保留） |
| `neur/TimeWheel/SingleLevelTimeWheel.as` | 新增 `_epochCounter`；`acquireNode` 末尾 `node.epoch = ++_epochCounter` |
| `naki/DataStructures/FrameTaskMinHeap.as` | 新增 `_epochCounter` + 私有 `acquireNode(taskID)` 统一取用入口；3 处取用点全部改走它 |
| `neur/ScheduleTimer/Task.as` | 新增 `public var nodeEpoch:Number = 0` |
| `neur/ScheduleTimer/TaskManager.as` | 分发时 `if (task && node.epoch != task.nodeEpoch) task = null;`；9 处插入点快照 `task.nodeEpoch = task.node.epoch` |

**不变式**：所有插入路径都源自 `CerberusScheduler.evaluateAndInsertTask` → `acquireNode`
（`addToSingleLevelByNode` / `addToMinHeapByNode` 只改 `ownerType`，不碰 epoch），
故任何拿到的节点 `epoch >= 1`，且调用方**在调用之后**才快照 → 无假跳过。
零帧任务走独立分发分支（`updateFrame` 的 `zeroFrameTasks` 段），不经过归属校验。

**另外两个 fix 保留**：
- `removeTask` 里物理移除后 `delete task.node`（陈旧节点顶掉并发任务）
- `updateFrame` 分发循环加「缓存断链哨兵」：`if (nextNode != null && nextNode.next == null)`
  才从 `tasks.getFirst()` 重新定位（回调解链后续节点时不再丢掉本帧剩余任务）

### 编译环境现状

- 沙箱把 `schtasks.exe` 列入黑名单（Program Blacklist），**无法查/触发计划任务**
- PowerShell 在本沙箱无输出返回
- `scripts/compile_state_uncertain.marker` 不存在，`compiler_errors.txt` 不存在
- 结论：**本轮改动未经过 CS6 编译验证**，仅做了 BOM/括号/注释静态配平检查


### 校验

四文件 BOM `ef bb bf` ✓、花括号配平 ✓、块注释配平 ✓。

---

## 本轮收尾（2026-09-12）

- 调度器（`TaskManager` / `TaskIDNode` 等 5 文件）的改动**已全部回退**，工作区与 HEAD 一致（`git diff --stat HEAD` 为空，全仓 `grep nodeEpoch` = 0）。
- 问题分析与候选方案记在 `docs/已知问题登记-2026-09-09.md` **问题 6**（仅登记，未修复）。
- 天启大封印定位语义变更的源码改动保留在 `SealDomain.as` / `单位函数_雾人_aka_fs_主动战技.as`，**未经 CS6 编译验证**。

---

## 天启大封印 · 特效重做（眼睛改柳叶形 / 手间改双鱼+双柱 / 新增驱逐白瀑）

用户要求（原话要点）：①手间是**两道分离的光做太极鱼式运动**，然后**分成两道光柱向上冲**，
匹配双臂动作；②眼睛**形状匹配玩家左右眼**、左右各一个、内侧贴眼眶外侧明显溢出成
**整块连贯流光**（不要小三角）；③被驱逐的单位不再直接消失，加**从下到上的白色瀑布状
白光** + 播 `黑闪.wav`。**CS6 元件用户自己做，不要我碰资产**。

### 产物（`tmp/天启大封印-特效/`）

| 目录 | 帧数 | 画布 | 挂载点 |
|---|---|---|---|
| `眼睛流光/眼白光_左_00..05.svg` / `_右_` | 6 循环 | 200×76 | 眼眶中心 (100,38) |
| `手间流转/手间_00..09.svg` | 10（6 鱼 + 4 柱） | 240×300 | 手位 (120,236) → 容器 (80,-151) |
| `驱逐白瀑/驱逐白瀑_00..07.svg` | 8 一次性 | 420×500 | (210,470) = 单位脚底 |

另：`preview_open.html`（三卡预览）、`起手特效接入说明.md`（重写，含 linkage 名
`天启大封印驱逐白瀑`）、`check_eye_left/right.png` `check_hand.png` `check_purify.png`（接触印相）。

### 关键设计决策

- **眼眶 = 柳叶形**（两段圆弧对扣：`R=(L²+H²)/(2H)`、圆心 `(0,-(R-H))`），不是椭圆。
  半长 26 / 半高 8 → 眼角夹角约 68°，才是杏仁眼
- **单侧溢出**：`w(u)=((u·side+1)/2)^2`，`x' = x + side·1.55·L·w·wave·m`、
  `y' = y·(1+3.10·w·wave·m)`。内侧 w=0 → 逐层精确落在眼眶上，外侧才溢出
- **流光 = 沿内眼角→外眼角的整数行波**（波数 4、时间周期 1），不是离散光丝 → 连贯无三角
- **两鱼从构造上不相碰**：各占 150°、相位差 180° → 天然留 2×30° 缝。
  标准太极的互补 S 曲线无法用相似变换分开（三处水平切线必然吻合，实测最小距 1.3px 已重叠），
  多边形内缩又会在鱼尾尖楔形处自交
- **柱底半距 18 → 柱顶半距 50**（V 形张口，跟双臂撑开对齐），冲出高度 228
- **白瀑靠几何淡出**：不能用 opacity，改成"底边离地上抬 + 柱体收细到 0.12"被抽走；
  13 缕竖光条中间高两边矮 = 瀑布水幕顶

### SealDomain.as 改动（唯一耦合点）

- `消失特效名` `""` → `"天启大封印驱逐白瀑"`；新增 `消失音效名 = "黑闪.wav"`
- `放逐()` 顺序改为 **①特效 → ②音效 → ③死亡结算**（原来特效在死亡之后，等于看不见）。
  特效用 `EffectSystem.Effect(名, x, y, 100, true)`（forceTrigger 无视数量上限）
- **不用帧计时器 / 延时任务**（用户明确否决）：单位当帧结算掉，特效元件自己播完注销
- 音效走 `_root.播放音效("黑闪.wav")` —— 音效系统**按文件名索引，不带子目录**
  （实测 `soundland.wav` 在 `sounds/export/特效/` 下，代码里也是裸文件名）
- 文件校验：BOM `ef bb bf` ✓、花括号 80/80 ✓、块注释 23/23 ✓（**未经 CS6 编译**）

### 坑

- `_render_check.py --sheet` 的 bg 参数**不带 `#`**（传 `2b3444`），前缀参数**不带末尾下划线**
  （`眼白光_左` 不是 `眼白光_左_`）
- 生成脚本自带 `bbox()` 越界自检，改画布尺寸后一定跑一遍（白瀑第一版 y 溢出 30px）

### 追加：手间冲出段改成「长水滴 + 碎裂消散」（用户嫌原来是蜗牛触角）

- 删掉 `_beam`（外弓锥形光柱）→ 换成 `_drop`：**头 = 半径 15 的半圆帽**（冲锋方向那头圆而胖），
  尾半宽 `w(s) = R·s^0.7`（`DROP_TAIL_P`，越小尾越胖，1.0 就是锥子），层叠绕**头心**收缩 → 头亮尾淡
- 尾长 `DROP_TAIL_LONG 150 → SHORT 62`：`tail_y = min(HGROUND, head_y + 尾长)`
  → 前期尾巴搭在手心，头越过 y≈86 后尾巴自然离地（脱离手心），不用写分支
- 头部起始位置 `HGROUND-55`（不是 -10）：否则第一帧水滴只有 10px 长，看不出形状
- **碎裂靠几何，不用 opacity**：中轴 5 段，每段两头各"啃掉" `FRAG_EAT=0.72`，
  碎片缩到 `1-0.95×frag`（末帧 <1.2px 直接不画 = 彻底消散），左右交错飘散 `FRAG_DRIFT=14`
  ⚠ 第一版把 gap 同时加到 s0/s1 上，frag 稍大就 `s1<=s0` 全被跳过 → 碎片整帧消失（假象"太快没了"）
- 帧数 6+4 → **6+6 = 12 帧**（0-5 鱼 / 6-7 水滴 / 8-10 碎裂 / 11 消散）

## 天启大封印：封印领域 Z 轴判定改成「单向 200」

用户调了元件位置（图层问题）→ 宿主原点被摆到参考点脚下**再往下一段**（更靠下），
但视觉中心仍在参考点脚下。所以 `SealDomain` 的 Z 判定改成单向。

`scripts/类定义/org/flashNight/arki/skill/SealDomain.as`：

（ Y 偏移：先按 90 实现，用户嫌多，最终定 **50**）

- `范围半径Z` 150 → **200**，语义从「Z 半径」变成「Z 方向纵深」
- `补收()` 里 `dz` 反号：`dz = 状态.中心Z - 单位.Z轴坐标`，前置 `if (dz < 0 || dz > 范围半径Z) continue;`
  → 只收 Z 比中心小（视觉更靠上）的单位；比中心更靠下的一律不收
- X 轴仍走椭圆联合式 `dx²/300² + dz²/200² ≤ 1`（上半椭圆，纵深边界处 X 收窄到 0）
- `中心Z` 依然读 `宿主._y`（宿主是 attachMovie 的子弹元件，没有 `Z轴坐标`），未改

**两套 Z 读法并存的原因**：起手选目标走 `按距离索敌`（双方都读 `Z轴坐标`，圆形），
封印收押走 `SealDomain`（中心读 `_y`、单位读 `Z轴坐标`）—— 站立时 `Z轴坐标 === _y` 所以等价，
只有宿主/施术者腾空时才会偏。

### 再改：Z 判定上下不对称（上方 200 / 下方 40）

用户把元件又整体下挪了 40 → 比中心略低的单位画面上仍被法阵压住，所以加了下方容差。
`SealDomain`：`范围半径Z = 200`（上方纵深）+ 新常量 **`下方容差Z = 40`**，判定：

```actionscript
dz = 状态.中心Z - 单位.Z轴坐标;                  // >0 = 单位在中心上方
if (dz > 范围半径Z || -dz > 下方容差Z) continue;
本侧半径Z = (dz >= 0) ? 范围半径Z : 下方容差Z;   // 椭圆上下各按自己那半收窄
```

### 补：`法阵Y偏移` 常量（同技能，主动战技.as）

用户提醒"没敌人时的兜底位也要带偏移，否则和玩家不在一条线上"。偏移后来从 90 调到 **50**（用户嫌 90 太多）。
两处落点统一写成 `参考点._y + 法阵Y偏移`：

用户提醒"没敌人时的兜底位也要 +90，否则和玩家不在一条线上"。两处落点已改为
`参考点._y + 法阵Y偏移`：

- `建上下文` attachMovie 的初始 `_y`（无敌人时法阵的出生位）
- `取落成位置`（没有法阵坐标时宿主自己推算的落点）

有敌人时不走这两处：法阵元件自己的帧脚本会把 `_y` 设成"目标脚下 +90"，
`落成` 在撤法阵前直接取 `法阵._y` 当落点（1591 行），所以天然带偏移。
**元件里那条跟随逻辑必须手动带上 90** —— 改 `法阵Y偏移` 时要同步改元件，不然两处对不上。
