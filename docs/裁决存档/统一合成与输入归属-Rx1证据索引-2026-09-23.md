# 冻结包证据索引

以下路径均相对于原输入 ZIP 解压根目录，行号指 UTF-8 文本实际行号；JSON 使用字段路径，不虚构页面或 filecite。本文仅定位已提供材料，不代表所有声明已由本次 Windows 重跑认证。

## E01｜包与产物身份

- `manifest.json`：scope 明示 local manual review，不是 runtime release/offline build kit；61 项 path/size/SHA-256 本次核对一致。
- `executed-artifacts.json`：head 为 `51e2ebb981f1e9728d54004a41416da03b0545c5`，status 为 NOT_DEPLOYED + dirty worktree；9 个运行产物的声明哈希，不含其二进制。
- 本次 `review-verification.json` 记录独立校验结果。G2 的 managed/native 声明哈希与该表一致，不等于构建复现。

## E02｜任务、范围与当前状态

- `00-提交给Pro的提示词.md` 全文：两条有限路线、不能只扩位/写 floor/假定重建；G2 证据范围及真实装配要求。
- `01-补证报告.md` 全文，特别是第 1、3、4 节：长会话反例、当前测试状态、人验前条件。
- `sources/docs/统一合成与输入归属-分阶段施工计划-2026-09-22.md` 第 9、11 节。当前状态以 §11 为准，早期正向标题/状态不能覆盖该续接记录。
- `sources/docs/裁决存档/统一合成与输入归属-R1裁决-2026-09-22.md`：RCE-1/2/3，G1/G2 与 C1 有限样本边界。

## E03｜长期反例与旧默认覆盖

- `evidence/g1-long-session-20260923.log` 全文：五个 epoch、两个零 floor、交付 1/1/1/0/0，FAIL failures=2。
- `sources/launcher/native/world-compositor/InputBridgeSelfTest.cpp:85–99`：长会话模式，不设置两个 floor。
- 同文件 `148–175`：半范围与 wrap 默认用例主动设置/推进 floor。
- `evidence/g1-native-recheck-20260923.log`：默认 19/19 与长期失败分开解释。

## E04｜接收端比较、交付与手势状态

- `sources/launcher/native/world-compositor/InputBridge.cpp:18–35`：lastEpoch/localFloor 及 StaleEpoch24；队列有界就安全的注释。
- 同文件 `143–182`：Init/Stop/Repaint 在普通门之前；consumedSeq 先写；shared/min/local 门；lastEpoch+1。
- 同文件 `196–221`：Down SetFocus，按钮状态存完整 flags，up/activeGesture；MOVE+Down 两次 original 调用。
- 同文件 `223–237`：失焦/菜单边界使用 issued；没有清 activeGesture。
- 同文件 `55–80`：IAT 包装器也访问 shared/target/enabled 全局态，退休范围不只 WindowProc。

## E05｜Host 发号、队列、取消与编码

- `sources/launcher/src/Guardian/WorldCompositor/WorldCompositionSurface.cs:41–49`：计数器、Host IsDragging 的有限语义。
- 同文件 `84–114`：新手势 epoch++，sequence++，queue_full 取消。
- 同文件 `138–177`：目标改变拒绝、几何重映射、Send 成功后 focus 回调、Up 本地清理。
- 同文件 `192–225`：RefreshPointer sequence++；Cancel 总是 epoch++；floor 先于 Post；hidden 触发取消。
- `sources/launcher/src/Guardian/WorldCompositor/WorldPointerMapper.cs:26–50`：epoch24/geometry8/sequence32 编码及比较镜像。

## E06｜Host 共享状态与退出缺口

- `sources/launcher/src/Guardian/WorldCompositor/NativePointerBridge.cs:34–66`：启动/READY/shared map/background completion。
- 同文件 `68–103`：每次 Send 直接 WriteIssuedEpoch；RaiseMin；EndpointConsumedSeq 非完成 ACK。
- 同文件 `104–120`：RepaintAsync 可在途；Dispose 发一次 Stop 后立即释放 Host 映射。
- 同文件 `126–169`：88 字节共享页；plain Marshal.Read/Write；低24位掩码；signed consumed。
- `sources/launcher/native/world-compositor/InputBridge.h`：协议 v3 布局、共享88字节、无数据 session/gesture 字段。

## E07｜桥恢复、旧消息与 broker 独占

- `sources/launcher/native/world-compositor/InputBridge.cpp:120–132`：Restore、未验证的 WNDPROC/IAT 恢复、当前回调内 stopped=1。
- 同文件 `252–290`：Initialize，映射验证、状态归零、DLL pin、子类/IAT 安装。
- 同文件 `313–320`：target 为空时任一 packetMessage 可触发 Initialize；stop 检查及 Restore。
- `sources/launcher/native/world-compositor/InputBroker.cpp:22–43`：mapping、SetProp、hook、Init，缺少跨版本实例级安装互斥。
- 同文件 `45–63`：初始化/关闭等待、Stop 重发、超时后仍 unhook/remove/return；删除未校验属性仍属于本次实例。

## E08｜G1 原始队列结果与夹具边界

- `evidence/g1/summary.txt`：46 个 ASSERT PASS；S1 endpoint-only、S2 Host Deactivate、S3 saturated queue CancelPostFailure；旧包未见、新手势重准入。
- `evidence/g1/host.log`、`target.log`：实际 epoch/seq、队列/焦点/原处理器轨迹。
- `evidence/g1-queue-recheck-20260923.log`：该次运行完整输出。
- `sources/launcher/native/world-compositor/g1-fixture/Program.cs:188–205,215–218,260–271`：FocusChild、生产 Intake、consumed>=seq、每次 readmit 显式 refocus。
- 同文件 `349` 附近：Surface focus callback 仅 HostLog("focus_cb")。
- S3 未证明“已经交付 Down 后，CancelPostFailure 且无后续包”的清理活性。

## E09｜G2 原始结果、几何与未执行声明

- `evidence/g2/{positive,stall-f,hold-frames,capture-output,flash}.json`：`topologyExperiment.contentPhases[]` 下 capturedFrames/presentedFrames/verdict/proofs[].Roi。
- 上述文件：`inputForwarding`、`humanAcceptance`、`performanceBenefit`、`ownedFlashExitedNormally`；不能略去。
- `evidence/g2/summary.json`：两正、三负、四非法组合，共9项；预期负控 exit2。
- `evidence/g2/flash.json`：`topologyExperiment.fRegionScreen`=(109,212,640,320)，`.f.windowRect`=(109,213,640,320)；`.assertions.cropIsFRegion` 不能独立解释差异。
- 同文件 `.entryFacts` 注明 AltTab/Taskbar 为 derived；`.untested` 列明多屏/DPI/负坐标/真实键鼠。
- 所有上述 JSON：声明 managed/native DLL 哈希与 executed-artifacts 一致；原始截图不在输入包内。

## E10｜G2 检测器生产/Probe 实现

- `sources/launcher/perf/flash-compositor/Program.cs:208,262–268`：F 区域实际从 embedPanel 取，crop 相对于 S frame。
- 同文件 `274–296`：Flash 嵌入与记录的实际窗口矩形。
- 同文件 `479–504`：crop/覆盖断言复用 fRegionScreen；未直接核对实际 F 客户区屏幕矩形。
- 同文件 `604–630`：多份 ROI hash 变化、逐像素误差 <=2、marker/palette/未证明的分别判定。
- 同文件 `643–651`：自建 Flash 素材进程请求关闭后超时强制清理。
- `sources/launcher/native/world-compositor/Compositor.cpp:381–388,452–487`：诊断调用在 Present 之前；raw 1:1 ROI 全RGB摘要/逐像素对应；每份两次完整 CPU 回读。
- `sources/launcher/perf/flash-compositor/test-g2.ps1` 全文：9项套件的实际成功条件。

## E11｜显示生命期与焦点执行门

- `sources/launcher/src/Guardian/WorldCompositor/WorldCompositorController.cs:91–124`：bridge Start 和 native/session/surface 初次创建绑定。
- 同文件 `199–208,243–255`：异常进入整体 StopCapture/owner.Close；失活 Cancel；整体 Dispose 不适用于常规输入续接。
- `sources/launcher/src/Guardian/GuardianForm.cs:720–770`：watchdog 抑制、资格与外层执行前复核。
- `sources/launcher/src/Guardian/WindowManager.cs:943–1008,1035–1055`：实际主尝试及 fallback SetForegroundWindow/SetFocus。
- `sources/launcher/src/Guardian/PanelFocusRestoreGate.cs`：纯策略/资格更新，不等于每个 Win32 执行点都已经通过真实测试。

## E12｜Host 全量、跳过项与独立验证范围

- `evidence/g2-host-tests-20260923.log` 末尾：5704总计、5700通过、4跳过；testhost confirmed parallel test collections=off。
- 同日志含4个 `[SKIP]`：PortraitWebViewRuntime；PlayerInfo B006VisualCapture；PlayerInfo B006RuntimeQualification；PlayerInfo B005RuntimeQualification。
- `evidence/g2-roi-native-build.log`、`g2-roi-final-build.log`：记录的构建过程，未由本次执行重建。
- `evidence/g2-doc-final-20260923.log`：存在历史链接 warning；`g2-diff-check-20260923.log`：包内 diff check 记录。
- 本回包 `verify_review.py`：只读校验 JSON/文本/哈希与数学边界，不执行输入包代码，不是实际修补或 Windows 回归。
