# Launcher runtime v2 可复现构建与发布列车

**文档角色**：Launcher Windows runtime 的身份、构建、证明、排队、promotion 与 CI 策略 canonical deep doc。

## 2026-09-05 当前正式发布：R1 存盘迁移、领取续查与保存反馈

本节为当前发布锚点；后续带较早日期的列车记录均是历史，保留其当时状态，不覆盖本节。

- implementation `962dbbb701`，合并上游 `bbdb1fac1de053f195c3d041f534edbc7a30cb7a` 的 merge commit `5891ce2631`；release source `5a9d06210244bedebac41d19579ca1921c362182`，不可变 tag `runtime-build-v2/20260905-r1-reward-save-feedback-v1`；release tree `4499716f3ca13ccd058d25b08b51c58b93d1bfe5`；request `089854B6A952C942254B54FABB2C4618142AEBD0AF17EC9536B7741E023F0F16`。最后一次派生字典变更仅刷新排序与生成元数据，物品/发型集合未变。
- build identity `9F1B2BF9AB9AACF23FFC34924A66137376EE9CE232A98BFB7B45AD6950444B4D`；33-file payload closure `E3AC8562C7CE83DAABCF8D329D31A472D6970B99074A3A58871C4928072C188E`；正式 Core DLL SHA-256 `B86236D9B67DF20E46E1C9E3D9AE437D90502DE601A22D9E455C6221FB5F069C`。
- 本地 X509 `builder-local-c / physical-host-c`，keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`；GitHub OIDC/Sigstore `github-hosted-windows`，builder `47668032A22DA24C1AD5B651D4D46284C6E000B7F099DB536BEC31557B003B23`，[cloud run 33945207841](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/33945207841)。云端获取签名小包后，对真实本地 CAS candidate 全量 replay；两端同一身份和逐文件闭包满足双 signer / 双 faultDomain，没有复制凭据或把开发候选当作 builder 票。
- production policy **40/40**，receipt SHA-256 `DC798B6BFF338707E7ABED5E0E41CBF1987B63FB7A84DA8820E9865057B344C9`；manifest SHA-256 `2BB68BFE20D52ED231758F0000A1328E11F6372887CD8F671343E6449D2CE6CF`；promotion 后磁盘 consensus SHA-256 `5B6712C6351033101B47F09E730CBF24D20515CD830C0CB145B3283BEBD1F967`。Git 换行过滤后的 blob 身份与磁盘 SHA 是不同口径。
- `2026-09-05T04:47:43.140698Z` 完成原子 promotion。正式运行库、签名共识、GitHub proof replay 与正式根 bootstrap `--verify-only` 通过；旧 bundle 保留在 `tmp/runtime-promotions/20260905T044715246Z-39e417003c4e4b919b230a5489a07dd5/previous`。部署提交与 post-promotion Audit 待回填。
- 合并后重新由 CS6 发布 asLoader；完整 Launcher runner **4,713 通过 / 3 既有跳过 / 0 失败**，AS2 map-loot **684/684**、Web loot state **80/80**。源与 SWF 归属、旧 UI 不刷新的依据和精确证据见 [R1 收尾记录](R1存盘API迁移收尾-2026-09-05.md)。本轮为 **promoted / FIELD_REVALIDATION_PENDING**；用户对合并前方案反馈“可行”并授权发布，没有在合并后的正式入口重跑游戏旅程，不称业务 `standard_entry_verified`，不覆盖既有专项边界，也不代表新稳定整包。

源码推送审计 [run 33945185729](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/33945185729) 成功，精确输出 `state=source-ahead / deploymentChanged=false`，base `bbdb1fac1de053f195c3d041f534edbc7a30cb7a` → source `5a9d06210244bedebac41d19579ca1921c362182`。它不替代部署提交的审计。

## 2026-09-05 上一正式发布：军阀基础闭环与战后黑屏修复

本节保留上一次正式发布及其当时验收边界。

- implementation `29157bff9a`；release source `41a8bdc9d55bc153d9f9678aa646c34eb7691883`；不可变 tag `runtime-build-v2/20260905-warlord-basic-loop-v1`；release tree `a9a0d3f899e459ec13f5e60370d0c3b2ea930744`；request `89508212BBC4C24CC7E70D5871C9858FC86A83791E2CA18A33353F843554660A`。派生字典仅有排序与生成元数据刷新，没有修改存档内容或修复语义。
- build identity `7902F8DFCE50E3F09C245E0F849E9AC3E3D7B07791D66C543C60E38472A59CAE`；33-file payload closure `09E8754A6E8DEE9ABB4A389AA7F578AC92E19650268912EC42B44FE9E63601A3`；正式 Core DLL SHA-256 `27AF835FA33C71E0E9CE0FE82A96D54D0D4270226DD0CC053814DC5A98CE1B55`。两端产物均与人类已验收的 `warlord-s61-visible-r16` 逐字节一致。
- 本地 X509 `builder-local-a / physical-host-a`，keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`；GitHub OIDC/Sigstore `github-hosted-windows`，builder `E00FB938522A8EE72B480D8E9210591D28AB1D17413A3FBF0217A1231D8921AE`，[cloud run 33939427506](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/33939427506)。本地使用已有不可导出私钥，没有修改 registry 或复制凭据；同一 source tag/commit/tree 的两张真实票满足双 signer / 双 faultDomain。
- production policy `40/40`，receipt SHA-256 `20BB1761AFD7C9512FEDEA615A68D41D458767217CF1829945DBAC1A50CFD50F`；manifest SHA-256 `A1B0F8676A79280ADCF113D3FCAA07767A227E0573FE7105E88C44663AEE1356`；promotion 后 consensus 文件 SHA-256 `16EFAF87C4221E1301DB80EB3C9E4083A13E78EC6C05A35290CBDE33D439F828`。Git 换行过滤后的 blob 身份与这里的磁盘文件 SHA 是不同口径。
- `2026-09-05T02:40:48.6497994Z` 完成原子 promotion；部署提交 `2b2913d3bf453f1b6783a68509f72e8058320bfa` 已快进推送。完整运行库、签名共识、独立 GitHub proof replay、暂存态 bundle/consensus 与正式根 bootstrap `--verify-only` 全通过。旧 bundle 保留在 `tmp/runtime-promotions/20260905T024022536Z-6ff22c46a8784dd0a5627f7cd80fe305/previous`。
- 人类在同字节 r16 完成约 30 分钟 Demo 2：38 场战斗均 admission/terminal/resume applied，最终胜利、领取完成和正常退出。该模块准确状态为 **HUMAN_ACCEPTANCE_PASSED / promoted**；本次没有执行发布后正式入口真实游玩，不称业务 `standard_entry_verified`，不外推 Demo 1 或所有 Slice 1–6 功能。军阀仍只从测试菜单选关，不开放 Slice 7；机器有主板故障，未使用 Computer Use、自动游玩或 GPU 压测。本机 producer/政策检查串行、限制到两个逻辑核。

远端部署审计 [run 33939954269](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/33939954269) 首次 attempt 已完成并成功，于 `2026-09-05T02:50:02.5979761Z` 精确输出 `state=promoted / deploymentChanged=true`，base `41a8bdc9d55bc153d9f9678aa646c34eb7691883` → deployment `2b2913d3bf453f1b6783a68509f72e8058320bfa`，再次通过 33-file closure 和双 signer / 双 faultDomain 共识。源码先行推送的 [run 33938717114](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/33938717114) 也成功，但不替代本次部署审计。

## 较早列车记录（历史）

**当前正式发布（2026-09-04，K 店批量领取与 itemUseOpenMany 多包开礼包）**：business source 为 `b1d2c5d6d1`（K 店 shopClaimBatch 全链）+ `249b9c9a19`（itemUseOpenMany 全链）+ `90964f71c9`（openMany Web 模块 WB112 拆分），最终 release source 为 `565d8e09ce364c23ad543e821d2957f011315cfd`（tag `runtime-build-v2/20260904-kshop-batch-openmany-v2`）、release tree `17e222281768161e30d19ef6ef3b3cc9e4b833e9`、request `B09B5A3BD9B7DA7D0D0326CDA145A5A6EB636E5A1D719433F633F91A12D8ED93`（v1 request `BE413607…` 因业务 commit 自身 WB112 超限停于 receipt 38/40，业务侧拆分后按选项 A 重冻结并原子 supersede；v1 tag 保持不可变未移动）。build identity `AA44637ADD9537621F01EB5610BCC3FB7C0486F142934CA0D17E0A491B67F238` 与 33-file payload closure `CC66C6AF455036A1CCC22F21A3CF483861CA060E9752422765294503C2C56FE0` 在 v1/v2 间完全一致（Web 拆分在 payload 三域之外）。
本机不可导出 X509 `builder-local-c` / `physical-host-c`（keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`）与 GitHub OIDC/Sigstore builder `FF23CB7709DE3C28F28618333C7DA1D21B413CD962DA9247DE29081A7CCB8207` / `github-hosted-windows`（cloud run `33865563931`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `3948B44863DC42757EAEC42117470821F8EF1CEEDBB57DAD65B1058268B1C8DE`（与 v1 candidate 逐字节一致），40/40 production receipt SHA-256 为 `C2DC5432C8B77B4743D44F2A9DA85EB8F79358D47962E288C494B25FBEB2A7DE`，manifest / consensus SHA-256 分别为 `BDE91FBED55E7AB4D4D19BCCDDDFC6CE8F612E1286043247B85D0F904FBB9943` / `C6395833003E61B8AAF60C62F75223ED6A43DD459E535DC82C817F77518A5836`。
直接 promotion 全链重新核验 release tree、production policy、两份证明、33-file closure 与 signed consensus 并原子写入正式 runtime；GitHub proof replay 与正式根 bootstrap `--verify-only` 通过，旧 bundle 留有可恢复备份。deployment commit `14c569d1ef856faa05388af090f796f7299bd45a` 已 ff 推送，post-promotion Audit run `33866627449` 通过并精确输出 `state=promoted`、`deploymentChanged=true`（base `565d8e09ce…` → head `14c569d1ef…`）。
本列车业务内容：K 店 legacy 待领取原子批 `shopClaimBatch`（Host Fresh/ReplayOnly 绑定、wire v1 行指纹 `(purchasedToken,rowFingerprint)` 双 FNV-1a lane、全或无 {0,K}、专用非淘汰 receipt lane、单项 claim 同 bundle 升级三重绑定）与 `itemUseOpenMany` 多包原子开礼包（K 次 roll/append、一次扣 K、一个 receipt、一次 fence、失败 exact restore 不承诺同随机结果）；测试基线 KShop 75/75、PAT 117/117、map-loot 675/675、Launcher xUnit 4586 pass / 3 skip / 0 fail、Web 40/40 + Edge 155/155、contracts 70/70。当前准确状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`:测试员已于正式入口回归 K 店列表/单领/批量与礼包单开多开并确认有效(限定旅程业务复验通过,不外推完整产品 `standard_entry_verified`)。

**上一正式发布（2026-09-04，SafeExit Arm 门控真序列与 ArchiveTask shadow 有序化 R3a）**,SafeExit Arm 门控真序列与 ArchiveTask shadow 有序化 R3a）**:business source 为 `69cb62c39f`(SafeExit 面板 Arm 门控）+ `535ae56820`(ArchiveTask 单 FIFO 与墓碑同门),最终 release source 为 `6645fe58439887fb90070508661ca9e63095f68c`(tag `runtime-build-v2/20260904-safeexit-archive-order-v1`)、release tree `a927e3fedb7244d9516531a3e06b930a9caaddae`、request `BFA5D510633E5CBA7558A8B971F73D2F4C18A6EAB0E071306BB96683FCF6BDD9`。build identity `E78725290695BD5BCF405A1F142D7B39E32F6FC08C9727D420351DB75E2FE0F1` 与 33-file payload closure `478231438644BAB15CBAC92891A7162B9CCFC4227ECEAC8D31D7941DFBF56B1B` 为全新(C# 源在 payload 内,未复用任何旧 CAS 票)。
本机不可导出 X509 `builder-local-c` / `physical-host-c`(keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`)与 GitHub OIDC/Sigstore builder `4AB514011DDBEBA05DE1A567FBE6464596CD12632DAA06031F198D552B1CA1F1` / `github-hosted-windows`(cloud run `33827625530`)对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `53E93E1E7FED046EC76B55028A81A5826D5CC899B87D2D995D735D0A772023B0`(两构建域逐字节一致),40/40 production receipt SHA-256 为 `6DE7C0634CC2993F1C68F96ABF94709574D80C28A12113EE8F8849D03066273F`,manifest / consensus SHA-256 分别为 `B986893A0F09190C53992F49F44897B7A5BA8CBBCE02BF972E9600ACC22736E5` / `3EFF8513FDAB218829D0561FA43AF8A672096412D99C1E34BC21F43979CE86C8`。
直接 promotion 全链重新核验 release tree、production policy、两份证明、33-file closure 与 signed consensus 并原子写入正式 runtime;GitHub proof replay 与正式根 bootstrap `--verify-only` 通过,旧 bundle 留有可恢复备份。deployment commit `d195acaf23696f6820dad88ec2725403fc1dc983` 已 ff 推送,post-promotion Audit run `33828667059` 通过并精确输出 `state=promoted`、`deploymentChanged=true`(base `6645fe5843…` → head `d195acaf23…`)。
本列车业务内容:SafeExit 面板 EXIT_CONFIRM 只认 Arm 后本轮 sv:1→sv:2(背景迟到 sv:2 不再提前授权);ArchiveTask 内置全局单消费者 FIFO,bus 全部 op 与 seed/repair/rebuild 同步入口统一顺序门,墓碑检查/版本检查/原子写/基线更新同临界区,普通写不得清墓碑仅显式 recreate 换代,`_prevSnapshots` 只在写入成功且接受后更新;全量 xUnit 4572 = 4569 pass / 3 skip / 0 fail。当前准确状态为 `promoted / FIELD_REVALIDATION_PENDING`:部署后尚未从正式入口重跑安全退出保存流程与高频存盘下读档核对,故不称业务 `standard_entry_verified`,由测试员现场反馈收束。

**上一正式发布（2026-09-04,Reward 存盘风暴止血与 K 店 durable finality）**,Reward 存盘风暴止血与 K 店 durable finality）**:business source 为 `05d3656ad4e2a1c41d0a73538ced2b441aabe577`（存盘风暴止血三提交）,release source history 随后并入 K 店 durable finality `8dc2940e6c`、ADR 勘误与裁决路线 `af5f201938`、材料字典 provenance 重绑定 `ee3d418570` 与本列车 tag 配置冻结 `c666c3bbaa`,最终 release source 为 `c666c3bbaa18d505c289f488094d098361fe432d`(tag `runtime-build-v2/20260904-reward-save-storm-v2`)、release tree `ac0a3a8cd4e473b988273b19999dfe17101a0814`、request `23AD3915889502F63F198E98603FA9436775F9D0D1A02EACBE019B650273278D`(旧 request `BC1F4B475DD15E4C455996B75972DBDCE3ABDB27560A24E5B99B04AEBDA577E0` 因材料字典 provenance 过期按既定解法修复后原子 supersede,v1 tag 保持不可变未移动)。build identity `6AEDAFF36B75BA0C810B03F5A7B2241F3866A74F2DBF60BF2B549C17B4D1100E` 与 33-file payload closure `825F5B2F214A2C64C93FB66D81D38A37151B8D703522BF85CE5500188A669362` 在 v1/v2 两 request 间完全一致（材料字典属 policy 域,payload 三域未变)。
本机不可导出 X509 `builder-local-c` / `physical-host-c`(keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`)与 GitHub OIDC/Sigstore builder `AD099B7E03564E2BDEE20378EB26FC37DA9B2B238C146F874B42BB975ED3411F` / `github-hosted-windows`(cloud run `33820443529`)对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `23FD3DA709ABE22A7EA0F3CE6FFBFBB135821A8CF2B2B5AC636F6605B611570A`(两构建域逐字节一致,携带 LootTask mixed-cut fence;根 EXE / Core EXE / miniaudio 未变),40/40 production receipt SHA-256 为 `7339FD925F14A7E5D307D7E16A77780831754E1E0FD5C233AFFF1B313320985C`,manifest / consensus SHA-256 分别为 `5E7482F913002BAEA0D0C89525A9787DAE401E5E548514E5F7259AF5D3EF720B` / `1B77203DCCD3459ACC839C24976C329C4DE3AA38679A39441BEB5E4D34218203`。
直接 promotion 全链重新核验 release tree、production policy、两份证明、33-file closure 与 signed consensus 并原子写入正式 runtime;GitHub proof replay 与正式根 bootstrap `--verify-only` 通过,旧 bundle 留有可恢复备份。部署以 merge commit `81396275e24ee31182a24bd50c60c977111cc067` 与并行 K店/ADR 主线合流后推送(结果树精确等于 `af5f201938` + 3 个部署文件,零冲突,未 rebase),post-promotion Audit run `33821697381` 通过并精确输出 `state=promoted`、`deploymentChanged=true`(base `af5f201938…` → head `81396275e2…`)。
本列车业务内容:Reward claim root durable-cut 桥接(全量存盘 2N+1→N+1,探针 N=2/10/20 实测 3/11/21)、pending/commit_pending 混合投影三层封死(AS2 无快照 / C# fail-closed / Web 不应用)、语义 fault-cut 10 切面真实重启矩阵、F5 局部性能清理(存盘次数零漂移)、K 店 checkout/claim durable finality(flush 先于 commit,失败 exact restore 回 commit_pending)、材料字典 provenance 按 LF 规范字节重绑定(永久消除忠实 checkout 必失败缺陷);测试基线 map-loot 648/648、character-build 全绿、Launcher xUnit 4553 通过 / 3 跳过、asLoader 重发 Compiler `0/0`。当前准确状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`:测试员已于正式入口复验收件箱连领、连续开箱与 K 店 checkout/claim 并确认无异常(限定旅程业务复验通过,不外推完整产品 `standard_entry_verified`);自动门证据与后续路线(R1 分层 19 步 / R3a / R3b)见 [存盘风暴止血 ADR](存盘风暴止血-Reward相邻durable-cut桥接与存盘次数回归门-ADR-2026-09-03.md)。

**上一正式发布（2026-09-03,loot 车道只读观测与开箱拒开反馈）**:implementation / 测试反馈 source history 由 `2943c3458b` 收束(随列车并入维护者即时调整 `a60dfdd944`、材料字典重派生 `444c5c4ab4` 与本列车 tag 配置冻结 `d10d587e79`),最终 release source 为 `d10d587e79bb84e04b8bb58146ee799129ef1c39`(tag `runtime-build-v2/20260903-loot-lane-observation-v1`)、release tree `912210f10c65dff72c71bd9f6da39b548747cab9`、request commit `49ca0018b94015dd12091e632bcf5696fb6bc2e9` 与 request `22D549D3F7042FAB0A54D396D82311B7E0987DFB13A202432F3F783F912BE943`。artifact source `48E257CB5BD5A4477212F2F94A993BF647FC1D56B34F1713FCD8E6EE5642C2CB`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `EC04F15BDF401C073C5606FB47232BFC92931B8BA22841356B628A9C8FD755DE` 形成 build identity `CE754EDD6BB0E24DC98F347A6C3F26FC1836417C54A0A8D10C1F145F2CDF358F`;bundle tree `d4ef7b74a9f842fecac78dad2aba9f7aa76b22c8` 的 33-file payload closure 为 `DD530373E6FD1BFFC21ABCDFBBD78A1A76ED93DF3E5C4EB75A85513209C6725C`。
本机不可导出 X509 `builder-local-c` / `physical-host-c`（keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`）与 GitHub OIDC/Sigstore builder `CC063AE7B662E883F4B83B166144C873375F7497FE071BD62EADB622C60053C2` / `github-hosted-windows`（cloud run `33717586170`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式根 EXE / Core DLL / Core EXE / manifest / consensus SHA-256 分别为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`、`9C7055B4FD1E655A6FC217E0AFA141C81F998B030C4B82CDC6F70426CE9213EE`、`86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`、`847545E68D32AE85F599190E68725799A1597055BF968E825D5CF8DC6B3524BD`、`F541B95FF0A843E77C209FF00793E0E47C9EC85F9BEBF31E80AE812401F2DF6B`；40/40 production receipt SHA-256 为 `6A2558D754D5DE3BE3580F9E69B4A8C730AECC1B4E5930B8BF6262913D80528A`。
直接 promotion 全链重新核验 release tree、production policy、两份证明、33-file closure 与 signed consensus，并于 `2026-09-03T05:21:14.4751506Z` 原子写入正式 runtime；GitHub proof replay 与正式根 bootstrap `--verify-only` 通过，旧 bundle 留有可恢复备份。deployment commit `793c8b960235e30c94f7d1cd8dfe56f7dd5cdb20` 已推送，首次 post-promotion Audit run `33718671517` 通过并精确输出 `state=promoted`、`deploymentChanged=true`（base `d10d587e79bb84e04b8bb58146ee799129ef1c39` → head `793c8b960235e30c94f7d1cd8dfe56f7dd5cdb20`）。
本列车修复堕落城深处通关奖励数据（浮士德的情报书恢复必掉，主线 39 解锁）、新增开箱拒开玩家分桶 toast 反馈与 loot 车道只读观测（O1 tuple 扩展 `lootLane` / `rewardInboxActive`，Host 接受 5 键 legacy 或 7 键 tuple，其余 fail-closed）；Launcher 单测 4551 通过 / 3 跳过，asLoader 重新发布 Compiler `0/0`。中途两次 request 收敛：首个 request `04923B6AB213B502E6269142C057C2553FAED6CDA301A0F0CBF07589005261B2` 因材料字典 provenance 过期在本地 preflight 停止，第二个 `510482CF39ACF2F823E1778DD392D4614B1592109DE433FA087A47DF840F6F84` 为冻结本列车 source tag 配置而轮换；两者均未 dispatch 云构建、未 promotion，由最终 request 原子 supersede。当前准确状态为 `promoted / FIELD_REVALIDATION_PENDING`：部署后尚未从无 candidate 正式入口重跑主线 39 交付、拒开 toast 与连续开箱回归，故不称 `e2e_verified` 或业务 `standard_entry_verified`，由测试员现场反馈收束。

**上一正式发布（2026-09-03，Reward 根事务与黑市软锁观测）**：implementation / H-A / H-S source history 由 `bfdf261016` 收束，最终 release source 为 `0b71d91987bee27f399bedc90a2c648a8bcbf44e`（tag `runtime-build-v2/20260903-reward-root-o1-v1`）、release tree `68ae915ad90ab96dc31c6371b37a1469be11a934`、request commit `924328adfd2605e4344b58011267a6aee7ba980d` 与 request `A0DA1D0F5E3E91EF9A53BC8D81419E5917016B860EB49AC655DA4D83EDAF7367`。artifact source `865177C6F22B5CB516364F981CE19D0E7379E36DC7BD491D16C569F388B45D87`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `40ECE8032B7D80E6B0ECB724C2E9A2CC6BF92DD03BDE4E148035BAC0BF8D96FF` 形成 build identity `1E09139852A981BBDA4A99E65F4FCF5BDBDD71E44DDE39EE13FC5B71ABF29709`；bundle tree `997211baf097ac139fb0d2d79b6d454576d81ddb` 的 33-file payload closure 为 `81DE32947F126EEC968280C4771C427140A82268CA042F91BE945F60A441F76B`。
本机不可导出 X509 `builder-local-c` / `physical-host-c`（keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`）与 GitHub OIDC/Sigstore builder `A714D2836BD6EE928FD163BA46032555665E57F99D27DEAE151237CE0AD25CEA` / `github-hosted-windows`（cloud run `33701115609`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式根 EXE / Core DLL / Core EXE / manifest / consensus SHA-256 分别为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`、`AE0E56CFCA82E12DE54845636D36A3F2D5E9D9EFE94A7BFC30228B21E00F8E8B`、`86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`、`BEE0507B2F55C04039A38C086998A2A74043EF9003AA22C66B29C26CE33E750C`、`86124784FE893AA474A12BB1DB2B9770C3BA6DF8365779C42C3057FD0B8C52E2`；40/40 production receipt SHA-256 为 `BC6467ED923BE45E3341545A4DF35BD558B1F379A253E01DCE8D34E6A6A5C6C5`。
直接 promotion 全链重新核验 release tree、production policy、两份证明、33-file closure 与 signed consensus，并于 `2026-09-03T00:56:05.591629Z` 原子写入正式 runtime；GitHub proof replay 与正式根 bootstrap `--verify-only` 通过，旧 bundle 留有可恢复备份。人工验收候选与正式构建具有相同 build identity，但前者的开发态 managed bytes / closure 不充当 builder vote；发布硬门只接受本地和云端精确一致的正式 closure。deployment commit `95ea7e8f7c862ea28f6b8c7c906173a701d71336` 已推送，首次 post-promotion Audit run `33702253144` 通过并精确输出 `state=promoted`、`deploymentChanged=true`（base `0b71d91987bee27f399bedc90a2c648a8bcbf44e` → head `95ea7e8f7c862ea28f6b8c7c906173a701d71336`）。当前为 `HUMAN_ACCEPTANCE_PASSED / promoted / FORMAL_BUSINESS_REVALIDATION_PENDING`；部署后尚未从无 candidate selector 的正式入口重跑 Reward 跨重启领取与 O1 业务旅程，故不称 A/S 专项 `standard_entry_verified`。详细 H-A/H-S 边界见 [止血治理 ADR](AS2-WebPanel止血治理-窗口生命周期与Reward根事务-ADR-2026-09-01.md)。

**上一正式发布（2026-09-02，W/B0 窗口生命周期根治）**：implementation / H-W commit `28edc13560` 将 invalid geometry admission 移到 pause/focus/presentation 之前，保存 generation-bound committed snapshot，并为同代同矩形 restore 增加一次 focus-free presentation replay。首个 immutable tag `runtime-build-v2/20260902-window-lifecycle-w1` / request `AE7B4246693EAF19B66D1F92D82A6A4EE51A653D22B514B52E0B238B57CBECD9` 在本地 production policy 暴露材料字典 raw-byte sidecar 过期后停止，未 dispatch 云构建、未 promotion；tag 保持不可变，由 W2 全新 request supersede。
最终 release source 为 `b2a70248eb6fae5dda843d2a7f7156a18b03ef7e`（tag `runtime-build-v2/20260902-window-lifecycle-w2`）、release tree `c35b896578a4b5f7b8751081a600ef374781b589`、request commit `d90c901f863406d4fd4f8cf50d1167a9d579b9a5` 与 request `981A0D150FFDCDB1B2B430E44DE6B9D2FBE76726B8EAA515711950325A731096`。artifact source `9CC6F9121F454254C6D5F0BCC57751D63B7B2F4CDF99B019CF71D23308B69776`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `B69006CD1207B8FAF4361A5CA8B548DFC703DAE47EB5FB4AA9142CD63584061B` 形成 build identity `7BD7CB663FFE7F338BBAA5566CD738B22A106A0835D272A7498D43C2EBE7972D`；33-file payload closure 为 `DBAD534395A8383DFD29DF0321BFEE39AC30040763A957E8260013E6D480F18A`。
本机按一次性 enrollment 新增的不可导出 X509 `builder-local-c` / `physical-host-c`（keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`）与 GitHub OIDC/Sigstore builder `8B958CC4E6C87DC7D9406842EA1AC0CEBAF8D4FFFE93332BC4AD421D893CFD8D` / `github-hosted-windows`（cloud run `33641807449`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式根 EXE / Core DLL / manifest / consensus SHA-256 分别为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`、`3B346B9818FFACC536B250BC2CB41D9FD67FE19B8D74CD82DF2527430226A361`、`9D888B3BE393BDAB4CF10C74ECF912E6F2744867A15707B4819A7B42DC7E1E01`、`8DCD3B45730ED595F2A558A1C6539E39D2F06A9202F12E401669DC6F33AD6220`；40/40 production receipt SHA-256 为 `869F8855B37274D3FD88F47877E040C4A27628E81D590357891107EFE58BD2E7`。
严格 `-VerifyOnly` preflight 明确零 runtime/release-state mutation；正式 promotion 随后重新执行全链并于 `2026-09-02T14:47:42.0256661Z` 原子写入受控部署。deployment commit `a3b0b5f77027be295cf574c6751310f634067812` 已推送，首次 post-promotion Audit run `33645182028` 通过并精确输出 `state=promoted`、`deploymentChanged=true`（base `b2a70248eb6fae5dda843d2a7f7156a18b03ef7e` → head `a3b0b5f77027be295cf574c6751310f634067812`）；worktree/index bundle、signed consensus、完整 GitHub proof replay 与正式根 bootstrap `--verify-only` 均已通过。
维护者已在隔离候选完成同尺寸任务栏恢复、QQ/浏览器外部抢焦与关闭路径复核并明确回复“有效”，因此 W 专项状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`。该 H-W 候选不是正式发布票；部署后尚未从无 candidate selector 的正式入口重跑 W 业务旅程，故不称 W 专项 `standard_entry_verified`。A/A1 Reward 根事务与 S/O1 观测属于独立第二列车，不能由 W promotion 代签；其后续独立发布状态只读本文顶部当前发布段与[止血治理 ADR](AS2-WebPanel止血治理-窗口生命周期与Reward根事务-ADR-2026-09-01.md)。下行仍以“当前”描述的早期段落均按历史列车输入阅读，不覆盖顶部当前段。

**历史正式发布（2026-08-30，测试反馈稳定性修复）**：implementation commit `f1dddf655898df802d680f0d382372c687d34076` 修复关卡奖励持久化/即时交付、Loot v2 自动入栏、药剂 v3 原槽 affinity、空背包装备调制关闭、动态插件槽容量与前台所有权判定。首个 immutable tag `runtime-build-v2/20260830-tester-feedback-stability-v1` / request `82E4C73347D16FADC3888273FEC1F73AB432F9CB9F332F7A502B22A6A3C325DD` 在本地 production policy 因材料目录 provenance 过期以 38/39 停止，未 dispatch 云构建、未 promotion；刷新受版本控制生成物并轮换 tag 后，最终 release source 为 `5789d597fbb7af32753fe4a35887b1f2a3a34e10`（tag `runtime-build-v2/20260830-tester-feedback-stability-v2`）、release tree `f0151025a88c416a5580ca658c9917ca3a922b4a`、request commit `6bfea1a72670ca40d244bef2a27c7a4bf7fa2fbf` 与 request `90E4BFB9875C73EE6672E89F404763C68E2BF33184AD2ED68421D5C975ECEED6`，并于 `2026-08-30T02:57:47.5575637Z` 完成正式 promotion。v1 tag 保持不可变，旧请求只作 `NOT_DEPLOYED` 失败历史。
artifact source `06B5634D68D11382661222292178024A47F634DF5FA819ED47E34587ED6204F4`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `EDC62EE43A7D347A2DBCDF7D1FB864595ED456757BE14B9743E55C66160F140C` 形成 build identity `9B146D22C82925853757DCA8D63CDDBECD31CD19C43EC1771F6B26D8F5824CEE`；payload closure 为 `C8B22C4532AD259E4C514BED30C87DC016E2FE17F9C001158F4CF18F900E1C94`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC/Sigstore builder `0DA07505D82F271D66FB0CFEB0E1ABB16BF7652CB94C6184EAF424243BDD8B95` / `github-hosted-windows`（cloud run `33288898204`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `783ED17121AF986074894A4BEC0D09B28046FE7BD068F8F3522174462936A529`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `2ABC014B29EF7551F42D483C3BBF123E0FE9582F9DBD2F4CD76294C446295BCF`，consensus SHA-256 为 `7114944CAFD7C52F608EBDF22A37990EDEE88BDE29B872C4A97A7C8B284DF67E`；39/39 production receipt SHA-256 为 `E35133850D7DBAD88B3AD9EF28ACC44E9DA8D097D29C3EC719C6A1294578FF79`。
deployment commit `3e23bda255dae09e20e309a12c5b21d86b28f347` 的首次 post-promotion audit run `33289302965` 已通过，并明确输出 `state=promoted`、`deploymentChanged=true`（base `5789d597fbb7af32753fe4a35887b1f2a3a34e10` → head `3e23bda255dae09e20e309a12c5b21d86b28f347`）；worktree/index 33-file bundle、2 signers、2 faultDomains、GitHub attestation、signed consensus、原子 promotion 与正式根 bootstrap `--verify-only` 均已通过。
本增量准确状态为 `promoted / FIELD_REVALIDATION_PENDING`。干净开发机未能稳定制造外部抢前台，正式入口也没有重跑领奖重启读回、战斗空调制关闭或药剂三来源回原槽，因此不称 `e2e_verified`、`HUMAN_ACCEPTANCE_PASSED` 或业务 `standard_entry_verified`；这些由测试员现场反馈收束。

**上一正式发布（2026-08-29，启动前门与角色创建迁移收尾）**：release source commit `95019c7e63492d9cb88c010d1ee06376281c590b` 将主时间轴 82–126 的启动前门、角色创建、作者/版本 Markdown 与版本记录迁入 C#/Web，同时保留 AS2 最终一次权威提交、frame 130 既有读盘网关与旧 SOL/备份保护契约。immutable tag `runtime-build-v2/20260829-frontdoor-authority-migration-v1`、release tree `dba425955faeae279dd083a9ee97ffbb900bf07c`、request commit `6a7df11f44c3075ee422598e1d2bd7a7be433243` 与 request `86DC638511BFE275791BB2A2DEAC525F8B2CC51675DDD917C44F07F57C34E9DA` 于 `2026-08-29T13:38:56.2121450Z` 完成正式 promotion。
artifact source `40C06A30219974E012B6321AEAEB1F41437A54850F77F1A0280995BB6206F66F`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `D671817517C673B16C450CEDBE3B8B0DA34EF8E2D81CFA15AF0AFCB3AA1B720F` 形成 build identity `DBA565EFC247492C9921C24D664BC896083366346551F3145C993397304C4F4E`；payload closure 为 `B72DC4721E9062C3FE357259DCD41DB5DC2D74DA0CC524F781E89147FE872801`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC/Sigstore builder `37246F2160E8D13B17A1A2D61B2598594A9484E8C81D02FBBC98FACE97669180` / `github-hosted-windows`（cloud run `33255138023`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `BA36F11CEEB156D5946144D67C665077AC2E072BAFFDF2E4A624892BC6FE0876`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `8AA58BA24DAA581F1E8632E6F02D52E53D7D86A564F69FEC2DF2F36289BA386F`，consensus SHA-256 为 `FF1ECD1F32DEF4E905EE6D2AA8E868ACA3C978BA2B291826FCE4C27519AED241`；39/39 production receipt SHA-256 为 `FD4C8F62B8E782C9BCB672CC519E1F11955FD33544DC60B2715F854E975EF945`。
deployment commit `fdb4fe30aa911bbac91d60d7c349c217fa71a78c` 的首次 post-promotion audit run `33255727460` 已通过，并明确输出 `state=promoted`、`deploymentChanged=true`（base `95019c7e63492d9cb88c010d1ee06376281c590b` → head `fdb4fe30aa911bbac91d60d7c349c217fa71a78c`）；worktree/index 33-file bundle、2 signers、2 faultDomains、GitHub attestation、signed consensus、原子 promotion 与正式根 bootstrap `--verify-only` 均已通过。
维护者已确认最终隔离候选实际使用无问题，专项状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`。部署后没有从无 candidate 的正式入口重跑普通读档、新建 durable 后重启读回、重建备份或旧 SOL 保护旅程，因此不称本功能业务 `standard_entry_verified`；根 `--verify-only` 只证明正式 bootstrap、manifest、identity 与 payload 完整性。Audio H2 继续保持独立门，不能由本次通用 runtime promotion 代签。

**更早正式发布（2026-08-29，装备调制最终态与 Web 反馈修复）**：implementation commit `ec32a3406a1792925af91b623c225e3480862302` 封闭已穿戴装备经进阶/插件变换越级和 `replace_mod` 最终超槽，并收束不可用进阶、Web 错误反馈、四入口布局与强化度交换迟到候选误报。首个 immutable tag `runtime-build-v2/20260829-equipment-tuning-final-state-v1` / request `38B4DBFDEE2C3005DE46113F3797818C142CCB2E9D9DB8D8656B3E3E76CC9730` 在本地 production policy 因材料字典 provenance 过期以 38/39 停止，未 dispatch 云构建、未 promotion；受版本控制生成器刷新字典并轮换 tag 后，最终 release source 为 `cda82e6e4128faf8664235e48b3ed0d84c14a553`（tag `runtime-build-v2/20260829-equipment-tuning-final-state-v2`）、release tree `f52fe54c5e292856280343c74a0dd56cdbd2b3c8`、request commit `4c1ce32af56a92efc7ca3233baaa0f51f351fd23` 与 request `3A7030BF2E3177039DD012ED5B852AFD01171EA7690C22572DD7956115B6D426`，并于 `2026-08-29T01:49:04.3470426Z` 完成正式 promotion。v1 request 已由 v2 原子 supersede，旧 tag 保持不可变。
artifact source `E4633E4370CF41921FD7304F33F12F8038A48EAE1B4CC37A3588FF4A4EB29B16`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `83D5E4E91529EDD0D447A30B4E7C58E03F92C91B09CD755DD761BCF4F5D716C8` 形成 build identity `BEA826A1DD33CAB9548B19F328C8C16264EFDD27F2FAA407689B00EF679774B3`；payload closure 为 `4860E38FD8DA98B7B9A7080D51E986EE1BC9CEB8FC4BB2EEE28B7D6647881556`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC/Sigstore builder `ABE14CF3360B1AAC057E3A08DCFBC199E37915309B95E2E43DBBC9BC7C4D319D` / `github-hosted-windows`（cloud run `33227123193`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `AD1C53C8550AB21520CE5685ABCB3D189371EBD13B65F6987D2304F340DB4129`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `B1326FEE49829B12D8DA8504DBF32D1EF9CA31E2E0BF5425E18712A0B929DF49`，consensus SHA-256 为 `7FD54A794665E6FDBDCBE220D938637F3399AA39FC5EE088F961A0D7545AF90B`；39/39 production receipt SHA-256 为 `46ED9AF8CA18F6472705BBD9B5C66D08447E7D2930F21ED43C57FE5AA3516CF6`。
deployment commit `43c8ad6ea23f677bcb2dd7d406e8024b831d93dc` 的首次 post-promotion audit run `33227550372` 已通过，并明确输出 `state=promoted`、`deploymentChanged=true`（base `cda82e6e4128faf8664235e48b3ed0d84c14a553` → head `43c8ad6ea23f677bcb2dd7d406e8024b831d93dc`）；worktree/index 33-file bundle、2 signers、2 faultDomains、GitHub attestation、signed consensus、原子 promotion 与正式根 bootstrap `--verify-only` 均已通过。
维护者已确认等级/容量最终态、不足材料反馈、四入口布局和强化度交换修正版有效，专项状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`；非法或手改存档按单机开发期策略继续保留。部署后没有从无 candidate 的正式入口重跑装备进阶、插件变换、强化度交换或存档旅程，因此不称本功能业务 `standard_entry_verified`；根 `--verify-only` 只证明正式 bootstrap、manifest、identity 与 payload 完整性。

**更早正式发布（2026-08-29，关卡转场、复活状态与右上交互修复）**：implementation commit `306a3cb1e8140a0f3c86a09e8b7874ef50b9be8a` 后，release tag 配置冻结于 `daa09409fac05ff4ce778a04f70b22206b839dc2`；production policy 首轮预检发现上游 8 个装备模组 XML 已变化但 `material_dictionary.generated.json` 输入哈希未刷新，遂由受版本控制生成器更新并提交为 `b2385ee83ebd2511e501bc2b2fdd6131310aa663`。该 commit 是最终 release source（tag `runtime-build-v2/20260829-stage-nav-revive-v1`），release tree `6a410bd5e80c3d37f23f3554e5f5f86b09d54da7`、request commit `9b5f15a30960cd047331af3929dce714e7a9fd1c` 与 request `C64342535E6D8CEBD88D2D359A118C75CEC967071EA00A22CEA4783A769C69E9`，并于 `2026-08-28T23:33:31.1024992Z` 完成正式 promotion。旧 request `170D0CD29B68FAE6F5B2944B5BB4E32135312AB245E6D2A815D870CAD5735F33` 在预检失败后被新请求 supersede，未取得云端共识、未 promotion。
artifact source `DDF581BED63A37AE2861DD3FB8105110FB110F8EDAAEA53B7ABEF96B33A8A36A`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `BE8859A59D9940FAFDEDB79D3D57D3280033CE9F544DF52DF88670AA27723655` 形成 build identity `47001CF2926D0E47AF235AF3672BBEE984F69162570167029118FE61791A5B69`；payload closure 为 `24C95A76AEF22CC40A3EE38F61EB916DF6AD664AF4F5006462452BDD80ED0296`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC/Sigstore builder `387EC78270435A3DDF2687648CD0064864C561BC7419DBD48F8C74664F27F298` / `github-hosted-windows`（cloud run `33220240979`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `B03C2F9453CEA7F29A986421BC7182761DEC59E80ADC1726B628B59690BB984A`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `F9137C77777F4618D085894A8DC451A9CF1EB7BB14F845D880A135105980DAF4`，consensus SHA-256 为 `2BFE3D7CE6DD8A50725A4DC4B77B27DFA168D425522A3FF927CB588B62E4BF33`；39/39 production receipt SHA-256 为 `3D356FFA7B4D50F500DB69E86E66266AE33FFDFE659177434A85BEC712831772`。
deployment commit `95dec8e1c770187c57c98be6256ece6260fbed79` 的首次 post-promotion audit run `33221250923` 已通过，并明确输出 `state=promoted`、`deploymentChanged=true`（base `b2385ee83ebd2511e501bc2b2fdd6131310aa663` → head `95dec8e1c770187c57c98be6256ece6260fbed79`）；worktree/index 33-file bundle、2 signers、2 faultDomains、GitHub attestation、signed consensus、原子 promotion 与正式根 bootstrap `--verify-only` 均已通过。
维护者在与正式 Core 逐字节相同的隔离候选上完成选关、死亡/复活、返回基地、焦点与复活按钮布局复验，结论为 `HUMAN_ACCEPTANCE_PASSED / promoted`。部署后没有从无 candidate 的正式入口重跑上述业务或存档旅程，因此不称本功能业务 `standard_entry_verified`；根 `--verify-only` 只证明正式 bootstrap、manifest、identity 与 payload 完整性。

**更早正式发布（2026-08-28，斗兽星期级全量标定 Gate F）**：implementation commit `6bb797cfc62eb4c5ddf9dffbb1952c3287b00729` 后，最终 release source 为 `c64a5440e5506a3f1567143711f984d063e56505`（tag `runtime-build-v2/20260828-arena-calibration-gate-f-v3`）、release tree `abe5cd0ef38fff3e596b4bfe4c84ea3c30e5ead8`、request commit `1d858e7716e61a0ca0e67df1cfd9b1166b3f7ee0` 与 request `62E4E1537771F0638DB9204A950DD19598962F7E8BBE7D73E2916FAB519982FE`，并于 `2026-08-28T00:40:47.8095293Z` 完成正式 promotion。
artifact source `9A8AA0DB1EB179908F0511FEE5B55272B904750CF4C9C313B2CEC916C3FEB240`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `3057C4A8F928E19EAB49262C75140ED9DFC3E46617B4E4D4BC7DB372E89F96CF` 形成 build identity `48E2ACEA81194C0D6C3A89226DEC2748192612B5514D3F3ADB8444FA4AF6C528`；payload closure 为 `DA7E5BD135FF2407ED7CE459F521BEA95C9CB6F5CC63AA5291ABAC795DAF59F1`。
本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC/Sigstore builder `97725DDD7EF0D7461B9EB8945B35F3F3AD1AC40FE4359BD38AB484F4BE249953` / `github-hosted-windows`（cloud run `33130111918`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `A6A3C16D322C03718679E2E1E83D6EB959D671C3BFA366A1D6C86D3B5CCE6364`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `5CE2C65C27DEE48D3320A4003B0E85DFC972A22F07AFC37B9A0FDAE834203240`，consensus SHA-256 为 `DE2B322575ACB4C4529B917F109759535DD80C788B32EB560D9C26374A5D94CD`；39/39 production receipt SHA-256 为 `AC2FE8CA89D3A88C70C383E4C3DF84639DDFA956761793A16D66780634D95738`。
deployment commit `693baf7051d9e67be8930b309dc14eea65c0eab6` 的首次 post-promotion audit run `33130680653` 已通过，并明确输出 `state=promoted`、`deploymentChanged=true`（base `c64a5440e5506a3f1567143711f984d063e56505` → head `693baf7051d9e67be8930b309dc14eea65c0eab6`）；worktree/index 33-file bundle、2 signers、2 faultDomains、GitHub attestation、signed consensus、原子 promotion 与正式根 bootstrap `--verify-only` 均已通过。
领域计划已把工作簿哈希 + 单元格 provenance、参数闭包、`contamination` Schema/fixture、timeout 口径、真正 Schema 实例校验、受保护存档快照和低注意力 durable campaign 纳入同一 fail-closed 契约。tracked draft 含 58 个 scheduled candidate，另将 `B12` quarantine，共 198 个 10–25 run shard / 3,255 个机器 run；首批三份 10-run fresh soak 固定为 `B2 / B9 / G2`，`C9` 仍保留 `10 + 20 + 25` run 与 side-swap。`node tools/arena-calibration/run-checks.js` 当前为 40 个 Schema / 64 项检查，`node tools/test-agent-entry-contract.js` 同轮通过。
早期 immutable v1 因材料字典派生过期而停止；v2 已通过 policy 与 GitHub cloud build，但 promotion 在任何正式写入前因 worktree `policyHash` 漂移 fail closed。根因为被忽略的 `tools/arena-calibration/node_modules/` 混入 Worktree policy view；v3 明确排除该生成依赖并加入回归门，同时把受 `.gitattributes` 约束的 AS2 发布输入物理规范为 LF。v1/v2 tag 均保持不可变，状态为 superseded / `NOT_DEPLOYED`。
在该 promotion 节点仅证明正式 runtime `promoted`；当时 Gate F plan 尚未 `freeze/arm`，三份 soak 与其余 195 个 shard 尚未执行，因此该发布证据没有新的正式斗兽样本、真人 PVE、Gate F low-touch/星期级通过或业务 `standard_entry_verified`。后续 campaign 进度以测试矩阵和 Gate F 自身证据为准，通用 promotion 仍不得外推为数值拟合、玩家体感或存档业务 E2E。

**上一正式发布（2026-08-28，双药剂组与八槽共享冷却）**：implementation commit `9a85f95e748a986faca8f9a846ab360e2c7900d2` 后，最终 release source 为 `b2bc05775c621616fe64be55354aebe21c63a2af`（tag `runtime-build-v2/20260827-dual-drug-banks-v1`）、release tree `826f37292cb591782e3d3cc41145b9158a426032` 与 request `9FE68E5BEB945B969A2D4CDAA1D20B2D2838A4208DB8773EF7344918B0383658`，并于 `2026-08-27T16:08:05.5759255Z` 完成正式 promotion。
artifact source `EACC73DC5A54D87EB390413EB54FF5A8DEEE631DBE619B297E8DBB9FC7434E67`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `8E444CED52CE3DB25C680663B7DDF9EF1DD1987D8F07D95FDBC778ED8E0AE955` 形成 build identity `6C9CF4699C217CC65083038D3AA69B0D6640C4E8DF5A50D367D0628E366D379D`；payload closure 为 `1D0C3A1272CD084ECC9532B0565E017E07A233C1511B4372C091FF081701252F`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC/Sigstore builder `C1BF6E8247E0D943EBD9480A4915504196BE5AB13BD90150E67D2460A06917FB` / `github-hosted-windows`（cloud run `33090620311`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `D793647E666423EB73FE78F8E272CDF9A2E5792B9D694259639A6B35DD1624D0`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `641A327E5D970B04CC1BD5E3B633EAD00C6D9BF0BA69C596ADBE2CCF4D6E04B7`，consensus SHA-256 为 `A8186A1597082E90C248A5DB9FE14E95F5D5970236F2C40E78A75F024CC4A512`；39/39 production receipt SHA-256 为 `361CAAEDD6322796631112882DB069C5B1C2F6599AF20685F9A20BB719BD6677`。
deployment commit `6902b2b6ed067c4882e9a67267d055ce0db90b34` 的首次 post-promotion audit run `33092179946` 已通过，并明确输出 `state=promoted`、`deploymentChanged=true`；worktree/index 33-file bundle、2 signers、2 faultDomains、GitHub attestation、signed consensus、原子 promotion 与正式根 bootstrap `--verify-only` 均已通过。
本列车把药剂背包扩为 `2 组 × 4 lane = 8` 个物理槽，但仍只有四条可同时使用的共享冷却；`6` 使用独立 `drug:switch` 冷却切换活动组，`7/8/9/0` 使用上下同列槽，成功切换同帧抑制用药且不重置 lane。Settings 升为 36 键 / `keySchemaVersion=2`，Character Build 严格投影 `drugLayout.v=2` 两排四列，PlayerInfo 第五列使用两帧 `○ / × + 1 / 2` 图标。fresh 门为 player manual `486+57+55+14`、Character Build `674`、Equipment Tuning `78+170`、Host focused `266/266`、Launcher `4308 pass + 3 explicit opt-in skip / 4311 total`、Web Character Build `222/222×3`、production workbench `1110/1110×3 + 12/12 + 18/18`、Settings `18/18 + visual 116/116`、PlayerInfo 与 asLoader fresh Compiler `0/0` / FFDec；维护者已确认功能、切换手感及最终图标有效，故专项状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`。
部署后前两次 strict formal smoke 在 Default input desktop 持续 `GetForegroundWindow()==0` 时按设计 fail closed 为 `trusted_runner_credential_timeout`，均不计成功。Default input desktop 上确认 `GetForegroundWindow()!=0` 后，复用同一 runner/harness 配置的无 candidate selector run `2478a9f25873043318f2402f80105b52` 取得同一 lifecycle 的两次 verified status、fresh handoff/reveal、可信 shutdown receipt、Guardian/Flash code 0、存档哈希不变且未触发强制清理；报告 SHA-256 为 `0F3349124141B5570B9FD51061AD736A71999CF1824DBF76B6AEBF50AFE41BF9`。因此正式 identity/lifecycle 窄纵切达到 `standard_entry_verified`；`businessJourneyExecuted=false`，没有从正式入口重跑八槽、切换、旧档/键位迁移或重启读回，不称本功能业务 `standard_entry_verified`。

**更早正式发布（2026-08-27，关卡结果原生状态槽与基地结算工作台）**：implementation commit `32ae67a1c57e10002ed00354d172ae88207a24fa` 后，最终 release source 为 `732898b8aa1308cf820976324f47bba97f654e41`（tag `runtime-build-v2/20260827-stage-outcome-settlement-v2`）、release tree `d498236a3ac06294f0c871dba73302bb24bc398e`、request commit `b7fc471c02ce5fc0290904773c78c720465ab20f` 与 request `CB8268D83DB35DF3BC7D22587822F279C29A93CD12A15B1F10DF14759B000991` 完成正式 promotion。
artifact source `90E820069B1C3D32E50A05CA28C9F1F6CFF83605863F95F56B86DDE8FFA24D0C`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `E16D72B231715E534D75DFFF47CBB64B0539512B0839C813D04722C4202018B7` 形成 build identity `03F9825C2A2A248C73A7FA6FFB4E6C4305D49C4F97BD9E6042747B9BC5A0F5AA`；payload closure 为 `5DA2B3053BF70EFCDCE74A503C33098774F9026AAD6E2D1F555C1A9EC5181D07`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC/Sigstore builder `8D01D752BC01F072456E3A39D97A4CCDC7674C98552EF44EDEDA2C6C352D32C4` / `github-hosted-windows`（cloud run `33070890481`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。正式 Core DLL SHA-256 为 `5B010C6040A5E80E1E8923FD02D6B632AB2D309511F8C6A54BA7D78B7516314F`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `55EF9CA79802FC020AD17F19ABB0E1AAAE8AC9F8442B051CC0272085B62B1C1B`，consensus SHA-256 为 `7F2C584599A851EBF265C22F564116043EE11D06E91EDB8DC80958531D706615`；39/39 production receipt SHA-256 为 `977B2B2A7466C307198438C1F8362CE9ED06F3415977F664CB4E402B4192A181`。
deployment commit `339b15694d631d483736880c0dfd44429f6926a3` 的首次 post-promotion audit run `33071572650` 已通过，并明确输出 `state=promoted`、`deploymentChanged=true`；worktree/index 33-file bundle、2 signers、2 faultDomains、signed consensus、原子 promotion、正式根 bootstrap `--verify-only` 均已通过。
本列车把仍在推进的关卡死亡/胜负决策投影进 `StageOutcomeTask → RightContextWidget` 既有 32px 条件状态槽，把返回基地后的行动报告、奖励领取、材料只读存量和库存整理交给 `sourceKind=stage_settlement` Web 工作台。AS2 `StageRunSession` 与 `LootContainerService` 继续独占胜负/生命、帧时、复活币、转场、奖励随机化和资产写；批量领取一次最多 50 个 exact lease。fresh 门为 AS2 `301/301`（StageRunSession `88/88`）、Settings `42/42`、Host focused `37/37`、Launcher `4290 pass + 3 explicit opt-in skip / 4293 total`、Web `98/98、6/6、57/57、9/9、28/28、17/17、68/68、53/53、63/63、18/18`，以及 Settings visual `116/116`、Workbench strict audit `0/0`、UI ratchet `67/67`、visual atlas `66/66`。`scripts/asLoader.swf` 为 1,161,797 bytes / SHA-256 `BB7823F9FF61F1B390810A8AD46913159DDED0FEE2926186451B255B01CFDB97`，fresh Compiler `0/0` 与 FFDec 复核通过。
维护者已确认与正式 Core 逐字节相同的 A3 隔离候选有效。无 candidate selector 的 `automation/start.ps1` 随后确认 `formal_runtime`、exact identity/closure、bus ready、正常 Guardian 关闭与零新增 Flash/WebView2/ports 残留；因未选择存档，预热在 45 秒 deadline 后以 Flash code 0 安全回收，没有 fresh `bootstrap_reveal_ready`，也没有重跑关卡业务。因此准确状态为关卡结果与基地结算 `HUMAN_ACCEPTANCE_PASSED / promoted`；本轮正式入口只提供身份、总线和正常退出窄证据，不称业务或完整入口 `standard_entry_verified`。首个不可变 v1 tag `runtime-build-v2/20260827-stage-outcome-settlement-v1` / request `ADAEC470A51D93C56D3A4E3154E2AC5EB1AA7D498A435AD126B258F64E00B563` 因 production policy 暴露材料目录派生过期而停止，未 dispatch 云构建、未 promotion；刷新派生物后由 v2 request 原子 supersede，v1 tag 保持不可变。

**更早正式发布（2026-08-27，库存显式批量转移真批量事务）**：implementation commit `96ee67c58d88ce202ee41e6ffc32037911349426`、协议与验收门 commit `b6e555d47293e2e709595d4673bfa40be2f885b2`、审计映射 commit `e979c2e8ba64aa2d9ec24aa579d8654c365540a8` 与人工验收记录 `801e0ae69b00a5f0dded5727d17e34940952464d` 后，最终 release source 为 `5f5cfce7c162ba616bfc51f3c03f3134e937d36a`（tag `runtime-build-v2/20260827-inventory-batch-transfer-v1`）、release tree `91e2378b4036f7cb6fdbdbe4aa15d656732b840b`、request commit `acadbcc35a2ee90e9b3c0ed1ccbd341db62c26bd` 与 request `BA233DEDA6FC141F195C2F58E0D71C47872C0ADF309128E3D1B06BCBD127E774` 完成正式 promotion。
artifact source `AEE69D3DBA29620856FC65541726E05910B502498DDDFD517F5D976FF2E41AA4`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `0C31ECE73330A24696BCB371B69C27345A7602A444BF0D9F3A8B0BC233E88CC6` 形成 build identity `E746A365F2715556A3298C4CBF1E926F039BC147625D4612F850768C9A52ADA3`；payload closure 为 `8175440099ECF58708CD35F81F9C4A0B81390EC64EF2896D96EB885779A20475`。
本地 X509 `builder-local-b` / `physical-host-b` 与 GitHub OIDC/Sigstore `github-hosted-windows`（cloud run `33032741729`）对同一 identity/closure 达成双 signer、双 faultDomain 共识；39/39 production policy、deployment commit `4d5fd254752a149ce07006f8f48391ab26485f61` 与 Audit run `33034014440` 均通过。维护者已确认大量物资即时完成；正式入口未重跑库存写或写后重启读回，因此准确状态为库存批量转移 `HUMAN_ACCEPTANCE_PASSED / promoted`，不称业务或持久化 `standard_entry_verified`。

**更早正式发布（2026-08-26，黑市鉴定注释通道）**：implementation commit `839b978901aa60f93c87ee7a55318970b053e6bc`，政策门修复 `f4d076743c0a92a1dccd30f4f7c06514e03cbbe8`（材料字典重派生）与标签轮换 `14ee9f0a802f6fb8b64a24180fd9fe6029fc0e3a` 后，最终 release source 为 `14ee9f0a802f6fb8b64a24180fd9fe6029fc0e3a`（tag `runtime-build-v2/20260826-blackmarket-annotation-channel-v2`）、release tree `bf8a78503df478497a266d76f9eb6da0a3a3bd8c`、request `76B301432063B7E899D26B97EB072A10EC11F0B58A0C207AD9F8D58CAF92A599` 完成正式 promotion。
artifact source `A124BF34DCE5C2BCD75FA1A7A7B5CE56A6FDB9C38E3A0AC1DBFDD0FC2AD1A0DF`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `3E9235EA9F12C23EF535988C0ABCAC6BBA9A11B1405C06B7AB114408A988B98E` 形成 build identity `EF970F10917CCE73F27BF1FA08002D2E13D135A1DB4570C79716CBDA47505D7E`；payload closure 为 `C9E487CFAA62A9E79B88AA8EA289F499EDDCECF7A26C76867EE1EDD4A4991FB4`。
本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `693B5CABF9C533D78161BDE3FFE9C7230270A48F72D5302E68ED8880C3874F4F` / `github-hosted-windows`（run `32973495149`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `E30C2FD4D5BE016C6554C818F85A7853CA8E27DFB6D6D64B3D53B685DAD0BDE4`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`（未变），根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`（未变），manifest SHA-256 为 `433A5E1637ECDAB40D675176E17DC92869B4D11FD18DA2C1C3E87DC7DEB4F04A`，consensus SHA-256 为 `9D87E89E059793BAE718C9AB3399316709D846DF78E82653B85DE3996FE42143`；39/39 production receipt SHA-256 为 `6BF7761B7B9A355A081382A4449C30475E90BA7DB8E62600E76531441A8AB641`。
deployment commit `e7b425886685b0147f8714d7284620487f111ad9` 的首次 post-promotion audit run `32976767231` 输出 `state=promoted`、`deploymentChanged=true`；worktree 33-file bundle、2 signers、2 faultDomains、signed consensus、正式根 bootstrap `--verify-only` 均通过。
本列车为黑市鉴定（匿名影子测试）新增 AS2 权威注释一跳：AS2 `blackmarketTooltip` 复用 `_root.Web物品注释HTML`，C# `BlackMarketTask` 仅 tooltip 透传 + `panel_resp` 包封 + 面板实例绑定，Web hover 走统一 PanelTooltip/buildItemRichHtml；面板另含合成台机器化视觉四期打磨、全目录 1438 件视觉池（同舱同组配对）、揭晓释放真实身份与调试抽屉。黑市仍是 `SHADOW_MINIGAME` 匿名 shadow 降级，不写正式库存/货币/存档；不称黑市业务 `standard_entry_verified`。fresh 门为黑市 Node `25/25`、浏览器 harness `11/11`、装备预览 `44/44`、Launcher focused `4/4`（BlackMarketTaskTests）、asLoader 编译 `0/0` 且 SWF 含新命令（FFDec 复核）。
维护者已在隔离候选目视确认面板有效。正式入口未重跑 hover 注释业务旅程（依赖含本 task 的构建进入），准确状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`。途中轮换：首个 request `8B2DFDF49175472978A19827680C51AA2F00B6C2B9E37637D1AE89EC01410E5F`（sourceRef 未轮换）与 v1 tag/request `7EADAFCBC659287BC73B7EB79674A867B9AE5F39799C04B6F10FDAF06E709EF4`（政策门暴露材料字典派生与头像工具行尾漂移）均停止，由最终 request 原子 supersede；v1 tag 保持不可变未移动。Audio H2 继续为独立 `pending`。

**上一正式发布（2026-08-26，打击伤害数字 C# 原生化）**：implementation commit `064f462b6e927bc56bae08e342e8be21aa5d17cf`，最终 release source `1eaa51d2afec10c8c6a9bf5abe1ff20343208e19`（tag `runtime-build-v2/20260826-hit-numbers-csharp-v1`）、release tree `9e30c52740f2c18e4fc3c732a571aa314e2fced3`、request commit `53c54c920baf0f20512b99208e99cdf8d9044f12` 与 request `99FA28B523CB89C3F68B14DEBE2DEC4A1C6B6E2A966DBEB665F605753290890A` 完成正式 promotion。
artifact source `EE2A0030DCFC9C60AB1FBAF9FBF1A8A6669B1A688EAA1F6902AE412F2A5E3046`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `BCC6BB9E315B41631760E668EA207DAF7ED8E49F45821B7E676AB2FEC067CA66` 形成 build identity `3B075D1FD1E429137253315E5B2095B4D3C942FE42D039E43D55C93625FDA187`；bundle tree `c41dc95621cb8453281d353267e35248ad507cac` 的 payload closure 为 `8E25D01458A05334FF1CFF44CB9A1F0E8B1BCE6420A4DE21D41B4D648479CF1D`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`，证书 thumbprint `647AFE92BD801518AF25F2A2EE1845E6847C2118`）与 GitHub OIDC builder `8654E63F0679502148828E5E6D0489BDEDE88312050D076F47F736ADA81EA69F` / `github-hosted-windows`（run `32923040870`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `5D160A15ECD569D3582E8BD41768A33C99BE92910E283276C514C68DABABC4BB`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `0D2A858320E93BF524D4C76EDA021FF59D8C2334503623DE7610C5BED334D43B`，consensus SHA-256 为 `8834037EEC57DC4CCB621ED8C6549692BBE5DCAC52F1DE621F681A4BA52B9202`；39/39 final receipt SHA-256 为 `096080A9620608D6CA1824D5E0D5DA09011A7E32D6204596D1F53A6A16098432`。
deployment commit `cef2c8d227049248d5767a06e0bf57700931e0b2` 的首次 post-promotion audit run `32923796401` 输出 `state=promoted`、`deploymentChanged=true`；worktree/index 33-file bundle、2 signers、2 faultDomains、signed consensus、正式根 bootstrap `--verify-only`、独立 v2 bundle/consensus replay 与完整 GitHub Sigstore replay 均通过。
本列车退役 V8/Flash 打击数字 renderer 与旧 `_root` 偏好，C# 唯一 reducer/overlay 提供 `off / balanced / total / classic / detail`、有限/无限世界行、屏外剔除、四模式统一颜色/效果语义与固定 32,768 段精确账本。fresh 门为 Hit Number `97/97`、Settings Host `20/20`、Settings Web `18/18`、Launcher `4213 pass + 3 explicit opt-in skip / 4216 total`；视觉 harness 生成 26 张图与 2 段视频，平均 `0.910 ms/frame`，GDI `3→3`。随上游合入的 TimePool 配置亦以 `configured=3 / pools=3 / refs=9`、AS2 focused `46/46`（8 cases）和 Compiler `0/0` 复验。
维护者已接受与正式 Core 哈希相同的 D8 隔离候选，覆盖高频命中、四种可视模式、颜色语义、逐发长数字排布与暂停态伤害对账。无 candidate selector 的 `automation/start.ps1` 随后确认 `formal_runtime`、精确 identity/closure、bus ready、正常 Guardian shutdown 与零 Flash/WebView2/hotkey guard 残留；该 smoke 没有从正式入口重跑战斗，故准确状态为打击数字 `HUMAN_ACCEPTANCE_PASSED / promoted`、正式入口身份/生命周期窄纵切 `standard_entry_verified`，不称打击数字业务 `standard_entry_verified`。

**上一正式发布（2026-08-25，限时关卡 TimePool）**：implementation commit `920f2c7d8b30cf4ec19204f2490fc3be44b69c3b`、发布输入/EOL 修正 `4c2426f7b4df18ab1ceaa75daf4413b2cc90a6e5`，最终 release source `a4a85dbdcb266f66677eef28875e0862892e48ad`（tag `runtime-build-v2/20260825-stage-time-pools-v3`）、release tree `0b4778aa346a6acce276052debf79a93af7a39a1` 与 request `ADDBA21EF66AA9429D00D349E0ACD33F27BE55641CB3B9F541ACB6BEAC47D043` 完成正式 promotion。
artifact source `A0B4A5F65F4763AA322C2ACDD211DA3DC74C1F47660370266FFC5B650DE81FF4`、producer recipe `B5E7BC39D1F4B999D29F94F88725F6567319741FABF9740F320653E0A5407B90`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `F30603FF717C9C4B4426161451C119DCC3A0982DBF7C1A270DDEED26F371C7C8` 形成 build identity `50ED16457B8C82787A495F957259A9544AD96C819E8D1EF11087D5AF06E0BFB0`；payload closure 为 `60981913A1D18682C06B6ABF2CC6DB7EC0F57345BAF8B1D372E67D7EC3ADE5EB`。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`，证书 thumbprint `647AFE92BD801518AF25F2A2EE1845E6847C2118`）与 GitHub OIDC builder `B4A625D76B6E132856557C169BA3C9FA63C2B3147ABCF8AB0B905FF354C8DF40` / `github-hosted-windows`（run `32837546069`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `1AA2774BFD49A579EDE75E1593CD83DF3832A9A8693E9D2C629ACDB1DC32900A`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `DFB8887FA2CEE2BD07EA0D98930D623FCBBC313015F25E6BB6B858724D59A51D`，consensus SHA-256 为 `6DA7E3E6601268CFDA06AEC92DBA8971CCB15944B5498358463A5849C062533C`；39/39 final receipt SHA-256 为 `F69E8A0569838920B86A42F3B704EABFEEF5AE4E6193A6389FB97055DDDAD9B4`。
deployment commit `9f68a3ee5fbd6db9447118da12fa0fa0a00d1829` 的首次 post-promotion audit run `32838658629` 输出 `state=promoted`、`deploymentChanged=true`；worktree/index 33-file bundle、2 signers、2 faultDomains、signed consensus、正式根 bootstrap `--verify-only`、独立 v2 bundle/consensus replay 与完整 GitHub Sigstore replay 均通过。
本列车新增会话级独立 TimePool：池可跨不连续 `SubStage` 累计，一个子图也可引用多个池；AS2 独占计时与失败裁决，Launcher 的 `T` 快车道只展示 keyed HUD。首批配置为 JK 救援 300 秒、核电站四图 600 秒、断壁残垣前两图 600 秒；XML validator 为 `configured=3 / pools=3 / refs=7`，AS2 focused `46/46`（8 cases）、Compiler `0/0`、Launcher focused `16/16`、Launcher 全量 `4115 pass + 3 explicit opt-in skip / 4118 total`、runtime v2 `181/181`、production policy `39/39`。合并源发布的 `scripts/asLoader.swf` 为 1,147,468 bytes / SHA-256 `3BCC14D3A74FB71E366605A50520ACFDB88BDB5F5DD6CB4573055DD40E93545C`。
维护者已确认限时关卡行为有效。无 candidate selector 的 formal runtime attempt `eb0d6743820512eccccfc6138cdf5605` 证明 fresh reveal、两次同 lifecycle `session.status=verified`、精确 identity/closure、strict terminal receipt、Flash/Guardian exit 0、玩家调制档哈希不变及零进程/会合点/凭据残留；report SHA-256 为 `F22C76803562C24C06C10764B7798D8914E9B65007B3DEC882D63876C7BB8BA4`。该 attempt 明确 `businessJourneyExecuted=false`，故准确状态为限时关卡 `HUMAN_ACCEPTANCE_PASSED / promoted`、正式入口身份/生命周期窄纵切 `standard_entry_verified`，不称限时关卡业务 `standard_entry_verified`。第一次过早发出 shutdown 的 attempt `2a76b07b8223dc3d249366df2d3c5365` 未得到可信 completion，按失败保留，不被成功 attempt 覆盖。
首个不可变 tag `runtime-build-v2/20260825-stage-time-pools-v1` / request `BBCDF4C8D8965C09FC865B7D9614111FF55C4267B7D233EAFC7BFB73BDF43E13` 在本地 policy 37/39 停止；v2 tag 修复 EOL 后本地 39/39，但冻结文档仍误报 v1 门计数，故两者均未 dispatch 云构建、未 promotion。v3 使用全新 tag/request 完成共识与部署，三个 tag 均保持不可变。

**再上一正式发布（2026-08-25，军阀战术演习 AS2 战斗）**：implementation commit `7e9c053512`、合并 AS2 publish commit `cac7b7a475`，最终 release source `248cca7be212219655319c666304407b0568e658`（tag `runtime-build-v2/20260825-warlord-as2-battle-v2`）、release tree `2bbf0eaa19f10461d5feb9cc6c0e26a1a2f9845a`、request commit `131947527fc604b12312f7e960e3bdedbe601195` 与 request `4F57D8E7BA7499021E4A36823BFB27A42BDAE043CA9483BF46B34F42AB09456D` 完成正式 promotion。
artifact source `B7F6CE6F652961216BD48D515150087B08FB26320229AB028D5D86F07ABFF277`、producer recipe `474755B6936B1E3CAA984722D6CC645BB84DC006ED2ADBF8355C4F6F1847E268`、toolchain `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD` 与 policy `62CC0DC1F5096C990D3D3ECD4E9DD11B195A25102D44DC783636C55B535366A8` 形成 build identity `6482B71F2811065A5537F5CDCB0E338D44C23C2BECDC13987D9924B289EC8C59`；bundle tree `c7e0531a3e9ec20db0075ebcaa395506c7d8f971` 的 payload closure 为 `4B46C47478DAC64E038F574F2636AFDFF9E0E6EA16F807171FF5F31E21237062`，bundle SHA-256 为 `3E4A5C73EC4AD8F3E166D3E4671A9E9E44F1C2C4146D40DF1C5A401B87976D79`。
本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `8273B61EB030A519118FF275A39F73351D5413DCB13A230D1E74ED6F6C318ED6` / `github-hosted-windows`（run `32821144648`）对同一 identity/closure 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `CA5E2FF757FB4636D033E0A97026C6B919C7E11BC2E08C35088A962B9B8A4E85`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`，manifest SHA-256 为 `915CA044CD0D93DF50FAFB9A1D26CD28244B239D96AEC1C91C40B7DC2A4AD902`，consensus SHA-256 为 `EAA56E4FEAC03DC160F86E364D4BE276D01FF64FC5A16E4D10E47B85138E7E68`；38/38 final receipt SHA-256 为 `95AA0D3E0570EAE9712DADF1EB237EE81D8EBB205A199F0BCB4B7FDD02942DF2`。
deployment commit `126c10a3d5bf46b66a973e8b68ad27c00ef9257d` 的首次 post-promotion audit run `32822831143` 输出 `state=promoted`、`deploymentChanged=true`；worktree/index 33-file bundle、2 signers、2 faultDomains、signed consensus、正式根 bootstrap `--verify-only` 与独立 consensus replay 均通过。
本列车把军阀 3D 沙盘、编组移动、生产调度、战宠卡牌目录投影、真实 AS2 战斗 handoff/receipt 与专用战后恢复路由纳入正式 Launcher。Warlord Node `75/75`、Edge/CDP `16/16`、共享 Minigame `57/57`、Launcher `4099 pass + 3 explicit opt-in skip / 4102 total`、Flash CS6 Compiler `0/0` 与 production policy `38/38` 均通过；合并源发布的 `scripts/asLoader.swf` 为 1,141,507 bytes / SHA-256 `690C1C871FFF915BCEFC17158146B79F805D70A69793014095343019F0454539`。
维护者已在隔离候选确认军阀沙盘体验与 AS2 战斗结束自动返回有效。无 candidate selector 的 formal runtime attempt `37181289fc744636844c4af38ee49107` 进一步证明 fresh reveal、两次同 lifecycle `session.status=verified`、精确 identity/closure、strict shutdown、Flash/Guardian exit 0 与零进程残留；它没有重跑军阀业务，故准确状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`，不称军阀专项 `standard_entry_verified`。战宠经济仍为 `observe_only / settlementPolicy=none / writesPlayerState=false`；二次交战、最终回基地及玩家存档/货币/战宠无污染保留独立门。
首个不可变 tag `runtime-build-v2/20260825-warlord-as2-battle-v1` / request `0E699D48A56D5219204CD2B97505ECAEAE94E7ACF811560924C5C86D49117211` 在本地 policy 暴露材料、商店派生物与事务门后停止，未 dispatch 云构建、未 promotion；v1 tag 保持不可变，request 由 v2 原子 supersede。

**更早正式发布（2026-08-25，测试反馈体验修复）**：implementation commit `a50813e70c8515a4f3c1efacf9f43b99989e9e4d`，最终 release source `86de257152c23536ae4590c6e8b42585aeaca290`（tag `runtime-build-v2/20260825-gameplay-feedback-fixes-v1`）、release tree `6f2bede155e864d2ab4cc5bd56250b03fb915701` 与 request `6028DF51C32EB96AFF5205C9FA5557DCF389480E46EF5E4370992F47F37F7D4C` 完成正式 promotion。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC builder `78C145BC579E2ED7BD575ACCF8CF2F2082F6F438F26F553454969D30311AA7D7` / `github-hosted-windows`（run `32788021387`）对 build identity `BA91356C398D3C7D3368587BB2DC2BAB38881A6046315F3BBC29DD57E1C01E07` / payload closure `02EC857A02CC090A6820D845C3BB00F66ABF74429B2CEA618202DD5654AB124B` 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `1FD26CEF076D0F4A4F7FDE84E7877F9476A6AB09679C3F6D24A336E3CAEAD081`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`（未变），根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`（未变），manifest SHA-256 为 `DAA61CAA4F76B5354ED06D5027BA9362F0915321744B7F9B52F6A5636974CC6C`，consensus SHA-256 为 `E55FF6A521B96EE5C641EA8A869F3460F8A3E1EFB383D2BFA912A428BDC6A4E1`；production policy `FA856F408F5F8EF15A15875DDBB1E5A05C70E459A4C7714ED8DBB9816C5AEE33` 的 38/38 receipt SHA-256 为 `DD68EA5EA733135CD5881C63EB0545388D165F1DE8122BB7A9C037D0E734027E`。
deployment commit `5dd6f8dfd79a81a77db9802b2125b6c318d62bb4` 的首次 post-promotion audit run `32789080009` 输出 `state=promoted`、`deploymentChanged=true`；worktree/index 33-file bundle、2 signers、2 faultDomains、signed consensus、正式根 bootstrap `--verify-only` 与独立 consensus replay 均通过。
本列车修复 LoadoutPicker 的 scope/anchor/drop-target 契约：背包拖拽写后不再自动切到 compatible，compatible 锚定槽位 A 时仍可向 Host/AS2 白名单允许的 B 槽精确交付。装备进阶射击间隔改投影玩家实际 `fireRate`；`DamageResult.NULL` 几何命中不再重置韧性恢复；选关任务标记在 hover 后保持，难度可直接进入，Web 发起且成功通关的任务在奖励关闭后回流 Web，失败、死亡与撤退不回流。发布前先 fast-forward 合并上游 Team 二级页 × 位置契约，再把测试断言统一到变换后的视口坐标。
领域证据为 Character production `370/370×3`、standalone `218/218`、session `36/36`、projection `8/8`，Team `222/222×3`，Equipment model `81/81` 与 browser 三视口各 `137/137`，Stage Select Edge `53/53`，Launcher 全量 `4085 pass + 3 explicit opt-in skip / 4088 total`；Flash fresh 为佣兵 `113/113`、装备/背包 `78/78 + 147/147`、战斗 `159/159`、Character 六套合计 `580/580`，全部 Compiler `0/0`、32K retry `0`，publish-only `scripts/asLoader.swf` 为 1,139,469 bytes / SHA-256 `D501FB6F3696126EEFDF254ABA3213AFABD32271DB6D92DEC0BD36AD14BA7BB3`。production policy 为 38/38。
本轮没有在 promotion 后从无 candidate selector 的正式入口重跑背包连续换装、受击韧性、装备进阶射速显示或选关任务回流等真实业务旅程。因此本列车状态为 `promoted`，不把自动门、Flash focused trace 或部署事实冒称这些专项的 `standard_entry_verified`；Audio H2 继续为独立 `pending`。

**上一正式发布（2026-08-24，NPC 商店整数千分比精确定价）**：implementation commit `0270cf7a1a0bdfbd6520019e4a88d4bbfb62d1be`、发布策略收口 commit `25c8eb8e5599839768c195373c34db353424f157`，最终 release source `0eda5137cc18caad5490c8284f3c6ab32ea23401`（tag `runtime-build-v2/20260824-npcshop-permille-v2`）、release tree `afd1b1002c14fac3935b5cf4024eff0c68e5c232` 与 request `D58407438774770285C32D217BF2D2583FC12C4925DBBA71EFA8B82DC85593BC` 完成正式 promotion。
本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC builder `FB6132DB1DBA240E16283553CE816A025567D9DD4C96FD7B7FA5D085D58278EA` / `github-hosted-windows`（run `32728441320`）对 build identity `4B58C7546464CA4B9B9DDB8E1D263947A4BFB9C4BC8133C95FC69CD307E043C6` / payload closure `45F0DF4B380A07EE62726CD92122B3104A5EDB7BDB41D7E08CCF6087DB56D271` 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `999932936C7E5E93D2D9D3F8E8B9C3CB5DFD998AC9C9978C19EE76EA879C9836`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`（未变），根 bootstrap SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`（未变），manifest SHA-256 为 `C208237FD5B40D5C8E07919A136D4BC89EC1E2851E14EE4B3BB7C3417B555506`，consensus SHA-256 为 `C3C8585BF61624DA84BBFC78CDF022F9872DA9B45943BF526C75B9A379DC9518`；production policy `DB9C198CDFC0A55B527144300454DBB28DD4DD19BAF114F07162404D2B8F7DCC` 的 38/38 receipt SHA-256 为 `D2A2DCDCC7D7E37FA1E9D0F5475F74B8E7F0A9D30E78065C508BEF84FC683426`。
deployment commit `4274894186c1cd2bf6335e15e597efdb512b2560` 的首次 post-promotion audit run `32730078012` 输出 `state=promoted`、`deploymentChanged=true`；staged/worktree 33-file bundle、2 signers、2 faultDomains、signed consensus、正式根 bootstrap `--verify-only` 与独立 consensus replay 均通过。
本列车把 NPC 商店买价 wire 从浮点 `buyMultiplier` 收敛为安全整数 `buyRatePermille`（`0..1000`），AS2/Host 固定按 `floor((basePrice × quantity) × rate / 1000)` 且每个中间值 checked 的同序公式复验，Web 不重算权威价格。口才 Lv2 `17100×940/1000=16074`、Lv10 `2700×700/1000=1890` 等向量明确纠正旧 binary64 少 1；非法、非整数、非有限或中间值越界全部 fail closed。合并后的材料派生表、shop portrait provenance 与 NPCShop `WB112` 预算亦从真源修正，production policy 未放宽。
领域证据为 NPC AS2 `66/66`、佣兵换装 `113/113`、玩家物资事务 `113/113`、Crafting `155/155 + Synthesis 18/18` 且 Compiler `0/0`，Launcher 全量 `4084 pass + 3 explicit opt-in skip / 4087 total`，Panel contracts `68/68`，NPC browser `130/130 + 23/23 + 2/2`，NPC runtime/SecondaryPage `10/10 + 14/14`，material-shop offline `277/277`，workbench ratchet `67/67`、strict audit `0/0` 与 production policy `38/38`。首个 tag `runtime-build-v2/20260824-npcshop-permille-v1` / request `FBF799EF420B03FE1C8C0BF13E9A6FF5AD48D1178ECA0E636C49CBAFB8C0C5D5` 在本地 policy 暴露材料派生表、头像 provenance 与真实 `WB112` 后停止，未 dispatch 云构建、未 promotion；上述最终 request 在同一队列原子 supersede。前三个构建域未变，故 build identity、本地签名票和确定性候选闭包按协议复用；policy hash、release tree、source tag/request 与云证明全部重新绑定。
维护者在 promotion 前确认测试存档内现存商店均有效，但该测试档可能把相关技能设为满级；部署后没有再从无 candidate selector 正式入口执行低/中等级口才报价与真实买卖旅程。因此本列车状态为 `promoted`，不把自动向量、部署事实或此前人工结果冒称商店专项 `standard_entry_verified`；Audio H2 继续为独立 `pending`。

**同日更早正式发布（2026-08-24，佣兵装备托管一期）**：implementation commit `b994d87b0545c9f0a102c0b3c7989e3b20e4cf8a`，最终 release source `d720b0e433fe4de1df2ab2c12ba2a8e21e6c2ce8`（tag `runtime-build-v2/20260824-merc-loadout-v1`）、release tree `61b33e4d6b0d823a94a591a72a85c84266ff7e0f` 与 request `EB68084CAE7A514BCFBEEB7DA85818BEA0D9CB6F70B26DE7980215E63028250F` 完成正式 promotion。
本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `E661D80E4CC6893A042B9237E55B0F20E05F0CCC8CE95F9022BE1C1D6208ED3A` / `github-hosted-windows`（run `32672992628`）对 build identity `8D595FFA45590BB19D7FDD2BDB52CA3CB669BC64C380FD55B1E399F43A92E57E` / payload closure `94665B231247953BC8486B0BB7A72146EA16DF60003317EC2B41F25E5E6CD43B` 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `100B8B387F3133B2F95E5F3128061D6D52C08B1DA723E2BC26331839F231AB12`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`（未变），manifest SHA-256 为 `942CD2F234CFF0E6177BD7D7534DDF657359F9F013BA34DF8A14C53FA206C1E3`，consensus SHA-256 为 `5C173963A8E0A1B2698F13A2FA53D0CE425882C531EF02B001CB181FD83708EA`；production policy `863D0AB3D3A5A222A7612F5B0F1649654104FBD1A5319B0A0A15BF1A496CF937` 的 38/38 receipt SHA-256 为 `8594028416A001FEF45B802175A1FD84997618DA704776CED8083E9CC3345BB1`。
deployment commit `f1e7a187a67747dca3cc96a52e67c49bd92af3ad` 的首次 post-promotion audit run `32673700808` 输出 `state=promoted`、`deploymentChanged=true`；staged/worktree 33-file bundle、2 signers、2 faultDomains、signed consensus 与正式根 bootstrap 均通过。
领域证据为 AS2 focused 97/97、ManagedLongGun 回归 126/126、Launcher 全量 4054 pass + 3 explicit opt-in skip / 4060 total、Team harness 三视口各 207/207、workbench audit 0/0 与 doc governance 通过；asLoader.swf 已发布含新代码。部署后未执行无 candidate selector 的正式入口 smoke，也没有重跑佣兵托管、玩家受击换装、黑市真实经济、设置写入重启、字体观感或 T800 武器命令业务旅程，所以总体状态为 `promoted`，不把任一专项冒称新的 `standard_entry_verified`。本列车首个 request `D8C29184679CD67355AA3F41AFB1DBCF72B81EBE182B038C7C1A4FBEDE12EF30` 冻结在云构建 sourceRef 轮换前的 implementation tree 上，未 dispatch 云构建、未 promotion，由最终 request 原子 supersede（前三个构建域未变，build identity 与本地签名票按协议复用）。Audio H2 继续为独立 `pending`。

**上一正式发布（2026-08-23，战宠生命周期、事务与跨栈面板加固）**：implementation commit `8e785dd7cbeb52996e2ba2c531a536b0e927238e`，最终 release source `416c4441947d40bc27ae3854178e87e2c5cf1ac9`（tag `runtime-build-v2/20260823-pet-lifecycle-transaction-panel-hardening-v2`）、release tree `71d5955735150e811407c742b3ed3d657c779ac2` 与 request `4A445F1F94DB33E017FAE14FCBC499982F80D305B6F9AD4CFD879864AEB6EF6D` 完成正式 promotion。
本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `67F6B646F5DF863E6398FB2F80F45A3E3EFC75CCD2C7C811341EF580F6851A59` / `github-hosted-windows`（run `32644169141`）对 build identity `036502C3E92B0AC3CF5309E747F0CC1B4B4BCD468443B3BFB2F9F8DB183F3925` / payload closure `9432302F90D34F5AF9282FBF8ACF173B191CCFC80B0D1AE1A7C2CD46AEEFD133` 达成双 signer、双 faultDomain 共识。
正式 Core DLL SHA-256 为 `165FB29FE8CB45C43148A0873218273DF2D55839E3D3044122C7E17804EC7E5E`，Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，manifest SHA-256 为 `515CF48B44AC3499DFACC1665C1180FEBD659B328EACE298BEDC7AFF461EC92E`，consensus SHA-256 为 `6A92F173A410EF4BC4969A1EF914E8E73FCB66A51661062911EDC3BFC4B611CD`；production policy `1BEDD3042CBEB199ACD4126DEBAEFDE69EA039473B5DD79018FBA39EE0078325` 的 38/38 receipt SHA-256 为 `936320F55201296D542EEA87414A64BB8B3AD0127A4B8013C7C1A039384BE287`。
deployment commit `df060eac491c7251b261a3f5626960a8ea18911a` 的首次 post-promotion audit run `32644777181` 输出 `state=promoted`、`deploymentChanged=true`；staged/worktree 33-file bundle、2 signers、2 faultDomains、signed consensus 与正式根 bootstrap 均通过。2026-08-23T14:33:21Z 的无 candidate selector 正式入口取得 fresh `bootstrap_reveal_ready`、前后两次 `session.status=verified`、同 lifecycle、同 identity/closure、strict shutdown receipt、Flash/Guardian exit 0 与零进程残留。
维护者在 promotion 前已确认战宠点击休息后卡片切回休息、当前图消失且下一图不再出战；部署后的正式 smoke 只验证身份和生命周期，没有重跑该战宠、玩家物资、设置、字体、黑市或 T800 业务旅程，所以总体状态为 `HUMAN_ACCEPTANCE_PASSED / promoted`，不把任一专项冒称新的 `standard_entry_verified`。首个不可变 v1 tag/request `5915805B73D65EE63D72B59939A10DDD76F2F7F10FC5C6D7733B7B613EB115FD` 因本地材料目录 policy fail-closed，未触发云构建或 promotion；修正发布门后由 v2 tag/request supersede，旧 tag 未移动。

**上一正式发布（2026-08-22，终结者 T800 托管长枪与射击核心）**：release source `d049daed44a537ef586f9e6254fcb824b98c4ee1`、tag `runtime-build-v2/20260822-t800-managed-longgun-v1`、request `1643570AB50AE207B2D09AC55D6D1F374DD72990070B71423372B6F184497C8A` 与 promotion `a22490867ebd03b931c3066141c0954b02be25da` 保留为 r5 历史发布证据。维护者当时已确认托管交互、M134 与 XM214 可见业务；正式入口未重跑三个武器命令，因此其准确状态仍为 `HUMAN_ACCEPTANCE_PASSED / promoted`，不称 T800 专项 `standard_entry_verified`。

**上一正式发布（2026-08-22，玩家物资事务与双向播报）**：玩家资产流动已收束到 `PlayerAssetTransaction` 已提交回执，覆盖获得/失去、商店买卖、制作、任务、奖励、换弹等现役生产入口；Host v1 严格协议、幂等、调度与 NativeHud 卡片支持方向、权威 itemKey、tier 和 loss 聚合。implementation commit `01d75b2e04`，最终 release source `cf2fbd7b07fde5e008675fa1e6bda725bdfb96dd`（tag `runtime-build-v2/20260822-player-asset-transactions-v2`）、release tree `08b17b9ca00a437dc6fe4dbf0116dcbccbaa680d` 与 request `DD030AC6C7D780FBECC827D11828BD4959191AF182E46921CB480E3B08D4B8FD` 完成正式 promotion。本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `55A499A0AF91FFB53BC95E0BC168A288705E675AC3F2469EF1453FFEBF0193BB` / `github-hosted-windows`（唯一 cloud run `32548053397`）对 build identity `6CD9AEB99B3B5A3DF470F1CDDD99DF11CF7F706394F9FD3AF6D5594ABC03BC78` / payload closure `60EA3AACDBFAAE178866317448FCEA0BEC0D3AA73E808AD4952ACF695571F62D` 达成双 signer、双 faultDomain 共识；正式 Core DLL SHA-256 为 `E0A7B31C70DA564ECCA43B8B58DDA2F6443553112DD95B6EE05182FB94F78474`，manifest SHA-256 为 `987051A0147B0C1E9E166091478035E9178F4F9D4C1413A4097FFF30313C09F2`，38/38 production receipt SHA-256 为 `4A5FC71CE10EBBBE4BDD487F34A0EA3A7FC9D1939E815544CE50F15A719E5546`。promotion commit `b7f075e53c31c5d4b67e02dcda1978695321d33d` 已推送，post-promotion audit run `32548688437` 输出 `state=promoted`、`deploymentChanged=true`（base `cf2fbd7b07` → head `b7f075e53c`）；部署后 33-file bundle、signed consensus 与正式根 bootstrap `--verify-only` 均通过。维护者已接受与正式 Core 逐字节相同的隔离候选，确认双向播报、换弹 loss 聚合和同物品买卖全过程有效；部署后未重跑无 candidate id 的正式业务入口，因此本列车称 `HUMAN_ACCEPTANCE_PASSED / promoted`，不称 `standard_entry_verified`，也不外推到真实玩家存档重启或全游戏 E2E。

**上一正式发布（2026-08-22，设置 Web Panel 与高 DPI 镜头预览）**：旧 Flash 设置入口已收敛到 Web-only `settings`，35 项权威键位、性能协议 0/1、声音试听、本机与 Web 偏好聚合、作弊码与救援首屏，以及二级 `settings_camera_preview` 镜头倍率模拟均进入正式 runtime；DPI Unaware Flash GDI source 会按 window/monitor DPI 还原为物理输出尺寸。implementation commit `2a0d5062fd`，最终 release source `d6684d441f134553ec56590ed975388dff06f1bd`（tag `runtime-build-v2/20260822-settings-web-panel-v1`）、release tree `34b3ef11ff9d8ab6a32fed67da3ee30bf7015606` 与 request `9B9982BB290DF8E0132F52DD4CA842100E9003FB6ADAB71908D5797FB3C198D7` 完成正式 promotion。本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `D41F0D5D3BBB3F53D2ACFF0C88A7DC3CBF6BBCF03476AACE6DF5040F55719F44` / `github-hosted-windows`（唯一 cloud run `32512318147`）对 build identity `AD4B23A7489EB011EA4FC54004DC343F4FA5AF1FCA1D3BBD9452760D18EF1D78` / payload closure `24747E9C71B31E1D7A82356D028A88C3D741CA7AF71D10AC2C97A920649A6051` 达成双 signer、双 faultDomain 共识；正式 Core DLL SHA-256 为 `54A0833082AD3394E002F2E1BD512C6027E6D30CFFEC9D80AC1AEAA1C9515ECF`，Agent Runtime Core EXE SHA-256 为 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD`，manifest SHA-256 为 `1DFDE250931AC0BBFBA477D87150093E9CA66FFFCE7D010BBABCA5FC286E3933`，38/38 production receipt SHA-256 为 `2B5E2C328667A7E05649B5D6A32497920DC68EB428680819907E89F39F0C2211`。promotion commit `de24f449fba0f004193392a273d09b875a4c3935` 已推送，post-promotion audit run `32513484781` 输出 `state=promoted`、`deploymentChanged=true`（base `d6684d441f` → head `de24f449fb`）；部署后 worktree/index 33-file bundle、signed consensus 与正式根 bootstrap `--verify-only` 均通过。维护者已接受同 identity/closure 隔离候选；部署后又以无 candidate id 的 formal Agent Runtime 精确打开 `settings_camera_preview`，取得 1600×900 WebOverlay WGC、全宽/全高有效内容与可信 shutdown，故“单屏设置诊断入口、可见 WebOverlay、正式身份绑定、可信退出”达到 `standard_entry_verified`。该结论不外推到 Flash pixels/input、物理 WebView2 输入、真实设置应用与存档重启回读、音频听感、救援业务效果或全游戏 E2E。

**上一正式发布（2026-08-21，字体资产 Gate E）**：C# / Web 运行时字体已收束到根 `fonts/`，以 XML/XSD 统一维护 asset、face、semantic role、preset、常驻/临时来源与下载回退；Web CSS/Canvas/动态 SVG/tooltip、Native HUD/Combo/伤害数字及四个高风险 Web consumer 已切换到语义角色，`temporary/**` 与 Flash 作者字体明确排除于玩家包。implementation commit `435b6ca5d5a9be40f2e7e31f49c348232a8b31c3`，最终 release source `df7d7fd5e199da736ba0a1f8f50fa0aad39fba57`（tag `runtime-build-v2/20260821-font-gate-e-v1`）、release tree `dc873015785b35a1176ff454ce581b860f473392` 与 request `F3B8B8F6B30B871A7CADC307D273C3F60B6CA9BFE3FA99920A6693AB26411A3F` 完成正式 promotion。本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `149E54CA4F8A0B7AC0ACD936B4F022FA23B74E7DB10BD380C6B229BDC35C05F9` / `github-hosted-windows`（唯一 cloud run `32454968377`）对 build identity `31868EBA538A26C3D6200BBEBE450D9E6EC8CFC6AB08AF70AFD708644BFE36E8` / payload closure `13F29BBD5792A8D2D13272761FE150C70C303388A0EA5E443BFE1A956C9D4811` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `D970911DEBECB7EDAC1E9FAA631BF4FF5C0B55CF36DB85AEEC57363E08A4B029`，manifest SHA-256 为 `D8EEC558C93081AE92C482D40351539CF6AF242E291A073342EA5D94643A54A0`，38/38 production receipt SHA-256 为 `BE4FC105F3005A3ED49233698674CA246335AF5A29D86CDD356749C8C2FDEBCB`。promotion commit `973dc8ab46fa8b627fe17c777eacfb4f62c918ee` 已推送，post-promotion audit run `32455849082` 输出 `state=promoted`、`deploymentChanged=true`（base `df7d7fd5e1` → head `973dc8ab46`）；部署后 worktree/index 33-file bundle、signed consensus 与正式根 bootstrap `--verify-only` 均通过。领域证据为 14 asset / 14 face / 28 role / 9 preset、fontctl 25/25、真实 Edge 字体矩阵 16/16、情报面板四档 DPI 各 30/30、四个消费者 1168 项、Launcher 全量 3900 pass + 3 explicit opt-in skip / 3903 total、production policy 38/38 与 doc governance。维护者已在发布前明示人工观感验收；未在 post-promotion formal runtime 重跑业务标准入口，第三方字体也未取得正式法律意见，因此本列车称 `GATE_E_COMPLETE / promoted`，不称 `standard_entry_verified`。

**上一正式发布（2026-08-20，黑市全目录影子鉴定 v1）**：全目录影子渠道已接入固定 `1024×576` 逻辑画布与三舱二选一，物品使用自动正交旋转、Alpha/SDF、PCA+骨架锚点和视线中心 fit；装备复用 `EquipmentInspector`、dressup manifest 与 `MercPortraits` 生成局部纸娃娃，放大检视与卡片共享旋转后对象坐标及固定覆泥母版；污泥语义为休眠军用纳米机器人，切组/生成期间由身份无关封存层阻断原图泄露。implementation commit `4b6ba9647065f776dad87947fe6ceb51ff3ee98c`，最终 release source `12d6ba9eb282106e058c292fc1fdb80a49534de3`（tag `runtime-build-v2/20260820-blackmarket-shadow-v2`）、release tree `a686e935fe390651259c181f05a90d13a87a0264` 与 request `2CE260E595A621B67DFF2F4004FF8C1707FA408BE11774C44820020605A112B6` 完成正式 promotion。本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `50312F5734191C6CD9EF07937A491D85B918C8FDE7721DEB1D121C1187E1EAC8` / `github-hosted-windows`（唯一 cloud run `32377991018`）对 build identity `FD740E89652E7D85AB070A949C7362DEEC44AD326FCBD7AE36BFDC95965BB599` / payload closure `B2EAD052D03C39FB2EF69E47F4E9335EA0EE0E734D88FEB14C31867A6A7E3915` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `A1AB8D8A5590F5F4A8A5CDDACF7FD35B4A1B0C7057808F3ED2A6356ADD25F024`，manifest SHA-256 为 `E7E2CEB4E56D6F9FDCA12EB43326EBC2DE4B46F4AD697E6C3992F0A6C840F4CB`，35/35 production receipt SHA-256 为 `83D7623C52EAFE33B7CBA924CA60D5028FD6216D641418D7301B9023AEA43308`。promotion commit `244b132efa3642f1d807964c9792a6e396399d9b` 已推送，post-promotion audit run `32379704093` 输出 `state=promoted`、`deploymentChanged=true`（base `12d6ba9eb2` → head `244b132efa`）；部署后 worktree/index 33-file bundle、signed consensus 与正式根 bootstrap `--verify-only` 均通过。领域证据为 Launcher 全量 3897 pass + 3 explicit opt-in skip / 3900 total、小游戏 51/51、黑市装备/检视 30/30、防具 492/492、lazy closure 28/28、strict UI 0 error / 0 warning、panel contract 66/66 与 production policy 35/35。首个 request `DF729291EAE91173352FD0CFA69EB67D75A5BF40BAB720D4B04653AEE90A546A` 在 policy 暴露材料字典与商店头像派生闭包陈旧后停止，未 dispatch 云构建、未 promotion，并由最终 request 原子 supersede。维护者已在发布前目视确认固定画布、自动聚焦与放大检视有效；黑市仍是 `SHADOW_MINIGAME_V1 / TUNING_REQUIRED / PRODUCTION_NOT_IMPLEMENTED`，真实余额、存档、掉落、AS2 权威结算及 post-promotion 正式业务旅程均未完成，故通用 runtime 只称 `promoted`，黑市专项不称 `e2e_verified` 或 `standard_entry_verified`。

**上一正式发布（2026-08-20，常驻 UI NativeHud 收敛 + 发布流水线提速）**：`useNativeHud=false` 旧分支整体拆除——Web 常驻 HUD 模块（notch/combo/currency/map-hud/sparkline）与 GDI+ 伴随层（ToastOverlay/NotchOverlay）退役，常驻 UI 全面收敛到 C# NativeHud；面板 toast 改经 `Bridge.send panel-toast` → `IToastSink.AddMessage` → `ToastWidget` 原生显示（白名单由原生 FlashHtmlParser 承载，面板打开期间 toast 冻结、关闭后恢复显示）。同日前置列车（release source `b57ef4de8529504bca45790b0400539ed7921c2d`、tag `runtime-build-v2/20260820-pipeline-speedup-v1`、request `AD1D9E40866C3A6D39D479E96036D3DEF132630E1CCC9EE863E814DC1CC12C39`、identity `C66AEFF4EC840BC65208DED523789DD21BB7F63A320E283790C10C479E702FDB`）完成发布流水线提速（批量 Git 身份计算、下载路由统一+粘性记忆+停滞检测、promotion 重复校验收敛、attestation 小包默认下载、阶段计时落盘、worker 孤儿扫描），其 payload closure 与 loot feed v3 逐字节一致（`3F87530B…` 未变，promotion 仅轮换身份/manifest/consensus），promotion commit `d0f6935b2cd55fd3b15436f3b57587352c27e5ee` 与 audit run `32325330661` 通过。当前部署：release source `0749ab6665b944515561236a85b867d691d536f4`（tag `runtime-build-v2/20260820-nativehud-convergence-v1`）、release tree `8386ad98d3cd6673a3c6350b8f9ce6652c5ba1de` 与 request `7F179F8AB1893B9DE3ABE108A6A71A092A6C569CBD1E1E015450DC863DCFB387` 完成正式 promotion。本地 X509 `builder-local-b` / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `73373648B1A25113780A0BBEC816628CD87A6471F4EB508D1D4FF97EC15FCF90` / `github-hosted-windows`（唯一 cloud run `32326211406`）对 build identity `FD744C7D2D96AFF1793CA7A8FA494A1E567EDF26FA19651B49F41353BD2A0973` / payload closure `64A56DC4F4FA2F980385FCF0759625C061C42FF164865B11C3453845AB7AC4E7` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `ECCAB79509BC58F2154DC0D051E41ACA1A14643B61C67D3C9D3D349587F68A2B`，manifest SHA-256 为 `710189B5859DC6DEA6F194DA74D3F74E60A799BB2B80953116C64262BCA01B56`，35/35 production receipt SHA-256 为 `AA0FDDFB21B0979E2E88A77BB36BD568F9CA1161EED78B7D78E439B291ACECF5`。promotion commit `cc9c4c9d103b32a909c7440aac1a896498e237a3` 已推送，post-promotion audit run `32326874123` 输出 `state=promoted`、`deploymentChanged=true`（base `0749ab6665` → head `cc9c4c9d10`）；部署后 33-file bundle、signed consensus 与正式根 bootstrap `--verify-only` 均通过。领域证据为 Launcher 全量 3896 pass + 3 explicit opt-in skip / 3899 total（较基线 −11 即整删的两个 legacy 测试类）、发布协议回归 11/11 套件、批量身份 parity 四域×双模式逐字节一致、production policy 35/35、doc governance 通过；维护者在同 identity/closure 隔离候选上完成约 76 分钟实机游玩（HUD/面板/toast/存档/干净退出）。未在 promotion 后重跑正式业务旅程，故本列车只称 `promoted`，不称 `standard_entry_verified`。

**上一正式发布（2026-08-19，loot feed v3 优先级、排版、头像与动效收口）**：五槽确定性分槽已收敛为共享优先级池与可恢复等待队列；任务/通关、开箱与 Boss 保真，精英/拾取可抢占杂兵，普通击杀仅对并发身份溢出做守恒压缩；`UnitUtil.getEliteLevel` 驱动普通/精英/Boss 视觉层级。卡宽改为 96–220px、8px 微量化且 `count=1` 不预留计数列，入退场与数字更新使用 SmoothStep，合并反馈限制在数字区；纸娃娃烘焙固定 `pixelRatio:1`、`animate:false` 与 exact 256×256 PNG。release source `1aad619d6c1b3af22e15f50e5329619f7e1b82bd`（tag `runtime-build-v2/20260818-loot-feed-v3`）、release tree `6032bf75d1fb8ddff9491f00901914629be7356f` 与 request `B717B850E2F1D4B0500706E424C5BABA36B020F9D0A6E3D4C956FE15BFB6799F` 完成正式 promotion。本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC builder `1EF17F5BEA36275B859D61A381A84479DC02EC323535485DCACAF34D9FB1E1E7` / `github-hosted-windows`（唯一 cloud run `32195201787`）对 build identity `50FC8B76D7A81DD1F5366E6FEBAC6EEA79117710A26D9637EB4E3CE01EAAFBFD` / payload closure `3F87530B7A80C53C0F0EF4CED485DE1C9FF2491980FB057829EF9E7BAE104944` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `13604AA52FFC31550FE18E41952F7804B26F1EDE2130C621F928950D13BC24AE`，manifest SHA-256 为 `91F73FC2E9F57428736CA9C8620EC3FBC48555E002B66D6BC0B06A7E7FCF4C13`，35/35 production receipt SHA-256 为 `156606D231096C700CA0666A429C48E2C71F121D06715E313835250783902D11`。promotion commit `22da98aa04ec7b375ad7879ec2155075e0834ea9` 已推送，post-promotion audit run `32196891795` 明确输出 `state=promoted`、`deploymentChanged=true`（base `1aad619d6c` → head `22da98aa04`）；部署后 worktree/index 33-file bundle、signed consensus 与正式根 bootstrap `--verify-only` 均通过。领域证据为 Launcher 全量 3907 pass + 3 explicit opt-in skip / 3910 total、portrait runtime 9/9、web doll-bake 8/8、fresh AS2 publish-only compiler 0/0、production policy 35/35 与 doc governance 通过。维护者已接受同 identity/closure 的隔离候选；未在 promotion 后重跑正式业务旅程，故本列车只称 `promoted`，不称 `standard_entry_verified`。

**上一正式发布（2026-08-18，loot feed 初版）**：左下 NativeHud 图标卡片流（五途径物品获得：拾取/通关/任务交付/战利品箱/情报折算；任务奖励 AS2 弹窗退役；enemyKilled 事件链击杀播报与 killStats 同收口；运行时纸娃娃头像异步烘焙——击杀携外观元组、C# FNV key、WebView2 生产 dressup 渲染器落盘、PortraitReady 占位卡原地升级；合并脉冲与可视签名重绘门控）随 implementation commit `3053a40567`、release source `67119fb8103e9ad96aea430af3187fc78e4cf937`（tag `runtime-build-v2/20260818-loot-feed-v2`）、release tree `5e98716245f5bcf4f12b17e3c9c501b0226fc944` 与 request `1E0C27E96F71B4F78F225AE837945C1FB4D21B7A65B7C6B6FAB4C395AB08E0C3` 完成正式 promotion。本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC builder `F5411F9AD8EFC7CB948A8C8C0D16D6D3C8A121CB82F37D59FA11887C0B866673` / `github-hosted-windows`（唯一 cloud run `32127122076`）对 build identity `89E7E00CC6183C30A28CBF4A662F8650AA8908AFCC2E8202FD203E3F16A1E384` / payload closure `EE7835968A403B3EFC87E1CECDC52DA6965A59B08C2D09903C7488EBC6711E3D` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `EDB79B8A59D4B35C4AB243AF81B17B5C9B6E8CDDAAA7BCE157CA86CC37203161`，33/33 production receipt SHA-256 为 `698E305D438DFCF01BBFE3ECC689B39A0DA33055DE000D45715AE5CF74A5F738`。promotion 提交 `f16af5dc2d44bc2391394c644375be772d19c30f` 已推送，post-promotion audit run `32130065217` 明确输出 `state=promoted`、`deploymentChanged=true`（base `67119fb810` → head `f16af5dc2d`）；部署后 33-file bundle、signed consensus 与正式根 bootstrap `--verify-only` 均通过。本列车前两个 request `2D74BBE0F082CA200F65E2EAD824EA586D494627BB550CC241019D3682ECFB9C`（config 仍指旧 tag）与 `9DF96067D5F041B321A7557825DDD9A252CE2E63DB6095483F1A7701073D1D68`（tag 未自指 sourceCommit）均未 dispatch 云构建、未 promotion，按协议原子 supersede；`runtime-build-v2/20260818-loot-feed-v1` 为未使用历史冻结 tag。领域证据为 Launcher 全量 3858 pass + 3 explicit opt-in skip / 3861 total、Loot/Doll 域 239 项、web doll-bake harness 8/8 与 merc-portrait runtime 8/8、fresh AS2 publish-only compiler 0/0（asLoader.swf 多轮 fresh 刷新）、doc governance 通过。维护者在发布前完成多轮隔离候选人工旅程（拾取/通关/任务/箱子/击杀/动效/全屏与角斗场纸娃娃）；未在 promotion 后重跑正式业务旅程，故本列车只称 `promoted`，不称 `standard_entry_verified`。

**上一正式发布（2026-08-17，合成工作台 P1–P4）**：完整/紧凑密度、跨容器持有量、复数计划标记、任务与金币/K 点采购高亮和精确往返、NPC 头像、装备来源指引及嵌套合成定位，随 implementation commit `eab7803ca5`、release source `fbd096f1a481580f60c9904eb162fb31416752a7`（tag `runtime-build-v2/20260817-crafting-procurement-workbench-v2`）、release tree `53a609832c293ab1539e5123d6216fa971d342a2` 与 request `E74B2E153AF64027EAAAF9D08DAFDE5379105BD3391BF733814B701F0CE0C290` 完成正式 promotion。本地 X509 `builder-local-a` / `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC builder `71A7A1AA17450528E5C65D6EBC945D18A07D01D30CF9CAA86F50A539565087D6` / `github-hosted-windows`（唯一 cloud run `32036301461`）对 build identity `41D05C08C6B37A3197545B1414A0098E9E60F262B61965C6CFDAE32E4F9B3A58` / payload closure `24B18E53A922293F3EDBA29997A7F722EA5E7E0417EF395FE9591C728D488D1F` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `907771B22A6A318741BBF690D338BF7AF9EBABB3BE91C5A0334C58439F21AD02`，manifest SHA-256 为 `0F22456E7251EF5A7842EC331C9D0F84B25E37AF1643D96113296C1F9E603038`，35/35 production receipt SHA-256 为 `ECBF4CD9F419F53135FAE7BD774E3ECA801E4CCCC48EC9DB9B0AA2B867CB9F5E`。promotion commit `1242711521aef2ed1024a9527d8d9a83e9d63c4f` 已推送，post-promotion audit run `32038829319` 明确输出 `state=promoted`、`deploymentChanged=true`；部署后 worktree/index v2 bundle 与 consensus、正式根 bootstrap `--verify-only` 均通过。本列车首个 request `EA5DE50C41E55EA2D633A7B63BC9D2E92FEE30E55DD0C5C3B346A3AAC7A3F012` 在本地 production policy 暴露材料字典生成绑定与商店头像 provenance 陈旧后停止，未 dispatch 云构建、未 promotion；生成物按既有工具刷新后由当前 request 原子 supersede，前三个构建域未变，故 build identity 与本地签名票按协议复用，新的 policy hash、release tree 与最终云候选 receipt 均重新绑定。领域证据为 282/282 配方身份、Panel contract 4 domains / 23 commands 与 66/66 变异、Crafting runtime 36/36、Panel runtime 41/41、Crafting 三视口各 `150+15+8+10+6+9+26+12+9+17`、NPCShop `130/130 + 23/23 + 2/2`、KShop 151/151、Launcher 3770 pass + 3 explicit opt-in skip / 3773 total、fresh AS2 Crafting 151/151 + Synthesis 18/18、publish-only compiler 0/0 与最终 `scripts/asLoader.swf` SHA-256 `955A50115F5ACF2AF401B79FDC976ED788AEDC12D10150F1452386A95C7B3573`。维护者在发布前已通过 P4 短人工旅程；因未在 post-promotion formal runtime 重跑该业务旅程，本列车只称 `promoted`，不称 `standard_entry_verified`。下行旧正式发布段落均按历史列车输入阅读，不覆盖本段。

**同日上一正式发布（2026-08-17）**：角色构筑手雷/药剂装入、卸下与获取堆叠语义统一，无目标背包总览，已穿戴装备强化度交换，仅持有配件默认筛选，以及背包候选调制期间切换已穿戴槽位修复，随 release source `60d672332d31bf7b33f75a9b42a4ac6454fb9ca8`（tag `runtime-build-v2/20260817-character-build-stacking-tuning-v2`）、release tree `52dfe8d392d105594570d20e2e77964f44c474e5` 与 request `5DF08056E7D8AC114D9F8D6BD7A2381CCCFECA4967BE21FA23C2BF404699A466` 完成正式 promotion。本地 X509 `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `A9981D44813388880C1CB65850ECCE0D515CC8C8A6572C0F6821A438B39096EA` / `github-hosted-windows`（唯一 cloud run `32022036259`）对 build identity `D7D97223C2A145237CAC9A3D9873E0CEFDF979D60FF2E45821C1CF37F2285636` / payload closure `7CA66AEE59AB4054B2F85B51E621D2AE2BCB4738CBEA92EEBF5FABD5B270E289` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `CF715C643FB89FCEB301789B3C8C006C5BA570C3FC09B04490AC4F6F25159303`，manifest SHA-256 为 `D4C6C28011C568480AF6AFF79B6293BCFF7CC386E8EC025705D64E106E4571F4`，35/35 production receipt SHA-256 为 `5A5ED39AA20CDF98B3F961444DD4AECFD0760DD430DE567CF4314C2F9F9AF559`。promotion 提交 `93449940ff863d7ec6fc2ab80a0540f178f7fa06` 已推送，post-promotion audit run `32025152299` 明确输出 `state=promoted`、`deploymentChanged=true`；正式根 bootstrap `--verify-only` 为 exit 0。本列车首个 request `E9EA5AA0F4768FA53931D381031C6D280AB1E785242AC1137C7772298FE1967F` 在本地 production policy 暴露 `material_dictionary.generated.json` 未刷新后停止，未 dispatch 云构建、未 promotion；刷新 `EquipmentTuningService.as` 消费者 SHA 绑定后由当前 request 原子 supersede，前三个构建域未变，故 build identity 与本地签名票按协议复用，新的 policy hash、release tree、最终云候选 receipt 均重新绑定。领域证据为 Launcher 全量 3760 pass + 3 explicit opt-in skip、Character Build production controller 1095/1095 + 12/12 + 18/18、tuning browser 三视口各 137/137、strict UI audit 0 error / 0 warning，以及两轮 fresh Flash focused run 与 publish-only `scripts/asLoader.swf`（Compiler 0/0；1,087,271 bytes；SHA-256 `D6E9EB9D41A19B336FD63F052377ABF8233C95A3593462805BA95D770542C0A2`）。这些机器证据与正式部署只支持 `promoted`；没有执行真实玩家存档写后重启旅程，故不称 `e2e_verified` 或 `standard_entry_verified`。下行旧正式发布段落均按历史列车输入阅读，不覆盖本段。

**上一正式发布（2026-08-16）**：选关界面双栏工作台基础体系对齐与品质打磨 release source `f0c501fcdf74220a5cb6388f01692db7c96389d2`（tag `runtime-build-v2/20260816-stage-select-workbench-v1`）、release tree `34c94d9136d897f83e942106151b6d4c5862ae3a` 与 request `DB58FE228CCEA955FC81BE2625676262BEBDE8AFD74108FE4939436AD3FAFA39` 已完成正式 promotion。内容：P0 计数真值闭环（`stage-select-golden.js` 单一真值 + `stageNames` 补登 + 文档漂移勘误）、P1 PanelScale/生命周期/token 化与 CSS/fixtures 拆分（零视觉变化 16/16 帧字节全等实证）、P2 键盘全链路与 pinned 决策检查器（双模裁决）、P3 instance/session/迟到回包守卫（AS2 逐字节零改动）、P4 控制器四层拆分（DOM-free ViewModel 为 Stage 3 三维沙盘预留 renderer 插座）与 hover 预览派生管线（默认预览 74→11）、AS2 `lockReason` 逐关锁定原因扩展与两轮实机打磨（选中态与残留卡/焦点环冲突结构性消除）。本地 X509 `physical-host-a`（keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`）与 GitHub OIDC builder `E962CA17D0A495150F0815F253662079691F4683A75CCFDB921F25DDEBEDFE84` / `github-hosted-windows`（唯一 cloud run `31945976799`）对 identity `915B3E22FCA273BA95FE850D5F13D9BB1CF708B920A4FE5BAFB02E2A45816B66` / closure `CF593FF86173506C791DAEBE2C8B9A1FA46CC68CB7E1E1C5AFDB8893B1FAD05C` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `A4ADD6A4A2EC6138F87D25945EE3727C2D9036C0CFB25831C61E251FAC053CA3`，promotion 提交 `a1798e4edf94be7168f01cc4f1490f2c1fb6fed5` 与 post-promotion audit run `31947003064` 均已推送/通过。本列车无 Audio emergency/bypass；Audio H2 仍为专项 `pending`。首个 request `3CC067A937D588A4576D554B06E1C7429F5177E90A8B0A5EF48CAF8CCF1DCCAB` 先于 source tag 冻结创建、config 仍指旧 tag 而未 dispatch 云构建，已由上述 request 原子 supersede（build identity 不变，本地签名票按 identity 复用）。领域工程证据为 mock-browser harness 53/53、focused xUnit 142/142 与 Flash CS6 publish-only smoke（compiler 0/0 + SWF 刷新 + FFDec 标志串核实）；正式入口 `runtimeMode=formal_runtime` 身份一致，且测试员在正式运行时内完成选关面板实机 smoke（选中/检查器/锁定原因/键盘导航/Esc 分层）并签收，达到 `standard_entry_verified`。下行旧正式发布段落均按历史列车输入阅读，不覆盖本段。

**上一正式发布（2026-08-16，历史明细）**：NPC 商店 snapshot 数量污染防护与跨层诊断加固 release source `9df1fb8fdadf1df10c6bcc9b0552ebcfcf16a834`（tag `runtime-build-v2/20260816-npcshop-snapshot-quarantine-v2`）、release tree `72656dde8da92ee5863f3fc760db8e9f20e07ead` 与 request `13EB5AC5F2413D7C1547D5E0486C5ACF8183C0145DE02785F8B1932FE5FE2FEC` 已完成正式 promotion。`DictCollection` 现在把正数但不符合安全整数契约的旧档项隔离出材料/情报业务投影，同时由 `toObject()` 原样回写，后续同名合法获得可显式修复；AS2 只报告隔离计数，Host/Web snapshot 诊断只携 callId、panelInstanceId、generation、校验阶段/字段路径与枚举原因，不记录物品名或原始数量。首个冻结 tag `runtime-build-v2/20260816-npcshop-snapshot-quarantine-v1` / request `E674F0E13CC8DC19C65F7F6B3A50307CD753124C82E0BAA1BB87D9E40B6717B4` 在本地 production policy 暴露真实 `WB112`（`npcshop.js` 1047/1040）后即失败，未 dispatch 云构建、未 promotion；诊断 facade 下沉至 `npcshop-runtime.js` 后由上述 v2 request 原子 supersede。本地 X509 `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `DB44DFAF3706D707AB5CFDE0309D0396D855F6F00A8AB5DF1C009E04C4E75AB3` / `github-hosted-windows`（唯一 cloud run `31928663322`）对 identity `C23D09677F191D6CCF5666E64E64F8CE664738E6F85AB46FDE916CED4F667708` / closure `FF6CD703624ECA341C8B52AAB726DA7DEDF3914FBD2306F0712A3BE5D3183C6E` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `6543352D859EFCB4361EB5C90AC8D61AA3A9955D770D958C4596745073287165`，promotion 提交 `72f988d4e6aca7936166f13a6e751707a5492c63` 与 post-promotion audit run `31930140127`（`state=promoted`）均已推送/通过。正式根入口 `--verify-only` 为 `exit 0`；污染夹具与自动门不能替代已丢失的原存档，且测试员机器与开发机物理隔离，因此本列车不声称原事故唯一归因、真实存档 E2E 或 `standard_entry_verified`。下行旧正式发布段落均按历史列车输入阅读，不覆盖本段。

**更早正式发布（2026-08-16，历史明细）**：Web UI 语义音效语言（13 事件语义词典、面板 profile standard/quiet/media/custom 分层、统一抑制与防双响规则）、audioPrefs Host→Overlay 静音偏好同步（修复启动页关闭 UI 音效后游戏内面板照响的 P0）与 17 个面板铺设/语义重标定 release source `b8565b3681e59dd977d71fb80b1493ffae8fac92`（tag `runtime-build-v2/20260816-web-ui-audio-semantics-v1`）、release tree `bf5bce5ea21863a72820eca20d02d46a0a6d5d38` 与 request `DFD21122D4AD20B351C9B47FC199A9B11AFE5F61E96EF98C8C557C619641C0F9` 已完成正式 promotion。本地 X509 `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `616496639BE84CEF2CDFABD75D0F7E2D2CC0E68012AFEF776103196208C445DD` / `github-hosted-windows`（run `31921168151`）对 identity `92D1CA0EC139CC9FD8D1144994792A47837FCC5FBCE1047D0DFCD9C92AA2124D` / closure `069F1A8A974CFE6F09898E89DD537943CCA5A3E522B719D30AA2DA5087D56F14` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `AB42F116426F975403BF16F96CED647A40F91B04ECC94FFA397DF891C2C98670`，promotion 提交 `88ac59b24d9c3014915cb100131d52c18950067c` 与 post-promotion audit run `31922327004`（`state=promoted`）均已推送/通过。音效语义权威契约见 [Web-UI音效-语义语言与接入契约-2026-08-15](Web-UI音效-语义语言与接入契约-2026-08-15.md)；harness 只证明路由与语义归属，实际听感未经人工验收。本列车无 Audio emergency/bypass；Audio H2 仍为专项 `pending`。正式根入口 `--verify-only` 为 `exit 0`，但没有执行面板业务专项正式旅程，故只称 `promoted`，不冒称 `standard_entry_verified`。下行旧正式发布段落均按历史列车输入阅读，不覆盖本段。

**更早正式发布（2026-08-15，历史明细）**：面板 tooltip 交互收尾（指针 Esc 退出检视、检视状态条折叠、suffix 底条、pinned 检视器标题/排版/1080p 溢出修复）与最小化恢复后 WebView2 合成链 wedge 黑屏修复（`KickWebViewCompositor` 1px bounds 抖动 + WM_SIZE minimize→restore 视口刷新）release source `29ab59fd7832d10d54badc3132a5153646c1fa3d`（tag `runtime-build-v2/20260815-overlay-compositor-restore-v2`）、release tree `978eaa7e75708b82fd8d57b9f8c63b11f5e689cb` 与 request `2EE9A1E09A07A93EE2B7D49D74BCCDFE71A6953E290AB7C8D6A58BC460DAC4A0` 已完成正式 promotion。本地 X509 `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `8C0233C1268C0AAE6ADBAB159B65CB14402683E6F6C05C37F53DD7AAFF57E44C` / `github-hosted-windows`（run `31892519833`）对 identity `74D14EE3A17CD12AE5F74CB6779CF702339E4D22E3F6C6523F1E9C72EFA04CA3` / closure `2F6B950B64441E1378A687549638464D420C255251D517EA935D866021D6D1CC` 达成双 signer、双 faultDomain 共识；正式 Core SHA-256 为 `73579C43BF6DFDF97B9EC5158A6492BFF3C1AC67357AA5A7D06B3B77BE6F4697`，promotion 提交 `a469f487f065abea7558a89a13f0345ab468d4a6` 与 post-promotion audit run `31897530643` 均已推送/通过。本列车无 Audio emergency/bypass；Audio H2 仍为专项 `pending`。正式根入口 `--verify-only` 为 `exit 0`，但没有执行面板业务专项正式旅程，故只称 `promoted`，不冒称 `standard_entry_verified`。下行旧正式发布段落均按历史列车输入阅读，不覆盖本段。
**更早正式发布（2026-08-11，历史明细）**：跨层回包键契约修复（商店购买结算、Loot 快照 balanceSummary 剥离、装备调制 loadout 失败包、角色构筑 reconcile 快照与失败码）release source `a87be26f1d6e80c0ea8883bbc10e46c895c35799`（tag `runtime-build-v2/20260811-cross-layer-wire-contract-v1`）、release tree `32e5b9be67de220eb297e6c568c90546b283c471` 与 request `44A8D5461BE38E0807A7BE1DFEDAD4F5065F7981C590460CAEBAB4153E577A5E` 已完成正式 promotion。无 candidate id 的正式入口复验 `runtimeMode=formal_runtime`、Guardian 总线就绪、Core SHA-256 / build identity / payload closure 与被提升身份一致；AS2 侧修复由同一 tree 的 `scripts/asLoader.swf` 承载，经 fresh trace 门（item-panels NPC 48/48、map-loot 151 断言、equipment-tuning 60/60 + 147/147）与全量 xUnit 3308+3/3311 验证。这些证据不代表物理双屏、物理设备输入、任意业务写或玩家目视签收。下行 P4/P5 与 A1–A6 段只保留为历史记录。
**上一 P4/P5 正式发布（历史明细）**：竞技场 P4/P5 与商城旧档兼容 release source `bf9be8c43b223b84f487464a7e6aa9eb8211630b`（tag `runtime-build-v2/20260808-p4-p5-source-complete-v1`）、release tree `44de7f66421a908723f196718e42867dce601f30` 与 request `424D9CC1975CF099A705C828E354215EF3F568B7198B6B19C13DB019CB25163C` 已完成正式 promotion。无 candidate id 的正式入口通过项目自有 Agent Runtime MCP 完成 Launcher 与 allow-listed Help WebOverlay WGC、strict terminal receipt、supported shutdown、存档不变及无新增残留进程的窄纵切，严格达到 `standard_entry_verified`。商城旧档与竞技场业务正确性由同一 identity/closure 的独立真实 E2E 持有；这些证据不代表物理双屏、物理设备输入、任意业务写或玩家目视签收。
**上一 A1–A6 正式发布（历史明细）**：双栏工作台 A1–A6 release source `730c6be781ddd22bfd7a59a2e7773acce892f105`（tag `runtime-build-v2/20260806-workbench-authority-a1-a6-v2`）、release tree `1434e71d44fe41ad0fa426bc857085f1aff940dd` 与 request `D45D0DEF50E159B8A875DCA922D856BEC69D64A6B779B6B7B7082D68AC6B92CF` 已完成正式 promotion。无 candidate id 的正式入口通过项目自有 Agent Runtime MCP 完成 Launcher 与 allow-listed Help WebOverlay WGC、strict terminal receipt、supported shutdown、存档不变及无新增残留进程的窄纵切，严格达到 `standard_entry_verified`。该状态不代表 Character/Equipment 业务 opener、A1–A6 写操作、物理拖拽、玩家目视签收或所有专项实机旅程；详细闭环见 [双栏工作台治理 ADR](双栏工作台-权威数据与交互能力一致性治理-ADR-2026-08-01.md) §18，F8 记录继续作为历史发布证据。

## 历史字体 Gate E 迁移状态

字体 Gate E 列车当时的 consensus 精确绑定 request `F3B8B8F6B30B871A7CADC307D273C3F60B6CA9BFE3FA99920A6693AB26411A3F` 与 release tree `dc873015785b35a1176ff454ce581b860f473392`。artifact source 为 `A9E87F4661F88021408CB3FC5D9259824BEF05FA112AC7FC1EFEC246D596CB27`、producer recipe `DEB37674DA3E8AB6888DF759316780B6F2C038C4E4899E4E13E6225821DE057B`、toolchain lock `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD`、policy `34FF4288013234CAFE8C7F459E546DCF5E8311E2D69F5884515F3D9ED099DC3D`；build identity `31868EBA538A26C3D6200BBEBE450D9E6EC8CFC6AB08AF70AFD708644BFE36E8`、payload closure `13F29BBD5792A8D2D13272761FE150C70C303388A0EA5E443BFE1A956C9D4811`、manifest SHA-256 `D8EEC558C93081AE92C482D40351539CF6AF242E291A073342EA5D94643A54A0`、promotion 后 Windows worktree consensus bytes SHA-256 `E4CD9770A5593F5849BAEAA780AD31AD539B9B5BDD29DF3223634705CBE159E5`。38/38 production policy receipt SHA-256 为 `BE4FC105F3005A3ED49233698674CA246335AF5A29D86CDD356749C8C2FDEBCB`；本地 X509 signer `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5` / `physical-host-b` 与 GitHub OIDC builder `149E54CA4F8A0B7AC0ACD936B4F022FA23B74E7DB10BD380C6B229BDC35C05F9` / `github-hosted-windows`（run `32454968377`）满足双 signer、双 faultDomain、production receipt 与 v2 strict/full-install verifier，于 `2026-08-21T06:42:37.9529236Z` 完成 promotion。

该字体列车当时正式部署的根 EXE SHA-256 为 `55EABB5C3280FDFEE8302843D969C6B6354808F1AF4DC24DB5E2397AA80499D8`（未变），Core DLL SHA-256 为 `D970911DEBECB7EDAC1E9FAA631BF4FF5C0B55CF36DB85AEEC57363E08A4B029`，`miniaudio.dll` SHA-256 为 `AC3A8FC3BE306D595918F392B44E298D95B71221311920E892C4BE2A6E3AD20A`（未变）。部署后 worktree/index 33-file bundle、signed consensus 与正式根入口 `--verify-only` 均通过；promotion commit `973dc8ab46fa8b627fe17c777eacfb4f62c918ee` 的 post-promotion audit run `32455849082` 精确报告 base `df7d7fd5e1…`、head `973dc8ab46…`、`state=promoted`、`deploymentChanged=true`。维护者在 promotion 前明示字体人工观感验收；没有在 post-promotion formal runtime 重跑字体业务标准入口旅程，第三方字体也未取得正式法律意见，因此该列车状态为 `GATE_E_COMPLETE / promoted`，不称 `standard_entry_verified`。下行仍以“当前”描述更早 source / identity 的段落均按历史列车输入阅读，不覆盖本段。

runtime v2 的工具、schema、队列、本地 X509 证明、GitHub OIDC/Sigstore 证明、promotion 与 CI 状态机已经完成正式闭环；**仓库当前受控部署已是 manifest/consensus v2**。上一 A1–A6 consensus 的 artifact source `066629EB072083E80BAD5629377CADE36E11BE6962309B8A8CBC0EBC789B4451`、producer recipe `B97998EA7246D6AB667902BCBBD7994DFA5F658A37CFA427D2EEEABA6924DE28` 与 toolchain lock `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD`，在 policy `5663E9FE9108E25A4E9C5831EA1B53DF040130D3F50386DF3EC88A30F300C61E` 下形成 build identity `E203E4F06F6701F8B583768145AE64F93D2599238C290DD71FD50A9FBAA7B422`；payload closure 为 `F606DF4D2B11579121C7122ECB80734053B0BDF39948921C65B0FDC6CB66800F`，manifest SHA-256 为 `9F96A65006985BA27E2CDEC9B67FCC684974B4FCF3BD33CAA8B1D13BC7480517`，promotion 后 Windows worktree consensus bytes 的 SHA-256 为 `83C5111A56AAFD50790E71FF3122EE13CD075B5CE34043ECDDF1DEE7B07F0EE7`（不是 Git clean-filter 后 LF blob 的字节哈希）。production policy 26/26 receipt SHA-256 为 `C454E4394B0CAB0FC1F8193A4A2D2129AB1734BF4137D00EB2CD4D1C483ECEF4`；本地 X509 signer / `physical-host-b`（keyId `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5`）与 GitHub OIDC builder `EF1DBDFE50A7A18E262ED5C10132C32155CC18BEB0D74E20E71EA6471D11756E` / `github-hosted-windows`（run `31101353810`）的双 signer、双 faultDomain、production receipt 与 v2 strict/full-install verifier 全部满足后，于 `2026-08-06T12:52:12.7937229Z` 完成 promotion。

上一 A1–A6 request 使用 ACL 收紧的 `C:\qw6` 与显式 UTF-8 本地 worker，并且只 dispatch 一次 GitHub run `31101353810`。轮询期间一次瞬时 TLS 超时后只恢复同一 run id，没有重新 dispatch；最终本地与云端有效证明精确绑定同一 source tag/commit/tree、build identity 与 payload closure。首列车 `99919be46d…` / v1 因 `WB112` 五个超限模块在本地 production policy 失败，未 dispatch、未 promotion，作为 superseded / NOT_DEPLOYED 证据保留；此前 F8 与 no-AS2-fallback requests 均只保留为历史。

v1 与一次性 `migration-bootstrap` 现在只保留为历史迁移审计输入。该 marker 曾精确绑定 base `711c469036ad6b1226833faf255499abb1ebf2ed`、旧 artifact closure 与目标 builder registry 字节哈希，并在 legacy deployment 零变化时解决“cloud workflow 必须先进入 default branch”的 bootstrap 悖论；marker 后的首个部署提交已经完成完整 v2 promotion。CI 从此只接受 v2 strict 状态，并永久拒绝 v2 → v1 降级。

上一 A1–A6 正式部署对应 source commit `730c6be781ddd22bfd7a59a2e7773acce892f105`、build identity `E203E4F06F6701F8B583768145AE64F93D2599238C290DD71FD50A9FBAA7B422`、payload closure `F606DF4D2B11579121C7122ECB80734053B0BDF39948921C65B0FDC6CB66800F`、Core EXE SHA-256 `86DF1F5DC611037DB3A85FD9BA0D43490394F232D25A4F62B59AA4F2B4B6E4FD` 与 Core DLL SHA-256 `75F35C025BDE29D4D713671763511B425D1AFA5758DD154931AC0FC5C59C4977`，包含双栏工作台 A1–A6、F8 Agent Runtime 与此前正式能力。无 candidate id 的正式根入口以 `formal_runtime` 启动并绑定上述 consensus；`tmp/manual-agent-acceptance/workbench-v2/formal-workbench-20260806T133125Z.json`（SHA-256 `932EB04FC699BC263A4F70980A4BF87CE5F2B9147104429559E38C82F0CA7B8A`）取得 `acceptancePassed=true`、Launcher/Help WebOverlay WGC、one-shot terminal receipt、可信 shutdown、测试存档不变与退出后无新增 Launcher/Core/Flash 进程，故该窄纵切严格达到 `standard_entry_verified`。native identity / closure 只覆盖正式 runtime 文件闭包；Web/Flash 字节与本轮行为另由冻结 source/release tree、production policy、报告和实际窗口绑定。本轮没有 Character/Equipment 业务 opener、A1–A6 写操作、物理拖拽、玩家目视签收或所有专项实机旅程，不能把总体状态外推成这些专项验收。

**PlayerInfo main-space v2 当前状态**：下面保留的旧 F/tag/request/quorum/47-Gate/Kimi 数值均为历史专项证据。qualification 闭包修复后的 F2 `891d9b08dbd826d8b2624c6bdc59082b3db57ecd` / r2 列车自身仍是 non-deployment train；其实现字节随后先被 `730c6be781…`、现被 `bf9be8c43b…` release 包含并进入 formal runtime。B0 仍为 `b0_accepted`，oracle 仍为 `oracle_frozen_for_b0`；本轮 formal smoke 虽观察 Launcher 与 Help WebOverlay WGC，却没有启用 PlayerInfo fixture、观察真实 `pi_*` 或执行 PlayerInfo-specific standard-entry E2E，因此不能从总体 runtime 发布状态外推 PlayerInfo 专项验收。

**r2 非部署可复现闭环**：request `6B5E9BDD5393553871C646952B1266E877D8B8941D50AB510B5387FC1261460D` 绑定 artifact source `6AAF891B6A27681B7493782FF9D85B3527DBD4B25AB086AABEDEEC02A062A4E0`、producer recipe `B97998EA7246D6AB667902BCBBD7994DFA5F658A37CFA427D2EEEABA6924DE28`、toolchain lock `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD`、policy `4F3F7749564CD82272B014C5EFF7A962FB63A533185AD3B27612C79CFF12744B` 与 build identity `DAE8A71C0AE04AF1BD89B0076DB1EF6EF5DFEEC51C32843D300D868581568ED1`。本地 X509 signer `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5` / `physical-host-b` 的 23/23 production receipt 为 12,144 B / SHA-256 `91BBF333D4EC65A387D9A7CF94A73F8B43F54A77E05F7C684821F767FAF630D5`；GitHub OIDC signer `1B2F3B53CC50695D620A90CE423BEF3FC55C0866AEE958E7A8042BE682185630` / `github-hosted-windows`（run `30502544175`、artifact `8744105960`、archive digest `9D3B5B760AFEABEB3F7E89D5490421B3F44BAB777EF4A4FA4A4DF4C325D490F3`）的 23/23 receipt 为 11,884 B / SHA-256 `454EA66A721BC59E63E81169F5E4832490788FDC21CC2C37925E9D0B76FB2C9F`。两者独立产出同一 33-file payload closure `051FA840019E24763724B5DA0868486AF94322F3BB990B1354D181989AA94D2C`、Core SHA-256 `6BBE712CB0F97AC127FA3DF18158CA3976D7A03A64DC8121794E7C7C1E18C6CC` 与 manifest SHA-256 `53F44B8188A425728975197CD8B38FF5CE523B0306CA2EB8EE1ECBD76FF77261`。[`runtime-promotion-preflight-main-space-v2.json`](../tools/player-info-hud/evidence/b0-06/runtime-promotion-preflight-main-space-v2.json) 为 15,128 B / SHA-256 `2D0A2A862ADFFF4254FB8AFB910B1529568DABD33350AE8E735AC20F52B47356`，`proofCount=2`；33 表示 manifest 枚举的 payload files，不把 manifest 自身另算成第 34 个 payload。本 closeout 单元同时包含该文件与 current-truth 文档；只有二者在同一提交落盘后才是 tracked evidence，单独的生成文件或文档不得如此称呼。报告的 runtime/release-state mutation、promotion、deployment 与 reusable-as-promotion-input flags 全为 `false`，故它只关闭机器可复现门，不能作为 promotion 输入，也没有执行 candidate、E2E、promotion 或部署。本段不声称在 F2 重跑 historical v1 的 Runtime Lane C。

**F2 隔离候选执行证据**：在上述 `-VerifyOnly` 之后，独立开发启动使用 candidate root basename `c-dae8a71c0ae0-08846e81b3-20260730t004223766z-e8ce2e3c`。其 metadata 绑定 build identity `DAE8A71C0AE04AF1BD89B0076DB1EF6EF5DFEEC51C32843D300D868581568ED1` 与 payload closure `051FA840019E24763724B5DA0868486AF94322F3BB990B1354D181989AA94D2C`，Core SHA-256 为 `6BBE712CB0F97AC127FA3DF18158CA3976D7A03A64DC8121794E7C7C1E18C6CC`。Guardian PID `21628` 从该 exact candidate 启动。用户于 `09:05:48` 点击 `start_game` 后，成功 child Flash PID 为 `20644`，命令行精确加载 human-live worktree 的 `Adobe Flash Player 20.exe` 与 `CRAZYFLASHER7MercenaryEmpire.swf`。同一 attempt 的 Launcher 日志在 `09:05:49.800` 命中 `WaitingGameReady -> Ready`，在 `09:05:58.250` 连续命中 `bootstrap_reveal_ready: Flash reveal cleared` 与 `performing reveal (panel swap)`，并在 `09:05:58.269` 命中 `[RevealProbe] setready.player_info_split 0.3ms`。fixture 选择仍为 `p50`，日志亦命中 `[PlayerInfoSplitSurface] fixture-only surface enabled; case=p50; old Flash HUD remains untouched`。这份证据只把 F2/r2 该历史列车自身从 `candidate_built` 推进到 `candidate_executed / source-ahead / NOT_DEPLOYED`，不证明业务 E2E、真实桌面视觉接受、promotion、标准入口或部署；其实现字节后来被 `9118eb…` 正式 release 包含，也不会反向改写该专项列车当时的状态。

**B0 人类接受与 oracle 冻结**：本 closeout 同提交纳入的 [`runtime-candidate-execution-main-space-v2.json`](../tools/player-info-hud/evidence/b0-06/runtime-candidate-execution-main-space-v2.json) 为 3,960 B / SHA-256 `F7A7B03F0333C61B467B50B1F7003D77764A130C7FE745098B9B0DFE09928E50`；[`human-acceptance-main-space-v2.json`](../tools/player-info-hud/evidence/b0-06/human-acceptance-main-space-v2.json) 为 3,509 B / SHA-256 `FB2ED0D6550F51A715E21FB21D110B7EF5B1B04BD46FBC3D5FEA69653C105831`。维护者在上述可见 `p50` exact candidate 上明确回复“可接受，可继续推进”，关闭 actual game composite、旧 Flash HUD 同屏且 untouched、z-order/occlusion、orb 可见性、HP glow 强度/颜色、MP 横向/基线/蓝条垂直对齐、透明 crop、桌面 click-through、时间表现与整体审美，并接受当场观察到的 144 DPI / dpr 1.5 缩放表现。oracle 决策为 `oracle_frozen_for_b0`，basis=`actual_main_runtime_human_acceptance_plus_source_bound_main_rsl_equivalent_capture`；这关闭 B0 human Exit Gate，但不声明 cross-renderer pixel parity、真实 UiData、额外物理显示器切换、业务 E2E、promotion、标准入口或部署。

**首列 v2 退役记录**：受保护 tag `runtime-build-v2/20260729-player-info-nativehud-b0-v2` 永久冻结到 `40853287e7ed04714d68935c0002f8ad6d8aea05`；q3 request `AC3917AB6AB79EB0C8B766F2E3C82D86E7C5CC05E7FF0867FF5A42B8B222EC4A` 的本地 X509 producer/票有效，状态为 `1/2`。该列车在 production policy 的 23 项中通过 22 项，唯一失败为 `candidate-player-info-svg-contract`：隔离 `RendererQualification.csproj` 漏挂已被 `PlayerInfoStrictSvg.cs` 使用的 `PlayerInfoRasterPlan.cs`，触发 CS0246；失败 receipt 为 12,141 B / SHA-256 `3B812E9576164D8472D16020AD0D88824127CB43F7D07B067C0BBAC108293315`。因此它是“因 production policy 门失败而退役”，不是 producer/request failed；未启动 GitHub builder，未形成 quorum/preflight。旧 tag/q3/CAS/票/失败 receipt 只保留审计，绝不移动、删除、supersede 或复用。

**Historical v1 非部署闭环**：PlayerInfo B0-03b～B0-06 的旧 v1 build train 在当时停在 `source-ahead / NOT_DEPLOYED`；较新的 `9118eb…` 正式 runtime 已包含后继实现字节，但不会反向改写该旧列车自身的非部署状态。该 v1 的 source-freeze 与 reproducibility/quorum Gate 曾闭合。该 v1 的 source-freeze F 为 commit `cb38600aae51f5019d09f87c33bd9e67d2b1f511`、tree `c74cc66f445921f5fcda1da04a2de0f013fef8ef`；在 F push、tag 冻结与 builder dispatch 时，远端 `main` 和不可变 tag `runtime-build-v2/20260729-player-info-nativehud-b0-v1` 均精确解析到 F，后继 docs 提交只会推进 `main`，该 tag 必须继续永久指向 F。该 v1 request `839C74FD1DF61ACC1DA580041F6FA71CA13A84DF1F43A55237BF9BEEF8648FB2` 绑定 artifact source `A7190F548851A505F9C6ACCE9F132E1741BAB4B47597473801C79DE32589DBC8`、producer recipe `B97998EA7246D6AB667902BCBBD7994DFA5F658A37CFA427D2EEEABA6924DE28`、toolchain lock `7B83229BE93F8244810CDD23DAFD97875B23857E547DE520035FE23B453CB3CD`、policy `0E52F22FD3A42C5770B5B0C7C58E48662097C01CACA8306595991AD61E4DF105` 与 build identity `7687B56106F0C19EFD4463DCA2B69B3892BD3EE39503A3FD5521ED6DB0257A9F`。本地 X509 signer `EB5D32E04B6EE8697850314E19698DE1A3FACFFCCC6418A12CF7FEDE6033CDA5` / `physical-host-b` 与 GitHub OIDC signer `FA7BFFEB064C9B263B3A8BB6CFB5A655891CD9A9065B02D3BD6862DBD42C34F4` / `github-hosted-windows`（run `30440903705`）分别实际构建并对同一 33-file payload closure `AF2E9E5727E9B6ADE056EBE4CA52BE93AD518116F10FF3307DA4409059CC724B`、Core `A45471CF4CB2286CCE3036AB7B6E72A99FD62B62DDBAF16E0E42234AF17FB652` 达成双 signer / 双 faultDomain 共识。production receipt SHA-256 为 `0E4E81E2D5D433277A726920A5FBBBB19CA10FEA3CBB47A66D3D237CEC0B02D0`；[`runtime-promotion-preflight.v2.json`](../tools/player-info-hud/evidence/b0-06/runtime-promotion-preflight.v2.json) 为 `preflight-passed`，且明确 `runtimeMutationPerformed=false`、`releaseStateMutationPerformed=false`、`promotionPerformed=false`、`deploymentPerformed=false`、`reusableAsPromotionInput=false`。因此这不是 promotion、标准入口验收或部署，报告也不能回灌为未来 promotion 输入。

两个实际 builder 均记录 `.NET SDK=10.0.300`：本地成功 worker 的 `[RuntimeBuildEnv]` 行，以及 GitHub run `30440903705` 的 Provision/Produce 日志均命中该值。该日志事实与 identity/closure 共识一起构成资格证据；不能只用本机预检替代 cloud 的 actual toolchain 记录。

生产项目已固定 `Svg.Skia 5.1.1` / `SkiaSharp 3.119.4`、8 个 canonical SVG、runtime manifest、第三方 notice、strict facade/qualification 与 candidate production contract。B0-05/06 的 historical v1 在实现基线 `bf8dd2c410267855c8ea12f25a594042b3158479` 完成 raster/cache、`split_required`、fixture-only split surface、47/47 机器资格和自动视觉闭包；两份旧 v1 最终 JSON 与同步 canonical docs 由 evidence/docs-only commit `81168ece8cd295d8e15b8f6baed46a0da7f9c549` 承载。旧 v1 F 的 parent `2882456c8777246fba8d2f6942ef1027caafc650` 已纳入 `origin/main@232d25ddf96ba39147f3335162d8a2d033c7822f`；旧 v1 F 相对 parent 的非文档字节只有 PlayerInfo source tag 配置与 release prepare 捕获的 `arena-unit-param-presets.js` 机械派生差量，前三域与 build identity 均未漂移。旧 v1 B0-03b candidate、B0-06 实现/视觉报告与 F 双 builder 证据仍是三个不同作用域；historical v1 的最终 K1 曾在 clean G 上取得 `K1_AUTOMATED_GATE=PASS`、P0/P1=0，但 formal owner 位于屏外，机器资格、Kimi PASS 和图像闭包都不证明可见桌面 DWM composition、游戏 z-order/occlusion、实际鼠标透传、审美或人类验收，当时旧 v1 状态最多为 `awaiting_human_acceptance`。该状态已被 main-space v2 取代。main-space v2 已在 clean F `40853287…` 完成 B0-05 32/32、B0-06 48/48、full 1747+3/1750 与正式 visual-evidence.v2，在 clean G `884fbf43…` 完成最终 Kimi `k3/high` PASS，并由 F2/r2 完成双 builder、23/23 policy 与不可复用 `-VerifyOnly` preflight；随后 exact isolated candidate 启动并由维护者接受，故 B0 为 `b0_accepted`。F2/r2 该专项列车自身仍为 `candidate_executed / source-ahead / NOT_DEPLOYED`；较新的 `9118eb…` 正式 release 包含其实现字节，但没有运行 PlayerInfo-specific standard-entry E2E，不能据总体 runtime 发布状态提升该专项验收结论。

该 historical v1 F 已在 clean detached worktree 完成最终复核：11 个 release prepare 输出全部匹配 F tree，doc governance、`git diff --check`、锁定环境 `bootstrap-runtime-build-env.ps1 -VerifyOnly` 与真实远端 admission audit（`Active`、`mainDirectPush=true`、tag creators=`Crazyfs,Flash-Night`）均通过。Runtime Lane C 为 11/11 个入口 exit 0，十个 scalar 套件合计 566，guardrails 单列 `scripts=3 / unsafeCandidateCases=3`，总耗时 831.271 秒；stdout 为 18,488 B / SHA-256 `9BFBDDD521BE54D70E0158CE595C37C54820EE4C66C73C5681AFC56636A524AC`，stderr 为 3,280 B / SHA-256 `F7FA16D6B9927C3B36D6A61FCBC49C00874EAAC41EBFC662F863793E442479C6`。stderr 是测试 fixture 的 Git EOL/clone 诊断，不改变 11/11 退出结论。此前 staged snapshot 的 scalar 567 / 863.010 秒只是一轮提交前历史预检，不得覆盖 clean F 的最终 566 / 831.271 秒。

historical v1 PlayerInfo F 当时另有一条不并入该列车的 Arena 传递派生债务：`tools/derive-arena-meta-teams.js` 读取 `data/stages/**`，但在该冻结点不在 release prepare 中，其 `--check` 只解析/打印、不比较 tracked 字节。只读内存复算当时得到 `data/arena/meta_teams.json` tracked 3,470,547 B / `635FB515…98F72`、现源 3,367,986 B / `26099187…47D3`；`launcher/web/modules/arena-meta-rosters.js` 1,291,607 B / `2F677C5F…9453` 对 1,292,790 B / `32F1D6B9…FD65`；继续派生时 `arena-custom-presets.js` 从 1,186,819 B / `26A77128…51A5` 变为 1,189,068 B / `21F4020F…68B6`。这条约 5.8 MiB 漂移仍是旧 v1 F 的审计输入，不能冒充 232d 的 PlayerInfo 邻接修复，也不改写旧 F 的 Exit。后续 P5 implementation `970a85dfdba` 已让 meta-team `--check` 精确比较两个 tracked 输出、把 meta-team/faction generator 纳入 release prepare，并将相关输入/输出纳入 runtime policy；当前三个传递 `--check` 均通过。因而“未纳入 prepare/仍有 5.8 MiB 漂移”不是当前状态，也不得覆盖后续 P4/P5 正式列车的独立 release/E2E 结论。

Windows EOL materialization 是 historical v1 F 当时接受的非阻断 tooling debt：系统 `core.autocrlf=true`，而四个生成物当时只有 `text=auto`。2026-08-23 复核用真实 clean clone 精确确认：Git 状态干净但 `arena-custom-presets.js` 被物化为 CRLF，raw worktree OID 与 index/clean-filter OID 不同，raw-byte generator `--check` 因而误报 stale；runtime v2 的 clean-filter 对象身份实现本身没有错误。
当前四文件已显式声明 `text eol=lf`，versioned test 钉死真实 CRLF、Git-clean 与 batch/single/index OID 等价；fresh `core.autocrlf=true` clone 物化 LF，raw/index/batch OID 相同，generator `--check` 与 `tools/test-runtime-build-v2.ps1` 156 项均通过。该修复已进入本次 release source、immutable request、双 builder closure 与 promotion，不再是 source-ahead；historical v1 F 的当时证据仍不反写。

2026-08-25 限时关卡列车的首个 `runtime-build-v2/20260825-stage-time-pools-v1` / request `BBCDF4C8D8965C09FC865B7D9614111FF55C4267B7D233EAFC7BFB73BDF43E13` 在本地 production policy 以 37/39 停止，未 dispatch 云构建、未 promotion：`material_dictionary.generated.json` 与 ShopPortraits provenance 把 `EquipmentTuningService.as`、`bake-dialogue-portraits.py` 的 raw worktree 字节写入 sidecar，但这两个输入仍是 `text=auto`，前一列车因局部补丁留下的混合 EOL 与当前全 CRLF 物化无法跨 checkout 复现。`.gitattributes` 随后把这两个 raw-byte 输入及 ShopPortraits `promotion-receipt.json` 显式固定为 LF，派生 sidecar 重新绑定全 LF tracked bytes；`tools/test-runtime-build-v2.ps1` 同步把完整材料/ShopPortraits raw-byte 输入输出集合纳入 effective attribute 与零 CR 回归，现役结果为 181/181。v2 冻结点已达到本地 39/39，但其文档仍错误保留旧 36/38 计数，故也没有 dispatch 云构建或 promotion；v3 校正为真实 37/39 后才进入当前共识与部署。三个 tag/request 都不复用、不移动。

本地 X509 worker 的首次隐藏 PowerShell 尝试也保留为 portability debt：父进程未显式初始化 UTF-8，中文 bundle 路径被错误代码页解码，historical v1 request `839C74FD1DF61ACC1DA580041F6FA71CA13A84DF1F43A55237BF9BEEF8648FB2` 按合同 fail-closed，`failure.json` 记录 `本地开发启动.cmd` 被解码为乱码。没有删除这份失败记录；随后对同一 request 使用显式 `chcp 65001`、`[Console]::OutputEncoding` 与 `$OutputEncoding` UTF-8 wrapper，`builder-local-b` 才成功生成上述 X509 proof。成功证明该受控 wrapper 有效，不证明正式 worker 入口的代码页 portability 已根治；后续须把 UTF-8 初始化固化进正式入口并增加中文路径 fresh-process 回归。historical v1 的最终 K1 曾接受其为 non-blocking portability debt，仍须由独立治理片根治。

历史 `chest-s0-a8a-local-r3/r4` 是 2026-07-18 基于 `a8a760a3cc` 的同机未注册诊断；S0 已从当前源码与 required-assets policy 退役，这些旧 artifactSource/buildIdentity/payloadClosure 只留在 Git 历史和旧 ADR 中，不代表当前工作树、release evidence 或待恢复的发布输入。

2026-07-19 本机构建阻塞已经解除：锁定 bootstrapper FileVersion `17.14.37502.11` 已把 side-by-side Build Tools 安装到 `C:\Program Files (x86)\CF7VS\BT1736`，cl `19.44.35228.0`、link `14.44.35228.0` 与 Windows SDK `rc` 均通过 lock 中的精确版本和 SHA-256 门；`bootstrap-runtime-build-env.ps1 -VerifyOnly` 与 `check-runtime-build-env.ps1` 均 exit `0`。安装后还修复了 Windows PowerShell 5.1 对 `vswhere` 顶层 JSON 数组的函数返回包装：现在逐实例输出，旧 VS 与 BT1736 不会被拼成一个虚假 `installationPath`，同一进程即可重新发现 side-by-side 实例。

2026-07-19 的 `AE1FC1EF… / 172A85C6…` 23-file diagnostic candidate 曾在临时开发目录完成基础启动、map/tasks/NPC 与早期 loot claim/close/unpause/存盘诊断，并暴露 `TransparencyKey` 点击穿透、claim-all 收束与 terminal late-ack 问题；它不含后续修复，也不代表以 `b072f97841ccb30e167c14495241ae64d9054e22` 为 upstream base 的本轮发布。该 candidate 及其后的 FCA19/B2AF/231388 等合并前 loot diagnostic candidate 均已退役。本轮人类 E2E 最初使用的隔离 candidate 位于 `tmp/runtime-candidates/v2/c-2a0cddb077b7-08846e81b3-20260721t100014612z-f32a40a3`，绑定 build identity `2A0CDDB077B760328B3141EFFFEEE3996841FA1CE49AD09E5D7339417F60A107`、payload closure `3B837DCDBC69AA47074E635DACACAE3B80263023E6032AC1FDF209768B1C150C` 与 Core SHA-256 `0F58BF864B8DE9C7FCEA098D7E1EEA1996BDE38D85D87E844B047B53F5247232`；corrected Agent entry、正常 NativeHud、同实例 organizer、普通 suspend/same-anchor 内容不变 reopen/final claim 已在该候选完成，因此它在当时达到 `e2e_verified / NOT_DEPLOYED`。随后同一 build identity / payload closure 经 source commit `c60aab2386aee4516608397373ae4c59148c5f77` 的 immutable request、production receipt、`builder-local-b` + GitHub OIDC quorum 与 strict verifier 完成 promotion；prewarm-held 正式入口 attempt `4eae1360cedd413fa3175db6a8997158` 又取得 fresh title marker、exact attempt receipt，完成真实 loot 两次 claim、terminal close/unpause 与存盘，故唯一 canary 达到 `standard_entry_verified`。此前一次正式入口 attempt 因 reveal watchdog 先于真实 title receipt 而以 `title_frame_not_observed` 失败，保留为换机/冷启动 portability 历史观察，不覆盖后续成功结论。隔离 candidate 的 25-file native payload 不含 `launcher/web`，外部 Web 字节仍由源码哈希与 WebView2 实机日志独立绑定，不能由 native identity/closure 代证。

## 不变量

- 单个进程、机器名或自由填写的 `BuilderId` 都不构成独立 builder；正式 quorum 至少需要两个不同签名身份和两个不同 `faultDomain`。
- producer 只生产二进制 payload，不运行生成器或产品政策审计；政策变化不能污染 payload 身份。
- request JSON 没有自由命令字段，只冻结 Git tree 与政策身份；但 Git bundle 本身含 producer/MSBuild/Cargo 源码，worker 会在 builder 账户下执行该冻结 commit 的构建代码。因此共享 queue 是受控写入信任边界，必须限制写权限；四域复算与 promotion 能阻止错误产物晋级，不能判断源码业务意图，也不能把恶意 queue writer 沙箱成无 RCE 能力。
- candidate、CAS 与 attestation 都不能直接覆盖正式部署；只有 promotion 能事务写入根 bootstrap、`runtime/`、manifest 与 release consensus。
- strict/promotion 状态逐文件验证字节闭包；日常 native 源码可以处于 `source-ahead` 而不立刻 promotion。事后 Audit 在部署字节未变时不读取 payload blob，也不声称验证源码内容语义；只有正式部署闭包变化才必须携带完整新共识。
- 工具链不匹配、证明不足、相同故障域、分叉 payload、脏部署或无效政策 receipt 都 fail-closed；禁止伪造第二 builder、手改 hash/receipt/attestation 或复制单机 candidate 到正式 runtime。

## 开发到正式验收状态机

| 状态 | 最小证据 | 明确不代表 |
|------|----------|------------|
| `compiled` | 编译命令成功，输出仍可位于 `bin/obj` 或其他临时目录 | candidate 已生成、运行时已加载新字节 |
| `candidate_built` | producer 返回唯一 immutable `candidateRoot`，metadata/manifest 的 build identity 与 payload closure 自洽 | 已执行、已部署 |
| `candidate_executed` | 推荐由 `automation/dev.ps1` 按当前 Worktree build identity 精确选择后启动；低层诊断也可用 `automation/start.ps1 -CandidateRoot <absolute candidateRoot>`。两者都须确认 `runtimeMode=isolated_candidate`，且实际 process path、Core SHA-256、build identity、payload closure 全部匹配 | 领域 E2E 已通过、正式入口已更新 |
| `e2e_verified` | 在上述已绑定 candidate 进程中完成受影响领域的真实 Web→Host→AS2→回包 / 写后回读门 | promotion 或正式验收 |
| `promoted` | 唯一 promotion 入口完成事务替换、v2 consensus 与 full-install `--verify-only` | 标准玩家入口已实际加载该身份 |
| `standard_entry_verified` | promotion 后无参数运行 `automation/start.ps1` / 根 bootstrap，确认 `runtimeMode=formal_runtime`、正式 Core 路径/SHA-256/build identity/payload closure 与被提升身份一致，并完成受影响领域 smoke | — |

状态只能按 `compiled → candidate_built → candidate_executed → e2e_verified → promoted → standard_entry_verified` 报告；允许因任务范围停在中间，但不得跨级命名。只有同一身份已达到 `promoted` 且随后达到 `standard_entry_verified`，才可称“已部署 / 正式验收”。日常开发显式走 `automation/dev.ps1`：它按当前 Worktree 身份精确复用/生成隔离 candidate，但始终为 `NOT_DEPLOYED`。`automation/start.ps1` 无参数只消费正式根部署，不得猜测或自动选择 `launcher/bin`、`tmp/runtime-candidates/` 中的“最新”输出；`start.ps1 -CandidateRoot` 仅保留为指定精确候选的低层诊断兼容入口，不再推荐手工直启 Core。

## v2 四域身份

`config/build/runtime-inputs.v2.json` 是输入域清单。四域互斥，发现同一路径同时属于两个域会失败。`launcher/native/audio-v2-build-inputs.v1.json.materializedInputs` 中每一项还必须进入这四域的并集；`tools/test-runtime-build-v2.ps1` 对该包含关系 fail-close，防止本地完整工作树可构建、正式 sparse request 却漏掉第三方生成输入。

| 身份 | 内容 | 变化后的动作 |
|------|------|--------------|
| `artifactSourceHash` | C#、C/C++、Rust 与项目/包输入等真正影响 payload 的源码；PlayerInfo 另以窄树只纳入 `Assets/**/*.svg` + runtime manifest，并固定纳入第三方 notice | 必须重新构建 |
| `producerRecipeHash` | 纯 producer、native build、环境门与确定性参数（含 `sol_parser/.cargo/config.toml` 的 `/Brepro` 链接参数） | 必须重新构建 |
| `toolchainLockHash` | runtime toolchain lock、`global.json`、Rust toolchain | 必须重新构建并重新取得环境资格 |
| `policyHash` | 生成器、审计器、队列/证明/promotion/CI 规则及已派生发布资产 | 新 request + 新政策 receipt；前三域未变时可复用同一 build identity/CAS payload |

`buildIdentityHash = SHA256(artifactSourceHash + producerRecipeHash + toolchainLockHash)`，故意不含 `policyHash`。`releaseTreeOid` 冻结完整 Git tree，`requestId = SHA256(releaseTreeOid + policyHash)`；两者分别回答“发布哪棵树”和“用哪套政策批准”。

四域哈希的 Git object identity 以批量方式解析：Worktree 域由单个 `git hash-object --stdin-paths` 进程消费全部仓库相对路径（stdin 以无 BOM UTF-8 字节直写进程管道，规避 Windows PowerShell 5.1 在 UTF-8 代码页下向原生 stdin 注入 BOM 的行为）；Index 域由一次 `git ls-files -s` 全量解析成 stage-0 查找表。失败语义不变：进程非零退出、输出行数与输入数不等、任一行不匹配 OID 形状、路径含 LF/CR/绝对/越界形态、或 stage-0 记录不唯一，全部 fail-closed。逐文件单进程实现保留为 `Get-Cf7RuntimeV2GitObjectId` 语义基准，`tools/test-runtime-build-v2.ps1` 的批量/单文件等价性回归逐字节钉死两种模式；2026-08-23 起该回归还写入真实物理 CRLF，确认 `eol=lf` 下 Git-clean 且 batch/single/index 返回同一 clean-filter OID。域内固定文件的存在性检查仍逐文件走 Git 查询；这残存的少量 spawn 不影响量级。

native 源码前缀内的非二进制契约文档也必须显式绑定，不能因为扩展名不是 `.cs` 就落在 release descriptor 之外。当前 [`launcher/src/AgentRuntime/Contracts/README.md`](../launcher/src/AgentRuntime/Contracts/README.md) 作为 C# 对照实现的发布约束固定归入 `policyHash`；它会改变 request/receipt，但不进入 `artifactSourceHash`，也不会冒充 DLL 字节变化。这里采用单文件绑定，不把整个 `launcher/src/**/*.md` 扩成构建输入。

`policyHash` 与日常审计触发集合不是同一个集合。`config/build/native-change-gate.v1.json` 联合前三域、payload、全局 native 扩展/入口名和 release 信任链路径，回答“这次 push/PR 是否值得启动 native/runtime 事后审计”；广义内容 policy 不因此变成 native。命中源码边界但未改部署字节时，Audit 成功报告 `source-ahead`，不要求即时 promotion。生成器、审计器和派生发布资产仍留在 `policyHash`，下一次正式 release 再用当时完整 release tree 建 request 与 receipt。

`policy` 树只绑定受版本控制的审计实现与契约；执行门恢复出的 `node_modules/`、`bin/`、`obj/` 等机器依赖必须由各树显式排除。否则同一干净 Git tree 会因本机已安装依赖而产生不同的 Worktree `policyHash`，并在 promotion 前失败关闭。`tools/arena-calibration/node_modules/` 因此不进入政策身份，其锁文件与受跟踪脚本仍照常绑定。斗兽计划中的两份 `source-provenance.json` 是仓库侧取证材料，不是 producer 或 policy 执行输入，也必须由 `excludePaths` 精确排除；计划、Schema、runner 与受跟踪校验脚本继续留在闭包内。

`payloadClosureHash` 对根 `CRAZYFLASHER7MercenaryEmpire.exe` 与 `runtime/**` 的实际 payload 文件有序计算，明确排除 `runtime/cf7-runtime-manifest.tsv`、证明与 release record。这样 manifest/policy 元数据变化不会被误判成二进制失衡；manifest v2 再记录四个构建字段中的前三个、`buildIdentityHash`、`payloadClosureHash`、工具链可读名和逐文件大小/SHA-256。

## producer 与政策闸门

发布链分成三个职责，不能重新合并：

1. `tools/prepare-launcher-release-assets.ps1` 只恢复锁定的 TypeScript/字典依赖并派生受跟踪发布资产。`save_schema.json` 默认保留；只有显式 `-SaveSchemaSource` 才从指定 canonical save 重建，避免私有存档和时间戳偷偷进入发布。
2. `launcher/build-runtime-candidate.ps1` 是纯 payload producer：先执行精确环境门，再在 job 独占的 native/Cargo/MSBuild/temp 目录构建 miniaudio、Rust parser、bootstrap 与 FDD Core，生成不可覆盖的 v2 candidate。Audio Platform v2 不再只复制 `miniaudio_bridge.c + miniaudio.h`：producer 按 canonical [`audio-v2-build-inputs.v1.json`](../launcher/native/audio-v2-build-inputs.v1.json) 的固定排序清单 materialize 多 translation-unit bridge/backend/decoder、pinned miniaudio/Xiph 源码和 Media Foundation adapter，以 lock 中的精确 flags 静态闭合为唯一 `miniaudio.dll`；codec LICENSE、dependency lock、notice、runner/config/fixture 分别显式落入 artifact source 或 policy 域，禁止 wildcard、loose codec DLL 与宿主 codec pack 漂移。candidate 尚无正式 consensus，因此这里只同步、有界等待 bootstrap `--verify-runtime-only` 并检查真实 exit code；失败会保留/输出受限日志，成功必须删除 `logs/`。它不跑 Web/数据产品审计，也不签名。candidate 根部的 `runtime-build-metadata.v2.json` 在 payload closure 之外（payload 配置只含根 EXE + `runtime/**` 减排除项），除既有四域 identity/closure 字段外还记录 `policyHash` 与 per-stage `stageSeconds`/`totalSeconds`（环境门、identity 前/后、miniaudio、sol_parser、bootstrap、dotnet publish、组装、bootstrap verify），schema 只加不减，供 worker 构建后比对与耗时诊断使用。
3. `tools/validate-launcher-release-policy.ps1` 是只读政策门：绑定 `releaseTreeOid` 与四域身份，验证 tracked tree 在审计前后未变化，按需严格验证 candidate，并把每项结果写成 `cf7-runtime-policy-validation.v2` production receipt。它既支持 clean commit 的 `Worktree` 身份，也支持工作树逐字节 materialize 同一 staged tree 的 `Index` 身份；candidate 始终按磁盘 payload 复核。候选优化检查会丢弃调用者注入的 `CF7_DOTNET_EXE`，重新运行锁定工具链门禁并只接受其选出的 host；门禁不产出精确 host 就禁止签发。`candidate-player-info-svg-contract` 同样丢弃调用者 dotnet 注入，使用锁定 SDK 做 locked restore，并对 exact candidate 核对 full canonical manifest、9 项 embedded resource/source bytes、strict 最小 raster、notice exact bytes 与禁用依赖；runtime 根或任意后代若是 reparse/junction，会在递归枚举前 fail-closed。随后实际 renderer-family DLL/native 相对路径集合须 exact=11、每项非空并记录 actual size/hash；这些实际字节再由 candidate payload closure/build identity 绑定。deps libraries 与唯一 renderer-bearing runtime target 也须精确相等；额外顶层 DLL、嵌套 native 文件或 deps library/runtime-target 都 fail-closed。只有 candidate 模式可 `policyEligible=true`，直接 Core 只作诊断。`required-web-runtime-assets` 必须覆盖生产懒加载闭包；地图资源箱必须逐项包含 `modules/loot/loot-runtime.js`、`loot-state.js`、`loot-view.js`、`loot-organizer.js` 与 `loot-panel.js`，任一缺失都 fail-closed 并在 receipt 点名。`panel-cross-layer-contracts` 使用的契约 JSON、validator 与变异测试脚本全部进入 `policyHash`，同时由 native change gate 和 GitHub workflow paths 触发事后审计，避免 sparse materialize 缺少门禁输入或未来只改契约却漏审。子审计 stdout/stderr 只进入人类/CI 日志，不能混入结构化 `checks[]`。receipt 只能写未跟踪路径。

`launcher/build.ps1` 只是人工兼容编排器：prepare → pure producer → policy。它只写隔离 candidate，最多把状态推进到 `candidate_built`，不写根 bootstrap 或正式 `runtime/`；它适合已准备好的本地 tree 做完整候选检查，但不是多机发布协议，也不会替代签名 worker、immutable request 或 quorum。

Audio Platform v2 的 H1/H2 是独立的产品验收证据，不是通用 runtime supply-chain 门。H1 继续约束 A1–A6 实现与 isolated candidate；最终 Audio 验收仍须让自动报告、真实 endpoint capture 与人工听感绑定同一 exact candidate，只有有效 H2 才能声称 Audio 专项 `e2e_verified` 或 `standard_entry_verified`。通用 source tag/request、双 fault-domain 构建与 promotion 只执行 immutable request、双 signer/faultDomain、production policy、strict verifier、原子替换与 rollback，不读取、生成或旁路 H1/H2/E3。现役 `cf7-runtime-build-request.v2` 保持严格原 schema，产品体验证据可诚实保持 `pending`，不得由部署事实反向补签。

未提交工作树的可见功能检查统一走 `automation/dev.ps1`（或根 `本地开发启动.cmd`）。它重算当前 Worktree build identity，只复用同身份且闭包唯一的 candidate；无命中时以 `-SkipPrepare -SkipPolicy -BuilderId local-dev` 新建隔离 candidate。`-Status` 只读报告匹配/过期/同身份闭包分叉，`-ReuseOnly` 禁止构建，`-ForceBuild` 强制新建但仍拒绝分叉闭包，`-BuildOnly` 只选择/构建并验证而不启动。忽略的 `tmp/runtime-dev/active.v1.json` 只是 repository-relative 选择索引，不授予信任，每次执行前仍重验字节身份。

`dev.ps1` 最终把精确 candidate 交给 `automation/start.ps1 -CandidateRoot`。该低层入口只接受当前仓库 `tmp/runtime-candidates/v2/` 下的 canonical 非 reparse producer 输出，严格核对完整安装哨兵、candidate metadata、runtime manifest、`buildIdentityHash`、`payloadClosureHash` 与 Core SHA-256，再调用 candidate 自身 bootstrap `--verify-runtime-only`；Core 启动后仍按同一身份反向自检，并显式使用当前完整安装根加载工作树 Web。只有报告/日志中的 `runtimeMode=isolated_candidate`、`processPath`、`coreSha256`、`buildIdentity`、`payloadClosure` 全部与预选 candidate 一致，才能报告 `candidate_executed`。目录 walk-up、候选树外搬运、reparse 别名或 marker/身份漂移一律 fail-closed；该模式始终 `NOT_DEPLOYED`，不产生签名、receipt 或 promotion 权限，也不得把 candidate 手工复制进正式 `runtime/`。

prepare 中的派生器必须字节幂等；例如 save-repair dictionary 仅在结构内容变化时刷新 `generated.at`。重复 prepare 因时间戳制造 diff 属于构建门故障，不能要求维护者提交无语义的时间漂移。

## 精确环境与隔离输出

- 新机器先运行 `tools/bootstrap-runtime-build-env.ps1`；已有环境用 `-VerifyOnly`。若已有实例的精确 MSVC 字节不匹配，bootstrap 不会用旧实例的同名 component ID 冒充锁定 payload，而会走锁定 bootstrapper 的专用 side-by-side 目录；只有工具字节已匹配、仅缺 SDK 时才对该实例执行 `modify`。Windows PowerShell 5.1 下必须逐个输出 `vswhere` 解析到的实例，禁止把顶层 JSON 数组作为单个 `Object[]` 返回后拼接安装路径。正式 producer 每次仍会重跑 `tools/check-runtime-build-env.ps1`。断网复用已有精确匹配 candidate 不需要云端；断网重建则必须预先安装通过锁定门的工具链，并已缓存 NuGet/Cargo 依赖。
- `config/build/runtime-toolchain.lock.json` 锁定 .NET SDK/host、Roslyn/MSBuild、MSVC `cl/link`、Windows SDK `rc`、Rust `rustc/cargo` 及 bootstrapper 入口字节；NuGet 图由 `launcher/packages.lock.json` 固定，其中 PlayerInfo 生产直接引用为 `Svg.Skia 5.1.1`，既有 `SkiaSharp` 保持 `3.119.4`，分发 notice 以 LF canonical byte 同时进入 artifact source 与 candidate payload。Visual Studio 安装器只是尽力补齐组件，不能把会移动的在线 channel 伪装成已固定 payload；最终资格始终以 `cl/link/rc` 的版本与 SHA-256 精确门为准。`.NET` provisioning 脚本也必须使用 `dotnet/install-scripts` 官方仓库的完整 commit URL 并固定 SHA-256，禁止重新使用会因 Authenticode 重签而变字节的 `https://dot.net/v1/dotnet-install.ps1`。
- 当前基线 `cf7-win-x64-2026-07-22` 为 .NET SDK `10.0.300`、Visual Studio Build Tools `17.14.36` / installer `17.14.37502.11` / MSVC toolset `14.44.35207`（cl `19.44.35228.0`、link `14.44.35228.0`）、Windows SDK `10.0.22621.0`、Rust `1.96.0`；精确 SHA 只以 lock JSON 为准。2026-07-22 已核验 `dot.net` 当前脚本的 Microsoft Authenticode 签名有效，去除签名块后与官方 `dotnet/install-scripts@4a37a9f9d1a061fc389d6515100336db4e51710e` 源码逐行相同，因此 provisioning 改用该不可变源码字节。2026-07-19 本机 BT1736 的 bootstrap `-VerifyOnly` 与独立环境门均为 exit `0`。
- producer 清除外部编译/链接/Rust 注入变量；Audio v2 的全部 C/C++/header/adapter 输入按 manifest 固定顺序与 SHA materialize 为 LF，使用固定 response file、SDK library、codec flags、`/pathmap`、`/experimental:deterministic`、`/Brepro`，并在候选中只产生一个静态闭合的 `miniaudio.dll`；Rust 每次 clean + locked；managed publish 不带 PDB/SourceLink。
- promotion preflight 的隐私回归对本地路径做全文排除，对 Windows 用户名和机器名按完整 JSON 字符串值排除；不能用两字符用户名之类的短标识做任意子串匹配，否则会把无关字段误报为机器身份泄漏。
- candidate 默认位于 `tmp/runtime-candidates/v2/c-<identity-prefix>-<builder-hash>-<run-token>/`，完整 build identity / builder label 只存 metadata 与证明，避免目录名把 legacy `MAX_PATH` 撑爆；producer 在编译前后都做 259 字符预算门，已存在目录不覆盖。native / Cargo / MSBuild / `TMP` / `TEMP` 的 job 工作根默认位于环境门规范化后的 machine-local `[IO.Path]::GetTempPath()/cf7-runtime-build-work/job-<token>/`，避免仓库位于 `Program Files (x86)` 等含括号路径时把 CMD 元字符传给 VsDevCmd；`CF7_RUNTIME_WORK_ROOT` 只能覆盖为短的本机绝对目录，卷根、UNC/映射网络盘、reparse point、仓库内/祖先、CMD 元字符或 projected MAX_PATH 超限均 fail-closed。队列 worker 从 request Git bundle 创建隔离 clone；输出按 job 分离，不再共享 `launcher/bin/Release`、Cargo target、MSBuild obj/bin 或临时目录。

## 本地 builder enrollment 与证明

每台本地发布机一次性执行：

```powershell
chcp.com 65001 | Out-Null
$entry = .\tools\register-runtime-builder.ps1 `
  -BuilderId <builder-id> -FaultDomain <physical-fault-domain>
```

脚本在 `Cert:\CurrentUser\My` 创建 3072-bit、不可导出的 RSA 私钥，只把 public certificate/`keyId`/epoch/faultDomain 写到 `tmp/runtime-builder-enrollment/`，**不会自动改 registry**。维护者核对机器与故障域后，才把 entry 合入 `config/build/runtime-builders.v2.json`。私钥不得导出或跨机复制；轮换/撤销通过新 epoch/key 或 `enabled=false` 完成。两台 VM 若共享同一宿主、磁盘或管理员边界，不得宣称两个 faultDomain。

tracked registry 继续保留 `builder-local-a` / `physical-host-a` 与 `builder-local-b` / `physical-host-b` 两张历史公钥；既有 consensus 曾实际采用 `builder-local-a` 的 keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3` / `physical-host-a` 和 GitHub Actions run `30364763726` 的 OIDC builder identity `1BDD1B0F419E0CA384E59986DBC1C9C9195A01CFEAF0838E116A8AD13B9BCEC3` / `github-hosted-windows`。任一单独本地票都仍不构成 quorum，也不授权单机 promotion。

本次 local proof 使用已注册的 `builder-local-a` 3072-bit CurrentUser 不可导出 RSA key：keyId `28DBEAF3761CCF3177FE396596A2557D8A6C9393371CD41DC893FF75A02723B3`、thumbprint `647AFE92BD801518AF25F2A2EE1845E6847C2118`、epoch `1`；未复制或导出私钥。不同故障域的第二票由 GitHub hosted OIDC/Sigstore 提供，双 faultDomain quorum 已满足；dirty staged/unstaged/untracked 工作树仍不能冒充 source commit。

2026-09-02，本机因不存在 `builder-local-a` / `builder-local-b` 对应的不可导出私钥，按一次性 enrollment 新增 `builder-local-c` / `physical-host-c`：keyId `CFB70E2D339ACB25E9B6C2873DF4F1AEEBA8EA75AD23B825724B27FCA70C0B86`、thumbprint `892236D1AE9EB9EFE0624E7C70B739F94A8DA6EF`、epoch `1`。旧私钥没有导出或复制；该登记只有在 tracked registry、immutable request、实际 local proof 与不同 faultDomain 的 GitHub OIDC proof 全部通过后才计入新 quorum，登记本身不构成票或部署。

worker 使用该 CurrentUser certificate 对 canonical payload inventory 做 RS256 签名。验证端只信 tracked registry 中启用且 epoch/faultDomain/certificate 全匹配的 key；旧式自由文本 builder ID 不再计入 v2 quorum。

## immutable request、队列与 CAS

需要跨机器时，所有 worker 指向同一具备原子目录 rename 语义且仅受信维护者可写的 `-QueueRoot`（例如 ACL 收紧且共享名前缀足够短的 SMB 共享）；默认 `tmp/runtime-build-queue` 只适合路径预算允许的单仓本地演练。目录包含 `requests/`、`leases/`、`results/` 与 `cas/candidates/`，不进 Git。QueueRoot 本身也受传统文件 259 字符、目录 247 字符预算约束，因为 CAS final path 保留完整 build identity 与 payload closure，失败记录还允许 128 字符 diagnostic 文件名；request 与 worker（包括 DryRun）都必须在建目录、取证书或编译前 fail-fast，复制时再按实际 payload 路径复核。Windows 本机正式队列应给每趟列车分配经预算检查的专用短根（例如本列车使用 `C:\qf8`），不要在 `%LOCALAPPDATA%` 后继续拼 release-id 深目录；worker 没有 RequestId 过滤，含未 `ready` / `superseded` request 的根不得直接混跑下一列车。队列可以共享，但 worker 的隔离 checkout 默认放在本机短路径 `%LOCALAPPDATA%\CF7\runtime-build-checkouts`；worker 会清除外部 `GIT_INDEX_FILE/GIT_DIR/GIT_WORK_TREE/object/config-count` 上下文，并在 materialize 前固定 local Git `core.autocrlf=false`、`core.longpaths=true`，避免 worker 账户的全局换行策略改变 Worktree identity。checkout/candidate 目录只使用 request、worker 的短哈希并预检 MAX_PATH；包含 legacy 深路径的本机 checkout 通过扩展路径形式安全清理。只用 `-CheckoutRoot` 或 `CF7_RUNTIME_CHECKOUT_ROOT` 覆盖，不要把 checkout 放进共享队列、网络盘或层级很深的项目目录。

最终 tree 已提交时使用 `Treeish`；只有纯本地双 builder 才可用 `Index` snapshot。需要 GitHub cloud builder 时必须先提交，并让 request 与 cloud workflow 使用同一 full commit：

```powershell
$request = .\tools\new-runtime-build-request.ps1 `
  -QueueRoot <queue-root> -SourceKind Treeish -Treeish <full-commit>
$requestId = $request.requestId
```

request 内含完整 frozen `releaseTreeOid`、四域 hash、build identity、source/request commit，以及只覆盖四个 v2 identity domain 精确 Git blob 子树的 `source.bundle`、`bundleTreeOid` 与 bundle SHA；大型无关 tracked asset 仍由 `releaseTreeOid` 绑定，但不会塞进 worker bundle。冻结 stage-0 条目时固定 `core.quotepath=false`，路径字段必须按仓库中的 UTF-8 字面值比对，不能让机器级 Git 配置把中文路径转成 C 风格转义文本。worker clone 后复核 `bundleTreeOid`，相同 tree+policy 幂等复用；tree 已过时就创建新 request 并用 `-SupersedeRequestId <old-id>` 标记旧列车，不删除历史。request 创建还会在同目录写只增不减的 `request.phases.json` 时间戳 sidecar（strict `request.json` schema 不变）；worker 成功发布与 promotion 完成也分别写 `results/_phases/<requestId>/` 下的 `worker-*` / `promotion-*` 阶段计时 sidecar，写失败只告警、从不当构建结果权威。worker 启动时扫描 checkout 根的孤儿 job 目录（有残留但无对应 result/failure 且无存活 lease），只报告、永不删除，供人工清理被强杀的 worker 现场。

Audio Platform v2 仍复用现役 `cf7-runtime-build-request.v2`，不得注入 H1/H2 字段。E2/H2 与 evidence-only E3 `docs/evidence/audio-v2/h2-request-link.json` 只属于 Audio 专项验收包：需要形成 Audio acceptance 时，可由 E3 旁路绑定 request raw SHA/requestId、source tag→S commit/tree、E1 manifest 与 E2 receipt；通用 builder、proof/quorum、`VerifyOnly`、promotion 与 deployment write 均不消费该 link。任何具体 Audio 列车若另有 owner 明示的产品发布取舍，按该次授权记录；不要把它复制成通用构建状态机。

每台已 enrollment 的本地机器运行：

```powershell
.\tools\invoke-runtime-build-worker.ps1 `
  -QueueRoot <queue-root> -WorkerId <builder-id> `
  -CertificateThumbprint <thumbprint> -Once
# 常驻机器可改用 -Watch；状态查询：
.\tools\get-runtime-build-request-status.ps1 `
  -QueueRoot <queue-root> -RequestId $requestId
```

worker 具有单机 mutex、request lease、heartbeat/TTL 与失败记录；抢到 lease 后从 Git bundle 隔离 clone、复核 frozen tree/identity、调用纯 producer、签名并发布结果。构建后 worker 不再对工作树重算四域 identity：纯 producer 已在自身收尾重查输入漂移并把四域 identity 写进 candidate 根部的 `runtime-build-metadata.v2.json`，worker 读取该 metadata 并逐字段与 immutable request 比对（不等即 fail-closed，绝不盲信），签名时经 `New-Cf7RuntimeBuildAttestationV2 -ExpectedIdentity` 传入 request 锚定值——该参数会校验字段形状并重推 `buildIdentityHash` 一致性，只在一致时跳过进程内重算；自定义 `-BuildCommand` 不产出 v2 metadata 时仍回退完整重算。失败时会在删除短 checkout 前把非 reparse、单文件 ≤1 MiB、总计 ≤2 MiB 的 bootstrap diagnostics 写到该 request 的 `_failures` 记录；若 queue I/O 已不可用、失败记录本身无法落盘，worker 只追加固定告警并保留原始构建错误，不能让二次诊断写失败覆盖首因或转成成功。CAS 地址是 `buildIdentityHash/payloadClosureHash`；发布前后都严格复核 candidate，key 对同一 build identity 出现分叉 closure 会作为 equivocation 拒绝。状态退出码固定为 `0=active 全 ready`、`10=pending/empty`、`20=failed/invalid`、`30=只有 superseded`。status 只统计 queue 内本地 X509 result；采用 local + GitHub 时显示 `1/2` 是正常的，最终 combined quorum 由 promotion 把该本地 proof 与外部 verified GitHub proof 一起计算。

推荐把便宜、可离线完成的失败门前移：先取得本地 X509 candidate/proof，再对**该本地 candidate** 跑一次 production policy preflight；这份 preflight receipt 只用于提前暴露 source、CSS、inventory 等政策问题，因为 receipt 绑定具体 `candidateRoot`，不能拿去批准稍后选中的 cloud candidate。preflight 通过后再消耗 GitHub hosted build；cloud proof 到手并选定最终 cloud candidate 后，仍须针对该 cloud candidate 重新签正式 production policy receipt，promotion 只接受后者。这样不削弱双故障域和最终 receipt 约束，同时避免本地即可发现的政策失败拖到云构建之后。

## GitHub hosted 独立故障域

`.github/workflows/runtime-cloud-builder.yml` 提供第二种 producer：只接受人工 `workflow_dispatch`，并在分配 hosted runner 前要求 `github.run_attempt == 1`，且 `github.actor_id` 必须是 `Crazyfs` 的 `91271520` 或 `Flash-Night` 的 `138298913`；失败后重新 dispatch，不能用 rerun 按钮绕过首次运行约束。授权只限制正式发布能力和 Actions 消耗，不构成第二人审批：两名授权发布者中的任一人都可以独立触发。从一次性 source tag dispatch full source commit 后，workflow 在明确的 `windows-2022` / VS 2022 runner family 精确 checkout、配置锁定工具链、运行纯 producer 并验证 v2 candidate；运行时还复核 `RUNNER_ENVIRONMENT`、`RUNNER_OS` 与 `ImageOS=win22`。`windows-2025` 自 2026-06 起已被 GitHub 迁到 VS 2026，不能再承载当前 17.14/v143 锁。checkout 先只取 config/materializer seed，再由 `runtime-inputs.v2.json` 展开四域精确文件集合；禁止为约 9 MiB 的 producer 输入铺开约 4.5 GiB 工作树并挤占 hosted runner 的安装/构建空间。Index 固定文件的存在性必须用 Git object 查询，不能比较受 `core.quotepath` 影响的展示文本；sparse-checkout 的标准输入固定为 UTF-8，因此根目录中文入口在无用户级 Git/终端配置的干净 hosted runner 上也必须可复现 materialize。producer 失败时上传独立 bootstrap/Visual Studio setup diagnostics。独立 `attest` job 仅对 deterministic envelope 调用 `actions/attest`；权限限定为 `id-token: write` / `attestations: write`，使用 GitHub OIDC + Sigstore/SLSA keyless provenance，不保存长期私钥。

`config/build/runtime-github-builder.v2.json` 固定 repository、signer workflow、release source ref、`github-hosted-windows-2022` runner class 与 `github-hosted-windows` faultDomain。日常 release train 使用一次性、单路径段的 `refs/tags/runtime-build-v2/<release-id>`：cloud config、dispatch helper、envelope/attestation verifier 与 admission audit 都拒绝其他命名空间及嵌套 tag，保证 `sourceRef` 必定受 creation + immutability ruleset 保护。tag 必须精确指向 source commit，helper 在 dispatch 前经 GitHub API 解析并 peel annotated tag、拒绝目标不等；workflow 再断言 `GITHUB_REF` 与 `GITHUB_SHA` 分别等于该 tag 和 requested full commit，helper 定位/等待 run 时也要求 `headSha` 相等。由此 GitHub 实际执行的 workflow 字节与被四域政策 hash 绑定的 workflow 字节来自同一 commit，证明同时绑定 tag、full commit 与 tree。远端 creation ruleset 只允许两个授权发布账号创建新 tag，再由无 bypass 的 update/deletion ruleset 冻结已创建 tag；不要删除或移动已经出具证明的 tag。runner 镜像的小版本仍由 GitHub 滚动维护；任何工具字节变化都会被 toolchain lock fail-closed，必须显式轮换基线，不能自动放宽。最短触发、等待、取回与验真命令是：

```powershell
$cloud = .\tools\invoke-runtime-github-build.ps1 -SourceCommitOid <full-commit>
# promotion 使用：-ExternalAttestationPath $cloud.proofPath（candidate 来自本地 X509 CAS）；
# 默认只下载小的 attestation artifact（envelope + Sigstore bundle），本地不落 cloud candidate。

# 需要把云端 candidate 字节也取回本地时显式下载 24MB 级大包（与旧默认行为一致）：
$cloud = .\tools\invoke-runtime-github-build.ps1 `
  -SourceCommitOid <full-commit> -IncludeCandidateArchive
# 此时 promotion 可继续用：-CandidateRoot $cloud.candidateRoot -ExternalAttestationPath $cloud.proofPath

# 进程或网络中断后，复用同一已完成 run；helper 会重验 run / artifact metadata 后续传 .partial：
$cloud = .\tools\invoke-runtime-github-build.ps1 `
  -SourceCommitOid <full-commit> -ResumeRunId <run-id>

# 只用于受控恢复/离线 transport fixture；不会绕过 metadata、双层解压或 Sigstore 验真：
$cloud = .\tools\invoke-runtime-github-build.ps1 `
  -SourceCommitOid <full-commit> -ResumeRunId <run-id> `
  -PreDownloadedArtifactArchive <outer-artifact.zip>
```

helper 只触发固定 workflow，用精确 `run-name`、`headSha` 与 dispatch 前 run ID 集定位本次同 commit run；`-ResumeRunId` 不重新 dispatch，但仍以同一套 identity 门重验指定 run。run 成功后，helper 查询该 run 的官方 Actions artifact metadata，要求唯一精确名称、artifact/run ID、`workflow_run.head_sha`、大小、未过期状态与 `sha256:<64hex>` digest 全部成立；随后用 GitHub CLI token 只向官方 API 换取短期 HTTPS redirect，不记录 token 或 signed URL。外层 ZIP 默认经 HTTPS 流式传输；redirect 探测与数据流永远共用同一条路由：`-GitHubArtifactTransport` / `CF7_GITHUB_ARTIFACT_TRANSPORT` 取 `auto|proxy|direct`（默认 `auto`），显式模式下两者都只用该路由，`auto` 先按 queue 根 `github-artifact-route.v1.json` 的 last-known-good 粘性路由、再 proxy、再 direct 的顺序用 redirect 小请求做 canary，胜者交给数据流并持久化，数据流连续失败即失效重探。连接/首包阶段由独立的短超时看门狗（`-ArtifactConnectTimeoutSeconds`，默认 15s）封顶，数据阶段另有停滞看门狗（`-ArtifactStallTimeoutSeconds`，默认 30s 无任何字节即中断本次尝试），整体 `-ArtifactDownloadTimeoutSeconds` 仍是总上限；失败只保留 `artifact-download/artifact-<id>.zip.partial`，重试发送 `Range`，严格要求 `206`、起止字节、总长和 `Content-Length` 与 metadata 一致，每约 5 MiB 只报告 received/expected 字节数。完整大小和 SHA-256 都通过后才把 partial 改名。

attest job 同时上传两个 signed artifact：大包 `runtime-cloud-builder-<commit>` 保持原有恰好三文件契约（`runtime-candidate.v2.zip`、`runtime-build-envelope.v2.json`、`runtime-build-envelope.v2.sigstore.json`），小包 `runtime-cloud-attestation-<commit>` 恰好两文件（envelope + Sigstore bundle）。helper 默认只取小包：attestation-only 外层 ZIP 走同样的 entry count、路径、大小、link/reparse 与 case-collision 门，然后以 `verify-runtime-github-attestation.ps1 -WithoutCandidateArchive` 验真——该模式信任根为“Sigstore provenance 签的是内嵌逐文件 SHA-256 清单的 deterministic envelope，且云端 attest job 在签名前已逐字节核过 zip==envelope”，本地不再重复 archive 字节断言；其余校验（canonical envelope、config/source/tree 绑定、本地源四域 identity 重算、`gh attestation verify`）全部不变，且该模式显式拒绝 CandidateRoot、Index 源与 release-record replay。candidate 字节与 proof 的最终绑定仍在 promotion 完成：promote 会把同一 proof 对真实本地 candidate root 全量 replay。`-IncludeCandidateArchive` 恢复旧行为：大包必须恰好三文件，内层 candidate 再走原有 safe extractor，最后完整调用 verifier。`-PreDownloadedArtifactArchive` 只是 transport 测试/恢复 seam：仍查询所选 run 的 exact metadata、校验外层大小/digest，并走相同的外层/内层解压和 Sigstore verifier，不能成为离线信任旁路。验证器通过 `gh attestation verify` 同时钉住 repository、workflow、source-ref、commit/tree、envelope/candidate inventory 与全部身份字段，并输出可直接交给 promotion 的 normalized proof。下载 artifact 本身不可信；只有该验证通过后才算 GitHub producer。推荐 quorum 是“一个注册本地 X509 builder + GitHub OIDC builder”；两个注册本地 builder 也可，但必须拥有不同 key 和真实不同 faultDomain。

`test-invoke-runtime-github-build.ps1` 的确定性默认套件通过预下载 seam 覆盖 exact metadata、outer/inner 恶意 ZIP、digest/run/head 负例、恢复选 run 与最终 verifier，并静态钉住 Range/长度/文件模式/进度契约；它不伪造 TLS 或把本地 HTTP 冒充 GitHub/Azure 网络行为。修改下载器后，除离线套件外还要用一个未过期的真实 Actions artifact 执行默认 helper，或受控构造 partial 后跑真实 HTTPS `206` 续传，确认最终 outer SHA-256 与 GitHub metadata 相等。

Actions artifact 只是短期交接介质：unsigned candidate/envelope 保留 1 天；失败 bootstrap diagnostics 保留 7 天；signed candidate/envelope/Sigstore bundle 大包与 attestation-only 小包均保留 7 天。超过 signed 窗口仍未 promotion 时重新 dispatch，不能把 artifact retention 当长期证据仓。promotion 后 tracked v2 consensus 内嵌的验证材料才是仓库审计记录。

## 政策 receipt 与 promotion

在与 request tree 完全一致、tracked 内容干净的工作树中运行。正式 receipt / X509 proof / promotion 链固定由 Windows PowerShell 5.1（`$env:SystemRoot\System32\WindowsPowerShell\v1.0\powershell.exe`）承载：当前 `pwsh` 7 的 `ConvertFrom-Json` 会把 attestation 的 ISO `createdAtUtc` 字符串自动解析成 `DateTime`，若在该宿主内直接调用 promotion，会改变 canonical payload 复算字节并按预期 fail closed。新机经 winget 安装 GitHub CLI 后，先用同一 5.1 进程确认 `Get-Command gh`；若旧父进程尚未继承新的用户级 `PATH`，重启父进程或只给本次发布进程显式补入已核验的安装目录，不复制 token、证明或私钥来绕过环境门。

```powershell
.\tools\prepare-launcher-release-assets.ps1 -ReleaseTreeOid <full-commit>
# 若生成物变化：审阅、提交，再用新 commit 建 request；不得对旧 request 继续发布。
.\tools\validate-launcher-release-policy.ps1 `
  -ReleaseTreeOid $request.releaseTreeOid `
  -CandidateRoot <verified-candidate> `
  -ReceiptPath tmp\runtime-policy-receipts\release.v2.json
```

promotion 自动读取 queue 中匹配 build identity 的本地签名结果，并可追加已 normalized 的 GitHub proof：

```powershell
.\tools\promote-runtime-bundle.ps1 `
  -QueueRoot <queue-root> -RequestId $requestId `
  -CandidateRoot <verified-candidate> `
  -PolicyReceiptPath tmp\runtime-policy-receipts\release.v2.json `
  -ExternalAttestationPath tmp\runtime-cloud-result\verified-github-proof.v2.json
```

若某个验收门只要求证明“同一冻结源已有两张真实 builder 票且闭包一致”，但明确**不授权部署**，必须复用同一 promotion 验证链的 `-VerifyOnly` 出口，不能另写第二套 quorum 解析器：

```powershell
$reportDir = [IO.Path]::GetFullPath('tmp\runtime-promotion-preflight')
New-Item -ItemType Directory -Path $reportDir -Force | Out-Null
.\tools\promote-runtime-bundle.ps1 `
  -QueueRoot <queue-root> -RequestId $requestId `
  -CandidateRoot <verified-candidate> `
  -PolicyReceiptPath tmp\runtime-policy-receipts\release.v2.json `
  -ExternalAttestationPath tmp\runtime-cloud-result\verified-github-proof.v2.json `
  -VerifyOnly `
  -ReportPath (Join-Path $reportDir 'preflight.v2.json')
```

`ReportPath` 必须是项目根内、父目录已存在、目标尚不存在的绝对 long path；其祖先不得是 reparse point/8.3 alias，并且不得落入 `.git`、`config/build`、queue、candidate、live deployment、任一四域输入树或 payload 输入。脚本声明的唯一仓内输出是以 CreateNew 写一份 canonical UTF-8/LF 的 `cf7-runtime-promotion-preflight.v2`：`status=preflight-passed`、`reportCreated=true`，同时明确 `runtimeMutationPerformed=false`、`releaseStateMutationPerformed=false`、`promotionPerformed=false`、`deploymentPerformed=false`、`reusableAsPromotionInput=false`；这不对 Git/gh 自身在仓外的实现缓存作无范围断言。报告绑定 request 五域、candidate manifest/逐文件 payload closure、policy receipt hash、去重后的 signer/faultDomain/proof 摘要；不含时间戳、本机绝对路径、用户名或机器名。写前和写后都会重验 request/bundle、registry、receipt、proof、worktree/tree、candidate closure/manifest 与 live deployment cleanliness；窗口漂移会删除本轮新报告并失败。该报告只能作验收证据，正式 promotion 必须不带 `-VerifyOnly/-ReportPath` 重新执行全链和事务检查。

`-VerifyOnly` 是按需生成验收报告的诊断入口，不是正式 promotion 的前置步骤，且其报告明确不可复用为 promotion 输入。没有独立报告需求时直接执行正式 promotion，避免把同一完整验证无收益地串行跑两遍。

promotion 重新验证 request、精确 `request.releaseTreeOid` worktree、receipt、candidate 与所有证明，要求至少两个不同 signer identity + faultDomain 且五项共同产物字段（前三域、build identity、payload closure）全等。候选 bundle 的首验把逐文件字节核对结果交给 closure 复用，不再对同一 candidate 做第二轮全量哈希；`tmp/runtime-promotions/` 内的 staged 复验与部署后 live 复验以 `-IntegrityOnly` 跳过四域 identity 重算（identity 已由 request 比对与 release-tree diff 钉死，最终 consensus 仍全量重算），逐文件 payload 哈希始终执行。promote 进程内已完成 `gh attestation verify` 的 GitHub wrapper 会写成 `github-preverified-proofs.v1` 伴随文件传给最终 `verify-runtime-consensus.ps1 -PreVerifiedGitHubProofPath`；consensus 对该部分只做记录内嵌字节与已验 wrapper 的结构/绑定比对（不相等或不覆盖即 fail-closed），跳过重复的哈希重算与 Sigstore 调用，独立运行与 post-promotion audit 不带该参数、仍是全量 replay。产品专项的听感、截图、H1/H2/E3 或其他人工验收证据不进入这个通用事务，也不能由 promotion 反向推导。随后脚本才可在 `tmp/runtime-promotions/` 组装 next/previous，事务替换正式 runtime、bootstrap 与 `config/build/runtime-release-consensus.json`。v2 consensus 内嵌 policy receipt 与全部签名/Provenance proof；正式安装完成后同步、有界等待 full-install bootstrap `--verify-only` 并检查真实 exit code，两个 verify 模式同时出现会按 CLI 误用拒绝。任何失败或 120 秒超时都进入自动回滚，previous 保留供人工恢复。

历史 `-AudioV2EmergencyOwnerAuthorizationPath` / `-AcknowledgeAudioV2NonH2Compliant` 参数及三次 H2/E3 复核已从通用 `promote-runtime-bundle.ps1` 退役；对应 validator 与 2026-08-15 receipt 仅作不可变历史审计。现在不需要为“体验证据 pending”建旁路状态机：promotion 始终精确 materialize `request.releaseTreeOid` 并执行同一组 supply-chain 硬门，Audio H2 状态由 Audio 专项验收记录单独表达。

同一分层也适用于变更触发：`config/audio-v2/**`、`docs/contracts/audio-v2/**` 与 `tools/audio-v2/**` 是 qualification/验收闭包，不再进入通用 runtime `policyHash` 或 Windows runtime Audit path filter。真正影响 DLL 的 `launcher/native/**`、`launcher/src/**`、`launcher/native/audio-v2-build-inputs.v1.json`、decoder lock、producer 与第三方源码仍由 artifact/producer 域、native change gate 和 cloud build 全量绑定；因此这项收敛不缩小 binary byte closure。

## CI 事后 Audit 状态机

`.github/workflows/runtime-bundle-integrity.yml` 是事后审计器，不是 required status context。它监听 `main` push、目标为 `main` 的可选 PR，以及获授权发布者的 `workflow_dispatch`；不再监听 `merge_group`，也不申请 Actions/Checks API 权限。静态 `paths` 只覆盖 native gate 的扩展名、基名、固定路径和前缀，再联合 artifact source、producer recipe、toolchain lock 与 payload roots/trees；PlayerInfo 的 assets/notice 由 artifact-source 路径触发，production validator 与 qualification corpus 是少数显式追加的 policy trigger。纯 docs/data/Flash/XFL/Web-only 变化不启动 Windows runner。修改 native gate 或 runtime input descriptor 时必须同步 workflow paths 与回归，避免“配置认为需要审计、GitHub 却未触发”的裂缝。

该 `paths` 过滤只用于常规成本控制。GitHub.com 对 path filter 的生成 diff 只检查前 3,000 个文件；匹配文件落在窗口之外时 workflow 可能不启动，而超过 1,000 个 commit 或 diff 生成超时会让 workflow 总是启动。故超大提交下既可能漏审，也可能让纯内容提交占用 runner；这不会改变正式 release 的 immutable tag / quorum / receipt / promotion 安全边界。官方行为见 [Git diff comparisons](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#git-diff-comparisons)。

job 名为 `audit-native-runtime`。所有事件都以 job-level `github.run_attempt == 1` 在 runner 分配前拒绝 rerun；`workflow_dispatch` 再限制为 `Crazyfs` / `Flash-Night` 的固定 actor ID，并传入 `-ForceDeploymentVerification`，强制当前 HEAD 走 integrity、source identity 与 strict consensus 全链，作为明确的 release-readiness 检查。push/PR 则用于对真实 diff 做低成本被动审计。它不解析外部成功绿灯锚，也不把某次 Actions success 变成服务端准入权。

`tools/classify-runtime-release-state.ps1 -Mode Audit` 先完成 Git path safety 与 native binding 检查，再计算 `deploymentChanged`。路径门拒绝控制字符、Windows 保留/歧义路径、非 NFC、未配对 surrogate 与所有 Unicode plane 的 noncharacter；历史树中的不安全路径仅可按 base 的 mode + object identity 原样存活，任何触碰、重命名或新引入都必须重新通过严格门：

| 状态 | 条件 | 行为 |
|------|------|------|
| `source-ahead` | native/release 输入发生变化，但根 EXE、`runtime/**`、runtime release consensus 与 builder registry 等部署闭包未变 | exit 0；输出 `state=source-ahead mode=Audit deploymentChanged=false`；不运行 verifier，不下载或重哈希 payload。这是正常开发态，不要求即时 promotion |
| deployment unchanged 但路径/绑定非法 | 危险 Git path、symlink/gitlink/mode/case collision，或新增/修改 native 对象未被 descriptor/payload/canonical release control 绑定 | 失败报警；修复边界，不用 descriptor 漏列绕过审计 |
| deployment changed | 根 bootstrap、`runtime/**`、manifest/consensus、builder registry 等正式部署闭包变化 | 运行逐文件 v2 integrity + strict signed consensus；缺少匹配 promotion、证明不足、闭包分叉或 v2 → v1 时失败 |

push 红灯发生时提交已经进入 `main`；workflow 只能报警，不能回滚。PR 事件可以提供提前反馈，但仓库不要求 PR，也不会把该检查设为 merge gate。正式 release 的可靠边界仍是 immutable source tag、local X509 + GitHub OIDC 双生产者、production receipt 与 promotion，而不是一次普通 CI success。

`config/build/main-branch-admission.v2.json` 描述远端仅有的三条不依赖 Actions 的 ruleset：`main-global-ref-integrity-v1` 无 bypass 地禁止删除与 non-fast-forward；`runtime-source-tag-creation-v1` 只允许两个授权发布账号创建 `runtime-build-v2/*`；`runtime-source-tag-immutability-v1` 无 bypass 地禁止已有 source tag update/deletion。没有身份 gate、Require PR、CODEOWNER 或 required status check。所有 write collaborator 都能 fast-forward 直推，包括 native 路径；GitHub Free 公开仓库没有本方案可用的服务端 path push restriction，完整残余风险见 [contribution-workflow.md](contribution-workflow.md)。

## 验证矩阵与诊断

```powershell
.\tools\test-runtime-dev-entry.ps1
.\tools\test-runtime-entry-guardrails.ps1
.\tools\test-runtime-build-v2.ps1
.\tools\test-runtime-release-policy.ps1
.\tools\test-runtime-build-queue.ps1
.\tools\test-runtime-github-attestation.ps1
.\tools\test-invoke-runtime-github-build.ps1
.\tools\test-main-branch-admission.ps1
.\tools\test-runtime-release-state.ps1
.\tools\test-runtime-build-consensus.ps1   # v1 migration guard
.\tools\test-runtime-release-consensus-v2.ps1

# Supplemental；不计入下述 Runtime Lane C 11/11 与 scalar 566
.\tools\test-resolve-runtime-trusted-base.ps1
.\tools\test-submit-contribution.ps1
.\launcher\tests\run_tests.ps1
```

在 historical v1 detached clean source-freeze F `cb38600aae51f5019d09f87c33bd9e67d2b1f511`、Windows PowerShell 5.1 上，当时最近一次完整 Runtime Lane C 复跑为 **11/11 个入口 exit 0**：其中十个会输出 scalar 计数的套件合计 **566** 项，`test-runtime-entry-guardrails.ps1` 另报告 `scripts=3 / unsafeCandidateCases=3`，总耗时 **831.271 秒**。完整 stdout 为 18,488 B / SHA-256 `9BFBDDD521BE54D70E0158CE595C37C54820EE4C66C73C5681AFC56636A524AC`，stderr 为 3,280 B / SHA-256 `F7FA16D6B9927C3B36D6A61FCBC49C00874EAAC41EBFC662F863793E442479C6`。`test-resolve-runtime-trusted-base.ps1` 是单列 supplemental，当时最近独立历史基线为 **9/9**，未在该次 11 项执行中复跑，也不计入 11 个入口或 566。Lane 自身不含环境 bootstrap，也不能单独替代真实双 builder；该 historical v1 轮另由 F request `839C74FD1DF61ACC1DA580041F6FA71CA13A84DF1F43A55237BF9BEEF8648FB2` 的 X509 + GitHub OIDC proof 与 promotion `-VerifyOnly` preflight 闭合非部署 quorum。该结论仍不产生 promotion、正式 runtime 变更或标准入口验收，也不批准当前 v2。

- candidate：`tools/verify-runtime-bundle-v2.ps1 -DeploymentRoot <candidate>`；只审字节闭包才加 `-IntegrityOnly`。
- 提交态由 classifier 按 manifest header 分流；手工 v2 复核用 `tools/verify-runtime-bundle-v2.ps1 -Staged` + `tools/verify-runtime-consensus.ps1 -Staged`。
- `artifactSourceHash` / `producerRecipeHash` / `toolchainLockHash` 不等：构建身份不同，不比较闭包。
- build identity 相同而 `payloadClosureHash` 不同：真实可复现性失败或 signer equivocation，停止 promotion 并逐文件定位，不任选其一。
- `policyHash` 不同但 build identity/closure 相同：建立新 request、重新跑政策 receipt；允许复用已验证 CAS，不重编 payload。
- native 源码变化且部署闭包未变：Audit 应以 `source-ahead` 成功；不要为了“追平源码”在每次 push 后抢构建锁、改 manifest 或立即 promotion。
- 部署闭包变化但无匹配 v2 consensus：事后 Audit 必须失败；不要靠重跑 Actions、手改 hash 或补文档把红灯洗绿。

升级 SDK/编译器是显式维护事件：人工核对官方来源，更新 lock 与 bootstrapper hash，在不同故障域重建并取得新 quorum，同轮更新本文与 Launcher 文档。不得关闭 hash 校验来迁就某台机器的自动 servicing。
