
## 2026-09-08：HeroUnarmed 技能组指定配置机制

- 维护者要求在 九命猫妖初始化 指定虎妙技能组+等级（此前标定表全量硬编码，无配置入口）。
- 新增 `HeroUnarmedSkillTable.buildLearnedTable/buildBattleSkills/buildAirSkillGroup`（`_buildFromConfig` 核心：null=全表；条目{技能名,等级,可选覆盖}，未知名静默跳过，等级→点数，可覆盖 冷却/消耗/限制/距离/优先级/单图一次/释放等级）。
- `九命猫妖初始化` 第3节改为配置区：技能组配置/战技组配置/空中技能组配置 三数组（现均 null=全表），写 `自机.已学技能表/单位战技组/单位空中技能组`。
- `HeroUnarmedSkillBrain`：战技/空中组实时读 自机 字段（重初始化改配置即时生效）；新增 `_applySkillLevel`（条目 `释放等级` 锁定 self.技能等级，否则按单位等级推导）。
- 维护者同日改动：`虎妙AI参数`→`配置AI参数`（全链路已同步）；标定表 霸体注释、飞身踢/咒针 距离min→200/250。

## 2026-09-08 凌晨：小跳/闪现方向 + 击倒脱困 + 空中保底震地 + 出场保护缩短

- **小跳/闪现方向**（HeroUnarmedSkillBrain._release）：机制已核实——技能动画不设输入时默认后跳（位移读 parent.方向，HitUpdater 注释证实）。现显式给输入：贴身→随机 上行/下行（只能上下跳）；未贴身→左行/右行朝目标（前跳拉近）。击倒脱困不走 _release。
- **击倒/倒地脱困**（tick 新 C2 分支 + _knockdownEscapeTick）：倒地||状态==击倒 时立刻 小跳（清方向输入走默认后跳，不跳帧），小跳不可用则闪现；小跳CD=1s 逐tick重试天然节流。依据 RespawnEventComponent：倒地只拦普通技能，小跳无条件可释放。追击平A chaseMeleeTick 补倒地守卫。
- **空中立刻出招**（tick C 分支）：检测加 状态=="空手跳"（升龙拳跳起后空中无韧性，不等浮空标记/动画收尾立刻出招）；_airSkillTick 全CD时保底强制震地（忽略CD，仅MP不足退化平A）。
- **出场保护帧 90→30**：九命猫妖.as 参数默认 + 防具_40+级.xml initParam（XML会覆盖默认，两处同步）。

## 2026-09-08：Z轴索敌范围扩到25（仅空手AI）

- `攻击判定Z` 参数默认 20→25（九命猫妖.as；影响 Engaging 进入/退出×1.3、技能裁决Z窗口、平A门控）。
- `HeroUnarmedAI` 构造中 `new UnitAIData()` 后覆写 `data.zrange = 25`——通用层 UnitAIData.init() 强制 10 的逻辑不动（只影响本 AI 实例；init 仅构造跑一次/updateSelf 不回写/WeaponEvaluator 不在本链路，覆写不会被冲掉）。
- XML initParam 中 出场保护帧 已被维护者删除，.as 默认 15 生效（维护者自行从30又改到15）。

## 2026-09-08：三个真 bug 修复（空中不出招/击倒不小跳/上下跳无效）+ 非战斗地图不接管

- **根因1 追击分支吞掉技能脑**：`HeroUnarmedCombatModule.chase()` 在 `状态==技能/战技` 时提前 return，且目标失效守卫在 brain.tick 之前 → 升龙拳跳起后（此时多半已被击退进 Chasing）和击倒时都进不到技能脑。现改为 `brain.tick` 无条件先跑（空中出招/击倒脱困/喝药都不依赖目标），之后才判"已出招/无目标不移动"。
- **根因2 跳跃方向被逐帧清空**：小跳/闪现的位移帧读 上行/下行/左行/右行，但 `MovementResolver.clearInput`（每AI tick）+ `MoveHelper.alignTick`（每帧）都清这些旗标 → 只设一次必退回默认后跳。新增方向锁 `自机.单位方向锁={上行,下行,左行,右行,截止帧}`，`HeroUnarmedMoveHelper.syncLockedInput(self)` 在 alignTick 开头 + chase/engage 移动输出后持续回写；参数 `跳跃方向锁帧=15`。
- **非战斗地图不再接管**：`九命猫妖初始化` 第4/5节用 `_root.当前为战斗地图 === true` 门控（StageManager 在关卡启动路径置 true、退出/清理置 false，早于刷怪），非战斗地图保留默认佣兵 AI 的跟随行为；Z 对齐每帧任务同样只在战斗地图注册。
- 待办（维护者要求暂缓）：索敌偶发丢失（场上明明有敌人仍搜不到）——Selector 已是每 AI tick 重搜，怀疑 `TargetCacheManager.findValidEnemyForAI` 的敌人缓存未登记该单位，需要时改用 `_root.帧计时器.获取敌人缓存` 兜底。

## 2026-09-08 深夜：释放前转向功能 + 三个异常定位（只定位未改）

- **转向功能**（HeroUnarmedSkillBrain._faceTarget，_release 开头调用）：释放前若 方向 与目标左右不一致 → 调 自机.方向改变("左"/"右")（主角函数，含 _xscale/文字翻转）；`锁定方向==true` 或 `飞行浮空==true` 时该函数内部会 return，我们同样跳过（不强行解锁、不锁定方向）。兜底：无 方向改变 时直改 方向+_xscale。
- **朝向字段权威口径**：`方向`("左"/"右") + `_xscale`，`_root.主角函数.方向改变` 是唯一权威入口；`锁定方向` 是既有关卡。
- **定位结论（未改）**：
  1. **无目标即站桩 = 最大嫌疑**。默认 HeroCombatBehavior 无目标时转 `FollowingHero`（朝主角移动，:245/238），我们的 Selector 无目标只索敌不移动 → 一旦 findValidEnemyForAI 返回 null 就完全静止，且站着不动使距离拉大更难重搜（恶性循环）。→ 好改，Selector 加跟随兜底约 20 行。
  2. **Z 轴死区 → 只移动不攻击/贴身发呆**。攻击判定X=90、攻击判定Z=25；Chasing 的 X 意图门槛是 `absdiff_x > 90*0.6=54`，Z 差>25 时 _decide 与平A 全部过滤 → 出现 absdiff_x≤54 且 absdiff_z∈(25,40] 的死区：wantX=0、wantZ 走不通（边界）时既不动也不出招。Z 对齐接管条件 |Z差|<4×每帧Z速 也覆盖不到。→ 好改：放宽位移/远程技能的 Z 限制或加"Z 未对齐时保持微动"。
  3. **索敌偶发失败**：`findValidEnemyForAI` 第一步 _fn_threat 几乎必败（玩家无 threat 字段），靠兜底 `_fn_validTarget`；兜底走 SortedUnitCache.findNearestWithFilter，快速路径若被 element 占用则邻域扩张受 **searchLimit=30 步 + 安全阀 distanceThreshold=AdaptiveThresholdOptimizer 阈值(30~300)×5** 限制 → 远距离搜不到。默认 AI 同样受限，只是被 FollowingHero 掩盖。→ 改 1 即可大幅缓解；不用动通用层。
- **其他确认**：AI 更新入口 `UpdateEventComponent.onUpdate` 仅 `_warlordActionAiHeld===true` 时冻结（战旗动作战专用）；`wakeupCheck` 依赖 FLA 子元件 思考标签（主角-男/佣兵npc/女仆/学姐 均带此元件，默认 AI 同判定，非我方引入）。

## 2026-09-08 深夜（续）：实施两处修复（仅我们 AI 文件内，通用层/默认AI零改动）

- **① 无目标跟随兜底**（HeroUnarmedBehavior._followWhenNoTarget，selector_enter 无目标分支调用）：索敌失败时朝 `TargetCacheManager.findHero()` 主角移动，对应默认 AI 的 FollowingHero。守卫：无目标跟随==0 禁用、技能/战技/击倒/倒地不动、自己就是主角则跳过。新参数 `无目标跟随=1`、`跟随停止距离=120`、`跟随停止Z=20`。
- **② Z 轴死区放开**（HeroUnarmedSkillBrain._decide + 新 `_isMobileSkill`）：位移/远程/躲避类技能（`类型=="躲避"` 或 `功能` 含"位移"/"远程"）改用放宽窗口 `位移技能Z放宽`（默认60），平A与贴身技能仍严格用 攻击判定Z=25。解决 absdiff_x≤54（X意图门槛 攻击判定X*0.6）且 absdiff_z∈(25,60] 时"既走不到位也不出招"的干追死区。
- **参数口径提醒**：九命猫妖.as 的参数覆盖走 `Number()` 转换，**只支持数值**——开关类参数用 1/0 表示（如 无目标跟随），不要写 true/false。
- **未改（待维护者定夺）**：Chasing 状态没有面朝目标（只有 Engaging 有 `方向改变`），追击时可能背对目标跑；维护者明确要求"没说的功能不要乱加"，故仅记录未改。

## 2026-09-08 凌晨：搓招技能接入 AI + 平A 失效根因修复

- **搓招机制（已核实全链路）**：玩家搓招 = InputCommand/CommandDFA 按键序列（波动拳↓↘A、诛杀步Shift→、燃烧指节→B、能量喷泉↓B、连环踢↑A）→ `_root.技能函数.使用XXX()` 校验被动前置 → 终点统一是 `_root.空手攻击路由.空手攻击标签跳转(unit, 招式名)`（引擎_fs_空手攻击路由.as:115，非主角-男走 man.gotoAndPlay）。**AI 无输入序列，直接调这个终点即可触发同一批招式元件**。
- **新增 5 个搓招条目**（HeroUnarmedSkillTable，_type:"搓招"，写进 已学技能表）：燃烧指节(200-400，参考飞身踢缩20%/被动升龙拳Lv1)、连环踢(50-180/拳脚攻击Lv2)、波动拳(250-500，参考气动波/内力爆发Lv2)、能量喷泉(100-220/裂地拳Lv1+MP≥10%)、诛杀步(200-300 位移突进/拳脚攻击Lv1)。
- **解锁校验复刻**：新增 `_passiveReady(sk)`——玩家侧前置校验在 单位函数_雾人_空手搓招指令.as（内力爆发Lv2/拳脚攻击Lv2/升龙拳Lv1/裂地拳Lv1+10%MP/启用），AI 不走输入层所以必须自行把关，被动缺失或未启用则该条目不进候选。
- **平A 几乎不触发的真根因**：平A 不经过 `_release`，`_currentSkillTier` 保持 B 分支复位的 **-1**；下一 tick 状态变"技能"走 `_tryInterrupt`，其换招条件 `(_currentSkillTier < 0 || ...)` 对 -1 恒真 → 任意 tier0 保命技（小跳/闪现/震地）立刻顶掉刚起步的平A。修复：平A 时置 `自机.单位平A中=true`，`_tryInterrupt` 开头守卫 return；tick B 分支（技能结束复位）清标记。三处平A落点（G 保底/I 兜底/chaseMeleeTick）均设标记 + 补 `_faceTarget()`。
- **平A保底概率 0.25→0.15**（性能考虑），两处同步：`九命猫妖.as` 参数默认 + `防具_40+级.xml` initParam（XML 会覆盖 .as）。
- 参数/字段复制链路已核实：`getLearnedTableCopy` 与 `_buildFromConfig` 均 `for(k in src)` 全字段浅拷贝，新增的 _type/被动/被动等级/MP比例 不会丢。

## 2026-09-08 凌晨（续）：击倒小跳时序错误修复

- **现象**：击倒时不小跳，落地倒地后才开始小跳；且小跳结束后反复高频小跳。
- **根因1（时序）**：tick 里击倒判定(C2)排在**空中判定(C)之后**。但破韧/被击飞时「击倒+浮空」是常态——`ImpactStateHandler.as:206-214` 的 AIRBORNE_DOWN 分支：浮空或倒地的目标被再击中会 `状态改变("击倒")`。于是 浮空==true 先命中 C 分支 `_airSkillTick` 后 return，C2 永远走不到；等落地 浮空=false、倒地=true 才轮到 C2 → 表现为"倒地后才小跳"。
  - 修复：C2 提到 C 之前；击倒检测用 `st.indexOf("击倒") > -1`（升空函数.as:176 同口径，防状态名带后缀），并加 `st != null` 守卫。
- **根因2（高频）**：原来每 tick 只要满足 击倒||倒地 就尝试脱困，倒地持续期间小跳 CD(1s) 一到就再放 → 观感"无限小跳"。
  - 修复：`_knockdownEscapeTick` 改返回 Boolean(是否放出成功)；新增 `_wasKnockdown` 边缘检测 + `自机.单位击倒已脱困` 单次标记——新一次击倒重置标记，CD 未好则下 tick 继续重试（保证"立刻"），一旦放出即打标记，本次击倒不再触发。
- **需求澄清（维护者）**：**倒地不需要 AI 小跳**（倒地有另一套起身方式，无需 AI 干预）；**只有击倒才需要小跳**。故 C2 已移除 `self.倒地 == true` 分支，倒地回归不干预（F 分支"倒地不出招"保留）。

## 2026-09-08 凌晨（续2）：升龙拳起跳后空中技能不触发 —— A 分支吃掉整段空中

- **现象**：升龙拳跳起后 踩人/地震/震地 从不在空中触发（落地后才补地面版）。
- **根因（与击倒小跳是不同原因）**：主动跳跃同样会置 `浮空=true`（PlayerTemplateUnitFixture.as:309，落地在 :96 置 false），但**从起跳到落地 状态 一直保持"技能"**。tick 的 A 分支（`st=="技能"||"战技"`）排在 C(空中) 之前，整段空中都被 A 分支吃掉走 `_tryInterrupt`，C 分支的 `st=="空手跳"` 永远轮不到（AI 不按键，走不了 JumpDeriveAction 的 A+B 派生路径，也就不会进"空手跳"状态）。
- **修复**：A 分支内加"空中优先"——技能播放期间只要 `浮空||飞行浮空`，立刻 `_airSkillTick()` 改出空中技能组；新增 `_isCurrentAirSkill()`（_currentSkillName 命中空中组）防反复换招，已出过则静默等落地，且空中一律不再走 `_tryInterrupt`。
- **优先级顺序最终形态**：A(技能中→先看浮空，空中出招) → B(复位) → C2(击倒脱困，indexOf 匹配，倒地不干预) → C(空中/空手跳) → D 出场保护 → E 喝药 → F 倒地不出招 → G 平A保底15% → H 技能裁决 → I 平A兜底。
- 击倒(状态="击倒") 与 A 分支(状态="技能") 互斥，无冲突；击倒+浮空 走 C2 脱困（正确，优先于空中出招）。

## 2026-09-08 凌晨（续3）：铁布衫/觉醒霸体 不可打断（表字段驱动）

- **需求**：当前技能是 铁布衫/觉醒霸体 时任何情况下都不打断，等执行完；被打断会导致效果未结算且因 单图一次 标记已写而本图永久失去该技能。实现方式由维护者指定：**技能表条目加 `不可打断:true` 字段**（非硬编码技能名）。
- **实现**：
  - `HeroUnarmedSkillTable`：觉醒霸体 / 铁布衫 两条加 `不可打断:true`（其余单图一次技能 兴奋剂/聚气 本次未加，需要可照加）。
  - `HeroUnarmedSkillBrain` 新增 `_currentSkill:Object`（当前技能条目引用，_release 与 _airSkillTick 释放后均赋值，B 分支复位时清 null）+ `_isNoInterruptSkill()`（读 `_currentSkill.不可打断 == true`）。
  - `_tryInterrupt()` 开头守卫 return（优先级最高的守卫，早于 单位平A中 之后、所有换招判定之前）。
  - A 分支"空中优先"同样跳过：`if (!_isNoInterruptSkill() && !_isCurrentAirSkill()) _airSkillTick(frame);` —— 防止增益技起手时若带浮空被换招。
  - B 分支（技能结束复位）加异常早退保护：不可打断技能且 `frame - _skillStartFrame < 30帧` 就脱离技能状态 → `delete 自机.单位已用永久技能[技能名]` 撤销单图一次标记，允许后续重触发（正常打完不受影响，仍只一次）。
- **根因回顾**：这两个技能 优先级=3，高血时 minTier=0、`pick.优先级 < _currentSkillTier` 对任意 tier0 保命技（小跳/闪现/震地）恒真 → 起手就被顶掉。
- **注意**：30 帧阈值是"异常早退"判据，若某不可打断技能动画本身短于 1 秒会被误判为打断而允许重放（当前两个都是长演出增益技，无此风险）。

## 2026-09-08 凌晨（续4）：收尾 —— 稳健性修正 + 全量自检 + 文档同步

- `单位已用永久技能[名]` 撤销标记由 `delete` 改为 `= false`（AS2 的 delete 对动态属性行为不稳定，判定口径是 `== true`）。
- **编译方式确认**：`scripts/类定义/` 不在 `asLoaderManifest` 的 include 清单中，与既有 `HeroCombatBehavior.as` 同走 Flash 类路径自动编译 → 新增的 6 个 `HeroUnarmed*.as` 无需注册清单，import 即可（此前担心的"类没进编译"不成立）。
- **全量静态自检通过**：6 个类全限定类名 `org.flashNight.arki.unit.UnitAI.behavior.HeroUnarmedXxx` 与文件路径一致；括号平衡（HeroUnarmedCombatModule parens=-2 系注释 `1) 2)` 所致，非语法问题）。
- 开发文档 `docs/人形怪AI定制-空手专用AI开发文档-2026-09-07.md` 追加「2026-09-08 实机反馈修复合集」：A 搓招接入 / B 平A被打断根因 / C 击倒脱困时序+高频 / D 空中出招 / E 不可打断（表字段驱动）/ F 其他同期修复 / G 编译与验收要点。

## 2026-09-08 凌晨（续5）：交战期 Z 抖动修复 + 攻击判定Z 25→20

- **现象**：不再贴线浮动，但会在离目标 Z 一段距离处上下反复走动、走进范围又走出去。
- **根因**：`engage()` 完全不输出 Z 移动（只有转向+brain.tick），而进入交战阈值是 `攻击判定Z`(25)、退出是 ×1.3(32.5) → 单位卡在阈值边缘：目标稍一移动 Z 差超 32.5 就被判出交战，Chasing 又走回 ≤25 再进交战 → 在 25~32.5 带内反复进出，观感即"离目标一段距离反复上下走"。不是浮点/取整问题。
- **修复**：
  1. `engage()` 加交战期 Z 微调：非技能/战技状态下，`absdiff_z > 交战Z死区(默认5)` 时继续输出 wantZ（或走 Z 对齐接管），贴到死区才停 —— 走进范围后稳定待在范围内。engage_enter 的"停用接管"注释同步更新。
  2. `攻击判定Z` 25→20（九命猫妖.as + HeroUnarmedAI 的 data.zrange 覆写 25→20，维护者要求砍宽）。
  3. 新参数 `交战Z死区: 5`（进入交战宽松 / 进入后贴紧，两套阈值分离）。
- 顺带：chase() 跑步切换的硬编码 `absdiff_z <= 20` 与新 攻击判定Z=20 正好一致，无需改。

## 2026-09-08 凌晨（续6）：Chasing 面朝目标 + 连续小跳闪现根因修复

- **Chasing 面朝目标**（维护者批准）：照抄默认 AI HeroCombatModule:229 写法，chase() 在 updateTarget 后 `方向改变("左"/"右")`；`方向改变` 内部有 锁定方向/飞行浮空 守卫，不会强行翻转。
- **连续小跳闪现根因（比"脱困标记没清理"更深）**：脱困标记本身正确（一次击倒只脱困一次，_wasKnockdown 边缘检测+B分支不误复位），但它只挡 C2 分支。真正原因是小跳/闪现 表内 CD 仅 1/2 秒+消耗 5/10MP 极低成本，且 `_decide` 里躲避类 受威胁 tier=0 / 不受威胁 tier=2 两条路都进常态裁决池 → 受威胁/低血状态每 tick 都有 1/3 概率再抽中小跳/闪现；低血时 `_tryInterrupt` 30帧后 `pick.优先级 <= _currentSkillTier`(0≤0 恒真) 还会把播放中的小跳"打断"换成同层随机（可能又是小跳）。
- **修复：位移使用间隔锁** `_dodgeLockUntil`（getTimer 时间戳）：`_release` 释放小跳/闪现后锁 位移使用间隔(默认3)秒；`_decide` 候选过滤跳过这两技。击倒脱困 `_knockdownEscapeTick` 直接 `_usableSkill` 不走 `_decide`，不受锁限制（保命优先）。参数 `位移使用间隔: 3`（九命猫妖.as）。
- **状态清理确认（无需改）**：方向锁 15帧窗口 syncLockedInput 自动过期清 null；B 分支复位 _currentSkillName/_currentSkill/单位平A中；击倒标记 下次击倒边缘检测自动重置。
- FLA 侧（维护者自行改）：小跳动画位移帧原读 Key.isDown(上键/下键/左键/右键)，AI 无键盘 → 需改为读 AI 写的 上行/下行/左行/右行 旗标（贴身=只随机上下、非贴身=上下/前、击倒=全false走默认后跳）。映射关系已告知维护者。

## 2026-09-08 凌晨（续7）：Z/Y 偏离上下抖动根因修复 + AI喝药接回血包上限

- **Z/Y 偏离+上下抖动根因（无需禁用升龙拳）**：跳跃/技能浮空期间 `_y` 由 空中控制器 独占积分（高度=Z轴坐标-_y，落地回写 _y=Z轴坐标，见 空中控制器.as:304-318）。而 ①`chase()` 在 状态==技能/战技 早退时**不清 虎妙Z对齐**（残留上一次的接管）；②`alignTick` 无浮空/技能守卫，每33ms `Mover.move2D` 执行 `_y=(Z轴坐标+=vy)` 把空中单位拽回地面，下一帧重力积分又拉起 → 每帧上下抖动；且 Z轴坐标 被持续拖走 → 落地后 Z/Y 偏离。升龙拳/小跳只是触发载体（跳起必现），禁技能不治本。
- **修复**：`HeroUnarmedCombatModule.chase()` 两个早退分支（技能/战技、无目标）补 `虎妙Z对齐=null`；`HeroUnarmedMoveHelper.alignTick()` 在 syncLockedInput 之后加守卫：浮空/飞行浮空/倒地/技能/战技 任一为真 → 清接管并 return（技能位移帧、方向锁回写均不受影响）。
- **AI喝药接回血包上限**：发现 tickHeal 直接调 佣兵使用血包（RegenerationCore 纯回复），绕过了 佣兵ai.as:69/90 的 血包数量>0 检查与扣减 → 虎妙等于无限喝。已补 `if(!(self.血包数量>0)) return;` + 喝时 `self.血包数量--`。**血包数量=3 的定义本身从未被我改过**（git log -L 确认，玩家模板迁移.as:2152，历史上只动过旁边 韧性系数/命中率 行）。
- **续7补完**：`engage()` 的 技能/战技 早退分支同款补 `虎妙Z对齐=null`。至此 `虎妙Z对齐` 的全部设置点（chase/engage）早退均清理，脱离/待机路径原本就清；`_followWhenNoTarget` 确认不设接管且跳过 技能/战技/击倒/倒地。alignTick 守卫作为最后防线。

## 2026-09-08 凌晨（续8）：贴边上下抖动/到不了边缘 根因修复

- **现象**：AI 走不到地图最上下边缘，离边缘一段距离就持续上下抖。
- **根因**：`applyBoundaryAwareMovement`（MovementResolver，他人模块约定不动）的脱困三级逃脱遇"地图边界"这种绕不开的阻挡时：压边→noProgress/blockAhead→选 altZ 反向逃离（24帧窗口）→窗口结束又朝边界压→再逃 → 无限振荡。地面行走本身到 Ymin/Ymax 是可行的（Mover 有硬钳制），是脱困逻辑不让走。
- **修法（全部在自有模块，MovementResolver/Mover 零改动）**：新增 `HeroUnarmedMoveHelper.clampZIntent(self, wantZ)`——意图朝边界时 probe=min(每帧Z速度,剩余边界距离)（端点恒在地图矩形内，贴边仍可走到最后一步），`Mover.isDirectionWalkable` 不通过 → 归零，从源头不给脱困逻辑喂被挡方向。接入点：①`chase()` wantZ 计算后；②`engage()` Z微调；③`HeroUnarmedBehavior._followWhenNoTarget`（新增 import）。另 `alignTick` move2D 前加行走复检，走不动即结束接管（防每33ms撞边被 resolveCollision 挤压抖动）。
- 语义：目标/主角在边界外或贴边时，AI 走到可行走的最边缘位置即停，不再振荡。
- **回退 chase() 追击期主动转向**：上轮加的"照抄默认AI"的 方向改变 在实机导致疯狂来回翻转（AI数据4帧陈旧+移动旗标每tick清写，与行走状态机朝向互相拉扯），已整体移除，仅留注释；engage() 的转向为原有逻辑保留。

## 2026-09-08 凌晨（续9）：升龙拳偶发飞出屏幕——仅诊断未修

- 升力机制：升龙拳浮空初始化(玩家技能.as:1737 控制器版)起跳，上升速度由容器帧脚本逐帧写 垂直速度，重力由空中控制器 skillFloat 统一积分，贴地起跳的峰值是设计内、有界的。
- **放大器A（"偶尔"的主因）**：AI 会在已浮空上升期（小跳空中/被击飞上行）出招；升龙拳浮空初始化的守卫 `!技能浮空&&!浮空` 在已浮空时跳过重置，但技能容器帧仍会把强上行 垂直速度 写进进行中的浮空物理 → 从"已有上行速度+已有高度"起步再冲一轮，峰值远超设计 → 飞出屏幕。
- **放大器B**：A分支（技能中+浮空）因 升龙拳∉空中组 每 tick 调 _airSkillTick → 上升途中就被换招成 震地/地震/踩人；新技能帧若再次写上行速度/重置浮空，冲量叠加。
- **屏幕外震地循环**：C分支空中每 tick _airSkillTick，三技全 CD 时**无视 CD 强制 震地**（设计本意"空中无韧性保底"）→ 高空反复重放震地直到某次落地——对应"屏幕外震一会才回来"。
- 疑似共因：_applySkillLevel 把 技能等级 拉满(10)，若升龙拳上升速度随等级缩放则每次都偏高（需 FLA 侧确认）。
- 侯选修法（待维护者拍板）：①升龙拳仅贴地可出（A/C分支过滤）；②A分支上升期(垂直速度<0)不换招；③强制震地兜底加最小间隔或限次。

## 项链斩杀(slay)修复 (2026-09-08)
- 根因：非武器槽位（颈部装备等）的 `<slay>` 在 DressupInitializer.updateProperty 的 else 分支被丢弃；slay 仅对 weaponPrefixKeys（长枪/手枪/手枪2/刀/手部/手雷）投影为 `<prefix>斩杀`。
- 修复：新增 `基础斩杀` 概念——updateProperties 重置、else 分支累积非武器 slay；四个消费点（玩家模板迁移 this.斩杀、ShootInitCore killValue、SkillAttributeCore 兵器/空手技能）改为 基础斩杀+模式斩杀 叠加，全部用三元/isNaN 守卫防 undefined→NaN。
- 注意：射击路径不读 this.斩杀，直接读 `weaponType+"斩杀"`；技能路径直读 `兵器斩杀`/`空手斩杀`，三处都要单独叠加。BOM 已复核。

## 2026-09-08 傍晚（续10）：升龙拳贴地限定 + 忽略Z轴属性（增益/兽王崩拳）

- **新属性（HeroUnarmedSkillTable）**：`忽略Z轴:true` = 释放判定跳过 Z 轴距离检查、X 轴距离照旧；挂 能量盾/觉醒霸体/兴奋剂/铁布衫/聚气 + 兽王崩拳（Z 轴无关突进）。`贴地限定:true` = 仅贴地可释放，挂 升龙拳。两者均为独立布尔位，与 空中限定 并列，配置数组可直接覆盖（_buildFromConfig 拷贝任意键）。
- **升龙拳飞出屏幕修复**：`_decide` 加 `if (sk.贴地限定 == true && !_isGrounded()) continue;`——只在贴地时走"地面起跳"分支（升龙拳初始化会复位残留标记并给 起跳速度），杜绝空中/起跳余量期起跳导致的冲量叠加。强制震地兜底**保持不限次**（限次可能回不到地面）。
- **0.5 容差（其他开发提到"调整时序后隐性行为失效"）对本 AI 的适用性**：判据是 `高度 = Z轴坐标 - _y`，我们两处已按同口径处理——①新增 `_isGrounded()`（`_y >= Z轴坐标 - 0.5`，与 升龙拳浮空初始化:1742 完全同口径，且**只看高度不看 浮空标记**，因为地面触发时标记可能残留 true）；②`alignTick` 空中守卫补 `inAir = _y < Z轴坐标 - 0.5`，覆盖浮空标记"置位前/清除后"的一帧空窗（一帧 move2D 就足以把 _y 拽回地面）。alignTick 的 Z 差死区原本就是 0.5。
- 结论：该容差思路对本项目**有效且必要**，已全部落到高度相关判定；其余位置（SkillBrain C/A 分支的空中判定）仍用 浮空 标记，避免行走动画轻微起伏误判。

## 2026-09-08 晚：编译方式已沉淀到 .workbuddy/memory/MEMORY.md「怎么编译」章节

## 2026-09-08 晚（续11）：平A窗口锁 + 中距技能提层（诛杀步/能量喷泉罕见根因）

- **平A频率极低的真根因（不是概率问题）**：`_root.主角函数.攻击`（玩家模板迁移.as:967）把 状态 置为 **攻击模式+"攻击"（"空手攻击"）**，不是"技能"。而 tick 的 B 分支在 `状态 != 技能/战技` 时**每 tick 无条件 `self.单位平A中 = false`** → 平A触发后下一 tick 标记即被清，实际只持续 1 tick（只打出第一下，连段不推进）。
- **修复：平A窗口**。①B 分支不再无条件复位；②新增 B2（位于 B 之后、C2 之前）`if (单位平A中 && _sustainNormalAttack(frame)) { tickHeal(frame); return; }` —— 逐 tick 续写 动作A 并跳过技能裁决（H）；③新增 `_startNormalAttack`（G 骰子 / I 兜底共用）、`_sustainNormalAttack`（中断判据）；④`chaseMeleeTick` 同款续写。窗口内仍调 tickHeal（喝药优先级高于平A）。
- **中断判据**：状态==技能/战技、击倒(indexOf)、浮空/飞行浮空/倒地、出场保护帧内、**低血且受弹道威胁**（高血受威胁不中断，否则弹幕图平A永远打不出）、目标脱离射程、窗口到期（平A窗口帧，默认30）。
- **参数**：`平A窗口帧: 30`（新）、`平A保底概率 0.15 → 0.3`、`中距技能提层: 1`（新）。
- **诛杀步/能量喷泉（及所有中距远程）罕见的根因**：不是不能触发，是被 tier 压死。分层裁决取最小层号，而 **兽王崩拳（优先级1，距离 0~500，刚加的 忽略Z轴 让它全程有效，CD8）** 在所有距离段都独占 tier1 → tier2 的 8 个中距技只能在它 CD 的 8 秒窗口里 8 选 1，单招平均几十秒一次，观感即"从不触发"。
- **修复：中距技能提层**（`_decide`）：脱离贴身带（`absdiff_x > 攻击判定X`）且非增益类且 `距离min >= 攻击判定X` 的技能 tier 提到 1（诛杀步/能量喷泉/波动拳/气动波/飞身踢/咒针/燃烧指节）。贴身带内完全不生效，贴身行为零变化。可用 `中距技能提层:0` 关闭。
- 另核实：`限制`/`点数` 两个表字段在技能脑里**从未被消费**（死字段）；`空手攻击标签跳转(unit,"能量喷泉")` 与玩家侧 `使用能量喷泉` 的落点完全一致（搓招指令.as:165/171），释放路径正确。

## 2026-09-08 晚（续12）：扭转乾坤 不可打断（按 单位.技能名 判定）

- 背景：扭转乾坤**不在本脑技能表内**（不参与裁决），由 九命猫妖周期 在 `hp<=1` 触发复活时
  `_root.技能路由.技能标签跳转_旧(自机,"扭转乾坤")`（九命猫妖.as:228）直接路由 → 进 状态=="技能" 时
  `_currentSkill == null`，原 `_isNoInterruptSkill()` 只看表条目 `不可打断`，认不出来 → 会被 _tryInterrupt 换招打断。
- 判定依据：`_root.技能路由.技能标签跳转_旧` 在 状态改变("技能") 前写 `unit.技能名 = skillName`（引擎_fs_技能路由.as:29）
  → **可用 单位.技能名 判断当前技能**（对外部触发的技能是唯一可用信号）。
- 改动（全在 HeroUnarmedSkillBrain.as）：
  1. 新增 `private static var 外部不可打断技能:Array = ["扭转乾坤"]`（写法先例：HeroUnarmedSkillTable.空中技能组、
     全库 private static var 2471 处，AS2 编译无风险）；
  2. `_isNoInterruptSkill()` 扩展：表内 `不可打断:true` **或** `单位.技能名` 命中外部名单 → true。
     自动覆盖 `_tryInterrupt`（已有该守卫）与 A 分支的 `_airSkillTick`（`!_isNoInterruptSkill()` 守卫），
     即空中也不会把它换成震地；
  3. B 分支"单图一次 撤销"补 `_currentSkillName != null` 守卫：外部技能无表条目，
     否则会写 `单位已用永久技能[null] = false` 脏表。

## 2026-09-08 晚（续13）：外部不可打断 方案选型（技能名 vs 改九命猫妖周期写 _currentSkill）——维持技能名方案

- 维护者提议：在 九命猫妖周期 释放扭转乾坤时把 `_currentSkill` 改成扭转乾坤。评估后**维持按 单位.技能名 判定**，理由：
  1. `_currentSkill` 是技能脑实例的 **private 字段**，周期函数只拿得到 `自机`，拿不到脑实例，实际只能新增单位字段（如 自机.虎妙外部当前技能）→ 仍是"读单位字段"，只是换了个名字；
  2. 新增字段**必须手动清理**（技能被打断/死亡/换图漏清 → 虎妙永久不可打断，比现在的问题严重得多），而 `技能名` 由路由层每次路由覆写、无需我们维护；
  3. 只覆盖那一个调用点；`技能名` 覆盖所有外部路由路径（装备/其他系统/将来的）；
  4. 装备代码要反向依赖 AI 内部字段名与语义，耦合更重。
- **可靠性已核实**：`技能路由:29` / `战技路由:35` / `AIEnvironment.routeSkill`（委托技能路由）**三条路径都写 unit.技能名**；搓招与平A 走 `空手攻击路由`，只写 `空手攻击名`、状态是 `STATE_BAREHAND="空手攻击"`（永不为"技能"）→ 不存在陈旧 技能名 误判窗口。
- **加固**：`_isNoInterruptSkill` 的外部名单分支加 `状态=="技能"||"战技"` 守卫 —— 技能名 技能结束后不清空，限定状态可避免 B 分支（状态已离开技能时也调用本函数）误判。至此规则精确为"正在播放技能/战技 且 技能名命中外部不可打断名单"。

## 2026-09-08 晚（续14）：删除"中距技能提层"，改回 table 优先级参数（维护者质疑后采纳）

- 维护者提问：改技能优先级不就是改 table 里的 优先级 吗，中距技能提层 有必要吗？**答：没必要，已删除。**
- 论证：提层条件是 `absdiff_x > 攻击判定X` **且** `sk.距离min >= 攻击判定X`；而候选过滤已保证 `absdiff_x >= 距离min`，
  且这批技能 距离min 最小 100 > 攻击判定X(90) → **`farFromMelee` 恒真，是死条件**。等价于直接把 优先级 2 改成 1。
- 关键原理（已写进表头注释）：裁决取"候选中最小层号"那一层，**同层才是竞争对手**；而距离过滤先于分层执行，
  中距/远程（距离min 100~250）在贴身带根本不进候选、贴身招在中距也不进候选 → 两个距离带互不挤占，
  所以中距/远程标 1 与贴身招同层是安全的；留在 2 反而会被 兽王崩拳（距离min=0 全距离）在自己的距离带里独吞候选。
- 改动：①`_decide` 删除 farFromMelee/meleeGate 与提层块；②删除参数 `中距技能提层`；
  ③table 优先级 2→1：气动波/燃烧指节/波动拳/能量喷泉/诛杀步/飞身踢/咒针（按技能名正则替换，避开并发编辑改过的 距离max）；
  ④分组注释改为"优先级1：输出（贴身段+中距/远程段）"、"优先级2：次要输出（升龙拳/一瞬千击）"。
- 唯一行为差异：`_tryInterrupt` 读 `sk.优先级`（提层只改局部 tier 不改条目），改 1 后中距技能打断当前 tier2 技能稍更积极。
- 注意：维护者在会话期间并发编辑过本表（兽王崩拳 距离max 500→395、燃烧指节 350→300、能量喷泉 320→280），
  以后改表优先用"按技能名匹配"而非整行精确匹配。

## 2026-09-08 夜（续15）：升龙拳+踩人飞天 = 起跳当帧被空中换招（复用 不可打断 位修复）

- **确认用户猜想**：踩人确实没等升龙拳结束就触发。链路：升龙拳释放（贴地限定，起跳置 浮空=true、
  状态 仍为"技能"）→ 下一 tick 走 tick 的 **A 分支浮空段**（SkillBrain:92）→ `_isNoInterruptSkill()`=false
  且 升龙拳∉空中组{震地,地震,踩人} → `_airSkillTick` 立即随机换成 震地/地震/踩人
  → 新技能再做一次浮空初始化/写上行速度，叠进进行中的上升物理 → 冲量叠加 → 飞出屏幕。
  即"升龙拳刚起跳就被替换"，不是落地后才触发。
- **修法：复用现成的 `不可打断:true` 位（不新增属性）**，升龙拳条目加此标记。
  - A 分支 `!_isNoInterruptSkill()` → 空中不再换招，升龙拳完整播完自己落地；
  - `_tryInterrupt` 也有同一守卫 → 起跳前摇/落地收招不被打断。
  - **代价极小**：升龙拳起跳后立刻浮空，本来走 A 分支浮空段（根本不调 _tryInterrupt），
    所以"不可被打断"实际只损失落地收招的 1~2 帧，低血保命链路基本不受影响。
- **顺手加固**：B 分支"单图一次标记撤销"补 `_currentSkill != null && _currentSkill.单图一次 == true`
  守卫 —— 升龙拳标了 不可打断 但不是 单图一次，否则会写 单位已用永久技能["升龙拳"]=false 脏键。
  （该守卫同时天然排除外部技能扭转乾坤：其 _currentSkill 为 null。）
- 表头注释补 `不可打断` 字段说明（两类用途：尾部结算技 / 跳跃技防空中换招）。
- 待观察：一瞬千击、燃烧指节同为"位移(突进)输出"，若实机也有飞天现象，同样加 `不可打断:true` 即可。
- 提醒（未改）：本表 波动拳/诛杀步 的 优先级 现为 2（上轮讨论过中距/远程应标 1），疑似被并发编辑改回，需维护者确认是否有意。
- **续15 补充（升龙拳不落地的事实核查，无需改逻辑）**：维护者指出"升龙拳播完不会落地，技能结束时单位还在天上"。
  核查结论：**不影响 不可打断 改动**。作用域仅"技能播放期间"——tick B 分支（技能已结束）无条件复位
  `_currentSkill = null` / `_currentSkillName = null`，`_isNoInterruptSkill()` 的表内分支随即失效、
  外部名单分支又限定 状态=="技能"/"战技" → 技能一结束守卫立刻解除；且 B 分支不 return，
  同一 tick 继续落到 C 分支（浮空=true）→ `_airSkillTick` 照常接 震地/地震/踩人（三技全 CD 时强制震地兜底）。
  即"升龙拳结束当帧就接下一个技能，不会干等落地"，与维护者期望一致。已在 A 分支注释写明该作用域边界。

## 2026-09-08 夜（续16）：击倒脱困完全剥离，改用原生 击倒时小跳

- **回旋踢后空手站立 bug（用户自行修复）**：根因是 FLA 起身回旋踢路径无人清 倒地 标记——
  倒地=true 会拦掉 AI 全部出招分支（F 倒地return / chaseMeleeTick / alignTick 守卫 / 平A中断判据），
  空手攻击路由完结只回 状态="空手站立"，永远到不了出招逻辑 → 一直挨打。
  用户已在回旋踢触发处加 倒地=false（以前没有该行），判定为正确且充分。
- **剥离自研击倒脱困（用户拍板：原生机制已存在）**：
  - 原生机制：主角-男.xml:1030 被击飞击倒 帧脚本，`_parent.击倒时小跳` 为真且 !落地 时
    每帧 random(15)：0=小跳、1/2=上下跳、3=前跳。九命猫妖初始化新增 `自机.击倒时小跳 = true` 启用。
  - HeroUnarmedSkillBrain 删除：C2 脱困分支、_knockdownEscapeTick、_usableSkill（仅脱困使用）、
    _wasKnockdown 字段、单位击倒已脱困 标记读写；C2 位置保留一行守卫
    `if (st.indexOf("击倒") > -1) return;`（击倒期本脑完全不出招，防与原生小跳互相干扰；
    击倒+浮空 也拦在此处，不再进 C 空中分支）。_dodgeLockUntil 位移锁保留（仍约束常态裁决/打断换招）。
  - 相关注释同步更新（_dodgeLockUntil、_release）。
  - 校验：全库无残留引用（仅注释提及）、括号平衡 0/0、BOM 保留。
- 注意： 击倒时小跳 是无条件属性（非战斗地图也生效），玩家控制目标已被 初始化 的 控制目标 return 挡掉。

## 2026-09-08 夜（续17）：屏幕上方震地久震不落——旗标与物理脱节 + 重力=1 缓降

- **现象拆解**：升龙拳+踩人叠加冲量飞出屏幕属正常（设计内）。异常的是高空放震地后不快速下落，
  在屏幕上缘外反复震地动画很久才落下来。
- **根因（全局机制，非 AI 独有）**：
  1. 震地/地震/觉醒震地 容器第一帧以 `if(_parent.技能浮空 == true)` 决定是否
     `stop() + 启用快速下落(重力20, minHeight30)`——快速下落走 fastFall 源，20倍重力秒落；
  2. 上一技能（升龙拳）结束时 RoutingLifecycle.completeAnimation → clearSkillFloatTask 删 skillFloat 源 +
     容器 onUnload 清 `技能浮空` 旗标，但人还在天上 → 单位靠 naturalFall 源下落，
     **重力 = _root.重力加速度 = 1/帧**（引擎_lsy_常数.as:5）→ 极慢；
  3. 虎妙 AI 在技能结束当帧接下一个空中技（震地）→ 容器第一帧检查时 旗标=false → 不启用 fastFall
     → 以重力 1 缓降 + 空中无韧性时 AI 强制震地循环重播动画 → "震上好久"。
  玩家在"旗标已清、人还在空中"的窗口手搓震地同样命中——所以是全局 bug。
- **AI 侧修复（已落地，HeroUnarmedSkillBrain._release 开头）**：
  `if (self.浮空 == true && self.技能浮空 != true) self.技能浮空 = true;`
  —— 浮空中出招把旗标补真（与物理语义一致），容器第一帧判定必成立 → fastFall 生效秒落；
  fastFall 落地分支自动清 技能浮空=false，收尾干净；fastFall 源优先级最高（tick 内最先 return），
  与残留 naturalFall/skillFloat 源并存无冲突（落地后统一落地块收尾清理）。
- **FLA 治本方案（待维护者拍板，覆盖玩家侧）**：三容器（技能容器-震地/地震/觉醒震地.xml）
  第一帧 `if(_parent.技能浮空 == true)` → `if(_parent.浮空 == true)`
  （浮空 由空中控制器每帧按真实高度维护 `unit.浮空 = (_y < z - tol)`，永不脱节）。
- **续17 修正（维护者反馈"降落速度正常、是卡住不回来"）**：真机制是**垂直速度残留**——
  升龙拳/踩人顶出屏幕时 垂直速度 为大负值（上升中），后续 skillFloat/naturalFall 重力仅 1/帧
  （引擎_lsy_常数.as:5），磨平需 |v| 帧（v=-500 → 约16秒），期间单位持续冲高/悬停屏幕外；
  下落段加速正常故"速度看起来正常"。边界系统不存在（全库无屏幕钳制），不是被边界挡住。
  震地 fastFall(+20/帧) 未启用（旗标脱节）时磨平更慢 → 雪上加霜。
- **修复升级（_release 浮空出招段）**：清残留上升速度 `if (垂直速度 < 0) 垂直速度 = 0` +
  补 技能浮空 旗标。击倒期已被 C2 拦下，不会截断被击飞弧线；升龙拳贴地起跳 浮空=false 不受影响。
  即使 FLA 侧容器条件改了，负速度残留对 fastFall 也有害（25帧才磨平、期间再冲高数千px），
  所以两处都要修。
- **FLA 侧定稿（维护者提问后确认）**：三容器条件改为
  `if(_parent.技能浮空 == true || _parent.浮空 == true)` —— 保留原语义+真实高度兜底，覆盖玩家侧。

## 2026-09-08 夜（续18）：屏幕边缘卡死+无限震地——真机制与修复（用户纠正诊断后定案）

- **用户纠正**：不是"浮空太久/速度磨平"，是**卡死**——单位悬停在屏幕边缘位置不动、反复放地震；
  浮空多久都正常，但必须持续下落，不能停在原地。
- **真机制（空中控制器._tick:271-277 + _airSkillTick 强制兜底组合）**：
  1. 三空中技全 CD → 强制震地每 tick 无视 CD 重放（每次重置 上次使用时间 → 池永不恢复）→ 状态恒为"技能"；
  2. 空中控制器 `_tick` 状态切换让出：`state=="技能" → delete naturalFall`；
  3. 强制震地**不走 _release**，没补 技能浮空 旗标（上一技能容器 onUnload 已清）→ 震地容器第一帧
     `if(技能浮空==true)` 不成立 → 不启用 fastFall；
  4. 三者叠加：所有物理源清空 → `停止(unit)` → **零物理悬停冻结**；MP 未耗尽前循环不放 →
     MP 耗尽退化平A（状态离开技能）→ naturalFall 恢复才落回 → "至少十几秒"。玩家侧不会循环放震地故不触发。
- **修复（AI 侧，HeroUnarmedSkillBrain）**：
  1. `_airSkillTick` 释放前补 `if (self.浮空==true && self.技能浮空!=true) self.技能浮空 = true;`
     （覆盖池内正常释放与强制兜底两条路）→ 容器启用 fastFall(重力20) 快速下落 → 落地自动清旗标并 play() 砸地 → 正常收招；
  2. **撤销上轮 _release 里的 `垂直速度 = 0`**（用户明确：不改速度逻辑，升降速度一律交还物理积分）；
     _release 的补旗标保留（语义一致、fastFall 落地自清）。
  3. 强制震地保持不限次（用户此前拍板）：有了 fastFall 后每次强制震地都会下落，不存在"限次才回得来"的问题。
- FLA 侧容器条件 `if(技能浮空==true || 浮空==true)` 仍建议改（覆盖玩家侧 + 双保险；物理源停止时 浮空 残留 true 恰好能兜住）。

## 2026-09-08 夜（续19）：贴边抖动残留修复——收口探测距离与脱困探测对齐 + 最后一程改 Z 对齐接管

- **维护者实测：续8的 clampZIntent 修完仍到不了上下边缘，接近时来回抖。残留根因 = 探测距离错配**：
  - clampZIntent 用每帧Z速度(~5px)小步探测 → 贴边 5px 内才收口；
  - MovementResolver.applyBoundaryAwareMovement 脱困探测 = 行走X速度*5 钳 20..60px，且 MARGIN=80 内
    才做 hitTest → 离边 20~60px 就判 blockedAhead → 反向逃离（24帧窗口）→ 回压 → 振荡；
  - 5~60px 距离带里收口放行、脱困判挡 → 照样振荡。另 noProgress 投影计数在贴边硬压时也会触发逃离。
- **修复（仍只动自有模块）**：
  1. `clampZIntent` 签名改 `(self, wantX, wantZ)`，探测距离与脱困完全同口径（行走X速度*5 钳 20..60、
     斜向端点），且 `room < probe` 直接归零 → 脱困逻辑永远收不到它会判挡的 Z 方向；
  2. **最后一程改道**：chase/engage 里 `rawZ != 0 && wantZ == 0`（被收口）时直接激活 虎妙Z对齐 接管，
     alignTick 用步长级探测 + `Mover.move2D` 小步直走，绕开脱困逻辑 → 能正好走到边缘；
  3. `alignTick` 末段加边界步长钳制：目标 Z 在界外时 `step = min(step, 剩余边界距离)`，
     端点恰好压在 Ymin/Ymax 上（isDirectionWalkable 判可行走）→ 精确贴边；走完（step=0）清接管；
  4. 跟随路径只更新签名（主角贴边时跟随停在 ~60px 外，可接受，无振荡）。
- 校验：旧签名无残留、括号平衡（CombatModule parens -2 为既有注释括号）、BOM/CRLF 与基线一致。

## 续20：升龙拳落地链失效回归修复（空中技能组不再触发）

- 现象：升龙拳结束后浮空期不再立刻接 踩人/震地/地震（此前接得上——尽管接得过早导致飞天）。
- 根因：续14 为修"升龙拳+踩人飞天"给升龙拳标 不可打断 并在 A 分支空中加
  `!_isNoInterruptSkill()` 守卫。但升龙拳从起跳到落地 状态 恒为"技能"（此前已实测），
  B 分支整个空中期不跑 → _currentSkill 一直是升龙拳 → 守卫把整段空中期全部封死。
  当时注释里"B 分支已复位 _currentSkill → 守卫随即失效"是错误假设，未经实机复验。
- 修复（HeroUnarmedSkillBrain.as）：
  1. 守卫收窄为"只封上升段"：相邻 tick 采样高度（Z轴坐标-_y，只读不写），
     高度变小 = 已进入下降段（动画已收、只剩落体，冲量叠加风险已消失）→ 放行 _airSkillTick；
  2. 新增 `_isJumpNoInterruptSkill()`：表条目同时 不可打断+贴地限定（跳跃型）才享受下降段放行；
     外部不可打断（扭转乾坤，_currentSkill==null）不命中，仍全程封死；
  3. 新增字段 `_lastAirborneHeight`，B 分支复位为 -1；
  4. 同步修正 SkillTable 不可打断 注释（原"等它自己落地收招"与现行为不符）。
- 语义：上升段守卫保住"防飞天"成果；下降段恢复"升龙拳结束立刻接空中技"原行为。
- 编译环境异常（与代码无关）：compile_test.ps1 早退——Flash CS6 Commands 目录里
  flash_project_path.cfg 与 cf7_compile_loader.jsfl 消失（已按 setup 模板重建），
  但 CompileTriggerTask 计划任务在沙箱内报未找到（无法确认真伪）。
  若仍报 [ERROR] CompileTriggerTask 未找到 → 需用户以管理员重跑 scripts/setup_compile_env.bat。
- 校验：BOM 保留、括号/圆括号平衡 0。publish 编译被环境问题阻塞，待环境恢复后补验。

## 续21：升龙拳接空中技仍未修好 → 转入实测诊断（加 [HU-AIR] 日志）

- 用户实测（23:37 重新编译后）升龙拳浮空后仍不接 踩人/震地/地震；确认已提交代码（HEAD）无此问题，
  问题在未提交改动里（升龙拳 不可打断+贴地限定 是其中之一）。
- 静态排查结论（均未找到确定性阻断点）：
  - 升龙拳容器仅 ~14 帧：帧5 升龙拳浮空初始化（浮空/技能浮空=true），帧14 战技路由.动画完毕(man,unit,true)
    → RoutingLifecycle.completeAnimation：清技能浮空任务 → 保留 技能浮空(__preserveFloatFlagOnUnload)
    → unit.动画完毕()（主角函数.动画完毕:991 → 空手模式 B4 → 状态改变("空手跳")）→ removeMovieClip
    → startNaturalLandingTask（自然下落恢复）。
  - 即容器结束后 状态应为"空手跳"、浮空=true → SkillBrain B 分支复位 → C 分支（无守卫）应直接
    _airSkillTick。CombatModule.chase 里 brain.tick 无条件调用，链路完整。
  - 三次静态推演都得出"应该能接上"，与实测矛盾 → 不再猜，加日志拿运行时数据。
- 新增调试日志（仅 _root.AI调试模式==true 时输出，前缀 [HU-AIR]，经 服务器.发布服务器消息）：
  1. A浮空：每 tick 输出 cur/st/浮空/技能浮空/高度/是否下降/无打断/跳跃型/已空中技；
  2. A技能段(未浮空)：升龙拳播放期 浮空=false 的帧；
  3. 技能态结束→：刚离开 技能/战技 态的第一 tick 输出 去向状态与三个旗标；
  4. C空中：进入空中分支时的 st/浮空/技能浮空；
  5. 空中裁决：pool 大小、pick、mp；pool=0 且震地不可用时输出退化空中平A。
- 待用户开 AI调试模式 实测抓 [HU-AIR] 日志定位（怀疑方向：容器结束回调链未生效/状态未离开
  "技能" 导致物理源清空悬停，或 pool 空+MP 不足退化平A）。
- 校验：BOM 保留、括号平衡 0。

## 续22：升龙拳接空中技失效·真因定位（FSM 吞 tick）+ 修复

- 用户 [HU-AIR] 日志实证：4154/4158 A浮空正常（Combat 在跑，上升段 h=73→131），
  4162~4194 约 10 个决策拍完全静默（无 A浮空/C空中/技能态结束），
  4198 恢复时已 st=空手站立 浮空=false → 只能在地面放震地。
- 推断：静默 = brain.tick 根本没被调用。chase/engage 均无条件 tick（状态早退在其后），
  排除。根 FSM 只有三态：Selector（无目标→_followWhenNoTarget，不 tick 脑）、
  Combat（tick ✓）、Evade（只 tickHeal，不 tick 脑）。
- 根因：_followWhenNoTarget 与 evade_action 的过滤列表只有 技能/战技/击倒/倒地，
  不含"空手跳"和浮空。升龙拳容器自然收完 → 状态"空手跳" → 若 FSM 恰在
  Selector/Evade，空中期整段不喂脑 → 空中技能组被吞，落地回 Combat 才恢复。
  （HEAD 里该路径已存在，此前被"技能中途主动打断换招"掩盖；升龙拳加了不可打断后
  走"容器自然收完"干净路径，坑暴露。）
- 修复（HeroUnarmedBehavior.as，两处同款）：
  1. _followWhenNoTarget：技能过滤后加 `if (self.浮空==true || st=="空手跳")
     { brain.tick(getFrame()); return; }`——空中不输出移动（空中控制器独占 _y），只喂脑；
  2. evade_action：技能过滤后加同款（用已有的 frame 变量）。
- 保留全部 [HU-AIR] 调试日志，待用户实测通过后再清。
- 校验：BOM 保留、括号平衡 0。用户自行编译。

## 续23：续22 修复收尾（FSM_Status 参数顺序核实 + Evade 空中守卫补全）

- 核实 FSM_Status 构造签名：FSM_Status(_onAction, _onEnter, _onExit)——action 在前。
  Evade 注册 (evade_action, evade_enter, evade_exit) 顺序正确，未装反；续22 两处
  喂脑修复均落在 action 路径，每 tick 生效。进 Evade 首帧仅 enter 不喂脑，
  差 1 帧 < 4 帧决策间隔，可忽略。
- 补充防御：_tryEscapeSkill 加空中守卫（浮空/空手跳 → _evadeJumpTried=true; return），
  防进 Evade 当帧在空中放小跳/闪现并写移动旗标与空中积分拉扯。
  初次插入时守卫误放在 var self 声明之前，已纠正到声明之后。
- 校验：BOM 保留、括号平衡 0。改动仅 HeroUnarmedBehavior.as，等用户编译实测。

## 续24：回退升龙拳接技能的全部修复尝试（用户裁定全部失败）+ 去掉升龙拳不可打断

- 用户实测：续20~23 的修复没一个生效，且引入新回归——小跳/闪现方向逻辑失效
  （应为"残血上下跳、满血上下+前跳"，变成总是默认后撤），嫌疑是 evade/follow
  空中喂脑与 _tryEscapeSkill 空中守卫干扰了位移技的输入旗标链路。
- 用户指令：回退所有"未定位到问题后对升龙拳接技能做的修复"，保留边缘浮动修复；
  同时去掉升龙拳的 不可打断（接受回到"升龙拳后立刻接空中技"的原行为，
  即飞天问题也一并回到原状）。
- 已回退：
  1. HeroUnarmedSkillBrain.as：A 分支恢复 HEAD 版守卫
     `if (!_isNoInterruptSkill() && !_isCurrentAirSkill()) _airSkillTick(frame);`
     （高度采样/下降段放行/技能名救援通道/全部 [HU-AIR] 日志删除）；
     _isJumpNoInterruptSkill() 删除；_lastAirborneHeight 字段及复位删除；
     B 分支 单图一次 收窄恢复 HEAD 版（_currentSkillName != null 口径）+ 删日志；
     C 分支 C空中 日志删除；空中裁决两处日志删除。
  2. HeroUnarmedSkillTable.as：升龙拳条目去掉 不可打断:true（贴地限定 保留）；
     表头 不可打断 注释块删除。
  3. HeroUnarmedBehavior.as：_followWhenNoTarget / evade_action 空中喂脑删除；
     _tryEscapeSkill 空中守卫删除。
- 保留（未回退）：
  - 边缘浮动全套：clampZIntent 新签名/边界收口/alignTick 守卫/Z对齐接管
    （Behavior 仅剩 clampZIntent 适配 2 行；MoveHelper/CombatModule 未动）；
  - 技能浮空 补旗标（_release 与空中裁决两处，修屏幕边缘卡死，属边缘问题）；
  - 击倒剥离（C2 原生化，用户此前明确要求）；
  - 喝药上限/平A窗口/位移锁/中距优先级等其他轮次修复；
  - 表内用户自调参数（兽王崩拳 CD10、燃烧指节 min50、能量喷泉 min80、一瞬千击 P1）。
- 回退后 Brain 与 HEAD 的剩余 diff = 击倒剥离 + 技能浮空补旗标 + 注释，共 88 行。
- 校验：三文件 BOM 保留、括号平衡 0；HU-AIR/高度采样/跳跃型 等标记残留 0；
  表内 不可打断:true 仅剩 觉醒霸体/铁布衫/聚气 三处（正确）。
- 用户自行编译验证。预期：小跳/闪现方向逻辑恢复；升龙拳后接技行为回到
  "已提交版本"状态（若飞天回归，属于去掉不可打断的已知代价）。
