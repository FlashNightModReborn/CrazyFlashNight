# 原生音频平台 v2：格式能力、桥接契约与可观测性 ADR

**文档角色**：CF7:ME Audio Platform v2 的解释性 ADR、分片 Gate、证据词典与恢复台账。H1 的唯一规范目标是 machine-readable [decision manifest](contracts/audio-v2/h1-decision-manifest.v2.json)；本文不得新增与 manifest 相反的规范。当前运行事实仍以正式 release 证据、代码、[`launcher/README.md`](../launcher/README.md) 和 [`testing-guide.md`](../agentsDoc/testing-guide.md) 为准；调查数据与证据上限见 [只读调研备忘](原生音频引擎-资源兼容性与静音故障只读调研备忘-2026-08-09.md)。

**状态**：`ACCEPTED / IMPLEMENTATION_AUTHORIZED_A1_A6 / PROMOTION_BLOCKED / NOT_DEPLOYED`。

**机器恢复标记**：`H1_STATE=accepted`；`H2_STATE=not_applicable_before_A6`。

**最后核对代码基线**：H1 activation commit `c5ca5dfa9718e8a7714038e929a64081b7fd0026` / tree `00bd7746b961169d8de55eafef64a2f911aaae95`（2026-08-09）是 accepted contract 基线；本文当前版本首次与完整 Audio Platform v2 implementation closure 同树落盘的 clean implementation commit 定义为 release source S。本文不得为其所在提交自引用 S commit/tree；在 source phase 必须从 clean Git object 及其 H parent 解析，进入 E1 后由 evidence manifest 的 `releaseSource` 精确绑定。当前正式 runtime 是 source `a87be26f1d6e80c0ea8883bbc10e46c895c35799`、release tree `32e5b9be67de220eb297e6c568c90546b283c471`、request `44A8D5461BE38E0807A7BE1DFEDAD4F5065F7981C590460CAEBAB4153E577A5E`、identity `C510ED0C27E78F3FE2552AFA37C3E3B2E673CEC54B8B5AC4E69D516D72BBD8BC`、closure `844B898C7B74633DF7392298286E3A354D24F344B2A02DEF9C6E5545DC1ACF81`；其正式入口已复核 `runtimeMode=formal_runtime` 与 promotion 一致，但本轮未跑无 candidate id 的 Agent Runtime MCP 窄纵切，故不声称该列车达到 `standard_entry_verified`。这些正式值仍不包含 Audio Platform v2。

---

## 0. Compact 后恢复卡

### 0.1 一句话目标

用一次受控 binary train 把“目录声称什么、native 实际支持什么、请求是否真正启动、内部是否有 PCM、真实设备是否输出”变成版本化且可程序验证的统一合同；补齐当前需要的 Vorbis 与 AAC/MP4，而不引入 FFmpeg 级通吃栈。

### 0.2 当前精确状态

- 已完成：只读链路、源码、资源 header、格式错位、Null backend 和发布闭包调查。
- 已完成：两份外部临时审查逐项裁决；成立项已进入 R2 decision manifest、schema、validator、mutation tests 与证据合同，临时审查不归档。
- 已完成：推荐甜区、H1/H2 两道人类闸、分片与停止线。
- 已完成：proposal P `e7dafe9a617d026b1e48cc8664cbdcd9b9054450` / tree `adb98de0cdc6cccf2c33fd5e8fd9a9813850a4b1` 已冻结；人类已精确接受 R2 manifest SHA-256 `518D1B5EA83960300B79D5C6DFB3CCDDAD3FFF1094C5950537E5C4FA9918469E`，独立 H1 receipt 已落盘。
- 已完成 H1 激活：H commit `c5ca5dfa9718e8a7714038e929a64081b7fd0026` / tree `00bd7746b961169d8de55eafef64a2f911aaae95` 是 P 的 direct evidence-only child；A1–A6 已获实现、隔离 candidate 构建和资格验证授权。
- 上一冻结 Source S `90985aecc9a3ce9a0bcd1ad11dde2b7158705644` / tree `beaaab2622bb9d3bfaa44485d63e274de14224a3` 曾闭合 A1–A5 source、qualification tooling 与文档矩阵，并由两个 clean unsigned producer 得到一致 candidate identity `1A675D3F200557E41E2821E74CE31309107051FE0EAEC79D9A402E7DFA146CEB` / closure `D5232F5688598118196EE09945E6FE2C4AD2C739AFB247A985BD6C920DA96A1F`。该身份只属于下述已失败、已作废的历史资格尝试，不得复用于修复后的 A6。
- 当前修复后实现要求 native BGM/SFX group 初始化后都显式进入 `started`；runtime contract 必须播放非静音 fixture，并同时观察 `playing=true`、推进的 `cursorFrames` 与非零 peak/RMS meter。qualification stimulus 在每条命令前对同一 ready generation 串行执行幂等 rearm，以覆盖初始 arm 早于 AS2 handler 而丢失的窗口；`sent=true` 仍只证明 socket delivery。本文当前版本与完整 implementation closure 首次同树落盘的 clean implementation commit 定义为新的 Source S，commit/tree 不在自身 ADR blob 中预填，而由提交后的 Git object 解析。
- 当前 A3：旧 S 的双 clean repro 仅保留为历史；当前 implementation/source matrix 已闭合，状态为 `implementation_complete_pending_exact_S_clean_repro`。提交后仍须从 exact 新 S 重新执行两个 clean unsigned producer 的 identity/closure/逐文件复现，不得沿用旧 candidate identity/closure。
- 当前 A6：旧 S 的 isolated candidate 已在 case 1 fail-closed，runId 已作废；当前版本尚无 exact-S 双 clean repro、新 isolated candidate、九份报告、44 个 case、四份真实 endpoint capture、十项人工听感矩阵或 E1。
- 当前正式 runtime 是 source `a87be26f1d6e80c0ea8883bbc10e46c895c35799` 的已 promotion 列车，不含本文目标能力；本轮未跑无 candidate id 的 Agent Runtime MCP 窄纵切，故该列车不声明 `standard_entry_verified`。当前也不存在 Audio v2 candidate identity/payload closure、真实 endpoint 结论、人工听感结论或 H2。
- 仍未授权：source tag、正式 builder/request、production proof、quorum、promotion 与正式部署；这些操作必须等待 A6 闭合后的 exact H2。
- 当前 H1 已有效；目标范围改变、frozen contract 漂移或 receipt 失配时才回到 `IMPLEMENTATION_BLOCKED`。

### 0.3 两类权威顺序

**目标/规范权威**：

1. [`AGENTS.md`](../AGENTS.md) 的仓库硬约束。
2. [`runtime-build-reproducibility.md`](runtime-build-reproducibility.md) 与 [`testing-guide.md`](../agentsDoc/testing-guide.md) 的发布/验证合同。
3. 人类 H1 精确接受的 proposal P 中 [R2 decision manifest](contracts/audio-v2/h1-decision-manifest.v2.json)。
4. 本文的解释、分片和台账；与 manifest 冲突时 manifest 胜出并必须修正文。

**当前运行事实权威**：

1. 当前正式 release identity/closure 与正式入口证据。
2. 绑定 source/artifact 的 exact candidate E2E。
3. 绑定 source/artifact 的新鲜测试输出。
4. exact source bytes 与当前部署 bytes。
5. 现役 canonical current docs。
6. [只读调研备忘](原生音频引擎-资源兼容性与静音故障只读调研备忘-2026-08-09.md) 等带日期快照。

提议 ADR 永远不能覆盖新鲜运行事实；发现冲突时记录 drift。当前事实也不能静默改写已接受目标，改变目标必须升 `scopeRevision` 并重签 H1。

### 0.4 已接受决策摘要

以下决策已由 H1 接受为目标合同。上一 S 曾为这些决策建立 source implementation，修复后语义必须随本文共同冻结到新 S；每项的当前资格状态读取 §15.3。`accepted-required`、源码存在、旧 final source matrix 或任一 exact S 都不能替代当前 S 的 clean repro、exact candidate、真实 endpoint 与人工听感，也不能外推为 deployed runtime：

| ID | 状态 | 决策 |
|---|---|---|
| AUDIO-V2-001 | accepted-required | production 只编入 WASAPI/DirectSound/WinMM 并显式禁止 Null；fallback 以 context+device 成功为单位，全部失败即显式音频失败 |
| AUDIO-V2-002 | accepted-required | native C ABI 使用版本化、caller-owned 结构体和 capability handshake；C# 启动时 fail-closed 校验 |
| AUDIO-V2-003 | accepted-required | catalog 由实际 capability、container/codec 探测和 probe 共同驱动，不再用硬编码扩展名宣称能力 |
| AUDIO-V2-004 | accepted-required | 承诺基线为 WAV、MP3、FLAC、Ogg Vorbis、AAC-LC/HE-AAC in M4A/MP4/ADTS |
| AUDIO-V2-005 | accepted-option | Opus 位于甜区上沿，H1 推荐纳入；WMA 默认不公开承诺 |
| AUDIO-V2-006 | accepted-required | 第三方 decoder 固定源码版本/哈希、静态链接进单一 `miniaudio.dll`，许可证/notice/构建身份闭合 |
| AUDIO-V2-007 | accepted-required | BGM 使用关联请求与结果；SFX 使用 ready generation 和汇总计数，不做逐枪往返确认 |
| AUDIO-V2-008 | accepted-required | SFX ready 前事件丢弃且计数，不回放过时音效；BGM 只保留 latest-wins intent |
| AUDIO-V2-009 | accepted-required | 错误保留原始 `ma_result`/HRESULT，并提供稳定 category/stage/retryable 语义 |
| AUDIO-V2-010 | accepted-required | BGM/SFX 双总线观测；内部 PCM、真实设备、endpoint PCM 和人工可听严格分层 |
| AUDIO-V2-011 | accepted-required | 设备丢失/切换采用有界恢复、generation 和显式失败，绝不静默退到 Null |
| AUDIO-V2-012 | accepted-required | 离线解码到 EOF、异常语料、真实 endpoint E2E 与人工听感共同构成资格闭包 |
| AUDIO-V2-013 | accepted-required | 不引入 FFmpeg、libVLC、GStreamer、系统 codec pack、网络流媒体或高级 DSP |
| AUDIO-V2-014 | accepted-required | 多个实现分片在 source-ahead/candidate 中验证，最终只做一次正式 promotion |
| AUDIO-V2-015 | accepted-required | H1 只授权实现与隔离 candidate；H2 绑定 exact candidate 后才授权 promotion |
| AUDIO-V2-016 | accepted-required | native mutation 由单一 owner queue 串行执行；状态/meter 只经原子快照读取，job 可取消并在 shutdown join |
| AUDIO-V2-017 | accepted-required | H1 绑定可达 proposal P 中的完整 canonical decision manifest、commit/tree/blob/SHA 与独立回执，不绑定持续更新的 ADR raw hash |
| AUDIO-V2-018 | accepted-required | 真实 backend 全失败时音频 fail-closed、不得发布 audio ready；Launcher 进入明确的无音频降级并可继续游戏，不把音频失败升级为进程 fatal |
| AUDIO-V2-019 | accepted-required | `audioSessionId`、`deviceGeneration`、`audioReadyGeneration` 与现役 transport `connectionGeneration` 分域，冻结推进、stale、job 与溢出规则，禁用裸 `generation` |
| AUDIO-V2-020 | accepted-required | catalog runtime compatibility probe 有界且不要求 EOF；offline qualification 独立解码到 EOF，二者不得混写 |
| AUDIO-V2-021 | accepted-required | validator 强制 canonical bytes、完整 manifest、Git provenance 与 schema；mutation tests 证明任一语义叶变化都会使旧摘要失效 |
| AUDIO-V2-022 | accepted-required | H2 必须绑定可恢复且由 S 内冻结 runner 现场重放的自动证据、真实 endpoint capture closure 与逐 case 人工听感矩阵 |

本表只作恢复索引；22 项完整规则及参数由 R2 decision manifest 唯一规范化。**当前 decision manifest SHA-256**：`518D1B5EA83960300B79D5C6DFB3CCDDAD3FFF1094C5950537E5C4FA9918469E`。

### 0.5 硬停止线

- **没有 H1**：不得编辑音频源码、协议、资产或构建输入；不得构建 candidate。
- **H1 未明确格式选项**：不得自行把 Opus/WMA 解释为已接受。
- **backend 为 Null、unknown 或未记录**：可以作为负例达到全局 `candidate_executed`，但 `audio_device_qualified=false`，不得进入 Audio E2E/H2。
- **只看到 load/cursor/peak**：不得称“有声”或 `e2e_verified`。
- **没有实际 endpoint PCM 与人工听感**：不得申请 H2。
- **没有 H2**：最高状态为 `e2e_verified / isolated_candidate / NOT_DEPLOYED`。
- **没有双 signer/双 faultDomain、production receipt 与 strict verifier**：不得 promotion。
- **promotion 后未用无 candidate 参数正式入口完成音频专项 smoke**：不得称 Audio v2 `standard_entry_verified`。
- 禁止手工复制 DLL、替换 manifest、复用 `-VerifyOnly` 报告或伪造 builder/proof。

### 0.6 当前唯一下一步

上一冻结 S 的 final source matrix 与动态计数只保留为历史基线，不外推到当前版本。旧 S 的 A6 runId `cdf7c0efbd6745d29ddb467808259f64` 在 `bgm_playback` case 1 因静音 fail-close：未观察到 AS2 ingress，native source 虽投影 `playing=true`，但 `cursorFrames=0` 且 meter 为 0；未生成有效 case 1 证据。该 candidate 随后已优雅停止，始终 `NOT_DEPLOYED`；没有 E1/H2，runId 永久作废。当前版本的 fresh source matrix 已通过：ABI `55`，runtime synthetic 与 shipped-large-M4A 均为 `219` checks / `initCategory=0`，offline 六 fixture，MF async `78/4`，Audio `201/201`，stimulus focused `16/16`，Launcher `3529 pass / 0 fail / 3 skip / 3532 total`，contract `673` leaf mutations，runner `28/28`，observer `20/20`，materializer `4/4`，operator `7/7`，assembler `5/5`，updater `5/5`，资产清单 `12/12` 与文档治理均通过；AS2 源码/SWF 本轮未变，继续精确绑定 fresh `185/185` 与 `scripts/asLoader.swf` SHA-256 `1F670AC3974C0A76F0D1DD57A414504AB37CFACB925D05078E9943D3234FC4A0`。qualification dependency 为 `568` 项，closure `BC1C5D7A0CEA694FD7751338C094660445E939AA6C411A2ACE0100BE3CB9378D`，manifest SHA-256 `DA5BA9A8553778CD7C26E430E73416CE92051B99A60C60DEFF02FC8C0E91BC62`。下一步是把本文当前版本与完整 closure 同树落盘为 clean Source S，重放 commit-aware H1 validator，再仅从 exact S 做两个 clean unsigned producer并比较 build identity、payload closure 与逐文件 rows。一致后才生成新 isolated candidate 并从新 runId 重跑完整 A6。不创建正式 source tag/request，不调用注册 builder/signer，不生成 production proof/quorum/receipt、promotion 或部署。

---

## 1. 问题、目标与非目标

### 1.1 问题

H1 调研时的正式 deployed runtime 与 proposal 基线把“消息已发送”“文件已被目录发现”“native load 成功”“播放图在推进”和“玩家真实听到”折叠成近似同一成功面。当时确认的问题包括：

- catalog 曾宣称 OGG，但当时的 native 未注册 Vorbis decoder；
- 11 个 AAC/MP4 文件曾使用 `.wav` 扩展名，另有注册 BGM 缺文件；
- 默认 backend 曾可落入始终成功的 Null；
- BGM/SFX start、readiness 和错误当时没有端到端确认；
- 当时只有 BGM 内部 peak，无法证明 SFX 或 endpoint；
- 当时没有可进入发布门的音频 qualification corpus 和真实 endpoint runner。

这些条目是 H1 因果基线，不是对修复后新 Source S 的描述。当前实现与重入状态见 §2 和 §15.3；正式 deployed runtime 仍保持上述旧能力，直到 H2 后的完整发布链实际完成。

### 1.2 目标

- 让播放器能力成为 native 可查询事实，而不是 C#/文档猜测。
- 让每个 BGM 请求可追踪到 accepted/deferred/started/failed/superseded。
- 让 SFX 预载、未知 ID、节流、丢弃和启动失败可计量。
- 让“全零内容”“增益静音”“Null backend”“设备丢失”“endpoint 静音”可区分。
- 支持当前玩家/资源需要的常见格式，并保留稳定扩展点。
- 让一次最终 promotion 覆盖能力、测试、notice、资产合同与正式入口。

### 1.3 非目标

- 不建设通用媒体播放器、视频 demux、网络流或 DRM。
- 不支持任意安装在 Windows 上的第三方 codec；只公开本 ADR allowlist。
- 不在本轮增加 HRTF、空间音频、卷积混响、MIDI、tracker、ReplayGain 或自动响度改写。
- 不用 limiter/normalizer 掩盖资源或混音错误；可增加 clip counter，改变声音需另行听感决策。
- 不把 AS2 改造成 native API 镜像；AS2 只消费产品级音频语义。
- 不批量重命名全部历史 SFX；文件名当前还是 linkageId 合同的一部分。

## 2. 当前地面真相

### 2.1 当前修复后 implementation（current version defines Source S / NOT_DEPLOYED）

- 本节的“当前”只指 H `c5ca5dfa9718e8a7714038e929a64081b7fd0026` 之后的修复后 source 语义；本文当前版本与完整 implementation closure 首次同树落盘的 clean commit 定义为新的 Source S。commit/tree 必须由提交后的 Git object 解析，不能在自身 ADR blob 中预填。上一 S `90985ae…705644` 的 candidate 与失败 run 只作历史诊断，不代表当前 exact candidate、E2E、promotion 或正式部署。
- native 边界已经是 `audio_bridge_v2.h` 的 fixed-width ABI：22 个带 size/version 前缀的 caller-owned struct、13 个 C export，capability/runtime/source/meter/SFX counters 与结构化 result 分离；C# `AudioNativeV2` 校验布局/ABI，`AudioEngine` 只保留兼容 facade。
- `AudioCoordinator` 是 managed 单 owner：native mutation、catalog replacement、recovery 与 shutdown 收束到同一队列，读者只消费不可变 snapshot。S 以有界 runtime poll 观察 native device recovery，保持同 session 的 `deviceGeneration` 单调，恢复 latest BGM intent 而不 replay SFX；shutdown 先关闭 admission，以 native cancel fence 解除被阻塞调用，再由 owner 完成幂等清理。该轮 recovery/shutdown 改动已进入 final source matrix。
- production backend policy 固定 `WASAPI → DirectSound → WinMM`，Null 在 production 不可达；context 成功但 device 失败仍继续下一真实 backend，全部失败进入 `failed_no_output`，Launcher 其他业务可继续。
- native 保留双 BGM slot/crossfade、SFX 多 voice/30 ms throttle，同时加入 BGM/SFX pre-master meter、start/seek 结果、七类 SFX 汇总 counter、runtime compatibility probe 与 offline EOF qualification probe。BGM/SFX group 初始化后必须分别显式 `start`；任一 group start 失败都使初始化失败，SFX group start 失败时先停止已经启动的 BGM group。runtime contract 必须实际提交非静音 BGM fixture，并在有界窗口内同时看到 `started`、`playing=true`、`cursorFrames>0`、`lengthFrames>0`、meter frame 推进及非零 peak/RMS；这能拦截“source playing 但 group 未启动”的静音图，仍不能代签物理 endpoint 可听。双 slot 与增益包络在本阶段只是源码实现事实；自动 qualification 只证明非零 `fadeSeconds` 请求取得相关 `started` result、source 被替换，以及相邻不超过 500 ms 的采样窗口持续有非零且推进的 BGM meter，因此只排除该观测分辨率下的无界静音空洞，不证明两个 slot 同时活跃或各 slot 的 fade envelope 正确。
- `MusicCatalog` 已改为 container/codec content sniff → capability match → 两次 size+mtime 稳定观测 → bounded runtime probe，并投影 exact `available|probing|unavailable`/reason；catalog qualification 和同连接 full snapshot 先于 `audio_ready`。
- BGM 使用 strict wire revision 2、request correlation 与 latest-wins result；SFX 使用 `S2|session|readyGeneration|batchSeq|ids...`，pre-ready/recovery/stale/throttle 等按事件数计数且不排队、不补播、不 replay。publisher 对纯 meter/counter snapshot 不重复广播 catalog/ready，只有 lifecycle epoch 改变才按 catalog → ready 投影。
- qualification runner、pinned toolchain writer、只读 observer pipe、strict stimulus pipe/client、decoder fixture materializer/operator、candidate/endpoint provenance 校验、WASAPI loopback capture/endpoint lister 与 draft-only assembler 已实现。toolchain writer 精确绑定 Node/MSVC/.NET/PowerShell/cmd/vcvars；runner 的隔离子环境不保留 PATH，并只使用 configuration hash-bound absolute `CF7_NODE_EXE`。stimulus 的每条 dispatch 都在同一 ready generation 内串行、幂等重发 `arm` 后才投递命令，避免首次 arm 早于 AS2 handler 而静默丢失；generation 在 rearm 与命令之间变化即 fail-closed。实际动作仍走生产 `AS2 → XMLSocket → AudioTask → AudioCoordinator/native`，`sent=true` 只证明 socket delivery，不证明 AS2 ingress、native started 或 endpoint 可听。assembler 只写 `HUMAN_REQUIRED`/`promotionAuthorized=false` 准备材料；这些工具的存在不等于 A6 输出已经生成或语义已经由人类接受。
- 前十个自动 case 在 Opus 后于 marker 外把 BGM gain 置零、dense SFX 后恢复，capture 先由 `IAudioClient.Start` 成功后的 `ReadySignalPath` 建立采集就绪栅栏，再允许刺激。runtime `f32` 与 endpoint WAV `pcm_s16le` 分域绑定；后四个 default-device/physical-route/sleep/no-stale case 和十项听感仍必须由人类完成。

### 2.2 资源事实

当前生成清单 `config/audio-v2/shipped-audio-assets.v1.json` 固定物理分母 `827 = 795 tracked + 32 ignored_source`；只有 795 个 Git-tracked 音频进入 H2 完整 EOF inventory，32 个 source-only ignored 文件保留 provenance 而不冒充投放内容。11 个 content-sniff 为 AAC/ISO BMFF、原扩展名错误的 BGM 已 canonical rename 为 `.m4a`，并同步 `sounds/bgm_list.xml` 与 packer allowlist。

缺失的 `Zenitsu Theme V3 (Godlike Speed).mp3` 继续作为 `audio_catalog_maintainers` 拥有的 disabled/missing diagnostic；latent `apwersound.wav` 继续作为 `audio_sfx_maintainers` 拥有的 item+AS2 reference diagnostic。两者都有 owner/reason/nextAction，不用静音占位文件掩盖。S 中清单已经生成器 exact check，canonical SHA-256 为 `E1FC5C6F24FDD0EF2EC667C0F316FA77237EAEB9C85096D3BD2EF09B90FB3D4D`；仍须在 A6 对 exact-S tracked corpus 解码至 EOF，当前清单和 rename 本身不证明可听。

### 2.3 外部能力

- `decoder-dependencies.lock.v1.json` 已 pin miniaudio/官方 decoder adapter `0.11.25`、libogg `1.3.6`、libvorbis/libvorbisfile `1.3.7`、libopus `1.6.1` 与 libopusfile `0.12` 的来源、archive/source SHA、build flags 与 license；这些源码静态链接进单一 `miniaudio.dll`。
- miniaudio 已采用 `miniaudio.c + miniaudio.h` 多 TU 形态；built-in WAV/MP3/FLAC 与官方 Vorbis/Opus adapter 共用受锁定 registry。
- Windows Media Foundation Source Reader adapter 只公开 allowlist 内 AAC-LC/HE-AAC in M4A/MP4/ADTS，处理 wide path、format change、seek、EOF/drain、取消和确定性 cleanup；WMA 虽可能是平台能力，仍未进入公开 capability。

这些是 release source S implementation 与依赖锁事实；只有 exact S 的 clean reproducible producer、runtime/offline probe 和 A6 candidate evidence 才能把它们提升为候选资格结论。

## 3. 目标拓扑与权威

```text
AS2 product intent
  ├─ BGM requestId + latest-wins intent
  └─ SFX frame batch + audioReadyGeneration
          ↓
C# AudioTask / AudioCoordinator
  ├─ wire revision / ack / counters / lifecycle
  ├─ capability-driven MusicCatalog
  └─ structured diagnostics
          ↓ versioned C ABI
Native Audio Runtime
  ├─ real-backend-only context/device fallback policy
  ├─ resource manager + decoder registry
  ├─ BGM/SFX buses + meters
  ├─ device generation/recovery
  ├─ bounded runtime compatibility probe
  └─ offline EOF qualification probe
          ↓
WASAPI → DirectSound → WinMM → explicit failure
```

| 层 | 权威 | 不拥有 |
|---|---|---|
| AS2 | 场景/BGM/SFX 产品意图、优先级和当前业务语义 | codec、设备、native load 成功判定 |
| C# | 协议、请求关联、catalog、readiness、错误翻译和 UI/日志投影 | decoder 内部和物理设备实现 |
| native | decoder、PCM、播放图、设备、meter、底层错误 | 场景优先级和玩家业务状态 |
| qualification | 语料、故障注入、endpoint 证据 | 生产业务权威 |
| 人类 | H1 范围和 H2 听感/promotion 授权 | 伪造机器证据或跳级状态 |

## 4. 设备与生命周期合同

### 4.1 Production backend

production 必须同时：

1. production compile policy 固定 `MA_ENABLE_ONLY_SPECIFIC_BACKENDS + MA_ENABLE_WASAPI + MA_ENABLE_DSOUND + MA_ENABLE_WINMM + MA_NO_NULL`，或经构建断言证明完全等价；
2. fallback 在 **context 与 playback device 都成功** 后才停止，不能把“第一个 backend 的 context 初始化成功”误当成“该 backend 可播放”；
3. 可由仅编入上述三种 backend 的 `ma_device_init_ex()` 在 device-init 层枚举，或显式逐 backend 完成 context+device；两种实现都必须保留相同结果语义；
4. 导出实际选中的 backend、device name/id、sample rate、channels、format 和 generation；
5. 若从 WASAPI 降级到 DirectSound/WinMM，记录结构化 warning；
6. 三个真实 backend 均失败时进入 `audioStatus=failed_no_output`、不发布 audio ready、目录与播放控制显示不可用及原因。

Null 只允许 test-only/offline qualification build。该构建必须带不可混淆的 capability/identity，且 production policy 明确拒绝。

音频 fail-closed 不等于 Launcher 必须退出；本 ADR 推荐并在 H1 决策块中冻结“明确无音频降级、游戏可继续”。若人类要求音频失败即终止 Launcher，必须改写冻结块后再签 H1。无论进程是否继续，都不得把 Launcher `Ready`、socket 存活或目录可见外推为 audio ready。

### 4.2 Device change

- 默认设备或路由变化推进 `deviceGeneration`。
- 恢复期间暂停接收新 native 播放，C# 进入 `recovering`。
- BGM 保存 current intent、loop、gain 和可用 cursor，在新设备成功后有界恢复。
- SFX 不回放 recovery 期间的历史事件；只统计 drop，ready 后接收新事件。
- 重试必须有界、可中断、可观测；耗尽后进入已定义的 `failed_no_output`，不无限后台循环。
- exact 次数/退避由 accepted manifest、S 与故障注入共同锁定；final source matrix 已重证 source 行为，但不能凭 source 结果外推 exact candidate 或真实端点。

### 4.3 Thread ownership

- 所有会改变 engine、sound、resource manager、device 或 decoder 状态的 native API 只由一个 owner queue/thread 串行执行；socket、UI polling、preload worker 和 recovery job 不得直接并发 mutation。
- meter、cursor、capability 和 device state 通过不可变/原子 snapshot 暴露；snapshot 读取不得与 sound/device uninit 直接竞态。
- preload、probe、MF 与 recovery job 必须可取消、可等待；job 不能在 generation 失效或 shutdown 后回写 handle。
- 设备政策故障注入必须持续包含“首选 backend context 成功但 device 失败”，并证明会尝试下一个真实 backend，而不是静默失败或落入 Null；该用例是最终统一矩阵的一部分。

### 4.4 Shutdown

停止 admission → 等待/取消 decoder jobs → 停声源与 device → uninit engine/resource manager/context → 平衡 MF/COM 生命周期。重复 shutdown 必须幂等；不得让异步 preload 在释放后回写 handle。

## 5. 格式与 decoder 合同

### 5.1 H1 推荐矩阵

| Container / codec | Backend | H1 推荐 | 公开扩展名 |
|---|---|---|---|
| RIFF/WAVE PCM/float | miniaudio built-in | include | `.wav` |
| MPEG audio Layer III | miniaudio built-in | include | `.mp3` |
| FLAC | miniaudio built-in | include | `.flac` |
| Ogg Vorbis | official miniaudio libvorbis adapter + pinned libogg/libvorbis/libvorbisfile | include | `.ogg`, content=Vorbis |
| AAC-LC/HE-AAC in MPEG-4 | Media Foundation source/decoder exposed as miniaudio-compatible data source/backend | include | `.m4a`, `.mp4` |
| AAC in ADTS | Media Foundation | include | `.aac`, `.adts` |
| Ogg Opus | official miniaudio libopus adapter + pinned libopus/libopusfile | recommended option | `.opus`; `.ogg` only after content probe |
| WMA/ASF | Media Foundation | defer public promise | none until qualified |
| other | none | reject | none |

扩展名只是发现 hint；container/codec 才决定 decoder。`.ogg` 不能等同于 Vorbis，必须区分 Ogg Vorbis 和 Ogg Opus。

Vorbis 路线在 H1 前已完成显式取舍：当时 vendored miniaudio `0.11.25` 确有可接入外部 `stb_vorbis.c` 的 optional wrapper，cursor 会正常维护；评审所称“push mode cursor 恒为 0”不成立。真正限制是 resource-manager/VFS streaming 会走 push mode，其 length 成功返回 `0`，后向 seek 需要重建 decoder 并向前暴力解码，而且 H1 调研基线尚未 vendored codec 本体。它适合极简 decode 场景，但不满足本轮 BGM 的完整 duration/seek/EOF 资格合同。因此 R2 明确选择官方 miniaudio libvorbis adapter + pinned libogg/libvorbis/libvorbisfile；S 已按该路线 vendored/锁定，`stb_vorbis` 继续作为已评估未选方案。

### 5.2 Media Foundation 边界

- 只接受 allowlist 中明确的 container/codec；第三方系统 codec 偶然能解码不自动扩大 capability。
- 必须支持 wide path、duration、seek、EOF/drain、format change 和 deterministic cleanup。
- COM/MF 初始化、worker thread、取消和 shutdown 必须有专项故障注入。
- 长 BGM 不允许以“全文件预解码进内存/临时 WAV”作为默认生产实现。
- S 已用 bounded Source Reader/data-source adapter 落实该路线；对外 ABI、capability 和结果语义不得随内部实现分叉，MF async/cancel/shutdown 故障注入已进入 final source matrix，仍须由 exact candidate/A6 证明真实运行资格。

### 5.3 依赖政策

- 优先采用 miniaudio 官方 adapter 与 codec 官方源码，不复制来源不明的单头文件 fork。
- exact tag/commit、SHA-256、build flags、license 与上游 URL 进入 tracked lock/notice。
- Vorbis 和 Opus 均为 BSD-family，但仍须分发 notice；不能以“宽松许可证”跳过审计。
- 所有 bundled decoder 静态链接进 `miniaudio.dll`，不增加 loose codec sidecar。

## 6. Native ABI 与 capability

### 6.1 ABI 原则

- 新 ABI 有独立 major/minor；breaking change 增 major，不静默复用现役签名。
- 所有 struct 以 `structSize + abiMajor + abiMinor` 开头。
- caller 分配输出结构和 UTF-8/UTF-16 buffer；native 不把需要跨边界释放的裸指针交给 C#。
- bool、enum、整数宽度固定；不把 C 编译器布局、`ma_result` enum size 或 miniaudio struct 直接暴露给 P/Invoke。
- C# 音频子系统先校验 ABI/capability，再发布 audio catalog/ready；不匹配只令音频 fail-closed，并按 `noOutputPolicy` 进入明确降级，不伪装成 ready。
- path 使用 wide/caller-owned buffer，完成规范化与允许根目录 containment 校验；超长路径显式失败，禁止固定 1024 字符截断。
- gain、fade、seek、loop 等数值先校验 finite 与冻结范围；负 fade、NaN、Inf 或溢出不得进入 native。

### 6.2 Capability 最小集合

```text
abiVersion
bridgeBuildId
miniaudioVersion
decoderBackends[]
containers[] / codecs[] / extensions[]
compiledBackendMask
selectedBackend / selectedDevice
supportsProbe / supportsSeek / supportsLoop / supportsDeviceRecovery
supportsBgmMeter / supportsSfxMeter
testOnlyNullEnabled
```

capability 分成 build capability 与 runtime device state；“编译支持 AAC”不能冒充“当前设备已启动”。

runtime snapshot 另含 `audioStatus=initializing|ready|recovering|failed_no_output|shutdown`、`audioSessionId`、`audioReadyGeneration`、`deviceGeneration` 和最近一次结构化失败；build capability 不得缓存覆盖这些动态状态。

### 6.3 结构化结果

每个可能失败的操作至少保留：

```text
requestId | operation | stage | category | nativeCode | hresult |
decoderBackend | audioSessionId | audioReadyGeneration | deviceGeneration |
retryable | messageKey
```

稳定 category：

```text
ok | missing | unsupported_container | unsupported_codec | malformed |
truncated | io_error | abi_mismatch | not_ready | stale_generation | unknown_id |
throttled | start_failed | seek_failed | device_unavailable |
device_lost | superseded | internal_error
```

不得再把所有 load failure 压成 `-2`，也不得丢弃原始 `ma_result`/HRESULT。

## 7. AS2/C# 桥接合同

### 7.1 Generation 域与状态机

禁止使用裸 `generation`。四个域不可互换：

- `connectionGeneration` 是现役 XMLSocket transport 世代，只防旧连接回调污染，不进入音频 wire 的 ready 判定。
- `audioSessionId` 是 process-scoped UUID；进程启动或 AudioCoordinator/native 完整所有权重建时生成且永不复用。
- `deviceGeneration` 是 native 真实设备生命周期世代，`uint64` 以十进制字符串过 wire；初值 `0`，每次真实设备初始化/恢复 attempt sequence 开始前推进。
- `audioReadyGeneration` 是 C# admission snapshot 世代，`uint64` 以十进制字符串过 wire；初值 `0`，每次初始化、恢复、capability 或 preload snapshot 重建开始前推进，重建结束后以同一世代发布 `audio_ready` 或 `audio_unavailable`。只有 ABI/capability、真实设备和完整 catalog 属于同一一致快照时才 ready。

请求的 `audioSessionId` 或 `audioReadyGeneration` 与当前值不等时返回 `stale_generation`、附 current tuple、零副作用。SFX 只计 `staleGenerationDrops`，不回放；BGM 只有携当前世代时才可 `accepted_deferred`。异步 job 必须捕获 session/ready 及相关 device 世代，完成时再次比对，stale completion 不得发布 handle/snapshot。计数器溢出时建立新 session 并重置；若 wire 无法可靠发布 reset，则音频 fail-closed。响应和诊断携完整音频 tuple，不能用 socket connection 世代代替。

### 7.2 BGM

- 每个 play/stop/seek/loop 请求携 `wireRevision=2`、`requestId`、`audioSessionId` 和当前 `audioReadyGeneration`。
- 结果至少区分 `accepted_deferred`、`started`、`stopped`、`superseded`、`failed`。
- bootstrap gate 只保留最新 BGM intent；被替代请求返回 `superseded`。
- `started` 必须在 decoder/source 初始化与 `ma_sound_start()` 成功后产生，不在 socket send 时产生。
- AS2 业务优先级仍由 `SoundEffectManager` 持有，C# 不重新实现 stage/jukebox/scene 决策。

### 7.3 SFX

- native/C# 完成 preload 后发布 `audio_ready {audioSessionId, audioReadyGeneration, deviceGeneration, loaded, failed, overrides, capabilityDigest}`。
- ABI、decoder registry 或真实 device 失败时改发 `audio_unavailable {audioSessionId, audioReadyGeneration, deviceGeneration, category, messageKey}`；不得同时发布 `audio_ready`。
- ready 前和 recovery 中的 SFX 不排队重放；分别计入 `preReadyDrops` / `recoveryDrops`。
- 每帧 fast-lane 保持批量低开销，不对每一枪做请求/响应往返。
- 汇总暴露 `unknownIdCount`、`throttledCount`、`startFailureCount`、`playedCount`，对首个/采样错误写结构化日志。
- `ma_sound_seek_to_pcm_frame()` 与 `ma_sound_start()` 均检查返回值；只有成功才计 `playedCount`。

### 7.4 Wire migration

- 新 wire 使用显式 revision；schema、AS2、C#、fixture 和文档同轮升级。
- implementation candidate 可短暂接受旧 wire 做差分诊断，但最终 release Gate 必须删除永久 v1 fallback 或证明它仅属于明确的 bounded migration window。
- 触及 `AudioBridge.as` 时先复核实际 source/include 归属；默认优先 `asLoader`，不能未经证据选择 `main`。
- AS2 编译结论必须有 fresh trace/Output；`publish_done.marker` 不单独算成功。

## 8. Catalog 与资产合同

### 8.1 Discovery 与 runtime compatibility probe

`MusicCatalog` 不再拥有硬编码“支持格式”真值。新流程：

```text
filesystem candidate
  → header/container sniff
  → native capability match
  → bounded runtime compatibility probe
  → available | probing | unavailable(reason)
  → catalog projection
```

手工注册但 missing/unsupported 的曲目保留可诊断记录，UI 显示禁用和 reason，不表现为可点击成功项。

玩家热投放文件在 size/mtime 连续稳定后才 probe；复制中的半文件保持 `probing`/`unstable_input`，不得被永久缓存为 malformed。

R2 runtime probe 只回答“当前 capability 是否可初始化并在上限内产出解码帧”，不签完整内容质量：两次 `size+mtime` 观测间隔 1000 ms；文件上限 512 MiB；单次最多 2000 ms、8 MiB 输入、96000 decoded frames；可取消。它不查询完整 duration、不要求 EOF。超时为 `inconclusive_timeout` 而不是 unsupported；有界窗口全零为 `compatible_signal_unknown`，不能写 `ok_silent`。缓存键含 normalized path、size、mtime、首 64 KiB SHA-256、capability digest 和 probe contract revision。

### 8.2 Extension mismatch

- shipped BGM 出现 extension/container mismatch 时，qualification 默认失败；本轮 A5 已把 11 个 AAC/ISO BMFF 伪装 `.wav` canonical rename 为 `.m4a`，未来新增 mismatch 仍须 canonical rename 或显式 owned exception。
- 玩家热投放文件可按内容解码，但必须标记 `extension_mismatch`；不得自动改写用户文件。
- 539 个历史 `.wav`/MP3 多数属于 SFX/linkageId，禁止批量改名；先用 content-based decoder 和 manifest 证明兼容。
- 11 个 MP4/AAC 伪装 BGM 已 canonical rename；缺失 Zenitsu URL 和 latent `apwersound.wav` 分别保留 owned diagnostic，不能用占位静音文件掩盖三种问题。

### 8.3 Offline qualification 与 silent content

只有 offline qualification probe 必须 stream decode 到 EOF，并输出 full SHA-256、frames、duration、peak、RMS、leading/trailing silence 和 non-finite count；单文件上限 120 秒，超时是 qualification failure，不冒充 unsupported。probe VFS 只在零字节物理 EOF 返回 `MA_AT_END`，末尾短读必须先把已读字节交给 decoder；full-hash 同遍扫描验证 Ogg page header/lacing/body 的物理边界，页内 EOF 归 `truncated` 且 `eofState != reached`，完整页再由 decoder 判定 malformed/可解码。`ok_silent` 是合法检测结果但默认不进入可播放目录；若设计上确需静音资源，必须在 asset manifest 显式 allow。该 probe 不得在 catalog/UI hot path 执行。

## 9. 可观测性与“有声”词典

### 9.1 Runtime diagnostics

至少提供：

- BGM/SFX pre-master peak/RMS 与 clip counter；
- source/group/master gain；
- cursor/length/playing/start result；
- selected decoder/container/codec；
- backend/device/state/generation/underrun；
- preload/recovery/drop/error counters。

生产 runtime 不常驻抓取系统 loopback；endpoint capture 仅在显式 qualification/diagnostic 模式启用。

### 9.2 结论词典

| 词 | 最低证据 | 不能推出 |
|---|---|---|
| `referenced` | 配置/AS2 引用存在 | 文件存在 |
| `identified` | container/codec 识别 | 可完整解码 |
| `decoded` | EOF、frame/duration 合法 | PCM 非零 |
| `content_nonzero` | peak/RMS 超过冻结阈值 | 播放图已启动 |
| `graph_signal` | start 成功、cursor 与 bus meter | 真实设备或 endpoint 有声 |
| `device_active` | real backend/device started | 系统 mixer/硬件可听 |
| `endpoint_nonzero` | loopback/端点捕获非零 | 听感质量可接受 |
| `human_audible` | 人工听见 | 无爆音、延迟或平衡问题 |
| `human_accepted` | 完成人工矩阵并签 H2 | promotion 或正式入口验证已完成 |

master/BGM/SFX gain 为 0 时是 `muted_by_gain`，不是 decoder failure。全零 packet 也不能单独代表 stop。

## 10. 构建、身份与发布合同

### 10.1 Build closure

修复后新的 release source S 必须冻结以下 source closure：

- `audio-v2-build-inputs.v1.json` 固定排序 materialize `miniaudio.c + miniaudio.h`、bridge/backend/support/decoder C/C++ TU 与 Xiph source；`build-audio-v2.ps1`/`build.bat` 把它们 deterministic link 为单一 `miniaudio.dll`；
- `decoder-dependencies.lock.v1.json` 固定 miniaudio/Xiph 来源、源码 SHA、flags、license 与 Media Foundation Windows SDK link inputs；`THIRD-PARTY-NOTICES.txt` 承载分发 notice；
- `runtime-inputs.v2.json` 的 native artifact source 覆盖 `.c/.cpp/.h/.rc` 与所需构建脚本/lock，新增 `.cpp` 不再逃逸 build identity；native change gate/workflow 已覆盖 native source 及 Audio v2 contract 触发面；
- qualification runner、observer/stimulus、依赖 manifest、configuration/fixture materializer/operator、asset inventory、endpoint capture/lister 和 draft-only assembler 显式进入 source/policy/qualification closure，不靠目录 wildcard 推断。

上一 S 曾从静止 source 重生成并 `--check` decoder lock、native build inputs、shipped asset inventory 与 runner dependency closure，也完成两个 clean worktree 的 unsigned pure producer；其 identity/closure 随失败 candidate 一并降为历史。native group start 与 stimulus rearm 已改变 artifact source，故新的 S 冻结前必须在同一新 bytes 上重新闭合派生输入与完整统一矩阵；冻结后再由两个 clean worktree 逐项比较 build identity、payload closure 与 file rows。当前没有新 S 的这两次结果，也没有可写入本文的修复后 candidate identity/closure。

### 10.2 State machine

| 状态 | Audio v2 最低证据 |
|---|---|
| `compiled` | 锁定工具链成功；新增 native/C#/AS2 tests 与受影响现役门通过 |
| `candidate_built` | immutable candidate、manifest、identity、closure 自洽；仍 `NOT_DEPLOYED` |
| `candidate_executed` | exact isolated candidate 进程已按全局 runtime v2 合同实际加载；无设备负例也可达到，但须记录 `audio_device_qualified=false` |
| `e2e_verified` | AS2→socket→C#→native→endpoint；BGM/SFX/格式/故障矩阵与人工听感通过 |
| `promoted` | H2 后 immutable request、双 builder、receipt、strict promotion/full-install verifier 完成 |
| `standard_entry_verified` | 无 candidate 参数正式入口加载同一 identity/closure，并跑音频专项 smoke |

通用 Help panel smoke 不能代签音频专项 standard-entry；Jukebox browser harness 也只证明 UI/控制面。

`audio_device_qualified` 是 Audio v2 的局部资格布尔值，不是新的全局 runtime lifecycle 状态。它至少要求 actual backend/device 已知、非 Null、device started 且 structured evidence 完整；只有 `candidate_executed && audio_device_qualified` 才可继续正向端点 E2E。无设备/首选 backend 失败的负例仍是有效的 `candidate_executed` 证据。

## 11. Qualification 与验收矩阵

### 11.1 Corpus

- WAV：PCM 8/16/24/32、float、mono/stereo、22.05/44.1/48 kHz。
- MP3：CBR/VBR、ID3、无 tag、短文件、长 BGM。
- FLAC、Ogg Vorbis。
- AAC-LC/HE-AAC：M4A/MP4/ADTS。
- Opus：仅 H1 纳入时加入 Ogg Opus、pre-skip/gain/seek。
- 负例：missing、0 byte、全零、长前导静音、truncated、wrong extension、unsupported codec、Unicode/特殊字符路径、畸形 duration、NaN/Inf。

### 11.2 Automated gates

1. 静态引用/资源/内容 magic 审计。
2. native ABI/device policy/decoder/offline EOF harness。
3. C# capability、错误映射、catalog、BGM ack、SFX ready/counter tests。
4. AS2 wire/focused TestLoader 与 exact publish target。
5. Jukebox browser harness 和受影响 Launcher 全量。
6. isolated candidate backend/device identity 与 loopback endpoint capture。
7. 故障注入：无设备、首选 context 成功但 device 失败并转下一 backend、设备切换、preload race、start/seek failure、decoder truncate、shutdown race。

### 11.3 人工听感

至少覆盖：

- 随包 MP3/WAV/FLAC、11 个 AAC/MP4 样本、Vorbis、Opus（若纳入）；
- BGM start/stop/pause/resume/seek/loop/crossfade；
- 密集枪声、重叠 voice、30 ms throttle 与 BGM+SFX 同时播放；
- master/BGM/SFX 0/默认/最大，确认无意外永久静音；
- 默认设备切换、拔插、蓝牙/HDMI 中至少一个现实路径、休眠恢复；
- 无爆音、明显延迟、声道错误、异常响度和恢复后重复旧 SFX。

## 12. 分片施工计划

A1–A6 已由有效 H1 解锁；当前进度与剩余 Gate 以 §15.3 为准。每片同轮更新台账，不把进度只留在聊天中。

### A0 — 文档与 H1

- 两份 canonical 文档、AGENTS 路由、R2 decision manifest + schema、H1、automated-report envelope/configuration/input/per-case、asset-EOF-results、asset-waiver、candidate/producer-verification、A6、listening 与 H2 Gate schemas、validator/tests、日志 provenance、治理与 diff 检查；两份外部临时审查删除且不归档。
- 先提交 immutable proposal P；人类冻结 codecs/backend/bridge/generation/probe/dependency/evidence 范围。
- Exit：H1 回执绑定 P commit/tree、manifest path/blob/SHA、ADR/validator/test blob，`promotionAuthorized=false`。

### A1 — ABI、capability 与 qualification 骨架

- 先建 versioned ABI、result/capability structs、C# fail-closed handshake。
- 建 offline corpus/runner 和 test-only sink，尚不改公开 catalog。
- Exit：ABI mutation tests、negative corpus 与生命周期测试通过。

### A2 — 真实设备政策与双总线观测

- real-backend-only context/device fallback、resource manager；production 排除 Null。
- backend/device/generation、BGM/SFX meters、start rc 和 bounded recovery。
- Exit：无设备 fail-closed、三 real backend 顺序、device loss/recovery 故障注入通过。

### A3 — Decoder、构建与许可

- 迁移 `miniaudio.c/.h`；引入 Vorbis、MF AAC，以及 H1 接受的 Opus。
- 扩 producer recipe、lock、notice、policy；只运行未签名、隔离的 diagnostic producer checks。
- H2 前禁止创建 release request/source tag、调用注册 signer/builder、云 dispatch、签名 proof、quorum、production receipt 或 promotion；双 signer/双 faultDomain 只属于 A7。
- Exit：完整 corpus EOF/seek/loop/cleanup；两个干净工作目录的未签名 diagnostic producer run 可复现。它们不构成正式 builder 共识或发布证据。

### A4 — Catalog、BGM ack、SFX ready 与 AS2 wire

- capability/content-driven catalog；禁用项 reason。
- BGM request/ack、latest-wins；SFX ready generation/counters/no replay。
- 原子迁移 AS2/C# wire 并跑 fresh Flash smoke/publish。
- Exit：协议 mutation、preload race、stale generation、unknown ID 与 superseded BGM 全覆盖。

### A5 — 资源合同收口

- 处理 11 个 AAC/MP4 伪装、缺失注册 BGM 和 latent SFX reference。
- 不批量改 SFX linkageId；输出 exception/manifest 与完整引用闭包。对当前两个 native discovery path 外 61 个文件逐个归为 `runtime_reachable | source_only | obsolete | owned_exception`，不得猜测 AS2 可达性。
- Exit：shipped asset audit 无未解释 missing/unsupported/mismatch，或每个例外有明确 owner/reason。

### A6 — Exact candidate E2E 与 H2 材料

- 先完成代码、资产、canonical docs 与构建输入，冻结 exact `releaseSourceCommit + releaseSourceTreeOid`；candidate 必须由该树生成。
- `candidate_built → candidate_executed → audio_device_qualified → e2e_verified`；失败设备负例可停在 `candidate_executed`。
- 真实 endpoint capture、BGM/SFX 全链、设备故障、人工听感。
- 自动报告、bounded endpoint captures 与逐 case listening matrix 按 `docs/contracts/audio-v2/*schema*.json` 生成；H2 Gate 的短 capture 必须 tracked（单个 ≤1 MiB、合计 ≤4 MiB），更长录音仅作非 Gate 诊断。
- Exit：在 release source S 的 evidence-only 后继 E1 中提交 canonical A6 evidence manifest，绑定 S、seal-time candidate attestation/runtime-manifest snapshot、Core/miniaudio/manifest bytes+hash、identity、closure、真实 backend/device、每份报告与可恢复 provenance、endpoint closure 与听感矩阵；等待人类审阅。任何 artifact source/input 修正都生成新 S 并重跑 A6。

### A7 — H2、promotion 与正式入口

- 人类精确绑定 S、E1、identity/closure、A6 evidence manifest、endpoint closure 与 listening matrix 并签 H2；在 E2 evidence-only 后继持久化独立回执。
- immutable source tag/request 必须精确指向 H2 已绑定的 `releaseSourceCommit + releaseSourceTreeOid`，不得改用包含回执/台账的后继 tree。
- request 创建后在 E3 写 evidence-only `h2-request-link.json`，绑定 H2 receipt blob/SHA、E1 evidence manifest、source commit/tree、source tag 与 requestId；不向现役 runtime v2 request schema 注入额外字段。
- 完成双 signer/双 faultDomain、production receipt、strict verifier 与 promotion。
- 无参数正式入口跑同身份音频专项 smoke。
- Exit：仅在全部闭合后写 `standard_entry_verified`。

## 13. 人类验收合同

### 13.1 H1_IMPLEMENTATION_ACCEPTANCE

H1 不绑定整份 ADR raw hash：§15 台账与证据会持续更新，绑定整份文件会自我撤销。H1 的唯一规范目标是 canonical [`h1-decision-manifest.v2.json`](contracts/audio-v2/h1-decision-manifest.v2.json)，当前 scope revision 为 `AUDIO-V2-H1-SWEET-SPOT-R2`，当前 SHA-256 为 `518D1B5EA83960300B79D5C6DFB3CCDDAD3FFF1094C5950537E5C4FA9918469E`。

manifest 固定为 UTF-8 无 BOM、LF、对象键递归排序、2-space indent、末尾一个 LF；SHA-256 覆盖整个文件字节。它完整承载 backend/no-output、decoder 具体实现、ABI、wire/generation、双 probe、线程/恢复、资源、observability、资格、H1/H2/release 与非目标。本文只解释；任何规范语义改变都必须升 revision、生成新 proposal 并重签。

H1 已按以下无循环 proposal/activation 顺序完成：

```text
P = commit(ADR + manifest + schemas + validator/tests + research evidence; temporary reviews absent)
  → human binds P commit/tree + manifest SHA and accepts H1
  → H = descendant evidence-only commit(H1 receipt + atomic recovery/status updates)
  → A1 may begin
```

P 必须是单 parent A0-only commit；相对 parent 的 diff 必须等于 validator 冻结的 A0 exact allowlist（`.gitattributes`、`AGENTS.md`、两份音频文档、本轮列明的 contracts、validator/test 与 research evidence），不能靠整个目录前缀夹带额外脚本，且 P tree 中不存在临时审查。P 必须是 HEAD/H 的 ancestor；`git ls-tree P <path>` 必须能恢复 manifest、全部 schemas、ADR、validator 和 test 的 exact blob。未提交工作树、unreachable blob 或 `commit=null` 一律不能被接受。P 形成后运行：

```powershell
node tools/audio-v2/validate-contract.js --proposal-commit <P> --print-h1
```

该命令生成唯一可接受的精确文本，形状如下；所有占位符必须由命令输出替换，人类不能只回复“看过/推进”：

```text
H1_IMPLEMENTATION_ACCEPTANCE
scopeRevision=AUDIO-V2-H1-SWEET-SPOT-R2
proposalCommit=<40-hex P>
proposalTree=<P tree oid>
manifestPath=docs/contracts/audio-v2/h1-decision-manifest.v2.json
manifestSha256=518D1B5EA83960300B79D5C6DFB3CCDDAD3FFF1094C5950537E5C4FA9918469E
promotionAuthorized=false
decision=accepted
```

接受后 Agent 在任何 A1 修改前写 tracked `docs/evidence/audio-v2/h1-implementation-acceptance.json`，严格符合 [`h1-implementation-acceptance.schema.v2.json`](contracts/audio-v2/h1-implementation-acceptance.schema.v2.json)。回执绑定 P commit/tree、manifest path/blob/SHA、ADR blob、validator blob、test blob、完整人类原文、A1–A6、`promotionAuthorized=false` 与 `NOT_DEPLOYED`。

H1 有效性算法：

1. 规范化 CRLF 与最多一个末尾换行后，人类原文必须与 `--print-h1` formatter 输出全文完全相等；禁止额外、重复、冲突字段或把拒绝文本后附模板。Agent 不得代签。
2. validator 从 P 读取并验证 exact bytes、canonical encoding、完整决策、schema surfaces、A0-only diff、Git provenance 与 P ancestry；`.gitattributes` 强制 audio-v2 contract/evidence JSON 与 validator/test JS 在所有 host checkout 为 LF，避免 Windows `core.autocrlf` 改写 raw digest；同时要求当前 attributes、manifest、全部 schemas、validator/test bytes 与 P 完全相等，不能用 HEAD 的弱化工具验证旧绑定。
3. H 必须是 P 的 direct single-parent child，且 diff 恰好只有 ADR、备忘、`h1-implementation-acceptance.json`。receipt 与同一 H commit 原子更新顶层状态、§0.2、§0.4 状态、§0.6、§13.3、§15.3、§16.3 及备忘恢复入口。
4. H 的稳定恢复面必须把唯一顶层状态行精确改为 `ACCEPTED / IMPLEMENTATION_AUTHORIZED_A1_A6 / PROMOTION_BLOCKED / NOT_DEPLOYED`，唯一机器标记行精确改为 `H1_STATE=accepted；H2_STATE=not_applicable_before_A6`，并出现 H1 `accepted`、A0 `completed_H1`、A1 `authorized_pending` 与“当前 H1 已有效”；备忘的两条唯一顶层行同步改为 `READ_ONLY_RESEARCH_COMPLETE / IMPLEMENTATION_AUTHORIZED_A1_A6 / NOT_DEPLOYED` 和 `H1_STATE=accepted`。validator 解析这两条唯一权威行并拒绝重复/互斥状态，禁止说明模板中的同名字样冒充当前状态。
5. 施工中的 ADR 台账/证据追加不失效；frozen manifest/schema/validator/test 任一变化必须升 revision、新 P、新 H1。manifest 任一语义叶变化会由 digest/mutation tests 使旧 H1 失效。
6. 完成 H commit 与 `node tools/audio-v2/validate-contract.js --proposal-commit <P>` 复核后才可开始 A1。

H1 只解锁 A1–A6，不解锁 source tag、正式 builder/signing、request 或 A7 promotion。早于 R2/P 的一般推进授权只足以完成 A0 与形成 P；当前有效授权来自人类对 exact P/R2 的 H1 接受及其 H activation receipt。

### 13.2 H2_PROMOTION_ACCEPTANCE

H2 只在 A6 已形成以下不可变链后生效：

```text
S = frozen release source commit/tree
  → exact candidate from S reaches e2e_verified
  → E1 = evidence-only descendant containing canonical A6 evidence manifest
  → human binds S + candidate identity/closure + E1 evidence and accepts H2
  → E2 = evidence-only descendant containing H2 receipt/status
  → runtime source tag/request still points exactly to S
  → E3 = evidence-only h2-request-link after request creation
```

E1 必须是 S 的 direct single-parent evidence-only child；diff 只允许本文与 `docs/evidence/audio-v2/**`。本文唯一顶层状态行必须精确为 `ACCEPTED / E2E_VERIFIED / HUMAN_PROMOTION_ACCEPTANCE_REQUIRED / NOT_DEPLOYED`，唯一机器标记行精确为 `H1_STATE=accepted；H2_STATE=pending_exact_human_acceptance；E1_STATE=evidence_ready`，台账另写 `E1_EVIDENCE_READY`；任何同文并存的 accepted/promotion-authorized 顶层状态都 fail-closed。其 A6 evidence manifest 严格符合 [`a6-evidence-manifest.schema.v1.json`](contracts/audio-v2/a6-evidence-manifest.schema.v1.json)，至少绑定：

- S commit/tree、candidate manifest/Core/miniaudio bytes+SHA、build identity 与 payload closure；首次生成 H2 文本时必须另传 absolute canonical live candidate root。validator 按现役 v2 规则枚举根 EXE 与 `runtime/` 完整 payload，拒绝 link/reparse、缺项和多项，逐 file row 重算 bytes/SHA 与 payload closure，并从 S 的 `config/build/runtime-inputs.v2.json` 重新计算 artifact-source/producer-recipe/toolchain-lock 三个 domain hash 与 build identity，不能只核 Core/miniaudio 或相信 manifest 自报 hash；
- 同一个 E1 还必须 tracked 保存 candidate-verification attestation 与原始 runtime manifest snapshot；attestation 绑定 seal-time observed root/time、完整 payload file count、source-domain config blob/SHA、candidate fields、snapshot blob/bytes/SHA 及 S 中 frozen validator blob。live root 只是一轮 sealing 前置，不是永久 locator；E2、compact 或跨机审计从 E1 attestation/snapshot、S 与候选哈希恢复，不能因旧临时目录清理而使已签 evidence 不可验证；正式 builder 后续仍必须复现同一 identity/closure；
- 真实 backend、device-id digest/name、sample rate/channels/format 与 `audioDeviceQualified=true`；
- 九个固定 reportId（资产 EOF、native ABI/decoder/lifecycle、真实 backend 故障注入、C# bridge/catalog、AS2 wire/publish、Launcher regression、BGM endpoint、SFX endpoint、device recovery endpoint）各一个符合 [`automated-report-envelope.schema.v1.json`](contracts/audio-v2/automated-report-envelope.schema.v1.json) 的 canonical tracked JSON blob；R2 manifest 对每个 report 冻结 required case matrix、report-specific source-input role matrix 和每个 case 的 required check IDs；普通 case 只接受 typed measurement，不接受自由 `observed`/`passed`；
- envelope 绑定 S、identity/closure、all-pass summary、case-results closure、S 内实际可恢复的 producer path/blob/SHA，以及 E1 tracked configuration JSON 和 input-manifest JSON。input manifest 必须排序列出三项 candidate artifact 和该 report 的全部不同 S input roles；validator 逐项恢复/recompute blob/bytes/SHA、role coverage 和整个 input closure，不能只写一个任意 64-hex，九组 configuration/input path 不得复用；
- 44 个 case 每项必须指向不同 path/blob/SHA 的 tracked structured JSON：普通 case 符合 [`automated-case-evidence.schema.v1.json`](contracts/audio-v2/automated-case-evidence.schema.v1.json)，回绑 producer/config/input closure/S/candidate，并对冻结 check IDs 输出有类型、可判定的 measurement；`shipped_corpus_all_files` 使用更严格的 [`asset-eof-results.schema.v1.json`](contracts/audio-v2/asset-eof-results.schema.v1.json)，validator 从 S 的 Git tree 重新枚举 `sounds/` + `music/` 下 `.flac/.m4a/.mp3/.mp4/.ogg/.opus/.wav/.waz` 完整集合，逐文件核对 path/blob/bytes/SHA 与 magic-sniff；PCM s16le 的 frames、peak 与 nonzero ratio 由 validator 直接从 S bytes 重算，压缩格式的 decode-to-EOF/frames/signal 由冻结 runner 在 seal-time 重跑；静音或非音频只能引用 S 固定路径、符合 [`asset-qualification-waivers.schema.v1.json`](contracts/audio-v2/asset-qualification-waivers.schema.v1.json) 的 exact path/exceptionId/owner/reason/signalClass，任意字符串例外无效；
- 每份报告另绑定符合 [`producer-verification.schema.v1.json`](contracts/audio-v2/producer-verification.schema.v1.json) 的不同 tracked verification blob。每份 raw live observation 都以 canonical JSON 经固定 `deflateRaw level=9` 与 canonical Base64 分块嵌入其自身 tracked configuration `argv`；不得依赖 validator 父进程环境变量，九份报告因此可在 E1 后分别恢复并重放。首次 `--print-h2` 时 validator 从 S 核对 `tools/audio-v2/qualification-runner.js` 与 [`qualification-runner-dependencies.schema.v1.json`](contracts/audio-v2/qualification-runner-dependencies.schema.v1.json) closure 的 exact bytes，把 S dependencies、E1 configuration/input/case/capture bytes materialize 到隔离目录，再以 live candidate 重放全部九份报告；runner canonical stdout 必须逐字节等于 E1 verification blob，单份上限 30 分钟。该机制能防止工作树/输入替换，却**不能机械判定恶意、空实现或只回显摘要的 runner 语义**；S runner 及其依赖是显式 H2 信任根，人类必须在 H2 前审查源码，并在 exact runner SHA 中接受。E2/compact 复验仍使用 E1 seal，无需旧 candidate 临时目录；
- endpoint capture 必须恰有四个 distinct tracked WAV，分别且只绑定 `bgm_playback`、`sfx_playback`、`bgm_sfx_mix`、`device_recovery` 一个 case，每个 case 恰出现一次，四份 path/blob/SHA 也必须彼此不同；候选运行时设备遥测 `device.sampleFormat` 必须忠实记录为 `f32`，与 loopback 落盘格式严格分层。每个 capture WAV 是 E1 内至少 1 秒的 `audio/wav-pcm-s16le`，单文件 ≤1 MiB、合计 ≤4 MiB，并绑定符合 [`endpoint-capture-configuration.schema.v1.json`](contracts/audio-v2/endpoint-capture-configuration.schema.v1.json) 的不同 E1 config 与 S 中固定 `tools/audio-v2/capture-endpoint.ps1` blob/SHA。config 精确回绑 candidate identity/closure、真实 device/backend、capture bytes/SHA、case、格式/rate/channels/duration、runId/time；validator 从 data chunk 重算并要求 peak absolute PCM16 ≥64、non-zero sample ratio ≥0.001，不能复制一份录音冒充四类 case，也不能用全零或单点噪声占位。采集工具同样是显式 H2 信任根：机器验证来源与闭包，人类必须审查工具语义并试听绑定录音；
- endpoint closure 是按 `captureId` 排序的 canonical JSON 数组 SHA-256，数组逐项包含 capture artifact、configuration artifact、tool artifact、case 与全部 media metadata，validator 必须重算；44 项 case evidence 对 endpoint cases 另有固定 `captureIds` 映射，不能用自由文字宣称已关联；
- 符合 [`human-listening-matrix.schema.v1.json`](contracts/audio-v2/human-listening-matrix.schema.v1.json) 的十项不重复、逐 case `passed` 的 canonical tracked JSON及完整 SHA；每项使用冻结的结构化 `captureIds` 映射，不接受自由 `evidence` 字符串替代录音绑定。

H2 Gate 不接受外部 locator：所有结构化报告、十项听感矩阵及上述四类短 endpoint capture 都必须由 E1 Git bytes 永久恢复。更长/高码率录音可作为非 Gate 诊断留在外部，但不能代替这些 bounded captures。孤立 `reportSha256`、总括 `humanListeningResult=accepted` 或不可定位口头听感均无效。

E1 后首次运行 `node tools/audio-v2/validate-contract.js --proposal-commit <P> --evidence-commit <E1> --evidence-manifest <path> --candidate-root <exact-live-root> --print-h2`；没有 fresh live full-payload rehash 与九份隔离 runner replay 就不输出 H2 文本。validator 同时从 S 读取 frozen P contract bytes、runner/dependencies、capture tool、waiver registry 与 report-specific inputs，从 E1 读取 candidate attestation/snapshot、九份 report、十八份 configuration/input provenance、44 份 structured case evidence、九份 producer-verification、四份 capture config/WAV 与 listening matrix，复核 Git blob/bytes/SHA、role/case/check/capture matrix、完整 shipped corpus、可直接解析 PCM 的 frames/signal、runner canonical stdout exact match、四 case 一一覆盖、closure、H→S→E1 ancestry/evidence-only diff。人类在接受前必须 source-review S runner/dependencies 与 capture tool；H2 原文必须与 formatter 输出全文完全相等，精确绑定 S commit/tree、build identity、payload closure、E1 commit/tree、evidence manifest path/SHA、candidate-verification SHA、endpoint capture tool SHA、endpoint closure SHA、listening matrix SHA、qualification runner SHA、`audioDeviceQualified=true`、`promotionAuthorized=true` 与 `decision=accepted`。E2/compact 复验可不传已经清理的 live root，但仍须从 E1 bytes 完整通过 attestation/snapshot 与 provenance/seal 校验。

Agent 随后在 E2 写 `docs/evidence/audio-v2/h2-promotion-acceptance.json`，严格符合 [`h2-promotion-acceptance.schema.v2.json`](contracts/audio-v2/h2-promotion-acceptance.schema.v2.json) 并保存完整原文。E2 必须是 E1 的 direct child，diff 恰好只有 ADR 与 receipt；ADR 的唯一顶层状态行精确改为 `ACCEPTED / E2E_VERIFIED / PROMOTION_AUTHORIZED / NOT_DEPLOYED`，唯一机器标记行精确改为 `H1_STATE=accepted；H2_STATE=accepted；E1_STATE=evidence_ready`，并原子写 H2 `accepted` 与“当前 H2 已有效”。随后用同一命令追加 `--h2-receipt <path>` 复核 E2。

E1/E2/E3 都是 **evidence-only 后继**：只能改变本文状态/台账及 `docs/evidence/audio-v2/**`，绝不是 release source。S 中每个 frozen contract blob 必须与 P 完全相等，即使当前工作树被还原也不能掩盖 S 漂移；H1 receipt 必须在 H/S/E1/HEAD 同字节，H2 receipt 必须在 E2/HEAD 同字节，commit-aware 恢复状态从 HEAD 的 ADR/备忘读取并要求工作树一致。现役 `cf7-runtime-build-request.v2` exact allowlist 保持不变；不得把 H1/H2 字段塞入 request、requestId、build identity 或 payload closure。source tag/request 必须精确指 S；request 创建后 E3 `h2-request-link.json` 旁路绑定 requestId、source tag、S、E1 manifest 与 E2 receipt。

任一 S、artifact source/input、candidate bytes、identity、closure、backend/device、automated report、endpoint closure 或 listening matrix 变化都会使 H2 失效，必须回 A6 形成新 S/E1 并重签。没有有效 E2 receipt 时不得创建 source tag/request、调用正式 builder、生成 production proof/quorum/receipt 或 promotion。

### 13.3 当前验收记录

| Gate | 状态 | 绑定 | 结论 |
|---|---|---|---|
| H1 | accepted | P `e7dafe9a617d026b1e48cc8664cbdcd9b9054450` / tree `adb98de0cdc6cccf2c33fd5e8fd9a9813850a4b1` / R2 manifest SHA `518D1B5E…469E` / tracked receipt | A1–A6 authorized；promotion blocked |
| H2 | not_applicable_before_A6 | — | promotion blocked |
| A6 failed attempt | invalidated / NOT_DEPLOYED | old S `90985ae…705644` / tree `beaaab2…24a3`；candidate `1A675D…6CEB` / closure `D5232F…6A1F`；runId `cdf7c0efbd6745d29ddb467808259f64` | case 1 静音 fail-close：无 AS2 ingress，source `playing` 但 cursor/meter 均为 0；已优雅停止，无 E1/H2，runId 不得复用 |

## 14. 风险与 stop-loss

| 风险 | 控制 | Stop-loss |
|---|---|---|
| MF seek/thread 生命周期复杂 | bounded A3 spike + fault injection | 无法稳定 seek/cancel 时只保留资产规范化，不硬接生产 |
| 新 codec 扩大 CVE/许可面 | 官方源码、pin/hash/notice、窄 allowlist | 依赖无法可复现或许可不清则拒绝该 codec |
| 设备恢复引入重复播放 | BGM latest intent，SFX no replay，generation | 出现 stale SFX/双播即停止 A2/A4 |
| content probe 拖慢 hot reload | background bounded runtime probe + explicit state | 不允许阻塞 UI/Flash ready；超时标 `inconclusive_timeout`，不误判 unsupported |
| 批量改名破坏 linkageId | shipped BGM 与 SFX 分开治理 | 未闭合全部 consumer 前禁止 SFX rename |
| meter 被误解为可听 | 结论词典 + endpoint/H2 | 任何 Gate 以 peak 单签即失败 |
| 一次性范围膨胀 | AUDIO-V2-013 非目标 | 出现 FFmpeg/网络流/DSP 要求时另开 ADR |

## 15. 文档同步矩阵与施工台账

### 15.1 A0/H1 已完成的文档闭包

以下条目是 P/H 阶段已经完成的历史闭包，不是 release source S 的实现清单：

| 文件 | 已完成动作 |
|---|---|
| 本文 | R2 解释合同、无循环 H1/H2 Gate、停止线与恢复协议 |
| 调研备忘 | 资源分母、stb/libvorbis 裁决、双 probe 与日志 provenance |
| `.gitattributes` | 强制 audio-v2 canonical JSON/JS 在 Windows/Linux checkout 均为 LF，稳定 raw digest |
| `AGENTS.md` | 音频 ADR 路由 |
| `docs/contracts/audio-v2/**` | canonical decision manifest 与 H1、report envelope/config/input/per-case、asset EOF/waiver、candidate/producer verification、runner dependency、endpoint capture config、A6、listening、H2 schemas |
| `tools/audio-v2/validate-contract.js`、`contract.test.js` | canonical/Git provenance validator 与全 leaf mutation tests |
| `docs/evidence/audio-v2/research-ready-preload-observation.json` | 固化 E2 的源日志 hash/mtime、最小原始 marker/timestamp、pairing 与六个 delta；不冒充完整日志归档 |
| `docs/evidence/audio-v2/h1-implementation-acceptance.json` | 在 H commit `c5ca5dfa9718e8a7714038e929a64081b7fd0026` / tree `00bd7746b961169d8de55eafef64a2f911aaae95` 中持久化 exact H1 接受 |
| `docs/codex.md`、`docs/kimi.md` | 临时外部审查；成立项提取后已删除，不归档 |

### 15.2 new Source S implementation 同步

| 变化 | 已同步的 canonical 文档/输入 |
|---|---|
| owner/ABI、backend、decoder、catalog、wire、ready/recovery | `launcher/README.md`、`agentsDoc/architecture.md` |
| 玩家发现格式、mismatch、missing/latent 提示与 11 项 rename | `sounds/README.md`、`sounds/bgm_list.xml`、`config/audio-v2/shipped-audio-assets.v1.json` |
| qualification observer/stimulus、operator/capture/assembler、final matrix、exact-S clean repro、candidate/endpoint/H2 Gate | `launcher/README.md`、`automation/README.md`、`agentsDoc/architecture.md`、`agentsDoc/testing-guide.md`、本文 |
| miniaudio/Xiph/MF 保留边界 | `docs/tech-stack-rationalization.md`、`launcher/native/decoder-dependencies.lock.v1.json` |
| producer/identity/policy/workflow 输入 | `docs/runtime-build-reproducibility.md`、`config/build/runtime-inputs.v2.json`、`config/build/native-change-gate.v1.json`、`.github/workflows/runtime-bundle-integrity.yml` |
| 第三方分发 | `launcher/THIRD-PARTY-NOTICES.txt` |
| 构建、asset、runner/capture 派生输入 | `launcher/native/audio-v2-build-inputs.v1.json`、`config/audio-v2/**`、`tools/audio-v2/**` |

上述既有同步曾进入上一 S；本轮 native group start、非静音 runtime contract、stimulus per-command rearm、重生成的 qualification dependency 与四份 canonical 文档必须一起进入本文当前版本所定义的 Source S。根 bootstrap、`runtime/**`、正式 manifest/consensus 与已部署 identity/closure 没有因 Audio v2 改变。

### 15.3 当前台账

| Phase | 状态 | 已有证据 | 未验证 | 下一步 |
|---|---|---|---|---|
| A0 | completed_H1 | P commit/tree、R2 manifest/schema/validator/tests、日志 provenance、三路审查与 tracked H1 receipt | — | 保持 frozen contract 不变 |
| A1 | source_implemented_verified | 22 struct/13 export fixed-width ABI、C# layout/handshake、single-owner coordinator、runtime/offline probe 与 qualification tooling 已实现；当前 source matrix 已通过 ABI `55`、Audio `201/201`、Launcher `3529/0/3` 与 contract `673` | exact-S clean repro、candidate 与真实端点资格尚不存在 | 提交本版本后进入 exact-S 双 clean repro |
| A2 | source_fix_verified_pending_exact_S_repro | production `WASAPI → DirectSound → WinMM`/no-Null、双总线 meter、七类 counter、recovery 与 shutdown 已实现；BGM/SFX group 显式 start，非静音 runtime contract 在当前真实设备分支两次通过 `219` checks / `initCategory=0`，MF async `78/4` | exact candidate device/recovery 与真实 endpoint capture 尚不存在 | 从 exact S 重现 producer/candidate 后再进入 A6 |
| A3 | implementation_complete_pending_exact_S_clean_repro | 旧 S `90985ae…705644` 的 `1A675D…6CEB / D5232F…6A1F` 因 A6 静音失败仅保留为历史；当前 source/test/doc/dependency closure 已闭合 | 旧 identity/closure 不得沿用；本文所定义 S 的两个 clean producer identity/closure/file-row 一致性尚不存在 | 提交后从 exact S 做两次 clean repro |
| A4 | source_fix_verified_pending_exact_S_repro | capability/content-driven catalog、catalog→ready barrier、BGM correlated result、S2 batch/counters 与 publisher epoch projection 已实现；qualification stimulus 每条命令在 same generation 下幂等 rearm，`sent` 仍仅为 socket delivery；focused `16/16`、Audio `201/201`、Launcher 全量已通过 | exact candidate wire/endpoint 尚不存在 | exact-S 双 clean repro 后随新 candidate 重跑 A6 |
| A5 | source_implemented_verified_pending_A6_EOF | 11 项 `.wav`→`.m4a` rename、packer/bgm_list 同步、`827 = 795 tracked + 32 ignored_source` 清单与 owned diagnostic 已实现；当前清单 `12/12`、digest `E1FC5C6F…B90FB3D4D`，dependency 568 项已重算 | exact-S tracked corpus EOF qualification 尚不存在 | A6 解码全部 tracked corpus 到 EOF |
| A6 | failed_attempt_retained_waiting_exact_S_repro | 旧 S candidate `1A675D…6CEB / D5232F…6A1F` 的 run `cdf7…59f64` 已在 `bgm_playback` case 1 因无 AS2 ingress、source playing 但 cursor/meter 为 0 而 fail-closed；candidate 已优雅停止且始终 `NOT_DEPLOYED` | 旧 runId 已作废；exact-S 双 clean repro、新 isolated candidate、真实 backend/device、九份报告、44 case、四份 endpoint capture、十项人工听感与 E1 全部不存在 | 只在 exact-S repro 一致后构建新 candidate 并用新 runId 从 case 1 重跑完整 A6 |
| A7 | blocked_by_H2 | 现役 runtime v2 发布链可复用，但 Audio v2 未进入该链 | A6/E1/exact 人类 H2 均不存在；未授权任何正式 source tag/request/builder/proof/quorum/promotion | 保持停止，等待未来 A6 闭合与 exact H2 |

## 16. 恢复协议

### 16.1 稳定搜索锚

```text
AUDIO-V2-001
AUDIO-V2-015
AUDIO-V2-018
AUDIO-V2-019
AUDIO-V2-020
AUDIO-V2-021
AUDIO-V2-022
AUDIO-V2-H1-SWEET-SPOT-R2
docs/contracts/audio-v2/h1-decision-manifest.v2.json
518D1B5EA83960300B79D5C6DFB3CCDDAD3FFF1094C5950537E5C4FA9918469E
c5ca5dfa9718e8a7714038e929a64081b7fd0026
00bd7746b961169d8de55eafef64a2f911aaae95
ACCEPTED / IMPLEMENTATION_AUTHORIZED_A1_A6 / PROMOTION_BLOCKED / NOT_DEPLOYED
H1_STATE=accepted
H2_STATE=not_applicable_before_A6
H1_IMPLEMENTATION_ACCEPTANCE
H2_PROMOTION_ACCEPTANCE
docs/evidence/audio-v2/h1-implementation-acceptance.json
docs/contracts/audio-v2/a6-evidence-manifest.schema.v1.json
docs/contracts/audio-v2/asset-eof-results.schema.v1.json
docs/contracts/audio-v2/asset-qualification-waivers.schema.v1.json
docs/contracts/audio-v2/automated-case-evidence.schema.v1.json
docs/contracts/audio-v2/automated-report-configuration.schema.v1.json
docs/contracts/audio-v2/automated-report-input-manifest.schema.v1.json
docs/contracts/audio-v2/candidate-verification.schema.v1.json
docs/contracts/audio-v2/endpoint-capture-configuration.schema.v1.json
docs/contracts/audio-v2/producer-verification.schema.v1.json
docs/contracts/audio-v2/qualification-runner-dependencies.schema.v1.json
docs/contracts/audio-v2/human-listening-matrix.schema.v1.json
docs/evidence/audio-v2/h2-promotion-acceptance.json
docs/evidence/audio-v2/h2-request-link.json
audio_device_qualified
releaseSourceTreeOid
AudioCoordinator
audio_bridge_v2.h
cf7_audio_bridge_v2_query_capability
WASAPI → DirectSound → WinMM
S2|audioSessionId|audioReadyGeneration|batchSeq
config/audio-v2/shipped-audio-assets.v1.json
tools/audio-v2/qualification-runner.js
tools/audio-v2/qualification-observer.js
tools/audio-v2/qualification-observer-client.ps1
tools/audio-v2/qualification-stimulus-client.ps1
tools/audio-v2/materialize-qualification-fixtures.js
tools/audio-v2/qualification-operator.js
tools/audio-v2/capture-endpoint.ps1
tools/audio-v2/list-playback-endpoints.ps1
tools/audio-v2/assemble-a6-evidence.js
tools/audio-v2/write-qualification-toolchain.ps1
config/audio-v2/qualification-runner-dependencies.v1.json

implementation_complete_pending_exact_S_clean_repro
failed_attempt_retained_waiting_exact_S_repro
90985aecc9a3ce9a0bcd1ad11dde2b7158705644
beaaab2622bb9d3bfaa44485d63e274de14224a3
1A675D3F200557E41E2821E74CE31309107051FE0EAEC79D9A402E7DFA146CEB
D5232F5688598118196EE09945E6FE2C4AD2C739AFB247A985BD6C920DA96A1F
cdf7c0efbd6745d29ddb467808259f64
HandleSfxFastLane
runtimeMode=isolated_candidate
standard_entry_verified
```

以下字符串只作为 H1 调研基线的 anti-regression 搜索锚；命中时必须判断是否是兼容 facade/test 或旧叙述回流，不能把它们当成当前架构：

```text
ma_bridge_init
ma_engine_init
ma_context_init__null
AUDIO_EXTENSIONS
All audio commands are fire-and-forget
PreloadFromDirectoriesAsync
```

### 16.2 每次恢复顺序

1. 读 `AGENTS.md`、本文 §0/§13/§15.3；先确认唯一顶层状态和机器恢复标记仍是本文开头的值。
2. 用 Git object 核对 H commit/tree 精确为 `c5ca5dfa9718e8a7714038e929a64081b7fd0026` / `00bd7746b961169d8de55eafef64a2f911aaae95`，其 parent 是 P；从 H 读取 tracked H1 receipt，并核对 P/tree/manifest SHA 与人类原文。
3. `git status --short`，保留无关改动；把“正式 deployed runtime”“H/P/H1 合同基线”“上一失败 S/candidate/run”“修复后新 S”“未来 A6 evidence”分层，禁止互相代证。
4. 先确认上一 S `90985ae…705644` / tree `beaaab2…24a3`、candidate `1A675D…6CEB / D5232F…6A1F` 与 runId `cdf7…59f64` 只作失败诊断：case 1 无 AS2 ingress、source playing 但 cursor/meter 为 0，candidate 已优雅停止，runId 作废且无 E1/H2。
5. 在 clean Git object 中确认修复后的新 S 是本文当前版本与完整 implementation closure 首次同树落盘的 implementation commit，核对 H ancestry/parent、完整 audio-related diff、frozen contract bytes、派生 lock/inventory/dependency closure；S commit/tree 不从本文猜值。随后运行 `node tools/audio-v2/validate-contract.js --proposal-commit e7dafe9a617d026b1e48cc8664cbdcd9b9054450`，工作树与 HEAD 不同字节时必须先停。
6. native group start、非静音 runtime contract 与 stimulus per-command same-generation rearm 改变 source/测试；必须在新 S 上重跑完整受影响矩阵，旧 S 的动态 hash/计数和 candidate identity/closure 不得沿用。
7. 从 exact 新 S 做两个 clean unsigned producer，逐项比较 identity/closure/file rows；一致后才生成新 isolated candidate，以新 runId 执行 A6 的 9 report/44 case/4 endpoint capture/10 listening matrix。
8. 当前没有 E1 或 H2。只有 A6 全部闭合后才能形成 evidence-only E1 并请求人类 exact H2；此前保持 A7 blocked，禁止任何正式 release 动作。

### 16.3 下次会话开场

```text
先读 AGENTS.md 与原生音频平台 v2 ADR §0/§13/§15.3。
当前 ADR 状态是 ACCEPTED / IMPLEMENTATION_AUTHORIZED_A1_A6 / PROMOTION_BLOCKED / NOT_DEPLOYED。
当前 H1 已有效：P=e7dafe9a617d026b1e48cc8664cbdcd9b9054450，tree=adb98de0cdc6cccf2c33fd5e8fd9a9813850a4b1，manifest SHA=518D1B5EA83960300B79D5C6DFB3CCDDAD3FFF1094C5950537E5C4FA9918469E；H=c5ca5dfa9718e8a7714038e929a64081b7fd0026，tree=00bd7746b961169d8de55eafef64a2f911aaae95。先从 H/P Git bytes 验证 receipt 与 frozen contract。
上一 Audio v2 S=90985aecc9a3ce9a0bcd1ad11dde2b7158705644 / tree=beaaab2622bb9d3bfaa44485d63e274de14224a3；其 candidate identity=1A675D3F200557E41E2821E74CE31309107051FE0EAEC79D9A402E7DFA146CEB、closure=D5232F5688598118196EE09945E6FE2C4AD2C739AFB247A985BD6C920DA96A1F、runId=cdf7c0efbd6745d29ddb467808259f64 在 case 1 静音 fail-close：无 AS2 ingress，source playing 但 cursor/meter 为 0。candidate 已优雅停止、NOT_DEPLOYED；无 E1/H2，runId 作废。
当前修复后语义：native BGM/SFX group 必须显式 started；runtime contract 用非静音 fixture 证明 cursor/meter 推进；stimulus 每条命令在 same generation 下幂等 rearm，sent 仍只表示 socket delivery。本文当前版本与完整 closure 首次同树落盘的 clean commit 定义为新 Source S，commit/tree 由 Git object 解析；旧 S 的动态计数、identity/closure 与 candidate 不得沿用。
当前正式 runtime 是 source `a87be26f1d6e80c0ea8883bbc10e46c895c35799` 的已 promotion 列车；本轮未做无 candidate id 的 Agent Runtime MCP 窄纵切，故不声称该列车达到 `standard_entry_verified`。Audio v2 exact-S 双 clean repro、新 exact candidate、真实 endpoint capture、人工听感、E1 和 H2 均不存在，机器状态保持 H1_STATE=accepted；H2_STATE=not_applicable_before_A6。
下一步：冻结新 S → commit-aware H1 validator/完整 source matrix → exact 新 S 两次 clean repro → 新 isolated candidate/新 runId → 从 case 1 重跑完整 A6。此前不得创建正式 tag/request、调用 builder/signer、生成 proof/quorum/receipt、promotion 或部署。
```

## 17. 参考

### 仓库内

- [只读调研备忘](原生音频引擎-资源兼容性与静音故障只读调研备忘-2026-08-09.md)
- [R2 canonical decision manifest](contracts/audio-v2/h1-decision-manifest.v2.json)
- [Audio v2 contract validator](../tools/audio-v2/validate-contract.js)
- [Launcher source of truth](../launcher/README.md)
- [系统架构](../agentsDoc/architecture.md)
- [测试矩阵](../agentsDoc/testing-guide.md)
- [runtime v2 发布列车](runtime-build-reproducibility.md)
- [文档治理](../agentsDoc/documentation-governance.md)

### 上游与平台

- [miniaudio manual](https://miniaud.io/docs/manual/index.html)
- [miniaudio custom decoder example](https://miniaud.io/docs/examples/custom_decoder.html)
- [miniaudio changes](https://github.com/mackron/miniaudio/blob/master/CHANGES.md)
- [Media Foundation supported formats](https://learn.microsoft.com/en-us/windows/win32/medfound/supported-media-formats-in-media-foundation)
- [Using the Source Reader to Process Media Data](https://learn.microsoft.com/en-us/windows/win32/medfound/processing-media-data-with-the-source-reader)
- [Opus license](https://opus-codec.org/license/)
- [Vorbis FAQ/license](https://xiph.org/vorbis/faq/)
