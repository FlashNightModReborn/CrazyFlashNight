# 铁枪会支线一期：任务规格

> **文档性质**：任务落地规格。本文承接 `docs/铁枪会支线一期-第一防线建设大纲.md`，用于逐个任务确定 ID、条件、NPC、对白节拍、完成条件、奖励、解锁内容、系统依赖与验收标准。
>
> **施工原则**：随任务推进补系统能力，随功能落地做验收。不先做完整泛化系统。

---

## 一、编号与范围

当前 `data/task/iron_spear_tasks.json` 已有：

- `70001`：磨刀石
- `70002`：防线补给

这两个任务只是旧占位，可以完全重写。正式一期主链从 `70001` 起排布。

| 顺序 | ID | 标题 | 阶段 | 状态 |
|---:|---:|---|---|---|
| 1 | 70001 | 前线调度板 | 第一段：失败的扩张 | 详细规格 |
| 2 | 70002 | 新人救援 | 第一段：失败的扩张 | 待展开 |
| 3 | 70003 | 补给线试行 | 第一段：失败的扩张 | 待展开 |
| 4 | 70004 | 损耗汇报 | 第一段：失败的扩张 | 待展开 |
| 5 | 70005 | 失败复盘 | 第一段：失败的扩张 | 待展开 |
| 6 | 70006 | 堡垒共识 | 第一段：失败的扩张 | 待展开 |
| 7 | 70007 | 废城压力升级 | 第二段：铁枪图腾 | 待展开 |
| 8 | 70008 | 铁枪会合同 | 第二段：铁枪图腾 | 待展开 |
| 9 | 70009 | 前出决战尸潮 | 第二段：铁枪图腾 | 待展开 |
| 10 | 70010 | 火力基建加固 | 第三段：防线加固 | 待展开 |
| 11 | 70011 | 守城验收 | 第三段：防线加固 | 待展开 |
| 12 | 70012 | 大厅入驻 | 第四段：验收与推进 | 待展开 |

当前施工准备结论：

- `70001 前线调度板` 已无系统阻断，可以进入正式任务数据与任务文本施工。
- 70001 主验收路径使用任务 `conditions` 的 `infraLevel`，检测 `前线调度板` 基建达到 1 级。
- 旧 `70001` / `70002` 占位任务仍可完全重写；正式施工时同步重写 `data/task/iron_spear_tasks.json` 与 `data/task/text/iron_spear_text.json`。

---

## 二、公共约束

- 任务链继续使用 `铁枪会#N` 口径，具体编号按最终 ID 顺序调整。
- 前治安官在一期完成前应优先位于第一防线，不再由主线22直接放入基地大厅。
- 第一防线告示板是一期主链的核心承载设施：先作为任务入口，再扩展为长期玩法入口。
- 当前不做实时车辆单位、车队 AI、移动护送路径和车辆血量系统；车队用地图点、残骸、临时防线和对白表现。
- 具体奖励数值先占位，等任务与系统切片跑通后再按当前经济节奏调整。

---

## 三、当前工作区状态

截至本规格当前版本，第一防线调度板已经有雏形：

- `data/infrastructure/infrastructure.xml` 已新增 `前线调度板` 基建，包含 `Level 0/1`、价格和材料。
- `flashswf/levels/地图-第一防线防区.*` 已有调度板场景资产雏形。
- `scripts/引擎/引擎_lsy_基建系统.as` 已留 `_root.基建系统.第一防线调度板.启动任务面板()` 接口，目前发布调试消息，可作为后续拉起 Web 告示板面板的桥接入口。
- 第一防线场景已初始化 `前线调度板` 基建元件；基建 UI 已有升级写入点，能把 `_root.基建系统.infrastructure["前线调度板"]` 推到 1 级。
- 任务 `conditions` 已支持 `infraLevel`，并会在任务面板显示进度；基建状态已纳入存档读写。
- 第一防线前治安官已通过 `对话名 = "前治安官_第一防线"` 使用专用闲聊文本；目标 XFL 已发布到对应 SWF，数据级验收通过。

命名约束：

- 数据基建名使用 `前线调度板`。
- 对白中可以称"第一防线告示板"或"前线调度板"。
- AS 侧现有桥接对象名为 `第一防线调度板`，后续接 Web 面板时以实际代码为准，不在任务文本中暴露。

前治安官双地点盘点：

- `flashswf/levels/地图-第一防线防区/` 的 XFL 场景中已经出现 `NPC/前治安官/前治安官-NPC` 实例，第一防线侧实体具备施工基础。
- `launcher/web/modules/map-panel-data.js` 已新增 `sheriff_first_defense_avatar`，并从源数据派生出 `前治安官@first_defense`；生成产物 `data/map/task_npc_registry.json` 不应手改。
- `data/map/map_panel.xml` 已让 `sheriff_first_defense_avatar` 在 `主线#13` 后显示。当前地图门控只支持"达到进度后显示"，不支持"后续任务完成后隐藏/迁移"，因此第一防线到基地大厅的完整迁移仍需要后续系统能力或最终任务切换方案。
- `data/map/map_panel.xml` 当前仍以 `主线#22` 控制基地大厅 `sheriff_avatar` 显示；一期完成前应改为由铁枪会支线推进前治安官转入大厅。
- 既有 `data/task/general_tasks.json` 与 `data/task/mercenary_tasks.json` 中的裸 `前治安官` 任务 NPC 已补 `base_lobby` hotspot，避免同名 NPC 导航歧义。
- `data/dialogues/npc_dialogue_闲杂人等.xml` 保留基地大厅的 `<Name>前治安官</Name>`，并新增第一防线专用 `<Name>前治安官_第一防线</Name>`。场景 NPC 已支持"显示名/对白 key"分离，第一防线实例仍显示 `前治安官`，但闲聊读取 `前治安官_第一防线`。

已确认的参照样本：

- `武器大师` 不是异名 key 路线。健身房与第一防线两个场景实例都使用 `名字 = "武器大师"`，闲聊 XML 也只有 `<Name>武器大师</Name>`。
- `武器大师` 的双地点成立，是因为任务层使用 `get_npc_hotspot` / `finish_npc_hotspot`，地图层使用 `master_swordman_gym_avatar` 与 `master_swordman_first_defense_avatar` 两个 avatarId。
- `武器大师` 在地图上能分开摆放，是因为两地使用了不同头像资源：`武器大师头像.png` 与 `武器大师头像第一防线.png`。前治安官当前复用同一头像资源，因此地图层已补充 per-placement `relX/relY` 覆盖能力，让同一头像能在不同 hotspot 使用不同坐标。
- 当前 `初始化NPC` 已支持检索名分流；`目标.名字` 仍是默认 key，但 `对话名`、`任务名`、`商店检索名`、`技能检索名` 可分别覆盖。

NPC 检索名分流能力：

- 不建议把第一防线前治安官改成 `名字 = "前治安官_第一防线"`。这会污染任务 NPC key、地图 label、商店/技能查询与任务文本口径。
- 已给 `初始化NPC` 增加可选字段：`对话名`、`任务名`、`商店检索名`、`技能检索名`。四者为空时全部回退 `名字`，因此旧 NPC 不受影响。
- 第一防线实例保持 `名字 = "前治安官"`，同时指定 `对话名 = "前治安官_第一防线"`；XML 中新增 `<Dialogues><Name>前治安官_第一防线</Name>...`，但 `SubDialogue` 内显示名与立绘仍写 `前治安官`。
- 本期只实际使用 `对话名`。`任务名`、`商店检索名`、`技能检索名` 是技术储备，用于未来同一可见 NPC 需要不同任务身份、店铺库存或技能导师表时再启用。

处理原则：

- 任务与地图层仍以显示名 `前治安官` 作为角色口径，但所有涉及前治安官的新任务必须显式写入 `get_npc_hotspot` / `finish_npc_hotspot`。
- 一期前半段使用 `first_defense`；一期收束任务再把大厅侧显示、对白与任务入口切到 `base_lobby`。
- 第一防线地图定位应从 `launcher/web/modules/map-panel-data.js` 添加，再运行派生脚本生成 `task_npc_registry.json`，不要直接编辑注册表产物。

前治安官第一防线阶段文案备忘：

- 核心定位：他明确知道第一防线问题重重，撑下来千难万难，但必须在所有人面前扮演成"局面仍在掌控中"的人。
- 公开口吻：像旧治安体系的前线长官，使用"警长"、"命令"、"各队"、"火力线"、"撤离路线"、"活着回来"等强组织词，刻意表现得运筹帷幄、无所不知、无所不能。
- 情绪功能：他需要时刻煽动士气。口号可以存在，但口号下面必须压着具体调度：记名、接应、断后、补给、火力、撤离和伤亡登记。
- 私下层次：对主角或少数关键人物可以露出真实判断：人手不足、物资不足、纪律不足、派系协调脆弱。但不能在公开场合承认"可能守不住"。
- 人物矛盾：他不是盲目乐观，而是在用表演维持秩序。玩家应能逐渐看出他在"演"，同时理解他为什么必须这样演。
- 避免写法：不要写成普通热血队长、单纯口嗨的警长，或真的相信后援无穷无尽的乐观派。任何"必胜"句式都应暗含"必须靠纪律和消耗把胜利做出来"。

---

## 四、任务 70001：前线调度板

### 4.1 基本信息

| 字段 | 规格 |
|---|---|
| ID | `70001` |
| 标题 | 前线调度板 |
| 所属链 | `铁枪会#1` |
| 开启建议 | 主线13完成后 |
| 接取 NPC | Shop Girl |
| 接取地点 | 基地车库/商店区域 |
| 交付 NPC | 前治安官 |
| 交付地点 | 第一防线 |
| 任务类型 | 对话 + 基建解锁/验收 |
| 核心目标 | 协助第一防线建立"第一防线告示板/前线调度板" |
| 完成结果 | 告示板可用，后续告示板任务入口具备承载点 |

### 4.2 剧情目标

Shop Girl 告知玩家：A兵团决定给第一防线投入一批佣兵协助前治安官建设据点，但第一防线不是成熟据点，缺少能统一登记任务、调度救援、发布拾荒委托和收拢求援信息的前线设施。

玩家前往第一防线与前治安官商谈。前治安官承认自己有名义和旧治安网络，但缺少能把 A兵团佣兵、公社新人、拾荒者、临时护卫队和后勤需求汇总起来的工具。玩家协助把告示板搭起来，作为后续救援、拾荒、护送、尸潮预警任务的入口。

### 4.3 告示牌/告示板美术约束

本设施建议命名为 **第一防线告示板**，对白中可称"前线调度板"。它不是普通木牌，而是半手工、半电子化的前线调度设施。

建议视觉要素：

- 底座来自废车门板、路牌、铁丝网、脚手架或防爆盾拼装。
- 板面有纸质委托、手写路线、弹孔、污渍、血迹、旧地图碎片。
- 局部有 A兵团临时标记、前治安官的治安章/封条、公社物资标签。
- 可以留出一个小型电子终端、信号灯、破旧屏幕或接线盒，为后续铁枪图腾接入留视觉位置。
- 不要画成完整、整洁、现代化的公告屏；它应体现第一防线半建成、资源紧张、多方共治的混乱状态。

功能暗示：

- 纸质委托：救援、拾荒、护送、盗贼截击。
- 地图标记：贫民窟、废城、车队残骸、补给线。
- 电子接口：后续可接入铁枪图腾，显示高科技订单或信号任务。

### 4.4 对白节拍

**接取：Shop Girl**

1. Shop Girl 说明第一防线清理后没有真正稳住，只是从"尸群占据"变成"人类勉强站住脚"。
2. A兵团愿意派佣兵过去，但不可能让每个求援都直接进基地流程。
3. 第一防线需要一个前线调度点，用来挂任务、登记伤亡、收集拾荒需求和发布救援请求。
4. Shop Girl 让玩家去第一防线找前治安官，确认他是否能接住这套调度方式。

**交付：前治安官**

1. 前治安官说明自己能统筹军警宪政派残部，但目前第一防线人手混杂，靠口头命令会失控。
2. 他承认公社新人、佣兵和临时护卫队都需要一个能看见任务优先级的公共节点。
3. 玩家协助完成告示板布设。
4. 前治安官确认告示板可用，并指出第一批求援很快就会贴上来。

### 4.5 完成条件

当前推荐以已上线的基建雏形为主路径：

- **主路径**：使用 `conditions` 的 `infraLevel` 检测 `前线调度板` 达到 `1` 级；交付后开放后续任务。
- **旧特殊需求路径**：任务系统仍保留 `special_requirements.infrastructure`，但 70001 不建议使用这条旧路径，因为 `conditions` 能给任务面板提供进度行并受派生校验覆盖。

推荐任务数据片段：

```json
"conditions": [
  {
    "type": "infraLevel",
    "params": { "name": "前线调度板" },
    "target": 1,
    "label": "前线调度板建成"
  }
]
```

后续 Web 面板入口可挂到 `_root.基建系统.第一防线调度板.启动任务面板()`；第一版允许该接口仅发布调试消息，确保交互路径先通。

### 4.6 奖励与解锁

奖励暂定：

- 金币：少量
- 经验值：少量
- 可选材料：低阶建筑/维修材料

解锁：

- 第一防线告示板可见/可交互。
- 后续任务 `70002 新人救援` 可从告示板进入或由前治安官引导到告示板。
- 第一防线进入"半建成但可调度"状态。

### 4.7 系统依赖

已具备/可复用：

- 任务支持 `get_npc_hotspot` / `finish_npc_hotspot`，确保前治安官交付地点为第一防线。
- 基建系统已能读取 `前线调度板` 配置，并按等级控制场景元件显隐/帧状态。
- 任务 `conditions.infraLevel` 可检测 `前线调度板 >= 1`，并能在任务面板显示进度。
- 基建等级已随存档保存/恢复，可作为任务完成条件。
- 前治安官已注册/派生到 `first_defense`，旧基地大厅任务已补 `base_lobby` hotspot。

需接线：

- 基地大厅 `sheriff_avatar` 的 `主线#22` 显示规则后续需要移除或改为铁枪会支线驱动。
- `前线调度板` 交互需要调用现有 `启动任务面板()` 桥接接口。

可延后：

- 告示板真实 Web 关卡列表 UI。
- 告示板在地图面板中的独立导航。

### 4.8 验收标准

- 完成主线13后，玩家能接到 `前线调度板`。
- 玩家能从 Shop Girl 接取，并被引导到第一防线前治安官。
- 建成 `前线调度板` 后，任务条件 `前线调度板建成` 显示满足，任务可交付。
- 交付后，`前线调度板` 基建保持 1 级，第一防线告示板在场景中处于可用状态。
- 点击/触发调度板时能调用 `第一防线调度板.启动任务面板()`，第一版至少能发布调试消息。
- 后续 `新人救援` 的入口有明确承载点，不再凭空出现。
- 若暂未实装告示板 UI，也必须在任务文本和场景状态中清楚表达其上线。

---

## 五、后续任务占位

### 70002：新人救援

从告示板进入关卡，救援公社新人并阻击盗贼。需要关卡对白与情报物品展示老佣兵阵亡、临时封锁线撑到主角到来。

### 70003：补给线试行

车队作为地图点/残骸与临时防线表现。前治安官与主角杀入尸潮，抵达被盗贼伏击的车队防线点，组织幸存者撤离并断后击溃盗贼。

### 70004：损耗汇报

向 Shop Girl/A兵团汇报救援与补给线试行结果，确认临时护送队和拉长补给线不可持续。

### 70005：失败复盘

前治安官、公社新人、幸存老佣兵、A兵团接口和公社代表复盘外勤惨败。

### 70006：堡垒共识

多方决定放弃散点拾荒，转向前线据点、加固防线、持久消耗。

### 70007：废城压力升级

告示板任务护送拾荒队前往旧车队残骸资源点，在僵尸与盗贼多方混战中回收资源和严峻情报。任务开头需要用短对白回顾第一段结果：告示板上线、车队惨败、补给线不可持续、多方决定堡垒推进。

### 70008：铁枪会合同

`soldier` 牵线，用回收资源加额外材料换取铁枪图腾落地方案，并验收图腾基建等级。

### 70009：前出决战尸潮

图腾上线后告示板出现尸潮关卡。防线尚未阵地化，前治安官、`soldier` 与主角主动出战挡住尸潮。

### 70010：火力基建加固

建设自动炮台、机枪阵地、固定火力点、供能/维修节点，并引入冻链经济视角。

### 70011：守城验收

完整版尸潮守城关，玩家在炮台和机枪支援下守护阵线，告示板转为长期玩法入口。

### 70012：大厅入驻

前治安官转入A兵团基地大厅，铁枪会一期收束。
