// 兼容性适配器：保持原有接口可用
// 实际实现已迁移到 org.flashNight.arki.component.Effect.LightingEngine

/*

import org.flashNight.arki.component.Effect.*;

_root.色彩引擎.生成光照调整参数 = function(光照等级, 视觉情况) 
{
    return LightingEngine.generateLightParameters(光照等级, 视觉情况);
};

_root.色彩引擎.根据光照调整颜色 = function(影片剪辑, 光照等级, 视觉情况, 使用矩阵变换) 
{
    LightingEngine.applyLighting(影片剪辑, 光照等级, 视觉情况, 使用矩阵变换);
};


*/