# 地图工作台与 C# 收束：两阶段施工

文档角色：地图领域实施与验收记录。最后核对代码基线：commit `049eb27e3ed6c5ee264d38b29806ad219267e6d2`。

第一阶段发布为 `HUMAN_ACCEPTANCE_PASSED / promoted`，后文历史证据保持原义。第二阶段已获“可行，开工”授权，正在当前工作区完成[内容创作设计](地图内容创作工作台-第二阶段产品与CSharp权威收束-ADR-2026-09-06.md)的实现和机器验证；须单独交付隔离候选并经过独立人类验收，目前未提交、推送或部署。当前 v2 定义／asLoader 不与旧正式 Core 混用。

## 节奏与范围

第一阶段完成地图定义、C# 维护内核和地图工作台，推进到隔离候选的人类验收；反馈迭代通过后再提交、推送和正式部署。第一阶段发布期间只读调研第二阶段；完成部署后才实施第二阶段，再经过独立人类验收。

第一阶段不迁移任务完成、解锁和导航执行权威，不改玩家存档，不编译 AS2。地图的配置写入与 NativeHud 定义加载归 C#；Web 保留真实地图 renderer、命中和相机。AS2 兼容目录暂时保留，删除时机归第二阶段消费者切换。

## 第一阶段切片

1. 从现役 JS 机械抽出 `data/map/map_definition.json`，保持页面、热点、筛选、头像和原始 XFL 校准信息等价；明确 NPC 名称、地点和玩法分组为只读身份。
2. C# 地图维护内核只接受固定编辑操作：场景图位置/尺寸、头像位置/尺寸、筛选按钮位置/尺寸、显示标题。场景热点矩形由图块并集更新，不能单独覆盖派生矩形。
3. 地图工作台从“其他 → 工具”进入，复用地图 Canvas renderer 和 PanelRuntime。候选预览不修改运行时全局目录，显式保存后重启游戏生效；撤回拒绝覆盖后续修改。
4. GUI 和 CLI 使用同一 C# 内核。浏览器开发入口只在本机显式运行，不新增常驻服务。文件写入使用内容摘要、固定路径、互斥与原子替换；超时后按同一操作编号查询。
5. 验证配置机械等价、编辑往返、冲突/撤回、地图与工作台浏览器交互、Host 边界、文档治理，构建并执行隔离候选。

## 人类验收

- 在地图工作台切换页面和对象，拖动图块/头像并调整尺寸，对比当前配置与候选；确认操作、字体和预览清晰可用。
- 应用一项小改动，重新启动同候选，确认完整地图和 NativeHud 小地图一致，普通地图导航及任务入口正常。
- 撤回本次修改，重启读回，确认地图恢复且其他修改未被覆盖。

首次交付验收时（2026-09-06 11:04），状态为 `candidate_executed / HUMAN_ACCEPTANCE_REQUIRED / NOT_DEPLOYED`。当时隔离候选已启动到启动器，尚未由人类完成以上旅程，没有提交、推送、source tag、双构建或 promotion。后续反馈、验收和发布结果见下文，不能把这一历史时点当成当前状态。

## 第一阶段机器证据与候选

- 4 页、40 地点、18 筛选、58 静态头像和 1 动态头像从现役配置机械提取；完整 MapManifest 与提取前逐字段等价，C# NativeHud 的 40 个地点与既有 HUD JSON 逐字段等价。
- 同一 C# 内核的 17 项检查通过：预览零写、身份保护、编辑往返、原子保存、重试幂等、旧摘要拒绝、实际内容对账、撤回保护和原始字节恢复。
- 真实 C# HTTP 后端的浏览器交互通过：全窗口布局、缩放补偿拖动、预览/对比、JSON 导入导出与重复对象拒绝、保存/精确撤回、重开后的未知结果对账与草稿保留、头像、关闭重开；自动保存测试已恢复定义原字节。
- 实际 WebView2 使用生产 `overlay.html` 与虚拟目录加载，启动数据逐字段一致，4 页 / 40 地点、零脚本错误；其他来源不注入数据。此处只证明加载链路，不代替游戏内维护面板与导航人工验收。
- 现役地图浏览器 52/52、头像可见性 7 OK / 0 warn、WebP 110 文件 / 103 引用 / 0 PNG、地图缩放 18 筛选 / 324 用例 / 0 error（保留 3 项既有视觉债务）；Workbench strict 与文档治理通过。
- Launcher canonical runner：4742 通过、3 项既有跳过、0 失败；SDK 解析器 7/7。没有编译 AS2 或修改 SWF。

隔离候选路径：`tmp/runtime-candidates/v2/c-df861f7751d8-08846e81b3-20260906t025831591z-48f65a41`。

- Build identity：`DF861F7751D8713D3D1D1731D0D370F6E6BD870C6CF6E2B9F6EBFE8157D9ED14`
- Payload closure：`2FB0874A547FB41671EAF55D5470877710CA4E389F836AA061077EECEECD5065`
- Candidate Core SHA256：`B1BD5DF242271C8520369555BC8889E48AF8063BD0600D6B12A17C90A2ABF45A`
- 地图定义初始 SHA256：`E53C5699D41FC08D54FEF0E2FC3F000669EED60EAF2F41696452AFC322FFD420`

`automation/dev.ps1 -ReuseOnly` 在执行前核验 33 文件闭包，实际 Guardian PID `25808`，启动时间为 2026-09-06 11:04（本机）；进程来自以上候选下 `runtime/CRAZYFLASHER7MercenaryEmpire.Core.exe`。启动器标题含“隔离候选 / 未部署”。

正式 Core SHA256 仍为 `AB110F95C4877FC37178F21CD8995C87024A85A10C01027BF0CC0532A525F036`，正式部署 35 文件闭包仍为 `4C5F36DB8F4CFC3DAAB485F045F38DFD71DB583476D98B7924F8D580B3BDC0A7`。本阶段涉及 C# + Web + JSON；验收请使用根目录 `本地开发启动.cmd`，不要把正式 EXE 启动等同为运行本阶段候选。浏览器开发服务器在机器验证后关闭，撤回备份保留在 `tmp/map-workbench/changes`。

复跑入口与写入边界见[地图工作台说明](../tools/map-workbench/README.md)。

## 人类反馈后的第一阶段迭代（2026-09-06）

首次体验反馈指出保存重启后的地图黑屏和画布观察空间不足。现场配置仍合法，维护者保存的 `base_roof_visual` / `base_roof` 位置为 `(287.32, 43.63)`，定义摘要为 `A620782E33C0B5CBF9CBEA4F890538F55E0AD0D9F5E43B9D32F86FA561264CC6`。11:27 编辑发生于隔离候选 PID 25808；11:28 重启日志指向正式 `runtime/CRAZYFLASHER7MercenaryEmpire.Core.exe`，该旧 Host 不提供本阶段地图启动数据。维护者随后明确保留开发者入口约定，不增加旧入口兼容层；本轮没有加入兼容副本，也没有撤回其编辑。

工作台改为画布优先：`canvas-editor` 的 228px 属性栏 + 弹性地图，`canvas-editor-focus` 隐藏属性栏；地图不再单独拼一个简化 renderer，而是在独立子文档直接运行生产 MapPanel、原 CSS、页签、右侧分层、头像、地点标签及自动取景。编辑边框叠加于生产坐标变换；关闭边框即可看生产外观。相机提供滚轮 / +/- 缩放、中键 / 空格拖动、聚焦所选、玩家取景复位，Esc 从专注模式返回。缩放只改视图变换，backing canvas 大小不随倍率扩大。设计态全部解锁、无任务标记、不执行导航，不代表实时玩家状态或新玩法权威。

筛选编辑按生产消费者收束为名称和纵向排序值；页面可同时编辑标题与页签名。当前定义投影测试不再把编辑前的旧 HUD JSON 当成编辑后的 golden，而是精确比较 C# 和既有 Web 纯投影；初始机械等价证据仍保留在上一节。

本轮新鲜验证：生产地图 52/52；C# 内核 17/17；实际 C# 后端浏览器覆盖拖动、滚轮/按钮缩放、平移、专注模式/Esc、真实页签/分层、保存后重新加载、精确撤回、头像与关闭重开；DPR 1.5 的 1024×576 / 1600×900 / 2560×1440 三组只读布局检查通过。实际 WebView2 验证生产顶层地图、作者子文档与 DTO 隔离通过；JSON 整数/小数按 JS Number 的精确数值语义对照，不使用误差容限。Workbench strict 为 0 error / 0 warning。机器测试均保留维护者编辑后的定义原字节。

此次为 Web UI 与测试/文档增量，没有修改上一候选的原生 payload。`automation/dev.ps1` 重新核验并复用前述 identity / closure，启动 Guardian PID `13076`；正式运行时未变。仍待维护者体验复验，未提交、推送或部署。

## 第一阶段发布授权与测试数据复原（2026-09-06）

维护者已确认维护/预览可用并授权恢复测试地图数据、取得本地/云端共识、部署和推送二进制。已按操作 `ec28d95f1439e1803ffb8a4753a012f0` 的真实回执撤回，完整字节恢复为初始 `E53C5699D41FC08D54FEF0E2FC3F000669EED60EAF2F41696452AFC322FFD420`，屋顶位置恢复 `(211.6, 95.25)`；工作台代码保留。此前段落中的未部署状态是施工时点，不是后续发布结果。

源码冻结前将地图定义、开发入口、作者预览资源及 CLI/真实 WebView2 测试源码纳入 policy 输入；明确排除各自 bin/obj，生产所需预览 HTML/JS/CSS 加入 required-web-runtime-assets。未增加宽松旁路或修改原生 payload 验真规则。拟用不可变 source tag `runtime-build-v2/20260906-map-workbench-phase1-v1`；实际 request、共识及部署证据在完成后另行补录，当前不得提前视为 promoted。

首次 source `4ae851e8fcfd755930cdcacdf167f5c1a3f9988d` 的本地构建已签名，但 production policy 在商店头像来源检查失败，未创建远端 source tag 或启动云端构建。已有 `Pig.json` 来源记录绑定 1093 字节的混合换行，Git LF 原文为 1050 字节；商品内容未变。使用既有 `refresh-shop-portrait-sources.py` 仅刷新来源/回执，34 张头像与 runtime manifest 逐字节保留，并将该 JSON 固定为 LF。此 `.gitattributes` 改动进入 producer recipe，旧 request `404F1028F806E487D602FE4958B6B939B812A656C75CCA45C7A018E62973B298` 须被新请求 supersede、重新构建签名；失败回执保留，不放宽检查。

## 第一阶段正式部署（2026-09-06）

最终 source `049eb27e3ed6c5ee264d38b29806ad219267e6d2`、tag `runtime-build-v2/20260906-map-workbench-phase1-v1` 与 request `D6065B1E49E664C1F4484F667D57801617728F53710737093C03B419B52045AB` 已绑定。注册本地 X509 `physical-host-a` 与 [GitHub hosted run 34015051061](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34015051061) 对同一 build identity / 33-file closure 达成共识，40/40 production policy 通过，`2026-09-06T06:01:02.9274360Z` 完成原子 promotion。

正式 Core SHA256 为 `B1BD5DF242271C8520369555BC8889E48AF8063BD0600D6B12A17C90A2ABF45A`，与已体验候选逐字节一致；recipe 因 LF 修正而更新，所以最终 build identity 与早期开发候选不同，不能复用旧签名身份。正式 bundle、2 signers / 2 faultDomains consensus 与根 bootstrap `--verify-only` 通过。完整身份、回执与后续远端审计以[运行库发布记录](runtime-build-reproducibility.md)为准。

正式入口现可直接使用“其他 → 工具 → 地图工作台”；测试 CMD 继续用于开发候选。地图定义仍为恢复后的初始 `E53C5699D41FC08D54FEF0E2FC3F000669EED60EAF2F41696452AFC322FFD420`。没有发布 AS2 / SWF，也没有将第二阶段条件求值或任务迁移混入本列车。人类确认仅覆盖本阶段维护/预览可用，未代签部署后业务复验。

部署提交 `fc9b650bfc3aff794dcbd8d4381c35e24e44c2f2` 已快进推送；首次[远端审计 run 34015714902](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34015714902) 成功，精确确认 `state=promoted / deploymentChanged=true`、33-file closure、2 signers / 2 faultDomains 和云端证明完整重放。

## 第二阶段研究清单（第一阶段发布期间）

第二阶段的产品目标已获授权扩大为“剧情与美术可生产地图内容的工具”，不止 C# 权威迁移：包括页面/地点/图块与素材绑定、稳定 NPC/驻点身份、结构化进度条件、可解释的进度预览，以及对游戏地图、HUD、任务前往的一致性验证。实际 Flash 场景、对话、任务、商店本体仍按其制作流程接入。发布期间由独立子代理只读调研，详细方案另行落盘，不改第一阶段冻结发布树，不提前实施第二阶段。

- 地图、TaskPanel、HUD 交付提示、StageRunSession 结算后导航与四源当前地点解析的完整消费者清单。
- 有限 AS2 事实采集与执行前新鲜度检查；任务可交付性仍由现役任务服务提供，不能误用历史已完成任务集合。
- C# 单一地图规则求值、短期对照和各入口切流；按实际职责删除 AS2 求值与重复枚举/别名。现役 Web `evaluateCondition` 只是 unlock flag 展示映射，不是链进度业务求值器。
- `map_catalog`、NPC registry 与构建期 task catalog 的兼容接口和最终删除点。
- 同名 NPC 多驻点、别名、大学 OR 条件、返回基地奖励提交/关闭顺序及陈旧导航拒绝。

剧情与美术两个只读子代理当时已完成调研，合并方案见[第二阶段内容创作设计](地图内容创作工作台-第二阶段产品与CSharp权威收束-ADR-2026-09-06.md)。当时没有修改第一阶段冻结发布树；随后按开工授权实施。任务端点只写原任务 JSON，不另建地图 taskBindings，也不引入通用 Gameplay StateCore。

## 第二阶段实施与机器验证（2026-09-06）

实现涵盖页面／地点／图块／人物／驻点／条件结构编辑、来源发现、图片候选／裁切／发布 linkage 提取、原任务端点补丁、A/B 原因树预览，以及同一 C# 地图域到 AS2／Web／NativeHud 的完整消费者切流。已删除旧 XML、地图 catalog／NPC registry／HUD sidecar 和对应加载／生成链。旧静态导出命令仅保留退役提示。详细 schema、协议与恢复边界以[工作台说明](../tools/map-workbench/README.md)为准。

原 4 页／40 地点／57 人物身份／59 驻点、8 组条件与 7 项头像门控由冻结 Git 树机械迁入 v2，任务 JSON 本体与玩法数值未改。当前初始 v2 定义 SHA-256 为 `81E08FD4B58C20E1E47124CF5327139827DBEF50EE3F3D638E07E9966B741041`；新页面、图片与端点写入测试只在独立副本执行并精确撤回。

已取得的证据：

- 冻结规则对照：552 组进度／交通工具、8,921 项断言；几何不变，包含大学 OR、未知与循环、固定／跟随及冲突。
- C# 地图、会话、作者批次、退役入口和 HUD 聚焦测试通过；最终 Launcher 全量 4,765 通过／3 项既有 skip／0 失败，实际 testhost 确认串行，耗时 1.4464 分钟。
- 真实 C# 后端＋生产 MapPanel：第五页／新物理场景、空类型创建、图片上传／裁切／外观分支、OR 条件、复制 ID、引用拒删、多文件应用／重开／原字节撤回、丢失响应只查询、三视口／专注／关闭通过。
- 同一 NPC 两个真实来源绑定、互斥 presence、固定与跟随端点、早晚方案、引用拒删，以及地图＋原任务文件字节撤回通过。这不是实际 Flash NPC 点击或交付旅程。
- 新鲜 CS6：MapDomainBridge 19/19；Map loot 698/698（含 StageRunSession 419/419），另 BoxInteractionArbiter 13/13 cases、53 assertions；BootSequencer 91/91＋Handshake 12/12。均为 compiler 0/0、无 32K retry 的独立 trace，不以 marker 或 SWF 存在代签。
- 最终 asLoader publish：1,263,093 bytes，SHA-256 `B9132F168DF8F0985B1EF022290633A5440E0F1526CE12815B54A7F0469C382E`，Compiler 0/0；11,187 个 AVM1 函数最大 50,569 bytes，均小于 60,000 硬门。只发布注入逻辑，不编主 XFL，不改根主 SWF。
- 生产地图 52/52、Tasks 63/63、Stage Select 57/57、panel contracts 70/70；实际隐藏／禁用 GPU 的 WebView2 启动投影、snapshot v4 作者 DTO 和来源隔离 3/3。Workbench `--release-tree --strict-warnings` 为 0 error／0 warning，文档治理通过。
- 地图布局、110 个 WebP／103 引用／零 PNG、18 分层／324 缩放用例通过；三项既有清晰度债务保留，未扩张豁免。
- 周边接线 215 项、发布状态分类 48 项、runtime v2 构建框架 184 项、发布策略契约 69 项通过；后两者是框架夹具，不是本阶段 40/40 production policy receipt。面板运行时 41/41、既有工作台 lazy 依赖闭包 28/28 通过；22 份本轮现存 AS2 源的 UTF-8 BOM 与工作区／索引 diff check 通过。

本轮 CS6 首次测试发现 AS2 不接受 `dynamic` 对象字面量键，已改为括号写入并重跑通过；启动 runner 的旧 89 项计数在新增地图失败门后同步为实际 91 项。首次 asLoader 的 compiler 虽为 0/0，独立尺寸门仍发现 `f36_9` 达 60,843 bytes，因此该产物未作为最终候选输入。已在战宠进阶的“影子刺客”定义前机械分文件，保持原定义／数值／加载顺序，并重发上列最终 SWF；新鲜 ManagedLonggun 126/126、DressupReferenceManager 与 EventBus 回归通过，Compiler 0/0、无 32K retry。现存证据副本在 `tmp/map-workbench/phase2-verification`，不提交临时二进制／日志。

最终补充门：冻结 v1 回执／HUD 投影与改名层级筛选 18/18，打包 runtime 配置 3/3；当前 v2 内容只读验证为 4 页、40 地点、240 任务与 28 个发布 SWF 来源。初始驻点仍是 legacy 适配，未绑定驻点不会混入“真实 NPC 绑定全部就绪”的统计。HUD 对照发现的未定义取景变量已修复；地图 QA 的 DOM／canvas 双绘断言改为等待同一绘制 revision，保留原计数与 1.5 秒上限，最终 52/52 通过。

## 第二阶段隔离候选交付（2026-09-07）

统一机器门已完成，`automation/dev.ps1 -ForceBuild -BuildOnly -CandidateLeaf map-phase2-v1` 构建并选中了本地候选。源码仍是 `3ac9227cbdfdeb6d73f99e408414b03bfc338b5d` 之上的未提交工作区，不冒充 immutable Git-tree 发布请求。

- 候选目录：`tmp/runtime-candidates/v2/map-phase2-v1`；Core DLL SHA-256：`E7A1116F5FE7396A9CC2EAFFA54B464FA59E0408DF3AD378C4C177D66E28BF32`。
- build identity：`2A5AD34B68FE41A2744AF20A694D52CF11D6DBDA7222457854FF6C800A2F79CF`；33-file payload closure：`55FEC9A514A60035ACD965BE99A6F39E513144CDED286D14E1F485ABCE8B91F0`。
- 严格文件完整性检查返回 `integrity-only / mode=worktree / files=33`；本地开发 active pointer 已绑定该候选。准确状态为 `candidate_built / NOT_DEPLOYED`，没有自动启动游戏。
- 正式根 EXE、`runtime/**` 与根主 SWF 保持原字节；正式 Core DLL 仍为 `B1BD5DF242271C8520369555BC8889E48AF8063BD0600D6B12A17C90A2ABF45A`。本轮新 asLoader 与 v2 数据属于工作区第二阶段内容，不能与旧正式 Core 混用。

验收请使用根目录 `本地开发启动.cmd`，进入“其他 → 工具 → 地图工作台”。应用／撤回后退出整个启动器，再用同一 CMD 重开；只返回标题不重新加载进程内地图内容。下一步由人类完成设计 §7 的三个自然判断：新地点玩家入口、NPC 剧情迁移、复制与安全撤回。没有提交、推送、云端构建或 promotion，未宣称 `candidate_executed / e2e_verified / promoted / standard_entry_verified`。

浏览器与作者测试服务已关闭；生产任务 JSON／玩家存档没有被界面测试修改。独立副本 `E:\cf7-map-ui-fixtures\ui-e4d91ef07f5248abb760bf465346166d` 与 `tmp/map-workbench/phase2-verification` 的证据保留。首次长路径副本失败目录 `tmp/map-workbench/fixtures/ui-b1ba4ae468be442685816b8d2e8eeaa7` 的清理未执行，未绕过限制；其中可能含源文件硬链接，不应编辑或作为可写测试副本。日后复跑按工作台说明重新创建独立拷贝即可。
## 帮助可用性与上游同步（2026-09-07）

按“帮助过于简略、需要追上上游并处理兼容”的反馈，本轮把六条概念提示改为[地图工作台操作指南](../launcher/web/help/map-workbench.md)：十个可搜索主题，从“移动基地屋顶、对比保存前、撤销末步”的零项目写入练习开始，覆盖对象区别、建页／接场景、条件与 A/B、人物迁移、任务端点、素材、应用／撤回和排错。顶栏唯一 `?`、左侧入门按钮和对象说明均进入同一共享 SecondaryPage；正文只读，支持键盘／Esc／焦点归还，右上关闭仍关闭整个工作台。另提供直接“编辑 B”，不再要求交换 A/B 选择器。

本地 `main` 从 `3ac9227cbdfdeb6d73f99e408414b03bfc338b5d` 快进至 `82d61d55abc17cb7e626ced888c7f6a6e96ef460`，吸收 17 条提交、571 个文件变化。本轮没有新建源码提交或推送；第二阶段未提交施工保留。与原施工重叠的五个路径为 testing guide、asLoader SWF、塌缩生成文件、NPC 初始化帧脚本和打包配置；SWF 二进制冲突以合并源码后的重新发布收束，塌缩文件由既有生成器重建，打包中的上游 `docs/story` 层保留。

兼容收束：

- 上游新增 `任务需求支线` 保留未接管 NPC 的历史完成门槛；显式工作台接管的驻点与原主线门槛一样，只消费本次 C# presence 许可，避免再次被 AS2 旧门槛拦截。操作指南解释绑定前需明确迁入原剧情条件。
- 新增三项任务纳入原任务源与派生目录；当前只读验证为 243 任务／4 页／40 地点／28 个发布 SWF 来源。原任务 JSON 与上游保持一致，地图定义仍为 `81E08FD4B58C20E1E47124CF5327139827DBEF50EE3F3D638E07E9966B741041`，没有改剧情数值或地图布局。
- 空且无未知写入的浏览器草稿自动接纳新基线；非空草稿与未知写入保留，内容变化时提示核对。读取阶段先恢复草稿再请求目录，目录失败不再抹掉保存的草稿，失败原因在界面中明确显示。

新鲜验证：帮助的十主题／搜索／对象定位、inert／Tab／Esc／返回／面板关闭、选择与画布不丢、独立编辑 B、三个视口、失败重试／迟到响应、空／非空／未知草稿与目录失败全部通过，零业务写入；原三条作者 UI 流程通过，最终草稿修复后再次通过多文件应用／重开／丢响应只查询／精确撤回。Workbench strict 为 0 error／0 warning，panel contracts、文档治理通过。C# 全量为 4,765 通过／3 项既有 skip／0 失败，实际串行；冻结规则 8,921 断言、回执／HUD 18 项与 NPC 初始化源码路径 9 项通过。

新鲜 CS6 为 MapDomainBridge 19/19、BootSequencer 91/91＋Handshake 12/12、ShotTimeline 37/37＋Qjz171LiveAnimation 15/15，以及 map-loot 698/698、BoxInteractionArbiter 13 cases／53 assertions；均 compiler 0/0、32K retry=0。最终 asLoader 1,265,323 bytes，SHA-256 `61E4E1A27E9003A092F2E8B09F06D4BCB5453346434976736177A9997D0F3782`，Compiler 0/0；11,197 个函数最大 50,569 bytes，未触及 60,000 硬门。上述不代签真实 NPC 互动、战斗或新任务旅程。

本轮没有改变 C#／原生输入字节；`automation/dev.ps1 -ReuseOnly -BuildOnly` 已精确复用并重验前述 `map-phase2-v1` 的 identity／33-file closure，正式 EXE／runtime／主 SWF 未变。仍以 `candidate_built / NOT_DEPLOYED` 交付，请完整退出并用“本地开发启动.cmd”重开后看新版帮助；没有自动游戏执行、正式 promotion 或第二阶段人类验收结论。

同步前 134 个现存文件原字节、11 处删除清单与 patch 备份保留在 `tmp/map-workbench/upstream-sync-a6eb644f585446c2aa497fac6548ad16`，具名 stash 为 `c5780b54b23ea03100638aa45e31bfed1be40a00`；只作为恢复依据，不应直接覆盖新的合并工作。新验证副本为 `E:\cf7-map-ui-fixtures\ui-c5a567ad591c44289bea1421cf09d3f5`，图片与编译证据分别在 `tmp/map-workbench/help-verification`、`tmp/map-workbench/help-upstream-verification`。未新建 Git worktree。

## 旧草稿冲突提示修复（2026-09-07）

维护者留下的一项旧草稿与更新后的项目不一致时，读取链保留草稿并停止预览，但原画布仍空白、字幕仍表示等待剧情投影，造成“还没加载完”的误导。本轮只修改 Web 状态呈现、对应测试与说明：画布直接显示冲突原因、保留数量和“导出保留的草稿／放弃试用草稿并加载当前项目”；不再发送旧草稿预览，编辑、导入、应用与方案调整暂时禁用。若有未确认写入，则先查询原操作，不能直接丢弃。重开时也清除上次残留的状态文字。

独立副本的 `test-help.js` 已验证最小／大视口按钮完整可达、明确暂停文案、导出保留原摘要与操作、关闭重开保留草稿、冲突期间零预览请求、显式放弃恢复生产预览，以及未知写入的原操作查询；原编辑／多文件应用／重开／丢响应只查询／原字节撤回流程亦通过。Workbench strict 为 0 error／0 warning，文档治理与 diff check 通过；测试服务已关闭。图片在 `tmp/map-workbench/help-verification/conflict-*.png`。这些证据不代替实际 WebView2 目视验收。

维护者真实浏览器中的一项旧草稿未被清理，继续作为验收样本：完整退出并用“本地开发启动.cmd”重开，确认画布出现明确冲突与处理入口，再由维护者决定是否导出和放弃，检查预览恢复。本轮未修改地图定义、任务源、asLoader 或 C#／原生二进制，未提交、推送或正式发布；没有自动执行游戏或代签人类验收。

## 保存关闭恢复与地图回包超时修复（2026-09-07）

维护者随后完成两次实际应用，第二次包含六项场景图块操作。记录显示工作台于 10:26:22 关闭、后台于 10:26:23 完成保存；磁盘文件与批次后像一致，但浏览器仍保留该操作的未确认草稿。重开逻辑原先先判断内容摘要变化，因而在查询原保存前就进入冲突态。现改为先查询原操作：已应用则采用已保存内容，未完成仍保留，确认未写入且基线未变则恢复可编辑预览；不重放写入，不弱化旧面板迟到响应隔离。

截图中的“启动器等待地图状态响应超时”是另一个缺陷：日志中状态包约 255 ms 已返回，但实际 asLoader 的 `MapPanelService` 将外层局部编号编为寄存器、回调却按变量名读取，wire 因此省略 `callId`，Host 无法匹配原请求。snapshot／导航均改用独立回调工厂形参保存 Host 数字编号；不放宽匹配、不延长超时，也不改地图布局或导航条件。

新鲜证据：旧实现被新增 CS6 回包测试复现五项失败；修复后地图桥 26/26、Compiler 0/0、32K retry=0。C# `MapTaskResponseTests` 5/5 通过，覆盖缺编号不能消费等待项、正确编号映射、乱序／重复、导航关闭结果与最大正整数。独立副本的帮助／冲突回归和真实后端编辑流程通过，包含面板先关闭、后台随后写完、面板重开／整个文档重建自动查询，以及迟到旧返回不能清掉新草稿。当前已编辑地图的生产 headless harness 52/52 通过；这些机器证据不代签实际游戏入口验收。

asLoader 最终发布为 1,265,358 bytes，SHA-256 `ABA433493B6B6445DB96BE1BD58844F73751C320A1FA44C63D9E5D18F4AB50DC`，Compiler 0/0；11,197 个函数最大 50,569 bytes。独立产物检查确认新回调工厂参数落在 activation scope（register 0），不是仅修改源码。首次 publish 曾报告无法创建 SWF，刷新门判失败；排除只读／持续占用后重试成功，未把首次 marker 当成功。红／绿 trace、发布输出和原产物反汇编保留在 `tmp/map-workbench/response-diagnostic-cb7918ccbea2425b9bc790cd3477d2b0`。

维护者当前地图定义 SHA-256 仍为 `D3677CCB239516DA85DE2F23A131F202EAE616B73B9FFBB57A27EC2C4531C430`；实际浏览器草稿与应用记录没有手工清理。下一次用本地开发入口完整重开后，应由工作台查询已完成的原保存，再检查生产地图加载与地点点击。此轮产品改动仅 AS2／Web，另增 C# 消费者测试；没有修改 C# 生产源码、根主 SWF、正式 EXE／runtime，没有提交、推送或 promotion。

收尾验证：BootSequencer 91/91＋Handshake 12/12、map-loot 698/698＋Box 13 cases／53 assertions 通过，均为 fresh trace、Compiler 0/0、32K retry=0；Workbench strict 为 0 error／0 warning，文档治理、AS2 BOM 和 diff check 通过。`automation/dev.ps1 -Status` 只读核验为 `ready`，唯一匹配仍是原 `map-phase2-v1` identity／33-file closure，不需要重建 C# 候选。独立测试服务已关闭；诊断导出清理被环境拦截，目录原样保留，未绕过限制删除文件。

## 创作视图与玩家预览分离（2026-09-07）

维护者确认前述修复可行后，继续反馈切到其他地图仍像停在 A兵团。当前模拟是初始进度：C# 正确隐藏未开放页面，但原作者画布沿用玩家隐藏策略，页签、分层和图块消失，缺少可编辑内容与明确解释。另在回归中复现父子页反馈竞争：子文档的迟到重绘报告被误当成新选页，覆盖外层编辑目标。

现每次打开默认“创作视图”，允许选择全部页／分层并编辑隐藏图块、地点与头像，以文字／虚线区分当前方案隐藏；“玩家预览”画布只读，仍按同一 C# 结果隐藏。整页隐藏显示实际页面名称、原因和返回入口；仅当前分层隐藏时只覆盖画布，其他可见分层仍可切换。未确定的外观只用基础图／占位，室友未知性别不猜测。模式切换保留草稿、模拟、相机与分层，不构造全解锁快照或放行导航。父层选页现在只接受明确点击，旧重绘不再改选页；单击边框只选中，达到拖动阈值才记录位移。

新 `test-view-modes.js` 已用独立副本的真实 C# 后端／生产 MapPanel 验证初期隐藏内容可创作、完整页与局部分层隐藏提示、其他可见分层可切换、页签与迟到重绘隔离、单击零改动与拖动、A/B／相机／草稿不变、同一 snapshot／导航许可不变、未知头像占位，以及 1024×576、1600×900、2560×1440 的按钮、文字、分层滚动与无横溢出；没有 apply。截图在 `tmp/map-workbench/view-modes`。旧作者 UI、多文件应用／撤回、关闭后保存与面板／文档重建查询、迟到旧响应、NPC 固定／跟随端点、帮助与冲突流程均已重跑通过；副本内容原字节恢复。

维护者当前已编辑地图的生产 headless 回归 52/52 通过。隐藏、禁用 GPU 的真实 WebView2 启动投影／创作子文档／跨来源隔离 3 项通过，编译阶段仍报告 WindowsBase 引用版本解析警告；未运行 Flash 游戏旅程。本轮产品改动仅 Web，另同步测试与帮助／canonical 文档；未改实际地图定义、任务源、作者真实草稿、AS2／SWF 或 C# 生产源码，不重建候选 runtime，没有提交、推送或 promotion。前述维护者“可行”不外推为本阶段三条自然旅程全部验收。

本轮 Workbench `--release-tree --strict-warnings` 为 0 error／0 warning，文档治理、所改 JavaScript 语法与工作区／索引 diff check 通过；当前 Web／帮助文件与验证副本逐个哈希一致。候选 Core 仍为 `E7A1116F5FE7396A9CC2EAFFA54B464FA59E0408DF3AD378C4C177D66E28BF32`，无需因本轮 Web 呈现修改重建 runtime。

## 第二阶段发布准备与上游集成（2026-09-07）

维护者确认本轮修复可行，明确授权跟进上游、取得云端共识、构建部署并推送。作者流程确认与发布授权不代签设计 §7 的全部真实游戏旅程；最终发布身份和事后审计另由运行库记录绑定。

本地从 `82d61d55abc17cb7e626ced888c7f6a6e96ef460` 快进至 `b4299ac93c876696056ddcbc60f5ee7023f36244`，合入 10 条提交、136 个路径变化，包含撤退结算、焦点持续录制、武器／头像与派生目录。144 个现存施工文件的原字节与 11 项删除清单先行保存在 `tmp/map-workbench/release-sync-885882171e81406dae06bc293852be45`，并以 stash `1ada88c797ab9ebb3a3097f87ba137b40a5feca8` 保留；没有新建 Git worktree。未触及上游的施工文件恢复原字节，避免 Git 换行转换改变作者草稿基线。

10 个重叠路径逐项合并：Host 保留地图域注册与上游焦点记录；StageRunSession 保留 C# 准入、撤退完成隔离和 rolling 观察。任务目录按当前原任务／物品及 C# 地图域重新派生，未手选某侧 JSON；测试计数合并为 StageRunSession 460。asLoader 二进制冲突通过合并源码后的 CS6 发布收束，不选旧 SWF 作为最终产物。

新鲜 CS6 证据：map-loot 739/739（267＋12＋460）及 Box 13 cases／53 assertions，runId `88a6a896cdba4ae793e382e6c07525cd`；地图桥 26/26，runId `91b47b92cc97433ea209fb810783419b`；BootSequencer 91/91＋Handshake 12/12，runId `20c797aeba9048c3be7fed25f1d915e9`。均为 Compiler 0/0、32K retry=0；asLoader 发布为 1,265,591 bytes，SHA-256 `8ED7099339ED6EF9771EEEFE6C76D19C12BBF3B8F8B5B3F6FF740C8804763ABF`，11,198 个函数最大 50,569 bytes，未触及 60,000 硬门。产物与 trace 分层记录，不把 publish-only 当行为通过。

本轮发布开始时备份与最终磁盘地图均为 `35ACC393F4FF7DDBBE6E29720A383ECA989D680EED4A0D00A9485DF115C45B33`（178,908 bytes），未清理真实浏览器草稿、存档或作者应用记录。上文 `D367…` 属于前轮修复时点，不作为本轮发布字节；收尾按本轮备份 manifest 逐字节确认。正式构建重新冻结本轮源码并取得注册本地 X509 与 GitHub OIDC 的同身份／同闭包证明；先前 `map-phase2-v1` 只作为开发候选历史，不复制到正式 runtime。

第一版发布源 `e131182909b9190659b13ce8b27af8a958b57a01` / tag `runtime-build-v2/20260907-map-workbench-phase2-v1` / request `9ADF3B2998DF76CD8B7522D0CBEB481316FE6C4E1B5B907430FF502D82EB0245` 已取得本地 `builder-local-a` 与云端 [run 34097840213](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34097840213) 的同身份／同闭包证明，以及 production policy 40/40；但未部署、未推主线。云端查询的一次 TLS 超时通过同一 run 续取恢复，没有重复 dispatch。

该版完整 Launcher 回归为 4,791 通过／1 失败／3 跳过；失败位于未改动的 `CommitSettledLog_TimeoutRequiresReconcile`。生产 `HandleTimeout` 先回包、后记录日志，旧测试只等回包便断言日志，存在定时器线程竞争。只修测试等待点，同时等待回包与日志；保留原 2,000 ms 上限、唯一回执和全部字段断言，不改业务顺序或放宽通过条件。修复后该用例独立连续 10 次通过。为保持完整发布树绑定，第二版采用新 source/tag/request，旧标签不移动，旧证据保留；前三域不变时复用经重验的本地签名 CAS 产物，重新取得新源的政策 receipt 与云端证明。

修复后的 canonical Launcher 全量为 **4,792 通过／3 项既有跳过／零失败**，1.4968 分钟，实际 testhost 再次确认关闭测试集合并行；完整日志保留为 `tmp/map-workbench/release-verification-88588217/launcher-full-v2.log`。第一版失败日志原样保留，不用重跑结果覆盖首因。

## 第二阶段正式共识与部署（2026-09-07）

最终 source `dfa156c8cd7e860c1e5795c7987525b76829635f`、tree `c04f1f3421ba6bb5d3cb9f86d1818c4862e7625c`、tag `runtime-build-v2/20260907-map-workbench-phase2-v2`、request `B60F0481BE4672C7CBC2AB8021537EF734B503C639D888FC7E0EE7C8D13A00F2` 已完成 **40/40 production policy + 2 signers / 2 faultDomains + 原子 promotion**；根 bootstrap 的有界完整安装 `--verify-only` 通过。build identity `FF623FA9C4191B54BC2DA05868B77E127BEB58B51EEA3147B4A62BC44BC0B387`、33-file closure `5366AA50BAEE0CDBAE6FED735EF0DA6A76C9627AA74891FD1E83D43B9FC13126` 与最终 Core `82CE7EA76F8420B4CB47DBBAF4A3A04868E0B51E2CC89F2B8ABB7EF7E116A498` 全部匹配。云端 [run 34099722252](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34099722252) 的新源证明与本地 `builder-local-a` 签名在 promotion 中统一重验。

第二版的本地政策 receipt SHA-256 为 `D0D02A023746369B1795B6340F627A6F3A6ACB19957F6B2AF52A2FB89BD0CBE4`。源码修正只改变测试／政策域，运行时产物与第一版逐文件相同，但第一版证明没有冒充第二版 source/tag/tree；旧 request 已 supersede，旧标签保留。合并后另复跑生产 Map **52/52**、Tasks **63/63**、Stage Select **57/57**。详细身份、时间及备份路径见[运行库发布记录](runtime-build-reproducibility.md)。

当前为 `promoted`，普通正式启动器已具备第二阶段代码；`本地开发启动.cmd` 保持开发入口语义。没有自动启动游戏或执行新地点／NPC 迁移等完整旅程，不称业务 `standard_entry_verified`。没有新增 Git worktree，注册 worker 的隔离 checkout 已清理；原始施工备份、签名 CAS 和回滚文件保留供恢复。

部署提交 `2fe7c95ae62a1347a6b7effd0d77be63e3987fec` 已快进推送；首次远端 [Audit 34102630481](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34102630481) 于 `2026-09-07T08:54:09.8812714Z` 报告 `state=promoted / deploymentChanged=true`，33-file closure 和双 signer／双故障域通过，`run_attempt=1`。收尾时 HEAD 与 `origin/main` 一致，索引／工作区干净，仅保留主 Git worktree；本段后续只补文档审计记录，不改正式产物。发布开始时地图备份与最终字节均为上列 `35ACC…`，没有清理或重放作者真实草稿。
