# 地图资源箱 S1/S2 真实战利品容器与 Web 双栏 ADR（2026-07-18）

**文档角色**：冻结首个真实奖励 canary 的 AS2 权威、Host/Web wire、失败恢复、生产选择器与退出门；不是全箱型铺开或生产 runtime 已发布声明。

**最后核对代码基线**：正式 promotion commit `6218f8b1d82efc57b77131616667fe45f3033297`（2026-07-22）；immutable tag `runtime-build-v2/20260721-map-loot-canary-v1` 指向 source commit `c60aab2386aee4516608397373ae4c59148c5f77` / release tree `e1e6e059f148a640b3e37cbf1d912f297156d956`。FCA19/B2AF/231388 与 `c-2a0cddb077b7-08846e81b3-20260721t100014612z-f32a40a3` 均保留为 promotion 前的历史 candidate；后者曾把唯一 canary 推到 `e2e_verified / NOT_DEPLOYED`，现已由同身份正式 promotion 与 standard-entry smoke 取代为当前发布证据。

**状态**：`APPROVED_S1_S2_CANARY_SOURCE_SLICE`；本 ADR 唯一授权的 `夺取材料.xml | SubStage 5 | Instance 0 | unlockPolicy=skip` canary 已达到 **`standard_entry_verified`**，但其余 S1/S2 资产与箱型、S3 Lockbox、S4 旧 UI 退役均不在本次结论内，整体迁移仍未完成。正式 request `FBA91942C57DC3E6662AB77AC88EEAE3CA2AFA3C82BF5FFEA6BC68CFD6C31AE4` 由 `builder-local-b / physical-host-b` 与 GitHub OIDC / `github-hosted-windows` 对同一 build identity `2A0CDDB077B760328B3141EFFFEEE3996841FA1CE49AD09E5D7339417F60A107`、payload closure `3B837DCDBC69AA47074E635DACACAE3B80263023E6032AC1FDF209768B1C150C` 形成双 signer / 双 faultDomain quorum；production receipt `55C7256E98393A01F87E51D880E37FEF2078379748D8BFCA8772AA112AAD2C4D` 通过后，于 `2026-07-21T16:39:47.2329893Z` 完成 promotion。promotion 后的 clean checkout 以 `formal_runtime` 加载 Core SHA-256 `0F58BF864B8DE9C7FCEA098D7E1EEA1996BDE38D85D87E844B047B53F5247232`；槽 `cf7_agent_loot_standard_entry_v3` / attempt `4eae1360cedd413fa3175db6a8997158` 取得 fresh handoff、真实 title-frame marker、exact loaded，随后经正常任务路线打开 canary，完成两次 claim、terminal close / unpause 与 `SaveManager.saveAll flush=true` + shadow confirm。较早的 `title_frame_not_observed` 探针已被这次 prewarm-held formal attempt 成功取代，不再代表当前 standard-entry 状态；本轮没有新增完全重启回读，冷启动持久化仍只引用 promotion 前的历史旁证。

实机 attempt `62f04fa249c44d94ba3171f619e61754` 使用 `panelInstanceId=panel.loot.8a1227b8229f4eef99ac052395e4e12b`：抗生素并入已有堆叠、黑暗吉他因满背包保留；organizer 中真实确认丢弃牛肉罐头 **×16**，背包 **50→49**；返回前 fresh `ACTIVE` snapshot 成功且零 close/rebind，随后领取黑暗吉他并自动回游戏。第一次真人点击还暴露 `SecondaryPage` 把 `.workbench-modal-layer` 纳入 underlay，令 modal 自身 `inert + aria-hidden`；旧 harness 的程序化 `.click()` 因可绕过 inert 而假绿。修复后 `secondaryUnderlay()` 排除 modal layer，modal FocusScope 只 inert shell 其他直接子节点，并由 browser **62/62**、focus integration **12/12** 与真实一次点击共同验证。

证据绑定必须区分 native 与 Web：该 candidate 的 25-file native bundle 不含 `launcher/web`，WebView2 从当前 `projectRoot/launcher/web` 读取外部文件，故 native build identity / payload closure / Core 不能证明 Web 修复字节。当次真人 E2E 运行副本另绑定下列换行敏感 raw-byte SHA-256；这些值只绑定 2026-07-21 实机使用的原 dirty 工作树字节，不应当作任意 checkout 的当前 raw hash：`workbench-components.js` `D3D9A5A4CCBE1303259ECD0FCEDABB5C7CE79E9562287473EC895A3FE75B65A4`、`workbench.js` `E6AB04C4EF4045C955E9009BE27F29D99679743D29A88DDDDEEBC6A1B141D7D1`、`loot-panel.js` `A1DEECD08BEE63CB5CAF6C226D2B326BE5ADE2C399F88F29AD2953D0AAD29E10`、`loot-organizer.js` `F85A43609B4C0194F6469C15510A0CD7C051CFC56ADFE0353B6D0EDB35C05905`、`loot-view.js` `B323D52DBE7C75BD5F32FEA6BF9CC420396E1C7A42CB3F5B21611E292EFA910B`、`launcher/web/modules/loot/dev/harness.html` `F6252EF9B549F08719AB0DE22FC8E3AA32F490E536C24E84E8D4FDCE5595E859`、focus integration test `6490DCAFD46A694F4C76AC10C61281CCE8162D6A6D87124CF260FBE2269A33C7`。 换行可移植性复核：七个文件均无 lone CR；逐字节执行 `CRLF→LF` 后，原真人运行工作树、当前短路径发布工作树与 HEAD Git blob 完全一致。规范化 SHA-256 / Git blob OID 分别为：`workbench-components.js` `B8FC6A1CB17C55F41DEF72F15085598C69ECC4F2A11C7058127477135DD8966E` / `dd1ad88c1b08bf06ecadf84e2d31d16ccb339ff7`；`workbench.js` `5459DDEE021674924A9A0767F1459AC20224269790AAD396C9BF8E302127EC9E` / `24eb9ea68bc40f1813503481da3c53ba6287062e`；`loot-panel.js` `59590D3B997EFB6B51A546783459658D70F042FF6FD430450E26A767DF64A10A` / `e66a7540fbfb2ace8411e4141a53ba3bf01e70eb`；`loot-organizer.js` `F85A43609B4C0194F6469C15510A0CD7C051CFC56ADFE0353B6D0EDB35C05905` / `d240c85e20f0fe78974f1f8bc04c5508841fbdf3`；`loot-view.js` `6A7A5259DC2D128283FEFF331DA93DA15A44FB939C79614052549ECD963FAC2D` / `cf317a30307d6f3ea584ce03e3df993296e367f8`；`launcher/web/modules/loot/dev/harness.html` `4ABE66096090FA1E63141ED592A4005476D26A1CC0EED5CC59BBBBCC2B7258AE` / `c7cf63f705cbe71d33748c18c847fe9c4c78c558`；`tools/test-workbench-focus-integration.js` `B8CF3CB0CC7636683197A7D3CB7201FC5FF9825C9B95BF54B2477EBE233C5B74` / `2752796df5bad9d36f099715ecabc3658c8e5f4a`。这证明真人 E2E 使用的源码与 HEAD 具有相同的 Git 规范化文本身份，但不证明新 checkout 的 raw bytes、正式 promotion 或 standard-entry。

上述 raw-byte / Git blob 段仍只描述 promotion 前 candidate 的历史证据。正式发布的 native payload 仍不包含 `launcher/web`；外部 Web 文件改由 immutable release tree 与 production policy receipt 绑定，其中 `required-web-runtime-assets` 已报告 **178 path(s) present**，随后 clean promoted checkout 的 WebView2 standard-entry smoke 实际打开 `loot`。因此可写“release tree / policy + formal smoke 绑定了 Web 运行字节”，不可写“native payload closure 包含 `launcher/web`”。

**父级治理文档**：[地图资源箱 → Web 战利品工作台与开锁流程·建设治理候选](地图资源箱-Web战利品工作台与开锁流程-前期调研-2026-07-17.md)

## 1. 纠错结论与范围

截图对应 `data/stages/副本任务/夺取材料.xml | SubStage 5 | Instance 0` 的 `装备箱`。两条掉落区间 `强化石 5..10`、`强化石 25..30` 与画面中的 6、29 完全一致。它没有 S0 双 marker，而 S0 又强制只接受无奖励 `保险柜` fixture，因此旧 AS2 `装备箱` 小窗是现有实现的确定结果，不是 Web 加载偶发失败。

本切片只处理：

- AS2 一次物化、会话内可寻址的瞬态 loot container；
- `loot` 专用 Host route 与 Web “背包—战利品箱”双栏；
- 单向领取、权威关闭、明确放弃、丢 ack 只读对账；
- 初次 open 的 Host/Web transport failure 才可把同一个 `ArrayInventory` 交给 claim-only 旧 AS2 renderer；accepted reopen failure 必须恢复 `LOOT_SUSPENDED / CONSUMED / EXPIRED`，禁止再次弹旧 UI；
- 截图所示单个装备箱的显式 `skip` canary；
- 生产 selector、协议、领域与回归自动门。

本切片明确不处理：

- S3 Lockbox 生产开锁；
- 其余八个网格箱的铺开；
- `row/col == 0` 的资源箱、纸箱、隐藏资源点；
- 旧资源箱 UI 的物理删除；
- 绕过 runtime v2 request、不同 signer/faultDomain quorum、policy receipt、strict verifier 或原子 promotion。

## 2. 不变量

1. 奖励只由 AS2 滚一次，Web 与 Host 不接收可重建奖励的真值。
2. 精确 rollout 目标在完整预检和物化成功前不 kill；物化结果先挂到 `LOOT_COMMIT_PENDING`，再提交唯一 opening kill。
3. `chestSessionId ↔ lootContainerId ↔ ArrayInventory` 一对一；第二个 active flow 不得覆盖第一个。
4. 普通物品移动时保留原 `BaseItem` 对象引用，包括 `level/tier/mods/lastUpdate`。
5. 材料、情报、金钱、K 点等特殊资产必须先完整预检，再以可回滚事务提交；目标不能完整接收时 source 整格不变。
6. 只允许 loot → 玩家权威域；背包 → loot、swap、任意目标槽伪造和普通 discard 均拒绝且零副作用。
7. `close v1` 不扩 wire：`remainingCount<=0` 优先归一为 `CONSUMED`（包括 `abandon=true`）；非空 `abandon=false` 为非终态 `LOOT_SUSPENDED`，非空 `abandon=true` 才为 `ABANDONED`；场景销毁只有在无未决 journal/effects/transport flag 且可安全收束时才是 `EXPIRED`。
8. 写入 timeout、断线或回包损坏不得自动重放 claim/close，只能查询跨过相应 operation identity 的权威状态。
9. Host/Web 失败只允许恢复同一个已物化容器，禁止重新读取或重滚 `target.掉落物`。
10. 未标记网格箱、所有直投箱与未实现的 `lockbox-v1` 继续既有路径。
11. 物化前冻结本 canary 唯一 outcome bonus（`逆向`）和 exact PRD engine；数量 `span=max-min+1` 必须不超过 AVM1 `random(n)` 的 `2^31-1` 上界，超界在任何随机推进前 fail closed。
12. claim 的 destination 已落地、source 删除失败且无法证明回滚时必须保留 exact pending journal；Web/Host 不重放写，只以 causal `lootQuery` 前滚。scene cleanup 也必须先完成或证明回滚，不能直接清 journal。
13. 普通 X、Esc、backdrop 都是 `close(abandon=false)`：非空箱先形成权威 `LOOT_SUSPENDED` 再回游戏；只有“放弃剩余”及二次确认可发送 `abandon=true` 并进入 `ABANDONED`。
14. recovery control、detached proof 与普通 Web query 是三条独立 wire：严格八键 recovery、严格九键内部 proof query、严格七键普通 query；proof identity 不得投给 Web，也不扩 AS2 15 键 / Host 20 键 response。
15. 初次 open fallback 只是 claim-only renderer：图标点击必须进入 `LootContainerService` adapter；背包→loot、swap、discard 与 legacy direct write 全部拒绝。same-anchor reopen 的任何失败都不得调用 renderer。
16. Host/Web 各自维护独立 freshness watermark；只有 matching pending call/identity 的畸形或未证明更高 revision 才抬高该 unknown operation 的水位且不覆盖 known authority，其他 call/identity 不得抬高，后续 proof 不得低于该水位；普通七键 query 不增加 `reconcileAfterCallId` 等 wire 字段。claim success 必须精确证明 revision `+1`、remaining `-1`、operationId 命中与 requested slot 为空；close success 必须精确证明 revision `+1`、本次 operation 与 close 意图对应的 expected state / remaining。capacity failure 只有 strict raw failure shape、exact current binding 与完整 known prestate 同时成立，且已知 revision、remaining、lastAppliedOperationId、`closeLease`、`containerVersion`、requested slot 与 slot lease 全部不变时才可跳过；错误名本身不是证明，Host 只在该 exact prestate 下保留已知 `closeLease/containerVersion`。
17. scene teardown 是 authority barrier：`SceneManager.removeGameWorld():Boolean` 在 `expireScene()` 未成功时必须在任何 inactive/dispose/dispatcher/gameworld teardown 前返回 `false`；淡出时间线停止且只排一个下一帧重试，pending 收束成功后才继续播放。`cleanupForRestart():Boolean` 也必须在任何 manager dispose 前先做 loot preflight，失败时保持整个 runtime 原样，禁止半清理。
18. ordinary `target_full / inventory_full` 只打开同一 tracked `loot` panel 内的嵌入式 organizer。子页全程保持 exact `panelInstanceId`、Coordinator `Bound`、原 pause lease 和 AS2 `LOOT_ACTIVE`，不发 `lootClose`、不切换 panel、不借道 AS2 HUD。库存 settled 后仍必须取得 fresh `ACTIVE` loot snapshot 才可返回主页；失败留页且绝不 replay 写操作。

## 3. 生产选择器

首版只接受目标对象自身拥有、类型为字符串并全等匹配的三字段：

```text
chestRolloutId = <ASCII 唯一 id，1..64>
lootFlowProfile = web-loot-v1
unlockPolicy = skip
```

同时要求：

- `presetName ∈ {装备箱, 保险柜, 生存箱}`；
- `row/col` 是正整数，`row*col` 与 preset 容量一致且不超过 64；
- rolloutId 在所有真实 XML 中唯一；
- 至少一条掉落规则，规则数不超过容量；
- 每条规则有已知物品名和合法正整数最小/最大数量，且 `最大数量-最小数量+1 <= 2147483647`；
- 不与 `chestS0FixtureId/chestS0As2GateId` 共存。

缺字段继续 legacy；出现部分字段、错类型、未知 profile/policy、重复 ID、direct 型误标或非法掉落配置则 fail closed，不 kill、不滚奖，并输出可诊断原因。不能用 `row/col` 单独推断箱型。

当前唯一 canary：

```text
file: data/stages/副本任务/夺取材料.xml
subStage: 5
instance: 0
preset: 装备箱
chestRolloutId: loot-canary-material-5-0-v1
lootFlowProfile: web-loot-v1
unlockPolicy: skip
```

## 4. AS2 权威状态机

```text
AVAILABLE
  └─ exact selector + no active flow ─→ LOOT_COMMIT_PENDING
       ├─ 未物化且不可恢复的 pre-commit 失败 ─→ EXPIRED（不暴露半份奖励）
       ├─ 初次 open 已物化后的 kill/activation/transport 失败 ─→ LOOT_ACTIVE + claim-only same-object recovery
       └─ own-kill + open-frame 成功 ─→ LOOT_ACTIVE
             ├─ remaining <= 0 + close ─→ CONSUMED
             ├─ remaining > 0 + close(abandon=false) ─→ LOOT_SUSPENDED
             │    └─ same scene + exact target anchor reopen ─→ LOOT_ACTIVE
             │         └─ accepted reopen transport fault ─→ LOOT_SUSPENDED（空→CONSUMED；anchor 失效→EXPIRED；无 legacy UI）
             ├─ remaining > 0 + close(abandon=true) ─→ ABANDONED
             ├─ safe scene/authority teardown（无 pending）─→ EXPIRED
             └─ initial-open Web/Host detach ─→ LOOT_COMMIT_PENDING + persistent transportDetach
                  └─ exact journal/effects/renderer/pause proofs ─→ LOOT_ACTIVE（空箱→CONSUMED）
```

`LOOT_COMMIT_PENDING` 有三个不可混淆的阶段：opening reservation 尚无 `pendingCommit`；claim 双资源写在 exact `pendingCommit` journal 存在时禁止 legacy 暴露；transport handoff 以持久 `transportDetachNeeded` 封锁所有旁路。只有未物化、没有可恢复 inventory 的 pre-commit 失败才可进入 `EXPIRED`。完整物化后的**初次 open** kill adapter / activation / accepted transport failure 可以进入 persistent transport pending，并在 exact journal/effects、claim-only same-object renderer 与 pause-release proofs 完成后回 `LOOT_ACTIVE`；renderer 每次领取仍由 service adapter 权威提交，不得清空、双向写或重滚奖励。reopen 保留 `openAttemptWasReopen` 因果身份：pre-accept 拒绝、同步发送失败或异步打开失败直接回原 `LOOT_SUSPENDED`；accepted 后 mount/navigation/socket/Host 故障复用 pending journal/effects 收敛，但必须恢复 `LOOT_SUSPENDED` 且不运行 legacy renderer，inventory 已空时为 `CONSUMED`，anchor 失效时为 `EXPIRED`。

claim 提交 journal 冻结 operationId/fingerprint、source exact item/slot，以及 ordinary empty/merge、scalar 或 collection destination 的 exact before/after。destination 写 API 返回 false/throw 不能直接解释为未写；只有观察到 destination-before + source-present 才可清 journal 回 `LOOT_ACTIVE`。destination-after + source-present 保持 `LOOT_COMMIT_PENDING`，`lootQuery` 按同一 journal 只前滚 source 删除。成功 operation journal 写入后，dirty 读回验证、loot lease cache 与 destination cache 失效是分段幂等的 mandatory effects；全部完成前仍为 `LOOT_COMMIT_PENDING`。transport detach 先持久化 flag；一旦 exact recovery nonce 已登记，只有严格九键 proof query 可推进，普通七键 query 只能得到 `commit_pending`。初次 open 按「exact claim journal → mandatory effects → same-object legacy renderer proof → local pause lease release proof」继续；reopen 只执行 journal/effects 与 suspended/terminal pause handoff，不允许 renderer。任一必要阶段 false/throw 都只重试未完成阶段。claim/close/snapshot/tooltip/generic legacy/scene terminal 均不得旁路，并按子事件至多发布一次。

旧 AS2 UI 只作为初次 open transport failure 的 claim-only same-object renderer，不再拥有滚奖、容器身份或直接写权；领取只走 `LootContainerService` adapter，背包→loot、swap、discard 与 legacy direct write 零副作用。`LOOT_SUSPENDED` 是玩家普通 X、Esc、backdrop 后的非终态：仅同一 scene、同一 target anchor 的再次互动可复用同一 triple/inventory，重开必须重铸 `closeLease`、`panelInstanceId` 与 `openAttemptSeq`。reopen 接受前失败恢复 suspended，accepted 后故障也只能收束为 suspended/terminal，任何 reopen failure 都不得走 legacy renderer。场景离开只有在 `expireScene()` 成功后才可继续 teardown；pending 时必须保留整个 authority/world 并单飞重试。

## 5. 冻结 wire v1

### 5.1 打开面板

AS2 只发送：

```text
task = panel_request
panel = loot
source = map_chest
initData = {
  v, chestSessionId, lootContainerId, containerEpoch,
  openAttemptSeq, displayName, capacity, columns
}
```

AS2 `initData` 严格八键；`openAttemptSeq` 是正整数 attempt 因果身份。Host 在 `OpenRequest/Binding` 保留它，生成 `panelInstanceId` 后投 Web 时剥离它，所以 Web initData 仍严格八键 `{v,chestSessionId,lootContainerId,containerEpoch,displayName,capacity,columns,panelInstanceId}`。open payload 不含物品、数量、概率、掉落规则或经济指令。

### 5.2 Web → Host → AS2

顶层 task 为 `loot_request`，版本固定 `v=1`。所有命令共有且只共有 11 个键：`{type,task,domain,panel,v,cmd,callId,panelInstanceId,chestSessionId,lootContainerId,containerEpoch}`；Host 要求 `type=task`、`task=loot_request`、`domain=panel=loot`，并用当前 binding 重建发往 AS2 的身份。动作与 AS2 action：

| cmd | AS2 action | 必需业务字段 |
|---|---|---|
| `snapshot` | `lootSnapshot` | identity、epoch、loot/backpack window |
| `tooltip` | `lootTooltip` | identity、epoch、source slot ref |
| `claim` | `lootClaim` | identity、epoch、operationId、`direction=loot_to_player`、source ref、`targetContainerId=背包` |
| `close` | `lootClose` | identity、epoch、operationId、closeLease、abandon boolean |
| `query` | `lootQuery` | 无额外业务字段；Host→AS2 严格七键 |

额外键严格为：`snapshot={loot:{offset,limit},backpack:{offset,limit}}`；`tooltip={expectedAuthorityRevision,source}`；`claim={expectedAuthorityRevision,operationId,direction,source,targetContainerId}`；`close={expectedAuthorityRevision,operationId,closeLease,abandon}`；`query={}`。普通 Web `query` 经 Host 重建后的 AS2 wire 必须严格为 `{task:'cmd',action:'lootQuery',callId,v,chestSessionId,lootContainerId,containerEpoch}` 七键，不能携带 `openAttemptSeq/recoveryNonce`。`source` 又严格为 `{containerId,slot,expectedLease,expectedContainerVersion}`。Host 必须验证当前 active `loot` panel、exact `panelInstanceId`、identity 与字段白名单，再转发给 Flash；Web 请求不能携带 `source=map_chest`，该受信 source 只存在于 AS2 打开面板信封。queue accepted、消息已 post 或 native 窗口存在均不等于 Web 已绑定。

### 5.3 AS2 → Host → Web

Flash `loot_response` 使用固定 15-key 顶层 union，每次都包含：

```text
task, callId, success, error,
chestSessionId, lootContainerId, containerEpoch,
authorityRevision, lastAppliedOperationId,
state, remainingCount, closeLease,
snapshots, tooltip, terminal
```

Host 校验后发给 Web 的 `panel_resp` 严格为 20 键：上面 15 键，加 `{type,domain,panel,cmd,panelInstanceId}`；固定 `type=panel_resp`、`task=loot_response`、`domain=panel=loot`，`callId` 换回对应 Web callId。该信封没有 `v`、`source` 或奖励配置字段，额外键一律拒绝。

- success 时 `error=''`；失败时不得携带猜测性 mutation；
- 非 tooltip 回包 `tooltip=null`；
- 非终态 `terminal=null`；
- 无 snapshot 时 `snapshots=[]`；
- active snapshot/query/claim 返回 `[lootSnapshot, backpackSnapshot]`；
- `CONSUMED/ABANDONED/EXPIRED` 的 close/query-terminal 返回空 snapshots；`LOOT_SUSPENDED` 也是非终态成功投影，`terminal=null`，旧 closeLease 已失效，等待同 anchor 重开时签发 fresh lease/instance；
- `terminal={kind,reason,remainingCount}`；
- `closeLease` 只在 active 权威投影中有效，终态或错误为空串。

### 5.4 Host → AS2 recovery control

Host 只可为当前 exact accepted visual binding 发送严格八键扁平命令 `{task:'cmd',action:'lootPanelRecovery',chestSessionId,lootContainerId,containerEpoch,openAttemptSeq,recoveryNonce,reason}`。`recoveryNonce` 是 Host 生成的 opaque 因果值；`reason` 只允许 `web_mount_failed | web_open_failed`。不得夹带 `panelInstanceId`、奖励字段或额外键，nonce/attempt 也不得投给 Web。AS2 在三元 identity 与当前 attempt 精确命中时可先登记 exact nonce；若 accepted callback 尚未被 AS2 观察，只返回 `recovery_pending`，不得提前推进 transport，callback 到达后才继续同一 proof。旧 attempt、零/坏 attempt、stale、terminal、畸形、nonce 冲突或未知 reason 都是零副作用。reopen 发出前先登记 opening pending：pre-accept 拒绝、同步发送失败或异步打开失败撤销 pending 并恢复 `LOOT_SUSPENDED`；只有 accepted 后的 exact-attempt 故障才允许 ForceDetach。该命令不是 `lootClose`，不能生成 `ABANDONED`；reopen 收束时 inventory 已空可为 `CONSUMED`、anchor 失效可为 `EXPIRED`，其余必须回 `LOOT_SUSPENDED` 且不显示 legacy renderer。

recovery 写入后，Host 只能发严格九键内部 proof query：普通七键 `{task:'cmd',action:'lootQuery',callId,v,chestSessionId,lootContainerId,containerEpoch}` 加 `{openAttemptSeq,recoveryNonce}`。该 query 不由 Web 发起，结果不投回已销毁 Web，且 AS2 response 仍严格 15 键、Host projection 仍严格 20 键。pending proof entry 捕获 exact binding/attempt/nonce/generation；只有该 entry 收到通过 sanitizer 的 `success=true` authority projection 才可 settle detached flow，`success=false` 的 terminal-like 回包不能冒充成功。普通七键 query 不得推进已登记 proof 的 transport fence。

AS2 的 completed recovery proof ledger 有界保存旧 attempt 的 authority-closed / connected / socket proof。只有更新 attempt 的 accepted callback、exact recovery command 或 exact proof query 能剪除严格小于该 attempt 的历史；迟到旧 query不得清同代或更新 proof。容量已满且无法安全剪枝时 open/close 以 `recovery_history_full` fail-closed，禁止盲目 LRU。Host fresh admission 同样 fail-closed：`LootTask` 持 admission lease 期间才允许 Coordinator 原子安装新 binding；旧 flow 仍为 write-unknown、detached reconcile、native close 或 pause-release 未决时，新请求返回 `flow_busy`。

## 6. Web 交互与恢复

- 使用 canonical `transfer-pair` profile 和共享 `PanelResponseRouter / PanelRequestMux / PanelLifecycle / DisposableStack / FocusScope / Workbench` 组件，不安装第二个 response listener。
- 左侧背包、右侧战利品箱；右侧容量按 snapshot，不分页。
- pointer、双击/快捷收取与键盘必须走同一个 claim intent；disabled 只作 UX，AS2 仍复核方向和容量。
- ordinary `target_full / inventory_full` 显示“整理背包”并打开同一 loot DOM 内的背包—战备箱子页。子页不调 `close`、`Panels.open('workbench')` 或 `WAREHOUSE`；只允许背包↔战备箱 `autoTransfer` 和经二次确认的背包整格 `discard`。X/Esc/backdrop 在子页时优先执行“返回战利品”；必须等 inventory ready、无 busy、无 `refreshRequired`，再以 fresh `snapshot` 证明 `LOOT_ACTIVE` 才可返回主页。任一同步失败留在 organizer，不 replay claim/close。
- organizer 的 inventory request 顶层恰为七键 `{type,domain,panel,cmd,callId,panelInstanceId,payload}`，固定 `type=panel/domain=inventory/panel=loot`，只允许 strict `snapshot/autoTransfer/discard` payload。Host 必须用 `LootPanelCoordinator.IsBoundVisualExact` 在同一锁内原子证明 state=`Bound`、active binding、PanelHost 与 exact instance，回包回显 `panel=loot` 及同一 instance；旧/畸形请求 fail closed。loot 占用 PanelHost 时，任何其他 panel 的 foreign `close` 必须隔离，不得关闭 loot 或释放其 pause。
- loot **主页**的正常 close 仍先取新鲜权威投影并复用 close v1：空箱归一提交 `CONSUMED`；非空右上 X、Esc、backdrop 发送 `abandon=false`，形成 `LOOT_SUSPENDED` 后关闭 visual binding 并回游戏；只有独立危险动作“放弃剩余”及 focus-trapped 二次确认发送 `abandon=true` 并进入 `ABANDONED`。`LOOT_SUSPENDED` 仅允许同场景同 target anchor 重开同一 triple/inventory，且重开使用 fresh closeLease/panelInstanceId/openAttemptSeq；reopen 任一失败都不得回弹旧 AS2 UI。
- claim/close 回包未知时进入 `reconcile_required`，只发 query；不能重放原 operationId 写入。Host/Web 各自的 freshness watermark 只吸收 matching pending call/identity 中畸形或未证明的更高 revision，且不覆盖 known projection；其他 call/identity 不得抬高水位，后续 proof 必须至少达到水位且不能回滚模型，普通七键 query 不增加 `reconcileAfterCallId` 等 wire 字段。direct claim success 只接受精确 `authorityRevision +1 / remainingCount -1 / operationId / requested-slot-empty`；direct close success 还须精确 `revision +1 / operationId / expected state / remaining`。capacity failure 只有 strict raw failure shape、exact current binding 与完整 known prestate 同时成立，且已知 revision、remaining、lastAppliedOperationId、`closeLease`、`containerVersion`、requested slot 与 slot lease 全部不变时才可跳过；错误名本身不是证明，Host 只在该 exact prestate 下保留已知 `closeLease/containerVersion`。
- ForceDetach 只销毁已被 Host 接受的视觉 binding，不发送 `lootClose`、不猜终态；它只用于 accepted 之后的 mount/navigation/socket/Host 故障，不能替代正常的 `LOOT_SUSPENDED` close，也不能用于 reopen 接受前失败。初次 open 的 proof chain 可含 claim-only same-object renderer 并回 ACTIVE；reopen 的 proof chain 严禁 renderer，只回 `SUSPENDED/CONSUMED/EXPIRED`。两者都不重做经济写、dirty/event，不提前放 pause。
- Host 对 queue accepted / `OpenPosted` 后仍未收到 exact bind 的窗口使用 generation/identity-bound、默认 2500ms 的 bind watchdog；超时、mount/open failure、导航或断线只占用一次 recovery signal。`PauseReleasePending` 必须保留 exact coordinator-owned binding并只允许 scoped retry，不能用 generic unpause、提前回 Idle 或解除另一面板的 pause；完成前 fresh admission fence 保持关闭。terminal 与 force-detach exact native close 的 enqueue false、completion false/丢失都必须以有上限的指数退避自主重试，直到 exact `PanelClosed` 或 Host teardown。
- socket replacement 必须先保留更高 generation，再对旧 generation 发布 disconnect，最后发布新 generation ready；frame ownership 同时复核 generation/client/stream，每代 disconnect 至多一次。replacement disconnect 是过渡，不得误判为 prewarm failure 或 Ready zombie；同代自然 EOF/force-close 才是实际掉线。recovery 写失败只能关闭发送时捕获的同一 generation，旧 frame/completion 不得影响 replacement，也不得跨代 PostToWeb 或释放 pause。
- 重复 open/close/query、迟到回包与旧 panel instance 均须幂等或零副作用。

## 7. 实施状态与分层证据

截至 2026-07-21，当前工作树已完成以下源码接线：

- AS2：`LootContainerService`、`LootMaterializationPlanner`、`LootClaimCommitCoordinator`、精确 rollout selector、一次物化 journal、初次 open claim-only same-object renderer/service adapter、first-class suspended anchor/reopen、reopen failure no-renderer、scene teardown barrier、strict recovery/query proof 与 scoped pause proof；
- Host：`LootPanelCoordinator` tracked exact binding、suspended close proof / fresh reopen binding、bind watchdog / exact close、`PauseReleasePending` scoped retry、`LootTask` strict wire 与独立 unknown freshness watermark、exact successful settlement、organizer 严格七键 inventory gate、原子 `IsBoundVisualExact`、foreign-close 隔离、stale Web post 隔离与日志去敏边界；
- Web：`loot-runtime.js / loot-state.js / loot-view.js / loot-organizer.js / loot-panel.js`、lazy 依赖闭包、transfer-pair CSS、普通主页关闭→suspend、同实例 organizer、子页返回与 fresh ACTIVE snapshot、背包 discard 二次确认、独立危险放弃、独立 freshness watermark、exact `+1/-1` 与 capacity zero-write proof、query reconcile / force-detach 与 pending lazy-cancel；
- 受控入口：受控进档顺序固定为：记录本次 `start` 日志水位 → 在该水位后同时取得 fresh handoff 与真实 `[LaunchFlow] bootstrap_reveal_ready: Flash reveal cleared`（watchdog 事件不计，缺失报 `title_frame_not_observed`）→ 仅调用一次 `agentEnterResolvedSave()` → helper fail-closed 调 `_root.notifyGameEntered()`，同包发送 attempt-bound `s:1|ga:<_bootstrapAttemptId>`，再以 `gotoAndStop` 进入“读盘”帧 → Host 排除 legacy 无 `ga` 包、将 receipt exact 绑定当前 attempt，且只有该 receipt 才设 `gameEnteredObserved=true` → runtime ready。每次 `start` 与后续 `s:0` 都重新加锁；裸 `s:1`、helper 已调用、watchdog 或 `revealPerformed` 均非成功证据。
- 数据：`夺取材料.xml | SubStage 5 | Instance 0` 只有 `loot-canary-material-5-0-v1` 一处 rollout declaration，`profile=web-loot-v1`、`unlockPolicy=skip`。

| 证据层 | 当前实跑信号 | 能证明 | 不能证明 |
|---|---|---|---|
| XML / 静态接线 | map static wiring **243/243**；S0 **454/454**；Rock Park 4 个 `TEMP` fixture 已精确移除，stage **41/41**；当前 XML 为 **28 declarations / 27 nodes / 26 identities**、Rock Park **active 1 / commented 8**、rollout **1** | selector、claim-only fallback、reopen no-renderer、pending/scene teardown barrier 源码锚点闭合，测试夹具未残留 | 不能替代 S0 exact fixture、真实 XFL 或全局 arbiter 真人回归 |
| Host | 相关定向 **108/108**、AgentControl **29/29**；Launcher clean full **1220/1220** | strict recovery/query、fresh admission、独立 freshness watermark、exact successful settlement、strict 7-key inventory、`IsBoundVisualExact`、foreign-close 隔离、closeLease/containerVersion 保留与 pause/close 边界在当前 source tree 一致 | corrected Agent entry 之外的 organizer 领域 E2E，或正式 runtime 已部署 |
| Web feature | state **46/46**；browser **62/62**；lazy/tracked failure/redaction **6/6**；strict audit **0/0**、atlas **48/48**、item-grid **14/14** | exact `+1/-1/operationId/requested-empty`、capacity exact 零写、同实例 organizer、settled 后 fresh ACTIVE snapshot、失败留页/零 replay、背包 discard 二次确认、普通主页 suspend/reopen 与 stale proof 拒绝 | promotion 前 candidate 已真人完成真实 WebView2 organizer/modal/NativeHud 主链，以及普通主页单次 X→挂起→同箱内容不变重开→最终领取；这是后续正式发布的领域 E2E 前置证据 |
| Web 共享门 | workbench primitives **7/7**；共享 lifecycle/focus/components、strict audit **0/0**、atlas **48/48**、item-grid **14/14** 全绿；1000 行阈值 off-by-one 与 equipment refresh busy 竞态修复后，三视口 harness **68/68** | 上游共享组件与本轮修复后的异步 barrier 没有残留自动回归 | 自动全绿不能消除人眼手感、系统 DPI、输入法或 reduced-motion 真机门 |
| 发布政策闭包 | Runtime Lane C **11/11 个入口 exit 0**：dev-entry **41**、entry-guardrails **scripts=3 / unsafeCandidateCases=3**（不计入 scalar）、build-v2 **85**、policy **58**、queue **16**、attestation **109**、GitHub helper **16**、main **13**、release-state **48**、build-consensus **8**、release-consensus-v2 **14**；10 个 scalar 套件合计 **408**；bootstrap / env 均 exit **0**。旧 **368/368** 仅为已退役历史基线 | 当前源码树的 runtime/admission 与 production closure 守门回归通过 | 领域 E2E 已通过、receipt 已签发或已 promotion |
| AS2 / 产物 | fresh CS6 runId `dbcc28a3d99444c38bde38029ff4bd61`：Service **159/159** + Planner **7/7** = **166/166**、0 failed、Compiler Errors **0/0**、BOM **227/227**；最终 `asLoader.swf` 为 **1,041,903 bytes** / SHA-256 `AD799970A8A2AFD0F9A402C704934F08985A3144FA608A7564E17BC4899C815E` / Git blob `3dd59f73f9fc71128da99c2eb03796290df1e010` | 当前生产 AS2 与本轮 claim-only/teardown 修复绑定到 fresh trace + publish 身份 | publish 不能证明 Host runtime 部署或真人焦点结果 |
| 持久化真人链 | 已退役 candidate 上完成 mixed claim-all→suspend→整理→same-anchor reopen→`CONSUMED`；手动存盘后完全退出，attempt `7560a368…add7` 从 SOL 冷启动，实时 SOL/JSON 四项值一致 | 成功内容持久且未由容器重放；冷启动回读已完成 | 当前 organizer 主链已另行实机；仍缺未领取瞬态容器跨进程持久化专项 |
| Runtime diagnostic（历史） | `c-fca19…978633c6` 保留为旧闭包证据；`c-2a0cddb077b7-08846e81b3-20260721t100014612z-f32a40a3` 绑定 Core `0F58BF86…7232`、build `2A0CDDB0…A107`、closure `3B837DCD…150C`，专用槽 `cf7_agent_loot_target_full_v1` / attempt `62f04…1754` 完成 exact Agent entry、正常 NativeHud、organizer 与 suspend/reopen/final-claim，达到 `e2e_verified / NOT_DEPLOYED` 后优雅退出 | 证明 promotion 前同一 native 身份的受影响领域 E2E | 不单独证明正式发布；现仅作为下行前置证据保留 |
| Runtime release / standard entry（当前） | request `FBA91942…AE4`、tag `runtime-build-v2/20260721-map-loot-canary-v1`、双 signer / 双 faultDomain、production receipt 与 strict verifier 后完成 promotion；clean promoted checkout 的槽 `cf7_agent_loot_standard_entry_v3` / attempt `4eae1360…7158` 完成 formal identity、真实 title marker、canary 两次 claim、terminal close/unpause 与存盘确认 | build `2A0CDDB0…A107` / closure `3B837DCD…150C` / Core `0F58BF86…7232` 的唯一 canary 已达到 `standard_entry_verified` | 其余 S1/S2 资产、S3/S4、S0 actual-feature 或本轮新的冷启动回读 |

当前证据链已经从 source-tree / candidate 门推进到正式入口：map **243/243**、stage **41/41**、Host 相关定向 **108/108**、AgentControl **29/29**、Host full **1220/1220**、Web **46/46 + 62/62 + 6/6**、focus integration **12/12**、fresh CS6 **166/166**、BOM **227/227**，以及 **1,041,903 bytes** / SHA-256 `AD799970…9C815E` / Git blob `3dd59f73…1e010` 的 publish-only SWF，构成 promotion 前自动门；`c-2a0cddb0…f32a40a3` 构成历史 `e2e_verified / NOT_DEPLOYED` 领域证据；request / receipt / quorum / strict verifier / promotion 与 attempt `4eae1360…7158` 的 formal smoke 则把同一唯一 canary 推到 `standard_entry_verified`。仍缺的是其余 S1/S2 资产与箱型、S3/S4 和 S0 独立人工门，不能写成整体生产迁移完成。

## 8. 发布与回滚

源码通过不等于当前运行时已修复。若改动 Launcher Host/Core，仍必须：

在 candidate 启动、Launcher 重启、正式 promotion 或回滚前，先阻止新的 loot open，并由 AS2/Host 双侧证明没有 active 或 `LOOT_COMMIT_PENDING` 容器。v1 不持久化未领取瞬态容器；无法证明 drain 完成时必须中止进程切换，不能把遗失未领取奖励当作部署 / 回滚成本。

1. 通过精确 runtime build 环境门；
2. 从 immutable Git tree 生成隔离 candidate；
3. 至少两个不同 signer 与 faultDomain 对同一 identity/closure 形成 quorum；
4. 获得 production policy receipt；
5. 通过 v2 strict staged verifier；
6. 使用正式 promotion 工具原子更新，禁止单机复制 DLL。

原门曾将同一待部署 candidate 的① S0 非生产 exact 双-marker actual-dev-wire 与② S1 真实 `skip` canary 都列为 promotion 前门。本轮维护者明确收窄并已执行发布门：**仅在 S0 的 AS2 / Host 两道开发门持续关闭、`data/stages` 没有 S0 双 marker，且玩家不可达 S0 时**，允许已有真人 E2E 证据的 S1/S2 单 canary 进入并完成正式 v2 promotion。该严格 scoped deferral 只验收已批准的 S1/S2 切片，不把 S0 记为通过、不豁免 S0 证据，也不允许启用或对玩家暴露 S0。任何启用 S0 的后续变更，仍必须先在同一 candidate（或重新冻结、正式绑定的新 candidate）上通过 exact 双-marker actual-dev-wire 与相应人工门。跨栈证据还必须在最终 publish 后记录实际 `asLoader.swf` SHA-256、candidate payload closure、Flash/Launcher 进程启动身份与完整重启时间；在 publish 完成前不得预填哈希，也禁止用旧进程、旧 SWF 或另一 candidate 的日志拼接验收。

正式部署已切换为 request `FBA91942…AE4` / tag `runtime-build-v2/20260721-map-loot-canary-v1` 对应的 build `2A0CDDB0…A107`、closure `3B837DCD…150C` 与 Core `0F58BF86…7232`。FCA19 与 `c-2a0cddb0…f32a40a3` 的真人 claim / suspend / reopen / organizer / 存盘证据继续作为 promotion 前历史层；双 signer + 双 faultDomain quorum、production receipt 与 strict verifier 已完成，promotion 后 attempt `4eae1360…7158` 又在 clean formal checkout 中完成唯一 canary 的打开、两次 claim、terminal close/unpause 与存盘确认，故该单 canary 达到 `standard_entry_verified`。历史 SOL 冷启动读回仍有效，但本轮 standard-entry 没有新增完全重启回读。

AS2 只在形成可用节点后发布 `asLoader`；不能把 `.as` 已修改、`publish_done.marker` 或旧 trace 写成编译成功。Web 静态文件可先验证，但没有当前 Host route 和新鲜 Flash 发布时不构成游戏内完成。

回滚顺序：先移除单个 canary 的三个 rollout 字段，让它恢复 legacy；再停用 `loot` Host/Web route；最后回退 S1 service。已经 active 的容器必须先收束；只有初次 open transport failure 可走同对象 legacy recovery，已 suspended/reopen 的容器不得借回滚弹旧 UI。不能在回滚中重滚或静默丢弃，回滚也不改玩家长期存档。

## 9. 退出门与人工验收

自动门至少包括：

- AS2：一次物化、`2^31-1` 数量跨度边界、luck/PRD 冻结、own-kill/death/onUnload 时序、稳定 lease、满背包零写、装备描述符保留、材料/情报容量、伪造方向、claim half-commit query/scene reconcile、异 operation/fingerprint 拒绝、三终态、duplicate close、ack-loss query、初次 open legacy same-object recovery、reopen no-renderer suspended/terminal recovery、strict 7/8/9-key shapes、proof ledger prune/capacity fail-closed，以及 renderer/unpause failure scoped retry；
- XML：声明/逻辑身份基线、唯一 rollout、profile/policy、direct 误标、S0 冲突、掉落规则与容量；
- Host：route/source/strict keys、active instance/identity、bind watchdog、fresh admission fence、断线/导航、strict 8-key recovery、generation-bound 9-key internal proof query、exact successful settlement / no stale Web post / no write replay、organizer strict 7-key inventory + `IsBoundVisualExact` + foreign-close 隔离、close v1 三分支、same-anchor suspend/reopen 的 fresh lease/instance、ForceDetach 故障恢复、replacement generation ordering、exact close retry 与 `PauseReleasePending` scoped retry；相关定向 **108/108**、AgentControl **29/29**、全量 **1220/1220**；
- Web：单向领取、键盘等价、取消放弃、同实例 organizer、子页 X/Esc/backdrop 返回、settled 后 fresh ACTIVE snapshot、失败留页/零 replay、背包 discard 二次确认、非空主页 suspend 后同 anchor reopen、close retry、独立 freshness watermark、exact `+1/-1`、capacity exact 零写、stale/generation 与 recovery；feature 门为 **46/46 + 62/62 + 6/6**，focus integration **12/12**，shared workbench 总门已闭合；promotion 前 candidate 已完成 corrected Agent、正常 NativeHud、organizer 主链与普通主页 suspend/reopen/final-claim 真人回归，formal standard-entry 又完成唯一 canary claim/close/save smoke；
- 全量 Launcher、Web workbench strict audit/CSS/atlas/item-grid、文档治理、`git diff --check`、AS2 BOM，以及 attempt-bound agent 标题帧契约门；
- 新鲜 CS6 TestLoader trace 与目标 `asLoader` publish 证据；
- 真实 WebView2→Host→Flash→经济写→存盘→完全退出→重启回读。

历史 candidate 已完成人工 mixed claim-all、普通 suspend、原生战备箱整理、same-anchor reopen、`CONSUMED`、嵌入式 organizer、手动存盘与完全退出后的 SOL 冷启动回读；`c-2a0cddb0…f32a40a3` 的 attempt `62f04fa249c44d94ba3171f619e61754` 还按 `start` 水位→fresh handoff + 真实 title marker（watchdog **0**）→唯一 helper→同包 exact-attempt receipt→runtime ready 过门，并完成 organizer、modal、普通主页 suspend/reopen/final-claim 与 Rock Park fixture 恢复。随后正式 request、双 signer + faultDomain、receipt、strict verifier 与 promotion 均已完成；standard-entry 的早期 `title_frame_not_observed` 仅是失败探针，已由 prewarm-held formal attempt `4eae1360cedd413fa3175db6a8997158` 的 fresh handoff、真实 title marker、exact loaded 与 canary claim/close/save 主链成功取代。当前后续工作只剩其余 S1/S2 资产与 S0 exact fixture / 全局 arbiter / 真实 XFL 等独立人工门；S0 项是后续启用 / 暴露 S0 前的硬门，不因本轮 promotion 自动通过，reopen fault injection 已由自动门覆盖，不要求玩家手工制造。

只有画面布局、焦点/键盘手感、真实关卡移动/战斗/箱体交互与掉落领取观感需要人类直接操作；存盘/完全重启后的权威状态和基础候选启动/握手应尽量自动留证，只把必要的画面与实时输入确认交给人类。唯一生产 canary 只有两格强化石，`classifyItem` 会把它们送入 material collection：即使背包满也应合法领取，因此该 canary 只能直接证明材料路径，不能证明 ordinary `target_full`，也不得据此宣称装备或其他特殊资产已获真机覆盖。ordinary 满背包、装备 descriptor、金币/K点/经验/技能点/情报若需人工跨栈观察，必须使用隔离的非生产 fixture 与克隆 / 备份存档，不增加生产 rollout。自动 harness、browser shim、历史 runtime candidate 或截图都不能替代对应门。
