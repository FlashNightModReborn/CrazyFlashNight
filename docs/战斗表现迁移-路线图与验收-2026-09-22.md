# 战斗表现迁移：路线图与验收

**文档角色**：战斗表现原生迁移的唯一批次、issue 指针与阶段结果汇总；长期规则归[迁移契约](../agentsDoc/combat-presentation-migration.md)。<br>
**最后核对代码基线**：commit `8554961ff5` + 2026-09-26 本地首批子弹原生显示候选；后者尚未提交或正式发布。<br>
**当前状态**：BP8 天气与氛围的配对源码、SWF 和原生运行库已 promoted；目标场景观感、弱机净成本与真实跑动手感仍待人验，#118 保持开放。BP1/BP2 的普通与加强普通子弹已闭合四形态影子覆盖，柔光修正候选实跑后取得初步正面观感反馈；维护者已实测暂停恢复与地图 Web 面板开合可用。256 绘制条目容量边界及 AS2 显示权交还已定向验证；Host 异常帧撤销仍只有源码路径核对，正式 runtime 尚未更新，常驻 [Epic #109](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/109)。<br>
**授权边界**：原始建账授权仅含文档提交推送与 Epic/issue；2026-09-24 用户另授权 BP8 天气源码施工和开发候选验证。2026-09-25 用户要求拉取上游、处理冲突，并在上游 LUT 列车完成后运行本轮列车；本轮按该范围提交、推送与正式发布。真实存档试写及游戏内感官验收未由发布授权代签。

## 1. 已对齐的方向

- 优先原生接管联弹、常用简单子弹与适合的战斗效果；C# 负责管理，原生合成器承接绘制。第二 Flash/OCX 进程不作为默认前置；只有出现有实测价值的兼容性阻塞才另行评估。
- 保留 Flash 编辑真源，按素材导出中间形式；AS2 保留战斗权威，普通子弹对象化不继承联弹伤害语义。
- 小型子弹允许提到人物上方。大型爆炸/浓烟、需要人物前后关系的视觉首批保留 AS2；不等待天空/地图拆层或播放器透明输出。
- 使用具体素材/行为/层级白名单，混合架构可以长期存在；不一次性搬全部素材，也不要求先实现完整时间轴解释器。
- 射线、几何残影、简单打击与天气粒子共用空间/时间/缓存/批量绘制能力，各自保留适配器和预算。人物主体与游戏模拟不纳入本阶段。
- 收益是待测假设；以减少主 Flash 峰值的净效果决定扩张，不用迁移数量或显示成功代签性能收益。

## 2. 当前源码依据与限制

| 入口 | 已核实的抽离条件 | 尚未证明 |
|---|---|---|
| [联弹管理](../scripts/逻辑/战斗系统/战斗系统_fs_联弹管理.as)、[ChainUnitManager](../scripts/类定义/org/flashNight/arki/bullet/BulletComponent/Chain/ChainUnitManager.as) | 单元体有数据与集中绘制；生成/回收仍耦合 MC，需拆显示端 | 新桥接成本、原生资源覆盖及净收益 |
| [RayVfxManager](../scripts/类定义/org/flashNight/arki/render/RayVfxManager.as)、[RayStyleRegistry](../scripts/类定义/org/flashNight/arki/render/RayStyleRegistry.as) | 正常挂在效果层，按风格路由，输入包含路径及段信息 | 所有风格的原生等价与时序对齐 |
| [VectorAfterimageRenderer](../scripts/类定义/org/flashNight/arki/render/VectorAfterimageRenderer.as) | 点集/曲线可抽离；当前宿主为 deadbody；drawClip 另有 MC 依赖 | 提层可读性、任意 MC 动态复制能力 |
| [WeatherParticleRenderer](../scripts/类定义/org/flashNight/arki/render/WeatherParticleRenderer.as) | AS2 天气选择与场景事件已有集中入口；原生粒子复用镜头变换投影世界锚点 | 游戏内密度、面板遮盖、弱机净成本与调色观感 |
| [BulletEffectHandler](../scripts/类定义/org/flashNight/arki/component/Effect/BulletEffectHandler.as) | 飙血会设置出血来源，不能仅按帧动画处理 | 所有行为消费者与贴地链路 |
| [PlayerHudArtCache](../launcher/src/Guardian/Hud/PlayerInfo/PlayerHudArtCache.cs)、[SVG 导出工具](../tools/xfl-ui-svg/README.md) | 有 SVG 栅格化缓存和源素材导出经验 | 已具备 GPU 批量子弹绘制或任意 Flash 时间轴支持 |

素材抽查：普通子弹为单帧单色形状，牛仔炸弹为单帧形状，铁枪能量单元体包含两帧画面，纸飞机包含嵌套素材和旋转补间。这是抽样，不是全量资产覆盖率；运行名单须在每批沿实际 linkage 和行为追踪。

## 3. 与在途统一合成施工的关系

统一合成和输入归属由[现有施工计划](统一合成与输入归属-分阶段施工计划-2026-09-22.md)及其当前施工方维护。本轮不改那份计划或其 Host/native 源文件，不复制当时工作区验证数字作为本项目的前置完成证据。

可先推进的工作：白名单候选调查、素材导出、注册点与帧时长对照、离线预览和验收场景准备。生产运行时接管则等待可消费的合成接口，入口就绪不等于必须等所有 HUD/Web 改造结束；依赖范围由 BP1 对实际阶段出口复核。

首次接入需消费的能力：世界表现提交位置、相机/几何代次、捕获延迟的处理约定、暂停/场景清理、设备生命周期、面板/HUD 遮盖及调色顺序。这是后续消费者需求，不要求当前施工方无条件预建通用场景图。

BP8 首批按 2026-09-24 用户确认的独立视觉目标施工：天气不要求原 AS2 粒子轨迹或捕获帧精确对齐，也不以旧版受限密度为美术验收上限。AS2 发天气状态，C# 管视觉所有权，原生 GPU 在同一合成输出绘制。随后单独接入氛围叠加，所有命名预设和直接 `<Overlay>` 由原生合成，AS2 不再绘制氛围回退；天空仍在 AS2。该简化不替代面板遮盖、场景清理和弱机实测。

GitHub #83 是旧渲染架构裁决候选，#103 是现场输入长期契约，均不能直接冒认为本次统一合成施工卡。当前专项尚无核实的一一对应卡时，使用上述仓内计划作为外部前置；未来有准确卡号再建立 native blocked-by，不为满足格式制造重复施工卡。

## 4. 交付包清单

ID 保持稳定，不因 issue 创建或拆分重排；具体白名单枚举在实现后归机器清单。本表只记录每批能力与交付边界。

| ID / issue | 交付包 | 前置 | 出口与人验 | 当前状态 |
|---|---|---|---|---|
| BP0 / [#110](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/110) | 契约、路线图与看板建账 | 本轮讨论与文档授权 | 文档巡检、范围提交推送、父子关系/字段核对；不代签运行效果 | 完成；规划卡关闭归档 |
| BP1 / [#111](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/111) | 原生表现提交底座与同步小样 | 统一合成提供可消费阶段接口 | 最小精灵/线带消费者，批量/所有权/失败处理、时序与固定负载成本；首个实机人验 | 本地影子通道候选；尚无原生精灵绘制/所有权接管 |
| BP2 / [#112](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/112) | 战斗素材定向导出与能力清单 | 可先独立进行，不等待 BP1 | 首批代表素材，源版本/注册点/帧/alpha/不支持项验证；对照预览 | 普通/加强两母版定向派生候选；待实际画面对照 |
| BP3 / [#113](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/113) | 小型联弹首批接管与净收益验证 | BP1、BP2 | 白名单联弹，伤害保持原权威，提层密集战斗、DRS/切场景/暂停实机；净成本对照 | Inbox |
| BP4 / [#114](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/114) | 常用普通子弹显示单元化与接管 | BP3 的首批结论 | 普通/加强/穿刺等按实际 linkage 逐项登记；固定碰撞体与帧脚本拆分；不改变联弹/单弹语义 | Inbox |
| BP5 / [#115](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/115) | 射线风格原生绘制 | BP1 | 首个简单风格先行；路径/段/命中不变，混合/耀斑/夜间辨识与时机人验 | Inbox |
| BP6 / [#116](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/116) | 点集与曲线残影接管 | BP1 | 几何/渐隐/时钟与提层人验；不包含任意 drawClip 复制 | Inbox |
| BP7 / [#117](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/117) | 简单打击效果白名单 | BP1、BP2 | 首批无复杂游戏依赖效果，事件一次性、命中时机与密集可读性；飙血暂留 | Inbox |
| BP8 / [#118](https://github.com/FlashNightModReborn/CrazyFlashNight/issues/118) | 天气粒子复用原生合成输出 | 现有世界合成路径的窄天气接口；战斗共享底座仍由 BP1 自身证明 | 状态/重连/切场景不双画，雨雪沙尘雾刀痕的隔离输出，游戏内面板/调色/弱机成本与视觉人验；不迁天气玩法、氛围叠加或天空 | 本地候选验证中；待游戏实跑与人验 |

上述是有具体交付目标的候选包，不代表全部已获代码施工授权，不提前转 Ready。BP4 的扩展以 BP3 结果为依据；BP5/BP6 的离线算法调查可先做，运行接入才受 BP1 阻塞。各批若需正式部署，另按授权与发布协议完成，不因卡片涉及 native 自动挂发布列车。

## 5. 验证与人类验收节点

自动验证遵循[契约验证章节](../agentsDoc/combat-presentation-migration.md#7-验证与人类节点)，围绕资源过期/缺失、事件重复、场景换代、队列边界、恢复与逻辑不变写必要测试，不为简单文档或低影响配置堆测试。

| 节点 | 人类需要判断 | 可合并的范围 |
|---|---|---|
| 首个世界表现候选 | 相机/DRS 抖动、源帧与效果错位、暂停和面板遮盖 | BP1 与首个真实消费者 |
| 联弹首批与普通弹扩展 | 人物辨识、密集覆盖、方向/缩放、命中观感；机器给成本数据 | BP3/BP4 的明确素材列表 |
| 新效果类别 | 射线亮度/时机、残影提层、打击提示、天气密度与夜间表现 | BP5–BP8 已就绪候选合并一份旅程，逐项记录结果 |

不为离线预览单独要求玩家反复开游戏；也不把用户认可提层策略当成所有素材已验收。机器结果与感官结论分别记录；未测项保留未测。

## 6. 维护与下一步

下一步先让当前统一合成施工收口到可消费接口；需要开始战斗迁移时，点查 BP1/BP2 的真实前置与共享文件，再选择首个有明确边界的包。不会因本轮建卡启动运行时施工。

GitHub 只放目标、仓内权威链接、授权边界、范围、验证出口与依赖，不复制完整方案。长期 Epic 保持开放；待办保持可见；已完成的规划卡可关闭归档，其余不因文档完成或入板变成已交付。

## 7. 规划收口记录

2026-09-22：契约与路线图首批以 `786b438b17` 推送 main，随后建立 #109 与原生子 issue #110–#118。BP3–BP8 的真实交付依赖已设为 native blocked-by；BP1 用 timing 标记在途合成接口前置，BP2 不被串入该等待链。

本轮只验证文档、范围提交与 GitHub 投影：文档巡检通过，保留仓内既有非阻断链接/行数提示；父子、依赖、Domain、验收方式和待办可见性已回读。规划卡最终关闭凭证见 #110。未执行游戏、AS2 编译、候选或 runtime 发布，不声明任何战斗接管完成或性能收益。

## 8. BP8 天气本地候选（2026-09-25）

用户授权本地施工，并接受以新视觉目标替代旧 AS2 粒子逐帧等价。AS2 天气规则与旧 Host 回退保留；Host 确认实际原生画面就绪后，每连接代切换显示端。原生合成器同帧绘制雨、雪、沙尘、雾和刀痕；氛围叠加、天空及天气玩法未迁移。

本地证据：新鲜 asLoader CS6 Compiler `0/0` 与 SWF 刷新，WorldLightingBridge focused `17/17`，协议延迟专项 14 组无失败；独立 GPU 夹具五种类型均执行，雨/雪/雾有实际截图，包含雨粒子的自动调色与视口矩阵通过。初版配对游戏开发候选已构建并核对 manifest 哈希；这些只覆盖 `compiled`、`candidate_built` 与隔离夹具的 `candidate_executed`，不代签游戏内观感。

2026-09-25 维护者运行初版配对开发候选后，反馈进入预期有天气的关卡仍看不到粒子。新鲜 `logs/launcher.log` 中雪地 `snow/0.7` 与刀痕 `slash/0.6` 均为 `weatherNative=true`，证明 AS2 已交出显示权；但 07:18:46 窗口移动后，Flash 客户区比 WGC 捕获底边多 1 像素，合成器持续报告 `source_outside_capture`，本次运行没有任何 `world_lighting_frame_handoff`，原生天气因捕获栅栏未通过而保持关闭。这是一次真实候选执行失败，不能记为天气 `e2e_verified`。Host 已改为只容忍各边最多 2 像素的已测量交集，超出仍拒绝，并增加原生天气状态日志；以这次实测坐标写入回归，全量 Launcher `5832 通过 / 4 跳过`。新隔离候选 `weather-geofix-0925` 的完整性校验通过，未启动游戏、未修改正式 runtime。下一步用新候选复查雪地与刀痕画面、窗口移动、面板遮盖、夜间观感和弱机成本；#118 保持开放。

同日上午的新候选实跑与截图指出第二轮观感问题：雪花在白色雪地上缺少对比，室内刀痕受 AS2 性能档位 `3` 清零且本身偏细，原生尺寸未跟随游戏镜头缩放，雨的落地弧线尚未迁移。后续源码改为：雪花浅色中心加冷灰轮廓，刀光增加长度/可读性；Host 从现有逐帧 F 包读取镜头比例，原生粒子按 1024×576 设计空间与镜头比例定尺寸；AS2 旧质量档只约束 AS2 回退，Host 对原生绘制使用固定有界预算；雨花在同一批次中按旧 AS2 的 Ymin/Ymax 纵深地面带绘制，不声称逐物体真实碰撞。AS2 focused 18/18 与 fresh CS6 asLoader Compiler 0/0、Host 全量 5832/4、原生 HLSL VS/PS 严格编译及雪/雨/刀光隔离 GPU 预览已通过；自动合成报告通过 3 次像素证明且最大误差 0。新配对候选 `weather-visual-0925` 已构建并通过完整性核验，原生 DLL 与 GPU 预览 SHA-256 一致；正式 runtime 未改。游戏内雪山、翅虎堂和雨花的观感、遮挡及性能仍待新候选复查，#118 保持开放。

用户继续指出尺寸虽已随镜头变化，刀光位置仍固定于屏幕，要求全部原生粒子跟随世界画面的平移与缩放。前版确实只用了 F 包的 `scale`，没有把 `x/y` 用于粒子位置。原生着色器现改为稳定世界锚点经 `stage = cameraOffset + world * cameraScale` 投影；雨、雪、沙尘、雾、刀光及雨花均使用这条路径，长地图的周期边缘回绕对大雾团与刀光淡出。场景中实际使用的 8 处 `<Preset>` 只有 `毒气/腐蚀/寒铁` 会展开粒子；`警报/医疗警报/工业警报` 只展开 AS2 氛围叠层，仍按此前范围暂留 AS2。雨雪沙尘也可由 `WeatherCondition` 直接触发。五种类型的移动镜头隔离 GPU 夹具均执行成功；带平移的刀光自动合成报告通过 3 次像素证明且最大误差 0。新配对开发候选 `weather-world-0925` 已构建、完整性核验通过，原生 DLL 与夹具 SHA-256 一致；正式 runtime 未改。游戏内镜头运移、缩放、遮挡及性能仍待该候选的人验，不能用静态预设清单或隔离夹具代签真实观感。

## 9. 氛围叠加本地候选（2026-09-25）

维护者明确要求把氛围预设全部迁移，且不保留 AS2 回退；天空盒仍是独立范围。`EnvironmentConfig` 保留场景预设和直接 `<Overlay>` 的解析，`GameWorldOverlayRenderer` 只投影封闭状态；`WorldLightingBridge` 将其并入现有 `world_lighting` 场景快照。Host 对 10 种命名氛围预设和直接叠加项映射原生编号，按场景就绪与捕获栅栏清理/启用。原生合成器在调色主 pass 内绘制低强度、可区分的灯光、雾色和扫光；后续天气粒子仍在主 pass 后绘制。直接叠加项保留色值、径向模式和脉冲参数。命名预设以新视觉目标重做，不要求旧 AS2 像素等价。

本轮专项证据：AS2 focused `23/23`，fresh CS6 asLoader publish Compiler `0/0` 且 SWF 刷新；严格类归属审计 main `0`、asLoader `668`、交集 `0`。Host 全量 `5844 通过 / 4 跳过`。HLSL `ps_4_0 /WX` 通过；11 个氛围编号均在隔离 GPU 夹具成功出帧并留截图，医疗警报的自动合成夹具 3 次像素校验最大误差 `0`，寒铁镜头平移/缩放参数经夹具接受。这些是源码、编译和隔离呈现证据，仍未代签实际游戏场景的遮挡、可读性、转场和性能。

开发候选 `tmp/runtime-candidates/v2/atmosphere-native-fix-0925` 已构建并通过 v2 完整性核验，状态 `candidate_built / NOT_DEPLOYED`；Core SHA-256 `FCBD653F33EB70005307758AC5B2544A621795D1E1F35EE5F73C148BDD2E525D`，原生 DLL SHA-256 `6D0308F5356FE55526521C195955A31B4A983F2D13023F32018B59839CB88296` 与 GPU 夹具一致，asLoader SHA-256 `09E45048BEED287FD34AE20AAB59870C827BD248DDBAE2A301196262FB54E8C8`。第一次候选构建在无关的 Rust `sol_parser` 步骤失败：工具链预检的 null 环境变量在本机保留为空值，Cargo 将空 `LINKER` 当成指定链接器。修正为实际删除 `Env:` 条目后，同锁定工具链单独重建及第二次完整候选均通过；失败目录不作可运行候选。

asLoader 发布曾晚于首次 240 秒等待上限约 13 秒；迟到的 SWF、Compiler `0/0` 和标记已归档，超时轮次不算通过。恢复后 900 秒等待预算下重跑，188 秒明确完成。正式 runtime 未更新；由于本批不保留 AS2 氛围回退，旧正式 Host 与新 asLoader 混用时氛围会缺失。实际体验应显式选择上述开发候选，待游戏实测后再决定是否走独立正式发布流程。

## 10. 跑动时粒子与氛围光的镜头时序修正（2026-09-25）

维护者实跑反馈：伤害数字跟随正常，而迁出的天气粒子和氛围光在人物跑动时有迟一拍的手感。该候选的实机日志显示 UHD 630 下合成输出约 30 FPS，世界 pass 的 CPU 提交多在 0.1–0.6 ms；这些指标不包含 GPU 完成与输入到显示延迟，不能据此排除所有性能问题。源码核查发现两个确定的额外等待：F 包镜头先只写 Host 缓存，最多再等一次 33 ms UI 定时器才送原生；原生线程又在节拍等待和 WGC 取帧之前复制镜头参数，可能用上一轮镜头绘制当前画面。

修正让 F 包在原来的伤害数字解析路径直接更新原生镜头状态；场景就绪门和原生句柄释放与该调用共用锁，避免转场旧状态或销毁后的调用。原生线程在实际绘制天气与氛围前再取一次最新镜头。没有提高合成帧率、外推角色位置或改 AS2 业务、天气/氛围视觉参数。Host 全量测试 `5844 通过 / 4 跳过`，原生开发构建通过；刀光加寒铁氛围的移动镜头隔离夹具在 1.5 倍缩放与持续平移下通过 3 次自动像素校验，最大误差 `0`。这些验证证明管线运行与原有像素诊断未退化，实际跑动手感仍需新候选游戏内复验。

新配对开发候选 `tmp/runtime-candidates/v2/weather-camera-sync-0925` 已通过 v2 完整性核验并选为本地开发入口，状态 `candidate_built / NOT_DEPLOYED`；build identity `E6A8CE2703B03FAE2E81054B3FE95ADE9101DBA33F2DE46CBFD621F191AAE9FB`，payload closure `940264DDFA5D430967BBED8F503634B3AE221018D977755352A7FDCF434B2E65`，Core SHA-256 `0BB561B6A5357A23DBF269D74DFF3066092910709CE8DC82E7DA340154CA12F5`。此次未改 AS2，继续配对上一轮已验证的 asLoader；候选原生 DLL SHA-256 `6C68FFBFB1647967A52F0ED16A20744DBDF5F1A0A3D915A1401E8A38EB028F83` 与上述移动镜头夹具一致。正式 runtime 未更新，待维护者在同一跑动场景复查手感。

## 11. 落地雨花漂移修正（2026-09-25）

维护者指出雨花落地后仍横向漂移。原生雨花分支把中心 X 直接设为 `rainPos.x`；该坐标每一帧仍随雨滴风速 `vx` 增长，而雨花的展开/淡出相位另行计算，因此已落地的弧线会继续滑动。现按本轮雨花的固定 cycle 反算撞击时刻，只在该时刻计算一次世界 X；可见周期内仅更新半径与透明度，再由 F 包镜头将固定世界落点投影到屏幕。视口边缘淡出避免世界场回绕时弧线横跨整屏跳闪。Y 仍按既有 Ymin/Ymax 地面带计算，不新增地形碰撞或声称逐雨滴精准碰撞。

验证已覆盖天气着色器 `vs_4_0` / `ps_4_0` 的严格 HLSL 编译、原生开发 DLL 构建、带 1.5 倍缩放和持续镜头平移的雨天隔离夹具；自动合成 3 次像素证明通过，最大误差 `0`。一次额外的夹具屏幕截图被其他前景窗口遮住，不计入雨花观感证据。实际游戏内落点稳定性仍需新开发候选的人验。

新开发候选 `tmp/runtime-candidates/v2/rain-splash-anchor-0925` 已构建并通过 v2 完整性核验，当前开发入口指向它，状态 `candidate_built / NOT_DEPLOYED`；build identity `5AE035990119F9DBD8B9A93A7F8957147CBEEAA28CB6D630E90E593D78F8175E`，payload closure `C38621ACB60118AE1788DF3CEBCD3718E056FEDA3DF158DF00DD2795B05E7009`。Core SHA-256 `0BB561B6A5357A23DBF269D74DFF3066092910709CE8DC82E7DA340154CA12F5` 与上一候选一致；原生 DLL SHA-256 `4E1B714251F123C0F95DAFD8DCFAE7EC9A668C8CBD1DADDB9E55E5C9325B2C33` 与本轮雨天夹具一致。正式 runtime 未更新。

## 12. 世界表现预设外置（2026-09-25）

关卡内容归属复核后，视觉目录从 Host 私有的 `launcher/data` 调整到 `data/environment/presentation_presets.v1.json`，与场景、关卡环境和调色表相邻。C# Host 只消费该文件，原生实现画法；场景 XML / AS2 仍决定选用何种天气与氛围。此次移动不改变 JSON 内容，但读取路径、校验器、夹具默认路径和内容文档都须同步，旧路径不再作为回退入口。

维护者要求尽早清掉“改一点美术数值就要跑二进制发布列车”的维护债。前一版场景 `<Preset>` 只负责选择：AS2 又存一份命名氛围默认色，原生 shader 再以写死的命名分支覆盖其色值；原生天气的颜色、数量与速度也写死。新合同将外置 `data/environment/presentation_presets.v1.json` 作为视觉数值真源：场景/AS2 仍选天气与氛围名，Host 严格加载目录并记 SHA，原生 ABI 4 接受有限画法类型与有界参数。命名氛围的 RGB、基础/边缘/动态强度、节奏、空间分布，以及五类天气的 RGB、数量、大小、透明度、速度、风速和雨花参数可直接改数据。AS2 不再登记氛围名称或展开氛围默认色；直接 `<Overlay>` 仍是明确的自定义叠加。新增一个现有画法类型下的名字只需目录项与场景引用，新增画法公式仍需原生改动。

`node tools/validate-world-presentation-presets.js --check` 检查版本、字段、数值边界与 XML 预设引用；Host 启动时再独立严格解析，拒绝重复字段、无效值和未登记名称，原生接口也限幅。新数据文件加载于游戏启动，修改后重启生效；目录 SHA 与 Core/DLL identity 分开记录。用同一原生 DLL、同一医疗警报加雨夹具，分别加载正式目录与只改 `medical.base` / `rain.count` 的临时目录，两次自动合成均为 3 次像素证明、最大误差 `0`；两份目录 SHA 不同而 DLL SHA 相同。这证明数据确实经过共享解析器和原生设置接口，自动像素证明仍不代替目标场景的美术验收。

本轮完整验证：AS2 `WorldLightingBridgeTest 25/25`、fresh CS6 asLoader publish Compiler `0/0` 且 SWF 刷新，严格类归属 main `0` / loader `668` / 交集 `0`；Host 全量 `5845 通过 / 4 跳过`。命名氛围 10 种、自定义叠加 1 种与天气 5 种在正式目录下逐项运行成功；两套 HLSL 的 VS/PS 严格编译通过。第一次 TestLoader 在旧 CS6 会话二阶段重开后超时，已核对未刷新 SWF、无 compiler 结果、scratch 已恢复并归档；重启 CS6 后的新一轮才计为通过。

配对开发候选 `tmp/runtime-candidates/v2/world-presets-v1-0925` 已构建、v2 完整性校验通过，曾选为本地开发入口，状态 `candidate_built / NOT_DEPLOYED`。build identity `2C3F3FB799D6D75902E20C70608F59E98CD6B1030CE69CE844D1AD7B41B8A03C`，payload closure `C68F31C172AC9943B39A8459488D1762F8CA675A50F3187915F80BD251AB7414`，Core SHA-256 `CC14EF1805BCDDA5B19968871DBE2BE3DF1ED702F9BF72164885CCF23749E9CD`，原生 DLL SHA-256 `4919756C580D52804F8866F686F344ECA8A278DB3CE248B25DBC9FF02E559CF0` 与隔离夹具一致，asLoader SHA-256 `0B96023D8EC0873E6059B86C04CA66D19CE0F33D543C4CA8940E1B63190693D7`，目录 SHA-256 `0A126ED82B6A021BDC9853E9B590E19D5DCAA903AE2D53E24A6DC80CD734324E`。正式 runtime 未更新；目录文件作为独立内容交付，正式分发仍需确认其安装路径与玩家标准入口，不能因数据可编辑便视为已部署。

目录归属调整后的开发候选 `tmp/runtime-candidates/v2/world-presets-environment-0925` 已构建、通过 v2 完整性校验并成为当前本地开发入口，状态 `candidate_built / NOT_DEPLOYED`；build identity `08AD66C8006F684E80925BF620C5DE3B36DD01A5D885B566E6DB60FBA2044C7C`，payload closure `BE14BCB2EA0F7D064EB09FB6AE1A46308EC292B5EE49D0D52BBBE7E0E0AB8BFF`，Core SHA-256 `485CD039DA6562E2EC73ABB16A447038CA2BD5CC177DF68EE04CD2FC1F656275`。目录 SHA-256 保持 `0A126ED82B6A021BDC9853E9B590E19D5DCAA903AE2D53E24A6DC80CD734324E`；Host 全量 `5845 通过 / 4 跳过`，雨加医疗警报隔离夹具从新默认路径读取目录，自动像素证明 `3` 次、最大误差 `0`。尚未进行游戏内场景观感验收，正式 runtime 未更新。

## 13. 同步上游 LUT 与退役天气 AS2 显示回退（2026-09-25）

本地天气施工以 `origin/main` 的 `62fc30d391` 为新基线：上游 `2ebbc43bfe` 已发布默认 `hardlight-dusk-v4` LUT、LUT 实验室及对应正式 runtime 的共识、manifest 和二进制，`62fc30d391` 已回填发布记录。隔离工作树从该基线接入本地未提交的天气、氛围与预设改动；原工作树的场景 XML 改动未进入该集成范围。冲突集中在原生合成器、探针、Host 会话/控制器与对应测试。合并结果让主画面先按当前矩阵或真实 3D LUT 调色，命名/自定义氛围在主 pass 叠加；天气粒子随后在同一输出中绘制，并用同一矩阵或 LUT 调色。配对原生 ABI 为 4，同时要求 LUT 与天气/氛围导出；未配对旧 DLL 会在会话建立时拒绝。

`WeatherParticleRenderer.as` 已缩成天气类型、强度、旧质量字段与本连接代原生能力的状态投影；不再创建天气 MovieClip，不订阅逐帧绘制，也不运行雨花或碰撞边界逻辑。`WeatherSystem` 的场景就绪事件只发布状态，`ServerManager` 断连后只撤销原生能力；天气选择留待新连接恢复，AS2 不补画。天空盒、天气业务时间、夜视与奖励权威仍按原职责保留。

当前机器验证：`WorldLightingBridgeTest 25/25`、fresh CS6 asLoader publish Compiler `0/0` 且 SWF 刷新；合并后 Host 全量 `5891 通过 / 5 跳过`（首次 Native HUD 呈现超时 1 项，单项及全量复跑通过）。矩阵与实际 `hardlight-dusk-v4.lutset` 两条隔离夹具均加载雨加医疗警报并成功出帧；LUT 夹具对三线性采样的自动像素证明 `3` 次、最大误差 `0`，视觉目录与 LUT 的 SHA 分别记录。夹具截图被其他前景窗口遮住，不计入画面观感证据；游戏内跑动、雨花、氛围遮挡及弱机净成本仍须在本轮配对候选复查。

配对开发候选 `tmp/runtime-candidates/v2/weather-lut-integrated-0925` 已通过 v2 完整性校验并成为该隔离工作树的本地开发入口，状态 `candidate_built / NOT_DEPLOYED`；build identity `F02D141531B6CAB23C3908A450B8E833874CAE9731809AF0A4CAB55EFD68A995`，payload closure `7D1D34E43122BB7BFDB89CFE16A0349421CA42976870AE9F5A5AE7217EC83F8B`，Core SHA-256 `6D00E60431F3B26254FEFB1D259B6B4DCA9C71C7D99276F0070A3F1D9D47F3E2`，原生 DLL SHA-256 `123A3FA6C63C39F24CFA0AE4A04CC90CB334CA83CC5E333FA4CCFAC674F9AD9A`，asLoader SHA-256 `D425EE2991540530A8CF87ACC5A052033319B7B560F4BC785C47F47D4CDAED70`。该候选自身始终为 `NOT_DEPLOYED`；外置视觉预设已随源码提交，独立于 runtime payload。

后续正式列车冻结源码 `2a3ff8150c` 与一次性标签 `runtime-build-v2/20260925-weather-lut-particles-v1`。本地 X509 与 GitHub OIDC 在相同 build identity / closure 上形成双故障域证明；严格 production policy `41/41`，promotion 写入新 runtime、manifest 和 signed consensus。部署提交 `fbed6cd274` 已快进推送；[CI Audit 36122374308](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/36122374308) 成功，回报 `state=promoted / deploymentChanged=true`。原 Steam 工作目录已快进到该提交并通过根入口 `--verify-only`。预设 JSON 固定 LF 检出后，原始字节 SHA-256 为 `0A126ED82B6A021BDC9853E9B590E19D5DCAA903AE2D53E24A6DC80CD734324E`；这项内容交付不改变正式 runtime identity。真实游戏场景和玩家标准入口业务复验仍未由 CI Audit 代签。

## 14. 普通与加强普通子弹的首批影子通道（2026-09-25）

Surveyor 残弹的独立修复已提交为 `fa9f455e74`，维护者实战确认持续扫射不再留下静止残弹。其直接原因是战斗动画第 209–216 帧缺少交替射击使用的 `攻击点2`，导致无效坐标与 `NaN` 碰撞盒；同时已把该单位两处机枪射击改为现成纵向联弹。这个回归结果只排除了该发射点/回收故障，不代签原生视觉迁移。

首批视觉源固定为 `普通子弹`、`加强普通子弹`，及六个枪式联弹前缀下对应的 `单元体-普通子弹`、`单元体-加强普通子弹`。普通单元体的三角形 Y 注册漂移 18 twip（0.9 像素）已在 XFL 对齐；加强型的常态/单元体基底轮廓与 GlowFilter 参数相同。`tools/combat-bullet-visuals/build.py` 从六份 XFL 源和对应 SWF 确定性生成 `data/combat_visuals/bullet_styles.v1.json`，`--check` 同时钉源/生成器/产物哈希。新滤镜、混合或复杂形状不静默折成简单三角形。

当前跨层路径仅为**影子采样**：配套 Host 严格校验派生目录与资源 SWF，按连接代发送具体四个 linkage 和六前缀；AS2 在帧末最多附加 256 个实际可见实例的完整快照；Host 检查连接代、场景 epoch、帧序、字段和值域，每两秒记数量、四形态覆盖、字符量、溢出与解析耗时。Flash 始终继续绘制且独占命中/伤害/生命周期；原生还没有精灵绘制、白名单显示所有权或故障恢复能力。先用明确武器/敌人的场景证明四形态接线，再建立原生绘制候选并验证位置、命中/消失时机、暂停和切场景。维护者指出开发环境与随机佣兵刷新会给 AS2 帧率带来不可控压力；净性能差值推迟到更稳定、同负载的对照，不作为当前功能施工的前置门，也不从这轮影子日志推断收益。

截至 2026-09-25 的本地证据：原版子弹库与 asLoader 的真实 CS6 发布均为 Compiler `0/0` 且目标 SWF 已刷新；定向派生 `--check` 与 class embedding child-only 通过。Host 全量测试在连接代细化前为 `5893 通过 / 5 跳过`，细化后的 Socket/视觉定向测试 `90/90`，四格汇总专项 `4/4`。维护者从 `本地开发启动.cmd` 启动了候选 `c-b2748f4f5061-08846e81b3-20260925t143755288z-58a5314a`；实际 Core 路径与候选匹配，Host 日志含目录加载和影子帧。该次执行的 153 份两秒汇总中 42 份有非零实例，峰值 17 个实例，`invalid=0`、`overflow=0`。这证明连接、白名单与部分实际子弹采样；旧汇总未按四形态拆分，不能声称全部覆盖。新四格计数候选 `c-5e8b995300fb-08846e81b3-20260925t145818334z-38abb66f` 当日已构建并通过 v2 完整性核验，build identity `5E8B995300FB1BE3AE2B8D58CB87531FAD47628329CB5387FC5C5604472D4227`，payload closure `0520F1DAE569E6002A411BAFC3A80C2DEC859262CA5DAA16C55323AA5DCFD034`；当日尚未实跑，正式 runtime 未更新。后续实跑与原生施工见 §15。

## 15. 首批子弹原生显示候选（2026-09-26）

维护者经根目录 `本地开发启动.cmd` 实跑同一个四格计数候选：第一轮普通子弹、枪式联弹普通子弹、枪式联弹加强普通子弹分别出现 847、433、168 帧；第二轮用加强普通子弹实际射击，普通加强子弹出现 524 帧、峰值同时 3 枚。两轮的目录摘要相同，解析拒绝与 256 实例截断均为 0。四格覆盖只证明 AS2 采集与 Host 接收，不代签原生绘制位置、风格或战斗等价。

候选源已在同一派生样式目录上增加原生 ABI 5 的三角形绘制；加强型 GlowFilter 先以三层半透明壳作有界近似，需人验边缘和亮度。Host 只在隔离候选的合成器与样式就绪、捕获画面可呈现时按连接代宣布 `native`；AS2 仅在新实例生成时为白名单普通弹与对象化枪式联弹选定原生显示端，保留原碰撞、伤害、衰减和销毁路径。帧末完整快照新增显示所有权标志；宿主仍检查连接代、场景 epoch、帧序和值域，原生仅消费已接管条目。容量超限时未接管条目留在 Flash，已接管组或弹体单向退回 Flash；能力撤销、断线、场景重置清理原生帧并恢复仍存活的 Flash 弹体。旧 Host/旧 asLoader 不会由单方开启显示接管。

机器验证已取得配对 asLoader 的真实 CS6 publish Compiler `0/0`、SWF 刷新与 main/loader 单一类归属通过；Host 全量 `5895 通过 / 5 跳过`，原生 C++ 固定工具链编译和子弹着色器 `vs_4_0`/`ps_4_0` 严格编译通过，派生目录 `--check` 通过。首次冷启动 CS6 时 30 秒触发任务提前终止，1.2 KB 不完整产物和不确定标记已按协议归档；常驻 CS6 下两次 publish 成功，末次 SWF SHA-256 `D9FE85D6DC3ACC82FC46E76B8367A5DACFCBFDA491269AC00C3CD2E5F8E82317`。隔离候选 `c-a8192ca983f7-08846e81b3-20260926t005340653z-daa4e35f` 已通过 v2 完整性校验并选为本地开发入口，build identity `A8192CA983F7D7421C13B785D91BD59EAD2E9497C23F3A60ED25723D4AD7E6ED`，payload closure `AE9E8D3F000B2BFC9FD1195E88A817D7BC6A8072A3AED7CCE8DBF79D9A3D2143`，Core SHA-256 `1827ED22B4E54E9E348B6111AC2FFB5C3D8BCEC422015A5A73B7F8880A38EEF0`，原生 DLL SHA-256 `3C49BC4EA95178742C07A31E29B21C1C8153A271798FFFFC4EC9585217383BEB`，视觉目录 SHA-256 `4AA050C1FFBA4747984C92DB79AF45C04E5B6732446A09ED3A5E76724D85CF5E`；状态仅为 `candidate_built / NOT_DEPLOYED`，尚无人验。人验聚焦四形态位置/长度/Glow、命中或超射程后的消失、暂停恢复、切场景与能力撤销后的残影和 Flash 回退；开发环境的净性能比较暂缓。

## 16. 加强普通子弹 Glow 实战反馈与修正（2026-09-26）

维护者通过通常的本地开发入口实跑 §15 候选并提供战斗截图：整体弹体视觉可用，加强普通子弹的光效却呈灰暗硬边楔形。bootstrap 记录了 `c-a8192ca983f7-...` 的实际 Core 路径；Host 记录到原生能力与非零原生子弹帧，故本次截图属于原生接管实测，而非仅有影子数据。日志未见原生帧失败或解析拒绝。XFL 中常态与联弹单元体都对同一嵌套基底使用黄色 `GlowFilter blurX=8 blurY=8`；旧原生 shader 则按局部 blur 值向外叠画 8/16/24 三档低透明度硬边三角形，最终宽度还随弹体和镜头缩放。由源码与截图推断，灰暗楔形主要来自这项画法差异；若追求逐像素亮度/宽度匹配，再补同武器 Flash 原版截图。

修正 shader 保留原三角形弹芯与调色/LUT 路径，改为单个有界四边形中的三角形距离计算：内部保持实体填充，外部按连续高斯型函数衰减；对当前 `blur=8` 暂用 `sigma=4`、三倍 sigma 外界。它是明确的视觉近似，不声称逐像素复现 Flash 滤镜。原生着色器 `vs_4_0`/`ps_4_0` 严格编译和固定 C++ 工具链编译通过；AS2、资源 SWF 与视觉目录均未为本次反馈修改。另从日志发现 5 次短暂 `capture_not_ready` 与 DRS 尺寸交接同刻发生，随即重发原生能力；这会让存活子弹退回 Flash、后续新弹又走原生。Host 改为仅在捕获持续未就绪 250ms 后撤销能力，场景/表面/连接的真实退出仍立即清理。修正候选需重新实跑并核对 Glow、无重影及切换时机；正式 runtime 未更新，净性能比较仍暂缓。

修正后的 Host 全量测试 `5895 通过 / 5 跳过`，文档巡检与 `git diff --check` 通过。隔离候选 `c-4fdb4e0ddae4-08846e81b3-20260926t012020790z-825cf151` 已通过 v2 完整性校验并成为当前本地开发入口；build identity `4FDB4E0DDAE410F2905637ECAF4C66ED3BDAFC19D941180344696C91CBD5F7CB`，payload closure `396E615C684CDA3DBE741F6D1A765F03845EF0BAC5866DD6E77C617EBD4B376C`，Core SHA-256 `7D5DED81C6F07C66F15F80501D84D0FFCFD6460E830655D2678F181B324E9965`，原生 DLL SHA-256 `1DBACC0EFC270C451985410ACDEC7D9713E91BC1556F3718378F2CA45F05F44A`。截至构建时状态仅为 `candidate_built / NOT_DEPLOYED`；随后实跑见 §17。

## 17. 柔光修正候选的实战回归（2026-09-26）

维护者从通常入口运行 §16 的配对候选并完成一轮实战，反馈“感觉还可以”；截图中未再见先前显著的灰暗硬边楔形。bootstrap 记录的 Core 进程路径准确指向 `c-4fdb4e0ddae4-...`。本轮 `launcher.log` 有 192 条两秒影子汇总，合计 10,179 个帧包；四形态均出现，峰值普通 6、枪式普通 21、加强普通 15、枪式加强 43，单帧合计峰值 57，`invalid=0`、`overflow=0`。原生帧汇总出现 34 次，未见原生帧失败、影子拒绝、世界合成失败或帧任务异常。显示端撤销 15 次：切场景 10、表面隐藏 4、连接关闭 1；没有之前与 DRS 换挡同刻出现的短暂 `capture_not_ready` 撤销。

维护者随后实测暂停恢复，反馈功能可行；这是该候选的人工运行证据，本轮未另取帧级时序记录。源码复核显示：世界合成器致命故障会关闭本次游戏；子弹帧校验/提交失败只撤销子弹能力；Flash socket 断开会尝试重连，Ready 态超过 10 秒未恢复业务连接会触发紧急退出。因此保留存活弹体的 Flash 显示权交还，不要求注入式断连后由裸 Flash 完整续玩。现有正常切场景/表面隐藏已经发生，但日志不能单独证明切换瞬间没有重影或漏绘；超过 256 绘制条目时的在线容量处理、子弹通道拒绝后的显示权交还也尚未覆盖。当前可记为 `candidate_executed` 与本轮场景的初步观感通过，不据此推断长时稳定性、逐像素 Flash 滤镜一致或净性能收益。源码与候选暂保留待审，正式 runtime 未更新；BP1/BP2 的看板卡片不据此直接关闭。

## 18. 面板开合实测与极端密度优先级（2026-09-26）

维护者在配对候选 `c-4fdb4e0ddae4-08846e81b3-20260926t012020790z-825cf151` 中边发射子弹边打开地图 Web 面板，再关闭面板：目视确认子弹未越过 Web 层，关闭后正确显示并继续飞行，随后发射仍正常。`launcher.log` 的本轮 Core 路径指向该候选；10:01:12 面板打开时记录 `surface_hidden sent=True`，10:01:13 的影子汇总仍有最多 11 枚子弹、`invalid=0`、`overflow=0`；10:01:15 关闭面板后记录 `bullet_visual_cap_native`。日志证明显示能力交接与数据仍到达，遮挡和连续性由维护者目视验收；该结果覆盖本次地图面板操作，不冒充所有 Web 面板的逐项测试。

维护者指出同屏超过约 100 发时原 Flash 已承受极高压力、接近不可操作；这作为本机体验判断，不设为通用硬阈值。首批优先守住可操作战斗中的伤害/生命周期权威、无隐形弹、无越层或重影；暂停和地图面板已有人工验证。256 是三端共同的**白名单绘制条目**上限，普通弹每枚占 1，枪式联弹每个有效单元体占 1，非白名单不计；它不限制 AS2 实际运行的逻辑子弹数。当前先填普通弹，再填联弹；剩余容量放不下整组时，该组单向退回 Flash。保留此安全处理，但不要求用真实战斗凑出 257 个绘制条目，也不以极端负载帧率作为当前首批验收门。后续用定向注入核对 256/257 跨界、整组回交及子弹通道拒绝后的所有权；净性能对照仍暂缓。

吞吐量扩张不能只调高 256：原生接管时联弹仍为每个单元体保留 MC，`渲染组` 每帧写 `_x/_y`，影子采样再从 MC 读取并逐项序列化；当前 Host 另有 65,536 字符的子弹节解析门。后续若高密度日志接近容量或出现 `overflow`，先按绘制条目、组数、包长和 AS2 帧负担定位瓶颈，再决定同步提高三端上限，或优先从 `ChainUnitData` 直接产生视觉快照、减少已原生接管单元体的 MC 热路径；这仍须保留回交时的当前显示状态。当前实跑单帧峰值 57 条目，不能据此证明更高上限的净收益。

## 19. 首批视觉层正式交付前的边界验证（2026-09-26）

维护者授权将当前视觉迁移作为独立批次收口，联弹组级近似算法不并入本次正式 runtime。新增真实 CS6 focused TestLoader：普通弹第 256/257 项、断连与能力撤销恢复、两单元体联弹组在 254+2 与 255+2 边界的整组回交，共 11 项行为断言。测试类首轮因缺显式导入编译失败，修复后重跑得到本轮 Compiler `0/0`、`11/11`、唯一有序 Start/Complete trace，TestLoader scratch 已复原；失败轮不计入通过。Host 新增普通/联弹合计 256 接受、257 拒绝的协议断言，Launcher 全量 `5896 通过 / 5 跳过`。`node tools/validate-melee-longgun-chain.js`、定向派生 `--check`、文档巡检与 `git diff --check` 通过。

asLoader 经本轮真实 CS6 publish 得 Compiler `0/0` 并刷新到 1,376,857 字节，SHA-256 `DC3FDACF0E7F5792AF6BF1713F8B3B112FD60C158807A999DCD0C6C0B15A39DA`；主 SWF/asLoader class child-only 与 single-ownership 审计通过。子弹资源 XFL 本轮未改，现有资源 SWF 与派生目录摘要由 `--check` 精确匹配，沿用此前新鲜资源发布证据。以上验证不代签正式双 builder 共识、promotion、标准入口业务回归，也不覆盖 Host 异常帧到 AS2 回交的注入式端到端路径；发布结果待同一文档继续回填。
