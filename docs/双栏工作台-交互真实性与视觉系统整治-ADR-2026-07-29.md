# 双栏工作台交互真实性与视觉系统整治 ADR

- **状态**：Accepted / Implementing（G0、G1–G5、F0 与本轮定向视觉 F、A1、A2a/A2b、A3、A4、B1–B7、C1–C3、E、右侧条件槽与 D 自动策略矩阵已完成源码及各自自动门；D 已在 exact candidate 上完成本机 150% DPI 的真实双宿主 production/development smoke，但 100% DPI 与真实触控/触控板 pinch 仍缺物理设备证据；B7 真实 Native/legacy HUD 与游戏内 WebView2/标准入口旅程、剩余旧 CSS / G6+ 尚未完成，不得外推为整体完成、游戏 E2E、promotion 或部署）
- **日期**：2026-07-29
- **最后核对代码基线**：当前 HEAD 与 `origin/main` 均为 commit `c6ca84714d134310793defda893772aac2c0d5ea`；`c96f4c3d750561022b706c72a4d53050431e627d` 只保留为本轮起始审阅与 F0 债务锚点。并复核 2026-07-30 当前施工树中上列已完成批次；这些后续源码尚未形成可发布的 immutable Git tree
- **文档角色**：本轮施工的 ADR、验收合同与抗 compact 续作备忘；每个批次只在同树验证完成后更新状态，不把整体规划冒充完成记录
- **适用范围**：Launcher Web 双栏工作台，以及为其提供权威状态、输入约束和导航闭环的 Host / AS2 接缝
- **稳定规则归属**：长期约束回写 [双栏工作台 UI 系统约束](../agentsDoc/workbench-ui-system.md)；尚未落地的目标态必须在 canonical doc 逐段标明“未实装”和所属批次，不能写成现役 API、门禁或已治理事实；对应实现与验证同轮完成后才移除标记。验证入口仍以 [testing-guide.md](../agentsDoc/testing-guide.md) 为准

> 本 ADR 已把临时用户反馈、临时外部审阅、截图、生产代码和现役 harness 重新核对成可独立续作的决策记录。临时输入不是后续施工依赖，也不作为代码真源。

## 0. 决策摘要

ADR 已冻结，施工按原子批次推进；以下总决策既约束已完成批次，也约束尚未落地的目标态。

这轮问题不是一组互不相关的“美化建议”，而是五类跨页面、跨层重复出现的系统性缺陷：

1. **界面没有如实投影当前能力**：可见按钮会静默无效，隐藏属性与实际可见性不一致，帮助入口和业务能力被玩家误认为有关联；
2. **用户所选对象、当前浏览对象、权威写入源混在一起**：候选数量、候选详情、候选调制和左栏来源标记缺少统一语义；
3. **决策区与导航栈没有稳定合同**：唯一主 CTA 会滚出视野，Esc 会跨层跳转，空态、错误态、不可执行态没有给出符合直觉的下一步；
4. **整备信息架构跨入口漂移**：技能等装备相关能力散落在刘海“游戏”行和 Character Build 直达按钮中，历史共同分类、现代 owner 边界与右侧常驻入口没有形成一致合同；
5. **视觉与动效规范部分停留在文档层**：本轮基线时布局 profile、颜色 token、字号下限、滚动条、named layer 和 reduced-motion 在生产 feature 中仍有明显 drift；E 已闭环 profile，G1–G5 已闭环 SecondaryPage、focus、语义 motion 与 reduced-motion，定向视觉 F 已关闭本轮识别出的排版、整组 blocked opacity 和三个真实滚动面缺口；剩余旧色值、skin 结构与 named-layer 扩围仍按后续独立原子批记账。

本轮采用以下总决策：

- **先修交互真实性，再做视觉统一**。任何可见控件必须真实可用，或以可读原因明确禁用；禁止静默 no-op。
- **接通现有能力，不再引入一套 UI 框架**。可复用 UI 边界仍只新增 Inventory header 状态投影与 `DualPaneShell` 封闭 profile；整备导航只扩展 feature-local 菜单呈现和 exact Character Build 关闭后固定目标意图，不建设通用导航 registry / DSL。
- **复用现役 primitive**：`HelpAction`、`ChoiceGroup`、`QuantityControl`、`PanelTooltip`、`CommitBar`、`FocusScope` 和现有 Equipment Tuning inventory source 能力优先。
- **把“浏览焦点、用户选择、权威来源、动作目标”分型**，不再用一个模糊 `item` 或 `_build` 是否存在推断行为。
- **主 CTA 永不进入内容滚动层**；阻断原因保持正文对比度，禁用态只压低控件能量，不把“为什么不能做”一起淡化。
- **把装备、战备箱、装备调制、技能、材料、情报统一为“整备”信息架构**。刘海分组与 Character Build 的整备菜单共享同一成员、命名和顺序；只统一玩家入口，不抹平同实例 view 与跨 owner panel 的生命周期差异。
- **右侧常驻 HUD 的条件槽只承担上下文说明、可操作通知或事务决策之一**。它不复制六项整备导航，也不与任务通知、SafeExit 共用时同时争夺命中区。
- **颜色、字号和动效采用触碰模块清零 + 总债务只降不升的 ratchet**，不把 1332 处存量色值和 P0 交互修复塞进一个巨型改动。
- **K3 最高思考强度完整异构对抗是本 ADR 的接受门和最终收口门**。ADR 接受前与早期施工树的完整审阅已结束并由主线逐条裁决，但旧 GO 只作为历史证据；当前 `605f…` source-ahead 加固树仍须以同一模型和最高思考强度完成最终增量复审、复现并裁决，不能因耗时或服务不稳降级，也不得拿旧 session 的 GO 冒充本轮终局。

## 1. 范围与非目标

### 1.1 本轮覆盖

- Character Build、Storage、Stats 与嵌入式/独立 Equipment Tuning；
- KShop、NPC Shop、Crafting、Loot、Skills 中采用工作台语言的页面；
- Native `NotchWidget`、fallback `NotchOverlay` 的“游戏 / 整备”分组、右侧 `RightContextWidget` 条件槽，以及 Host 固定整备导航意图到工作台的入口一致性；
- `DualPaneShell`、header action、EntityTile、HelpAction、QuantityControl、CommitBar、SecondaryPage、PanelTooltip；
- 工作台 CSS token、profile、排版、颜色、滚动条、状态层、动效层与 visual atlas；
- 与候选计数、候选 tooltip、候选调制、Ctrl+滚轮相关的 AS2 / Host 接缝；
- 真实 WebView2 内的焦点、缩放、Esc、滚动与玩家文案。

### 1.2 本轮不做

- 不新建动态组件注册表、action DSL、统一领域状态机或万能卡片；
- 不新建可注册的全局 NavigationRegistry、任意 `panelName/initData` 路由协议、最近使用入口、split button 或多级 mega-menu；
- 不把所有领域 preview/reconcile 合并成一个可配置大组件；
- 不一次性重写 `skins.css`、重排全部 cascade、拆完 legacy class 并同时改任务流；
- 不为了减少字面量给每个独特色值、间距或 z-index 都造 token；
- 不把帮助、tooltip、close、preview 变成业务 capability；
- 不把右侧常驻 HUD 条件槽改造成第三套整备导航；不让 hover 才出现的子菜单承担唯一入口；
- 不手工修改 Flash 资产或 SWF；B5 的协议纵切触及 `.as`，必须遵守 UTF-8 with BOM 和 Flash CS6 focused fresh trace 验证要求，且本批不据此发布 `asLoader.swf`；
- 不把本 ADR 称作功能完成、编译通过、部署完成或 `standard_entry_verified`。

## 2. 代码真值与基线快照

### 2.1 当前自动门

以下表格保留各原子批收口时的 fresh 快照；B5/G4 等较早行不是当前整合树总数。case 数只描述当次 runner，不冻结为长期合同；表后的“当前整合树”才是本 ADR 最新证据摘要：

| 检查 | 结果 | 说明 |
|------|------|------|
| A2b：`run-equipment-tuning-harness` + confirmation/interaction/source/model | 通过 | 三视口各 `96/96`；专项 `4/4 + 7/7 + 2/2 + 51/51`，覆盖固定 CTA、稳定 preview identity、source、锁投影与 ChoiceGroup 回滚 |
| A3：shared components/focus integration + NPC secondary + KShop/NPC/Skills/Loot 领域 harness | 通过 | 覆盖 SecondaryPage exact actions/header suppression、Help、player-copy、keyboard inspection、locked entity 与异常 mount fail-close |
| B1：`PreparationNavigationTests` focused | `25/25` | frozen 六项 tuple、off 旧生产 exact-set、on Native 四行/legacy 两行、progression reason 与同源几何 |
| B2：Host/Web + Flash focused | 通过 | Web workbench `23/23`；Flash run `dc1909f0ff9d43e19c099c4bf9f7a413` 为 EquipmentTuning `55/55` + InventoryPanel `144/144`、compiler `0/0`；行为只以该 fresh trace 为证 |
| B4：固定 post-close 基座 | 通过 | Host focused `381/381`、Launcher 全量 `1681/1681`；inventory modules `26/26`、Character standalone `215/215`、production `768/768 + 4/4 hidden-body`、Panel runtime `27/27`、contracts `62/62`；只启用 Skills，未触及 AS2/Flash |
| B5：Materials exact handoff | 通过 | Host focused `249/249`、Launcher 全量 `1707/1707`；Flash run `bec9c608004d48e0bcb18a7f1c0f5708` 为 EquipmentInventory `28/28`、NPC `46/46`、Inventory `144/144`、Crafting `36/36`，Compiler Errors/Warnings `0/0`、32K retry `0`；仅刷新 TestLoader，未 publish `asLoader.swf` |
| Right slot：RightContext/SafeExit/RightHudLayout/NativeHud focused | `88/88` | 覆盖 owner priority/switch/empty、装备 hint、真实 Bounds 订阅、Bitmap alpha 与 hit-test 排他 |
| E：profile / CSS / 真实 feature fixture | 通过 | default strict 与 release-tree strict 均 `0 error / 0 warning`；profile enum/atomic setter、`WB128/WB129`、六种 profile 的 1024×576 overflow/computed split、root/body footer 与真实领域 CTA/focus 均有 fresh 自动证据 |
| G1：shell-level SecondaryPage cascade / structural motion | 通过 | KShop `128/128`、NPC `106/106`；normal computed style 证明 `display:grid`、`z-index:55` 与真实约 `210ms` 进退场，reduced-motion 真实 consumer 探针证明 `0s`、无 animation 且直接落静态终态 |
| G2：生产 focus role 最终计算值 | 通过 | KShop `134/134` 覆盖 button、EntityTile、input、select、range 与 selected+focus；Character workbench 三视口 `768/768`，另有 slot selected+focus / stats scroll runner 门 `5/5`；无 skin 的 Loot `75/75` 证明封闭 profile 边界仍消费 `--wb-focus`；focus `12/12`、integration `15/15`、components `16/16`、primitives `10/10` |
| G3：语义 motion / neutral keyframes | 通过 | Skills 三视口 `150/150`、KShop `134/134`、Equipment Tuning 三视口 `96/96`；normal/reduced computed fixture 证明 reject/busy 在 normal 消费 exact duration/easing，在 reduced 归零且 outline、图标与文案不消失。全 CSS ratchet：raw color `2733→2728`、raw duration `164→147`、raw easing `115→105`、`transitionAll` `16→16`，ceiling 同轮精确下调 |
| G4：reduced-motion 收口与壳外 owner | 通过 | ratchet `32/32`；KShop `134/134` 另以 actual probe 覆盖 shell busy、exact workbench Tooltip owner、`tasks` 反例与 Equipment Inspector transition；Tuning 三视口各 `96/96` 并证明 ambient normal/reduced 静态终态；Character production 三视口合计 `786/786` + hidden/storage `5/5`，真实 renderer 全部关闭 animation。raw duration ceiling `147→144`；priority ledger 中 Inventory `4→2`、Skins `11→10`、Character `2→0`，没有新增 `!important` |
| Launcher xUnit 全量 | `1707/1707` | B5 完成后的同树总回归；不替代各专项 runner，也不等于 candidate 或部署 |

当前整合树的 fresh 摘要如下；它与上表历史批次、2026-07-28 已发布 Character 身份和下述 D-only candidate 都必须分开审计：

| 当前整合检查 | 结果 | 边界 |
|--------------|------|------|
| Character / shared Web | 通过 | production workbench `867/867` + hidden/lifecycle `12/12` + preparation menu `18/18`；inventory modules `31/31`、standalone 三视口各 `218/218`、dressup `222/222`、Tuning 三视口各 `115/115`（model `61/61`、runtime `25/25`、confirmation `4/4`、interaction `7/7`、source-marker leaf `2/2`）、Loot `77/77 + lazy 6/6 + player-copy 196 checks / 0 failures`、Crafting `119/119`、Intelligence `30/30`、Skills `150/150`、KShop `135/135`、NPC `106/106 + reduced 2/2`；components `16/16`、primitives `10/10`、focus integration `15/15`、preparation leaf `10/10`、item matrix `20/20`、atlas `48/48` |
| CSS / profile / ratchet | 通过 | ratchet `66/66`；default/release-tree strict 均 `0 error / 0 warning`；CSS bundle 20 imports / 690093 bytes；当前 ceiling 为 raw color `2637`、`<9px=0`、9px 玩家文字 `34`、raw duration `144`、raw easing `105`、`transitionAll=16`；WB040 已覆盖 spaced priority、末声明省略分号与任意 `all:* !important` reset |
| Host | 通过 | H1 定向历史门 `253/253`；合并上游后 Launcher 全量 fresh `1902 pass + 3 explicit opt-in skip / 1905`、0 fail；首次全量的既有 SkillTask 20ms wall-clock flake 经隔离 5 次均绿后仅把测试 timeout 调至 250ms，确定性 timeout 负例仍在。B0-05/B0-06 formal 与 B0-06 visual capture 本轮保持 explicit opt-in skip；只完成 qualification contract `3/3`、runner parser `0 error` 与失败 envelope/affinity AST 纯内存门。上游 F2 的 `48/48 / b0_accepted` 仅属其精确 base closure；当前右槽 workload 断言使其不能冒充本工作树 formal qualification；含 B7、Materials/Intelligence exact child return、双宿主 `WebView2BrowserPolicy`、配置/env 与 Host-reserved accelerator 边界 |
| Flash focused | 通过 | item panels run `49b52f058b4046bdba5dcafffd6bb50f`：EquipmentInventory `28/28`、NPC `46/46`、Inventory `144/144`、Crafting `36/36`；Character run `a5d8512263894ded9540aedec991c643`：`539/539`；Tuning run `a6f2a1abb3dd44189e9cd1ab4583490b`：EquipmentTuning `55/55` + Inventory `144/144`；三轮均为唯一闭合 block、fresh trace/output/errors、compiler `0/0`、32K retry `0` |
| D 双宿主实机 | 部分完成 | exact candidate `c-2da549ad5580-08846e81b3-20260729t095347915z-04dc7b0c` 在本机 150% DPI 完成 production/development Bootstrap/Overlay smoke；100% DPI 与真实 pinch 未覆盖。该 candidate 严格为 `candidate_built / NOT_DEPLOYED`，没有当前业务树游戏 E2E |
| K3 定向 F/G 复审（历史） | GO | session `90acd753-1f13-4df4-897d-5fd92ebd6806`，固定 k3 最高 thinking；六项条件 finding 当时已关闭，0 overdesign / 0 不合理交互 / 0 视觉阻断；只覆盖该轮冻结子集 |
| K3 早期全局终审（历史） | GO；已被后续源码覆盖 | session `2c40f85c-a99b-4c94-b0c3-84d56f6d3f56`，当时冻结集合 176 文件 / 12,140,990 bytes / aggregate `3C9C734C…64A5C1`；stdout `5AC26B9C…71899FF`，当时为 `0 Blocker / 0 High / 0 Medium / 0 新 Low`，详见 §14.9。当前 `605f…` source-ahead 加固树的最终异构复审仍待完成，不得继承该 GO |

G0 已恢复可相信的基线：terminal unlayered semantic-hidden utility、Help action hook、窄 HeaderProjection 与往返 exact-set 均有自动证据。后续批次不得删除这些门、放宽断言或重新引入 feature 可见 `display:... !important`。

现役 resolved CSS 闭包的其他 `!important` 也已进入 `tools/workbench-important-ledger.json` 的逐文件单调台账；这只冻结历史债务及其退出条件，不把它们升级为推荐机制。新增未登记 priority、上调 ceiling 或清理后不在同轮降表均由 strict audit 拒绝。

当前 publish-only `scripts/asLoader.swf` 为 1,065,911 bytes，SHA-256 `BAB9E46D140739F753262264DB326039432552783E87CC184DE8C423AD084CAB`，9,723 functions、最大 46,025B、compiler `0/0`；目标精确为 `scripts/asLoader/asLoader.xfl`，没有编 main。publish 的 `flashlog.txt` 未刷新，因此字节只证明发布动作，行为仍由上表 current focused runs 证明。D 批 exact candidate 的 build identity 为 `2DA549AD558013C964D18AF74680FCD9CD9A45CAE42439ECDDF122811F7F2038`，payload closure 为 `89B98ACDCEA6343D6C5C20F9A2DD8CAF9E620EC9ED4A521BC2AE8C55AED5E08B`，Core SHA-256 为 `DD7E1764A664969A19C8358B2CC99174DE106E6B8BF02AD8B6CC9881CCFA175F`；它只承载双宿主输入 smoke，严格为 `candidate_built / NOT_DEPLOYED`，不登记为当前业务树 `candidate_executed`、游戏 E2E、promotion 或标准入口证据。

### 2.2 “假按钮”的确切根因（`c96f4c3d750561022b706c72a4d53050431e627d` / G0 前历史）

在 `c96f4c3d750561022b706c72a4d53050431e627d` 基线中，`inventory-workbench.js` 已按 view 给 storage action group 设置 `hidden`，但 `inventory.css` 的 `.inventory-workbench-mode-actions { display:flex; }` 当时覆盖了浏览器 `[hidden] { display:none; }` 的表现。结果曾是：

- Stats 中出现本应隐藏的“返回构筑”“装备调制”；
- Character Build 中出现本应隐藏的同一组按钮；
- handler 又正确拒绝当前 view 下无意义的请求，于是玩家曾看到“能点、没反应”的假按钮；
- “返回构筑”还与同屏箭头返回形成重复语义。

这不是单个按钮漏绑事件，而是一个必须上升为共享约束的问题：**语义 hidden 必须在 computed style、命中区和可聚焦性上同时为真**。G0 已以 terminal unlayered utility、`WB-HIDDEN-001` 台账和 computed display / hitbox / Tab 自动门关闭该历史故障；只断言 DOM attribute 存在仍然不够。

### 2.3 视觉与动效债务快照

以下是 F0 冻结时对 `launcher/web/css/workbench/*.css` 的评审基线快照；G1–G5 的现役变化在本节随后逐批记载，不能把这些历史数字误读为当前树计数：

- 裸颜色共 1332 处：1046 个 hex，286 个字面量 `rgb/rgba`；
- 分布最重：`skins.css` 460、`skills.css` 203、`equipment-tuning.css` 187、`inventory.css` 165、`core.css` 134；
- `9px` 共 148 处，其中存在长中文、错误原因和主标签错误降级；
- 多个 feature 重复定义滚动条，代码中只有局部规则移除 Chromium 箭头；主线人工查看的反馈截图也出现白色系统滚动条，但该截图不作为仓库长期证据；
- motion token 已存在，但仍有 `.08s`、`.1s`、`.12s`、`.25s`、`.28s`、`.75s`、`1.8s` 等野生时长；
- reduced-motion 同时使用 `.001s`、`.001ms` 和 `none`，壳外 tooltip 与 JS RAF 还没有统一策略；
- `components.css` 的 SecondaryPage 使用 `opacity/transform/visibility`，KShop/NPC 的 unlayered `display:none/display:grid` 会让退场直接消失，实际绕过共享 structural motion。

G1 已关闭最后一项在三个 shell-level consumer 上的现役缺口：KShop settlement、NPC settlement 与 NPC help 不再由 unlayered skin 接管 `position/inset/z-index/display`，共享 SecondaryPage 的结构与进退场现在真实生效。`.npcshop-space-page` 是 settlement 内嵌 organizer，继续保留本地 `inset:0 / z-index:4 / display:none|grid`；Character Stats 与 Doll Preview 的全 body 几何也仍由各自 host 拥有，均不在 G1 壳级迁移范围内。

G2 曾以限定 `.workbench-shell[data-profile]` 的 unlayered 过渡 bridge 关闭生产 DualPane 的 focus role 缺口；G5 随后删除历史 feature/skin 的 `outline:none/0` 并退出该 bridge。当前 button、EntityTile、input、select、range、可聚焦滚动区只从 named `workbench.states` baseline 消费 `--wb-focus`；选择态使用 `--wb-role-selected` 边框，不占用同一 outline。Character slot 保留唯一 inner-card focus 环例外，并由 selected+focus computed gate 证明两种角色同时可辨；`WB135` 拒绝 bridge 或未登记 unlayered focus outline 回流。

G3 已关闭既定工作台 motion/keyframe 缺口：`skills/entities/inventory/core/equipment-tuning/skins/character-build/components` 与 foundation 中现役 KShop consumer 不再保留本批所列裸时长，过渡只使用允许属性并消费 micro/standard token 与 `--wb-ease-standard`；busy/reject/ambient animation 同样消费既有 standard easing。`kshop-pulse` 与 `skills-reject-pulse` 已由 shared motion 域中的 `wb-pulse-busy` / `wb-reject-pulse` 取代，reject 颜色消费 `--wb-role-reject`，Equipment Tuning 的唯一环境核心保留在 feature 同域并消费 ambient token。静态 exact 门与 normal/reduced computed fixture 同时守住时长、名称、最终动画和静态状态保留。G3 没有扫描或修改 lockbox/非工作台 `transition:all`，没有新增 JS reduced-motion helper、全局 `!important` 或 easing/token 体系，并把 `.001s/.001ms` 明确留给下一原子批。

G4 随后关闭工作台 reduced-motion 尾项：删除 `skins.css` 与 `character-build.css` 的 `.001s/.001ms` 伪关闭；删除 Loot 与 Equipment Inspector 对 shared root 的重复 reduced 声明；Tuning 本地规则仅保留状态可见所需的 `opacity:.55` 静态补偿。壳外 `#panel-tooltip` 只在 `#panel-container` 属于 exact `kshop/workbench/npcshop/crafting/skills/loot` 时关闭 loading pulse，`tasks` 反例保持原行为；Character renderer、Equipment Inspector 与 Tuning 图标现有直接 `matchMedia` 分支均由 actual normal/reduced probe 证明有效，所以没有为形式统一增加 helper、registry 或常驻 listener。SecondaryPage 的 `visibility 0s linear` 延迟离场合同没有误删。非工作台 `task_panel.css` 的 `0.001s` 是有意保留的范围外存量；历史 outline bridge 退出由 G5 独立完成，其余 structural bypass 与 named-layer 扩围改记 G6+。

这些数字是排序和 ratchet 的基线，不构成“一次性全部替换”的授权。

### 2.4 布局与 atlas 的证据边界

E 批已把 canonical doc 的六种语义 profile 接入生产：

- `workbench-profile.js` 只持有封闭枚举、共同 validator 与原子 setter，并以 frozen `validProfiles` 作为运行时、ratchet 与 profile 纯 Node 叶门共同消费的唯一合法列表，工具与测试不得复制枚举；`DualPaneShell` 在创建 DOM 前验证构造 profile，`setProfile()` 失败时内部值与 `data-profile` 都不变化；
- 所有 6 个生产构造调用都携合法 profile；KShop 与 Inventory 在同一 Shell 原位切 view 时通过 setter 同步 profile，Skills 与 Crafting 切 view 时则按目标 view 重建并构造 Shell；feature 不直接写该 attribute，也不在构造后追加同值 setter；
- `workbench/profiles.css` 独占壳级 `grid-template-columns/rows`，现役 feature 壳级覆盖与登记例外均为 0；compact 不再暗改宏观 split；
- Loot root CommitBar 使用 exact `data-workbench-shell-footer`，Storage body 批量栏使用 exact `data-workbench-body-footer`，profile 不从身份臆测 footer presence；
- 48 场景 atlas 仍使用同一套 synthetic shop/catalog DOM，并在三个 16:9 viewport 中切换 density、focus、reduced-motion 和 SecondaryPage；它只证明共享 primitive 的 scale / DPI 组合；
- Character Build、Storage、Tuning、Crafting、Skills、Loot 及 KShop/NPC 的真实 harness 已分别补入 1024×576 overflow、computed split、长列表/长中文、blocked/busy、固定 CTA 与 focus 断言，覆盖六种 profile。

因此证据继续分层：共享 atlas 不替代领域 fixture；领域 fixture 也不替代剩余逐文件视觉债务、G6+、真实 WebView2 smoke 或人工体验旅程。

### 2.5 整备信息架构与现役路由真值

旧 Flash 的稳定心智不是某个永远不变的页签顺序，而是“装备相关功能共处一个 Hub、平级一步切换”：

- 旧版 Hub 已有物品装备、个人信息、技能三个实装成员和一个“未实装”占位；装备与技能从旧版起就是一步平级切换；
- 最新 `flashswf/UI/物品与技能相关界面/LIBRARY/新版物品栏界面.xml` 才进一步把材料、情报纳入同一时间轴，五个入口仍全部一步直达；个人信息的相对位置同时发生变化，说明不能把某个历史顺序当协议；
- 主 XFL 中承载后勤战备箱的通用仓库界面是物品栏的 sibling 窗口，不是上述 Hub 页签；但 `InventoryIcon.quickMoveToTarget()` 明确支持背包与仓库/战备箱双向搬运，属于强邻接工作流；
- 现代“装备调制”统一承接旧强化、强化度转换和插件改装能力；旧组件原本嵌在物品栏帧中，背包装备可拖入并热切。它有强邻接依据，但不是旧版顶级页签。

因此本轮复刻的是**共同分类、熟悉命名和一步直达**，不是把旧 Flash owner、帧层级和顺序原样搬到 Web。

从 committed 基线到当前 B7 施工树有四个必须明确区分的差异：

1. committed 基线中 Character Build **Build view** 的意图集合是 `storage | stats | skills | help | close`；B7 当前源码默认 on 后 exact-set 已原子切为 `preparation-menu | stats | help | close`，显式 off 才恢复旧集合。Stats 始终另有 `back-build`；截图中的额外“装备调制”曾来自 §2.2 的 hidden 泄漏，不是合法基线；
2. B3 已让 Build → Storage/Tuning 都成为同一 Workbench instance 的有界局部 view 切换；它们不进入跨 owner intent；
3. Build → Skills 已有 exact-instance close/finalize/visual-retire/coordinator-settled 的严格交接；
4. Materials 与 Intelligence 都不能在 Character authority 仍绑定时直接打开；B5/B6 已分别接通 settled 后的 material nonce wait 与 Intelligence 同步 exact admission，B7 则只把这两个既有固定目标连同其他四项投影进最终整备菜单，不放宽 route authorization。

committed 基线曾把战备箱、情报、材料、技能与战队、平板、商城全部塞在“游戏”行。B1/B7 当前源码已让 Native `NotchWidget` 与 legacy `NotchOverlay` 消费同一整备 frozen tuple，并分别形成四行和至少游戏/整备两行；真实双渲染器 GUI 旅程仍须单独验收，不能从源码 fixture 外推。

## 3. 用户反馈逐项裁决

### F1：进入具体装备分类前看不到背包各分类数量

**裁决：接受，属于信息架构缺口。**

在基线 `c96f4c3d750561022b706c72a4d53050431e627d`，`CharacterBuildService.as` 只在选择目标槽后扫描背包并返回候选，没有投影始终可见的 category facet counts。玩家为了回答“我有没有这类装备”必须反复进入“刀 / 长枪 / 手枪”等目标，操作成本过高；本轮 C1 已按下述合同完成工作树施工，但尚未部署。

决策：

- Character Build 初始状态应能显示与构筑槽分类一致的候选数量；
- 数量是权威快照的只读 facet，不是 Web 本地按名称猜 taxonomy；
- `0` 也必须显示，不能用“没有角标”同时表示未知和零；
- facet counts 与当前筛选、权限和候选口径一致，写后随 snapshot 刷新；
- 计数只做 `CountBadge` 的 CSS/data slot，不新建 JS 组件体系。

### F2：角色构筑候选没有 tooltip，必须去收纳查数值

**裁决：接受，属于跨页面信息断裂。**

基线 Character Build 候选只绘制图标、名称与少量 delta，没有接入共享 `PanelTooltip`。候选 payload 已携带 exact inventory source，现有 Inventory Workbench 也已有 scoped async tooltip 样板；C2 已按下列边界完成。

决策：

- 为 Character Build 增加领域 tooltip adapter，复用现役 `inventoryTooltip`，不新增 AS2 endpoint；
- Web 直接复用现役 `PanelTooltip.createScope().bindAsyncHover(...)`；
- pointer 与键盘 focus 信息等价；
- tooltip key 必须含 exact candidate identity 和会话 fence，迟到结果不得复活 detached binding；
- tooltip 只负责说明，不持有选择、装备或调制 capability。

现役 C2 只接受 exact active workbench instance 上的 `inventory/tooltip`，payload 必须是 exact `{v,source,context}`，其中 `context={kind:"character_build_candidate",sessionGeneration}`。只要出现 `payload.context` 属性——包括 `null`、primitive、近似 kind 或多余字段——都必须进入严格分流并 fail closed，不能回落到普通 Inventory tooltip。Host 只放行最近一次已验证 candidates 回包中的 exact 背包 source；blocked 行仍可说明，伪造/旧 lease 不可借 tooltip 获得权威。snapshot、新 candidates 请求、任一写、rebind 或 close 都推进 completion fence，迟到回包只能降级为 `stale_state` 且不得复活富内容。Character 路径严格重建 rich response；无 context 的普通 storage tooltip 保留既有 generic lease-only 合同。Web scope/cache 绑定 candidate identity、session generation、slot 与 lease，失效后不自动重放或重试。

### F3：只能调制已装备物，不能调制当前候选

**裁决：接受；C3 已按下述边界接通。**

基线 Character Build adapter 只接通 `sourceKind:"loadout"`；共享 Equipment Tuning View 与上下游权威链已经支持 `inventory` source，候选 payload 也含 `containerId / slot / expectedLease`。C3 因此只补窄适配与刷新/返回事务，不要求玩家先穿上再调制。

决策：

- 选中可调制候选时，Character Build 将 exact inventory ref 交给现有 Tuning View；
- 不新建通用“来源选择总线”；
- 调制写入后必须刷新 exact candidate lease、候选 snapshot、tooltip cache 和当前选择；
- 候选已移动、过期或被装备时 fail closed，并以玩家语言解释，不对 stale intent 自动重放；
- 返回后恢复同一候选的语义 focus key；实体已不存在时使用明确的相邻回退规则。

现役实现让已选择且可调制的候选优先成为“调制候选”目标；没有候选选择时才回落到“调制当前装备”。`character-build-tuning-adapter.js` 严格重建四键 inventory source，拒绝 blocked、非装备、强化等级无效、near-shaped 或同物理槽歧义输入，并通过现有 `EquipmentTuningView` 的 inventory selection 进入同一 preview/commit 流。写后先完成已挂载 Storage coordinator 的窄 external-write/cache 收束（Storage 未挂载时不激活它），再读取 exact Character target、更新新 lease 候选、使 tooltip cache 失效并重绑当前 tuning source；entry/session/panel/target/request generation 任一漂移都丢弃迟到结果，未知或 stale intent 不重放。

返回优先匹配“同 physical slot + 写后新 lease”；实体消失时固定 next→previous，均不存在或出现同槽歧义时留在当前槽并聚焦候选标题，以玩家文案解释。滚动恢复顺序固定为先应用保存值，再以 `preventScroll` 聚焦语义目标；只有目标越出候选 viewport 时才按实际 PanelScale 做 nearest 校正，原位置仍可见时不扰动滚动。该机制不新建 endpoint、通用来源总线、第二套 cache coordinator 或跨领域状态机。

### F4：不打开“调制说明”似乎不能拆装配件

**裁决：症状成立，因果推断不成立。**

代码中配件安装、替换、卸下并未由 info panel 解锁；帮助只负责解释。真实问题是：

- “安全 / 快速”命名不能表达提交行为；
- 默认模式下点击会生成 preview，但唯一确认按钮位于可滚的 `.equipment-tuning-preview` 内；
- preview 最大高度 172px，材料和差异较多时 CTA 会滚出视野；
- 缺少明确的“已生成预览，请在此确认”注意力迁移；
- 玩家于是把“打开说明后才成功”误认为能力门。

决策：

- 帮助打开与否永远不得改变 capability，并加入负向断言；
- “安全 / 快速”改为更接近行为的“逐次确认 / 单件快捷”；
- 旁边常驻一句短说明：“批量、连锁与卸下全部始终需要确认”；
- preview detail 与固定 `CommitBar` 分离，主 CTA 不进入滚动层；
- 强化 number/range/mark/stepper/cap 生成 preview 后保持发起控件焦点；只有候选/交换 preview 才把确认区带入可见范围并允许迁移 CommitBar 的语义焦点；
- 不为此新造万能 `StagedCommandBar`。

### F5 / F6：Stats 与 Character Build 顶栏出现重复、无作用按钮

**裁决：P0，属于交互真实性缺陷。**

根因见 §2.2。决策：

- 先加 `.workbench-shell [hidden]` 等价的共享不变量，确保 hidden 在 computed style 上为 `display:none`；
- header action 状态由一个窄的 `InventoryWorkbenchHeaderProjection` 纯投影生成；
- 输入仅限 `{view, statsMode, returnTarget, buildAvailable, tuningAvailable, tuningState, busy, preparationAvailability}`；其中 `tuningAvailable` 是当前实例能否进入 tuning 的窄 capability，`preparationAvailability` 是 §5.6 固定目标键的窄状态记录，不把领域 profile、handler 或任意 destination descriptor 交给 presenter；
- 输出每个 action 的 `visible / disabled / pressed / label / reason`；整备菜单项额外输出 `current / destinationKind`；
- handler、领域路由与业务 authority 继续由 facade 持有；
- 同一语义不允许同时出现文字按钮与图标返回；
- 每个 view 的 header action 集合必须由状态表完整枚举并自动验证。

该状态表是 feature 内封闭的字面量映射，不是可注册 descriptor、action DSL 或跨面板 registry。

### F7：“安全 / 快速”不可理解，“卸下全部”仍需二次确认

**裁决：接受，属于模式命名和反馈错误。**

“快速”当前只适用于一部分单件 install / replace / detach；批量、cascade、enhance、conversion、“卸下全部”仍应预览确认。玩家按日常语言会把“快速”理解为所有动作立即执行，当前命名违反直觉。

决策：

- 使用 `ChoiceGroup` 承担选择机械，领域继续决定哪些 operation 可 quick commit；
- 标签采用“逐次确认 / 单件快捷”；
- 独立 battlebox 与 Character Build 嵌入态都投影同一个 confirmation mode 与同一份持久偏好；嵌入态在自身 PaneChrome/决策区呈现 ChoiceGroup，不把更多动作挤进全局 header；
- 常驻短说明必须直接位于选择器附近，详细矩阵放 HelpAction；
- “单件快捷”只省略符合白名单的单件 install / replace / detach 的人工确认动作；仍须先取得权威 preview / opaque token，并复核 source lease、revision、写入单飞与未知结果 reconcile，选择器不得跳过任何领域安全边界；
- 批量、cascade 与“卸下全部”始终不自动提交；触发后直接进入现有 CommitBar 的一次显式人工确认，不再叠加第二个确认模态；
- busy 期间选择器 visibly disabled，原因仍可读。

### F8：Ctrl+滚轮会改变 UI 大小，且没有提示或操作锁

**裁决：接受，属于 overlay/Host 输入不变量。**

commit `c96f4c3d750561022b706c72a4d53050431e627d` 基线的 `WebOverlayForm` 尚未冻结用户浏览器缩放；当前施工树已由两个宿主共同消费 `WebView2BrowserPolicy` 修复。该问题不能靠每个页面各绑一次 `wheel` listener 解决。

决策：

- 在 WebView2 / overlay 根层一次性禁止浏览器 Ctrl+滚轮缩放，并守住 `ZoomFactor`；
- 保留普通滚轮、工作台滚动区、纸娃娃 inspection wheel 和系统 DPI / `PanelScale` 正常路径；
- Ctrl+滚轮后 CSS viewport、WebView2 ZoomFactor 和命中坐标必须不变；
- 如需提示，只在首次拦截时给短提示；帮助页补充说明，但提示不是安全边界；
- Host 侧增加自动断言，真实 WebView2 再做人工 smoke。

### F9：装备相关入口分散，刘海“游戏”行过长，Character Build header 又同时承担跨页跳转

**裁决：接受，但不是把“收纳 / 技能配置”两个按钮机械藏进下拉。**

如果修复 §2.2 后只剩两个跨页动作，下拉会把两个一键入口都变成两步，收益不足。用户进一步确认旧 Flash 装备界面本就包含技能，而本轮目标还包括战备箱、独立调制、材料与情报；当六个目标都有真实路由后，一个单层、可见标签为“整备”的 section switcher 才成立。

canonical 成员与顺序固定为：

```text
装备 → 战备箱 → 装备调制 → 技能 → 材料 → 情报
```

顺序先放穿戴、物品来源和改造，再放能力配置与资料类页面；它不声称复刻某一版 Flash 的具体排列。外部统一短标签为“装备”，页面标题可以继续使用“角色构筑”；菜单当前项仍写“装备”并另行标记“当前”。

刘海分组调整为：

```text
游戏：战队 / 平板 / 商城
整备：装备 / 战备箱 / 装备调制 / 技能 / 材料 / 情报
辅助：点歌机 / 地图开关 / 修改器 / 帮助
系统：全屏 / 日志 / 其他
```

Character Build 的目标 header 为：

```text
角色构筑                         [整备 ▾] [个人信息] [?] [×]
```

菜单为单层固定 allowlist。以下六行按现役短标签书写；早期讨论中的“装备 · 角色构筑 / 战备箱与背包 / 背包装备调制 / 技能配置”只是用于消歧目标身份的文案草图，不是可覆盖 §7.3 frozen label 的现役合同：

```text
装备                    当前
战备箱
装备调制
技能
材料
情报
```

“装备”当前项使用 `aria-current="page" + aria-disabled="true"`，可聚焦并显示“当前”，激活零业务调用；它是导航定位状态，不是 §5.1 所禁止的可点外观 + silent no-op。“个人信息”保留一级直达。它虽然也曾在旧 Flash Hub 中，但现役实现是当前 Character session 的本地 SecondaryPage，使用频率与返回语义都不同；这是有意的现代化差异。Help 和 Close 永远直接可见。菜单中的“装备调制”与候选/槽位上的“调制当前装备”是不同动作；上下文与完整动作文案必须共同避免只写“调制”的歧义。

生产菜单不能显示尚未接通的“未来项”冒充完整功能。施工期间可在 dev fixture 以 capability gate 验证缺失目标；正式启用六项菜单前，材料与情报必须完成 §5.6 的 exact handoff。运行时暂时 busy/reconcile 与结构上尚未实现是两种状态：前者保留菜单项并显示阻断原因，后者在生产不可见。

## 4. 沿同类问题继续发现的真实缺陷

### 4.1 Esc 消费链不完整

嵌入 Character Build 的 tuning 会先让“调制说明”消费 Esc；独立 Inventory Workbench tuning 退出路径没有调用同一个 info panel Esc consumer。结果可能是说明仍开着，页面却退出 tuning；若 `_build` 曾实例化，还可能直接跳回构筑。

**约束**：Esc 必须由显式的 view / overlay stack 决定，不得由“某对象是否曾创建”推断返回目标。

统一顺序：

1. 输入草稿、modal、inspection 等最内层瞬态；
2. tuning info；
3. 当前 tuning；
4. storage；
5. build；
6. panel / game。

一次只消费一层。普通右上 `×` 与 native backdrop 始终执行当前 owner 的普通关闭，不伪装成“返回上一页”。Storage 只有在显式从 Build 进入时才返回 Build；独立打开时关闭到游戏。Crafting 内嵌整理背包继续遵守其既有“显式返回合成、普通关闭关闭 crafting owner”合同。

### 4.2 权威操作源没有持续可见标记

独立 tuning 会自动选择或点击选择左侧背包装备，但除 conversion 等个别场景外，卡片没有持久、可访问的 current source 标记。玩家看得到高亮变化，却无法确认右侧操作究竟绑定哪一件。

**约束**：

- browse focus、selected candidate、authority source、action target 是四个不同状态；
- source 必须同时有视觉轮廓、可见短标签和 ARIA current/selected 语义；
- debug 信息可含 exact ref，玩家层只显示名称、位置和当前操作；
- source stale 时撤销可提交性，不保留“看似仍选中”的旧高亮。

### 4.3 候选空态混淆四种含义

Character Build 右栏当前把“未选择目标”“正在加载”“确实无候选”“请求失败”压成同一条“选择一个装备或药剂槽”。

**约束**：最少区分：

| 状态 | 玩家文案 | 下一步 |
|------|----------|--------|
| `unselected` | 先选择左侧槽位 | 指向可选择区域 |
| `loading` | 正在查找可用装备 | 禁止旧候选提交 |
| `empty` | 此槽位暂无可用候选 | 提供收纳/获得来源提示 |
| `error` | 暂时无法读取候选 | 提供安全重试 |
| `ready` | 候选与数量已同步 | 正常浏览 |

空态统一的是语义合同 `{kind, statement, nextStep}`，不是万能状态组件。

### 4.4 Crafting 数量控件重建导致丢焦点，帮助与实物不一致

Crafting 当前在每次核算后清空详情并重建 `− / output / + / 最大`，点击后 DOM identity 和键盘焦点丢失；帮助却声称存在数字直输，实际只有 `<output>`。唯一确认按钮还与材料详情一起放在 `.crafting-detail-body` 的滚动层内。

**决策**：

- 迁移到现役 `QuantityControl`，复用节点并保持草稿、焦点与滚动；
- 帮助文案必须与实际输入能力一致；
- 材料/状态详情可滚，固定 `CommitBar` 不滚；
- `data-title` 伪 tooltip 必须同时响应 `:hover` 与 `:focus-visible`，中文说明不得继续使用 9px；
- 一次合法 intent 对应一次 preview，旧回包不得重建控件或覆盖新草稿。

### 4.5 “可查看”与“可执行”被混为 disabled

共享 EntityTile 的 disabled 会移除 tabindex 并阻断 activation：

- NPC 锁定商品因此无法由键盘查看“为什么锁定”；
- Tuning / Character 的 blocked 卡又可能仍可点击，但点击静默 no-op；
- Tuning 的 `.equipment-tuning-candidate.blocked` 会把整卡 opacity 降至 `.42`，恰好把卡内阻断原因也压暗；Character Build blocked 卡没有这条 opacity 问题，但仍存在激活静默 no-op。

**约束**：

- `inspectable` 与 `actionable` 分离；
- blocked 实体通常仍可聚焦、查看和听到原因；
- blocked activation 只解释原因，不发送业务请求；
- 真正 interaction disabled / busy 只禁用 CTA；
- 阻断原因保持正文级对比度，不能继承整卡低 opacity；
- 自动门检查 blocked reason 的 computed opacity / contrast 和键盘可达性。

### 4.6 KShop 切到物品管理后，帮助与关闭语义仍停留在商城

KShop 的 HelpAction 和 close aria 在创建时固定；`showInventoryMode()` 只更新标题、slots 和 skin。切换后玩家看到的页面与帮助、关闭读法不一致。

**约束**：同壳子路由切换必须原子更新 title、help spec、close label、action projection 和 focus return target；不能只换 body。

### 4.7 Loot 缺少 HelpAction，并泄漏技术词

Loot 的拖拽、双击、Ctrl+单击、Ctrl+A 主要藏在 ARIA 中，鼠标用户没有可见入口；正文还出现 `TRANSFER-PAIR / MAP CHEST`、“权威瞬态容器”“权威版本”“等待权威快照”“重放”“水位”等实现词。

**决策**：

- 补唯一 HelpAction 和常驻短提示；
- 保留玩家需要的动作、数量、来源和恢复动作，删除 revision 等无决策价值指标；
- “权威 / 对账 / 重放 / lease / token / revision / epoch / wire”等进入玩家文案静态词表门；
- “重新同步 / 重新核对”可作为玩家恢复语言，但领域 handler 仍保持不同语义，不抽 `RetrySyncAction`。

### 4.8 帮助入口实现分裂

Crafting、Inventory、KShop 已使用 `HelpAction`；Skills 自绘 `?`，NPC 有自己的 guide / secondary page，Loot 缺失。当前 `HelpAction` 还缺 harness 需要的标准 action hook。

**决策**：

- `HelpAction` 增加稳定 `data-header-action="help"`；
- Skills、Loot 和适合的 NPC 主入口迁移到现役 HelpAction；
- 领域正文继续分别维护；
- 暂不造 onboarding / tour 引擎；
- 只有 KShop 与 Storage 出现第二、第三个真实“一次性提醒”消费者后，才考虑仅负责 versioned localStorage 的小型 `GuideOnce` helper。

### 4.9 共享 structural motion 被 feature cascade 绕过

SecondaryPage 的共享 210ms 入退场依赖 `visibility` 延迟，但 KShop / NPC 的 unlayered `display:none / display:grid` 优先，退出时立即 `display:none`。Skills / Entity 的 unlayered focus 规则也可能覆盖共享 focus ring。

**约束**：motion 和 focus 统一必须以 computed style / 行为断言为准，不能以“token 已定义、selector 已存在”判定完成。

### 4.10 SecondaryPage 露出已经 inert 的底层顶栏

`SecondaryPage` 会把宿主中除自身和 modal portal 外的兄弟设为 `inert + aria-hidden`。KShop 结算、NPC 结算/帮助却只从 48px header 下沿覆盖 body，导致底层 Help、模式、密度和 `×` 仍保持“可操作外观”，实际已经不能点击或聚焦。现役视觉 fixture 还把这种构图固化成了看似正常的截图。

**约束**：

- SecondaryPage 取得焦点所有权时，底层不可操作区域不能继续呈现 enabled control 外观；
- 优先覆盖、隐藏或明确弱化底层 action strip，并在 SecondaryPage 内投影必要的 Back / Help / 普通 Close；
- 如果设计决定保留外露 action，它就不能属于 inert underlay，且必须真实可用；
- Back、Esc、native backdrop 与普通 Close 继续保留各自语义，不为了省按钮合并成一个动作。

### 4.11 OwnedInventoryPane 的父级 disabled 没有如实投影到子控件

`OwnedInventoryPane.setDisabled()` 只更新根状态；Inventory、NPC Organizer、Loot Organizer 的多个 EntityTile 仍以 `disabled:false` 和“点击转移”文案呈现，handler 在 busy / refreshRequired / returning 时却直接拒绝。Loot 的丢弃按钮也存在同类锁投影缺口。

**约束**：

- Pane、EntityTile 和 nested action 从同一份局部 `{inspectable, actionable, reason}` 投影消费状态；
- 祖先 `aria-disabled` 不能代替子控件的视觉、ARIA 和动作真实性；
- blocked 卡可继续查看详情，但必须移除转移/丢弃 affordance，激活只解释原因且业务调用数为零；
- Inventory、NPC Organizer、Loot Organizer 共用状态语言，不因此抽一个跨领域状态机。

### 4.12 Equipment Tuning 的锁状态没有覆盖所有控制族

强化数字框、步进、range、等级 marks 和“升至上限”没有把 `_needsReconcile` 纳入 disabled；tabs 与已安装阶级入口也没有完整覆盖 detaching、refresh retry 等锁。handler 随后静默 return，形成另一组可见 no-op。

**约束**：

- Tuning View 形成一个领域本地 `interactionLocked / reason` 投影；
- tabs、已安装阶级、强化 input/stepper/range/marks/cap 都消费同一投影；
- `needsReconcile` 时只保留“重新核对”主动作；
- detaching / refresh retry 时其他操作隐藏或 disabled，并显示“先完成同步”；
- 这是 feature 内状态收敛，不新增共享组件。

### 4.13 右侧常驻 HUD 的条件槽（已完成）

`RightContextWidget` 顶部已有“地图 / 任务 / 装备 / 设置 / 暂停 / 退出”六入口；其下方同一几何槽位曾由工具 hover、任务通知和独立 `SafeExitPanelWidget` 以 `_externalStatusSlotActive` 高度布尔隐式竞争。该缺陷现已闭环：旧布尔接线删除，组合 owner 只投影一个封闭结果；右槽没有扩成第三套整备导航。

**约束**：

- 条件槽在任一时刻只允许一种角色：`contextHint | actionableNotice | transactionDecision`；
- 优先级固定为 `transactionDecision > actionableNotice > contextHint > hidden`，高优先级出现时低优先级不保留 hitbox；
- `NativeHudOverlay` 是现役唯一仲裁点：每次只计算一个封闭 `RightContextSlotOwner`，再把同一结果投影给 `RightContextWidget` 与 `SafeExitPanelWidget`；两个 widget 不得各自推断优先级；
- `contextHint` 只提供一行有信息增量的说明，例如装备入口使用“打开角色构筑；其他整备功能在刘海或装备页”，不重复按钮标签；
- `actionableNotice` 最多一个明确下一步，例如任务可交付；不得扩成任意动作列表；
- `transactionDecision` 由 exact SafeExit owner 持有，状态与取消/退出/重试按钮同一行，其他 widget 不复制命中区；
- 不在此槽复制完整整备导航。游戏态任意整备入口由刘海“整备”行承担，面板内相邻跳转由 Character Build 的“整备”菜单承担；
- pointer hover 与可访问名称/测试探针表达同一说明；不得让只有 hover 才出现的文字成为理解顶栏按钮的必要条件。

现役 Native top tools 没有单独的键盘 focus owner；本批由 pointer hover 与同源测试探针固定说明，不虚构不存在的焦点链。`CompositeBounds` 只为 hover/repaint 保留透明合成 union，实际 `TryHitTest` 仍按 exact action 返回；透明 reserve 不算可见槽。

## 5. 交互真实性架构约束

### 5.1 可见控件合同

任一玩家可见 control 必须满足以下二选一：

1. 激活后产生可观察、与文案一致的效果；或
2. visibly disabled，并在同一上下文给出可读原因。

禁止：

- 可点外观 + handler 静默 return；
- `hidden` attribute 存在但 computed display 仍可见；
- 透明或被遮挡控件仍保留 hitbox / tab stop；
- 帮助、tooltip 或 hover 成为业务动作的隐藏前置；
- disabled 同时隐藏阻断原因。

自动验证至少检查：

- computed `display / visibility / opacity / pointer-events`；
- `getBoundingClientRect()` 是否仍形成命中区；
- Tab 序列和程序化 focus；
- pointer、Enter、Space 的等价结果；
- 可见 control 的 action trace 或 disabled reason。

### 5.2 四种对象身份

| 身份 | 含义 | 允许影响 |
|------|------|----------|
| `browseFocus` | 当前键盘/指针浏览位置 | tooltip、焦点摘要 |
| `selection` | 玩家显式选中的候选或批量项 | 本地 preview intent |
| `authoritySource` | 当前权威操作精确绑定的 inventory/loadout ref | preview/commit envelope |
| `actionTarget` | 当前动作将影响的目标槽、容器或 operation | CTA 文案与结果 |

这四者可以指向同一实体，但不能因此合并字段。视觉上必须让玩家知道当前 source 和 target；协议上 inventory ref 与 loadout ref 始终分型。

### 5.3 帮助与能力分离

- `HelpAction` 只呈现说明；
- 打开、关闭、从未打开帮助，得到的业务 capability 必须完全相同；
- 帮助正文随 view 更新，但不发业务消息、不持久化“解锁态”；
- 隐含操作同时需要常驻短提示；不能把全部可发现性压力推给 modal。

### 5.4 决策区合同

- 每个决策面只有一个主 CTA；
- 主 CTA、当前状态和阻断原因放在不滚动的 CommitBar；
- 可滚层只放差异、材料、返还、历史和长说明；
- preview 生成后 CTA 必须进入视野，不能要求玩家猜测要向下滚；
- pending 时旧 preview 明确失效；
- unknown result 进入 reconcile，不自动重放写入；
- blocked reason 保持正文对比，CTA 自身才进入 disabled/busy 表现。

### 5.5 明确导航栈

每个 view change 记录：

- `viewId`；
- `origin`；
- `returnTarget`；
- `focusKey`；
- 是否有未提交 draft；
- 当前最上层 Esc owner。

Esc 按栈逐层消费；header `×`、native backdrop 和 Host 普通关闭不读取这个返回栈。不得再用 `_build` 是否非空、DOM 是否曾 mount 等实现偶然性决定玩家去向。

若“整备”popover 已打开，它临时成为最上层 Esc owner：第一次 Esc 只关闭菜单、阻止冒泡并把焦点还给触发器；下一次才进入输入草稿/modal/inspection → tuning info → tuning → storage → build → game 的既有顺序。进入 modal 或 SecondaryPage 时菜单必须预先关闭，因此两者不能同时声称最上层 owner。

### 5.6 整备导航：统一入口，不统一生命周期

#### 5.6.1 目标分型

| 目标 | 类型 | 必须采用的路径 | 禁止 |
|------|------|----------------|------|
| 装备 | 当前 Build view | `aria-current="page" + aria-disabled="true"`、可聚焦并显示“当前”，激活零业务命令 | 再发 `EQUIP_UI`；现役重点击会触发 `panel_esc` |
| 战备箱 | 同 Workbench instance | `_build.prepareLeave()` 后切换 `storage`；保留 Character session、pause lease 与 exact instance | finalize Build、打开第二个 workbench |
| 装备调制 | 同 Workbench instance | `_build.prepareLeave()` 后直接建立/恢复 inventory scope 并切换 standalone `tuning` | 先假点 Storage 再模拟按钮；复用 loadout source |
| 技能 | 跨 owner | 继续使用现役 `requestClose("navigate_skills")` 严格交接 | `Panels.open("skills")`、乐观卸载 |
| 材料 | 跨 owner | 新增 exact-instance、固定目标 `navigate_materials` 关闭后意图；settled 后启动带 Host opaque requestId 的材料专用 opener，并等待 exact panel admission | 活动 Character authority 下直接 `RouteMaterialUi()`；把 `openMaterialUI` 发送成功当成目标已打开 |
| 情报 | 跨 owner | 新增 exact-instance、固定目标 `navigate_intelligence` 关闭后意图；settled 后使用 Host 固定生产 initData | Web 传任意 panel/initData；直接普通 `OpenPanel` |

Build → Storage/Tuning 是局部 view change，不进入 Host post-close intent。二者返回 Build 时保持同一 `panelInstanceId`、Character session 和 pause owner，不重新打开装备服务；显式从 Build 进入的 Storage 与 standalone Tuning 都显示唯一“返回构筑”，Esc 一次只退当前局部 view。独立从刘海打开的战备箱/调制仍普通关闭到游戏，不因 `_build` 曾存在而伪造返回。

若 Build 正处于“调制当前装备”的嵌入态，选择菜单“装备调制”必须先 exact detach loadout source，再建立 inventory source；不得继承当前 selection、lease、preview、`_returnState` 或迟到回调。菜单中的 standalone tuning 始终表达“从背包选装备”，嵌入态始终表达“当前穿戴物”。

#### 5.6.2 跨 owner 固定目标意图

B4 已固定三目标编码与一次性交接基座，B5 已实装 Materials exact opener，B6 已实装 Intelligence 同步 exact opener；B7 当前源码已把 header/focus/rollout 作为同一 production presentation 单元切换，以下是现役合同：

1. Web 只能从固定 reason 集合发送 `navigate_skills | navigate_materials | navigate_intelligence`；
2. Host 在关闭屏障前复验 exact active/bound Character Build instance；B4 intent 只保存 `{enumTarget, exactWorkbenchInstance, phase, generation, lifecycleEpoch, timer}`，不保存 handler、panel/initData、registry 或任意 destination；
3. Character Build 完成本地 finalize；Host 等待 exact visual retire、AS2 detach/persistence proof、pause release 与 coordinator settled；
4. exact coordinator settled 时，Host 在同一 lifecycle 锁内销毁 armed intent/arm timer，再按固定 `switch(enumTarget)` 进入一次 target-open wait / admission state；异步目标必须先安装 target wait 再发命令，避免同步回包抢在 wait 之前。Skills、Materials 与 Intelligence 三个显式分支均已实装；
5. Skills 保留 nonce/preflight 与既有 `SkillOpenTimeoutMs`；Materials 生成 Host opaque `openRequestId`，经 `openMaterialUI({openRequestId})` → `CraftingPanelService` 原样回显，只消费 exact wire `{task:"panel_request", panel:"crafting", source:"nativehud_materials", initData:{view:"materials"}, openRequestId}` 与对应 Host admission；该相关路径的顶层和 `initData` 都按封闭 shape 校验，`view` 错层、额外导航字段、错误 source/view/requestId 一律拒绝；Intelligence 使用 Host 内建封闭 `{mode:"prod",source:"runtime",debug:false}`，在 settled 后同步消费 exact idle admission，Web 不传 panel/initData；
6. `CharacterBuildPreparationNavigationTimeoutMs` 已只覆盖 arm → exact coordinator settled。超时原子清除 armed intent 和 arm timer，迟到 settled 零打开；它不观察 `panel_request`，也不复用为目标打开计时器；
7. settled 后 Skills 由 `SkillOpenTimeoutMs` 管理 target-open wait；Materials 使用独立 `MaterialPanelOpenTimeoutMs`，迟到或 near-match `panel_request` 由 wait 的 generation/lifecycle epoch 拒绝；Intelligence 使用同步 exact admission，不安装 target timer、不复用 arm timer，也不进入 Skill/Material `FixedPanelOpenWait`；
8. competing intent、实例变化、visual-not-idle、detach/finalize 失败、目标 admission 拒绝或任一阶段超时都取消对应状态；迟到 callback 不得打开目标。若失败发生在 Build finalize / visual retire 前，则保留或恢复当前 Build 并按当前 gate 对应的固定 action 恢复焦点；若 destructive close 已发生，则进入与目标打开失败相同的至多一次 Character Build 回滚，回滚也失败才停在游戏并给出可操作提示；
9. `returnFocusAction` 是封闭枚举：迁移期 parser 只接受 `skills | preparation-menu`。B7 on 时 Skills 返回与三目标失败回滚统一投影 `preparation-menu`；显式 off 时恢复 `skills`。Web 只按固定 action identity 聚焦，不能把值解释为 selector；gate/focus 不配对时拒绝 launch，不能猜测或局部降级；
10. B4–B7 的 ordinary `×`、Esc 和 native backdrop 继续关闭到游戏；H1 后只有 Character-origin 且 Host exact-instance 绑定的 Materials/Intelligence 显式 `navigate_character_build` 能返回装备，仍不得把它推广成通用跨面板返回栈。

Web 不得传 `panelName`、任意 `initData`、回调脚本或 return target。Host 不得复用 Crafting → Storage 的 `_returnTarget`，也不把 Storage/Tuning 纳入 post-close intent。这个边界是“Character Build 离开时的三目标白名单”，不是全局导航服务。

`WebOverlayForm` 的 workbench close envelope validator 只扩充上述三个 exact reason，并继续要求 exact `panelInstanceId` 与封闭 key shape；foreign instance、未知 reason、额外 target/initData 字段一律在 arm 前拒绝。三种 reason 只是 enum 编码，不授权 Web 选择任意目标。

材料的老 HUD 无 nonce 路径只可在**不存在 armed material intent，也不存在 material target-open wait**时继续普通 idle 打开；任一 material wait 存在时，missing nonce 请求必须拒绝、零打开并给出“正在打开材料”的可操作状态，不得旁路、消费或隐式 supersede 当前 intent。只有 Host 生成、AS2 原样回显、长度/字符合法且与当前 generation/lifecycleEpoch 完全一致的 requestId 才能完成相关导航。`TrySendGameCommand("openMaterialUI") == true` 只表示命令已投递，不表示 Crafting panel 已准入；target-open timeout、near-match、重复、错层/额外字段、错误 source/view 或 Host admission 失败都走 §5.6.2 的阶段化回滚/提示合同。若该回显触及 `.as`，必须同步协议 canonical doc、保持 UTF-8 with BOM，并完成 focused Flash fresh trace。

#### 5.6.3 Header 与菜单状态投影

G0 完成后、B7 切换前的历史 Build exact-set 为：

```text
storage | stats | skills | help | close
```

B7 默认 on 后的现役 Build exact-set 为：

```text
preparation-menu | stats | help | close
```

Stats exact-set 保持：

```text
back-build | help | close
```

Stats、纸娃娃放大页或其他 SecondaryPage 取得 FocusScope 后，底层 `preparation-menu` 必须 hidden/inert，不得只保留可点击外观。Storage/Standalone Tuning 使用自己的局部 header projection，不复制“整备”菜单。

`InventoryWorkbenchHeaderProjection` 输入使用一个**固定键集合**的 `preparationAvailability`，只允许 `equipment / battlebox / tuning / skills / materials / intelligence`，每项投影 `{visible, disabled, reason}`；输出菜单项增加 `{current, destinationKind}`。canonical identity 使用 `battlebox`，只有 handler 内部把它映射为 Workbench `view:"storage"`，不得让 `storage` 变成第二个玩家路由名。它不是 descriptor 数组，不携带 handler、panelName 或领域对象。所有 handler 和 authority 判断仍由 facade / Host 持有。

触发器在 Build 中始终可打开，即使所有目的地暂时被 write/reconcile/pending 阻断；玩家需要在菜单内读到具体原因。当前 `equipment` 项固定 `current:true / disabled:true / reason:"当前"`。当前 frozen tuple 的六项目标均已实现；若某目标被结构性撤除，必须整体关闭总 gate，而运行时暂时不可执行的目标继续使用可聚焦 `aria-disabled` 项并显示正文对比度原因。锁状态在菜单打开期间变化时原位更新，不关闭菜单、不移动当前焦点。

#### 5.6.4 菜单交互与视觉合同

- 触发器可见标签固定为“整备 ▾”，带 `aria-haspopup="menu"` 与真实 `aria-expanded`；Click、Enter、Space 都执行同一 toggle，不执行最近目标；
- ArrowDown 总是打开并进入 roving menu；键盘打开后优先聚焦带 `aria-current` 的装备项，即使它是 `aria-disabled` 也可被朗读；只有当前项结构不存在时才聚焦首个可用项；
- ArrowUp/Down 循环，Home/End 到首尾，Enter/Space 激活；
- Esc 只关闭菜单、阻止继续冒泡并归还触发器；下一次 Esc 才交给 view stack；
- Tab/Shift+Tab 只关闭菜单并按浏览器正常 header 顺序继续，不 `preventDefault`、不人工移动焦点、不建立 focus trap；外部点击只关闭菜单；
- 切换 view、进入 Stats/SecondaryPage、打开 modal、开始 close、rebind、deactivate、destroy 都必须同步关闭菜单并清理 document listener；
- 不使用 hover-open、split button、最近目的地、自适应主按钮、多级菜单、图标徽章墙或二级说明卡；
- popover 固定为单列 `204px`，不能挤动 header；1024×576 下不越出 viewport、不遮挡 Help/Close；
- 普通态只消费 shared structural motion；reduced-motion 直接显隐，不做位移；
- 中文使用现役 UI 字体栈，不继承 Consolas-first 的模式按钮排版。

#### 5.6.5 刘海独立装备调制入口

刘海“整备”行中的“装备调制”是用户要求的独立生产入口，不是 Character Build 菜单局部切换的别名。新增固定 command key `EQUIPMENT_TUNING`：

```text
Native/legacy HUD EQUIPMENT_TUNING
→ Host 生成 exact native tuning open intent / opaque openRequestId
→ AS2 openInventoryWorkbench(
     profile="battlebox",
     view="tuning",
     source="nativehud_equipment_tuning",
     openRequestId=Host-owned
   )
→ panel_request 原样回显
→ Host exact tuple + generation + admission 校验
→ 打开 standalone workbench tuning
```

禁止复用 `agent_control`、dev/test source、直接 `PanelHost.OpenPanel` 或 Web `Panels.open`。Host send 成功不等于打开成功；timeout、nonce/source/profile/view 近似匹配、重复回显和 admission 拒绝都必须清除 pending intent并提示，不得留下 pause/HUD/backdrop 残余。已有 active panel 或相同 tuning instance 时拒绝重复打开，不把它实现成 `EQUIP_UI` 那样的重点击关闭。

独立 tuning 与“战备箱与背包”仍是同一 Workbench instance 的兄弟 view，可以双向切换且不创建 Character Build session。Tuning 顶栏的兄弟切换文案使用中性的“战备箱与背包”，不能写“返回收纳”，因为从刘海直接进入时并没有先前的收纳页；只有显式从 Build 进入时才额外显示“返回构筑”。独立入口的 `×`、Esc 与 native backdrop 普通关闭到游戏。

“装备调制”不因当前背包没有可调制装备而隐藏；页面负责呈现可解释空态。只有游戏/服务尚未 ready、入口 progression gate 未满足或现有 owner 不能准入时显示 disabled reason。

## 6. 组件与抽象裁决

原则：只有两个及以上消费者因**相同原因**变化，才上收共享边界；仅 DOM 相似不构成抽象理由。

| 候选 | 决策 | 最小边界 | 明确拒绝的扩张 |
|------|------|----------|----------------|
| `InventoryWorkbenchHeaderProjection` | **新增** | 纯函数投影 header action 与固定整备菜单项状态；不发请求 | 通用 action DSL、动态 toolbar/navigation registry、handler descriptor |
| Inventory feature-local menu presenter | **扩展既有 header 模块** | 单层 DOM、ARIA、focus、outside-click 与确定性销毁 | 跨面板路由、authority、最近目标、多级菜单、全局组件 |
| Character Build post-close intent | **扩展现役 Skills 屏障** | exact instance + `skills/materials/intelligence` 固定枚举，settled 后一次目标打开/一次失败回滚 | 任意 panelName/initData、通用 return stack、Storage/Tuning 跳转 |
| Native tuning open intent | **新增固定入口** | `EQUIPMENT_TUNING` + Host opaque requestId + exact battlebox/tuning tuple | 复用 agent_control/test source、直接打开 Web/Host panel、通用 opener registry |
| `DualPaneShell({profile})` | **扩展** | 校验封闭枚举并写 `data-profile` | 动态 `LayoutProfileRegistry`、JSON 布局系统 |
| `HelpAction` | **推广现有** | 标准入口、spec update、modal 委托、销毁；补 action hook | 业务 capability、教程引擎 |
| `ChoiceGroup` | **推广现有** | “逐次确认 / 单件快捷”的 pressed/disabled 机械 | 调制规则和 quick-commit 判定 |
| `QuantityControl` | **推广现有** | Crafting 复用稳定 DOM、A/E、键盘与草稿 | 价格、材料、容量判断 |
| `PanelTooltip` | **推广现有** | Character 候选领域 adapter + scoped async binding | 新 `ItemTooltipBinding` 万能组件 |
| `CommitBar` | **推广现有** | Tuning/Crafting 固定 CTA 与状态 | `StagedCommandBar` 大组件 |
| `CloseAction` | **只允许轻量 factory** | type、glyph、class、audio、action hook | canClose、回滚、pending、领域文案状态机 |
| Empty State | **语义合同 + CSS** | `{kind, statement, nextStep}`，可选无状态 helper | 通用空态业务组件 |
| `CountBadge / StateChip` | **CSS/data slot** | 视觉角色、ARIA、命中避让 | JS 配置对象体系 |
| Context Hint | **CSS 角色** | 字号、间距、长文案与键盘等价 | 全局引导编排 |
| Scrollbar | **CSS utility/token** | track、thumb、hover、button、corner | `ScrollSurface` DOM/JS 组件 |
| Authority Preview | **架构模式** | browse → preview → commit → reconcile + CommitBar | 接收 lease/token/price/capacity 的通用组件 |
| Retry / Sync | **不抽组件** | 共享 warning 样式与玩家词表 | 模糊不同 reconcile 语义 |
| Guide Once | **推迟** | 满足多消费者后仅抽 versioned preference helper | tour、步骤注册表、跨面板 onboarding 状态机 |
| `escapeHtml` | **窄工具** | 优先 `textContent/setAttribute`；必要时分 text/attribute escape | “万能 sanitizer”或错误复用三替换版本 |
| Ctrl+滚轮 | **Host 根约束** | overlay 一次性冻结用户缩放 | 每页 `WheelGuard` |

共享组件不得接收 lease、token、revision、price、capacity 等领域字段。共享层变化必须验证 DOM identity、focus、scroll、tooltip scope 和 listener 清理，而不是只比较代码行数。

整备菜单的 DOM/ARIA/focus 机械能力放入现有 `inventory-workbench-header.js` 的 presentation 域，或在该域内按行数门拆一个无业务权力的窄文件；`inventory-workbench.js` 只编排固定目标 handler，且总体应通过搬移逻辑净减行。菜单模块不得自行监听 Bridge、持有 Character session 或直接调用 Host panel。

## 7. 布局与信息架构决策

### 7.1 profile 落实

`DualPaneShell` 接受以下封闭 profile：

- `catalog-decision`
- `archive-reference`
- `transfer-pair`
- `library-action-strip`
- `library-decision`
- `character-build`

profile 只决定壳级网格、FlowRail 角色、最小列宽和可用的 footer track；footer 是否存在仍由真实结构标记决定。领域内部特殊构图可使用受控 token，但不得在 feature 中静默创造第七种结构。新增 profile 必须先改 canonical doc、说明消费者与最小视口，并增加真实 fixture。

- 所有生产 `DualPaneShell` 都必须显式声明合法 profile；
- 生产 view 到 profile 的唯一映射固定为：KShop `shop → catalog-decision`、KShop `inventory → transfer-pair`、NPC Shop `→ catalog-decision`、Crafting `recipes → catalog-decision`、Crafting `materials → archive-reference`、Inventory Workbench `storage → transfer-pair`、standalone `tuning → library-decision`、`build/stats-underlay → character-build`、Loot `→ transfer-pair`、Skills `manage → library-action-strip`、Skills `trainer → library-decision`；“教师研习 / 调制”不再同时出现在两种 profile 的用途说明中；
- Inventory 与 KShop 在同一 Shell 内原位切 view，不能只在构造时选一次 profile；Shell 提供与构造器共用封闭枚举校验的 `setProfile(profile)`，feature 在自己的单次 chrome/view 同步中调用它，但不得直接写 `data-profile`。Skills 与 Crafting 切 view 时重建 Shell，必须按目标 view 在新构造调用中显式选择 profile，不追加同值 setter；
- profile 只声明可用的壳级 track，不推断 root footer presence。root footer 必须是 Shell root 的直接子节点并带 `data-workbench-shell-footer`；CSS 仅在该真实结构存在时启用 footer row。Loot 的 root CommitBar 与 Inventory Storage 的 body 内批量栏不因此被错误同构，也不为 footer presence 创造第七种 profile；
- 双栏宏观 `grid-template-columns/rows` 只由 profile CSS 持有，feature 只负责 pane 内部构图；
- density 只改变实体格密度，默认不得改变双栏 split；确有领域理由时必须定义受控 profile variant，并进入本文和真实 atlas；
- 独立 Equipment Tuning 在 compact 下从 `52:48` 跳到 `42:58` 的历史行为已经退出；“看紧凑图标”不再同时改变整页构图。Character Build 嵌入态继续保持自己的 `character-build` 55:45 profile，不受该历史选择器影响。

### 7.2 header action budget

- 同一视图只保留一个 Help、一个 Close；
- “返回上一层”与“关闭到游戏”图形和文案必须不同；
- 不同时出现重复的文字返回与箭头返回；
- action 按 `context / preference / help / close` 分组，领域主动作与全局关闭不混排；
- 1024×576 下 action group 单行、无横向滚动；
- 次要设置优先收拢为 ChoiceGroup / overflow，不得压缩主标题和关闭命中区；
- view 切换必须原子更新 action projection，不保留上一 view 的可见按钮。

### 7.3 整备分类的跨入口一致性

- canonical route 顺序固定为 `equipment / battlebox / tuning / skills / materials / intelligence`；
- 玩家短标签固定为“装备 / 战备箱 / 装备调制 / 技能 / 材料 / 情报”；Character Build 菜单不得自行补写“角色构筑 / 与背包 / 配置”等扩展词；
- C# 的 Native/legacy HUD 共享一份 frozen route tuple；Web 保留一份同形 frozen tuple，并由跨栈测试比较 exact identity/label/order。禁止为跨语言“单源”引入运行时 JSON registry，也禁止三处无测试手抄后逐渐漂移；
- 右侧常驻“装备”按钮是 `equipment` 的 pinned alias，保持一键打开 Character Build；它不拥有独立 route identity，也不改成必须先展开分类；
- 技能正式归入“整备”，不得同时留在刘海“游戏”行；“游戏”行缩为战队、平板、商城；
- “个人信息”是装备内部本地 SecondaryPage，不进入刘海整备行，也不进入下拉菜单；它继续作为 Character Build header 一级动作；
- 进入游戏后六个整备成员保持结构可见；progression/warehouse/service 未满足时显示 disabled 与具体 reason，不再像现役 `RequiresWarehouse` 一样从某入口静默隐藏。Character Build 菜单与两套 HUD 对同一 gate 的 visible/enabled/reason 必须一致；
- Native `NotchWidget` 的目标是四个有标签行“游戏 / 整备 / 辅助 / 系统”。新增第四行后，expanded height、width、hover/click rect、动画 union bounds 与低分辨率裁切必须从可见行数推导，退出现役固定 `*3` 与手写 `Max` 链；
- legacy `NotchOverlay` 不必补齐 Native-only 的整个“辅助”产品面，但至少把游戏与整备渲染成两条可辨识、无裁切的 production action 行。其 active-region/WM_NCHITTEST、toolbar height、绘制 Y、按钮 rect 与 expanded width 必须消费同一行几何；退出单行高度和 600px cap 可裁掉按钮的假设；
- legacy 与 Native HUD 可以有不同绘制实现，但游戏/整备可见成员、顺序、enabled reason 和 command route 必须一致。

### 7.4 右侧条件槽的状态优先级（已完成）

| 槽位角色 | 内容 | 是否可交互 | 退出条件 |
|----------|------|------------|----------|
| `transactionDecision` | SafeExit 存盘状态与取消/退出/重试 | 是，由 SafeExit exact owner 命中 | 事务完成、取消或安全超时 |
| `actionableNotice` | 单一任务/系统通知与明确下一步 | 仅在确有动作时 | 动作完成、状态失效或通知超时 |
| `contextHint` | 当前 top tool 的一行说明 | 否 | hover/focus 离开或更高优先级出现 |
| `hidden` | 无信息增量 | 否 | 有上层状态 |

右侧条件槽不做常驻空框；没有地图、通知、事务和 hover/focus 上下文时整个视觉槽 computed hidden、零 hitbox。外部状态必须给出 exact slot owner，而不是只声明“高度被占用”；不能留下“保留了高度但没有任何 owner 绘制”的空白。`CompositeBounds` 为 hover tooltip 预留的透明 union 仅是鼠标进入/重绘边界，不算可见条件槽，不得因此生成背景或命中 action。装备提示必须解释入口与相邻整备位置，不能只重复“装备”。

**现役实现状态**：上述优先级已经由 `NativeHudOverlay.ResolveAndProjectRightContextSlotOwner` 唯一实现。transaction 时 RightContext 的 notice/hint 像素与 hitbox 都为零；notice 时 hint 为零；hint 只绘制说明、不拥有 action hitbox；hidden 或缺组合 owner 时条件槽的可见几何、像素与命中为空。装备说明固定为“打开角色构筑；其他整备功能在刘海或装备页”。透明 `CompositeBounds` 可继续为地图/hover 布局保留空间，但必须 click-through。

### 7.5 空态与计数

- counts 在能降低试探成本时前置，不让玩家逐个打开分类确认是否为空；
- `0 / loading / unknown / error` 是四种不同投影；
- empty 文案采用“事实；下一步”，不使用只有“无”的死端；
- routine ready 不占底部，blocked/error/reconcile 才持续占位。

### 7.6 tooltip 与常驻摘要

- compact 模式隐藏正文时，tooltip 和可见当前浏览摘要共同补齐信息；
- tooltip 不替代错误原因、主 CTA、当前 source 和关键模式说明；
- 长中文说明不使用 9px、等宽字体或过度字距；
- 壳外 tooltip 同样服从主题 token、viewport gap 和 reduced-motion。

## 8. 视觉系统决策

### 8.1 排版角色

新增或收敛为少量角色 token，而不是给每个 selector 建 token：

| 角色 | 建议下限 | 用途 |
|------|----------|------|
| `badge` | 9px | 短数字、英文缩写、角标；不承载长中文和错误 |
| `body` | 10px | 中文正文、条件、阻断原因、帮助正文 |
| `label` | 11–12px | 卡片名称、按钮、主标签 |
| `pane-title` | 15–17px | pane 标题 |
| `shell-title` | 约 20px | 工作台主标题 |

约束：

- 8px 玩家文字直接禁止；
- 9px 中文只允许极短角标，不允许帮助、错误、条件和物品主名；
- Consolas/等宽字体只给数字、短状态和仪表，中文字体栈明确回落到现役 UI 中文字体；
- letter-spacing 收敛为少量角色，不在 9–10px 中文上叠高字距；
- 触碰模块中的违规字号必须归零，存量总数只降不升。

### 8.2 颜色与 skin

- 新增和触碰行只使用 `--dls-*`、`--theta-*`、`--wb-semantic-*`、`--wb-accent-*` 及 `color-mix` / `rgba(var(--*-rgb), alpha)`；
- 不为每个旧颜色机械制造一一对应 token；
- token 必须表达 surface、line、text、muted、accent、focus、success、warning、danger、disabled、economy 等稳定视觉角色；
- skin 只允许覆盖 token；本轮首先禁止 skin 改写 `display / grid / overflow / hitbox / focus / state priority`，再逐段迁移旧结构规则；
- 经济琥珀与 warning 语义分开，不能用同一种颜色同时表示价格和危险；
- accent 只用于 selected/source/focus/pending/主决策，不能让每块 pane、每行和 header 都常亮同等粗边；
- 颜色之外继续使用轮廓、图标、文字与形态表达重要状态。

执行顺序按债务密度拆为 CSS-only 原子批：

1. `skins.css`
2. `skills.css`
3. `equipment-tuning.css`
4. `inventory.css`
5. `core.css`

每批只处理一个视觉角色或一个文件域，不与任务流重构、named-layer 全迁移同时进行。

### 8.3 滚动条

本轮不建设全局 scrollbar utility 或 JS `ScrollSurface`。只对已证明存在真实溢出、且属于同一 7px 工作台窄轨语言的 exact 领域滚动面收敛：

- 轨宽；
- track / thumb / hover token；
- `::-webkit-scrollbar-button` 去除；
- corner；
- reduced-motion 下的静态 hover 结果。

现役接入面固定为 `.inventory-owned-grid`、`.loot-backpack-grid` / `.loot-source-grid` 和 `.equipment-tuning-detail`。KShop categories 的 2px、NPC popover 的 6px、legacy grid 的 4px 都是另有真实用途和回归证据的独立身份，不因“统一”被机械改成 7px。`overflow`、`max-height`、滚动窗口身份、焦点恢复和 scroll restore 仍由领域负责；验证必须使用 `scrollHeight > clientHeight` 的真实 fixture，并读取 standard 与 `::-webkit-scrollbar` 的 width/track/thumb/border/button/corner computed style。

这些 exact 规则使用 `scrollbar-gutter:stable` 和 skin token，但不自动给任意容器添加 `tabindex`；滚动区是否进入 Tab 序列仍由领域的信息可达性决定。Loot 的八列 44px 目标必须在 stable gutter 存在时仍无横向溢出，不能为了让测试转绿缩小命中区或隐藏横向 overflow。

### 8.4 状态对比与层级

- blocked reason 不继承整卡 opacity；
- disabled 控件可降低饱和度或强调度，但文字原因维持 body contrast；
- surface 与间距建立大分组，柔线建立内部层级，accent 不作为默认边框；
- z-index 只为跨 stacking context 的 page / modal / tooltip / drag ghost 建语义层；局部 `0/1/2` 不机械上收。

### 8.5 玩家词汇

静态审计应拒绝常态玩家层出现：

- lease、token、revision、epoch、wire、callId；
- “权威瞬态容器”“重放写入”“协议水位”等实现词；
- 无玩家决策价值的版本号和诊断英文 eyebrow。

保留玩家真正需要的：

- 当前来源；
- 正在核对 / 重新同步；
- 是否可能已成功；
- 下一步安全动作；
- 可复制的脱敏诊断入口。

## 9. 动效语言决策

### 9.1 语义时长

| 角色 | token | 时长 | 用途 |
|------|-------|------|------|
| micro | `--wb-motion-micro` | 90–120ms | hover、focus、press |
| standard | `--wb-motion-standard` | 140–180ms | 选择、状态切换 |
| structural | `--wb-motion-structural` | 180–220ms | SecondaryPage、决策区切换 |
| reject | `--wb-motion-reject` | 约 250ms，最多两次 | 明确拒绝 |
| busy | `--wb-motion-busy` | 900–1200ms | 权威等待 |
| ambient | `--wb-motion-ambient` | 1200–1800ms | 每面板最多一个环境核心 |

工作台内不再新增裸时长。既有 `.08s/.1s/.12s/.25s/.28s/.75s/1.8s` 按语义迁移，不能只按最接近数字机械替换。

> **现役实现（G3 已完成）**：本批限定的八个 workbench fragments 与 foundation 现役 KShop consumer 已完成上述迁移；micro/standard/reject/busy/ambient 均消费既有 token，ratchet 的 raw duration ceiling 已从 `164` 精确降到 `147`。lockbox 与其他非工作台动效不在本批扫描/改写范围，不能据此宣称全 CSS 裸时长清零。

### 9.2 属性与职责

允许的过渡属性：

- `opacity`
- `transform`
- `color`
- `background-color`
- `border-color`
- `outline-color`
- `box-shadow`
- `visibility`，且只用于延迟离场

禁止：

- `transition:all`
- 宽高、grid/flex 几何与会推动布局的动画；
- 普通可用控件的无限循环；
- feature 自己定义新的时长/缓动体系。

JS 只写数值和切 class，不拥有时长与 easing。拖拽、inspection camera 等直接操纵路径必须在操纵期间 `transition:none`。

> **现役实现（G3 已完成）**：本批过渡与 busy/reject/ambient animation 均消费现有 `--wb-ease-standard`，没有给 ratchet scanner 增加按动画名豁免，也没有创建第二套 easing/token。`background` transition 已在限定范围改为 `background-color`；`transitionAll` 总量保持 `16→16`，因为本批明确不扫 lockbox/非工作台存量。

### 9.3 reduced motion

> **现役实现（G4 已完成）**：shared root 统一关闭 `.workbench-shell` 内非必要 pulse、shake、travel、transform 与 transition；`skins.css` / `character-build.css` 不再以 `.001s/.001ms` 冒充关闭，Loot / Equipment Inspector 不再复制 root 声明，Tuning 的 feature-local reduced 规则只保留 `opacity:.55` 静态补偿。SecondaryPage 合法的 `visibility 0s linear` 延迟离场不属于伪关闭，必须保留。

- 壳外 `#panel-tooltip` 只在相邻 `#panel-container` 的 `data-panel` 为 exact `kshop/workbench/npcshop/crafting/skills/loot` 时关闭 loading pulse；`tasks`、map 等非工作台 owner 不受影响；
- Character renderer、Equipment Inspector 与 Tuning 图标各自现有的直接 `matchMedia` 分支已经以真实 renderer、transition 与 animation 终止门验证，reduced 下仍保留静态图标、尺寸、outline 和文案；
- 现役仅这三个有界 JS consumer，故 G4 不建立共享 helper、registry 或常驻 preference listener；未来若出现多个需要共同生命周期管理的帧循环，再按真实重复提取窄 helper；
- 工作台 CSS 已无 `.001s/.001ms`；仓库中剩余 `task_panel.css` 的 `0.001s` 属非工作台、有意范围外存量，不得把本批表述为全站清零；
- actual normal/reduced fixture 必须同时证明 normal 动画真实运行、reduced 动画/transition 数量归零、静态结果仍可见，不能只断言 media query 文本存在。

### 9.4 cascade 与真实生效

SecondaryPage、focus ring 和 state motion 的验收读取 computed style：

- inactive page 仍保持结构 grid，但 `visibility:hidden`；
- active page 的 opacity / transform / visibility 符合 structural contract；
- 退场完成后不可聚焦、不可命中；
- feature / skin 不能用 unlayered `display:none` 绕过共享离场；
- focus-visible 的最终 outline 是角色 token，而不是被 feature 静默覆盖；
- button、EntityTile、input、select、range 和可聚焦 scroll region 都有对应 focus 证据；
- selected 与 focus 可以叠加辨认，不能用同一条轮廓互相覆盖；
- legacy `kshop-pulse` 等 keyframes 移入共享 motion 域并改为领域中立命名，消费统一 busy token。

> **现役实现（G3 已完成）**：shared motion 域现持有 `wb-pulse-busy` 与 `wb-reject-pulse`；core、inventory 与 foundation KShop consumer 统一消费 busy token，Skills reject 消费 reject token/role color。Equipment Tuning ambient core 的定义和消费原本同域，因此只换 ambient token，不为表面统一搬家。normal/reduced fixture 证明动画关闭后 busy 图标/文案和 reject outline 仍存在。

> **现役实现（G4 已完成）**：shell 内 reduced shutdown 由 shared root 唯一持有；壳外 Tooltip 由 exact workbench owner 规则接管，没有新增 `!important`。Character/Inspector/Tuning 的 actual normal/reduced probe、Tooltip 的 `tasks` 反例，以及 SecondaryPage 的静态终态共同防止“选择器存在但级联未生效”。

> **现役实现（本轮定向 F 状态收敛）**：named `workbench.states` 仍会输给历史 unlayered feature/skin，因此 `states.css` 在 named baseline 后、且在 feature/skin 后导入的位置保留 disabled `ChoiceGroup` 与 blocked `CommitBar` 两组 terminal unlayered convergence。规则只改变 option/CTA/status 的角色色与饱和度，不给父容器或阻断原因设置 opacity；当前 specificity 足够且不使用 `!important`。Tuning 和 Loot 的真实 computed fixture 分别守住原因/选中态、CTA/status 的 `opacity:1`、对比度、尺寸和原生聚焦语义。未来若出现更高 specificity 冲突，必须先复现并收窄 selector；确需 priority 时单独进入 ledger，不能把 G0 的 `WB-HIDDEN-001` 当作通用许可证。

## 10. 施工分批

每一批必须可以独立验证、独立回退；不得把 Web P0、跨层 authority、Host 输入、全量 CSS 收敛塞进一个提交。

### G0：恢复可信基线（已完成）

1. 先修共享 hidden 不变量，保证 feature 的 `display:flex/grid` 不能把 `[hidden]` action 重新显示。现役 `workbench.components / states / motion` 属于 named layer，而历史 feature/skin 是 unlayered；普通 layered 声明无论 specificity 和导入顺序都不能压过普通 unlayered 声明，因此禁止把修复放进上述 named layer 后声称完成；
2. hidden 兼容规则固定放在 `panels.css` 最后导入的 unlayered terminal compatibility utility 中，使用 `.workbench-shell [hidden] { display:none !important; }`。这是语义 hidden 的迁移期唯一 `!important` 例外：在规则旁登记原因、作用域和退出条件；静态门拒绝 feature/skin 对 `[hidden]` 设置任何非 `none` display 或用 `!important` 反向覆盖。只有历史 feature/skin 完成分层、且同一 computed display / hitbox / Tab 矩阵证明不再需要时，才可移除例外；
3. 为 `HelpAction` 增加稳定 `data-header-action="help"`；
4. 同时消除 `return-build` 泄漏并让 Character Build workbench harness 转绿；只补 help hook 不算完成；
5. 增加 hidden 的 computed display / hitbox / focus 断言，固定当前 header action 状态矩阵与失败截图复现；
6. 记录 WB112 行数预算：`inventory-workbench.js`、`character-build.js`、`skills.js`、`npcshop.js`、`workbench.js` 均接近阈值，不提高阈值、不压行；
7. HeaderProjection 放入窄新模块并从 facade 搬走散落逻辑，目标是 facade 净减行；其他触线模块只按 canonical 既有“facade 抽 header/close 协调”路径拆分，不临时发明框架；
8. 不在红灯上开始 profile 或大规模 CSS 迁移。

### A：Web 交互真实性

#### A1 Header 与导航（现役导航原子性已完成；整备菜单生产切换已由 B7 闭环）

- 在 G0 已建立的 `InventoryWorkbenchHeaderProjection` 上扩展固定整备菜单状态；
- 移除 Stats / Build 泄漏按钮和重复返回；
- 在 presentation-only header 模块实现 dev-gated 单层整备菜单及其 ARIA/focus/lifecycle；facade 只绑定固定 handler，本批不切换生产 exact-set；
- 把 Esc 改为显式 view stack；
- KShop inventory mode 原子更新 help/close/action；
- 每个可见 action 消除静默 no-op。

#### A2 Tuning 决策区（A2a / A2b 已完成）

- “逐次确认 / 单件快捷”接入 `ChoiceGroup`；
- 独立与 Character Build 嵌入态消费同一 confirmation mode/persistence，分别放在适合自己的 header 或 PaneChrome；
- 常驻说明模式边界；
- Tuning detail 与固定 CommitBar 分离；
- 独立 tuning 接入 info Esc consumer；
- 左栏 current authority source 以窄 source-port glyph、`aria-current` 与完整 accessible name 持久可见，不恢复粗重文字贴纸；
- 插件槽容量只消费当前 `snapshot.equipment.modSlotCapacity + snapshot.equipment.mods`；inventory/loadout 物品格的 `modSlots[]` 只属于 presentation projection。capacity 仅接受 `0..64` 整数且 installed≤capacity，不设三槽业务上限；`0` 明示无槽，missing 不猜，negative/fraction/Infinity/installed 超限/容量超限等 malformed 明示容量未知。真实剑圣腿甲固定 `1 个进阶 + 3 个插件`，`1/3 + 2` 个空插件槽用于剩余容量态，另以独立 4+ synthetic 证明无三槽硬编码。顶部 summary 是持久快捷总览，进阶/已装/空槽均可聚焦进入对应操作；配件 operation 槽区按同一 capacity 提供大尺寸替换、单拆与安装入口。空槽只导航到 `install_mod`、零 preview/commit，不向 authority 提交物理槽号；summary/operation focus key 按 surface + 槽序号唯一化；批量卸下独立于 slot group；
- blocked reason 与 disabled CTA 分离；
- 让 tabs、阶级和全部强化控件共享 reconcile / detaching / retry 锁投影。

现役实现已把 detail 与固定 CommitBar 分离，独立/嵌入态共享 confirmation mode；preview 更新保持 DOM/draft/focus/scroll identity，inventory/loadout source 常驻可见，`readPending / busy / reconcile / detaching / retry` 对 tabs、tier、input/stepper/range/marks/cap 与 CTA 使用同一锁投影。`projectLoadout` 按 owner 保存并只恢复自己接管节点的 base/original `disabled` 与 `aria-disabled`，不得误解锁 drug/empty/blocked 或原生 disabled；强化 number/range/mark/stepper/cap 保持发起控件焦点，候选/交换 preview 才可迁移 CommitBar 语义焦点；confirmation wrapper 使用有名称的 `role="group"`，busy/`aria-disabled` 下沉真实控件族，不污染 generic section。ChoiceGroup reject/throw 会回滚 pressed 视觉。该结论只覆盖 Tuning feature，不替代 C3 candidate tuning。

#### A3 状态与可发现性（已完成）

- Character 候选五态；
- inspectable / actionable 分离；
- OwnedInventoryPane、Inventory/NPC/Loot 子卡与丢弃动作使用同一锁投影；
- SecondaryPage 不再露出已经 inert、却看起来可用的底层 header action；
- Loot / Skills / NPC 帮助入口收敛；
- Loot 与其他面板去技术词；
- KShop/cart 等真实键盘 inspection 缺口一并按领域修复。

现役实现已经把 shared SecondaryPage 收敛为 exact Back/Help/Close 与 header suppression 引用计数，并接入 Loot/Skills/NPC；同时完成 Loot player-copy gate、KShop keyboard inspection、NPC locked entity inspectability 和对应生命周期/异常挂载护栏。A3、定向视觉 F 与 G3–G5 完成不表示剩余旧 CSS / G6+ 的视觉或 cascade 债务已核销；各批只覆盖其明示范围。

#### A4 Crafting（已完成）

- 迁移 QuantityControl；
- 固定 CommitBar；
- 保持 DOM identity、草稿、焦点和滚动；
- 修正帮助与真实输入能力不一致；
- 让 `data-title` 说明对 hover 与 focus-visible 等价，并移除 9px 中文说明。

### B：整备导航纵切

该纵切必须按下列顺序形成独立可验证、可回退提交；不能与 Header CSS、全量 Notch 重绘或其他 Character Build 能力混在一起：

1. **B1 信息架构与 HUD 几何（已完成）**：当批先以默认关闭的 `PreparationNavigationV1` rollout gate 落地 C# frozen tuple、Native 四行、legacy 游戏/整备两行与统一 progression reason；B7 后代码默认和随仓配置已切为 on，off fixture 继续精确保留旧生产 HUD，on fixture 验证新几何。battlebox/tuning 在 `questProgress <= 13` 时结构可见、disabled，并统一说明“完成基地整备后开放”。右侧条件槽的单 owner 仲裁也已独立完成，不新增整备子按钮；
2. **B2 独立调制 production contract（已完成）**：`EQUIPMENT_TUNING` exact Host/AS2 opener、opaque nonce、baseline/admission/lifecycle/一次消费与 fail-closed 矩阵已落地。成功只打开 `{profile:battlebox,view:tuning,source:nativehud_equipment_tuning}` standalone Workbench；与 Battlebox 同实例本地互切，普通 Close/Esc 回游戏。legacy renderer 仍保留；B7 只让默认 on 的 toolbar 触达该既有 handler；
3. **B3 局部 Workbench view（已完成）**：Build → Battlebox / standalone Tuning 保持 exact instance/session/pause；嵌入调制切 standalone 时先完成 exact detach；两条路径都能显式返回 Build。feature-local `WorkbenchViewStack` 只接受 `build | storage | tuning`，支持相邻 back 与有界 ancestor unwind，不保存 handler 或任意目标；导航在目标 ready 后才 commit，失败恢复原 DOM 与 opener focus，Esc 一次只退一层。独立 HUD 打开的 storage/tuning 没有 Build ancestor，Close/Esc 仍普通回游戏；
4. **B4 固定 post-close 基座（已完成）**：Skills intent 已收敛为只持 target、exact instance、phase/generation/lifecycle epoch/timer 的三目标 enum 小结构，不包含 handler、registry、DSL 或任意 destination。strict parser 只认 `navigate_skills | navigate_materials | navigate_intelligence`，但仅 Skills enable；Materials/Intelligence 在任何 close/open 前拒绝。arm→settled 与 target-open timeout 已分离，settled 在同一锁内销毁 arm 阶段并进入 Skills wait；pre-destructive 失败保留/恢复 exact Build，退场后至多一次 Character rollback，lifecycle/competition/stale/late callback 均有 once fence。`returnFocusAction` 只认 `skills | preparation-menu`，B4–B6 Host 仍发 `skills`，旧 header 不变；
5. **B5 Materials（已完成）**：direct HUD 与 Character post-close 都先建立独立 material target wait，再由 Host 生成 opaque requestId，经 AS2 原样回显后只准入 exact `crafting/nativehud_materials/materials` 封闭 wire；missing nonce 仅在无 armed/wait 时保留老 HUD ordinary open，pending 时拒绝且不消费，nonce-bearing wrong/near-match 作为当前 target failure 清 wait。send 成功不冒充打开，timeout/admission/competition/lifecycle/迟到/重复均有 once fence，Character 退场后失败至多一次原生 Build 回滚；
6. **B6 Intelligence（已完成）**：只在固定 switch 内启用 Intelligence。exact settled 后在同一 lifecycle fence 内销毁 arm/timer、捕获 Host exact idle admission，并以 Host 内建封闭 `{mode:"prod",source:"runtime",debug:false}` 同步打开；零 AS2 nonce、零 target timer、零 `FixedPanelOpenWait`。admission/open false 或 exception 在退场后至多一次 Character rollback，rollback 失败保持游戏态并给出装备入口提示；competing intent/panel、epoch、navigation/热重载、socket/shutdown、迟到/重复 settled 全部 fence。Host 已接受后 pause socket false/throw 不反转成功边；
7. **B7 原子生产切换（源码与自动门已完成）**：`PreparationNavigationV1` 的代码默认和随仓配置均已切为 `true`；显式 `false` 或配置项存在但不是合法 bool 时 fail closed 到完整旧呈现。on 时 Native/legacy HUD 同步切为游戏/整备分组，Build exact-set 为 `preparation-menu | stats | help | close`，Skills 返回及 Skills/Materials/Intelligence 回滚统一聚焦 `preparation-menu`；off 时恢复旧 HUD、`storage | stats | skills | help | close` 和 `skills` focus。Host 只给 Build `initData` 注入严格布尔 `preparationNavigationV1:true`，off 时省略，非 Build 永不携带；Web 只接受严格 optional bool、同 gate 的固定 focus 和固定六项 action 映射，类型错误或 gate/focus 不配对直接拒绝 launch。该 gate 只切 presentation，不改变 B2–B6 route authorization。

B7 当前源码已原子切换 production presentation，但这只说明同一施工树的 Host/Web/HUD 源码与自动门闭环。尚未形成 immutable candidate，也未完成真实 Native/legacy HUD、游戏内 WebView2、显式 off 回退、promotion、部署或标准入口旅程，因此不能称为已部署或正式验收。菜单只允许既有六目标；disabled 项必须给出运行时阻断原因，不能用“即将开放”替代未实现能力。

B3 fresh 自动证据：inventory workbench modules `25/25`；production Character Build workbench 三视口各 `253/253`，合计 `759/759`，另有 storage-to-build hidden-body `4/4`；standalone Tuning 三视口各 `96/96`；lifecycle `8/8`、focus `12/12`、focus integration `15/15`、components `16/16`；strict UI audit `0 error / 0 warning`。这些断言逐名覆盖 Build → Storage → Tuning → Build 的 bounded unwind、独立 HUD storage/tuning 的 Close/Esc → game、embedded tuning exact detach 与迟到 callback fence、失败回滚与 opener focus；B3 没有 Host/AS2 协议改动，因此未运行 Flash、未 publish，也不表示 B5–B7 或生产 rollout 已完成。

B4 fresh 自动证据：Launcher focused `381/381`、全量 `1681/1681`；inventory workbench modules `26/26`；standalone Character Build 三视口合计 `215/215`，production workbench 三视口各 `256/256`、合计 `768/768`，另有 hidden-body `4/4`；Panel runtime `27/27`，Panel contracts `62/62`。断言覆盖三 reason 封闭 parser 与仅 Skills enable、exact instance、arm/target 两 timer 分离、settled race、competition/cancel/lifecycle/late callback、一次 rollback，以及双值 focus parser/非法 selector。B4 没有修改 `.as`、Flash 资产或 SWF，故未运行 CS6、未 publish；这些自动门也不等于 candidate、游戏 E2E、promotion、部署或 B5–B7 完成。

B5 fresh 自动证据：Launcher focused `249/249`、全量 `1707/1707`；tracked item-panel runner 的唯一完成 runId 为 `bec9c608004d48e0bcb18a7f1c0f5708`，EquipmentInventory `28/28`、NPC `46/46`、Inventory `144/144`、Crafting `36/36`，Compiler Errors/Warnings `0/0`、32K retry `0`，TestLoader 最大函数体 `19,072B`。Host/TaskRegistry 负例覆盖 missing nonce 保留 wait、wrong/near nonce 当前目标失败、顶层/`initData` 封闭 shape、send-false/throw、timeout、admission、competition、lifecycle、late/duplicate、direct 与 Character post-close 两来源，以及至多一次 rollback；AS2 覆盖 omitted 参数的 legacy envelope、合法 token exact echo 和显式畸形 token 零发送。三份修改的 `.as` 均保留 UTF-8 BOM；本批未 publish `scripts/asLoader.swf`，也未构建 candidate、执行游戏 E2E、promotion 或部署。

B6 fresh 自动证据：Launcher focused `257/257`、全量 `1715/1715`；inventory workbench modules `26/26`，standalone Character Build 三视口合计 `215/215`，production workbench 三视口各 `262/262`、合计 `786/786`，另有 hidden-body `5/5`；Panel runtime `27/27`，Panel contracts 4 domains / 23 commands。Host 负例逐项覆盖 exact settled 后同步 admission、固定三键 initData、零 AS2 nonce/target timer/Skill-Material wait、admission false/throw、pause socket false/throw、competition、lifecycle advancement、navigation/热重载、shutdown、迟到/重复 settled、至多一次 rollback 与 rollback 被 fence 后的可行动提示。B6 没有修改 `.as`、Flash 资产或 SWF，故未运行 CS6、未刷新 TestLoader、未 publish，也未构建 candidate、执行游戏 E2E、promotion、部署或标准入口验收。

B7 与最终 header 真实性收口的 fresh 自动证据：preparation leaf `10/10`、inventory workbench modules `27/27`；production Character 三视口各 `277/277`、合计 `831/831`，另有 1024 hidden-body/lifecycle `12/12` 与三视口 normal/reduced 键盘/几何 `18/18`；Tuning 三视口 `96/96`、KShop `134/134`；Launcher focused `237/237`、全量 `1720/1720`。断言覆盖严格 bool 注入/省略、on/off focus、WebOverlay recovery、两套 HUD/Program 同 gate 接线、固定六路映射、current/disabled 零业务、progression reason、menu lifecycle、ARIA、roving focus、Esc/Tab/Shift+Tab、outside pointer/focus、`204px` 单列几何和 reduced-motion；另在真实 held-reconcile busy 窗口验证可见 stats/close 仍有 hitbox，四个 reasoned action 保留基线 accessible name、生产 cue 切为 `error`、DOM 激活只解释对应动作原因（Close 含关闭语义）、恢复精确还原，且零 stats/navigation/close 穿透。该门不冒充 trusted pointer/Enter/Space 输入。standalone tuning 兄弟按钮也已按 §5.6.5 固定为“战备箱与背包”。本批没有修改 `.as`、Flash 资产或 SWF，未运行 CS6/publish，也未构建 candidate、执行游戏 E2E、promotion、部署或标准入口验收。

`PreparationNavigationV1` 是一个 Host-owned、全开或全关的**临时 rollout gate**，当前代码默认和随仓配置均为 on；显式 off 或非法配置值整体回到旧呈现。它只仲裁新旧呈现切换，不参与 route authorization；不得派生六个 destination flags、远端配置表或运行时 registry。标准入口验收和约定回退观察窗结束后，应删除旧呈现分支与该 gate，避免双实现长期并存。

### C：跨层 Character Build 纵切（C1/C2/C3 已完成）

三个能力必须拆开施工、分别闭环：

1. **C1 分类 facet counts（已完成）**：AS2 权威投影 → Host validator → Web badge / empty state；
2. **C2 候选 rich tooltip（已完成）**：exact source → 复用 inventory tooltip endpoint → scoped PanelTooltip；
3. **C3 候选直接调制（已完成）**：inventory source → preview/commit → exact lease refresh → 候选恢复。

每条纵切都必须保留旧客户端/旧 payload 的 fail-closed 或渐进降级行为。若触及 `.as`，新增/重建文件必须 UTF-8 with BOM，并按实际 Flash target 做 CS6 fresh trace；不能用 `publish_done.marker` 单独声称通过。

C1 的 wire 只在 loadout projection 上增加可选 `candidateFacets:{scope:"all",filterFacets,filterItemCount}`，不增加命令。AS2 复用同一次完整背包 snapshot，用候选结构资格检查复扫 11 个装备 `use` 与药剂，并在复扫前后以 `containerVersion` 围住 revision；相关计数不一致、结构损坏或 revision 漂移时省略整组字段。Host 接受 exact legacy shape 或只多该字段的 exact shape；字段出现后递归结构、计数关系、重复 id、控制字符、scope 和 `0..50` 任一畸形都拒绝整份响应。Web 只消费相关 `use` leaf：`手枪2` 合并 `手枪2 + 手枪`，药剂槽读取 `药剂`；明确 `0` 与旧 payload/异常投影的 unknown `—`，并把等价语义写入 ARIA。badge 只装饰既有稳定 slot，零额外业务请求，不接管 key/focus/scroll，也不把完整背包 `filterItemCount` 误写成候选总数。

C1 fresh 自动证据：facet leaf `16/16`、projection leaf `4/4`、Character session `22/22`；focused Host xUnit `172/172`，Launcher 全量 `1646/1646`（0 failed / 0 skipped）；standalone Character Build 三视口合计 `215/215`，production workbench 三视口合计 `711/711`，另有 hidden-body `4/4`；strict UI audit `0 error / 0 warning`，CSS bundle 20 imports。官方 focused TestLoader runId `7c6da8cc494747ad979b899eaeb87b85` 取得 SaveManager `162/162`、EquipmentInventory `28/28`、InventoryPanelService `144/144`、transaction `19/19`、CharacterBuildService `167/167`、PlayerInfoProvider `19/19`，合计 `539/539`，Compiler Errors/Warnings `0/0`、32K retry `0`；`.as` BOM 与 runner scratch 恢复也已复核。该证据仅闭合 C1 源码纵切，不能替代 C2/C3 各自证据，也不等于 `candidate_built`、游戏 E2E、promotion 或部署。

C2 复用 `InventoryPanelService.inventoryTooltip`，没有修改 `.as`、SWF 或 Flash target，因此本批不运行 CS6、不 publish。Host 以 exact context 分流、最近候选 source allowlist、session/instance/write completion fence 与 Character-only strict rich response 封闭读路径；普通无 context storage tooltip 保持旧合同。Web 以独立 `character-build-candidate-tooltip.js` leaf 持有 scoped binding/cache，不拥有选择、装备或调制能力；pointer/focus 共用内容，basic fallback 在 rich pending 时仍可读，失效/超时不重放。fresh 自动证据：C2 Node leaf `6/6`，session `22/22`、projection `4/4`、facet `16/16`、slot transition `18/18`；Host `CharacterBuildTaskTests|InventoryTaskTests` focused xUnit `226/226`、Release build `0 error`；standalone Character Build 三视口合计 `215/215`，production workbench 三视口各 `256/256`、合计 `768/768`，另有 hidden-body `4/4`，dressup `211/211`；strict UI audit `0 error / 0 warning`。该证据闭合 C2 源码与自动门，但不能替代 C3 独立证据，也不等于游戏 E2E、immutable candidate、promotion 或部署。

C3 开工前已在完整主仓复核 inventory-source 写入闭环：`launcher/src/Tasks/EquipmentTuningTask.cs` 与 `EquipmentTuningService.as` 都支持 `sourceKind:"inventory"`、`containerId:"背包"`、slot、`expectedLease` 及写后背包 snapshot/new lease。本批据此只修改 Launcher Web adapter、现有 Storage cache 窄端口、懒加载/发布资产清单与测试，没有修改 Host、`.as`、Flash 资产或 SWF，因此不运行 CS6、不 publish。

C3 fresh 自动证据：candidate tuning leaf `11/11`，tuning capability `28/28`、projection `4/4`、Character session `22/22`、Equipment Tuning model `48/48`、Inventory workbench modules `26/26`；standalone Character Build 三视口合计 `215/215`，production workbench 三视口各 `262/262`、合计 `786/786`，另有 hidden-body `5/5`；Equipment Tuning 三视口 `96/96`；default/release-tree `--strict-warnings` UI audit 均为 `0 error / 0 warning`。断言固定 exact source、selected-candidate precedence、loadout fallback、写后唯一候选读取/new lease/source 重绑、Storage 零激活、tooltip invalidation、迟到 callback fence、同槽歧义 fail closed、next→previous→empty、非零滚动与 PanelScale 下 nearest-visible focus，以及 1024px 三动作单行/min-width。该结果闭合 C3 源码与自动门，但未形成 immutable candidate，不等于游戏 E2E、promotion、部署或标准入口验证。

### D：Host 浏览器输入不变量（代码/自动门已完成；150% 双宿主实机已完成，100%/真实 pinch 待设备）

- `WebView2BrowserPolicy` 在 `BootstrapPanel` 与 `WebOverlayForm` 完成 EnsureCore 后、首次 Navigate 前共同应用；生产态统一关闭用户 zoom/pinch、browser accelerator、DevTools、默认右键、自动填充与密码保存，Host 写入的 `ZoomFactor`、系统 DPI、PanelScale、普通 wheel 与 inspection wheel 保持原路径；
- 显式 `webView2DeveloperMode` / `CF7_WEBVIEW2_DEV_MODE` 只恢复未被 Host 热键合同保留的 browser accelerator、DevTools 与默认右键，用户 zoom/pinch、自动填充与密码保存仍关闭。`KeyboardHook` / `HotkeyGuard` 保留 `Ctrl+W/R/P/O/F/Q`，所以开发态以 `F5` 作为 reload 正例；`Ctrl+R` 在两种模式都继续服从 Host 合同，不能用作策略正反断言；
- Host 自动测试已枚举两个宿主的 production/development 设置矩阵、AppConfig/env、首次导航前应用和负向分支；`audit-workbench-ui` 只验证 Web/CSS 与发布接线，不解析 C#；
- exact candidate `c-2da549ad5580-08846e81b3-20260729t095347915z-04dc7b0c` 已在本机 144 DPI / 150% / DPR 1.5 实际运行。production 两个宿主日志均为 `developerMode=False zoom=False pinch=False accelerators=False devTools=False contextMenus=False autofill=False passwordAutosave=False`；Ctrl+plus、Ctrl+wheel、右键、F12、F5 后页面/截图/导航数保持不变。development 两个宿主日志均为 `developerMode=True zoom=False pinch=False accelerators=True devTools=True contextMenus=True autofill=False passwordAutosave=False`；Bootstrap 的 F5 reload、默认右键与 F12 DevTools 均实际可用，Ctrl+plus/Ctrl+wheel 仍不改变 viewport；
- 同轮实际验证 Overlay 的 PanelScale 瞬态几何为 `1024×576 → 800.05×450.03`、普通 OS wheel 可令临时 browser-native overflow `scrollTop 0→120`。帮助路由经真实 `window.chrome.webview.postMessage` 打开并由真实 close button 关闭；这些只证明输入/路由链，不冒充完整游戏面板几何或 B7 E2E；
- 当前机器只有 150% 显示器且无触控/触控板，不能安全制造 100% 物理显示或真实 pinch。CDP 合成 pinch 不记 E2E；因此 D 批目前是“自动门 + 150% 双宿主实机”，不是完整 100%/150%/pinch E2E。该 candidate 也没有 promotion 或标准入口身份。

### E：布局 profile 与真实视觉证据（已完成）

- `DualPaneShell({profile})` 在 DOM 创建前以封闭枚举 fail fast；`setProfile(profile)` 与构造器共用 validator，并原子投影内部值和 exact root attribute；
- 每个生产 consumer 显式选择 profile；KShop / Inventory 原位切 view 时通过 Shell-owned `setProfile(profile)` 与自己的 chrome/view attribute 同步更新，Skills / Crafting 切 view 时重建 Shell 并在新构造中选择目标 profile；
- 纯 import facade 中的 `workbench/profiles.css` 接管全部壳级列宽和行轨；feature 壳级 `grid-template-columns/rows` 覆盖、例外均清零，density 往返保持同一宏观 split；
- Loot root CommitBar 与 Storage body footer 使用不同 exact marker，computed row、命中区和可聚焦 CTA 由真实结构驱动；
- Character Build、Storage、Tuning、Crafting、Skills、Loot 及 KShop/NPC 的真实 DOM fixture 覆盖六种 profile，并在 1024×576 最低逻辑画布验证横向溢出、窄 pane、长中文/长材料、拥挤 header、blocked/busy、固定 CTA 与 computed split；
- 48 atlas 继续作为 synthetic shared scale / DPI 矩阵，不冒充领域验收。

本轮 fresh 结果：default strict 与 `--release-tree --strict-warnings` 均 `0 error / 0 warning`；profile 单测通过 closed enum + atomic projection，ratchet `23/23`；atlas `48/48`、item matrix `17/17`；Character Build 主 harness 合计 `208/208`（三视口）、workbench 合计 `705/705`（三视口各 `235/235`，另有 storage/hidden-body 各 `4/4`）、dressup `211/211`；Tuning 三视口各 `96/96`、Crafting 各 `117/117`、Skills 各 `150/150`，KShop `123/123`、NPC `97/97`、Loot browser `73/73` + lazy `6/6`。这些数字只描述同一施工树的当次输出，不冻结为长期合同，也不表示 candidate、promotion 或部署。

### F：视觉 CSS ratchet（F0 与本轮定向视觉 F 已完成；剩余存量另立原子批）

- F0 已扩展 `tools/audit-workbench-ui.js` 并冻结 `c96f4c3d750561022b706c72a4d53050431e627d` 基线：touched-line / baseline 计数门覆盖一般裸色、9px 玩家中文、裸 duration/easing 与 `transition:all`；当前门通过只表示没有新增债务，不表示存量已经清零；
- 本轮定向视觉 F 只处理 K3 与代码追扫确认的玩家可读性/状态/滚动条缺口：`<9px` 已从 `1→0`，9px 玩家文字从 `94→34`，剩余项限定为短数字、角标、符号与技术刻度；长中文正文、条件和错误原因至少 10px，主标签、名称和按钮至少 11px，唯一 8px balance badge 提升为 9px；
- disabled `ChoiceGroup` 与 blocked Loot `CommitBar` 不再整组压暗，原因/状态保持正文对比度；terminal unlayered 机制与 `!important` 边界见 §9.4；
- 只给 `.inventory-owned-grid`、Loot 双滚动面和 `.equipment-tuning-detail` 增加 exact 7px themed stable scrollbar。Loot 64/50 槽与 Tuning 长预览均形成真实纵向溢出；KShop full-density production DOM 又在 1024×576 下证明 `.inventory-owned-grid` 纵向溢出、零横向溢出与 `scrollTop 0→60`。三类门均读取 standard thin 和 pseudo track/thumb/border/button/corner computed style；八列 Loot 仍保持 44px 命中和零横向溢出；
- 同批把触碰到的正文/状态色迁到现役 semantic/skin token，不扫荡未触碰颜色；raw color ceiling `2705→2645`，raw duration `144`、raw easing `105`、`transitionAll` `16` 不变。最终 default/release-tree strict 均 `0 error / 0 warning`，ratchet `36/36`；
- fresh 领域证据：Tuning 三视口 `98/98`、Loot browser `77/77` + lazy `6/6`、Crafting `117/117`、Skills `150/150`、KShop `134/134`、NPC `106/106 + reduced 2/2`，atlas `48/48`、item matrix `17/17`、components `16/16`、primitives `10/10`、focus integration `15/15`。Character preparation selector 与 reasoned busy header 的生产回归已以 42px item、真实 hitbox、基线 accessible name、`error` cue、DOM zero-through 激活和三视口 `831/831 + 12/12 + 18/18` 收口；
- 本批不改变 authority、focus、scroll window identity 或命中区，不建设全局 scrollbar/disabled abstraction，也不把剩余旧颜色、skin 结构与 G6+ named-layer 扩围塞入同一轮。

### G：motion / cascade 收敛（G1–G5 已完成；G6+ 未完成）

- **G1（已完成）— shell-level SecondaryPage cascade 与 structural motion**：KShop settlement、NPC settlement 与 NPC help 已删除 unlayered skin 中重复的 `position/inset/z-index/display`；结构由 shared components 唯一提供，normal 态保持 `display:grid / z-index:55` 并真实执行约 `210ms` 的 opacity/transform 进退场。`SecondaryPage.open()` 在同一 task 内 mount 后立即 open 时，只做一次有界 layout commit 再切 active，保证首帧也从 inactive 几何开始；
- G1 的 reduced-motion 真实 consumer 直接得到 `transition-duration:0s / animation:none / transform:none` 的静态终态。为压过现役 unlayered skin 的等 specificity 规则，root-level reduced selector 显式命中 `.workbench-shell .workbench-secondary-page`，复用已登记的 reduced-motion priority 声明；没有新增 `!important`、全局 layer 或 ledger 例外；
- `.npcshop-space-page` 明确保留为 settlement 内嵌 organizer：本地 `inset:0 / z-index:4 / display:none|grid` 不迁升为 shell page。Character Stats 与 Doll Preview 的 full-body geometry 同样是 host-local，不属于 G1；
- G1 的正常/减弱 computed-style 门、真实 CSSTransition、关闭后的不可见/不可命中，以及上述 nested exception 均已进入 KShop/NPC harness；KShop 的布局门改读真实主 body 与 shell clipping，避免把 inactive page 的受控 `translateY(6px)` 误报成可滚动溢出；
- **G2（当批历史状态；退出已由 G5 完成）— focus role 的最终 computed convergence**：G2 当批时，named `workbench.states` 中的 baseline 无法压过当时的 unlayered feature normal declarations，因此 `states.css` 曾暂时增加限定 `.workbench-shell[data-profile]` 的 unlayered production bridge。边界使用构造器保证的封闭 profile，而不是可选 skin；当批覆盖 KShop、NPC、Crafting、Skills、Character/Storage/Tuning，也覆盖刻意没有 `data-workbench-skin` 的 Loot。bridge 只写 `outline` / `outline-offset` 并消费 `--wb-focus`，没有新增 `!important`、状态机、DSL 或全局 layer；现役实现已由 G5 删除该 bridge；
- G2 覆盖 button、EntityTile、input、select、range、`[data-scroll-region]` 与既有显式 tab stop。`.workbench-source-selected` 的选择角色迁到 `border-color:var(--wb-role-selected)`，使 selected 与 focus outline 可同时存在；`.character-build-slot` 继续由 inner card 画 `--wb-focus` 混色环，外层例外避免双环，并由 runner 同时核对 focus mixed token、selected token、轮廓与边框；
- 该 unlayered bridge 当批就是明确的级联迁移边界，不是永久 feature override；其退出条件已经由 G5 满足：历史 feature/skin 的 `outline:none/0` 已删除或迁移，KShop 全控制族、skinless Loot、Character slot/stats scroll、全领域 harness 与 atlas 的 computed 门在删除 bridge 后仍全绿，现役只保留 layered baseline；
- G2 fresh 证据：KShop `134/134`、Loot browser `75/75` + lazy `6/6`、Character workbench 三视口 `768/768` + storage/slot focus `5/5`；NPC `106/106` + reduced `2/2`、Skills `150/150`、Crafting `117/117`、Tuning `96/96`；focus `12/12`、integration `15/15`、components `16/16`、primitives `10/10`；atlas strict `48/48`、item matrix `17/17`、default/release-tree strict audit 均 `0 error / 0 warning`、CSS bundle 20 imports。G2 未修改 Host/AS2 或 SWF，不代表真实 WebView2 smoke、candidate、promotion 或部署；
- **G3（已完成）— CSS 语义 motion / keyframe 收敛**：限定 `skills/entities/inventory/core/equipment-tuning/skins/character-build/components` 与 foundation 现役 KShop consumer，把 `.08/.1/.10/.12/.25/.28/.75/1.8s` 迁入 micro/standard/reject/busy/ambient token，transition 统一既有 standard easing 与允许属性；`.001s/.001ms` 和 visibility `0s linear` 延迟合同原样保留；
- G3 把 `kshop-pulse` / `skills-reject-pulse` 改为 shared neutral `wb-pulse-busy` / `wb-reject-pulse`，core/inventory/foundation KShop 与 Skills 分别消费 busy/reject token；Tuning ambient core 原本定义/消费同域，只换 ambient token。没有 JS helper、状态机、全局 `!important`、新 token/easing 或 scanner feature 白名单；
- G3 fresh 证据：Skills 三视口 `150/150`、KShop `134/134`（含 inventory 生产流与 busy normal/reduced）、Tuning 三视口 `96/96`；focus `12/12`、integration `15/15`、components `16/16`、primitives `10/10`、atlas `48/48`、matrix `17/17`、bundle 与文档治理门同树通过。并发 C3 施工期间曾因 `inventory-storage-workbench.js` 暂超 900 行出现单一 `WB112`；C3 收口后已 fresh 复跑 Inventory modules `26/26` 与 default/release-tree `--strict-warnings` 两门 `0 error / 0 warning`，该阶段 warning 只保留为历史，不能冒充最终树状态。G3 ratchet ceiling 已精确收为 raw color `2728`、raw duration `147`、raw easing `105`、`transitionAll` `16`；本批未扫描或修改 lockbox/非工作台 `transition:all`；
- **G4（已完成）— reduced-motion 收口与壳外 owner**：删除 `skins.css` / `character-build.css` 的 `.001s/.001ms` 伪关闭，删除 Loot / Equipment Inspector 的重复 local reduced block，Tuning 仅保留 `opacity:.55` 静态补偿；shared root 唯一关闭 shell 内 motion。`#panel-container` exact workbench owner 的相邻 Tooltip loading pulse 纳入 reduced，`tasks` 反例保持 normal；SecondaryPage 的 `visibility 0s linear` 延迟离场保留；
- G4 没有新增 `!important` 或 JS 抽象。Character renderer、Equipment Inspector 与 Tuning 图标的现役直接 `matchMedia` 分支分别以 actual renderer disabled、真实 CSSTransition 归零和 ambient animation 静态终态证明有效；当前重复不足以建设 helper/registry/listener。非工作台 `task_panel.css` 的 `0.001s` 明确留在范围外；
- G4 fresh 证据：ratchet `32/32`，raw duration ceiling `147→144`，Inventory / Skins / Character priority ledger 分别 `4→2`、`11→10`、`2→0`；KShop `134/134` + Tooltip/Inspector/Secondary actual motion probe，Tuning 三视口各 `96/96` + ambient probe，Character production 三视口合计 `786/786` + hidden/storage `5/5`。Skills `150/150`、NPC `106/106 + 2/2`、Crafting `117/117`、Loot `75/75 + 6/6`、Inventory modules `26/26` 及 focus/integration/components/primitives/lifecycle/inspection 均同树通过；default/release-tree strict 均 `0 error / 0 warning`，atlas `48/48`、matrix `17/17`、bundle 20 imports；
- **G5（已完成）— focus cascade bridge 退出**：删除 `states.css` 的 `.workbench-shell[data-profile]` unlayered production bridge；named `@layer workbench.states` 的 baseline 现在唯一提供 `outline:2px solid var(--wb-focus)` 与 `outline-offset:2px`，覆盖 button、input、select、EntityTile/option、显式 tab stop 与可聚焦 scroll region，且 normal/reduced-motion 使用同一计算值；
- G5 删除 `core/skins/entities/inventory/crafting/equipment-inspector/skills/equipment-tuning/components/character-build/character-build-stats` 中历史 `outline:none/0` 和 feature-local focus shorthand；hover 的 border/background/box-shadow 与 dragging/drop accept/reject 等语义状态 outline 原样保留。没有新增 `!important`、状态机、token 或 named-layer 范围；
- `.character-build-slot` 是 layered baseline 的唯一显式排除项：outer 保持 `outline:0`，inner card 用 exact `2px solid var(--wb-focus)`、`outline-offset:-2px`，selected 边框继续消费 `--wb-role-selected`。生产 gate-on preparation trigger/items 复用真实 controller/DOM，在 1024 路由中证明 normal/reduced 精确环与鼠标不伪造 `:focus-visible`；
- `tools/lib/workbench-ui-ratchet.js` 增加 CSS 结构解析器，`WB135` 同时拒绝 bridge 回流和未登记的 unlayered focus outline；只放行 named layer、drag/drop 状态 outline 与 Character slot 的 exact outer/inner 合同。G5 同轮把 touched raw color 改为现役 semantic/skin token，raw-color ceiling `2725→2705`，没有用 ceiling 掩盖债务；
- G5 fresh 证据：ratchet `36/36`，focus `12/12`、integration `15/15`，default/release-tree strict audit 均 `0 error / 0 warning`；production Character 1024 的真实 preparation menu visibility/focus `6/6`，KShop `134/134`、Tuning 三视口 `96/96`、Skills `150/150`、NPC `106/106 + 2/2`、Crafting `117/117`、Loot `75/75 + 6/6`。当批完整 Character runner 曾被后续 pose matrix 的 `男/手雷` 旧 fixture 阻断；B7 收口时已按当前 `手雷站立` 语义刷新 fixture，并以三视口 `828/828` fresh 复跑关闭该阻断；
- **G6+（未完成）**：删除剩余 feature 对 shared structural motion 的 unlayered 绕过，再按真实收益评估 named layer 扩围。不得把这些项倒灌回 G5，也不与业务流改造同批。

### H：补充 UX 加固（当前 source-ahead 已实装；未部署）

- **H1 Character 来源 Materials / Intelligence 返回构筑**：只有由 Character Build handoff 产生、且 Host 绑定到 exact `panelName + panelInstanceId + lifecycle` 的 Crafting Materials / Intelligence 子实例可投影 `navigationOrigin:"character_build"` 与 `canReturnCharacterBuild:true`。两字段只控制展示，真正一次性 capability 只保存在 Host；尚处 `PendingInstanceBind` 时没有授权任何同名实例，ordinary crafting/intelligence same-name open 必须在 admission/initData enrichment 前原子撤销 pending bind，forward echo 丢失、取消或迟到都不能授权后来普通实例。Web 返回只发送 exact 五键 `{type:"panel",panel:"crafting"|"intelligence",cmd:"close",panelInstanceId,reason:"navigate_character_build"}`；Native HUD 直开、普通 `×` / Esc / backdrop、stale/rebound/foreign instance 都普通关闭到游戏。Intelligence 只在 authoritative `state / bundle / snapshot / glossary_snapshot` 请求在途时阻断返回，tooltip 与后台 glossary catalog 不阻断，settle 后恢复。Host 必须先 exact retire 当前子 visual/owner，再复用 native equipment typed preflight，以 fresh workbench nonce 建立新 Character session/snapshot，并按现役 gate 聚焦 `preparation-menu`；navigation/热重载、socket、shutdown、competition 和 lifecycle epoch 前进都撤销 one-shot。不得建立通用 `returnTo`、跨 panel stack 或 destination registry；
- **H2 快捷确认边界**：沿用 F7/A2；“单件快捷”只省略一次人工确认，永不跳过 preview/token/lease/revision/write/reconcile。batch/cascade/remove-all 只保留 CommitBar 的一次显式确认，不出现自动提交或第二确认模态；
- **H3 插件槽与 source 身份**：容量只消费当前 `snapshot.equipment.modSlotCapacity + snapshot.equipment.mods` 的 `0..64` 整数并要求 installed≤capacity，不设三槽业务上限；`0`、missing、malformed/超限与 4+ synthetic 都是显式状态，真实剑圣腿甲按 `1 个进阶 + 3 个插件` 投影。顶部 summary 是持久快捷总览，进阶/已装/空槽均可 Tab/点击进入相应操作；配件 operation 是大尺寸详细操作面，按同一 capacity 全量显示，已装槽点击替换并保留独立单拆，空槽仅进入安装候选、零 preview/commit，批量卸下位于 slot group 外。两 surface focus key 必须唯一；空槽序号不构成物理槽提交。当前调制源以 `12×12px` source-port glyph + `aria-current` + 完整名称表达，交换候选使用“用于交换”语义，颜色/glyph 不能单独承担身份；
- **H4 层级筛选面包屑**：固定上层 PaneChrome 标题 + breadcrumb、下层当前层级选项 toolbar。breadcrumb 宽布局同行显示完整路径，祖先 crumb 可返回；只有自身真实溢出时折叠中段省略号，并保留 root/current 与完整 accessible path。下层选项取得整行宽度，宽度充足时同排，真实不足时自然换行且无横向滚动。省略号 `aria-hidden` 且 `title` 列出被折叠中段；未折叠时不留该 title。不得把上下两层合并或在有 facets 时回退 native select；
- **H5 Character 候选取消**：第一次 click/Enter/Space 只固定预览；再次单击同一候选或在同一候选上按 Space 清除选择/预览且零写入。只有同一已选候选上的明确、非 auto-repeat Enter 才触发唯一主 CTA，并继续受 disabled 与写门约束；
- **H6 调制写入体感与长期会话收口**：inventory 交换 preview target 固定为 exact `{sourceKind:"inventory",containerId:"背包",slot,expectedLease}` 四键；“卸下全部”改为与插件卡同高的最右方形批量按钮，仍位于 slot group 外。单件快捷允许先行投影 `aria-busy` 与 intent 阶段，但不得乐观改写数量、snapshot、lease、token 或权威槽位，也不得排队/重放 mutation。commit 成功先显示 Host 已验证的 `response.snapshot`，inventory external-write lock 持续到 fresh refresh；source ref 精确一致时省略第二次 tuning snapshot，否则 fresh read/retry/reconcile。owner-only 状态变化不得重建 snapshot 引用未变的 pane DOM。Storage ↔ Build 导航增加 transaction generation 与 destroyed fence，迟到 prepare callback 不得 activate/mount/history/focus；pending 期间 controller 上报不同合法 view 时先 supersede 旧事务并推进 generation，再按封闭三视图收敛，禁止新 view 已提交而旧 pending 仍锁住导航。所有领域 timeout 后仍未终结的 orphan transaction 由 30 秒 watchdog discard/abort，恢复可操作态且迟到 callback 零副作用。新增两条叶门分别覆盖分叉上报与 orphan prepare；在缺少 live hit-test、`ProcessFailed`、heap/console 证据时，不把重启恢复的黑屏写成已证实内存泄漏、renderer crash 或透明 overlay 遮挡。

H1–H6 的“已实装”只描述当前未提交 source-ahead 树与相应自动门，不代表 immutable candidate、游戏 E2E、promotion、部署或标准入口验收。

H1–H6 收口并合入上游后的 fresh 汇总为：Host H1 定向历史门 `253/253`、Launcher 全量 `1902 pass + 3 explicit opt-in skip / 1905`、0 fail；Crafting 三视口 `119/119`、Intelligence `30/30`、Equipment Tuning 三视口各 `115/115`（model `61/61`、runtime `25/25`、confirmation `4/4`、interaction `7/7`、source-marker leaf `2/2`）、Character standalone 三视口各 `218/218`、workbench `867/867 + 12/12 + 18/18`、inventory modules `31/31`、dressup `222/222`、preparation leaf `10/10`、KShop `135/135`、item matrix `20/20`、ratchet `66/66`，default/release-tree strict UI audit 均为 `0 error / 0 warning`。本 UX 分片没有运行 fresh CS6 compile/publish；这些数字只绑定当前 source-ahead 树，也不表示 candidate、游戏 E2E、promotion 或部署。

## 11. 验收矩阵

### 11.1 所有工作台改动必跑

```powershell
node tools/audit-workbench-ui.js --strict-warnings; node tools/test-workbench-profile.js; node tools/test-workbench-ui-ratchet.js
node tools/check-workbench-css-bundle.js
node tools/run-workbench-visual-atlas.js
node tools/run-item-grid-visual-matrix.js
node tools/test-workbench-components.js
node tools/test-workbench-focus.js
node tools/test-workbench-focus-integration.js
node tools/test-workbench-lifecycle.js
node tools/test-workbench-primitives.js
```

### 11.2 受影响领域

```powershell
node tools/run-character-build-harness.js
node tools/run-character-build-workbench-harness.js; node tools/test-inventory-workbench-preparation-menu.js
node tools/run-character-build-dressup-harness.js
node tools/run-equipment-tuning-harness.js; node tools/test-equipment-tuning-confirmation.js; node tools/test-equipment-tuning-interaction.js; node tools/test-equipment-tuning-source-marker.js
node tools/run-crafting-harness.js
node tools/run-kshop-harness.js
node tools/run-npcshop-harness.js
node tools/run-skills-harness.js
node tools/run-intelligence-harness.js
node launcher/web/modules/loot/dev/run-harness.js
```

整备导航还必须扩展现役 `NotchWidgetAnimationTests`、`RightContextWidgetTests`、`RightHudLayoutTests`、`WebOverlayFormPanelCloseTests`、`LauncherCommandRouterTests`、`PanelHostSkillInstanceTests` 与相关 panel-instance tests。Material requestId 合同必然触及 AS2：`CraftingPanelServiceTest.as` **只**证明合法 requestId 原样回显、显式非法/超长 token 零发送，以及 missing token 仍生成 legacy ordinary envelope；`LauncherCommandRouterTests` / 对应 PanelHost tests **另行**证明该 ordinary envelope 只在无 armed material intent / material target-open wait 时准入，存在任一 wait 时必须拒绝且零旁路。新增 reason 白名单、Host intent 和 Native HUD 布局不能只由 Web browser harness 证明。

跨层、Host 与 Flash 的附加验证仍按 `agentsDoc/testing-guide.md` 选择，不在 ADR 复制易漂移命令。

### 11.3 必须新增或强化的行为断言

| 维度 | 必须证明 |
|------|----------|
| Header | 每个 view 的 visible action exact-set；hidden computed display、hitbox、Tab stop 全为零 |
| Preparation IA | C#/Web frozen tuple 的六项目标、短标签、顺序、route exact-match；技能不再残留于“游戏”行；progression gate 在 Native/legacy/menu 投影同一 visible/enabled/reason |
| Atomic rollout | `PreparationNavigationV1=off` 或配置项存在但非法时生产 HUD/header/focus 仍为旧 exact-set；`on` 时 Native/legacy 分组、Build menu、Host `preparation-menu` 输出和 Web 固定映射同时生效；Web gate/focus 注入不配对时拒绝 launch，不形成半套 IA |
| Notch geometry | Native 四行与 legacy 游戏/整备两行的高度、expanded width、active region、hover/click rect、动画 union bounds；最低视口无裁切、绘制与命中一致 |
| Native tuning entry | `EQUIPMENT_TUNING` exact echo 顶层只有 `{task,panel,source,initData,openRequestId}`、`initData` 只有 `{profile:"battlebox",view:"tuning"}`；携 exact source 或 `tuning.open.*` token 的 near-shape 不回落 ordinary open；timeout/duplicate/active-panel 零打开；standalone tuning↔battlebox 与普通关闭到游戏 |
| Preparation menu | current `aria-current+aria-disabled` 且零业务调用；Click/Enter/Space 同为 toggle，ArrowDown 打开并进入 roving，Home/End/Esc/outside-click 正确；Tab/Shift+Tab 只关闭且不 `preventDefault`/人工移焦；动态 disabled reason、SecondaryPage suppression 与 listener/focus 清理 |
| Local preparation views | Build→Storage/Standalone Tuning 保持 exact instance/session/pause；embedded→standalone tuning 先 detach；两者返回 Build 不 finalize |
| Cross-owner navigation | 三个固定 reason 的 exact-instance arm；arm→settled 与 Skill/Material target-open timer 严格分离且 settled 原子换阶段；competing/stale/timeout 取消；关闭前失败保留/恢复 Build，关闭后失败至多一次 Build 回滚；当前默认 on 输出 fixed `preparation-menu`，显式 off 恢复 `skills` |
| Child return to Build | 仅 Character-origin Materials/Intelligence exact instance 获得 Host one-shot；展示 hint 不授权；ordinary same-name open 在 enrichment 前撤销 `PendingInstanceBind`，forward 丢失/取消/迟到不转授；Intelligence 只以 authoritative `state/bundle/snapshot/glossary_snapshot` busy 阻断，tooltip/后台 catalog 不阻断且 settle 恢复；Web exact 五键 close、普通 close/Native HUD 负例、child exact retire→fresh workbench nonce/new session、lifecycle/competition 取消均须证明 |
| Materials correlation | exact wire 的 `initData.view` 层级与封闭 shape；Host requestId 经 AS2 原样回显；near-match/late/duplicate/wrong layer/extra field/source/view/admission 全拒；missing nonce 仅在无 material wait 时 ordinary-open，pending 时拒绝且零旁路 |
| Right HUD slot | 单一 slot owner；`transaction > notice > hint > hidden`；空视觉槽零几何/命中；装备 hint 有信息增量；SafeExit 与 notice/hint 绘制、hitbox 互斥 |
| Action truth | 每个可见 action 有效果或 disabled reason；无 silent no-op |
| SecondaryPage | underlay 可见 action 要么 computed hidden/disabled，要么排除 inert 且真实可用；Back/Help/Close exact-set 正确 |
| Help | 从未打开/打开/关闭帮助，capability 完全一致 |
| Esc | draft/modal → info → tuning → storage → build → game，一次只退一层 |
| Candidate states | unselected/loading/empty/error/ready 文案与下一步各异 |
| Identity | browse focus、selection、authority source、action target 可独立变化且显示正确 |
| Tuning source | inventory/loadout exact ref 分型；inventory 交换 preview target 仅接受 exact `{sourceKind:"inventory",containerId:"背包",slot,expectedLease}` 四键；stale/near-shaped source fail closed |
| Tuning capacity / quick mode | capacity 只接受当前 snapshot 的 `0..64` 整数且 installed≤capacity，无三槽业务上限；覆盖 `0`、missing、malformed/超限、真实剑圣腿甲 `1 个进阶 + 3 个插件` 与独立 4+ synthetic。顶部持久快捷 summary 与配件 operation 同源但职责分离，两处空槽只进入候选且零 preview/commit，focus key 按 surface 唯一；remove-all 是 slot group 外、同 rail 最右且与卡片同高的方形批量动作。single quick 仍过完整 authority gate，batch/cascade/remove-all 恰好一次人工确认；intent 即时反馈只投影 busy/phase，不改权威数量/snapshot/lease/token/槽位；commit 先采用 Host snapshot，仍锁到 fresh inventory refresh，exact source ref 一致时省略第二次 tuning read，否则 fresh reconcile |
| CTA | Tuning/Crafting 内容溢出时主 CTA 始终可见、可聚焦；Tuning 至少覆盖非 enhance 的长材料/差异场景 |
| Inspectability | blocked 实体可由键盘查看原因，但不发业务 intent |
| Owned inventory lock | ready/busy/refresh/returning 下 Pane、Tile、nested action 投影一致；允许态一次调用、阻断态零调用 |
| Tuning lock | idle/readPending/busy/reconcile/detaching/retry 下 tabs/tier/stepper/range/marks/cap exact disabled 集合；`projectLoadout` owner-scoped 保存/恢复 base/original `disabled` 与 `aria-disabled`，不误解锁原生/blocked 控件；confirmation wrapper 是有名称的 `role="group"`，generic section 不承载 `aria-disabled` |
| Focus/scroll | preview、snapshot、密度、tooltip 更新不破坏稳定 key、字段焦点和窗口滚动；number/range/mark/stepper/cap 强化 preview 保持发起焦点，只有候选/交换可迁移 CommitBar 语义焦点；候选再次 click/Space 清预览且零写，明确非 repeat Enter 才可提交 |
| Breadcrumb | 宽布局完整单行路径与祖先返回；窄布局只折叠中段，root/current/ancestor 有完整 accessible path，省略号 `aria-hidden` 且 title 列出被折叠中段，未折叠时无该 title；零第二行/横向滚动/native select |
| Typography | 错误/原因/帮助中文 ≥10px；主标签 ≥11px；无 8px 玩家文字 |
| State contrast | blocked reason 不继承低 opacity；disabled 与原因视觉分层 |
| Scrollbar | 真实 overflow fixture 无白色系统轨和箭头 |
| Motion | normal/reduced computed style；无布局动画、无野生新增时长、壳外 surface 受控 |
| Profile | 各生产 feature 显式 profile；1024×576 最小画布零横向溢出 |
| Browser input | Overlay 中 Ctrl+滚轮后 ZoomFactor、CSS viewport、命中坐标不变；普通 wheel 与 inspection wheel 正常；Bootstrap 与 Overlay 的玩家模式均无默认浏览器右键菜单、DevTools 或 accelerator；显式开发模式恢复未被 Host 保留的 accelerator、DevTools 与右键，F5 是 reload 正例，Ctrl+R 继续走 Host 合同 |
| Lifecycle | rebind/close/destroy 后 listener、timer、RAF、tooltip binding 不增长；Storage ↔ Build 导航 generation/destroyed fence 使迟到 prepare callback 零 activate/mount/history/focus。长期黑屏归因须有 live hit-test、`ProcessFailed`、heap/console 证据，不以“重启后恢复”单独断言内存泄漏或 renderer crash |

现役静态门固定为 touched-line / baseline ratchet：

- 触碰行禁止裸颜色、裸 duration 和裸 easing；
- 历史裸颜色、违规字号和野生 motion 计数只降不升；
- `DualPaneShell` 生产调用缺 profile 直接失败；
- feature 直接覆盖壳级 `grid-template-*` 默认失败，受控例外进入白名单并注明退出条件；
- 不能再用“token 已定义”代替“生产 selector 已消费并在 computed style 生效”。

### 11.4 人工体验旅程

自动门之外至少走完：

1. 从游戏打开 Character Build，在未点槽位时直接判断每类背包候选数量；
2. 用鼠标与纯键盘比较两个候选的 rich tooltip；
3. 直接调制背包候选，制造 stale/moved source，再安全恢复；
4. 在独立和 Character Build 嵌入态分别切换“逐次确认 / 单件快捷”，安装、替换、单拆、卸下全部时始终看得见下一步；
5. 连续 Esc 验证 info、tuning、storage、build 逐层退出；
6. 进入 Stats 与 Build，确认无假按钮、无重复返回；
7. 在 Character Build 用鼠标和纯键盘展开“整备”，逐项进入战备箱、装备调制、技能、材料、情报；确认装备当前项不关闭页面；
8. 在 write pending、reconcile、embedded tuning、目标打开失败和 competing intent 中验证原因、取消、一次回滚与焦点恢复；
9. 展开 Native/legacy 刘海，确认“游戏 / 整备”成员与顺序一致、锁定项同样可见并说明原因、技能只出现于整备；从刘海独立进入装备调制并切往战备箱，关闭回游戏，右侧装备仍一键直达；
10. 悬停右侧六入口、触发任务通知和 SafeExit，确认条件槽按 transaction/notice/hint 优先级互斥且没有空框或重复整备按钮；
11. 制造长材料/长差异，使 Tuning 与 Crafting detail 溢出，确认 CTA 固定；
12. 在 Loot、KShop inventory mode、Skills、NPC 中核对帮助和关闭上下文；
13. normal/reduced-motion 各走 SecondaryPage、整备 popover、reject、busy；
14. Ctrl+滚轮、普通滚轮、纸娃娃 inspection wheel、玩家右键与显式开发模式连续交叉验证；
15. 从 Character Build 分别进入材料与情报，使用显式“返回装备”回到 fresh 构筑；制造 forward echo 丢失/取消/迟到后 ordinary same-name open，确认 pending bind 已撤销且普通 `×` / Esc / backdrop 只回游戏；情报 tooltip/后台 catalog 在途时仍可返回，只有 authoritative state/bundle/snapshot/glossary_snapshot 在途时阻断并在 settle 后恢复；
16. 在宽/窄布局沿分类多级下钻并用祖先 crumb 返回，确认上层 title + breadcrumb 与下层当前层级选项职责分离；宽态选项不提前换行，真实窄态自然换行且无横滚，breadcrumb 只在自身不足时折叠中段；核对 root/current/ancestor 的完整 accessible path、省略号 `aria-hidden` 与被折叠中段 title，宽态不得残留该 title；
17. 对同一 Character 候选再次单击和按 Space 取消预览，确认零写；再以明确 Enter 走唯一 CTA；
18. 在 Tuning 分别核对 capacity=`0`、missing、malformed/超限、真实剑圣腿甲 `1 个进阶 + 3 个插件` 与独立 4+ synthetic，确认没有三槽业务硬编码；顶部持久快捷总览与 operation 详细区的进阶/已装/空槽均可 Tab/点击进入对应操作但零提前 preview/commit，空槽不承诺物理槽号，详细槽重渲焦点不跳回顶部，卸下全部位于 slot group 外；同时核对 source-port glyph/ARIA，并验证 single quick、cascade 与卸下全部恰好一次人工确认。

## 12. 风险、回退与非决策

### 12.1 主要风险

- 候选 tooltip 与候选调制会扩大 exact ref、lease 和迟到回包矩阵；
- header 投影若顺带重写 facade，容易把窄修复扩大成路由重构；
- CSS token 化若与 named layer 同时进行，视觉差异难以定位；
- Ctrl+滚轮若在 document feature 层拦截，会误伤正常滚动和 inspection；
- profile 迁移若只改 data attribute、不补真实 fixture，会制造“文档上完成”的假绿；
- disabled/inspectable 分离若没有 aria 与 intent 断言，可能重新出现点击静默无效；
- 整备菜单若先于 Materials/Intelligence handoff 发布，会出现“当前构筑已关闭、目标却未打开”的高破坏性假导航；
- Materials 若没有 Host-owned requestId 回显，或 pending wait 仍准入无 nonce ordinary open，迟到/旁路 `panel_request` 可能错误消费新的 post-close intent；若 arm 与 target-open 复用同一 timer，则 exact settled 会与刚建立的目标 wait 竞态；
- 独立 `EQUIPMENT_TUNING` 若复用 agent_control/test source 或缺少 exact admission，会把生产入口绑到自动化特权路径；
- B1 几何、B2 opener、B7 header/focus 若不受同一生产 gate 约束，会短暂暴露无真实目标或无焦点落点的半套 IA；
- 把局部 Storage/Tuning 与跨 owner Skills/Materials/Intelligence 塞进同一通用 router，会丢失 exact instance、pause 与 rollback 边界；
- 刘海新增一行若只改按钮数组，可能让 expanded bounds、hit rect、legacy fallback 和视觉宽度发生漂移；
- 右侧条件槽若同时承载整备按钮、任务通知和 SafeExit，会产生重叠 hitbox 与错误破坏性操作 owner。

### 12.2 回退策略

- Web-only 原子批按 feature 回退，不回退共享 authority 契约；
- 跨层三条纵切分别版本化，不共享一个巨大 payload switch；
- candidate tuning 写失败回到只读候选状态，不自动改穿戴或重放；
- E 必须按 Shell/profile module、全部生产映射、中央 profile CSS 与对应真实 fixture 整体回退；不得只撤 data attribute 或让单个 feature 悄悄恢复旧比例；
- CSS ratchet 允许按文件回退，不允许通过放宽全局阈值掩盖失败；
- Host zoom 变更若误伤 inspection，只回退输入策略，不在各页面临时打补丁；
- B1–B6 的底层实现可分别回退；B7 当前已切 production source，任一 HUD/header/focus 成员需要回退时必须整体关闭 `PreparationNavigationV1`，同时恢复旧 HUD、`storage | stats | skills | help | close` 与 `returnFocusAction:"skills"`，不留下半套入口；
- Materials/Intelligence handoff 的底层协议仍可分别回退；当前 B7 on 时若某目标结构性撤除则整体关总 gate，只有运行时暂时不可用才保留项目并显示 reason；
- 右侧条件槽改造失败时恢复纯提示/通知/SafeExit 现役路径，不把整备菜单临时移入该槽。

### 12.3 明确非决策

- 不新增 `HeaderToolbar` 通用 descriptor DSL；
- 不新增全局 `PreparationNavigationRegistry`、任意 panel intent 总线或跨所有面板复用的 header menu；
- 不让生产 `EQUIPMENT_TUNING` 复用 agent_control/test source，也不抽 Native opener registry；
- 不新增 hover-open、split button、最近目的地、动态主动作、多级菜单或“未来功能”disabled 预告项；
- 不把右侧条件槽改造成第二个刘海/第三套整备导航；
- 不新增 `LayoutProfileRegistry`；
- 不新增 `StagedCommandBar`；
- 不把 `AuthorityPreview` 变成领域参数容器；
- 不新增 `ScrollSurface` DOM/JS；
- 不新增 `RetrySyncAction`；
- 不在本轮建立 tour / onboarding 引擎；
- 不把 1332 处颜色一次性清空作为 P0 完成条件；
- 不把 z-index 全部机械替换成四个全局值；
- 不为了“统一”让 HelpAction、tooltip 或 modal 参与业务授权。

## 13. 完成定义

只有同时满足以下条件，本轮整治才可称为完成：

- F1–F9 的源码与自动证据全部闭环；F8 已完成本机 150% DPI 双宿主人工旅程，但 100% DPI 与真实触控/触控板 pinch 在取得相应物理设备前必须继续标记未验证；
- 新发现的 Esc、source marker、候选五态、Crafting 焦点、inspectable/actionable、KShop 上下文、Loot 引导/术语、CTA 滚动问题全部关闭；
- Stats / Build / Tuning 不存在可见无效控件或重复返回；
- 刘海“整备”行与 Character Build 菜单使用同一六项目标、顺序和命名，技能不再留在“游戏”行，右侧装备快捷入口仍一键直达；
- Native 四行与 legacy 游戏/整备两行在最低视口绘制/命中一致；progression 项跨 HUD/menu 同样可见、同样禁用并说明原因；
- 刘海 `EQUIPMENT_TUNING` 通过 exact production opener 直达 standalone tuning，可与 Battlebox 同实例互切并普通关闭到游戏；
- `PreparationNavigationV1` 关闭时生产 HUD/header/focus 保持旧 exact-set；打开时 Native/legacy 分组、Build header、Host focus 输出和 Web fixed mapping 原子切换，没有任何半套生产状态；
- Build header exact-set 为 `preparation-menu | stats | help | close`，菜单六项全部有真实结果或运行时可读阻断原因；当前装备项不会误触关闭；
- Build→Storage/Standalone Tuning 的同实例路径，以及 Skills/Materials/Intelligence 的 exact post-close handoff、arm→settled / target-open 两阶段 timeout、材料 exact wire/requestId 相关性、关闭前后分阶段失败结果、竞态取消、至多一次回滚和固定焦点全部闭环；
- Character-origin Materials/Intelligence 的 exact-instance 返回能力、严格五键 close、ordinary same-name open 撤销 `PendingInstanceBind`、Intelligence 窄 busy gate、普通关闭负例、child retire→fresh workbench nonce/new session 与 lifecycle 取消全部闭环；
- Tuning 单件快捷不越过任何 authority gate，cascade/remove-all 恰好一次确认；插件容量覆盖 `0..64`、missing、malformed/超限、真实 `1 个进阶 + 3 个插件` 与 4+ synthetic，顶部持久快捷槽总览、operation 详细槽、跨 surface 唯一焦点、等价空槽入口、slot group 外且同 rail 最右的方形批量动作、owner-scoped disabled/ARIA 恢复、强化/候选分型焦点、具名 group、source glyph、上下分轨且带完整 title/AT 的宽窄面包屑与候选再次 click/Space 取消均有真实行为门；
- inventory 交换 target exact 四键、presentation-only intent 反馈、Host snapshot-first + refresh-lock + exact-match 省略重复 read、owner-only 零 pane 重建，以及 Storage ↔ Build generation/destroyed 迟到 callback 负例均有专项自动门；没有 live 诊断证据时不把重启恢复黑屏外推为内存泄漏或 WebView renderer crash；
- 右侧条件槽落实 `transaction > notice > hint > hidden`，没有无 owner 空框、重复整备导航或 SafeExit hitbox 重叠；
- 所有主 CTA 在内容溢出时仍可见；
- HelpAction 覆盖适用面板，且帮助与 capability 的负向断言通过；
- Character Build workbench harness 从基线红灯恢复，所有受影响领域 harness 通过；
- profile 在生产 shell 中真实接通，并由领域 fixture 证明；
- 触碰模块字号/颜色/动效债务清零，存量 warning 不增加；
- normal/reduced-motion 与 named/unlayered cascade 的 computed-style 门通过；
- Ctrl+滚轮不再改变 WebView2 UI 尺寸且不误伤其他 wheel；
- K3 完整异构审计完成，每条 finding 有“接受 / 部分接受 / 拒绝 / 推迟”裁决及代码证据；
- 施工完成时同步 `agentsDoc/workbench-ui-system.md`（整备 IA / Header / owner）、`agentsDoc/as2-web-panel-migration.md`（三目标 handoff / material 与 tuning nonce）、`launcher/README.md`（Native/fallback Notch、右槽和固定入口）、`agentsDoc/testing-guide.md`（新 Host/AS2/HUD 门）；`node tools/validate-doc-governance.js` 与 `git diff --check` 通过；
- 若涉及 AS2 / Host，完成各自 canonical 测试合同要求，不以本 ADR 替代 fresh trace、xUnit 或真实 WebView2 证据。

## 14. K3 异构对抗合同与裁决

### 14.1 调用合同

近终稿必须在无 `.git` 的隔离只读证据包中，使用：

- Kimi Code CLI；
- 模型 `kimi-code/k3`；
- `--thinking`；
- `--plan`；
- `--max-steps-per-turn 100`；
- 不切换模型，不关闭 thinking，不因耗时长而截断；
- 仅审阅，不修改主工作区；
- 暂时性网络/进程失败只允许以相同模型、thinking、证据和 prompt 重试。

审计重点：

1. 是否仍有用户可见的静默 no-op、错误返回和隐藏 capability；
2. 本 ADR 是否过度设计、增加不必要组件或维护复杂度；
3. interaction / authority / navigation 边界是否足以施工；
4. 视觉、排版和 motion 约束是否能被自动验证；
5. 批次是否足够原子、能回退且不在红灯上施工；
6. 是否漏掉与反馈同根的 UX 缺陷。

### 14.2 审计状态

**已完成。**

- Kimi Code CLI `1.36.0`；
- session `25d4d482-1084-4e10-8e63-acac4e00586b`；
- 固定 `kimi-code/k3 --thinking --plan --max-steps-per-turn 100 --print --final-message-only`；
- 运行约 908 秒（2026-07-29 07:37:42–07:52:49，Asia/Shanghai）；
- 隔离证据目录位于系统临时区，向上无 `.git`；6687 个文件、约 237 MB；
- tracked evidence zip SHA-256：`5CA498B06C839870E123A4A9B5643E81A8386C173350124B8B391E8A5575BB5F`；
- 送审 ADR SHA-256：`548857BE54B97F3C6A11C825E9A60A3DB71EC7A470EB8C3DD0DE86EE183A6487`；
- 首次启动仅因 CLI 把 UTF-8 stdin 按错误代码页解码而在进入审阅前失败；随后以 UTF-8 进程环境、相同模型/thinking/plan/证据/prompt 原样重试，没有降级或截断；
- K3 未修改真实工作树，也未运行测试；它的代码结论由主线另行复核，原始 prompt、plan、stdout/stderr 不进入仓库。
- 完成裁决后，workspace/system temp evidence、prompt、stdout/stderr 与用户级临时 plan 已删除；仓库只保留本节的调用摘要和裁决。

K3 总裁决：**READY WITH REQUIRED CHANGES，0 Blocker / 3 High / 5 Medium / 4 Low**。三个 High 和所有会改变施工边界的 Medium 已在本 ADR 中收口，故本 ADR 转为 Accepted；这只表示决策文档可以指导施工，不表示任何代码问题已经修复。

### 14.3 裁决表

| K3 finding | 主线复核证据 | 裁决 | ADR / 施工影响 |
|------------|--------------|------|----------------|
| H1 G0 只补 Help hook 仍无法转绿 | harness exact-set、`return-build` hook 与 `display:flex` 泄漏均由主线复核 | **接受** | hidden CSS 修复前移到 G0，和 Help hook 一起构成恢复基线的必要条件 |
| H2 HeaderProjection 输入缺 tuning 存在性 | `TuningHeaderController` 只在 battlebox profile 创建 | **接受** | 增加窄 `tuningAvailable` boolean；不把 profile 对象、registry 或 DSL 交给 presenter |
| H3 WB112 行数预算零余量 | 最终 static audit 的同口径现值为 `inventory-workbench.js` 550/550、`character-build.js` 550/550、`skills.js` 1195/1200、`npcshop.js` 999/1000、`workbench.js` 999/1000；普通文本行数工具会少报末行差异 | **接受** | G0 记录预算；HeaderProjection 以搬移逻辑、facade 净减行为目标；禁止抬阈值或压行 |
| M1 嵌入式 tuning 没有 confirmation mode 入口 | `character-build-tuning.js` 无 mode UI，独立 battlebox 才有 header controller | **接受** | 独立/嵌入态共享 mode 与偏好，嵌入态放 PaneChrome，不挤全局 header |
| M2 裁剪包不能完整证明 inventory-source 跨层写入 | 主线在完整仓确认 `EquipmentTuningTask.cs` 与 `EquipmentTuningService.as` 的 inventory exact ref/lease 路径 | **部分接受** | 决策不降级；C3 仍保留完整仓 preflight，防止以后只凭 Web 推断 |
| M3 touched-line ratchet 尚不存在 | 现 audit 只拒绝 `<9px`、单一 crystal 字面量和 `transition:all` 等窄规则 | **接受** | F0 先实现一般裸色、9px 中文角色、裸 duration/easing 的 baseline/touched gate |
| M4 隔离包不能复跑基线、bytes 口径不明 | 主线在真实工作树已实际运行四个门；checker 输出与原始 CRLF 字节口径不同 | **部分接受** | §2.1 标明主工作树实跑与 checker 归一化口径；83/83 仅为当前输出，不冻结 case 数 |
| M5 `.42` opacity 应收窄到 Tuning | 主线核对 Character Build blocked 卡无该 opacity | **接受** | §4.5 拆清“tuning 压暗原因”与“character 静默 no-op” |
| L1 隔离包没有反馈截图 | 主线已人工查看原始图片，但临时图片不归档；CSS 缺口可独立证实 | **部分接受** | 改为代码证据为长期依据，图片只记为主线临时人工观察 |
| L2 `52:48 → 42:58` 只影响独立 tuning | selector 需要 `data-workbench-view="tuning"` | **接受** | §7.1 明确不误改 Character Build 55:45 |
| L3 enhance-compact 已解除 172px 上限 | CSS 例外由主线复核 | **接受** | CTA 验收明确覆盖非 enhance 长材料/差异场景 |
| L4 `bindAsync` 是别名，生产使用 `bindAsyncHover` | tooltip 生产调用命名由主线复核 | **部分接受** | ADR 使用现役 `bindAsyncHover`；本轮不为命名洁癖单独做全局改名 |
| 遗漏：Crafting 伪 tooltip hover-only / 9px | `crafting.css` 有 `data-title` hover 规则，无等价 focus-visible | **接受** | 并入 §4.4 / A4 |
| 遗漏：两个玩家 WebView2 都保留默认右键菜单 / DevTools | `WebOverlayForm.cs` 与启动期默认可见的 `BootstrapPanel.cs` 都明确把 context menu 和 DevTools 设为 true | **接受并补齐范围** | D 批冻结两个宿主的玩家浏览器行为；诊断只在同一个显式开发模式保留，并验证 Bootstrap、InputShield/CDP 均不受损 |
| 遗漏：NPC locked 商品键盘不可查看 | EntityTile disabled → `tabindex=-1` | **接受，已覆盖** | 作为 inspectable/actionable 分离的明确验收样本，不新增组件 |

### 14.4 主线审计后的补充发现

K3 不是第二个 source of truth。其终局形成前，主线的最后一轮全控件追扫又独立发现三项 K3 未报告的问题，并已写入 §4.10–§4.12：

1. KShop / NPC SecondaryPage 露出已经 inert、却仍保持 enabled 外观的底层 header action；
2. `OwnedInventoryPane.setDisabled()` 只锁父级，Inventory / NPC / Loot 子卡和 Loot 丢弃按钮仍呈现可执行外观；
3. Equipment Tuning 的 reconcile / detaching / retry 锁没有覆盖 tabs、已安装阶级和全部强化控件。

三项都由现有状态投影、EntityTile 和 SecondaryPage 合同修复；没有据此增加共享状态机或新框架。

### 14.5 整备修订的 K3 补充完整审计

F9、§4.13、§5.6、§7.3–§7.4 与整备施工/验收合同属于原送审 SHA 之后的物质性增补，因此不能借用 §14.2 的旧哈希声称已经覆盖。主线在近终稿再次完成一轮整份 ADR 的 K3 最高强度只读审计：

- Kimi Code CLI `1.36.0`；
- session `eb254e9f-288d-4383-9f41-e35e248976f5`；
- 固定 `kimi-code/k3 --thinking --plan --max-steps-per-turn 100 --print --final-message-only`，未切模型、未关闭 thinking、未因耗时截断；
- 运行约 876 秒（2026-07-29 09:33–09:48，Asia/Shanghai）；
- 隔离证据目录位于系统临时区，向上和包内均无 `.git`；26 个文件、1,259,727 bytes；
- 送审 ADR SHA-256：`3DF570EB76610A2044110E5AA71304050B023ECB62D6ADFF3F3F867FD0ADB298`；
- 审计覆盖整份 ADR，特别追问旧 Flash 心智、六项目标、local/cross-owner 分型、close intent、菜单键盘/生命周期、RightContext/SafeExit owner、Native/legacy Notch 几何、过度设计和内部冲突；
- K3 未修改真实工作树，也未运行测试；输出由主线逐项对代码复核，原始 plan/prompt/stdout 不进入仓库。

补充总裁决：**READY WITH REQUIRED CHANGES，0 Blocker / 3 High / 4 Medium / 4 Low**。所有 High/Medium 与会改变施工边界的 Low 已在本节裁决并回写正文，因此 ADR 保持 Accepted；该裁决在当时只代表施工合同可用。B7 导航现已完成源码与自动门，但仍不代表 candidate、游戏 E2E、promotion、部署或标准入口验收。

### 14.6 整备修订裁决表

| K3 finding | 主线复核 | 裁决 | 回写结果 |
|------------|----------|------|----------|
| H1 原 K3 哈希不覆盖导航增补 | 旧 SHA 的确早于 F9/§5.6/整备 B 批 | **接受** | 新增 §14.5 独立 session、候选 SHA、范围与裁决，禁止借旧审计冒充 |
| H2 回滚仍投影 `returnFocusAction:"skills"`，最终按钮已删除 | Host 当时只生产 `skills`，Web 也只识别该字面量；B7 现已闭环 | **接受并已实装** | §5.6.2 增加迁移期二值 allowlist；B4–B6 保持旧输出，B7 已与 header/Web mapping 原子改为 `preparation-menu`，显式 off 恢复 `skills`，禁止任意 selector |
| H3 legacy 刘海单行高度/active-region/600px cap 与新分组冲突 | `NotchOverlay` 绘制、hit-test 与宽度均有单行假设；Native 也有固定三行公式 | **接受** | §7.3 定义 Native 四行、legacy 最少游戏/整备两行及同源几何，B1 与测试矩阵点名高度/宽度/命中 |
| M1 抗 compact 仍写旧 HeaderProjection 合同 | §15 的确未含整备 keys/intent | **接受** | §15 更新 canonical tuple、两阶段 exact-set、local/cross-owner、右槽和审计事实 |
| M2 Skills 正向 arm 没有 arm→settled timeout | 现役 1.8s 只覆盖后续 Skill open；5s 只覆盖逆向返回 | **接受** | 三目标新增独立 `CharacterBuildPreparationNavigationTimeoutMs`；settled 原子换成 Skill/Material target wait，不复用 timer，阶段化回滚进入 §5.6/测试 |
| M3 战备箱在刘海隐藏、菜单却计划 disabled | `RequiresWarehouse` 当前直接影响可见性 | **接受** | §7.3 统一 progression 为结构可见 + disabled reason，Native/legacy/menu exact 对照 |
| M4 `_externalStatusSlotActive` 只占高度，不能仲裁 notice/SafeExit | `RightContextWidget` 仍可能绘制 notice，SafeExit 在同一矩形另画 | **接受** | §4.13/§7.4 新增单一 `RightContextSlotOwner` 仲裁与绘制/hitbox 互斥 |
| L1 §2.5 易把 Build exact-set 读成全 view set | Stats 另有 `back-build` | **接受** | §2.5 明确“Build view”，保留 Stats exact-set |
| L2 当前装备项与可见控件合同有表面冲突 | 它是 orientation status，不应 dispatch `EQUIP_UI` | **接受** | F9/§5.6 明确 `aria-current + aria-disabled + 当前`、可聚焦、零业务调用 |
| L3 稳定规则需回写 canonical doc | 现役 canonical 仍只冻结 Skills 与旧 HUD 七项 | **接受，施工触发** | §13 点名四份 canonical 落点；本 ADR 不把未实施规划伪写为当前真值 |
| L4 `CompositeBounds` tooltip reserve 与“空槽零几何”有张力 | reserve 是透明 hover/repaint union，不是可见槽或 action hitbox | **接受风险并澄清** | §7.4 明确透明 reserve 可保留，但不得绘制空框或创建 action hitbox |

### 14.7 补充审计后的主线新增发现

并行代码核对还发现两项 K3 摘要没有单独列出的施工空洞：

1. 刘海六项中“装备调制”在审计时尚无独立 production command/admission；§5.6.5 冻结的 `EQUIPMENT_TUNING`、Host opaque requestId、exact battlebox/tuning tuple、standalone 关闭与兄弟 view 文案已由 B2 实装，并由 B7 默认 on 的 toolbar 投影触达；
2. Materials 原先的 `openMaterialUI` 只证明命令发送，AS2 回来的 `panel_request` 没有与 post-close intent 相关的 requestId；该空洞已由 §5.6.2/B5 的 material-specific opaque nonce 原样回显、exact admission 与独立 wait 关闭。

两项都使用固定目标协议，不据此建立通用 opener、NavigationRegistry 或 return stack。

### 14.8 定向视觉 F / G5 异构复审

定向视觉 F 与 G5 收口后，主线再次使用本机 Kimi Code k3 最高 thinking 做隔离只读复审：

- session `90acd753-1f13-4df4-897d-5fd92ebd6806`；
- 固定 `kimi-code/k3 --thinking --plan --max-steps-per-turn 100 --print --final-message-only`，没有切换模型、降低思考强度或因耗时终止；
- 隔离证据根 `C:\Users\Administrator\AppData\Local\Temp\cf7-k3-fg-review-7e20e97c842c49a3a12bba222904bf38` 向上和包内均无 `.git`；
- stdout SHA-256 `45DAE97A382BA9329CEB5C3A679CA455C694AE56615D8603D1424A6A711A1753`；
- 六项条件 finding 经主线逐项复核与回归后全部关闭，最终裁决 **GO**：没有 overdesign、不可维护扩张、不合理 UI 交互或视觉美学阻断。

K3 另留三项非阻断观察。第一项已由主工作树补齐真实 production DOM 门：KShop full-density harness 在 1024×576 下对 `.inventory-owned-grid` 证明纵向真实溢出、零横向溢出、`scrollTop 0→60`，并读取 standard `scrollbar-width: thin` 与 WebKit 7px track/thumb/border/button/corner computed style；这只扩展既有 KShop runner，没有抽取共享 scrollbar abstraction。其余两项继续维持窄合同：9px 例外依赖 selector 命名，由 ratchet 精确守门；菜单 click 不主动 seed roving tabindex、locked 文案在菜单/header 各有一处重复，目前均有行为门且不足以引入新状态机或文案 registry。该轮只覆盖定向视觉 F / G5；§14.9 也只记录其后当时冻结树的历史复审，不能覆盖当前 `605f…` source-ahead 增量。

### 14.9 历史全局异构终审（已被后续源码覆盖）

当时收口继续使用同一 Kimi Code session `2c40f85c-a99b-4c94-b0c3-84d56f6d3f56`，命令固定为 `kimi-code/k3 --thinking --plan --max-steps-per-turn 100 --print --final-message-only`。全程没有切换模型、关闭 thinking 或因耗时终止；每轮只读隔离包向上与包内均无 `.git`，K3 未修改真实工作树、未重生成 dressup manifest，也未覆盖 Character 并行素材。

审阅采用“发现即窄修、同 session 复核”的连续链，而不是拿旧 GO 冒充新源码：

- 175 文件包 aggregate `4EE2C5B0…594DF3` 的 stdout `F34C3F17…E5A3C80` 给出 GO，并指出 Header title / Close accessible name 两项 Low；主线补齐生产音频 capture、基线 accessible name 与恢复门；
- 176 文件包 aggregate `F21B691D…D513F31` 的 stdout `F99E77E8…0A349A15` 给出 GO，并列出 profile 静态门防御深度、Close reason 与送审子集边界五项 Low；主线逐项关闭；
- 176 文件包 aggregate `F2F7BB29…95220843` 的 stdout `1C275717…1FAD0CC` 再次 GO；唯一可行动 Low 是多行解构漏检，另一个同名 helper 观察被裁决为刻意 fail-closed、无需为低概率命名冲突引入作用域解析器；
- 最终冻结集合位于 `C:\Users\Administrator\AppData\Local\Temp\cf7-k3-final-review-multiline-9a56c3cdd99b492ab18e48f178ac1cde`，排除运行后 `k3.multiline.*` 日志后为 **176 文件 / 12,140,990 bytes**，按排序后的 `relativePath + NUL + lowercase(fileSha256) + LF` 聚合为 `3C9C734C940CDF34566BB003D084E43B88394293A565E39257C18B12AB64A5C1`；最终 stdout SHA-256 为 `5AC26B9C8BA648911BF0E4D1FE2E57C6A9E380FA07306CCC3E98F88BC71899FF`。

当时裁决：**GO，0 Blocker / 0 High / 0 Medium / 0 新 Low**。K3 亲验单/多行 object/array 解构全部 fail closed、正常成员写不误伤，并确认已登记的同名 helper 保守拒绝符合避免过度设计与维护扩张的原则；该结论只绑定下述历史隔离包，不是当前 `605f…` source-ahead 加固树的最终审阅结果。

隔离包是 curated read-only review subset，不是可独立复跑所有仓库门的发布包。当前主工作树 `66/66` ratchet、default/release strict `0/0`、Character `867/867 + 12/12 + 18/18`、Dressup、Host 与 Flash 数字仍只由真实主工作树的 fresh runner 证明；子集未纳入的二进制 PNG 或完整依赖不能被反推为产品缺失。该历史审阅只证明当时冻结树没有已知审阅阻断；§14.10 也只记录后续 `a800…` 冻结切片的历史审阅，不能覆盖当前 `605f…` source-ahead 加固树。两轮结论都不改变 `candidate_built / NOT_DEPLOYED`、100% DPI/真实 pinch、B7 游戏旅程、promotion 与标准入口仍缺各自证据的事实。

### 14.10 历史 `a800…` 加固切片异构终审（已被后续源码覆盖）

当时 `a800…` 施工与 clean closure 回归完成后，主线复用 Kimi Code session `be410a29-5827-46a9-b1d6-7860c33a9d1f` 做第三轮只读终审。命令固定为 `kimi-code/k3 --thinking --plan --max-steps-per-turn 100 --print --final-message-only`，UTF-8 环境正常；约 21 分钟内没有切换模型、降低 thinking 或因耗时终止。K3 明确排除两份私有未跟踪文档，未修改、暂存、还原或生成任何仓库文件。最终 stdout SHA-256 为 `A78D5C8EF19C51A397C8EE449C464CCBB8A6778E78A9D7F124404F8BC7C104C5`，完整计划报告 SHA-256 为 `2EBDD4229FF7E18A156E945D64D17B2088BEE2A27A65EE8094286C0E36771BAF`。

终审前使用 `git stash create` 生成不改变主工作区或索引的临时闭包 commit `3f15a6a77d09bc56044c4b699b952ffe74a534e2`，在 detached clean worktree 的 CRLF checkout 上验证。首轮由该隔离树发现 `tools/test-equipment-tuning-model.js` 的函数提取只匹配 LF，修为 `\r?\n` 后重新生成闭包；第二轮取得 Launcher `1892 pass + 3 explicit opt-in skip / 1895`、0 fail、同步进程 exit 0，Tuning model `55/55`、ratchet `66/66`、Focus/Fake DOM `15/15`、profile leaf 通过、Intelligence Edge `30/30`、doc governance 通过、default/release-tree strict `0 error / 0 warning`，且 tracked status 为 0 项。Playwright 只通过 junction 复用主工作区已安装、按政策忽略的 `launcher/perf/node_modules`，不属于源码闭包；临时 worktree 与 junction 验证后均已移除，主工作区依赖仍在。

当时裁决：**GO — 0 Blocker / 0 High / 0 Medium**，并明确写出“FINAL GO：可以结束本轮源码与文档施工”。M1–M5、最初三个外部意见和跨域终扫八项均在该切片关闭：profile ratchet 已从峰值 416 行缩到 220 行并把动态真实性交回既有 Fake DOM 门；AS2 malformed capacity、Intelligence 异步返回屏障、历史计数、38 个新增文件闭包均复核通过；production profile、terminal unlayered `[hidden]` 与 WebView2 双宿主均一致。K3 另记五项非阻断 Low：confirmation disabledReason 冗余兜底、Web 4 秒与 Host 5 秒复位窗口、ratchet helper 复用造成的静态/运行计数阅读差异、已接受的局部开发环境变量 override，以及明示的 `NOT_DEPLOYED` 状态。它们没有能力泄漏、权威绕过或该历史切片阻断；该 GO 不覆盖 §14.11 之后的源码。

### 14.11 槽位与层级筛选增量异构复审（历史切片）

用户进一步明确“剑圣腿甲”为 `1 个进阶槽 + 3 个插件槽`，并要求顶部常驻槽位与 operation 详细槽都能从空槽直接进入配装；层级筛选则固定为上层 title/breadcrumb、下层当前层级选项。主线在完成字段、焦点、ARIA、等价空槽和宽窄布局修正后，复用 Kimi Code session `65f624b8-0dbc-4265-b12b-12272a18195c`，以 `kimi-code/k3 --thinking --max-steps-per-turn 200` 对当前工作树做只读复审；没有降低 thinking、切换模型、因耗时终止或修改仓库文件。

K3 亲自复跑 Tuning 三视口 `115/115`、model `61/61`、item-grid matrix `20/20` 与 KShop `135/135`，当时裁决 **FINAL GO，P0/P1/P2 均为 0**。它确认顶部进阶/已装/空槽全部可点击比“空槽惰性”更符合 AS2 常驻槽位和减少点击的目标，同时不绕过 preview/token/lease/revision/write/reconcile；summary/operation focus key 已按 surface + 序号唯一，空槽不伪装成可提交的物理槽号，宽态层级选项不提前换行、窄态自然换行且无横向滚动。其三项 P3 只记录未来真实容量接近两位数时再评估 roving tabindex、理论上不支持 tier 的 source 渲染门，以及“空槽聚焦首个候选”这一已明示的等价入口取舍；当前真实三插件 schema 下不据此追加抽象。该切片早于 H6 的写入生命周期与导航销毁竞态收口，不是当前 `605f…` source-ahead 树的最终异构签字。

### 14.12 `605f… → c6ca…` source-ahead 最终异构终审

本轮使用本机 Kimi Code CLI `1.36.0`、固定模型 `kimi-code/k3`、`--thinking --plan --max-steps-per-turn 200 --print --final-message-only`，session 为 `86b601cb-7c14-4e09-b93b-29abfa0b8c25`。审阅明确排除两份私有未跟踪文档、并行 Character Build 范围与 manifest/report 生成；没有因约 18 分钟首轮耗时关闭 thinking、切换模型或降低步数。续审一次在最终输出阶段被本机 GBK 无法编码特殊箭头中止，只修正 UTF-8 输出环境后用同一 session/模型/参数继续，不用低配结果替代终审。

首轮裁决 **NOT READY，P0=0 / P1=1**：K3 指出 `storageChanged()` 在 live pending 期间接到不同合法 controller view 时会提交实际 view，却保留旧 `_pending`，使后续 request/close 持续被拒。主线以独立 Node 复现证明该控制流成立，再做窄修：合法分叉上报先 `_supersede` 旧事务、清 timeout、推进 generation，旧目标为 Build 时 suspend；随后才沿既有封闭三视图 plan/commit/reset 接纳 controller 实际状态。另加 30 秒 orphan watchdog，只 discard/abort/恢复操作，不提交、不宣称成功；finish/abort/supersede/destroy 全路径清 timer，迟到 prepare callback 继续由 generation 拒绝。两条新 unit 分别覆盖 divergent report 与 orphan timeout，inventory modules 从 `29/29` 增至 `31/31`。canonical/ADR 同步这一路径，同时继续明确：它关闭的是可证竞态/活锁，不证明原长会话黑屏源于内存泄漏、renderer crash 或透明 overlay。

同 session 最终裁决 **GO，P0/P1/P2 阻断项 0**。K3 逐行复核 watchdog/supersede 的同步上报、timer 插入、same-view、null pending、resource/focus 与 late-controller 交错，确认旧 P1 已关闭；并撤销“面包屑零断言”（生产 `kshop-ui8gcv` 已覆盖宽屏同层 top/无横溢出）、“三槽 rail 遗漏”（当前真实合同为独立进阶 marker + 三插件 marker，动态 4+ capacity 属调制 surface）、“display name 比对”（实际为权威 canonical `itemName`）和失准文档锚点意见。唯一保留的是 P3 维护债务：`equipment-tuning-view.js` 的 inventory/loadout commit settle 路径仍有局部重复，后续可在独立批次提取；本轮不为非阻断重复做高风险重构。

K3 亲自只读运行 inventory modules `31/31`、item-filter `22/22`、focus integration `15/15`、tuning model `61/61`、interaction `7/7`、confirmation `4/4`、source marker `2/2` 与 profile leaf。主线另在最终修复后 fresh 运行真实 Edge Character workbench `867/867 + 12/12 + 18/18` 与 KShop `135/135`，并重跑 strict UI audit `0 error / 0 warning`、CSS bundle、文档治理及差异门；浏览器结果不是 K3 亲跑，不能混写。该阶段性 GO 只签字当时 `605f…` + source-ahead 源码和文档，没有构建 candidate、执行游戏 E2E、promotion、标准入口或部署，也没有重生成 dressup manifest/report。

同步 `origin/main` 到 `c6ca84714d134310793defda893772aac2c0d5ea` 并融合 4 个重叠文件后，主线仍用同一 session、同一 `kimi-code/k3`、`--thinking --plan --max-steps-per-turn 200` 做第二次只读终审；持续 827 秒，没有降级、切换模型或因耗时中止。K3 逐项核对 PlayerInfo schema v2 / `ExpectedAffinityMask` / 双形态失败报告、右侧条件槽新 workload、navigation supersede/watchdog、半乐观调制写生命周期、槽位/空槽/卸下全部、候选 toggle、Materials/Intelligence 返回、两层 breadcrumb、canonical 现行性、terminal unlayered hidden 与 WebView2 双宿主策略。

同步后最终裁决仍为 **GO，P0/P1/P2 均为 0，无交付阻断项**。K3 亲跑 inventory `31/31`、item-filter `22/22`、focus `15/15`、tuning model `61/61`、runtime `25/25`、interaction `7/7`、confirmation `4/4` 与 source marker `2/2`；C# `1902 + 3 / 1905`、真实 Edge Tuning `115/115`、Character `867/867 + 12/12 + 18/18`、KShop `135/135`、strict audit、CSS、BOM 与文档治理由主线 fresh runner 证明，不冒充 K3 亲跑。唯一保留 P3 仍是 `equipment-tuning-view.js` inventory/loadout commit settle 的局部重复，后续独立批次再提取；长会话黑屏根因仍未被本轮直接复现，本文只登记已修的 stale lifecycle/pending 路径，不虚构“内存泄漏已证明”。B0-05/B0-06 formal 与 B0-06 visual capture 保持 opt-in skip，上游 F2 `48/48 / b0_accepted` 仅属 exact base closure；当前新增 workload 后仍是 source-ahead / `NOT_DEPLOYED`。

## 15. 抗 compact 续作备忘

若会话在施工前或施工中 compact，按以下事实继续，不从零重做审阅：

1. 本 ADR 当前核对 HEAD 与 `origin/main` 均为 `c6ca84714d134310793defda893772aac2c0d5ea`，`c96f4c3d750561022b706c72a4d53050431e627d` 仅是起始审阅/F0 锚点；本文另逐项记录当前施工树已经取得 fresh 自动证据的稳定批次，但它们尚未部署；
2. 临时反馈和临时外部审阅不是 canonical，后续只依赖本 ADR、代码和 `agentsDoc/workbench-ui-system.md`；
3. G0/F0/定向视觉 F/A1/A2a/A2b/A3/A4、B1–B7、C1–C3、E、G1–G5、右侧条件槽与 D 自动策略矩阵已完成各自源码与自动门；D 已完成本机 150% DPI 双宿主 production/development smoke，100% DPI 与真实 pinch 仍缺物理设备证据；B7 真实 Native/legacy HUD 与游戏内 WebView2/标准入口旅程、剩余旧 CSS 与 G6+ 仍未完成。任何 case 数与绿灯只接受同一源码树的 fresh runner 输出，不继承本节旧快照；
4. G0 已用 terminal unlayered `WB-HIDDEN-001` utility 修复 computed hidden，并接通 `data-header-action="help"` 与 HeaderProjection；computed display / hitbox / Tab 和 Build exact-set 均已进入回归门；
5. “假按钮”根因是 `display:flex` 覆盖 `[hidden]`，不是漏绑 click；
6. 帮助并未解锁调制；真实问题是模式命名、preview 反馈和 CTA 被放进滚动层；
7. `InventoryWorkbenchHeaderProjection` 与封闭 `DualPaneShell({profile})` 均已落地；profile module 只持有枚举/validator/原子 setter，中央 `profiles.css` 独占壳级 tracks，生产缺失/非法 profile 与 feature 壳级覆盖由 `WB128/WB129` fail closed。HeaderProjection 当前接收固定 `equipment/battlebox/tuning/skills/materials/intelligence` availability，菜单 presenter 与 Host intent 都是 feature/协议窄扩展，不是 registry/DSL；
8. 整备 canonical 顺序固定为“装备 → 战备箱 → 装备调制 → 技能 → 材料 → 情报”；游戏行缩为“战队 / 平板 / 商城”，右侧装备仍一键直达；
9. G0 先证明原 Build exact-set `storage | stats | skills | help | close`；B7 已把 `PreparationNavigationV1` 的代码默认和随仓配置切为 on，并将两套 HUD、Build `preparation-menu | stats | help | close`、Host focus 输出和 Web fixed mapping 原子切换。显式 off 或非法配置值整体恢复旧呈现，中间不允许半套 IA；
10. Build→Battlebox/Tuning 是同 instance/session/pause 的 local view；Skills/Materials/Intelligence 是 exact post-close intent，二者不得进入同一通用 router；
11. `EQUIPMENT_TUNING` exact Host/AS2 contract 已实装，使用 Host opaque requestId 与 exact battlebox/tuning tuple，不复用 agent_control/test source；`PreparationNavigationV1` 当前默认 on，玩家 toolbar 已在源码中投影该固定入口，但尚待真实 GUI/标准入口验收；
12. 三目标 handoff 的 arm→settled timer 与 Skill/Material target-open timer 严格分离；Materials exact wire 把 `view` 放在 `initData`，AS2 原样回显 requestId，pending wait 时无 nonce 必须拒绝，命令 send 成功不等于目标已打开；
13. 右侧条件槽已由 `NativeHudOverlay` 单点实现 `transactionDecision > actionableNotice > contextHint > hidden`，同一 owner 投影给 RightContext/SafeExit，绘制与命中互斥；不复制整备导航；
14. Native 目标为四个标签行，legacy 至少有可辨识的游戏/整备两行；两者的绘制、active region、hit rect 与宽高从同一几何推导；
15. 优先复用 HelpAction、ChoiceGroup、QuantityControl、PanelTooltip、CommitBar；
16. Character Build 三条跨层能力纵切保持拆分：C1 counts、C2 tooltip 与 C3 candidate tuning 均已完成各自源码及 fresh 自动门，任一纵切证据不得代替另两条；
17. Host Ctrl+滚轮、默认右键菜单和玩家 browser accelerator 是根层输入不变量，不做每页 wheel guard；developer mode 只恢复未被 Host 保留的 accelerator，`Ctrl+R` 仍属 Host 合同，浏览器 reload 正例使用 `F5`；
18. 视觉 ratchet 与业务 P0 分批；F0 已冻结 `c96f4c3d750561022b706c72a4d53050431e627d` 债务基线和 touched-line 门，后续按 F/G 核销存量，skin 先禁止结构越权，不一次性重写；
19. G4 已清除工作台 `.001s/.001ms`、收敛 shell root 与 exact Tooltip owner，并以 actual normal/reduced 门证明 Character/Inspector/Tuning 静态终态；G5 已删除历史 focus outline 重置、退出 unlayered bridge，并用 `WB135` 冻结 layered baseline 与 Character slot 唯一例外；非工作台 `task_panel.css` 的 `0.001s` 有意范围外，structural bypass 与 named-layer 扩围归 G6+；
20. WB112 当前几乎零余量；新 presenter 必须搬走 facade 逻辑，禁止抬阈值或压行伪造余量；
21. 原 ADR K3 完整审计、整备修订补充审计、定向 F/G5、§14.9、历史 `a800…` 与槽位/层级切片均为历史切片；当前 `605f…` source-ahead 加固树已按 §14.12 用 k3 最高 thinking 完成“首轮发现 P1 -> 窄修 -> 同 session GO”最终异构复审，P0/P1/P2 阻断项为 0；
22. H1–H6 已在当前 source-ahead 树接通 exact child return、快捷确认边界、插件槽/source glyph、宽窄面包屑、候选取消、presentation-only 写入反馈，以及导航 destroy/divergent-report/orphan-timeout 三类生命周期收口；只按同树 fresh 自动门认领，不得外推为部署；
23. 本文已记录分批施工状态，但不是部署或正式发布结果；当前 D-only candidate 已完成 150% DPI 双宿主输入 smoke，严格仍为 `candidate_built / NOT_DEPLOYED`，不得冒充当前业务树 `candidate_executed` 或游戏 E2E。100% DPI/真实 pinch、B7 游戏旅程、promotion 与标准入口均必须引用各自真实同源码树证据；最新合并树已经取得 §2.1 所列三轮 fresh focused TestLoader 与精确 `asLoader.xfl` publish-only 证据，但这些仍只是源码行为门和注入层产物，不能与历史 D candidate 拼成 E2E 或部署结论。
