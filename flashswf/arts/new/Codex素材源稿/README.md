# Codex 素材库

唯一编辑源为旁边 `Codex专用素材/Codex专用素材.xfl` 及其完整目录；CS6 直接发布 `Codex专用素材.swf` 供运行时使用。迁移完成后已移除同名 FLA，避免维护两份源。原稿放在本目录，避免 CS6 另存 XFL 时清理目标目录。装配和发布入口见 [美术资产装配](../../../../agentsDoc/art-asset-assembly.md)。

QJZ171 来自用户提供的 `QJZ171-CS6-Handoff.zip`。保留原 SVG、分件布局、枢轴和美术说明供回溯；这些交接说明是来源资料，不替代项目规则。402 个原生形状、14 个分件和总装均保留可编辑性。首轮装配以源图握持位置为注册点，翻转至枪口朝右，源图到游戏素材的总倍率为 0.24，完整枪长约 435 个素材局部像素。该倍率是试装选择，未声称按原型实测尺寸标定；记录见 `QJZ171/assembly.json`。

导出链接：

- 素材锚点：`Codex专用素材`
- 纸娃娃：`枪-长枪-Codex-QJZ171`，带 `枪口位置` / `动画` 实例
- 图标：`图标-Codex-QJZ171`，第 1 帧为 24×24 遮罩内的机匣特写及底影，第 2 帧为横向完整展示，供地面掉落使用。

图标结构复用既有 XM214 的遮罩 / 被遮罩 / 底影及第二帧分层。掉落帧以常规男模 `空手站立` 的背负长枪 holder 纵轴倍率约 0.274917 为标尺：总装倍率为 `0.48 × 0.274917 ≈ 0.13196`，完整长度约 119.36 个世界局部像素；不再使用旧的约 61 像素 UI 缩略尺寸。取景矩阵、遮罩和度量来源记录在 `QJZ171/assembly.json`。人物外层身高缩放及实际观感仍由游戏内回归核对。

纸娃娃、图标特写和掉落体量已由用户验收。2026-09-05 建立独立 `QJZ171` 机枪物品，使用上述两个链接，配置在 `data/items/武器_长枪_机枪.xml`，从 Pig 金币商店购买；数值及 evidence 见 [标定案例](../../../../docs/QJZ171-独立物品与Flash资产流程标定-2026-09-05.md)。

借用测试已结束：`XM214-CageFrame` 的 `icon` 恢复为 `XM214-CageFrame`，`dressup` 恢复为 `枪-长枪-XM214-CageFrame`。存档始终没有修改，原装备、数值、配件及生命周期维持原值；材料字典与 Web dressup 清单随独立物品重新派生。2026-09-05 用户已确认独立物品有效，并授权合并上游后推送。
