# 暂存物资并入共享收纳工作台：调研与施工方案

**状态：HUMAN_ACCEPTANCE_PASSED / promoted。** 用户确认第二轮注释修正后，已同步上游，完成最终源双故障域构建共识、40/40 production policy 与原子部署。本次施工记录见第 10 节；第 1–9 节保留前期调研语境，§10.4–10.8 为历次候选与发布修正记录，最终身份、正式入口证据与清理见 §10.9。

**施工基线：干净 commit `c1e51dd1e8dba6bf72452cfef3af8b6f09b36acf`（2026-09-12）。** 已重新核对相邻施工落地后的共享物品视图、注释、Host 与 AS2 接点。前期调查基线为 `e66c9f73859a1779f4c1a16c6e66d4e2b6727981` 及当时工作区增量，不作为本轮精确源身份。

现役暂存功能与存档事实以[统一奖励暂存 ADR](统一奖励暂存与非阻塞领取-ADR-2026-09-11.md)为准；共享界面规范以[双栏工作台 UI 系统](../agentsDoc/workbench-ui-system.md)为准；本文负责本次收敛的决策、差异和施工顺序。

## 1. 已对齐的决定

用户希望降低玩家学习成本与多套实现的维护成本，明确倾向直接对齐战备箱，并接受在共享层补足体验的方向。下一轮交付按以下目标执行：

1. 暂存物资接入现有 `InventoryStorageWorkbench`，与背包—战备箱、背包—仓库共用同一收纳工作台。左侧仍是背包，右侧为当前可访问的收纳来源。
2. **UI、CSS、格子、工具栏、分页、密度、详情展示、键盘焦点、拖拽与批量控制器都以战备箱的现役实现为基准。** 不复制一份再改颜色、间距或按钮，也不为暂存设计独立的新版视觉。
3. 暂存仅取出；不能存入、交换回填、丢弃暂存条目，不能直接供制作、交任务或战斗使用。限制由能力与领域校验共同表达。
4. 数量选择、批量结果、容量反馈等通用改进进入共享层，战备箱也使用；暂存的持久化、来源身份与领取路由由业务适配承担。
5. 先完成共用与旧实现退役。额外视觉打磨、跨页多选、全文搜索、复杂排序、极端库存完整保存优化可分别推进，不能成为首次收敛的前置大工程。

**判断是否复用的标准是依赖同一份生产代码，而不是截图相似或引用了同一个物品格函数。** 后续改一次共享按钮、密度或交互反馈，各来源都应获得改进。

## 2. 当前体验差的原因与覆盖范围

现役暂存确实复用了 `OwnedInventoryViewShell`、`ItemGrid`、物品格、分页等基础资产，但外层布局、详情区、数量输入、选择集合、批量执行和反馈循环仍自行实现。历史 ADR 中“复用战备箱”描述的是这些已复用部分，不代表整个收纳工作台与控制器已经共用。

| 已知问题 | 当前证据与玩家影响 | 正确迁移后的覆盖 |
| --- | --- | --- |
| 布局与视觉另起一套 | `character-build-stash-view.js` 自建工具栏、网格与右侧详情；`character-build.css` 保留独立 `.character-build-stash` 布局。占用同一工作台却形成另一种操作空间 | 同一工作台和 CSS 可直接消除这类分叉 |
| 看不到接收背包 | 暂存页面右侧显示详情，没有战备箱页面的可操作背包；玩家不能边看空位、边整理、边领取 | 接入真实双容器视图可直接解决可见性；容量判断仍须权威回执 |
| 相同物品采用不同操作 | 暂存仅绑定点击与 Enter/Space，未接收纳页的拖拽与详情交互绑定；`exportOffer()` 返回空、目标拒收。已有 Ctrl 整项取出和数量按钮，但不是同一控制链 | 共享交互可覆盖机械操作；定点投放、数量与单向限制要补领域能力 |
| 筛选、翻页、选择与焦点不连贯 | 暂存每次 `render()` 清选择，固定 32 项 offset 分页，无全局类别查询；方向键按固定六列计算，完整密度下实际网格可为五列 | 共用工具栏、真实列数和焦点管理可修一部分；刷新保留策略及全局查询仍需实现 |
| 批量结果不透明 | 当前视图未逐项呈现 `accepted`，只记录 `blocked`；随后刷新用总数覆盖状态。全部取出会随扫描切页，玩家难以区分执行、跳过与完成 | 共享结果区、稳定浏览位置和操作反馈需要一起补齐 |
| 数量与容量反馈不足 | 超过最大值时按钮仍可点击，处理器直接返回；背包空间不是所有物品的真实目的地，单靠“空几格”不能说明为何可领或受阻 | 共用数量控件可覆盖输入校验；可领量、去向与失败原因仍由领域返回 |

按上述六类归纳，约两类可随正确共用直接覆盖，四类需要共享控件和领域适配配套完成；这不是已完成比例，也不能换算成可靠的体验提升百分比。

另有独立性能问题：原暂存 ADR 已记录 4096 件极端库存完整保存仍有秒级停顿。共用 UI 可以减少布局与操作负担，不能据此承诺消除 AS2 候选复制和完整 SOL 保存成本。

本次证据包括生产源码和沿用现役组件的隔离浏览器夹具。夹具观察到翻页后选择清空、受阻提示被刷新覆盖、越界数量无反馈、完整密度方向键跨行错位；夹具使用模拟领取回执与占位图标，不能证明真实 WebView2 输入、AS2 写入、当前美术效果或保存重启旅程。实际页面高度也受 Character Build 父布局影响，不能从独立夹具外推生产占高。

## 3. 共用边界：一份工作台，两个领域适配

以下是目标职责划分，不是已存在的新 API：

```text
InventoryWorkbench（面板、导航、密度、会话与关闭协调）
  └─ InventoryStorageWorkbench（同一双栏 UI 与机械交互）
       ├─ 普通库存适配：背包、战备箱、仓库
       │    └─ InventoryCoordinator → InventoryTask → InventoryPanelService
       └─ 暂存来源适配：稳定条目、查询、取出与回执
            └─ 复用 ItemUse 领取链 → RewardStashService → RewardStashStore
```

不要为暂存注册新 Panel、创建第二个 Shell 或建立独立缩放方案。现有 `1024×576` 逻辑画布、`PanelScale`、头部和 density 状态继续由工作台拥有。原暂存入口应导航到同一工作台内的收纳视图；返回恢复原子页上下文。

如需切换收纳来源，使用现有标题或导航插槽与共享控件，仅列出当前入口已授权的来源。接入暂存不扩大仓库或战备箱的场景访问权限，不把暂存入口变成任意地点打开仓库的捷径。

| 部分 | 应共用的现役实现 | 暂存适配负责的差异 |
| --- | --- | --- |
| 页面骨架 | `InventoryWorkbench`、`InventoryStorageWorkbench`、现有 transfer-pair 样式 | 标题、已授权来源、单向能力、条目总数 |
| 格子与详情 | `InventoryWorkbenchOwnedView`、`OwnedInventoryViewShell`、现役物品详情绑定 | 暂存条目到共享 ViewModel 的投影与详情请求 |
| 选择与执行 | `QuickTransferController`、`InteractionBroker`、`PointerDragController` | 稳定来源引用、允许的动作、取出命令与结果 |
| 数量与反馈 | `WorkbenchComponents.QuantityControl`、共享命令栏与状态区 | 权威数量上限、合法去向、受阻原因 |
| 查询与分页 | 同一类别工具栏、pager 与焦点恢复 | 暂存全局查询和有界页面，不伪造普通库存槽位 |
| 写入与恢复 | 工作台已有写占用、关闭协调和刷新机制 | ItemUse 暂存事务、原操作查询、提交状态与旧版恢复 |

只抽出确有两个消费者需要的接口；优先在现有 ports 中增加来源引用、能力和结果适配，不先造一套通用插件注册器或第二套事务框架。

## 4. 施工前必须解决的实际差异

### 4.1 来源身份不是同一种槽位

普通库存用 `containerId + physicalSlot + expectedLease`；暂存用 `storeId + commitRevision + entryId/revision`，分页位置随删除、合并而变化。现有 `QuickTransferController` 的 key 和访问函数依赖物理槽位，不能直接把第 N 条暂存记录当成第 N 个普通槽位。

共享选择、拖拽和异步详情应拿到适配器提供的稳定引用。暂存仍以条目身份提交；背包目标仍用当前物理槽位及 lease。异步查询、详情与写后刷新同时绑定面板代际、来源和查询版本，旧页面响应不得覆盖切源后的新页面。

### 4.2 会话生命周期不能靠搬 DOM 解决

当前 `Program.cs` 将 `CharacterBuildTask.IsExactSession` 传给 `ItemUseTask`；AS2 的 `CharacterBuildService.validateItemUseContext()` 同样检查活动会话、generation 与非 stale 状态。暂存 transport 依附 `CharacterBuildItemUse.Controller`，在角色构筑获取会话后绑定。

切到收纳页时，导航会调用 `CharacterBuildController.suspend()`，其中关闭前端 ItemUse 控制器。与此同时，`CharacterBuildSession.suspendView()` 只取消视图读取，**并不等于 Host / AS2 会话被 finalize 或关闭**。因此实际障碍是前端领取通道的所有权和生命周期交接，而非“切页必定销毁全部后端授权”。

建议先复用仍有效的会话机制，把需要跨子页存活的领取通道交由工作台协调，视图挂载与通道关闭分开。必须同时验证从角色构筑进入、直接从收纳入口进入、返回以及关窗；直接入口可能尚未建立 Character Build generation。

优先验证能否以不挂载人物渲染器的方式复用现有会话服务。若必须改变会话归属，设计最小的工作台会话接入，并保持 Host 与 AS2 精确身份校验；不要保活整个隐藏构筑页面，也不要删去 generation 校验。具体接口由首个跨栈纵切确定，不在调研文档中伪定一套新 wire。

### 4.3 背包与暂存同时可操作后，要协调冲突写

现有 `InventoryCoordinator.beginExternalWrite()/completeExternalWrite()` 已能占用库存写入并在完成后刷新。暂存领取使用独立 ItemUse 写状态。接入双栏后，领取、背包整理、拖拽、丢弃和切源必须纳入工作台的同一次操作协调，不能出现领取未定而另一个控制器已经解锁的状态。

可复用现有 external-write 接口，但需证明提交结果、暂存页与受影响库存快照的采用顺序；未知结果先核对原操作，期间保留冲突写保护。已确认的“没有可领项”可回到可操作状态，未知写不能伪装为失败并换 operationId 重发。

这是新增双栏并存带来的接入要求。本次没有据此断言现役系统存在跨域并发漏洞；Host 与 AS2 的共同写准入需在纵切中连同调用入口核对。

### 4.4 数量与目标位置需要真实领域支持

现役暂存 `stashTake` 支持条目数量，但不接收目标格；现役普通库存 move/merge/swap 与自动转移操作按整项处理。共享 `QuantityControl` 已存在，但尚未接入这条普通库存转移链。不能把“有数字输入控件”写成“已有部分转移能力”。

目标交互沿用战备箱：Ctrl 点击与批量操作自动安置；对能够进背包的物品，拖到明确格位时尊重该格位。装备保留完整实例；暂存不允许以 swap 把背包物品换回暂存。目标被占用、类型不兼容或 lease 过期时明确拒绝并保留可恢复选择，不能悄悄投到别处。

指定数量保留，并作为共享转移能力补齐。非堆叠装备只允许整实例；堆叠物品的最大可选量、部分可接受量与目标合并限制由领域校验。数量输入即时反馈整数、正数和上限，禁用不能提交的按钮并说明原因。

材料、情报等沿原权威路由进入对应持有区；部分消耗品还可能优先进入装备中的手雷槽或既有药剂槽。当前 `ItemUtil.require/acquire` 决定去向，不能把所有成功都写成“已放入背包”。这些物品不显示虚假的定点背包投放能力；自动领取结果说明真实去向。

### 4.5 两种批量结果不能硬套

当前普通库存 `autoTransferBatch` 最多 50 项，按选择顺序提交可成功前缀，在第一个无法安置的条目处停止；现有控制器校验 `completedCount` 和 `failure.index` 的前缀关系。暂存 `stashTake` 最多 32 项，遇到受阻条目可继续尝试后续条目，返回逐项 `accepted/blocked`。

例如背包满时，前面的装备可能受阻，后面的材料仍可领取。把这次结果压成“成功前 N 项”会误报完成对象；把暂存改成第一项失败就停，也会退化现有领取能力。

共享 UI 区分执行所选与尽量取出，适配层保留各领域已经确认的结果语义。通用结果应能表达每个稳定来源的已取数量、剩余数量、去向及受阻原因；这是建议的内部结果模型，不要求直接改写现役全部协议。普通库存 v1 前缀契约先保持，若增加尽量转移能力，应另行明确命令意图和测试。

显式批量继续是一批一次有界事务，不能退回 N 次单项调用。每个来源保留自己的批次上限；全部取出跨批循环时只在确认上一批后继续，让出界面帧，停止只取消后续批次。处理游标与玩家正在浏览的页分离，避免整页跟着自动执行跳动。

完成反馈至少显示本次已取、未取及原因；保存或响应未知时展示核对状态。刷新不能用库存总数覆盖本次结果。受阻条目保留在暂存，不能把整体 `success:true` 当作全部已取。

### 4.6 查询、整理与选择状态要分清

暂存目前只有固定 32 项 offset 页面；全局类别筛选属于必补能力，不能只筛当前页导致玩家误以为其他页没有目标物品。分类口径应复用现役物品分类规则，由权威查询或受其约束的索引完成；列表和页码 DOM 继续有界，不把全部条目拉到 Web 来补筛选。

普通库存工具栏的 `sortAndMerge` 会实际整理物理槽位，并非只改变显示顺序。暂存来源不能把同一按钮误接成库存物理整理；不适用的能力不提供，若以后新增显示排序，则在共享工具栏声明该语义。全文搜索和复杂排序可留待共享升级。

首轮沿用战备箱的页内选择：翻页、切类别或切来源时明确退出批量模式并提示选择已清除。普通刷新仍按稳定身份恢复当前焦点与可保留选择；执行完成后按结果移除已完成项，对受阻项保留清晰反馈。跨页选择是后续共享能力，不为暂存私造一套。

网格方向键使用当前布局的实际列数，完整/紧凑只改呈现，不改变动作能力。空页回退、最后一项领取、重开和返回都需要确定的焦点落点。

## 5. 必须保留的业务边界

- 保留现役 `_saveExt.rewardInbox` v2 库存权威、稳定条目与已提交前像；本次 UI 收敛不要求新建第二份暂存库存或迁移现有 v2 存档。
- 页、详情、角标和容量说明保持纯读；打开空暂存不创建存档扩展，不触发 ACK、迁移或保存。
- 取出是内部移动，保持 `ownershipDelta:0` 等现役资产语义，不重复记获得或重复结算奖励。
- 暂存删除、接收库存、命令回执与保存结果仍属于同一 Reward 候选提交。定点、数量与批量扩展不得绕过这条链。
- 旧 v1 根事务、K 店 legacy 行和领取恢复保持原身份、证明和迁移顺序；共用界面不代表可以删除旧存档恢复能力。
- 写后同步受影响背包、暂存页、角标及其他实际接收区域。返回角色构筑需重新采用权威快照，但单纯浏览不得重建人物并清除瞬时 Buff。
- 极端规模保存性能单列。可测每批请求数、保存次数、快照数和耗时，不以“批量 UI 更快”代替存档成本证据。

## 6. 源码接手地图与退役清单

| 现役入口 | 接手重点 |
| --- | --- |
| [inventory-workbench.js](../launcher/web/modules/inventory-workbench.js)、[navigation](../launcher/web/modules/inventory-workbench-navigation.js)、[config](../launcher/web/modules/inventory-workbench-config.js) | 保持唯一面板与外壳；补来源导航上下文、会话归属和关闭协调；不能仅在 `warehouse/battlebox` 白名单中粗放加值 |
| [inventory-storage-workbench.js](../launcher/web/modules/inventory-storage-workbench.js)、[owned-view](../launcher/web/modules/inventory-workbench-owned-view.js)、[quick-transfer](../launcher/web/modules/inventory-workbench-quick-transfer.js) | 将物理槽引用、双向能力和前缀结果的硬编码改为所需适配点；保持一份渲染与交互实现 |
| [inventory-runtime.js](../launcher/web/modules/inventory-runtime.js)、[workbench-components.js](../launcher/web/modules/workbench-components.js) | 沿用库存协调与 QuantityControl；暂存命令不伪装为普通库存命令 |
| [character-build.js](../launcher/web/modules/character-build.js)、[session](../launcher/web/modules/character-build-session.js)、[item-use-channel](../launcher/web/modules/character-build/character-build-item-use-channel.js) | 入口改为工作台导航；拆开视图暂停和跨页领取通道生命周期 |
| [stash-transport](../launcher/web/modules/character-build/character-build-stash-transport.js)、[ItemUseTask.RewardStash.cs](../launcher/src/Tasks/ItemUseTask.RewardStash.cs)、[ItemUseService.as](../scripts/类定义/org/flashNight/arki/item/ItemUseService.as) | 保留版本化精确校验、查询恢复与提交语义；按已证明的会话接入调整边界 |
| [InventoryPanelService.as](../scripts/类定义/org/flashNight/arki/item/InventoryPanelService.as)、[RewardStashService.as](../scripts/类定义/org/flashNight/arki/item/RewardStashService.as) | 普通库存数量扩展、暂存全局查询与定点计划；各自权威写和结果适配 |
| [panels-lazy-registry.js](../launcher/web/modules/panels-lazy-registry.js)、[feature-loader](../launcher/web/modules/inventory-workbench-feature-loader.js) | 暂存目前随 Character Build feature 加载；收纳直接入口需要自己的最小依赖闭包，不能偶然依赖先开构筑页 |

最终退役对象：

1. 删除 `character-build-stash-view.js` 的独立模板、选择循环、数量提交和批量执行实现；原入口只保留必要的导航适配。若删除文件，清理所有 feature/lazy 与测试引用。
2. 删除 [character-build.css](../launcher/web/css/workbench/character-build.css) 中专属暂存布局规则，包括独立两区比例、工具栏、详情、数量和 footer 样式；共享能力所需样式只进入现役 workbench CSS。
3. 当前暂存 transport 的领域能力需要保留或迁入薄适配，不能随旧 UI 一并删掉恢复与持久化代码。迁移路径、协议及 lazy 变化发生时再同步 canonical 文档。
4. [test-reward-stash-view.js](../tools/test-reward-stash-view.js) 转为共享工作台的暂存场景测试，不能继续把旧视图快照当作新方案验收标准。

## 7. 推荐施工顺序

每步完成必要验证后继续，不把步骤拆成必须反复请用户确认的流程。以下是后续实施建议，本轮没有执行。

| 顺序 | 工作与可交接结果 | 该步消除的不确定性 |
| --- | --- | --- |
| 1. 刷新基线并验证最小纵切 | 核对并行迁移；在同一收纳页读暂存，再完成一件普通装备的权威取出、背包刷新和返回；覆盖直接入口及构筑入口 | 会话、导航、写占用、lazy 依赖能否成立；先证明共用路线可运行 |
| 2. 收敛共享交互 | 抽出来源身份与能力接点，接上同一 owned view、详情、拖拽、QuickTransfer、QuantityControl；普通库存接入适用的数量能力 | 确实共用控制器，且战备箱/仓库行为得到保持或升级 |
| 3. 补齐暂存能力 | 全局类别查询、合法定点目标、数量、逐项结果、尽量取出与有界多批；保持原保存和恢复边界 | 解决“组件共用但业务语义缺一半”的问题 |
| 4. 替换入口并退役旧实现 | 原暂存入口导航到共享收纳；删除专属 UI/CSS/加载引用，迁移测试；与现役原生详情通道对齐 | 防止两套实现继续维护、隐藏入口回流或冷启动缺模块 |
| 5. 完成适用验证并报告边界 | 跑第 8 节对应检查；更新实现涉及的 canonical 文档；取得同候选的真实输入和保存读回证据 | 证明实际玩家旅程，而不只证明 DOM 与静态引用 |

可以在同一施工批次完成多步。首个纵切只作内部验证，不把缺少数量、结果或恢复的临时页面当作最终交付。若会话方案需要扩大到全工作台协议重构，应先比较更小的既有服务复用途径，再确认实质产品或范围变化。

## 8. 验证与交付要求

| 层级 | 必须证明的行为 | 现役可扩展入口 |
| --- | --- | --- |
| Web 结构与生命周期 | 一个 Shell/StorageWorkbench；三种来源同组件；冷启动可直达暂存；切源和迟到响应隔离；旧 UI/CSS 无生产引用 | [workbench modules](../tools/test-inventory-workbench-modules.js)、[lazy closure](../tools/test-inventory-workbench-lazy-closure.js) |
| 共享交互 | 完整/紧凑真实列数，鼠标与键盘可达，单向限制、目标格、数量边界、翻页退出选择、刷新恢复、批量停止与结果不被覆盖 | [components](../tools/test-workbench-components.js)、[focus integration](../tools/test-workbench-focus-integration.js)、迁移后的暂存浏览器场景 |
| Host 与 AS2 权威 | 精确会话与身份、过期 lease/revision 拒绝、单项与批次数量、非前缀 accepted/blocked、目标占用、回滚、未知结果只查询原操作、纯读零保存 | [ItemUseTaskTests](../launcher/tests/Tasks/ItemUseTaskTests.cs)、[InventoryTaskTests](../launcher/tests/Tasks/InventoryTaskTests.cs)、[RewardStashServiceTest](../scripts/类定义/org/flashNight/arki/item/RewardStashServiceTest.as)、[InventoryPanelServiceTest](../scripts/类定义/org/flashNight/arki/item/InventoryPanelServiceTest.as) |
| 恢复与规模 | 旧 v1 恢复、legacy 迁移、背包满但材料可领、最后一项/空页、受阻项不丢、32 项边界、多批停止；分别记录 RPC、快照、保存次数与延迟 | [暂存测试入口](../scripts/run-reward-stash-tests.ps1)、[规模测试](../scripts/类定义/org/flashNight/arki/item/RewardStashScaleTest.as)、[库存故障门](../tools/run-inventory-fault-gate.ps1) |
| 实际界面与持久化 | 同一候选中真实图标/详情、物理 WebView2 输入、写后保存和重启读回；密度与画布往返 | 按[验证矩阵](../agentsDoc/testing-guide.md)选择当前入口，记录实际运行身份 |

浏览器至少覆盖：空暂存、少量混合物品、超过一页、背包满与可合并目标、真实长名称/装备详情、完整与紧凑密度。规模夹具使用有界列表；纯占位图标截图不足以确认密度与可读性。

玩家体验验证集中为三条可完成的旅程：

1. 打开暂存，直接看到背包；查看同一件装备并拖到指定空格；返回再打开，位置、剩余量和焦点一致。
2. 背包接近满时，领取指定数量和一组混合物品；看懂哪些已领取、哪些留在暂存及原因；整理背包后继续，操作与战备箱一致。
3. 暂存、战备箱与构筑之间往返，包含一轮多批领取停止；正常保存退出后重启核对物品总量、装备实例与暂存余量。

真实异常恢复用隔离存档和既有故障门验证，不让测试员靠破坏正常存档来制造未知写。AS2 编译遵守 GUI CS6、UTF-8 BOM 与新鲜 trace 规则；业务注入默认核对 `asLoader`，测试使用 `TestLoader`，不因修改 `.as` 就默认编主 XFL。实际编译和 runtime 构建须等当前占用作业协调结束。

只有源码/夹具通过时就报告对应层级；不能称真实输入或保存重启通过。后续若授权正式发布，沿用[现役 runtime 发布流程](runtime-build-reproducibility.md)，本方案不建立新的发布门，也不代替专项体验证据。

## 9. 额度重置后的接手说明

先读本文第 1、4、6、7 节，再对照当前差异；无需重复讨论“直接对齐还是另做一套皮肤”。用户已选择直接共用，未要求先升级整套美术。

调查时并行的 NPC 菜单与原生注释迁移触及 `inventory-workbench-owned-view.js`、`character-build-stash-view.js`、`ItemUseTask.RewardStash.cs`、`InventoryTask.cs`、`InventoryPanelService.as`、tooltip 生产通道及相关测试。该任务现已完成收尾，记录见 [NPC 菜单与原生注释迁移](NPC菜单与原生注释迁移-ADR-2026-09-12.md)。本次暂存退役消费其现役共享详情实现，不恢复旧私有 Web tooltip 分支；后续接手仍须核对源码差异。

仍需施工验证的工程选择有三项：会话通道的最小归属调整、共享结果模型与现役两种批量协议的适配、全局查询及定点/数量扩展的版本化边界。这些可以通过代码与纵切解决，无须重新向用户索取已给出的产品偏好。

只有出现超出本文的实质产品取舍才需要再对齐，例如扩大关卡内仓库访问、允许物资回存暂存、默认跨页保留选择或改变原有批量规则。没有这些变化时，按已确认方向推进即可。

本次文档储备提交仅包含本调查文档和共享规范中的目标态链接，不包含生产实现、编译产物或候选运行证据，也不表示已发布功能。

## 10. 共享收纳施工记录

### 10.1 本轮授权与接口范围

开工时工作区干净，相邻迁移已收尾。按用户授权使用四路 SWE2 Max 分别实现 AS2、Host、Web 会话和共享 UI，主控统一接口、代码复核、验证与隔离候选。任何代理报告均不是双 signer / 双 faultDomain 发布证明。

本轮约定保持既有协议版本，以可选字段扩展已存在的操作；旧字段组合、旧回执与恢复请求继续有效，未知字段仍拒绝：

- `stashPage` v2 可选 `filterSpec` 沿用库存分类规范；响应采用全局命中数量、有界 32 条页面和同口径 facets。无筛选字段维持原响应形状。
- `stashTake` v2 可选顶层 `target:{containerId:'背包',slot,expectedLease}`，仅用于单项定点投放。无目标的旧请求保留原指纹；定点失败不能悄悄改投其他格。自动领取继续采用真实材料、情报、装备栏、药剂栏和背包路由。
- 库存 v1 只在 `move/merge/autoTransfer/autoTransferBatch` 的来源引用允许可选 `quantity`。目标引用、交换、丢弃不因此扩权；批次上限与前缀结果保持原约定。
- `warehouse/battlebox` 入口权限和 `build/storage/tuning` 视图枚举保持原范围。暂存是收纳视图内部来源；工作台协调已存在的 Character Build 会话与 ItemUse 通道，直接收纳入口使用无人物视图的会话接入。

以上是当前施工接口，不表示最终自动门或运行时闭环已经通过。实现与测试完成后在本节记录实际结果及修订。

### 10.2 基线与验收环境

开工基线已验证：暂存 fresh CS6 `82/82`（runId `b14ca6329af1473b81e5e85d91462af1`）、库存 Web runtime `87/87`、工作台模块 `37/37`、共享组件 `17/17`，以及 lazy 依赖闭包。工作台模块测试曾停在相邻迁移留下的旧 `loadView` 文本断言，已按现役 `loadFeature(descriptor(...))` 调整；此修正没有改变生产行为。隔离基线的 ItemUse/Inventory/owner Host 测试 `344/344`、0 失败。它们只证明开工基线，不作为新功能通过记录。

隔离工作树为 `C:/cf7-stash-0912/resources`，用于本地候选、独立 SWF 与验收存档。正式存档仅复制，已记录源 JSON 及正式部署文件哈希。隔离目录另有“暂存验收”和“满包验收”两个合成样本：各 42 条混合暂存，分别为普通背包与 50 格满背包，用于数量、跨页筛选、定点投放和非前缀领取验证。它们不是正式玩家存档，也不能代替真实玩法旅程。

首轮候选身份和实际载入证据见 §10.4，反馈与修正见 §10.5；首轮人验未通过，领取保存闭环仍待复验，不执行云端共识、promotion、正式部署或本轮实现推送。

### 10.3 定向回归与证据

- Web：`node tools/test-inventory-workbench-stash-source.js`、`node tools/test-inventory-workbench-storage-source.js`、`node tools/test-inventory-workbench-stash-navigation.js`、`node tools/test-inventory-workbench-quick-source.js`、`node tools/test-character-build-stash-authority.js`；配套现役 inventory runtime、workbench modules、Character Build ItemUse 与 lazy closure 回归。
- DOM：`node tools/test-reward-stash-view.js` 现改为真实共享 InventoryStorageWorkbench；旧独立暂存快照退役。构筑/收纳导航继续跑 `node tools/run-character-build-workbench-harness.js`。
- AS2：`scripts/run-reward-stash-tests.ps1` 与 `scripts/run-item-panel-tests.ps1 -Suite Shared`，必须核 fresh trace、Compiler Errors 与函数体门。业务逻辑候选编译 `asLoader`；不因本轮 `.as` 变化编译主 XFL。
- Host：`StashSharedContractTests` 及 ItemUse/Inventory/owner 定向测试。隔离候选还需实际 WebView2 输入与保存/重启回读；夹具、编译和本地候选均不代表人验、云端共识或正式部署。

当前已完成：暂存 AS2 114/114、普通库存 AS2 194/194（并行 EquipmentInventory 28/28），均有本轮 fresh trace 和 Compiler Errors 0/0；Host ItemUse/Inventory/owner 定向 408/408；共享来源适配 16 项、双端采用/恢复 8 项、会话归属 11 项、ItemUse 21 项、工作台模块 37 项及 lazy closure。普通库存新测试先发现三个失败，其中两处为新增样本前提错误，一处为合并溢出后未继续寻找空格的实现缺陷，均修正后重跑通过；暂存初次样本使用未注册保存原因，改用既有 `reward.map_stash` 后重跑通过。未放宽原有保存原因或回滚门。

补充通过：来源导航 7/7、共享批量/数量 17/17、库存 runtime 87/87、共享 primitives 12/12。共享收纳 DOM 场景已验证全局分类、选择本页、翻页清空选择、指定数量、明确目标格、实际 pointer 拖拽、Ctrl 自动领取、来源往返、非前缀批量、未知结果只核对原操作、关闭占用及 1024×576 边界。DOM 使用真实共享组件，Bridge/ItemUse 和图标为夹具；不能称真实 WebView2 写入或实际暂存图标人验通过。

隔离 `asLoader` 已由 CS6 发布，Compiler Errors 0 错误 / 0 警告且 Output Panel 本轮刷新；publish-only 没有新的运行 trace，因此不以该编译声称业务运行通过。之后候选实际载入的证据另见 §10.4。

`run-character-build-workbench-harness.js` 仍停在纸娃娃 structural fit / draw 字段断言，记录为未通过。以 `git show HEAD:<path>` 提供本轮变更前的 Web 源文件后，原基线得到同一失败（`fitFields:[] / drawFields:[]`）；本轮未降低断言或修改无关渲染器。共享工作台 release-tree 审计无错误，保留 `character-build.js` 641/640 行软阈值提示；默认全树审计的既有 appearance-service 颜色问题不计作本轮通过。

### 10.4 首轮隔离候选与人验交接

首轮候选：`C:/cf7-stash-0912/resources/tmp/runtime-candidates/v2/stash02`，交接时为 **`candidate_executed / NOT_DEPLOYED / HUMAN_ACCEPTANCE_PENDING`**；用户随后反馈未通过，已正常退出，后续复验使用 §10.5 的更新候选。

| 身份 | 值 |
| --- | --- |
| 构建输入源哈希 | `040B77250C6B1496C930B900F774D0B96661857B7CFDA734E50E5ED9C499479F` |
| Build identity | `25C34B89E2E32A71717F0694FB233D386BCE71F5602090794E9979C72461D584` |
| Payload closure | `0B07119555D1628CD9F3ADD124378817FB47DE1245DC171DA606B5ED4CA28B81` |
| Core SHA-256 | `E6810E980B92CE56AF523304C8108C28CF4DDB91D6C8D9454FC6ED06D62A59CB` |

本地 candidate producer 与 33 文件完整性校验通过。`automation/start.ps1 -CandidateRoot .../stash02` 核对了实际 Core 路径、哈希、identity 和 closure。2026-09-12 22:00 的 fresh `bootstrap_handshake` / `bootstrap_reveal_ready` 与 `SaveManager.loadFromMydata OK: 满包验收 lv99` 绑定 attempt `4733cdc6132d46c68b52123dbd02c11f`；实际进程 PID 为 11080。已目视进入基地并从原生装备入口打开构筑，真实装备图标、50 格背包与“暂存 42”角标正常。

首次候选运行发现两类不同问题，均保留记录：Host 读取 v2 暂存时对 `lastCommit:null` 使用 JToken 子索引，导致存档列表误报损坏，已改为显式 JObject 判断，SaveMigrator 定向 71/71；样本缺失现役要求的 `legacy` v1 空兼容记录，已在两个尚未进行业务写入的隔离样本中补回原记录，未放宽 AS2 校验。前一 `stash01` 已正常关闭，不能继续作为验收候选。

当前交付共用 Shell、OwnedView、QuickTransfer、拖拽、数量控件与 CSS，旧 `character-build-stash-view.js` 及专属暂存布局已删除。批量入口为共享“选择本页 → 执行所选”，暂存一批最多 32 条、普通库存最多 50 条；翻页/分类/来源变化清空未提交选择。本候选没有跨页“全部取出”自动循环，第 4.5 节跨批停止规则保留为后续扩展约束。

桌面自动操作工具把嵌入的 WebView2 控件判为另一个窗口，重新激活、重新观察及控件定位后仍拒绝点击；未使用其他输入注入绕过该检查。机器停在已载入的构筑页，**实际暂存内的物理输入、领取保存、正常退出重启回读尚未完成**，因此不称 `e2e_verified`、人验通过或正式入口验收。

测试员在当前隔离候选点击“暂存 42”，集中完成以下旅程：

1. “满包验收”：确认左背包、右暂存与战备箱视觉/操作一致；选择装备、黄铜和普通 hp 药剂批量领取，核对受阻装备仍保留、可领取项进入真实持有区，反馈能说明去向与原因。
2. “暂存验收”：领取指定数量的手枪弹药，拖装备到指定空格；切换完整/紧凑、分类及页码，查看真实装备详情，确认选择与显示清楚。
3. 暂存、物理收纳、构筑往返，关闭再打开；正常保存退出后重启相同隔离存档，核对背包格位、药剂/材料变化与暂存余量。

候选工作树和两个样本为人验有意保留，确认后再清理。本轮 23 个正式存档基线文件和 5 个正式入口/部署基线文件哈希未改变；本轮实现尚未提交或推送。局部日志、样本 manifest、DOM 图片和候选启动入口位于忽略目录 `tmp/stash-shared-20260912/`。人验反馈先修正同范围问题并更新候选，确认通过后才冻结正式源身份、获取云端共识及执行既有构建部署推送流程。

### 10.5 首轮体验反馈修正

用户在 `stash02` 中提出四项问题：暂存入口先显示战备箱、数量编辑多一次弹窗、12 瓶普通 hp 药剂无法转移、物品注释只有图标。首轮体验未获确认，不进入正式发布流程。

- **入口直达**：首次进入收纳时，真实库存协调器持有 `bootstrap` 只读同步占用。暂存导航此前把它当作不可切换，释放了领取意图；现在等待该同步结束后进入暂存，实际写入占用和失败状态仍拒绝切换。
- **数量与明确操作**：`InventoryWorkbenchStorageControls` 在共享底栏直接挂载现役 `QuantityControl`，选中堆叠即可滑动或输入，无数量弹窗。相邻“领取 / 存入 / 取出”调用原 QuickTransfer 自动归位；非法数量阻断提交，换物品重置数量，同一物品开始拖拽保留已选数量。战备箱、仓库和暂存使用同一实现。
- **药剂领取**：暂存的权威路由会把同名已装备药剂补入药剂栏；指定背包格会被拒绝为 `target_incompatible`。保留该权威规则和定点失败不改投的边界，明确“领取”按钮走自动归位，并在定点不适用时指向该按钮。真实日志可确认用户曾发起定点请求，脱敏日志不能单独证明每次拒绝的具体物品和原因。
- **完整注释**：ItemUse 返回 `{success:true,tooltip:{...}}`，共享提示需要顶层注释字段；来源适配现在展开 `tooltip` 并保留成功标记及原生 `document`。现役共享提示渲染恢复名称、属性、介绍和获取方式，协议与 Host/AS2 未改动。

本轮定向验证：来源导航 8/8（含真实 `bootstrap` 占用）、ItemUse 22/22（含完整请求通道到来源适配再到共享注释）、来源适配 16/16、批量/数量 17/17、工作台模块 37/37，以及 lazy closure 和 CSS 闭包。共享 DOM 场景现使用真实 `PanelTooltip` 与暂存响应适配，并验证首次直达、12 瓶药剂按 3+9 领取、非法数量拒绝、数字指定格、滑条指定数量拖拽、Ctrl、切页、来源往返、非前缀批量、未知结果核对和 1024×576 边界。Bridge/ItemUse 的权威数据仍是夹具，不代称本轮真实药剂写入、保存重启或体验人验通过。§10.3 的既有纸娃娃 harness 失败边界保持不变。

更新候选为 `C:/cf7-stash-0912/resources/tmp/runtime-candidates/v2/stash03`，状态 **`candidate_executed / NOT_DEPLOYED / HUMAN_ACCEPTANCE_PENDING`**。本地 producer 和 33 文件完整性通过，2026-09-12 22:42 启动的实际 Guardian PID 22080 已核对候选路径及身份，目视停在已选“满包验收”的启动确认页，尚未执行本轮游戏领取旅程。本轮为 Web 修正，native 构建身份及 payload closure 与 §10.4 相同；工作树中 24 个本轮 Web 源文件逐个匹配并记录在 `tmp/stash-shared-20260912/stash03-web-sources.json`，该清单 SHA-256 为 `9D081BB29C2FFCF357537F3666A888D5D92A8D6A4137CAB3E5BAE1139C61B843`。不能单用未变化的 native identity 代称 Web 修正已获人验。

收尾 release-tree UI 审计为 0 错误，保留已有 `character-build.js` 641/640 行软阈值提示；文档治理通过。正式 23 个存档基线文件及 5 个部署基线文件哈希均未变。复验保留用户已操作后的隔离存档，不重新填充两个合成样本。集中核对：从构筑“暂存”一次进入；选中血药就地调数量并领取、查看完整注释；保存退出重启后核对剩余量。确认通过后再执行原有云端共识与正式构建部署推送流程。

### 10.6 第二轮：来源切换后注释失效

用户认可其余体验，指出“初进战备箱注释正常 → 切到暂存失效 → 切回战备箱仍失效”。此时尚不进入正式发布。

根因位于 `InventoryStorageWorkbench.renderView`：新来源挂载时已渲染物品格，后续快照投影又对整棵仍在使用的视图执行 `releaseTree`；共享 `GridRenderer` 会复用权威对象未变的格子，不重跑绑定，导致这些格子失去注释。未变的左侧背包也受影响。上轮 DOM 验证在切换后先进行了筛选、翻页，重建格子让绑定恢复，掩盖了第一次进入的缺陷。

本轮生产修正仅两处：快照投影不再抢先清理整棵活动视图，格子替换和删除由现役 `GridRenderer` 清理；来源切换时只对真正离开的旧视图释放注释绑定，隔离其迟到回包。继续使用同一共享注释组件，不强制重建网格，不修改领取规则、存档或 Host/AS2。

`tools/test-reward-stash-view.js` 在修正前已稳定复现首次直接进入没有活动注释绑定。修正后分别跑构筑直达与战备箱入口，每条连续三轮往返、切换后立即悬停两侧物品，不先筛选/翻页；同时验证旧来源迟到回包不能覆盖当前注释、没有游离绑定、关闭后绑定归零。原数量、领取、拖拽、批量、恢复及 1024×576 回归继续通过；工作台模块 37/37。此轮使用既有隔离 `stash03` 的 native 候选，更新外部 Web 源文件并重启载入，原 native identity/closure 不变，当前 Web 清单与 PID 以忽略目录下最新 `handoff.json` 为准。仍需真实 WebView2 的这条切换旅程复验。
### 10.7 人验通过与正式发布收口

2026-09-12，用户复验第二轮切源注释修正后确认“可行”，并授权“同步上游，走完整后续流程”。本专项达到 **HUMAN_ACCEPTANCE_PASSED**，覆盖共享布局与交互、入口直达、就地数量、药剂操作、完整物品注释及来源往返。机器未独立证明用户未明确报告的保存重启全矩阵，不因此扩写为完整产品或正式入口业务验收。

被确认的 Web 清单 SHA-256 为 `8424F9648B9AB04CB8774803D79C342FBD6675FFFADAB3AD6A95859B6AECBD73`，native identity / payload closure 仍为 §10.4 所列。上游快进到 `d35277e83b7c390dadbf1e0e6f4b428e9d46ce53`，保留天启大封印修复与 UI 迁移台账；双方 AS2 源码合并后重新由 CS6 发布 asLoader，不覆盖上游修复或复用旧候选 SWF。正式列车采用 `runtime-build-v2/20260912-stash-shared-workbench-v1`；最终源、双生产者证明、policy 和 promotion 的实际结果以 [runtime 发布记录](runtime-build-reproducibility.md)为准，创建标签或提交源码本身不表示部署成功。

发布前验证：Launcher canonical 为 5,335 passed / 3 existing skipped / 0 failed（5,338 总数）；精确 SDK resolver 7/7、16 项 prepare 输出及主线/tag 保护检查通过。合并后的 asLoader 为 1344938 bytes / SHA-256 495FA286F9DB397A477C5AFE97AB326056496FF308820A3DFB63A58802B2D7CA，本次 CS6 Compiler 0/0，发布目标已关闭；SWF 含 SealDomain 及新增定点领取方法。外层 180 秒超时保留为工具层失败，原 JSFL 于 23:24 完成，经新鲜 diagnostics、SWF 与 IDE 静止复核后解除本轮 uncertain marker，未重触发编译。候选 Web 24 个源文件仍与人验清单逐字节一致。SWE2 补充审阅因非交互权限退出，未产出审阅结论；主控接手关键差异复核，不把该失败调用计入独立证明。
### 10.8 发布门体量告警收口

首次冻结源 `9154bbced7de54637c359786c26c2fcee652a7dc` / tag `runtime-build-v2/20260912-stash-shared-workbench-v1` 的源码 Audit 成功，cloud run `34702454701` 与本地签名产物一致，但 production policy 的严格工作台门拒绝了前期保留的 `character-build.js` 641/640 行告警。嵌入严格审计的 ratchet 测试为 66/67；发现明确阻断后停止同源后续重复审计，没有签发通过 receipt 或部署。旧 request `EEAB38480BF6AC58667931912405EA0EEA33EEA12BB2581A7B90D8B042CD742D`、标签与证明均保留为 superseded / NOT_DEPLOYED。

修正将暂存会话就绪通知原样移入已有 `character-build-stash-authority.js`，主构筑控制器只委托调用，体量回到 634 行。没有提高阈值、删除保护门或改变玩家交互；原生/AS2/数据源未变。会话归属 11/11、ItemUse 22/22、工作台模块 37/37 与 lazy closure 回归通过，真实共享 DOM 的两条入口各三轮往返、迟到回包隔离及数量/领取场景继续通过。最终列车使用 `runtime-build-v2/20260912-stash-shared-workbench-v2`，重新绑定完整源码与政策；前三域及文件闭包相同的本地签名按现役协议复用，云端必须针对新冻结源重新构建和签名。

### 10.9 正式部署与收尾证据

2026-09-13（北京时间）最终源 `c78cf257ff8e216c89c670c654202275391ddc47` / tag `runtime-build-v2/20260912-stash-shared-workbench-v2` 完成正式部署，当前 **HUMAN_ACCEPTANCE_PASSED / promoted**。request `B4983FB43A23F13E4B639F0E057C612247002A1DDABA5DBE29FEB03BC26EAD3B` 绑定本地 X509 `builder-local-b / physical-host-b` 与 [GitHub OIDC/Sigstore run 34703639787](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34703639787)，相同原生 build identity / 33 文件 closure 均与 §10.4 的已验收候选一致。新源 API/workflow/run SHA、不可变标签及最终证明已重放；production policy **40/40**、严格工作台审计 **0 错误 / 0 告警**、ratchet **67/67**。原子 promotion 于 `2026-09-12T16:07:23.8137792Z` 完成；完整身份、签名/清单哈希及可恢复上一版位置见 [runtime 发布记录](runtime-build-reproducibility.md)。§10.7 的 v1 身份和当时 Web 字节一致性为历史阶段证据，最终 Web 包含 §10.8 的等价职责提取。

部署提交 `75830157e0b1dce7dd020257d2136473a2331b43` 已快进推送 `main`；[事后 Audit 34705020122](https://github.com/FlashNightModReborn/CrazyFlashNight/actions/runs/34705020122) 成功，独立重放最终云端证明，在 Index 中核验 33 文件、2 signer / 2 faultDomain，并明确输出 `state=promoted / deploymentChanged=true`。

无 candidate 参数的正式入口实际运行 PID **37704**，Core SHA-256 `E6810E980B92CE56AF523304C8108C28CF4DDB91D6C8D9454FC6ED06D62A59CB`，路径、identity/closure 与正式 manifest 一致，bus ready 且欢迎页显示正常。未确认进入玩家存档；后台预热到期退回 Idle、Flash 退出码 0，再由窗口正常关闭完成 `guardian.shutdown_complete` / `core.main_exit`，无本次 Core/Flash 遗留。正式入口领取、保存重启的逐项业务未在本次重跑，不称专项 `standard_entry_verified`。

开工基线含 22 份存档 JSON 与 1 份版本标记。收尾有 21 份存档 JSON 哈希不变；测试槽 `cf7_agent_task_detail_callid_v1.json` 已在 23:35（UTC `2026-09-12T15:35:15.2788935Z`）更新，早于本次正式入口 00:12 启动。启动 AutoRepair 对全部槽 `clean, skip`、`applied=0`；未覆盖或恢复这一既有差异。版本标记在启动时重写，不把它记作玩家资产变化。

本轮隔离 worktree `C:/cf7-stash-0912/resources` 已确认无运行进程，限定 53 项本任务变更后归档 74 文件 / 8,522,675 bytes，源码差异、验收存档和日志逐项验哈希，再于 2026-09-12 23:42 移除。归档 `tmp/stash-shared-20260912/retired-stash03` 及 `release-worktree-cleanup.json` 留在本地。其他任务的 `C:/cf7-audio-r7-sniff-fix/resources`、`C:/cf7-audio-v2-repro-s11-b/resources`、`C:/cf7-focus-0909/resources` 保留；worker 报告的三处既有孤立 checkout（前缀 `84ae52d278f3`、`8b2dfdf49175`、`b650df8f2817`）也未删除。本次 worker 自有 checkout 已正常清理。
