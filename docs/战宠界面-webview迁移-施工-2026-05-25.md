# 战宠界面 WebView 迁移 — 施工记录 2026-05-25

## 目标

将 `flashswf/UI/战宠相关界面.swf`（宠物信息界面 + 领养宠物界面）从 Flash 迁移为 HTML，通过 WebView2 面板系统被 C# 壳调用。复用现有的 Panel 架构（panels.js + PanelHostController + Task 桥接）。

## 架构

```
JS (pet-panel.js / pet-data.js)  ←→  C# (PetTask.cs)  ←→  AS2 (PetPanelService.as)
         ↕                                    ↕
    panels.js / Bridge.send()           XmlSocketServer / MessageRouter
```

- **静态数据**：`pet-data.js` 包含 110+ 宠物定义、9 分类、25 种进阶方案（从 `data/merc/pets.xml` 和 `单位函数_aka_战宠进阶.as` 提取）
- **运行时数据**：通过 `petSnapshot` 命令从 AS2 `_root.宠物信息` 查询
- **写操作**：领养、出战/休息、进阶升级均经 C# PetTask 桥转发到 AS2 执行

## 新建文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `launcher/web/modules/pet-data.js` | ~580 | 宠物定义、分类、进阶方案元数据 |
| `launcher/web/modules/pet-panel.js` | ~450 | 主面板：列表视图 → 详情/进阶 → 领养商店 |
| `launcher/src/Tasks/PetTask.cs` | ~190 | Web↔Flash callId 桥接层（参考 ArenaTask） |
| `scripts/类定义/org/flashNight/arki/merc/PetPanelService.as` | ~430 | AS2 端 11 个 gameCommands 处理器 |
| `scripts/逻辑系统分区/战宠系统_WebView.as` | 4 | 帧脚本安装入口 |

## 修改文件

| 文件 | 变更 |
|------|------|
| `launcher/web/modules/panels-lazy-registry.js` | +4 行，注册 `pets` panel |
| `launcher/web/css/panels.css` | +300 行，战宠面板 Cyberpunk 样式 |
| `launcher/src/Guardian/LauncherCommandRouter.cs` | `PETS` → `petPanelOpen` + `OpenPanel("pets")` |
| `launcher/src/Guardian/WebOverlayForm.cs` | +PetTask 字段、SetPetTask、HandlePanelMessage 路由、ResolvePanelCloseGameCommand、ClearPending |
| `launcher/src/Program.cs` | 创建 + 注入 PetTask |
| `launcher/src/Bus/TaskRegistry.cs` | 注册 `pet_response` 异步路由 |
| `launcher/CRAZYFLASHER7MercenaryEmpire.csproj` | +1 行 `<Compile Include>` PetTask.cs |
| `scripts/asLoader/LIBRARY/asLoader.xml` | +1 行 `#include` 战宠系统_WebView.as |

## 数据协议

### AS2 查询命令

| cmd | action (AS2) | 说明 |
|-----|-------------|------|
| `snapshot` | `petSnapshot` | 获取全部宠物信息快照 + 玩家状态 |
| `adopt_list` | `petAdoptList` | 获取可领养宠物列表（按分类） |
| `preview_advance` | `petPreviewAdvance` | 预览进阶效果 |

### AS2 写命令

| cmd | action (AS2) | 说明 |
|-----|-------------|------|
| `adopt` | `petAdopt` | 领养宠物（填充空槽位） |
| `deploy` | `petDeploy` | 出战/休息切换 |
| `advance` | `petAdvance` | 执行进阶方案 |
| `expand_slot` | `petExpandSlot` | 扩充宠物格子 |
| `rename` | `petRename` | 重命名宠物 |

## 踩坑记录

### 1. UTF-8 BOM 缺失
- **现象**：PetPanelService 所有命令 timeout
- **原因**：新建的 `.as` 文件缺少 `EF BB BF` BOM，Flash 编译器静默跳过
- **修复**：`PetPanelService.as` 和 `战宠系统_WebView.as` 添加 BOM

### 2. `cat.ids` 属性名错误 → `cat.List`
- **现象**：打开领养商店后 UI 卡住（10s timeout 后恢复）
- **原因**：`_root.宠物商城列表[c].List` 是 XML 解析后的二维数组（按 `<List>` 行分组），代码中误写为 `.ids`
- **修复**：三层嵌套遍历（行→列→petId），过滤 null

### 3. 领养槽位逻辑错误
- **现象**：扩展格子后领养永远失败
- **原因**：`handleExpandSlot` 调用 `_root.开宠物格子()` 同步 push 空数组，但 `handleAdopt` 又 `push` 新条目，导致 `宠物信息.length >= 宠物领养限制` 恒为 true
- **修复**：改为查找空槽位（`slotEntry.length == 0`）并赋值，而非追加

### 4. .NET Framework 4.6.2 目标包缺失
- **现象**：MSBuild 报 `MSB3644: .NETFramework v4.6.2 引用程序集未找到`
- **环境**：该机器安装了 4.6.1 和 4.7.2，跳过了 4.6.2
- **修复**：`mklink /J v4.6.2 → v4.6.1`（4.6.2 与 4.6.1 API 兼容）

### 5. PetTask.cs 未加入 .csproj
- **现象**：MSBuild 报 `CS0246: 未找到类型或命名空间名称 PetTask`
- **原因**：经典 MSBuild 项目格式需显式 `<Compile Include>`
- **修复**：在 `.csproj` 中添加编译条目

## 编译状态

- **asLoader**：已编译 ✓
- **C# launcher**：Release 模式编译通过（0 新错误，仅 13 个预存警告） ✓

## 待办

- [ ] 端到端测试：工具栏按钮 → 面板打开 → 领养 → 出战 → 进阶 → 关闭
- [ ] 战宠头像图标映射（当前缺少宠物头像图片）
- [ ] dev harness 页面（独立浏览器 QA 测试）
- [ ] 进阶方案执行上下文验证（`scheme.条件.call(ctx)` 在 AS2 中 `this` 绑定是否完全兼容）
- [ ] 重命名功能 UI（已预留 AS2 桥接口，JS 端尚未实现 UI 入口）
