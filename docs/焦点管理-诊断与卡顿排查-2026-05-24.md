# 焦点管理 — 诊断与卡顿排查（长期备忘）

> ## ⚠️ 终态导航（2026-05-24 23:30 三次校准后）
>
> **当前终态结论在 §9.7 + 姊妹文档 [`docs/诊断设施与首轮基线-2026-05-24.md`](诊断设施与首轮基线-2026-05-24.md) §7**。本文 §1~§8 是探索过程的**考古记录**——保留以防再走错路，**不是当前结论**。
>
> **TL;DR 终态**：UI 7s 冻结是真实但**低频偶发**事件（5+1 次重启未复现）；reveal 路径已 67ms 实测**永久排除**；真根因 backlog 在 `webOverlay.idle_sequence "full"` / `[Panel] EX_STYLE idle-full` / `FocusRestore` 重试时序之间；已补 `[IdleProbe]` / `[FocusProbe]` 子分段等待下一次复现；复现路径 = 启动后立刻切外部窗口 + 等 panel swap + 切回 launcher。**生产玩家几乎不触发，不阻断发布**。
>
> 新接手 agent **请直接跳到 §9.7 + 姊妹文档 §7**。

- **首版日期**：2026-05-24
- **状态**：架构 4 层就位 + 诊断日志补全；**冻结根因低优先级 backlog**（见上方终态导航）
- **性质**：长期问题备忘录。记录"我们已经知道什么、试过什么、为什么不能再走同一条路"
- **触发**：测试员反馈偶发卡顿持续存在；commit `e1d1ee0f6` 之后改动收口，需要日志体系才能继续推进

---

## 0. 怎么读这份文档

焦点管理是 launcher 的**长期挣扎点**——从 2026-04-01 第一次架构变更到 2026-05-23 的收束，已经迭代过至少 5 轮、跨越 7 周。每一轮都"修好了眼前的回归、但下一种边界又冒出来"。

读者可能是：

- 隔一段时间回来接手的作者本人，已经忘了"为什么去抖窗口是 200ms 不是 450ms"
- 一个新设备上的 AI 会话，没有本机 memory，靠 git 接力
- 协作者，想理解某条 `[Activation]` 日志意味着什么

**若要改动焦点管理的任何一环，先读 §6 历史时间线**——确认你要做的事不是某轮已经否决过的方案。

---

## 1. 为什么这是个长期问题

Flash SA 嵌入 launcher 的窗口栈本质上有**多个独立信号源**在抢"前台"这个语义：

| 信号源 | 形态 | 工作粒度 |
|---|---|---|
| `WM_ACTIVATEAPP` | 进程级激活 | 同进程窗口间切换**不**触发，跨进程切换才触发 |
| `GetForegroundWindow()` | 窗口级前台 | 任何瞬态窗口都会改变它 |
| `WM_SIZE`/`SIZE_MINIMIZED` | 最小化态 | 与前台正交 |
| `SystemEvents.SessionSwitch` | 会话锁屏 | 默认桌面侧前台恒为 NULL |
| WebOverlay panel 接管 | 同进程前台转移 | 不触发 WM_ACTIVATEAPP，但 `GetForegroundWindow()` 变 |
| 嵌入后的 Flash 子 HWND | PID 归 Guardian，但仍是独立 HWND | `IsFlashForeground` 判定要考虑会话边界 |

外部干扰源更多：

- QQ / Telegram / Discord 等 IM 通知的 toast 窗口（瞬时抢焦再归还）
- 输入法候选窗（搜狗/微软拼音）
- Steam Overlay 弹出
- Windows 系统通知 / 任务栏弹窗
- 杀软扫描时的瞬态窗口
- 后台程序退出后留下的"焦点真空"（`GetForegroundWindow()` 返回 NULL）

**核心难点**：这些信号在时间维度上重叠，单一信号不足以判定"用户是不是真的在玩游戏"。任何用单一信号源的判定都会在某种边界翻车，过去半年的迭代基本就是这件事的"打地鼠"。

---

## 2. 当前架构（4 层防御，commit `e1d1ee0f6` 收束）

> 详细注释在源码里，这里只画地图。修改前**先**回去读源码 doc-comment。

### 层 ①：`AppActivationState`（权威激活态，去抖）

- **路径**：[`launcher/src/Guardian/AppActivationState.cs`](../launcher/src/Guardian/AppActivationState.cs)
- **信号源**：`WM_ACTIVATEAPP`（进程级）+ `WM_SIZE`/`SIZE_MINIMIZED`
- **喂入**：[`GuardianForm.WndProc`](../launcher/src/Guardian/GuardianForm.cs)
- **去抖窗口**：200ms（曾是 450ms，e1d1ee0f6 收窄）
- **设计意图**：取代散落各处的 `GetForegroundWindow()` 现场轮询。后台 toast 瞬时抢焦再归还会产生 `WM_ACTIVATEAPP(false)→(true)` 抖动，宽限窗内重新激活视作从未失活。
- **线程模型**：UI 线程写、钩子线程/FrameTask 线程读，全 volatile + 单调 TickCount，**无锁**。改这里前先确认你理解 §3。

### 层 ②：`KeyboardHook`（按键归属判定）

- **路径**：[`launcher/src/Guardian/KeyboardHook.cs`](../launcher/src/Guardian/KeyboardHook.cs)
- **核心方法**：`ShouldInterceptForOurApp(bool strict)`
- **判据**（按优先级）：
  1. 非 strict 键：先看 `AppActivationState.IsAppActive`（去抖后）
  2. `GetForegroundWindow() == NULL`（真空）→ 视作我方（真空下按键不会落到别的程序）
  3. 前台窗口 pid 归本进程或 Flash 进程
- **strict 例外**：破坏性键（Ctrl+Q 杀进程）跳过判据 1 的去抖宽限——宽限窗内无法区分"toast 抢焦"与"用户真的切走"，宁可漏触发也不能在别的程序里把启动器干掉。

### 层 ③：`PerfDecisionEngine`（性能决策门控）

- **路径**：[`launcher/src/Guardian/PerfDecisionEngine.cs`](../launcher/src/Guardian/PerfDecisionEngine.cs)
- **门控**：`!IsAppActive || IsMinimized` → 停决策 + 3 秒 cooldown
- **为什么**：Flash 非前台自行降帧，AS2 上报的 FPS 已不可信。继续决策会把后台降帧误判成性能不足，污染 tier——回到游戏后画质卡在低档（这是过去的回归 bug）。
- **panic 兜底**：失活时**不**停 panic 降级（旧实现因 panic 被掐断导致 FPS 探底后无救援）。

### 层 ④：前台看门狗（兜底真空回收）

- **路径**：[`GuardianForm.SetupForegroundWatchdog`](../launcher/src/Guardian/GuardianForm.cs)
- **节奏**：400ms 定时器
- **触发条件**：`GetForegroundWindow() == NULL` 连续 **≥2 tick**（≈800ms）
- **动作**：`WindowManager.RestoreFlashInputFocus("fg_watchdog:vacuum")`
- **为什么需要连续确认**：MSDN 明确 "a window is losing activation" 瞬间 `GetForegroundWindow()` 即为 NULL——单帧采样会与用户主动切窗竞态，误把交接瞬间当真空、把前台抢回 Flash 对抗用户。
- **停转条件**：`SessionLock`/`ConsoleDisconnect`/`RemoteDisconnect`（锁屏 / 安全桌面 / 快速用户切换期间默认桌面侧前台恒为 NULL）。

---

## 3. 诊断日志体系（2026-05-24 补全）

补这层日志的原因：测试员反馈卡顿仍存在，但 commit `e1d1ee0f6` 之后**抢焦肇事者在日志里完全不可见**——`AppActivationState` 的写入静默、`PerfDecisionEngine` 的门控静默、`[FocusRestore]` 只在真空已成型后才打（fg 已是 NULL）。

### 3.1 `[Activation]` — WM_ACTIVATEAPP 进出对

源：[`GuardianForm.WndProc`](../launcher/src/Guardian/GuardianForm.cs) WM_ACTIVATEAPP 分支

```
[Activation] lost fg=0xABCD pid=1234 class=TXGuiFoundation title="QQ"
[Activation] gained gapMs=87
```

- **`lost fg=`** 是抢焦发生瞬间 `GetForegroundWindow()` 抓到的肇事窗口快照（pid/class/title）。**这是唯一能识别"是谁抢的焦"的实时窗口**——抢焦窗口之后可能消失，再查就晚了。
- **`gapMs`**（lost→gained 时间差）直接区分场景：
  - `< 200ms` → toast 瞬时抢焦，被 `AppActivationState` 去抖吃掉；理论上不影响性能决策
  - `200ms ~ 数秒` → 真失活了，`PerfDecisionEngine` 已停决策
  - 秒级 → 用户切走又切回

### 3.2 `[PerfGate]` — 性能门控转换

源：[`PerfDecisionEngine.Evaluate`](../launcher/src/Guardian/PerfDecisionEngine.cs) 失活门控分支

```
[PerfGate] closed reason=deactivated tier=0 latestFps=27.3
[PerfGate] opened tier=0 latestFps=14.2 cooldownMs=3000
```

仅在【转换】时打，不每帧刷。配 `[Activation]` 一起读就能回答"抢焦时 tier 在哪个档、回来后是不是被卡在低档"。

### 3.3 已有的 `[FgWatchdog]` / `[FocusRestore]`

为完整性记录在此（不是这次新加）：

- **`[FgWatchdog] foreground vacuum detected`**：看门狗确认了 ≥2 tick 真空，准备回收。此时 `GetForegroundWindow()` 已是 NULL——肇事者已退场，**抓不到名字**。
- **`[FocusRestore] <reason> ...`**：所有主动回收前台的路径都打。reason 字段决定归因来源：
  - `fg_watchdog:vacuum` — 来自 §2 层 ④
  - `panel_close:idle:<tag>` / `panel_close:soft:<tag>` — 来自 WebOverlay panel 关闭
  - `set_ready` 等 NativeHud idle 调度

---

## 4. 日志解读速查表

测试员复现卡顿后，把卡顿时间戳前后 ±5 秒的 `logs/launcher.log` 截出来，对照下表：

| 日志组合 | 解读 | 下一步 |
|---|---|---|
| `[Activation] lost` + 短 `gapMs (<200)` + **无** `[PerfGate]` | 去抖吞掉的 toast 抢焦 — 卡顿成因**不**是性能控制 | 看 `fg=` 里的 class 名锁定骚扰进程；考虑用户能否关掉它的通知 |
| `[Activation] lost` + 长 `gapMs (>200)` + `[PerfGate] closed/opened` 配对 | 真实失活；**重点看 opened 时 tier 是不是 1** | 若卡低档，回去检查 panic 降级路径是否在失活期间被误触发 |
| `[FgWatchdog] vacuum` 但**没有**对应的近期 `[Activation] gained` | 抢焦未归还的真空场景 | 往前找最近的 `[Activation] lost fg=` — 那个 class 名就是嫌疑人 |
| 短时间内多次 `[Activation] lost/gained` 配对 | 后台程序周期性骚扰（自动更新、心跳通知） | `fg=` 的 class 名锁定；判断要不要在 launcher 层"原谅"它（白名单不算失焦） |
| `[FocusRestore] ... pass1=fail pass2=fail` | AttachThreadInput hack 都失败了 — 焦点被某个高权限进程钉死 | 查 `fgBefore=` 的 pid，看是不是 UAC 提升进程 / 反作弊驱动 |

### 4.1 已知嫌疑进程的窗口 class 名速查

- `TXGuiFoundation` → QQ
- `Chrome_WidgetWin_*` → Chromium 系（Discord/Edge/Electron 通知）
- `Shell_TrayWnd` → Windows 任务栏右下角弹窗
- `ChineseIme*` / `SogouImeIE*` / `MSCTFIME*` → 输入法候选窗
- `vguiPopupWindow` → Steam Overlay
- `tooltips_class32` → 通用 tooltip 弹窗
- `Windows.UI.Core.CoreWindow` → UWP 应用通知中心

---

## 5. 还没回答的疑点（卡顿根因候选）

补完日志后等测试员日志样本，下面这些都是当前**没排除**的候选：

1. **去抖窗口 200ms 仍不够短**：某些 toast（如 Windows 11 通知中心）可能停留 300~500ms，恰好超出去抖、刚好触发 `[PerfGate] closed/opened` 一对，3 秒 cooldown 期间画质会被切到低档；用户感受是"卡了一下"。
   - 怎么验：日志里如果出现"`gapMs` 300~400 + `PerfGate` 配对 + 后续 `[PerfActive]` 出现 tier=1 → 0 的回弹"——就是这个。
2. **🚨 看门狗在长真空中根本不触发**（**2026-05-24 测试员日志已升级为主嫌疑**）：
   - 原本假设：watchdog 反应慢（800ms 才动作）
   - **实测打脸**：测试员侧多次出现 `[Activation] lost fg=0x0` 持续 7s ~ 270s，期间 `[FgWatchdog] vacuum detected` **0 次** 触发；同期 FrameTask 线程的 `[PerfGate]` 正常打日志，**只有 UI 线程上的 Forms.Timer 静默**。
   - **推论**：UI 线程被某种东西阻塞了，导致 watchdog `Tick` 根本不来。看门狗与症状是同生共死的——UI 卡死时它也死了。
   - 怎么验：见 §5.6 + §8.2 并行探查 Track A。
   - **2026-05-24 Codex Track A 回填**：已新增独立于 UI 线程的 `[UIFreezeProbe]` 背景探针（见 §8.7 Track A）。下一轮测试员复现时，若长 vacuum 期间出现 `ui_thread_stale age≈vacuum时长` / `vacuum_seen seconds=N`，则该候选继续升级；若 `ui_tick_age` 保持低值，则说明 Forms.Timer 没死，应回头查 watchdog 触发条件被抑制。
3. **Flash 子窗口 PID 归属错乱**：嵌入后 PID 归 Guardian，但底层 SetParent 与窗口生命周期的某些边界态（Flash 自重启？）可能让 `IsWindowInFlashSession` 错判。
   - 怎么验：`[FocusRestore]` 的 `fgBefore=` pid 是否在某些时刻出现非 Guardian 且非 Flash 的值。
4. **panel 关闭瞬间的双跳**：WebOverlay panel 关闭会触发 `panel_close:idle` 路径，期间 `GetForegroundWindow()` 可能短暂为 NULL，看门狗误判真空。
   - 怎么验：`[FocusRestore] panel_close:*` 之后 800ms 内是否紧跟 `[FgWatchdog] vacuum detected`。看门狗有 `IsPanelMode` 抑制，但 panel→idle 切换瞬间的窗口期可能漏。
5. **AS2 端的 FPS 上报受失焦节流影响**：Flash SA 非前台时帧率被系统降到 ~8 FPS。如果 AS2 在`[PerfGate] closed` 之前就上报了几个低 FPS 样本，可能已经触发 panic（panic 不被门控）。
   - 怎么验：`[PerfActive]` panic 跳变与 `[Activation] lost` 的时序。
6. **📜 UI 线程被场景切换 burst 阻塞导致 Watchdog 死亡**（**已降级，见 §9.7 + 姊妹文档 §7**）：
   > ⚠️ 此候选在 2026-05-24 下午升级为主嫌疑，但 23:30 三次校准后已**精确化并降级**：UI 冻结**真实存在**（wall=7000ms 铁证），但**触发场景不是 gameplay 的场景切换 burst**，是 boot 期 reveal_done 后的 panel swap / FocusRestore 时序。下方原文留作探索过程记录。

   - 测试员明确报告："猛地一卡 + 游戏内鼠标失效 + FPS 掉 + 需要点窗口外面再点回来"——这是教科书级的"foreground vacuum"症状，理应被 watchdog 自动恢复。
   - 测试员排除条件：卡死之前 ≥30s 未碰 QQ，无任何前兆。**已排除 QQ alt-tab 作为主因**。
   - 日志铁证：长真空期间 0 次 `[FgWatchdog]`，但 FrameTask 线程的 `[PerfGate]` 仍正常打——**只有 UI 线程上的 Timer 死了**。
   - 触发上下文：`[Activation] lost fg=0x0` 之前 1~2 秒常出现高密度活动 burst——shadow save（17KB JSON）+ bgm_stop/play + data_query x4 + scene 加载。**强相关但未确认因果**。
   - **2026-05-24 Codex Track B 回填**：严格按 §8.2 的“vacuum 前 5 秒”窗口复查测试员日志，17 次 `fg=0x0` 中 7 次命中 shadow/bgm/data/panel burst（41.2%），`加载场景背景` 0 次命中；dev 对照日志 `logs/launcher.log*` 未见 `fg=0x0`。结论：burst 是部分相关、不是单一充分解释；“场景切换 burst → UI hang”从“强相关”降级为“优先复现场景之一”，等待 Track A 新日志确认。
   - 候选卡源：
     - (a) Flash 子窗口本身消息泵被场景加载阻塞，Windows 撤销其前台权 → vacuum；同时同进程的 launcher UI 线程也被某种共享资源拖累
     - (b) WebView2 / Chromium 子进程同步 IPC 阻塞 launcher UI 线程
     - (c) DesktopCursorOverlay / InputShield / NativePanelBackdrop 某条 GDI 路径同步等待
     - (d) .NET 进程内 lock 死锁（现有日志看不到）
   - 怎么验：需要加**独立于 UI 线程的背景诊断线程**（见 §8.2 Track A），用 P/Invoke 直接探测 + IsHungAppWindow，证实/证伪 UI 是否冻结。
   - **2026-05-24 Codex Track C 回填**：按 §8.2 搜索 `WebOverlayForm` / `PanelHostController` / `BootstrapPanel` / `InputShieldForm` / `NativePanelBackdrop` / `NativeHudOverlay` / `Hud/`，没有发现 `.Result` / `.Wait()` / `.GetAwaiter().GetResult()` 或 `SendMessage()` 这类“UI 线程同步等跨进程返回”高嫌疑点。`PostWebMessageAsJson` 与 `ExecuteScriptAsync` 未同步等待，`InputShieldForm` 的 CDP 注入是 `await CallDevToolsProtocolMethodAsync` 串行异步。候选 (b) 暂降为低/中嫌疑，不删除，等 `[UIFreezeProbe] hung_window` / `ui_thread_stale` 证据再决定是否审 CDP 队列或 WebView2 browser process。

---

## 6. 历史时间线（修改前必读）

| 日期 | commit | 改动 | 当时解决的问题 |
|---|---|---|---|
| 2026-04-01 | `d703daced` | 架构变更以解决交互输入焦点问题 | 早期 Flash 嵌入后输入焦点丢失 |
| 2026-05-17 | `086f7eb36` | 增强对 webpanel 状态与游戏状态下的焦点约束 | WebOverlay panel 与 Flash 之间焦点切换的边界 |
| 2026-05-22 | `16c412ad3` | 优化焦点管理的架构（引入 `AppActivationState`） | 散落各处的 `GetForegroundWindow()` 轮询不一致；toast 瞬时抢焦污染性能 tier |
| 2026-05-23 | `e1d1ee0f6` | 收束重构、修复行为回归 | 去抖窗口 450→200ms；Ctrl+Q strict 路径；看门狗 ≥2 tick 确认；锁屏停转；防异常冒泡 |
| 2026-05-24 上午 | （本次） | 加 `[Activation]` / `[PerfGate]` 诊断日志 | 卡顿仍偶发但日志看不到抢焦肇事者 |
| 2026-05-24 下午 | （pivot） | 测试员日志否决 QQ 主因假说，定位**UI 线程冻结 → Watchdog 死亡** | 加背景诊断线程证实 / 证伪 UI 冻结；详见 §5.6 + §8 |
| 2026-05-24 晚 | Codex Track A/B/C | 新增 `[UIFreezeProbe]` 背景探针；完成测试员日志 burst 相关性分析与 WebView2 同步调用审计；`launcher/build.ps1` + `launcher/tests/run_tests.ps1` 通过 | Track A 已具备复现采样能力；Track B 显示前 5 秒 burst 命中 7/17、不是单一根因；Track C 未发现 UI 线程同步等待 WebView2 的高嫌疑点 |

### 已被否决的方案（**不要重新提出**）

- ❌ **把去抖窗口拉长到 500ms+**：会让破坏性热键的安全窗口扩大，Ctrl+Q strict 路径就是为这个加的护栏。
- ❌ **轮询 `GetForegroundWindow()` 取代 `WM_ACTIVATEAPP`**：早期实现，输给了"窗口级前台 vs 进程级激活"的语义差——同进程窗口切换会被误判失活。
- ❌ **看门狗单帧采样真空就动作**：与用户主动切窗竞态，玩家反馈"切出去窗口又被吸回来"。
- ❌ **失焦（非最小化）也停决策**：导致 panic 降级被掐断，FPS 探底后无救援。e1d1ee0f6 之前的 bug。
- ❌ **QQ alt-tab 作为本次卡顿主因**（2026-05-24 测试员沟通否决）：测试员明确"卡死前 ≥30s 未碰 QQ"，QQ 抢焦虽是真实干扰但不是这次卡顿的源头；不要再投入资源做 QQ 白名单 / cooldown 调整作为这次卡顿的修复方案。
- ❌ **缩短 PerfGate cooldown（3000→800ms）作为本次卡顿修复**：基于 QQ 假说提出的方案，QQ 假说已否决，连带搁置；先证伪 / 证实 §5.6（UI 冻结）再决定动哪里。

---

## 7. 下一步（已被 §8 取代）

> ⚠️ 原 §7 的"按速查表分类"流程基于"卡顿源自抢焦"前提。2026-05-24 测试员日志推翻该前提——长真空期间看门狗不动作意味着架构有结构性问题，不只是分类的问题。
> **请直接读 §8** 获取并行探查的完整交接。

**不要**直接动架构——4 层防御之间的耦合性很强，过去 5 轮迭代每次想"顺便简化一下"都引入了新的边界 bug。诊断阶段只加日志 / 探针，不动现有 4 层防御。

---

## 8. 并行探查交接上下文（2026-05-24 续，新接手 agent 必读）

> ⚠️ **本节 Track A/B/C 已全部回填并实测落地**（见 §8.7）。**新接手者请直接读 §9.7 + 姊妹文档 §7**。本节保留作派发模板，下一次（若需要）追新嫌疑时仍可复用。

> 本节是给**新接手**的 agent（可能是另一会话的 Claude、Cursor、Codex、人类协作者）的自包含交接说明。即使跳过 §1~§7，只读本节也应能开展工作。**但开工前请按 §6 已否决方案清单自检不要走回头路。**

### 8.1 TL;DR（30 秒了解状态）

- **症状**：测试员侧 launcher 偶发"猛地一卡 + 游戏内鼠标完全失效 + Flash FPS 掉 + 必须 alt-tab 出去再点回来"。无前兆。同一现象在 dev 机感受不到（机器性能高 / 不一定能复现窗口）。
- **现有架构**：4 层焦点防御已就位（§2），日志诊断 `[Activation]` / `[PerfGate]` / `[FgWatchdog]` / `[FocusRestore]` 已布设（§3）。
- **关键死结**：测试员日志里**长 vacuum（fg=0x0 持续 7s~270s）期间 `[FgWatchdog]` 从不触发**——按设计应该 800ms 内就动作。同期 FrameTask 线程的 `[PerfGate]` 正常打日志，证明**只有 UI 线程上的 Forms.Timer 死了**。
- **当前主嫌疑**：UI 线程被某种东西阻塞（候选见 §5.6）。
- **当前需要的不是修代码，是先证伪 / 证实假说**——加独立诊断探针，让测试员复现后看新日志。

### 8.2 三条并行探查 Track（彼此独立，可并发派发）

#### Track A — UI 线程冻结假说的证伪 / 证实（写代码）

**目标**：加一条**独立于 UI 线程**的背景诊断线程，让我们能区分"UI 线程冻结"和"用户真 AFK"。

**触点**（建议改的文件）：
- `launcher/src/Guardian/GuardianForm.cs`（构造一个 `Thread` 或 `Task` 后台线程）
- `launcher/src/Guardian/WindowManager.cs`（已有 `DescribeForeground` / `IsForegroundVacuum` 复用）
- 新建 `launcher/src/Diagnostic/UiFreezeProbe.cs`（推荐独立文件，与现有 `Diagnostic/` 目录的诊断组件同 namespace）

**最小可行实现**：

1. 后台 `Thread`（不是 `Forms.Timer`，避免被 UI 线程拖死），1 秒一次轮询。
2. 每次轮询：
   - P/Invoke `GetForegroundWindow()` 取系统前台。
   - P/Invoke `IsHungAppWindow(_flashHwnd)` 与 `IsHungAppWindow(GuardianForm.Handle)` 检测窗口是否被 Windows 标记为 hung。
   - 读 UI 线程上 `_foregroundWatchdog` 最近一次 Tick 的时间戳（需要在 `OnForegroundWatchdogTick` 头部加一行 `_lastWatchdogTickTick = Environment.TickCount;` 暴露给后台线程）。
3. 触发日志条件（择一）：
   - 系统前台连续 ≥3 秒为 NULL → `[BgWatchdog] vacuum_seen seconds=N ui_tick_age=M ms`
   - `IsHungAppWindow` 任一返回 true → `[BgWatchdog] hung_window flash=<bool> guardian=<bool>`
   - `ui_tick_age > 2000` → `[BgWatchdog] ui_thread_stale age=M ms`
4. **不要** 让后台线程自己抢焦——只观察 + 打日志。回收焦点的动作仍归 UI 线程上的 watchdog（保持现有职责分工）。

**P/Invoke 参考**：
```csharp
[DllImport("user32.dll")]
private static extern bool IsHungAppWindow(IntPtr hWnd);
```

**交付验收**：
- 编译走 `powershell -ExecutionPolicy Bypass -File launcher/build.ps1`（不是裸 msbuild，详见 [feedback_launcher_build memory](../memory/feedback_launcher_build.md)）。
- 测试员复现卡顿后，新日志应能回答：长 vacuum 期间，`ui_tick_age` 是否真的 ≥ vacuum 时长？`IsHungAppWindow` 怎么报？
- **不动现有 4 层防御**——只加观察探针。

#### Track B — 场景切换 burst 相关性分析（只读日志，不写代码）

**目标**：验证"vacuum 总在场景切换 burst 之后发生"的相关性。

**输入数据**：
- 测试员日志：`C:\Users\Administrator\Downloads\launcher (1).log`（3732 行，本机 dev 已下载）
  - 若 agent 在其他机器上跑，需 dev 把它放到共享位置 / 嵌入到 prompt。
- dev 日志：`logs/launcher.log` + `logs/launcher.log.1`（对照组）

**任务**：
1. 用 grep 抓所有 `[Activation] lost fg=0x0`（不是 `fg=0xN..` 这种有 pid 的）。
2. 对每个 vacuum 事件，回看前 5 秒的日志，记录是否出现下列 burst pattern：
   - `[XmlSocket:JSON]` 含 `"op":"shadow"`（shadow save）
   - `bgm_stop` 紧跟 `bgm_play`
   - `data_query` 连续 ≥2 条
   - `[Panel] EX_STYLE` 状态切换
   - `加载场景背景` 类 LogBatch
3. 产出 markdown 表格：
   | vacuum 时间 | gapMs | 前 5 秒 burst fingerprint | 是否包含 scene transition |
4. 计算相关性：vacuum 事件中场景切换的命中率。

**交付验收**：
- 一份 markdown 报告，约 100~200 行。
- 若命中率 > 70%，把"场景切换 burst → UI hang"列为 Track A 的优先验证场景。
- **不需要改任何代码**，纯日志分析。

#### Track C — WebView2 / Chromium 同步调用审计（只读代码）

**目标**：找出 launcher 代码里所有"UI 线程同步等 WebView2 / 跨进程返回"的调用点——这些是 UI 线程冻结的天然嫌疑。

**触点目录**：
- `launcher/src/Guardian/WebOverlayForm.cs`（主嫌疑，3000+ 行）
- `launcher/src/Guardian/WebOverlayPanel*.cs` / `launcher/src/Guardian/PanelHostController.cs`
- `launcher/src/Guardian/NativePanelBackdrop.cs`
- `launcher/src/Guardian/BootstrapPanel.cs`
- `launcher/src/Guardian/InputShieldForm.cs`
- `launcher/src/Guardian/NativeHud/`（如存在）

**搜索模式**（用 ripgrep / Grep）：
- `\.Result\b`（Task.Result 同步等待）
- `\.Wait\(\)` / `\.Wait\(\d+\)`
- `\.GetAwaiter\(\)\.GetResult\(\)`
- `Invoke\(` 后跟 lambda（非 BeginInvoke）
- `SendMessage\(` Win32 同步消息（PostMessage 异步则没事）
- `ExecuteScriptAsync` 后是否有 `await` / `.Result` / `.ContinueWith` 混用
- `CoreWebView2.PostWebMessage*` 是否在 UI 线程同步路径

**产出**：一份清单，每条形如：
- 文件:行号
- 调用栈上下文（是否真的在 UI 线程上）
- 是否能改异步 / 改 BeginInvoke

**不要**：不要直接重构调用点。先列清单，跟 dev 对齐优先级。

**交付验收**：
- 一份 markdown 列表，分高 / 中 / 低嫌疑级。
- 高嫌疑 = "肯定 UI 线程" + "同步等跨进程"。这些是 Track A 验证 UI 冻结后第一批改的候选。

### 8.3 关键证据（嵌入式，agent 不需要拿日志文件）

测试员日志中最有诊断价值的 4 段（line 号来自 `C:\Users\Administrator\Downloads\launcher (1).log`）：

**证据 1：长 vacuum 期间 watchdog 死亡**（line 2080-2084）
```
10:19:43.264 [Activation] lost fg=0x0
10:19:49.003 [PerfGate] closed reason=deactivated tier=0 latestFps=30.0
   ←—— 102 秒纯静默，0 条 [FgWatchdog]，0 条 [Frame:UI]，0 条 cursor_control ——→
10:21:31.684 [Activation] gained gapMs=108422
10:21:31.998 [PerfGate] opened tier=0 latestFps=29.9 cooldownMs=3000
```
解读：FrameTask 线程在跑（5.7s 后能打 `[PerfGate]`），但 UI 线程上 400ms Timer 整整 102 秒不响应。

**证据 2：vacuum 前的 burst**（line 2069-2080）
```
10:19:42.835 shadow save 17329 chars (callId 62)
10:19:42.836 ArchiveTask Shadow saved
10:19:43.019 bgm_play /sounds/BONUS/I've - Urban Surface.mp3
10:19:43.096 data_query NPC dialogue x4 (callId 63-66)
10:19:43.254 DesktopCursor Active -> CallerOff  ← UI 线程最后一条日志
10:19:43.264 Activation lost fg=0x0
```

**证据 3：相似的另一次 vacuum 前 burst**（line 2125-2134，10:30:50 场景）
```
10:30:50.165 shadow save 17287 chars
10:30:50.357 bgm_play
10:30:50.420-424 data_query x4
```
（这次 vacuum 起点是 10:26:27.024 `lost fg=0x0`，218 秒长，前驱 burst 距离更远——所以相关性不是 100%）

**证据 4：bootstrap 收尾的 QQ 抢焦正常恢复**（line 999-1003，对照组）
```
09:57:21.021 [Activation] gained gapMs=5844
09:57:21.024 [Panel] EX_STYLE idle-full
09:57:21.028 [Activation] lost fg=QQ
09:57:21.033 [Activation] gained gapMs=15
09:57:21.044 [FocusRestore] panel_close:idle:native_hud:set_ready pass1=fail pass2=ok ... fgBefore=QQ pid=29812 ...
```
这是 panel close 路径在 QQ 是前台时强行 AttachThreadInput 拉回前台——**这条路径工作正常**，区分性强。

### 8.4 交付物格式

无论哪条 Track，请用以下格式回报：

```markdown
## Track [A/B/C] 结论

### 关键发现
- 1~3 条要点

### 证据
- 文件:行号 / 日志片段

### 假说更新
- 哪些 §5 候选应该升级 / 降级 / 删除

### 下一步建议
- 1~3 条具体动作，包含 file:line 指针
```

并直接编辑 `docs/焦点管理-诊断与卡顿排查-2026-05-24.md` 的 §5、§6 历史、§8 结尾，把结论固化进文档；**不要新建独立报告文档**——分散会让下一轮接手者找不到。

### 8.5 不要做的事（避免互相踩脚）

- ❌ **不要动 4 层防御的任何文件**：`AppActivationState.cs` / `KeyboardHook.cs` / `WindowManager.cs`（除了 Track A 允许加 public probe 方法）/ `PerfDecisionEngine.cs` / `GuardianForm.cs` 的 `OnForegroundWatchdogTick` 主体。**只加诊断，不改逻辑。**
- ❌ **不要改 PerfGate cooldown / 去抖窗口数值**：已在 §6 已否决方案里。
- ❌ **不要新建 QQ / 应用类名白名单**：测试员侧已排除 QQ 抢焦作为本次卡顿主因。
- ❌ **不要把诊断日志直接打到现有 `[FgWatchdog]` / `[Activation]` 通道**：新增独立前缀（如 `[BgWatchdog]` / `[UIFreezeProbe]`），便于回滚和过滤。
- ❌ **不要尝试"顺便简化一下"焦点架构**：参见 §6 已否决方案。

### 8.6 协作同步点

任意 Track 出结果后：
1. 把发现追加到本文档 §5 对应候选（更新状态：升级 / 降级 / 否决）。
2. 在 §6 历史时间线加一行（日期 + 谁 + 改动 / 发现）。
3. 若 §8.5 的某条"不要做"被新证据推翻，**先回头跟 dev 确认**再开始动手——这些是过去 5 轮迭代换回来的硬约束。
4. 仓库 `MEMORY.md` 中 `focus-management` 入口若涉及修改，同步更新一行 hook。

### 8.7 Track A/B/C 结论回填（2026-05-24 Codex）

## Track A 结论

### 关键发现
- 已新增独立于 UI 线程的 `UiFreezeProbe` 后台线程，默认随 `GuardianForm.BindWindowManager` 启动，每 1 秒采样一次；只观察 + 打 `[UIFreezeProbe]`，不抢焦、不调用 `RestoreFlashInputFocus`。
- 探针会记录三类证据：前台 NULL 持续 ≥3 秒的 `vacuum_seen seconds=N ui_tick_age=Mms`、`IsHungAppWindow` 的 `hung_window flash=<bool> guardian=<bool>`、UI watchdog tick 超过 2 秒未更新的 `ui_thread_stale age=Mms`。
- `OnForegroundWatchdogTick` 仅在头部更新时间戳与 Guardian hwnd 快照；原有 4 层防御逻辑、去抖窗口、PerfGate cooldown、watchdog 回收条件均未改。

### 证据
- `launcher/src/Diagnostic/UiFreezeProbe.cs:17`：新增探针类；`launcher/src/Diagnostic/UiFreezeProbe.cs:142` / `:167` / `:182`：三类诊断日志。
- `launcher/src/Guardian/GuardianForm.cs:189` / `:204`：`BindWindowManager` 后启动探针；`launcher/src/Guardian/GuardianForm.cs:740`：watchdog tick 时间戳采样；`launcher/src/Guardian/GuardianForm.cs:609` / `:1713`：退出与 dispose 时停止探针。
- `launcher/CRAZYFLASHER7MercenaryEmpire.csproj:180`：编译包含 `UiFreezeProbe.cs`。

### 假说更新
- §5.2 “看门狗在长真空中根本不触发”升级为当前首要待证伪假说：下一轮日志应直接回答 UI tick 是否在 vacuum 期间停摆。
- §5.6 “UI 线程冻结导致 Watchdog 死亡”保持主嫌疑，但因 Track B/C 只给出部分相关 / 无高嫌疑同步等待，必须等待 `[UIFreezeProbe]` 证据再决定修复方向。

### 下一步建议
- 测试员复现后优先截取 `[Activation] lost fg=0x0` 到 `[Activation] gained` 期间的 `[UIFreezeProbe]` 日志；若 `ui_tick_age` 随 vacuum 一起增长，沿 `launcher/src/Guardian/GuardianForm.cs:738` 的 UI Timer 死亡路径继续查。
- 若 `hung_window flash=true`，优先查 Flash 子窗口消息泵 / 场景加载；若 `guardian=true`，优先查 launcher UI 线程同步等待或锁。
- 不要让 `UiFreezeProbe` 做自动恢复；若未来要把真空回收迁到非 UI 线程，先按 §8.5 回头与 dev 确认。

## Track B 结论

### 关键发现
- 测试员日志 `C:\Users\Administrator\Downloads\launcher (1).log` 中共有 17 个 `[Activation] lost fg=0x0`；dev 对照 `logs/launcher.log` / `.1` 未出现同类真空入口。
- 严格按 vacuum 前 5 秒统计，7/17（41.2%）命中 shadow/bgm/data_query/panel burst；`加载场景背景` 在前 5 秒窗口内 0/17 命中。
- 结论不是“场景切换 burst 必然导致 vacuum”，而是“部分 vacuum 紧贴 shadow/bgm/data burst；另有多数 vacuum 前 5 秒无明显 burst”。

### 证据
- `C:\Users\Administrator\Downloads\launcher (1).log:2080`：10:19:43.264 长 vacuum，前 5 秒含 shadow + bgm_play + data_query x4。
- `C:\Users\Administrator\Downloads\launcher (1).log:2315`：10:35:15.119 vacuum，前 5 秒含 shadow + bgm + data_query + Panel EX_STYLE。
- `C:\Users\Administrator\Downloads\launcher (1).log:3686`：11:11:59.546 vacuum，前 5 秒含 shadow + bgm + data_query；但 `:2116` / `:2715` / `:3527` 等长 vacuum 前 5 秒无 burst。

### 假说更新
- §5.6 的“场景切换 burst → UI hang”从“强相关”降为“部分相关，适合作为 Track A 优先复现场景”，暂不作为单独修复依据。
- §5.2 不受降级影响：无论是否有 burst，长 vacuum 中 `[FgWatchdog]` 不动仍是结构性待证问题。

### 下一步建议
- 下一轮复现时同时看 `[UIFreezeProbe]` 与 burst 指纹；如果只有 burst 命中的 vacuum 出现 `ui_thread_stale`，再回查 shadow save / bgm / data_query 的 UI 线程交互。
- 若大量无 burst 的 vacuum 也出现 `ui_thread_stale`，把根因范围扩大到系统前台/Flash hwnd/窗口消息泵，而不是只盯场景切换。
- 可以补一个离线分析脚本，但不要新建长期报告文档；结果继续回填本文 §5 / §8。

## Track C 结论

### 关键发现
- 指定触点未发现 `.Result` / `.Wait()` / `.GetAwaiter().GetResult()` / `SendMessage()` 这类“UI 线程同步等待 WebView2 / 跨进程返回”的高嫌疑点。
- WebView2 发送路径主要是 `PostWebMessageAsJson` 与 `ExecuteScriptAsync`，当前没有同步取返回值；大量跨线程入口使用 `BeginInvoke`，不是阻塞式 `Invoke`。
- `InputShieldForm` 存在串行 `await CallDevToolsProtocolMethodAsync` 的 CDP 注入队列，但这是异步等待，不是 UI 线程同步等待；除非 Track A 证明 browser/CDP 相关 hung，否则先不重构。

### 证据
- `launcher/src/Guardian/WebOverlayForm.cs:1535` / `:1547` / `:1558`：PostToWeb 通过 `BeginInvoke` 回 UI 后 `PostWebMessageAsJson`，`ExecuteScriptAsync` 未 `.Result`。
- `launcher/src/Guardian/BootstrapPanel.cs:175` / `:188`：Bootstrap PostToWeb 同样是 `BeginInvoke` + `PostWebMessageAsJson`。
- `launcher/src/Guardian/InputShieldForm.cs:649`：CDP 注入为 `await _targetWebView.CallDevToolsProtocolMethodAsync(...)`；`InputShieldForm.cs:462` 等 `m.Result` 是 WinForms `Message.Result`，不是 `Task.Result`。

### 假说更新
- §5.6(b) “WebView2 / Chromium 子进程同步 IPC 阻塞 launcher UI 线程”降为低/中嫌疑：当前没有静态高危同步等待证据。
- 仍保留 WebView2 方向作为后续分支：如果 `[UIFreezeProbe] hung_window guardian=true` 或 CDP/browser 相关时间点对齐，再审异步队列饱和、browser process hang、DevTools Protocol 调用耗时。

### 下一步建议
- 暂不直接改 WebOverlay / PanelHost 调用点，避免违反 §8.5 的“先列清单再对齐优先级”。
- 若 Track A 新日志显示 UI stale 但 `IsHungAppWindow` 两端均 false，优先查 .NET 进程内锁 / UI 消息泵饥饿；WebView2 同步等待不是第一批修复候选。
- 若需要继续 Track C，下一步可给 `InputShieldForm.cs:629` 的 CDP 队列加独立耗时诊断前缀，但仍只加日志，不改交互语义。

---

## 9. 突破：UI 冻结根因强候选已定位（2026-05-24 晚续，A.1 阶段交接）

> ⚠️ **§9.1 ~ §9.6 的"reveal watchdog 是 7s 冻结主因"结论已被 §9.7 推翻**。本节保留作"调试史的错路标本"——读它的价值是看到"reveal 同步链"这条路已被走过且证伪，**不要照着改代码**。
> **当前终态请直接读 §9.7 + 姊妹文档 §7**。

### 9.1 TL;DR

UiFreezeProbe 加 enter/exit 边界后实测到 **wall=7000ms maxAge=8985ms** 的 boot 期 UI 冻结。**触发时刻精确对齐 `bootstrap_reveal_ready` watchdog 兜底**。Track A / B / C 的部分假说由此被精确化或排除。详细分析、时间轴、A.1 下一步在 [`docs/诊断设施与首轮基线-2026-05-24.md`](诊断设施与首轮基线-2026-05-24.md) §5.1 — 后续 A 线工作请以那份为准，本文档保留作为投入产出史。

### 9.2 关键时间轴

```
13:00:04.003 LaunchFlow → Ready                      ← Flash 早已就绪
13:00:04.7~10.5  toast: 各种数据加载完毕             ← 游戏侧加载中, UI 还活
13:00:13.832 (推算) UI tick 最后一次                  ← 死于此时
13:00:14.004 [LaunchFlow] Flash reveal watchdog fired after 10000ms
             SWF 可能未部署 sendRevealReady           ← 兜底触发 = 冻结起点
13:00:14.019 [Bootstrap] WebView2 suspend failed: ...disposed
13:00:20.820 [UIFreezeProbe] hung_window flash=True guardian=True
13:00:22.630 title="CF7:FlashNight (未响应)"          ← Windows shell 自己加的
13:00:23.823 [UIFreezeProbe] ui_stale_exit wall=7000ms maxAge=8985ms
```

### 9.3 对 Track A/B/C 结论的更新

| Track | 原结论 | 新发现后的状态 |
|---|---|---|
| **A**（场景切换 burst → UI hang） | "强相关" 后降为 "部分相关" | **进一步精确化**：UI hang 假说**成立**（实测 wall=7000ms），但触发场景**不是** gameplay 切换 burst，而是 **boot 期 reveal watchdog 兜底**。boot vs gameplay 是两个独立时段，B 线的 burst 假说还是 boot 阶段的 burst（reveal 兜底叠加多个 SetReady）。 |
| **B**（场景切换 burst） | "部分相关，适合作为 Track A 优先复现场景" | **触发场景已锁定**：不是用户操作触发的场景切换，是启动期 SWF 没发 `bootstrap_reveal_ready` → 10s watchdog → 兜底 reveal 路径在 UI 线程做同步重活。`shadow save / bgm / data_query` 等运行期路径**与本次实测的冻结无关**（gameplay 阶段 UI 一直活，见时间轴）。 |
| **C**（WebView2 同步等待审计） | 没发现 `.Result/.Wait()`，降为低嫌疑 | **审计范围漏了 dispose 生命周期**：Track C 看的是 **runtime** 调用（PostToWeb / ExecuteScriptAsync / CDP 队列），但兜底 reveal 路径里执行的是 **bootstrap WebView2 `Dispose()`** —— WebView2 Dispose 是已知阻塞主线程的同步 COM 调用（Chromium subprocess 退出协议）。Track C 应该把 bootstrap.Dispose() 的调用点（[`launcher/src/Guardian/BootstrapPanel.cs`](../launcher/src/Guardian/BootstrapPanel.cs) 和 [`launcher/src/Guardian/GameLaunchFlow.cs`](../launcher/src/Guardian/GameLaunchFlow.cs) 的 reveal 段）加进嫌疑面，作为 A.1 的第一查证目标。 |

### 9.4 兜底 reveal 路径同步链（强候选）

```
bootstrap WebView2 dispose             ← 跨进程 COM 同步, 单点最可疑
  → suspend on disposed CoreWebView2   ← 上面那条 dispose error 的来源
  → panel swap (BootstrapPanel → FlashHostPanel 可见性切换)
  → DesktopCursor.SetReady
  → NativeHud.SetReady
  → HitNumber.SetReady / WebOverlay.SetReady
  → readyWiring 完成 + DiagnosticsBootstrap.ReadySnapshot()
```

WebView2 `Dispose()` 单点最可疑，但 panel swap + 多个 overlay SetReady 串行叠加也能凑够 7 秒。**A.1 的工作 = 用 `PerfTrace.Scope` 在每段前后打时间戳，定位最贵那一段**，不改代码。

### 9.5 次生 bug：SWF 端 `bootstrap_reveal_ready` 缺失

[`launcher/src/Guardian/GameLaunchFlow.cs`](../launcher/src/Guardian/GameLaunchFlow.cs) 的 reveal watchdog 注释明确："SWF 可能未部署 sendRevealReady"。本身不会让 launcher 冻（兜底应该走得动），但**它触发了**冻结路径。两个 bug 叠加：

- **次生**：SWF 主动 `revealReady` 信号缺失 → 总是走 10 秒 watchdog 兜底
- **主因**：兜底 reveal 路径在 UI 线程做 7 秒同步工作

A.1 同时核查 SWF 端 `scripts/类定义/` 里有无 `bootstrap_reveal_ready` 发送实现 —— 如果根本没写，补 SWF 端 1 行可能就避开兜底；但 launcher 兜底路径异步化也得做，不能依赖 SWF 永远不漏 revealReady。

### 9.6 "已被否决方案" 清单不变

§6 的"已被否决的方案"全部仍然否决。本次定位**不**为以下方案翻案：去抖窗口拉长 / 轮询取代 WM_ACTIVATEAPP / 看门狗单帧采样 / 失焦也停决策 / QQ 白名单 / 缩短 PerfGate cooldown。**真正可走方向是 A.1 数据驱动后的兜底 reveal 路径精细化重构**。

---

## 9.7 二次校准 (2026-05-24 22:00)：reveal 不是冻结源

>  **⚠️ 9.2-9.5 的"兜底 reveal 路径同步链是 7s 冻结主因"已被推翻**。

perf-latest.jsonl 实测：
- `launch.reveal_start = 13:00:14.005` (thread 7→1)
- `launch.reveal_done  = 13:00:15.215` (thread 1)
- **整个 DoPerformReveal 耗时 1.21s**, 不是 7s
- ui_stale_enter 在 `13:00:16.817`, 比 reveal_done **晚了 1.6s**

校准后的事实：
- watchdog fired → DoPerformReveal 都顺利完成
- 7 秒冻结发生在 reveal_done **之后**
- SWF 端 `sendRevealReady` 是有的（[`scripts/通信/通信_fs_bootstrap.as:107`](../scripts/通信/通信_fs_bootstrap.as) + DOMDocument.xml frame 81）, baseline log line 888 显示 SWF 确实发了（只是慢于 watchdog）

校准后的工作（详见 [`docs/诊断设施与首轮基线-2026-05-24.md`](诊断设施与首轮基线-2026-05-24.md) §5.1 / §7 校准版）：
1. **A.1 已落地**：DoPerformReveal + readyWiring 都加了分段 PerfTrace.Scope + RevealProbe 日志, 下一轮 baseline 直接验证 reveal 各段耗时
2. **新嫌犯**：`webOverlay.idle_sequence "full"`（reveal_done 同毫秒触发, 旧日志内部无 sub-trace；新日志看 `[IdleProbe]`）/ EX_STYLE 改写 / FocusRestore 同步阻塞 / stale 段内 UI thread 工作（log 静默不可见）
3. **A.1 真正目标变成**：证伪 reveal 是冻结源, 然后 investigate 转向 `webOverlay.idle_sequence` 内部 + stale 段路径

9.2-9.5 段保留作"调试史的错路标本"，**不要照着改代码**。

## 9.8 续查落点：idle/focus 子分段探针（2026-05-24）

基于 §9.7 的新嫌犯清单，本轮没有改 4 层焦点防御，只补诊断：

- `launcher/src/Guardian/WebOverlayForm.cs`：`webOverlay.idle_sequence` 现在输出 `[IdleProbe] done mode=<full|soft> panel=<name> total=<ms>`；`DoFullIdleSuspend` / `DoSoftIdleRestore` 内部对 `suspend_timers`、`sw_hide`、`ex_style`、`notopmost/z_order`、`transparency`、`restore_focus` 写 `PerfTrace.Duration`，单步 ≥25ms 时追加 `[IdleProbe]` slow 行。
- `launcher/src/Guardian/WindowManager.cs`：`RestoreFlashInputFocus` 现在分段记录 `describe_before`、`pass1`、`attach`、`pass2`、`describe_after`、`total`；launcher.log 只在慢路径、走 pass2 或失败时输出 `[FocusProbe]`。
- `launcher (4).log` 复核结果：有 5 次 `fg=0x0`，但没有 `[UIFreezeProbe] ui_stale_enter` / `hung_window` / `vacuum_seen`，说明这份日志没有复现 13:00 的 UI 线程冻结。最长一次 `gapMs=13390` 期间 Frame/ETW/LogBatch 仍持续输出，属于失活/前台切换，不是 UI 挂死。

下一次复现时，按这条链读日志：

```
launch.reveal_done
webOverlay.idle_sequence "full"
[IdleProbe] ... slow step / done
[FocusProbe] ... pass2/fail/slow
[UIFreezeProbe] ui_stale_enter / hung_window / ui_stale_exit
```

判定规则：
- 若 `[IdleProbe] full.restore_focus` 或 `[FocusProbe]` 总耗时贴近冻结时长，继续查 `SetForegroundWindow` / `AttachThreadInput` 与外部前台窗口。
- 若 `[IdleProbe] full.ex_style` / `full.sw_hide` 慢，继续查 WebOverlay HWND、`SetWindowLong` / `ShowWindow` 与 DWM 状态切换。
- 若 `[IdleProbe] done` 很快但随后 `ui_stale_enter`，冻结源在 idle_sequence 后续 UI 工作，不再盯 panel close 收尾。
