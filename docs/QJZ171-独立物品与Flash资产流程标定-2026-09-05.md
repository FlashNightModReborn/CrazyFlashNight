# QJZ171 独立物品与 Flash 资产流程标定

日期：2026-09-05。本文保存本次策划裁定、可复算证据和已验收的资产事实；日常操作入口为 [美术资产装配](../agentsDoc/art-asset-assembly.md)，数值真源为 [物品 XML](../data/items/武器_长枪_机枪.xml) 与 [balance 台账](../tools/cf7-balance-tool/records/weapon-balance-audit.xml)。

## 设计决策

用户先确认美术流程有效，再要求建立独立物品；定位为 **37 级、1 层高价金币机枪**，60 发弹容、低射速、大威力，以超重步枪式节奏区别于高速压制机枪。888 是认可的威力彩蛋，不为凑数改穿刺、弹药或层数。此次明确授权配置数据及完成 evidence。

| 配置 | 本次值 | 依据 |
|---|---|---|
| 分类 / 使用栏 | 机枪 / 长枪 | 保留机枪身份，不纳入大容量压制机枪 |
| 等级 / 重量 | 37 / 24 kg | 位于 35–40 级段；重量体现重火力代价 |
| 威力 / 弹容 / 间隔 | 888 / 60 / 240 ms | 名义 250 发/分；运行时帧调度和换弹会改变持续射速 |
| 冲击力 | XML 4，面板 125 | 下述反比语义，不把 XML 4 当成面板 4 |
| 弹体 / 弹药 | 巴雷特子弹 / 机枪贫铀穿甲弹 | 穿刺口径由用户确认；弹药目录当前价 8000 |
| 换弹惩罚 / 散布 | 50 / 5 | 以换弹和机动代价约束短点射强火力 |
| 售价 / 入口 | 460000 金币 / Pig 目录 53 | 一层高价购买；未挤占现有货架编号 |

### 巴雷特子弹口径

用户明确确认巴雷特子弹按正常穿刺子弹填写，因此依据 WBR-PIERCE-003 取 `pierce=2`。原资产位于 [巴雷特子弹.xml](../flashswf/arts/原版素材库-子弹/LIBRARY/子弹-普通与穿刺组/巴雷特子弹.xml)，索引与 [bullets_cases.xml](../data/items/bullets_cases.xml) 也存在该链接。该老式弹体依赖自身时间轴处理后续碰撞；名称没有“穿刺”字样，不能把名称分类器的 FLAG 结果当作全部行为证明。

这里记录的是明确的平衡口径，未把未知的逐目标穿透数量等同为运行时实测。没有再叠加反器材、口径或原型名带来的隐含系数。火光、音效及命中特效复用已有巴雷特资源，无新增专属生命周期或技能。

### 冲击力语义

[GunStatsBuilder](../scripts/类定义/org/flashNight/gesh/tooltip/builder/GunStatsBuilder.as) 与 [EquipmentStatProjector](../scripts/类定义/org/flashNight/arki/item/equipment/EquipmentStatProjector.as) 使用 `floor(500 / impact)` 显示面板冲击力；[ImpactHandler](../scripts/类定义/org/flashNight/arki/component/StatHandler/ImpactHandler.as) 以伤害乘 50 再除该原始值累积冲击。故 XML 从 20 改到 4，是面板 25 升到 125。公式同样传 XML 原值 4，不能传显示值 125。

### 独立价格层

设计顺序是先确定购买限定高价金币 +1，再确定穿刺品类及报价，最后反验；不是枚举层数让旧价格入带。

- DPS `weightLayers=1`：唯一贡献 `acquisition.high-price=+1`。
- 审计专用 `priceLayers=1`：用户已选定的高价金币层，符合工作簿 `装备价格!D25`。不把总 DPS 层数普遍映射为价格层；运行时 balance 仍只有原有八输入。
- `category=2`：独立来自 `装备价格!D13` 的穿刺类定价规则；物理 `damageType=1`，长枪价格双枪系数为 1。
- 推荐金币价 `37 × 3900 × 1.6¹ × 2 × 1.6⁰ ÷ 1 = 461760`。
- 现价取整为 460000，`priceRatio=0.9961884962`，落在 0.8–1.25 审计带内。

### 获取路径

新物品只接入 [Pig.json](../data/shops/npcs/Pig.json) 的 `catalog.53`。未配置 K 点、合成、任务、礼包、关卡或敌人装备发放。证据检查器每次读取当前价格和有效等级，并扫描 `data/`、`config/`、`scripts/` 的 XML/JSON 精确名称及 AS 具名引用；定义自身、派生字典、资产索引不算获取路径。出现其他引用时停止确认，先查其语义。

静态具名扫描不证明任意动态拼接脚本都不存在。此次同时核对 [礼包目录](../data/items/消耗品_礼包.xml) 使用显式 entries，及 [单位初始化掉落物](../scripts/逻辑/单位函数/单位函数_fs_aka_玩家模板迁移.as) 只从该单位已有装备生成掉落，不从全物品表自动抽取；QJZ171 未加入任何 NPC 装备。玩家丢弃已购装备、存档修改器和开发测试发放不构成新的正常获取预算。

## 工作簿身份

`workbookVersion=1` 固定 SHA256 为 `BAC3D341DB2B2BF966C3D473ED4793725BAF0B68BE01BA0D2804A76D6DCB840A`。可用 `git show ff5ea2e95a76321a2f14bb445b07b9f85502a7c4:<工作簿路径>` 读取该历史字节，路径见 [工作簿](../0.说明文件与教程/武器-技能数值-价格-合成表填写的参考公式（修改后请勿上传git）.xlsx)。

当前工作区整文件 SHA 为 `357E07955281D4CB3884902B58165E19E97852216AE5A76965D3EBD04FB4E107`，与冻结字节不同。2026-09-05 用 openpyxl 的 `read_only=True, data_only=False` 逐坐标比较两版本：枪械页各 584 个非空单元格，装备价格页各 91 个非空单元格，值/公式差异均为 0。因此本次未更换公式版本或篡改冻结 SHA，也不声称整个工作簿文件相同。

## 公式标定

按 [公式实现](../tools/cf7-balance-tool/packages/core/src/formulas/weapons.ts) 复算，输入为：

```json
{"level":37,"bulletPower":888,"shootInterval":240,"magSize":60,"magPrice":8000,"weight":24,"dualWieldFactor":1,"pierceFactor":2,"damageTypeFactor":1,"shotgunValue":1,"impact":4,"extraWeightLayers":1}
```

| 指标 | 值 |
|---|---:|
| averageDPS | 16593.906415155707 |
| weightedDPS | 15989.131809406106 |
| (averageDPS / weightedDPS − 1) | +3.7824105333% |
| 本轮 ±5% 公式标定 | 通过 |

无 data_*、技能或 lifecycle 覆盖；60 发不领取 ≥125 发机枪的容量层。该模型使用其固定换弹假设，未把 `reloadPenalty=50` 的实战节奏伪装为已精确建模。静态 confirmed / displayEligible 证明公式与证据闭合；用户随后已确认独立物品有效，授权合并上游后推送。

## 资产验收与复用

用户已验收：独立 Codex 素材库 → things-new 共享库挂载 → 主文件 AS 链接 → 纸娃娃 / 两帧图标 / 掉落体量。首轮整枪缩略图改为第一帧 24×24 遮罩机匣特写；第二帧完整枪长从约 61 调到 119.36 世界局部像素后确认有效。数值配置阶段沿用已接受的 CS6 产物；后续单一编辑源清理补跑 XFL 直接发布，见下节。

402 个 DOMShape、14 个分件仍可编辑；装配矩阵与原稿见 [素材说明](../flashswf/arts/new/Codex素材源稿/README.md) 和 [assembly.json](../flashswf/arts/new/Codex素材源稿/QJZ171/assembly.json)。独立物品使用 `icon=Codex-QJZ171`、`dressup=枪-长枪-Codex-QJZ171`；XM214-CageFrame 两处借用值已恢复，存档没有改写。

本轮复用经验已归入 canonical 装配入口及 [XFL 工具投资路线](xfl-agent-工具栈-长期路线-2026-05-24.md)：按已验证操作范围判断能力，不再以“agent 不能生成 DOMShape / icon”或等待数月作为操作门槛。CS6 原生发布、导出闭合与人的最终视觉判断仍有明确作用。

## 复验入口

在 `tools/cf7-balance-tool` 执行 `npm run typecheck`、`npm test`、`npm run roundtrip-check`、`npm run balance-sync -- --check`、`npm run balance-check`。首次同步使用 `balance-sync -- --write`，由台账机械生成运行时 balance 与两种摘要。

仓库根重新派生材料目录与 Web dressup 后运行相应检查、文档治理与 `git diff --check`。最终游戏复验：重启加载数据，在 Pig 购买 QJZ171，核对 37 级 / 60 发 / 888 / 面板冲击 125、穿透与换弹手感；确认已装备 XM214 恢复原外观。此处不要求新的人类批准单。

### 2026-09-05 执行结果

| 检查 | 结果与范围 |
|---|---|
| 类型检查 / Vitest | 通过；24 个测试文件、729 个测试，包括独立价格层、其他来源拒绝及 ledger-only 字段不泄漏 |
| XML 往返 | 99/99 通过 |
| balance-sync check | 38 个物品/profile，0 文件待同步，台账摘要无漂移 |
| balance-check | 38 条检查、0 失败；QJZ171 confirmed、displayEligible=true、无 issue；其他旧记录仍有 22 条原有警告 |
| QJZ171 摘要 | inputDigest 为 fnv1a32:6489b482；sourceDigest 见台账，由工具实际生成 |
| 材料目录 | derive 与 --check 通过 |
| Web 恢复 | 两把枪的物品→skin 映射、导出元数据与图片均存在；XM214 定向重新导出的 PNG 与 HEAD 原文件逐字节一致 |
| 烘焙报告 | XM214 选中/解析/导出均为 1；symbolErrors、exportErrors、metadataErrors、missingSymbol、missingFrame 均为空 |
| 数据与资产静态检查 | 独立条目唯一、现役 list 加载、Pig 目录正确；XM214 条目与 HEAD 原值一致；QJZ171 导出、MovieClip 命名接口、遮罩及第二帧存在 |
| 文档 / 差异 | 文档治理、案例本地链接与 diff --check 通过 |

本机首次 FFDec EXE 导出报告显示 Java 未发现，虽然脚本退出码为 0；改用已验证的 Animate Java 17 与 `tools/ffdec/ffdec.bat` 后上述导出成功。该经验已归入装配入口。临时完整报告保存在 `tmp/codex-qjz171/`，复验命令与持久依据由本页及结构化台账承载，不依赖提交临时日志。

## 单一编辑源清理与上游同步

用户确认新物品有效后指出 FLA/XFL 双份源会污染搜索。Codex 库已由 CS6 直接打开 `Codex专用素材/Codex专用素材.xfl` 并发布到现役 `flashswf/arts/new/Codex专用素材.swf`，Compiler 为 0 错误、0 警告，产物实际刷新。无需另存同名 FLA，旧 FLA 已移出素材树，临时备份位于忽略的 tmp。

原生 XFL 再发布使 SWF 从 10965 变为 10960 字节；纸娃娃导出（919×251）及图标第 1/2 帧（各 128×128）与已验收图片 **RGBA 逐像素一致**，三个导出链接完整，图标 bakeProvenance 已刷新绑定新 SWF。这是源迁移验证，不重复要求人类做已通过的视觉验收。

全 flashswf 检查另发现 `3XD的素材.fla`（14040815 字节）：

| 维护证据 | 记录 |
|---|---|
| XFL 最初建立 | `465d242ea1`，2025-11-24，提交“打成XFL” |
| FLA 最后更新 | `70b506fb27`，2026-01-31，提交“修正mk23出框的问题” |
| 作者最新维护 | `00c4ee2860`，2026-09-05，3xd123456 提交“添加新武器和数据调整”；更新 XFL 和 SWF，没有更新 FLA |

内容比较发现旧 FLA 与现役 XFL 有数百个元件差异；维护历史说明旧副本已经停更，而非仍有独立编辑职责。因此删除旧 FLA，保留现役 XFL/SWF；旧文件仍可从 Git 历史恢复。只读引用检查未发现工具或运行时依赖该 FLA。`things-new.fla` 没有由现役完整 XFL 接替，继续保留。

该经验归入 canonical 装配入口，并在原有 `audit.py` 增加第 8 项双份源提醒；提醒不自动删除或强迫重编历史库。上游 `f2b00af435` 的核电站地图资产修改已无冲突快进合入，重扫素材索引时同步收录其新增导出；未把该上游关卡动画问题扩展为本次武器任务。

## 2026-09-06 材质与闭盖弹箱翻新

按用户提供的 `QJZ171-Closed-Ammo-Box.zip` 装配新版。包 SHA-256 为 `6aac1b9f9ce8238b39757c7e9e4b2c371b5b8606cf60c7084bbfd56bd7296a93`；导入最终 `XFL/LIBRARY/QJZ171_质感升级` 的原生矢量，沿用现有 CS6 / AS2 工程外壳。作者 SVG、弹箱布局、枢轴和材料配方同步到 [源稿目录](../flashswf/arts/new/Codex素材源稿/QJZ171/)，逐元件来源与现役命名对应见 [assembly.json](../flashswf/arts/new/Codex素材源稿/QJZ171/assembly.json)。

原有 14 个枪身分件的局部矩阵全部保持；新增远侧完整弹箱及箱体、闭合上盖、提手三个子件。美术从首轮 402 个形状变为 341 个形状、106 个渐变、19 个图形元件；带游戏外壳的完整库为 27 个元件。`动画` MovieClip、握持与枪口、24×24 图标特写及掉落帧外壳与翻新前 XML 语义一致，物品数值与购买入口保持。

本机 Flash CS6 `WIN 12,0,0,481` 完成 15 个总装实例、3 个弹箱子件的移动复位，实际进入箱体 / 上盖 / 提手的 3 / 2 / 2 个细节层，原生保存现役 XFL 并发布。341 组形状边界、填充方向与层级比较通过；CS6 写回时会重排边段并量化渐变矩阵，最大系数差为 `1/65536`，没有将其宣称为跨版本 RGBA 零差。XFL audit、命名与 Include 干运行检查均通过，linkage 全量重扫没有索引语义变化。

本轮 Compiler Errors 为 **0 错误 / 0 警告**，发布 `Codex专用素材.swf` 为 **11610 字节**，SHA-256 `35cb07103341ee63aec5545ff9192e9c04cfd338127a423c18c25400455fa375`。实际 SWF 包含图标、纸娃娃与库锚点三个导出，保留 `动画` / `枪口位置` 实例，位图标签为 0。现役 `things-new.swf` 的 ImportAssets2 继续通过 `flashswf/arts/new/Codex专用素材.swf` 消费同一锚点，本轮发布归属为该独立素材库。

经素材工作台共享 CLI `bake --item QJZ171 --kind all` 生成候选，再 `apply --job 68e3a44dd520414191f4579126db9fca` 应用；两帧图标与纸娃娃 PNG 已更新，导出无错误。图标清单只改变 `Codex-QJZ171`，dressup 清单只改变 `枪-长枪-Codex-QJZ171`，物品映射和 rig 保持；图标 provenance 绑定上述新 SWF。浏览器工作台已查看男女长枪站立、空手背负及完整素材画面。该检查属于生产 Web 组件的本机预览，不代替正式游戏中的射击、遮挡与掉落人工体验复验。

原生检查、编译诊断、前后备份及本机预览保存在忽略的 `tmp/asset-handoff/qjz171-refresh-20260906/`，工作台候选与应用备份保存在对应任务目录；临时编译校验结束后逐字节恢复现役编译脚本。当前编辑真源继续为完整 XFL，没有引入第二份现役 FLA。

同日后续已增加由成功射击事件驱动的枪管后坐与复进，当前动画配置、最终 SWF 和 AS2 集成证据见 [枪管动画与可复用制作流程](QJZ171-枪管动画与可复用制作流程-2026-09-06.md)。本节保留静态翻新节点的发布记录。

## 2026-09-06 减重与连射节奏调整

用户确认 1.6 m 素材使动作更易辨认，随后明确授权按 **20 kg、270 ms** 调整。37 级、888 威力、60 发弹容、冲击原值 4、弹药 8000、DPS 高价金币层 1、价格层 1、穿刺系数 2 和售价 460000 均沿用已有裁定。描述同步突出轻量化携行与较慢连射；不改变枪口火光、弹体或换弹惩罚。

20 kg 是不带三脚架、带满弹箱的游戏制作估值，不声称为型号精确称重。公开原文给出含两脚架枪体 12 kg，三脚架另计 6 kg；其中每 60 发“减轻 1.2 kg”是轻弹的减重幅度。[资料原文](https://modernfirearms.net/en/machineguns/china-machineguns/qjz171-12-7mm-2/) 同口径完整枪弹的一个厂商样本为 125–132 g；结合轻弹减重假设只用于估算量级，不能替代国产具体弹种、弹链和空箱的实测。[厂商样本](https://azersilah.com/en/mehsul/63)

本轮只读重核工作簿：当前文件仍为 `357E07955281D4CB3884902B58165E19E97852216AE5A76965D3EBD04FB4E107`，与冻结版本相比，枪械页 584 个、装备价格页 91 个非空单元格的值与公式差异均为 0。按现有公式实现复算：

| 指标 | 24 kg / 240 ms 历史值 | 当前 20 kg / 270 ms |
| --- | ---: | ---: |
| averageDPS | 16593.906415155707 | 14848.736221761434 |
| weightedDPS | 15989.131809406106 | 15244.921728371752 |
| averageDPS / weightedDPS − 1 | +3.7824105333% | −2.5988031534% |
| ±5% 公式审计范围 | 通过 | 通过 |

以上是公式结果，固定 900 ms 换弹项未精确模拟 `reloadPenalty=50` 或运行时帧取整。`EnhancedCooldownWheel.addTask` 在 30 FPS 下向上取整，基础 240 ms 为 8 帧、270 ms 为 9 帧；在无射速加成时，后者对应约 300 ms 的调度间隔。动画因此同步扩展为 **1 帧待机 + 9 个动作帧**，第 4–5 帧停留在后坐终点，再复进到第 10 帧。

平衡台账的 lifecycle 证据更新为 `WBR-LAYER-002`：当前控制器只写动画帧，不改变成功射击、伤害、射击间隔、弹容或弹药。纯视觉动画不会增加额外 DPS 层；compact profile 与两种 digest 由现有 `balance-sync` 生成，验证结果及素材发布记录见 [动画案例](QJZ171-枪管动画与可复用制作流程-2026-09-06.md)。

具名扫描最初命中 `Qjz171LiveAnimationTest.as` 中构造的测试物品。复核调用点后，将样例身份、物品 XML、SWF 和导出链接统一放入专用 TestLoader 模板参数，测试类继续通过 `BaseItem` 和生产生命周期入口检查真实素材。该入口仅用于聚焦测试，未加入 asLoader 启动或游戏发放路径；金币获取检查器保持原规则。

## 2026-09-06 冲击力与自带暴击补偿

用户确认 20 kg / 270 ms 的动画效果有效，随后明确要求使用巴雷特的冲击力档位，并补充自带暴击。当前配置为 **`impact=2`（面板 250）**、**`criticalhit=5`（5% 概率，1.5 倍伤害）**。20 kg、270 ms、888 威力、60 发弹容、换弹惩罚、弹药与既有动画保持。共用巴雷特的子弹和枪火不自动继承其武器属性；本次只改 QJZ171 的两个字段。

暴击采用既有数值型机制：`DressupInitializer` 从 `data.criticalhit` 构造概率函数，`ShootInitCore.createCritLogic` 将它传到子弹，`CritDamageHandle` 把可暴击的 `破坏力` 乘以返回倍率。数值 5 表示 5% 概率，命中判定成功时倍率为 1.5，平时为 1；没有新增满血条件、专属脚本或暴击公式。

| 口径 | 当前结果 |
| --- | ---: |
| 基础 averageDPS（formula 1） | 14848.736221761434 |
| 基础 weightedDPS（formula 1） | 15202.110963794898 |
| 基础公式残差 | −2.3245110029% |
| 可暴击破坏力的期望倍率 | `1 + 0.05 × (1.5 − 1) = 1.025` |
| 将独立毒伤留在倍率外的简化期望 DPS | 15212.824502528285 |
| 上述简化期望相对基础 weightedDPS | +0.0704740201% |

5% 暴击用于补充既有输出余量，不另赠加权层。工作簿 `枪械!C32` 的 25% 暴击上限占一层是上限参照，不把本枪的 5% 提升到 25%，也不据此新增一层；DPS `weightLayers=1` 及独立价格层 1 仍来自原金币获取裁定。现役 35 级 ValmetM78 的 `criticalhit=5` 是同等级附近的数值型使用先例。

**公式与效果分列**：当前 formula 1 没有 criticalhit 输入，所以正式 `balance-check` 仍报告基础残差 −2.32%。补充估算把公式中独立毒伤约 285.205 DPS 留在暴击倍率外，仅将其余部分乘以 1.025；若粗略把全部伤害统一放大，则约为 +0.1174%。这些是设计估算，不是已实测的整场 DPS，未精确模拟闪避、格挡、护盾阈值、穿刺后续命中和过量击杀。5% 本体暴击在运行时入口处的期望倍率有明确代码依据，其他效果不借此改写八个基础系数。

台账以 `WBR-LAYER-003` 记录已量化的内建效果，`WBR-DPS-003` 单独引用其期望口径，`sourceDigest` 绑定包含 criticalhit 的有效数据。AS2 使用的 v1 `inputDigest` 仍按既有八输入及机械字段计算，不声称该紧凑摘要单独覆盖暴击字段。数据与台账由现有同步器更新；本次不修改生产 AS2、XFL、共享弹体或任何 SWF。
