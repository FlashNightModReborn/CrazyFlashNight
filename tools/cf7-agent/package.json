{
  "name": "cf7-agent-client",
  "version": "1.0.0",
  "private": true,
  "description": "Dependency-free CF7 Agent Runtime v1 client, JSONL CLI, and MCP stdio adapter",
  "scripts": {
    "test": "node --test tests"
  },
  "engines": {
    "node": ">=20"
  }
}
