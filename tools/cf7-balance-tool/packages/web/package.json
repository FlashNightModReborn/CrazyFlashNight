{
  "name": "@cf7-balance-tool/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.electron.json --pretty false",
    "typecheck": "tsc -p tsconfig.json --pretty false && tsc -p tsconfig.electron.json --pretty false",
    "dev:renderer": "vite --host 127.0.0.1",
    "dev:electron": "tsx src/electron/main.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "@cf7-balance-tool/core": "0.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}