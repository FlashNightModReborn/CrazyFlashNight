trace("FocusedTestRunId item-panels-crafting Start: __FOCUSED_RUN_ID__");
// 与默认 All suite 使用完全相同的生产 leaf 初始化；Crafting 只缩减测试调用，
// 不缩减被测运行时闭包。
_root.preloaders = _root.preloaders || [];
_root.loaders = _root.loaders || [];
#include "逻辑系统分区/商店系统_兼容.as"

org.flashNight.arki.item.CraftingPanelServiceTest.runAllTests();
trace("FocusedTestRunId item-panels-crafting Complete: __FOCUSED_RUN_ID__");
