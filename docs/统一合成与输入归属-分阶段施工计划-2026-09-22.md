# 统一合成与输入归属：分阶段施工与 Kimi Code 接班计划

**文档角色**：本专项阶段范围、任务归属、验证出口与续接记录的唯一施工入口。<br>
**最后核对代码基线**：commit `0a65b1162ee0a2f239a6afc82d6bceada8a1f179`（2026-09-22）。<br>
**当前状态**：规划完成，尚未施工；A0/C0/C1/B 的测试均未执行。<br>
**授权边界**：本次仅授权产出文档、提交、推送；接班者取得用户施工指令后再改代码。文档、原始回执和 Git 提交本身不授予运行真实存档、正式发布、外部消息或后续 push 权限。

## 1. 接班从这里开始

目标：保留已发布的捕获、光照、动态分辨率成果，让游戏内容逐步进入统一呈现与输入体系。首批建议范围是 **有限 A0 → C0**；用实测决定 C1，接口稳定后才批量迁移 HUD。粒子与天空/地图拆分后置。

三份材料各司其职：

- [本计划](#2-裁决采纳与待定项)：施工时读范围、出口和最新续接记录。
- [Pro 原始回执](裁决存档/统一合成与输入归属-GPT6Pro原始裁决-2026-09-22.md)：外部分析原文，不是运行证据；核对具体论证时按需读。
- [裁决准备报告](reports/统一合成与输入归属-GPT6Pro裁决准备-2026-09-22.md)：冻结输入、历史故障与 F1–F8 来源。原 85 文件材料固定于 `73c258af5d`，部署提交仅更新其中两份文档，受审代码未变。

设计理由继续归[长期渲染 ADR](launcher-渲染架构-长期决策-2026-05-21.md#72-2026-09-22统一合成与输入优先的顺序评估)，现有开发命令归[捕获合成 README](../launcher/perf/flash-compositor/README.md)。不要依赖 `Downloads`、本机聊天历史或原始 ZIP 才能接班；Git 中已有回执、上下文摘要和实际源码。

开始前只做以下复核，命中变化再扩读：

1. `git status --short`、`git log -5 --oneline`；与上述基线比较本阶段命中文件，保留其他人的修改。
2. 按 AGENTS 路由读取 Host/跨层/诊断验证；涉及 AS2 才读 AS2 与 CS6 合同，涉及持久写才进入存档合同。
3. 确认 Kimi 的实际模型/工具可用、Devin 指定模型可用及用户授权的消费范围；本机曾用 `swe-2-max`，不是未来可用性保证。不得用旧 CLI 参数或旧配额记录代签。
4. 做一次能力检查：构建与日志工具、正确 PID/HWND 的观察、截图或人工取景、实验恢复方式。没有桌面工具不阻止 A0，但 C0/C1 的对应项目必须交给人类或标未验证。
5. 第一份工作结果应是复核差异和当前首个子任务，不重新全文调查全仓，不立即启动所有 lane。

## 2. 裁决采纳与待定项

| 内容 | 本计划处理 | 理由 / 边界 |
|---|---|---|
| A0 → C0 → C1＋Native 样本 → B | 采纳为分阶段路线 | 先证伪最大未知；失败可以收缩，不一次重写 |
| F1 多 HUD 诊断覆盖 | 纳入 A0 修复 | 诊断缺陷成立，不等于业务 owner 被覆盖 |
| F2 主 HUD 层序维护 | 保留为待复现线索 | 缺订阅不等于生产可达故障；强制重排负控与正常入口分别记录 |
| F3/F4 桥旧包与投递结果；F8 本地消费观测 | 纳入 A0 | 发送、接收与业务执行必须分层；两端配套 |
| F5 首帧就绪与持续活性 | 纳入 A0/C0 观测 | 静态/暂停不按固定帧龄判死，不自动杀进程 |
| F6 UI 阻塞与低级 hook | 纳入受控实验 | 不能声称旧现场 hook 已被移除；不在 A0 顺手重写整条线程模型 |
| F7 去抖与立即取消 | 收敛解释，保留立即输入撤销 | 显示防闪烁、性能调度和即时输入资格不是同一事实 |
| NULL 前台看门狗自主恢复 | 纳入 A0 收窄 | 保留观测；无当前用户返回/会话内交接资格时不得自行夺焦 |
| 普通 FocusRestore 成功谓词 | 纳入 A0 核实与修补 | 分开 API 返回、前台根匹配、实际内部 focus；不要简单复用同一个布尔参数改变全部含义 |
| S/P/F 两顶层根拓扑 | C0 候选，不是已成立结构 | 要验源覆盖出帧、反馈负控、任务栏/Alt-Tab、真实键盘与恢复 |
| 返回应用第一击只激活 | **产品待定** | Pro 建议不直接成为需求。首样保留当前体验基线，隔离可比较的策略；需要改变首击语义时向用户展示具体差异再决定 |
| surgery 草稿/取消作为 C1 面板 | 采纳为首个验证对象 | 用真实 opener 和页面；首轮不执行付费应用 |
| Native 按 tile/脏块复用缓存 | 采纳性能边界 | 不把多个小窗口改成每帧整屏 CPU 位图 |
| 单 HWND 多 visual | 可作为首阶段出口 | 不必取得 Web 可采样最终纹理；不承诺 Independent Flip/MPO 或固定收益 |
| D 全部一次替换 / E 默认换 Flash 宿主 | 不采用 | E 仅在实测能力阻塞后独立裁决 |

前台看门狗与成功谓词已在本次规划中回查源码；其修补效果仍未测试。其他外部代码结论由承接 lane 对当前基线再次核对，发现反例就收窄，不能为符合回执而改测试。

## 3. 不变量与最小接口

### 3.1 责任边界

- `PanelHostController` 继续拥有 exact 面板实例、打开关闭和原暂停交接；协调器只读取现有身份，不复制业务状态机。
- AS2 继续拥有复活扣币、奖励、存档、交易、退出与玩法。焦点/图形失败不能重发业务写、解锁未知结果或推断成功。
- 键盘逻辑目标、指针手势目标、真实 OS 焦点分别表达。`InputEpoch` 不是业务 revision，HWND/PID 数值不是完整端点身份。
- 每个 Down 只选一次目标；Up/Cancel 只能结束该手势。失焦、替换、捕获丢失或无效几何撤销旧资格；Cancel 不合成 Click，也不默认给 Web 补 MouseUp。
- 世界源、HUD、Web 的坐标变换与当前可交互呈现使用同版几何；出队不能把旧坐标直接套进新尺寸。桥收到旧包时必须在 SetFocus/原窗口过程之前拒绝。
- 保留已发布 AS2 中性化与 Host 光照配对；关闭合成器、露出原 Flash 不是当然正确的回退。失败回到完整配套候选，不单独替换桥 DLL。

### 3.2 先约定语义，不提前造通用框架

主控在 A0 开始时把下列字段落到本阶段实际类型中，更新本节映射；不再另建一份永久协议正文：

| 概念 | 必须表达 | 不能冒认 |
|---|---|---|
| 端点身份 | 当前 Flash 实例、HUD HWND/实例、panel instance/document generation | 同 PID 全部合法；只用 focus generation 代表 exact panel replacement |
| 输入信封 | 序号、输入代次、手势归属、原时钟域、几何版本 | 增改 AS2 封闭业务消息只为追踪方便 |
| 投递状态 | 本地拒绝/消费/透传、未入队/已入队、端点接收、原处理返回 | `delivered=true` 代表全部成功 |
| 恢复资格 | 来自仍有效的用户返回或会话内交接；执行前复核 | NULL 持续时间替代用户意图 |
| 呈现状态 | 首帧准入、尺寸/几何匹配、设备健康、预期是否推进、活性未知 | 时间戳证明语义 scene 或已扫描到屏幕 |
| Native 更新 | tile/内容与几何代次、局部边界/clip、预乘 alpha、脏区、buffer 生命周期 | 每帧全屏重画上传 |

增量复杂度限于一个协调入口、Flash/Native/Web 三类适配、一个呈现根。A0 不要求提前实现全部协调器；通过窄接口接入现役保护。新共享结构、字段或文件必须有本阶段消费者，不预建任意 provider、通用场景图或持久事务框架。

## 4. 首批 A0：有限防护与观测

按 A0.1 → A0.2 → A0.3 的集成顺序推进。可以并行只读审阅/独立测试准备，不能并行修改相同文件。

| 子任务 / 归属 | 最小施工 | 必须验证 | 出口 |
|---|---|---|---|
| A0.1 多表面诊断；`Diagnostic/FocusTrace.cs`、`FocusWindowSnapshot.cs`、`NativeHudOverlay.FocusDiagnostics.cs` | 用实例/HWND 注册与注销替代全局唯一 HUD 槽；采样归实际接收表面；本地消费分支补明确观察 | 主/bottom/buff 共存、最后实例销毁、错误线程、失效句柄；原事件不被探针额外吞掉 | 快照可明确归属；没有“找不到就冒认另一个 HUD” |
| A0.2 焦点资格；`GuardianForm.cs`、`WindowManager.cs`、既有 focus gate | NULL 看门狗保留观察，撤去无资格的自主恢复；普通恢复核查实际内部焦点；共享已有会话内交接保护 | 根正确但内部焦点错误、外部/NULL 前台、锁屏、最小化、迟到 panel 回调；无资格时零恢复调用 | 成功谓词准确，合法关闭交接不退化；尚不宣称现场根治 |
| A0.3 桥边界；`WorldCompositionSurface.cs`、`NativePointerBridge.cs`、`InputBridge.h/.cpp`、`InputBroker.cpp` | 两端配套版本；有限队列；接收端可即时看到撤销代次；Post 返回与接收证据分级；失败撤销手势 | 旧 Down 已入目标队列后撤销；取消消息排在旧 Down 后；Post 失败/队列满；目标重建；旧 Up；几何变化 | 旧包不能 SetFocus/执行；桥及 Host 配对构建，未确认状态不再称 delivered |

代码根为 `launcher/src/`，原生桥根为 `launcher/native/world-compositor/`。新桥协议由一个 SWE2 lane 贯通 C# 与 C++，不能把两端分给无共享冻结接口的不同作者。

桥撤销不能只作为一条排在旧包后面的取消消息。可复用现有共享映射或其他最小机制，但必须用反例证明接收端在处理旧包前已经看见撤销；共享布局、版本、内存可见性、溢出策略和退出恢复同批验证。不把普通日志当作 ACK，不盲重放失败输入。

A0 同时分离捕获“曾经 ready”和当前推进证据，先观测停帧/暂停/阻塞差异；不新增自动重启游戏或重复抢焦策略。钩子线程迁移若显著扩大范围，先记录受控阻塞结果，交主控决定是否进入后续输入承载替换。

复用测试：`FocusTraceTests`、`FocusTracePhysicalHistoryTests`、`NativeHudInputRoutingTests`、`WindowManagerFocusRestoreTests`、`PanelFocusRestoreGateTests`、`KeyboardHookStateTests`、`WorldCompositorTests`。新增只测反例与不变量，不写实现镜像测试。

## 5. 首批 C0：源／输出隔离的决定性实验

候选拓扑：S 保留现役 Guardian/Flash 来源角色；F 是独立 Flash 进程中嵌入 S 的实际窗口；P 是不同顶层根的玩家呈现窗口，候选 `P.Owner=S`，不是 S 的 `WS_CHILD`。这是实验假设，不是所有系统都已验证的宿主合同。

优先复用 `launcher/perf/flash-compositor/` 小样、`WorldCompositorController`、`NativeCompositorSession`、原生 `Compositor.cpp`。先做隔离实验，再改生产装配。不要在这一阶段搬完 HUD 或替换 Web 后端。

| 实验 | 方法与对照 | 通过条件 |
|---|---|---|
| C0.1 对象隔离 | 记录 S/P/F 的路径、PID/TID、句柄生命周期、父子/owner关系、实际 capture HWND | P 输出不处在选定源捕获子树；不是只比较窗口标题 |
| C0.2 自捕获负控 | 仅 P 显示变化标记，检查原始源帧；另设故意捕获输出的错误配置 | 正确配置看不到标记，错误配置能被检测；诊断像素读回不进入生产每帧路径 |
| C0.3 源活性与几何 | 真 Flash 运行：正常、完全覆盖、部分遮挡、移动、最小化恢复；可用设备上测跨屏/DPI/负坐标 | 应推进时确有新源内容；暂停/静态另计；重复 Present 旧纹理不算源活性；恢复后几何匹配 |
| C0.4 玩家入口与焦点 | 检查任务栏、Alt-Tab、关闭/恢复、真实 Flash 按键；记录操作驱动是否主动激活 | 无第二裸源入口/闪现、无互相抢焦；未具备物理设备的项目明确未测，不冒充全兼容 |

实验必须能够失败。仅 WS_VISIBLE、捕获创建成功、最后一帧可见均不足；不以 `WDA_EXCLUDEFROMCAPTURE` 默认绕过自捕获或破坏正常录屏。当前 HWND 捕获 API 的 Windows 版本要求按官方 API 核对，不能沿用 WGC 大类的较低版本数字。

**阶段出口**：形成最小可运行候选和 C0 结论（成立、明确失败或部分未知），携带实际身份、原始指标及限制，交 Pro 节点 R1。C0 的关键条件失败时不批量迁移；保留现役完整版本，比较 A0＋B 收缩路线，不自动启动替换 Flash 宿主。

## 6. C1 与 B：条件性任务，不提前锁死实现

C0 成立后，主控结合 R1 复核实际接口，再细化以下任务；保留同一本文，不另建竞争施工计划。

### C1：一个真实 Web 面板＋两个 Native 代表样本

- 用 `surgery` 姓名草稿与取消，真实 opener→页面→关闭→世界；不得另造相似 demo。首次接手检查 [lazy 注册](../launcher/web/modules/panels-lazy-registry.js)、`plastic-surgery.js`、`plastic-surgery-runtime.js`、共享外观预览、对应 CSS 与现役 Host task，复用业务处理器。
- CompositionController 适配 visual、鼠标/滚轮、capture、光标、真实 OS 焦点、Tab 与中文 IME、父窗口/DPI变化、故障恢复和所需可访问性。SDK 版本不代签现场 Runtime；运行时能力拒绝要有可识别原因。
- 合成器面板态显示与 AS2 原暂停责任分开处理；不顺带重写暂停协议。
- 带入一个现役 Native 交互目标和一块现役 PlayerInfo 分块缓存，证明统一命中与 GPU 承载，而非只有世界＋网页。
- 先用无持久写目标验证路由；复活/返回业务纵切只在用户授权的测试范围与存档恢复合同内执行。首轮 surgery 不点付费应用。
- Web 捕获丢失/取消不能默认通过补 MouseUp 实现；必须证明页面不会因此提交。旧、新端点不得同时消费同一手势。

**出口 R2**：真实文本输入/取消/返回、Flash 按键释放、Native 样本和缓存成本成立；Pro 对接口稳定性与失败反例复核后，才扩大 B。缺某能力时明确停在 C1，或保持现役 Web 后端先做 B，不能称 Web 已统一。

### B：批量适配与退休

1. 按 controller/绘制资产/窗口外壳拆分职责，逐层接入主 HUD、对白、资源、bottom/buff，再接伤害数字与光标。
2. 保留 PlayerInfo 字形、蒙版、图标及光栅缓存；GPU 常驻资源只上传脏块，几何移动优先改变换。
3. 每迁一个端点，只启用一条输入/显示后端；其取消、模态和业务身份与原行为对照。
4. 逐个退休旧 Form、InputShield/CDP 路由及自主恢复回调，核运行窗口清单和重开资源释放，不只删除源码。
5. 粒子/天空只保留排序、世界/屏幕坐标、调色域和生命周期接点；不为未来透明度预造深度图或另一个引擎。

## 7. Kimi 主控与 SWE2 的执行分工

| 角色 | 可以独立承担 | 必须汇回主控 |
|---|---|---|
| K3 主控 | 基线核对、冻结接口、授权范围、集成顺序、结果归因、本文续接 | 产品取舍/范围变化交用户；架构反例进入 Pro 节点 |
| SWE2 诊断 lane | A0.1 与对应测试，限定诊断文件 | 多 HUD 注册/注销结果、错误线程与失效实例反例 |
| SWE2 焦点 lane | A0.2，限定 Guardian/WindowManager/gate | 合法与非法资格矩阵、实际焦点验证；不得兼改 C0 装配 |
| SWE2 桥 lane | A0.3 两端、broker 与配套测试 | ABI/版本/共享布局、撤销可见性与失败处理 |
| SWE2 实验 lane | C0 隔离小样、负控与观测工具 | 可复现刺激、源帧和输出的区别、失败条件 |
| SWE2 审阅 lane | 冻结 diff 的独立反例审阅，不写同一实现 | 指出不成立的假设和缺失验证；票数不能代签结论 |

这些是工作归属，不要求同时开满五个任务。建议最多两个互不重叠的实现 lane 加一个只读审阅；更多并行由真实瓶颈决定。`Program.cs`、`WebOverlayForm.cs`、公共接口和本计划由主控统一集成。A0.2 与 C0 修改相同文件时必须串行。

使用 worktree 前检查磁盘和已有 worktree，遵守[贡献流程](contribution-workflow.md)；Windows 采用短路径。禁止多个 lane 共写主工作树、同时编译共享输出或操作同一个桌面/CS6/游戏进程。小型只读审阅不为形式隔离创建整仓 worktree。任务完成按既有流程收尾，不删除他人未提交内容。

Kimi 每个工单至少提供：输入基线、可写文件、不可改边界、行为不变量、测试入口、明确停止条件。SWE2 回报提供：变更路径、实际执行命令/结果、未测项、最小反例、产物位置。没有导出正文或只退出 0 不算完成。

## 8. 验证、桌面工具与人类分工

构建/验证命令复用已有入口，承接时先读脚本参数，不能猜选项：

- Host：`launcher/tests/run_tests.ps1`；先命中 focused，阶段候选运行前完成既有适用全量要求。
- GPU/捕获小样：`launcher/perf/flash-compositor/build.ps1`、`run.ps1`、`test.ps1`。
- 配套开发候选：同目录 `build-game.ps1`、`run-game.ps1`，输出/身份以 README 为准。不拷候选到正式 runtime。
- 改 AS2 才走真实 CS6 对应目标与 focused suite；不因文档或纯 C# 改动重编 SWF。
- 文档：`node tools/validate-doc-governance.js`、`git diff --check`。

**自动化负责**：构建、纯逻辑、故障注入、有限队列/代次、窗口身份、桥/帧/业务证据分层、日志采集、性能统计与差异核对。观察器不能注入业务成功；injected 输入、直接 SendMessage、物理手势分别标注。

**桌面工具负责**：可追溯的进入场景、有限动作、截图/录屏。必须记录其是否调用 Raise/SetForegroundWindow；故障观察区间不能每次点击前自动激活后宣称“第一击正常”。不要用远程控制自带的焦点修复掩盖问题。Kimi 没有桌面工具时，不把 shell 注入伪装成真实点击，可交人类操作同一测试脚本。

**人类负责三组短体验**（候选和采集已准备好后一次安排，不承担机器判读）：

1. 按住移动键切出，在外部释放，再返回：报告是否卡键、首击表现；记录确切动作和约略时刻。
2. 真实 surgery 页面输入中文、选候选、取消并返回世界：不付费应用；报告输入位置/焦点/取消是否自然。
3. 最小化恢复、窗口移动/缩放及真实战斗换档：观察闪烁、裸源、错位和操作跟手。无第二显示器时不能写已通过跨屏。

由 Agent 自动收集同会话身份、日志和输入/呈现状态，再与人类描述关联。体感“不顺”不得被单测绿覆盖；人类“看起来正常”也不证明未知写、全部键位或长时兼容。真实存档/付费/复活测试遵守本次具体授权，旧天气任务的现有槽位许可不自动继承给所有实验。

## 9. GPT-6 Pro 纠偏节点

R0 路线裁决已返回，原文见归档。不要每个补丁重新开题，也不要以静态裁决代替本机验证。

| 节点 | 何时触发 | 裁决问题 |
|---|---|---|
| R1 | C0 完成或出现决定性阻塞 | S/P/F 是否成立？能否进入 C1？失败时是否收缩 A0＋B？ |
| R2 | C1 形成可玩的真实纵切或 Web/IME 受阻 | 输入/呈现接口是否稳定？Native 适配是否避免退化？能否开始 B？ |
| Rx | 原假设被反例推翻、需扩大权限/重写共享协议/更换宿主 | 局部修补、收缩或改路线；不为普通编译错误调用 Pro |

每次输入只含：本次要决定的一句话、冻结 commit/diff、对应完整函数、候选实际身份、刺激和原始结果、失败负控、人工反馈、未测项、2–3 个候选处理。先脱敏，不上传凭据或真实存档全文；不再机械发送整仓或上次所有日志。

建议提交提示词：

> 请基于本阶段冻结代码和实际实验裁决是否进入下一阶段。逐项反驳成功声明，区分发送/接收/业务与源帧/呈现；指出负控是否有效、哪些缺口会改变路线。只给当前节点必要的修改，不重开全部架构。若材料不足，明确缺失证据，禁止补造实测。给出继续、有限修补或收缩路线及其条件。

回执原文归 `docs/裁决存档/`，本地主控回查后在本文更新采纳/收窄/驳回。用户已授权范围内的修补继续推进；新产品取舍、扩大范围、正式发布或凭据操作按已有合同处理，不为普通 revision 变化反复索要许可。ChatGPT 页面提交和费用范围以用户具体授权为准，不能因计划写了 Pro 节点就自动消费其他付费渠道。

## 10. 停止、恢复与完成口径

以下为实际停止相关分支的条件：捕获只能依赖未经证明的隐藏/离屏源；负控不能识别反馈；旧包跨代次 SetFocus/执行；Web 取消产生提交；出现新旧双输入；业务写被自动重放；图形恢复解除未知写锁；为合成复制业务权威。停止后保留最小反例和最后可运行配套候选，其他独立已授权工作可继续。

C0 失败不自动启动 E；C1 失败可保留现役 Web 后端研究 B；Native 性能不合格先调整分块/GPU承载而非重写全部美术资产。阶段完成必须列出实际覆盖，不用静态检查、注入输入或发布完整性代签真实现场。

最终代码提交、push、云端构建、promotion、部署和标准入口复验各按实际用户授权执行。中文提交说明写清阶段、玩家是否需要回归及未覆盖项。文档规划提交不触发 runtime 发布，也没有运行行为变更。

## 11. 续接记录（当前唯一状态）

| 阶段 | 当前状态 | 下一动作 |
|---|---|---|
| R0 / 规划 | Pro 回执已归档；本地主控形成上述采纳边界 | Kimi 收到施工指令后核对基线和工具 |
| A0.1 / A0.2 / A0.3 | 未开工，未运行本计划测试 | 首先 A0.1，按共享文件边界排后续 lane |
| C0 | 未开工，S/P/F 待实测 | 准备隔离小样与有效负控，不先迁移全部窗口 |
| C1 / B | 条件性后续，未开工 | 等前阶段结果，不抢跑批量迁移 |
| 人类测试 | 未执行 | 候选及自动采集就绪后安排短体验 |
| 后续发布 | 无本计划实现的候选或发布 | 依用户届时具体授权进入原发布合同 |

自然收口时在本节原位更新：实际 source/candidate 身份、完成范围、失败或未知项、下一动作及证据链接；机器大日志留本机临时目录，必要结论与相对仓库源码指针进入 Git。不要另建每日回执制度，不把供应商配额数写成长期事实。
